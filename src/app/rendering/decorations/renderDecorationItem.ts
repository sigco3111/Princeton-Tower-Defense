// Princeton Tower Defense - Decoration Item Rendering
// Contains all decoration rendering switch cases for various decoration types
//
// ──────────────────────────────────────────────────────────────────────
// MIGRATION PLAN — switch/case → registry-based dispatch
// ──────────────────────────────────────────────────────────────────────
// This file contains a ~33k-line switch statement with ~100 case branches.
// We are incrementally migrating each case into its own renderer function
// registered via `decorationRegistry.ts`.
//
// How to migrate a case:
//   1. Create a new file (or add to an existing group file) under
//      `src/app/rendering/decorations/renderers/`.
//   2. Export a function matching `DecorationRenderFn` from the registry.
//   3. Call `registerDecorationRenderer("<type>", yourFn)` at module scope.
//   4. Delete the corresponding case from the switch below.
//
// The dispatcher in `renderDecorationItem` checks the registry first;
// if a renderer is found it is called and the switch is skipped entirely.
// This lets us move cases one-at-a-time without breaking anything.
// ──────────────────────────────────────────────────────────────────────

import { ISO_COS, ISO_SIN, ISO_Y_RATIO } from "../../constants";
import type { DecorationType, Position } from "../../types";
import {
  drawIsometricPrism,
  drawIsometricPyramid,
  drawIsometricCrystalSpire,
  drawBrickFace as sharedBrickFace,
  drawOrganicBlobAt,
} from "../helpers";
import {
  drawIsoGothicWindow,
  drawIsoFlushDoor,
  traceIsoFlushRect,
} from "../isoFlush";
import { setShadowBlur, clearShadow } from "../performance";
import { drawBench } from "./benchShapes";
import {
  drawCarnegieLakeBoathouse,
  drawCarnegieLakePavilion,
  drawCarnegieLakeClockTower,
  drawCarnegieLakeRowboat,
} from "./carnegieLakeBuildings";
import {
  CHALLENGE_LANDMARK_TYPES,
  renderChallengeLandmark,
} from "./challengeLandmarks";
import { getDecorationRenderer } from "./decorationRegistry";
import {
  drawTree,
  drawBush,
  drawHedge,
  drawPine,
  drawCharredTree,
  drawSwampTree,
} from "./foliageShapes";
import { renderIdolStatue } from "./idolStatueRenderer";
import {
  renderFortress,
  renderSkullThrone,
  renderWarMonument,
  renderBoneAltar,
  renderSunObelisk,
  renderFrostCitadel,
  renderInfernalGate,
  renderNassauHall,
} from "./landmarkBuildings";
import {
  renderPrincetonChapel,
  renderFirestoneLibrary,
  renderBlairArch,
  renderWhigHall,
  renderEastPyne,
  renderProspectHouse,
  renderClioHall,
  renderMcCoshHall,
  renderRobertsonHall,
  renderHolderHall,
  renderClevelandTower,
  renderAlexanderHall,
  renderFineHall,
  renderFoulkeHall,
  renderTigerStadium,
} from "./princetonBuildings";
import { getRockConfig } from "./rockPalettes";
import { drawBoulderRock, drawSlabRock, drawSpireRock } from "./rockShapes";
import { drawRuinsRegionalOverlay } from "./ruinsRegionalOverlay";
import { drawDirectionalShadow } from "./shadowHelpers";
import { drawTentacle } from "./tentacleShapes";
import { drawTent } from "./tentShapes";
import {
  drawBrokenWallDecoration,
  drawIsoJaggedWall,
  drawRubblePile,
} from "./wallShapes";

const LANDMARK_TYPES_WITHOUT_AUTO_GROUND_SHADOW = new Set<DecorationType>([
  "pyramid",
  "sphinx",
  "giant_sphinx",
  "nassau_hall",
  "princeton_chapel",
  "firestone_library",
  "blair_arch",
  "whig_hall",
  "east_pyne",
  "prospect_house",
  "clio_hall",
  "mccosh_hall",
  "robertson_hall",
  "holder_hall",
  "cleveland_tower",
  "alexander_hall",
  "fine_hall",
  "foulke_hall",
  "tiger_stadium",
  "glacier",
  "fortress",
  "ice_throne",
  "obsidian_castle",
  "witch_cottage",
  "cannon_crest",
  "ivy_crossroads",
  "blight_basin",
  "triad_keep",
  "sunscorch_labyrinth",
  "frist_outpost",
  "ashen_spiral",
  "war_monument",
  "bone_altar",
  "sun_obelisk",
  "frost_citadel",
  "infernal_gate",
]);

export interface DecorationRenderParams {
  ctx: CanvasRenderingContext2D;
  screenPos: Position;
  scale: number;
  type: DecorationType;
  rotation: number;
  variant: number;
  decorTime: number;
  decorX: number; // Original decoration x position for seeding
  decorY: number; // Original decoration y position for seeding
  selectedMap: string;
  mapTheme?: string;
  // Optional split-pass controls used for large landmarks with ground shadows.
  shadowOnly?: boolean;
  skipShadow?: boolean;
  zoom?: number;
}

/**
 * Draws an organic blob shape instead of a perfect ellipse.
 * Uses multi-frequency sine wave noise (matching hazard drawOrganicBlob pattern)
 * to create natural-looking edges for water features.
 */
function drawOrganicWaterShape(
  ctx: CanvasRenderingContext2D,
  cx: number,
  cy: number,
  rx: number,
  ry: number,
  seed: number,
  bumpiness: number = 0.12
): void {
  ctx.beginPath();
  const pts = 24;
  for (let i = 0; i <= pts; i++) {
    const ang = (i / pts) * Math.PI * 2;
    const n1 = Math.sin(ang * 3 + seed) * bumpiness;
    const n2 = Math.sin(ang * 5 + seed * 2.3) * bumpiness * 0.5;
    const n3 = Math.sin(ang * 7 + seed * 4.1) * bumpiness * 0.25;
    const variation = 1 + n1 + n2 + n3;
    const x = cx + Math.cos(ang) * rx * variation;
    const y = cy + Math.sin(ang) * ry * variation;
    if (i === 0) {
      ctx.moveTo(x, y);
    } else {
      ctx.lineTo(x, y);
    }
  }
  ctx.closePath();
}

const drawBrickFace = sharedBrickFace;

/**
 * Renders a single decoration item based on its type.
 * This function contains all the rendering logic for every decoration type in the game.
 */
export function renderDecorationItem(params: DecorationRenderParams): void {
  const {
    ctx,
    screenPos,
    scale: s,
    type,
    rotation,
    variant,
    decorTime,
    decorX,
    decorY,
    selectedMap,
    mapTheme = "",
    shadowOnly = false,
    skipShadow = false,
    zoom: paramZoom,
  } = params;
  const zoom = paramZoom ?? 1;
  const landmarkSkipShadow =
    skipShadow || LANDMARK_TYPES_WITHOUT_AUTO_GROUND_SHADOW.has(type);

  const dec = { x: decorX, y: decorY };

  if (CHALLENGE_LANDMARK_TYPES.has(type)) {
    renderChallengeLandmark({
      ctx,
      decorTime,
      decorX,
      decorY,
      scale: s,
      screenPos,
      shadowOnly,
      skipShadow: landmarkSkipShadow,
      type,
      zoom,
    });
    return;
  }

  const registeredRenderer = getDecorationRenderer(type);
  if (registeredRenderer) {
    registeredRenderer(params);
    return;
  }

  switch (type) {
    // === GRASSLAND DECORATIONS ===
    case "tree": {
      drawTree(ctx, screenPos.x, screenPos.y, s, variant, decorX, decorY);
      break;
    }
    case "rock": {
      const rockConfig = getRockConfig(mapTheme);
      const paletteIdx = variant % 3;
      const shapeIdx = variant % 3;
      const pal = rockConfig.palettes[paletteIdx];
      const drawFn = [drawBoulderRock, drawSlabRock, drawSpireRock][shapeIdx];
      drawFn(ctx, screenPos.x, screenPos.y, s, pal, rockConfig, decorTime);
      break;
    }
    case "nassau_hall":
    case "princeton_chapel":
    case "firestone_library":
    case "blair_arch":
    case "whig_hall":
    case "east_pyne":
    case "prospect_house":
    case "clio_hall":
    case "mccosh_hall":
    case "robertson_hall":
    case "holder_hall":
    case "cleveland_tower":
    case "alexander_hall":
    case "fine_hall":
    case "foulke_hall":
    case "tiger_stadium": {
      const landmarkP = {
        ctx,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        seedX: decorX,
        seedY: decorY,
        shadowOnly,
        skipShadow: landmarkSkipShadow,
        time: decorTime,
        variant,
        zoom,
      };
      const princetonRenderers: Record<string, (p: typeof landmarkP) => void> =
        {
          alexander_hall: renderAlexanderHall,
          blair_arch: renderBlairArch,
          cleveland_tower: renderClevelandTower,
          clio_hall: renderClioHall,
          east_pyne: renderEastPyne,
          fine_hall: renderFineHall,
          firestone_library: renderFirestoneLibrary,
          foulke_hall: renderFoulkeHall,
          holder_hall: renderHolderHall,
          mccosh_hall: renderMcCoshHall,
          nassau_hall: renderNassauHall,
          princeton_chapel: renderPrincetonChapel,
          prospect_house: renderProspectHouse,
          robertson_hall: renderRobertsonHall,
          tiger_stadium: renderTigerStadium,
          whig_hall: renderWhigHall,
        };
      princetonRenderers[type]?.(landmarkP);
      break;
    }
    case "bush": {
      drawBush(ctx, screenPos.x, screenPos.y, s, variant, decorX, decorY);
      break;
    }
    case "grass": {
      // Enhanced 3D grass tuft with varied blade heights and colors
      const grassPalettes = [
        { base: "#4a5d23", dark: "#3a4d13", mid: "#5a6d33", tip: "#7a8d53" },
        { base: "#3d4f1c", dark: "#2d3f0c", mid: "#4d5f2c", tip: "#6d7f4c" },
        { base: "#556b2f", dark: "#45511f", mid: "#657b3f", tip: "#859b5f" },
        { base: "#6b8e23", dark: "#5b7e13", mid: "#7b9e33", tip: "#9bbe53" },
      ];
      const gp = grassPalettes[variant % 4];

      // Subtle ground shadow
      ctx.fillStyle = "rgba(0,0,0,0.08)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 1 * s,
        10 * s,
        3 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Draw multiple grass blades with varying properties
      const blades = [
        { h: 16, sway: 0, w: 1.8, x: -6 },
        { h: 20, sway: 0.3, w: 2.2, x: -3 },
        { h: 22, sway: 0.5, w: 2.5, x: 0 },
        { h: 18, sway: 0.7, w: 2, x: 3 },
        { h: 14, sway: 1, w: 1.6, x: 6 },
        { h: 12, sway: 0.2, w: 1.4, x: -4 },
        { h: 11, sway: 0.8, w: 1.3, x: 4 },
        { h: 15, sway: 0.4, w: 1.7, x: -1 },
        { h: 13, sway: 0.6, w: 1.5, x: 2 },
      ];

      blades.forEach((blade, idx) => {
        const gx = screenPos.x + blade.x * s;
        const sway =
          Math.sin(decorTime * 2 + blade.sway * 3 + dec.x * 0.1) *
          (2 + blade.h * 0.1) *
          s;
        const tipSway = sway * 1.5;

        // Gradient from base to tip
        const bladeGrad = ctx.createLinearGradient(
          gx,
          screenPos.y,
          gx + tipSway,
          screenPos.y - blade.h * s
        );
        bladeGrad.addColorStop(0, gp.dark);
        bladeGrad.addColorStop(0.3, gp.base);
        bladeGrad.addColorStop(0.7, gp.mid);
        bladeGrad.addColorStop(1, gp.tip);

        // Draw blade as filled shape for better appearance
        ctx.fillStyle = bladeGrad;
        ctx.beginPath();
        ctx.moveTo(gx - blade.w * 0.5 * s, screenPos.y);
        ctx.quadraticCurveTo(
          gx + sway * 0.3 - blade.w * 0.3 * s,
          screenPos.y - blade.h * 0.5 * s,
          gx + tipSway,
          screenPos.y - blade.h * s
        );
        ctx.quadraticCurveTo(
          gx + sway * 0.3 + blade.w * 0.3 * s,
          screenPos.y - blade.h * 0.5 * s,
          gx + blade.w * 0.5 * s,
          screenPos.y
        );
        ctx.closePath();
        ctx.fill();

        // Subtle highlight on some blades
        if (idx % 3 === 0) {
          ctx.strokeStyle = "rgba(255,255,255,0.1)";
          ctx.lineWidth = 0.5 * s;
          ctx.beginPath();
          ctx.moveTo(gx, screenPos.y - 2 * s);
          ctx.quadraticCurveTo(
            gx + sway * 0.3,
            screenPos.y - blade.h * 0.5 * s,
            gx + tipSway * 0.8,
            screenPos.y - blade.h * 0.9 * s
          );
          ctx.stroke();
        }
      });

      // Add a few seed heads on taller blades
      if (variant === 1 || variant === 3) {
        ctx.fillStyle = "#a89078";
        const seedX =
          screenPos.x + Math.sin(decorTime * 2 + 0.5 + dec.x * 0.1) * 3.5 * s;
        const seedY = screenPos.y - 21 * s;
        ctx.beginPath();
        ctx.ellipse(seedX, seedY, 1.5 * s, 2.5 * s, 0.3, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }
    case "crater":
    case "battle_crater":
    case "fire_pit": {
      // Subtle, environment-blending crater/pit - natural depression in terrain
      const isBattle = type === "battle_crater";
      const isFirePit = type === "fire_pit";
      const safeVariant = (variant ?? 0) % 4;

      // Variant-based size variations
      const craterVariants = [
        { debris: 3, depth: 3, outerRx: 24, outerRy: 12 },
        { debris: 2, depth: 4, outerRx: 16, outerRy: 8 },
        { debris: 4, depth: 2, outerRx: 32, outerRy: 16 },
        { debris: 3, depth: 3.5, outerRx: 28, outerRy: 10 },
      ];
      const cv = craterVariants[safeVariant];

      const craterX = screenPos.x;
      const craterY = screenPos.y;

      // Very subtle outer shadow - barely visible depression
      const outerShadow = ctx.createRadialGradient(
        craterX,
        craterY,
        cv.outerRx * 0.3 * s,
        craterX,
        craterY,
        (cv.outerRx + 4) * s
      );
      outerShadow.addColorStop(0, "rgba(40,35,25,0.12)");
      outerShadow.addColorStop(0.7, "rgba(35,30,20,0.06)");
      outerShadow.addColorStop(1, "rgba(30,25,15,0)");
      ctx.fillStyle = outerShadow;
      ctx.beginPath();
      ctx.ellipse(
        craterX,
        craterY,
        (cv.outerRx + 4) * s,
        (cv.outerRy + 2) * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Rim - very subtle earthy tone, blends with ground
      const rimGrad = ctx.createRadialGradient(
        craterX,
        craterY - 1 * s,
        cv.outerRx * 0.5 * s,
        craterX,
        craterY,
        cv.outerRx * s
      );
      rimGrad.addColorStop(0, "rgba(70,60,45,0.25)");
      rimGrad.addColorStop(0.6, "rgba(60,50,35,0.18)");
      rimGrad.addColorStop(1, "rgba(50,45,30,0.08)");
      ctx.fillStyle = rimGrad;
      ctx.beginPath();
      ctx.ellipse(
        craterX,
        craterY,
        cv.outerRx * s,
        cv.outerRy * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Inner depression - darker but still subtle
      const innerGrad = ctx.createRadialGradient(
        craterX,
        craterY + cv.depth * 0.3 * s,
        0,
        craterX,
        craterY + cv.depth * 0.3 * s,
        (cv.outerRx - 3) * s
      );
      innerGrad.addColorStop(0, "rgba(25,20,12,0.35)");
      innerGrad.addColorStop(0.5, "rgba(35,28,18,0.25)");
      innerGrad.addColorStop(1, "rgba(45,38,25,0.12)");
      ctx.fillStyle = innerGrad;
      ctx.beginPath();
      ctx.ellipse(
        craterX,
        craterY + 1 * s,
        (cv.outerRx - 2) * s,
        (cv.outerRy - 1) * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Subtle depth shading on back edge
      ctx.fillStyle = "rgba(20,15,8,0.15)";
      ctx.beginPath();
      ctx.ellipse(
        craterX,
        craterY - 1 * s,
        (cv.outerRx - 4) * s,
        (cv.outerRy - 2) * s,
        0,
        Math.PI * 1.1,
        Math.PI * 1.9
      );
      ctx.fill();

      // A few subtle dirt clumps around edge
      ctx.fillStyle = "rgba(65,55,40,0.2)";
      for (let d = 0; d < cv.debris; d++) {
        const debrisAngle = (d / cv.debris) * Math.PI * 2 + safeVariant * 0.8;
        const debrisDist =
          cv.outerRx * 0.9 + Math.sin(d * 2.1 + safeVariant) * 2;
        const debrisX = craterX + Math.cos(debrisAngle) * debrisDist * s;
        const debrisY =
          craterY + Math.sin(debrisAngle) * debrisDist * ISO_Y_RATIO * s;
        ctx.beginPath();
        ctx.ellipse(
          debrisX,
          debrisY,
          1.5 * s,
          0.8 * s,
          debrisAngle * 0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Battle crater - subtle scorch marks
      if (isBattle) {
        ctx.strokeStyle = "rgba(35,25,15,0.15)";
        ctx.lineWidth = 1.5 * s;
        for (let sc = 0; sc < 3; sc++) {
          const scorchAngle = sc * 2 + safeVariant * 0.5;
          const scorchStart = cv.outerRx * 0.7;
          const scorchEnd = cv.outerRx + 3;
          ctx.beginPath();
          ctx.moveTo(
            craterX + Math.cos(scorchAngle) * scorchStart * s,
            craterY + Math.sin(scorchAngle) * scorchStart * ISO_Y_RATIO * s
          );
          ctx.lineTo(
            craterX + Math.cos(scorchAngle) * scorchEnd * s,
            craterY + Math.sin(scorchAngle) * scorchEnd * ISO_Y_RATIO * s
          );
          ctx.stroke();
        }
      }

      // Fire pit - subtle warm glow
      if (isFirePit) {
        const firePulse = 0.2 + Math.sin(decorTime * 3) * 0.08;
        const fireGlow = ctx.createRadialGradient(
          craterX,
          craterY + cv.depth * 0.2 * s,
          0,
          craterX,
          craterY + cv.depth * 0.2 * s,
          (cv.outerRx - 4) * s
        );
        fireGlow.addColorStop(0, `rgba(180,80,20,${firePulse})`);
        fireGlow.addColorStop(0.5, `rgba(120,40,10,${firePulse * 0.4})`);
        fireGlow.addColorStop(1, "rgba(60,20,5,0)");
        ctx.fillStyle = fireGlow;
        ctx.beginPath();
        ctx.ellipse(
          craterX,
          craterY + cv.depth * 0.2 * s,
          (cv.outerRx - 4) * s,
          (cv.outerRy - 2) * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Tiny ember particles
        for (let e = 0; e < 2; e++) {
          const emberPhase = (decorTime * 1.2 + e * 1.2 + safeVariant) % 2;
          const emberAlpha =
            (emberPhase < 1 ? emberPhase : 2 - emberPhase) * 0.5;
          const emberRise = emberPhase * 5 * s;
          const emberX = craterX + Math.sin(e * 3 + decorTime) * 4 * s;
          const emberY = craterY - emberRise;

          ctx.fillStyle = `rgba(255,150,50,${emberAlpha})`;
          ctx.beginPath();
          ctx.arc(emberX, emberY, 1 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      break;
    }
    case "debris": {
      ctx.fillStyle = "#4a3525";
      for (let d = 0; d < 5; d++) {
        const angle = rotation + d * 1.2;
        const dist = (8 + variant * 3) * s;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + Math.cos(angle) * dist,
          screenPos.y + Math.sin(angle) * dist * ISO_Y_RATIO,
          6 * s,
          4 * s,
          angle,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      ctx.fillStyle = "#6d4c41";
      ctx.save();
      ctx.translate(screenPos.x, screenPos.y);
      ctx.rotate(rotation);
      ctx.fillRect(-15 * s, -3 * s, 30 * s, 4 * s);
      ctx.fillRect(-8 * s, -12 * s, 4 * s, 20 * s);
      ctx.restore();
      break;
    }
    case "cart": {
      const ctX = screenPos.x;
      const ctY = screenPos.y;
      const ctV = variant % 3;
      const ctD = 5 * s;

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        ctX,
        ctY + 3 * s,
        s,
        22 * s,
        10 * s,
        25 * s,
        0.28
      );

      // Isometric cart aligned to ground plane
      const woodDark = "#3e2723";
      const woodMid = "#5d4037";
      const woodLit = "#795548";
      const woodHi = "#8d6e63";
      const ironDark = "#37474f";
      const ironMid = "#546e7a";

      // === WHEELS (back, drawn first) ===
      const drawWheel = (wx: number, wy: number, wRad: number) => {
        // Tire rim (iron)
        ctx.strokeStyle = ironDark;
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.ellipse(wx, wy, wRad, wRad * 0.45, 0, 0, Math.PI * 2);
        ctx.stroke();

        // Rim highlight
        ctx.strokeStyle = ironMid;
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.ellipse(wx, wy, wRad, wRad * 0.45, 0, Math.PI * 1.1, Math.PI * 1.8);
        ctx.stroke();

        // Hub
        const hubG = ctx.createRadialGradient(wx, wy, 0, wx, wy, 2.5 * s);
        hubG.addColorStop(0, woodHi);
        hubG.addColorStop(0.6, woodMid);
        hubG.addColorStop(1, woodDark);
        ctx.fillStyle = hubG;
        ctx.beginPath();
        ctx.ellipse(wx, wy, 2.5 * s, 1.2 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Spokes
        ctx.strokeStyle = woodMid;
        ctx.lineWidth = 0.8 * s;
        for (let sp = 0; sp < 6; sp++) {
          const spAng = (sp / 6) * Math.PI * 2 + decorX * 0.5;
          ctx.beginPath();
          ctx.moveTo(wx, wy);
          ctx.lineTo(
            wx + Math.cos(spAng) * (wRad - 1 * s),
            wy + Math.sin(spAng) * (wRad - 1 * s) * 0.45
          );
          ctx.stroke();
        }

        // Iron hub cap
        ctx.fillStyle = ironDark;
        ctx.beginPath();
        ctx.ellipse(wx, wy, 1.5 * s, 0.7 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = ironMid;
        ctx.beginPath();
        ctx.ellipse(
          wx - 0.3 * s,
          wy - 0.2 * s,
          0.8 * s,
          0.4 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      };

      // Back wheels (partially hidden)
      drawWheel(ctX - 14 * s, ctY + 1 * s, 8 * s);
      drawWheel(ctX + 16 * s, ctY - 5 * s, 8 * s);

      // === CART BED — isometric box ===
      const bedL = 32 * s;
      const bedW = ctD;
      const bedH = 6 * s;
      const bedTop = ctY - 8 * s;

      // Bed bottom (visible due to iso angle)
      ctx.fillStyle = woodDark;
      ctx.beginPath();
      ctx.moveTo(ctX - bedL * 0.5, bedTop + bedH);
      ctx.lineTo(ctX + bedL * 0.5, bedTop + bedH - bedL * 0.25);
      ctx.lineTo(
        ctX + bedL * 0.5 + bedW,
        bedTop + bedH - bedL * 0.25 - bedW * 0.5
      );
      ctx.lineTo(ctX - bedL * 0.5 + bedW, bedTop + bedH - bedW * 0.5);
      ctx.closePath();
      ctx.fill();

      // Bed front-left face (lit)
      const bedFG = ctx.createLinearGradient(
        ctX - bedL * 0.5,
        bedTop,
        ctX + bedL * 0.5,
        bedTop
      );
      bedFG.addColorStop(0, woodMid);
      bedFG.addColorStop(0.5, woodLit);
      bedFG.addColorStop(1, woodMid);
      ctx.fillStyle = bedFG;
      ctx.beginPath();
      ctx.moveTo(ctX - bedL * 0.5, bedTop);
      ctx.lineTo(ctX + bedL * 0.5, bedTop - bedL * 0.25);
      ctx.lineTo(ctX + bedL * 0.5, bedTop + bedH - bedL * 0.25);
      ctx.lineTo(ctX - bedL * 0.5, bedTop + bedH);
      ctx.closePath();
      ctx.fill();

      // Bed front-right face (dark)
      ctx.fillStyle = woodDark;
      ctx.beginPath();
      ctx.moveTo(ctX + bedL * 0.5, bedTop - bedL * 0.25);
      ctx.lineTo(ctX + bedL * 0.5 + bedW, bedTop - bedL * 0.25 - bedW * 0.5);
      ctx.lineTo(
        ctX + bedL * 0.5 + bedW,
        bedTop + bedH - bedL * 0.25 - bedW * 0.5
      );
      ctx.lineTo(ctX + bedL * 0.5, bedTop + bedH - bedL * 0.25);
      ctx.closePath();
      ctx.fill();

      // Bed top face
      const bedTG = ctx.createLinearGradient(
        ctX - bedL * 0.5,
        bedTop,
        ctX + bedW,
        bedTop - bedW * 0.5
      );
      bedTG.addColorStop(0, woodLit);
      bedTG.addColorStop(0.5, woodHi);
      bedTG.addColorStop(1, woodMid);
      ctx.fillStyle = bedTG;
      ctx.beginPath();
      ctx.moveTo(ctX - bedL * 0.5, bedTop);
      ctx.lineTo(ctX - bedL * 0.5 + bedW, bedTop - bedW * 0.5);
      ctx.lineTo(ctX + bedL * 0.5 + bedW, bedTop - bedL * 0.25 - bedW * 0.5);
      ctx.lineTo(ctX + bedL * 0.5, bedTop - bedL * 0.25);
      ctx.closePath();
      ctx.fill();

      // Plank lines on top
      ctx.strokeStyle = "rgba(30,18,10,0.18)";
      ctx.lineWidth = 0.4 * s;
      for (let pl = 1; pl < 5; pl++) {
        const pt = pl / 5;
        const plX1 = ctX - bedL * 0.5 + bedW * pt;
        const plY1 = bedTop - bedW * 0.5 * pt;
        ctx.beginPath();
        ctx.moveTo(plX1, plY1);
        ctx.lineTo(plX1 + bedL, plY1 - bedL * 0.25);
        ctx.stroke();
      }

      // Side rails (raised edges)
      const railH = 8 * s;

      // Left rail
      ctx.fillStyle = woodMid;
      ctx.beginPath();
      ctx.moveTo(ctX - bedL * 0.5, bedTop);
      ctx.lineTo(ctX - bedL * 0.5, bedTop - railH);
      ctx.lineTo(ctX + bedL * 0.5, bedTop - bedL * 0.25 - railH);
      ctx.lineTo(ctX + bedL * 0.5, bedTop - bedL * 0.25);
      ctx.closePath();
      ctx.fill();

      // Rail plank separations
      ctx.strokeStyle = "rgba(30,18,10,0.22)";
      ctx.lineWidth = 0.5 * s;
      for (let rp = 1; rp < 4; rp++) {
        const rpT = rp / 4;
        const rpX = ctX - bedL * 0.5 + bedL * rpT;
        const rpY = bedTop - bedL * 0.25 * rpT;
        ctx.beginPath();
        ctx.moveTo(rpX, rpY);
        ctx.lineTo(rpX, rpY - railH);
        ctx.stroke();
      }

      // Right rail (back side)
      ctx.fillStyle = woodLit;
      ctx.beginPath();
      ctx.moveTo(ctX - bedL * 0.5 + bedW, bedTop - bedW * 0.5);
      ctx.lineTo(ctX - bedL * 0.5 + bedW, bedTop - bedW * 0.5 - railH);
      ctx.lineTo(
        ctX + bedL * 0.5 + bedW,
        bedTop - bedL * 0.25 - bedW * 0.5 - railH
      );
      ctx.lineTo(ctX + bedL * 0.5 + bedW, bedTop - bedL * 0.25 - bedW * 0.5);
      ctx.closePath();
      ctx.fill();

      // Rail top connecting edges
      ctx.fillStyle = woodHi;
      ctx.beginPath();
      ctx.moveTo(ctX - bedL * 0.5, bedTop - railH);
      ctx.lineTo(ctX - bedL * 0.5 + bedW, bedTop - bedW * 0.5 - railH);
      ctx.lineTo(
        ctX + bedL * 0.5 + bedW,
        bedTop - bedL * 0.25 - bedW * 0.5 - railH
      );
      ctx.lineTo(ctX + bedL * 0.5, bedTop - bedL * 0.25 - railH);
      ctx.closePath();
      ctx.fill();

      // Front wheels (in front of cart body)
      drawWheel(ctX - 12 * s, ctY + 5 * s, 8 * s);
      drawWheel(ctX + 18 * s, ctY - 1 * s, 8 * s);

      // Handles/shafts extending from front
      const handleEndX = ctX - bedL * 0.5 - 14 * s;
      const handleEndY = bedTop + bedH + 7 * s;

      ctx.strokeStyle = woodMid;
      ctx.lineWidth = 1.5 * s;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(ctX - bedL * 0.5, bedTop + 2 * s);
      ctx.lineTo(handleEndX, handleEndY);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(ctX - bedL * 0.5 + bedW, bedTop - bedW * 0.5 + 2 * s);
      ctx.lineTo(handleEndX + bedW, handleEndY - bedW * 0.5);
      ctx.stroke();

      // Handle grips
      ctx.fillStyle = "#2a1a10";
      ctx.beginPath();
      ctx.ellipse(handleEndX, handleEndY, 1.5 * s, 1 * s, 0.4, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(
        handleEndX + bedW,
        handleEndY - bedW * 0.5,
        1.5 * s,
        1 * s,
        0.4,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Variant cargo — different loads inside cart
      if (ctV === 0) {
        // Hay bales
        ctx.fillStyle = "#c8a860";
        ctx.beginPath();
        ctx.ellipse(ctX, bedTop - 3 * s, 8 * s, 4 * s, 0.2, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "#b89848";
        ctx.beginPath();
        ctx.ellipse(
          ctX + 6 * s,
          bedTop - 4 * s,
          6 * s,
          3 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.strokeStyle = "rgba(80,60,30,0.2)";
        ctx.lineWidth = 0.4 * s;
        for (let hl = 0; hl < 3; hl++) {
          ctx.beginPath();
          ctx.moveTo(ctX - 6 * s + hl * 4 * s, bedTop - 5 * s);
          ctx.lineTo(ctX - 5 * s + hl * 4 * s, bedTop - 1 * s);
          ctx.stroke();
        }
      } else if (ctV === 1) {
        // Barrels
        ctx.fillStyle = "#6d4c41";
        ctx.beginPath();
        ctx.ellipse(
          ctX - 3 * s,
          bedTop - 5 * s,
          5 * s,
          3 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "#5d4037";
        ctx.fillRect(ctX - 8 * s, bedTop - 9 * s, 10 * s, 8 * s);
        ctx.strokeStyle = "#3e2723";
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(ctX - 7 * s, bedTop - 6 * s);
        ctx.lineTo(ctX + 1 * s, bedTop - 6 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(ctX - 7 * s, bedTop - 3 * s);
        ctx.lineTo(ctX + 1 * s, bedTop - 3 * s);
        ctx.stroke();
      } else {
        // Sacks / cloth bundles
        ctx.fillStyle = "#a09070";
        ctx.beginPath();
        ctx.ellipse(
          ctX - 2 * s,
          bedTop - 3 * s,
          7 * s,
          4 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "#908060";
        ctx.beginPath();
        ctx.ellipse(
          ctX + 5 * s,
          bedTop - 5 * s,
          5 * s,
          3.5 * s,
          -0.15,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Tied tops
        ctx.strokeStyle = "#5a4a30";
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(ctX - 2 * s, bedTop - 6 * s);
        ctx.lineTo(ctX - 1 * s, bedTop - 8 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(ctX + 5 * s, bedTop - 7 * s);
        ctx.lineTo(ctX + 6 * s, bedTop - 9 * s);
        ctx.stroke();
      }
      break;
    }
    case "hut": {
      const cx = screenPos.x;
      const cy = screenPos.y;
      const v = variant % 4;

      // Shadow
      drawDirectionalShadow(
        ctx,
        cx,
        cy + 4 * s,
        s,
        28 * s,
        12 * s,
        40 * s,
        0.35
      );

      const hutRoof = (
        peakY: number,
        baseY: number,
        halfW: number,
        halfD: number,
        ov: number,
        dark: string,
        mid: string,
        lit: string
      ) => {
        const eIW = halfW + ov * ISO_COS;
        const eID = halfD + ov * ISO_SIN;
        const drop = 1 * s;

        const pX = cx,
          pY = peakY;
        const fX = cx,
          fY = baseY + eID + drop;
        const rX = cx + eIW,
          rY = baseY + drop;
        const bX = cx,
          bY = baseY - eID + drop;
        const lX = cx - eIW,
          lY = baseY + drop;

        // Back-right face (draw first, behind)
        ctx.fillStyle = mid;
        ctx.beginPath();
        ctx.moveTo(pX, pY);
        ctx.lineTo(rX, rY);
        ctx.lineTo(bX, bY);
        ctx.closePath();
        ctx.fill();

        // Back-left face
        ctx.fillStyle = dark;
        ctx.beginPath();
        ctx.moveTo(pX, pY);
        ctx.lineTo(bX, bY);
        ctx.lineTo(lX, lY);
        ctx.closePath();
        ctx.fill();

        // Front-left face (shadow)
        ctx.fillStyle = dark;
        ctx.beginPath();
        ctx.moveTo(pX, pY);
        ctx.lineTo(lX, lY);
        ctx.lineTo(fX, fY);
        ctx.closePath();
        ctx.fill();

        // Front-right face (lit gradient)
        const roofG = ctx.createLinearGradient(pX, pY, rX, rY);
        roofG.addColorStop(0, lit);
        roofG.addColorStop(1, mid);
        ctx.fillStyle = roofG;
        ctx.beginPath();
        ctx.moveTo(pX, pY);
        ctx.lineTo(fX, fY);
        ctx.lineTo(rX, rY);
        ctx.closePath();
        ctx.fill();

        // Fascia — left eave
        ctx.fillStyle = "#2A1E14";
        ctx.beginPath();
        ctx.moveTo(lX, lY);
        ctx.lineTo(fX, fY);
        ctx.lineTo(fX, fY + 2 * s);
        ctx.lineTo(lX, lY + 2 * s);
        ctx.closePath();
        ctx.fill();
        // Fascia — right eave
        ctx.fillStyle = "#3A2E22";
        ctx.beginPath();
        ctx.moveTo(fX, fY);
        ctx.lineTo(rX, rY);
        ctx.lineTo(rX, rY + 2 * s);
        ctx.lineTo(fX, fY + 2 * s);
        ctx.closePath();
        ctx.fill();

        // Tile ridges across visible front faces
        ctx.strokeStyle = "rgba(20,10,5,0.13)";
        ctx.lineWidth = 0.6 * s;
        for (let r = 0; r < 5; r++) {
          const t = (r + 1) / 6;
          const ry = pY + (baseY + drop - pY) * t;
          const rw = eIW * t;
          ctx.beginPath();
          ctx.moveTo(cx - rw, ry);
          ctx.lineTo(cx + rw, ry);
          ctx.stroke();
        }

        // Ridge highlight
        ctx.strokeStyle = "rgba(140,120,80,0.2)";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(pX, pY);
        ctx.lineTo(rX, rY);
        ctx.stroke();
      };

      const hutStoneWall = (
        bl: [number, number],
        br: [number, number],
        tr: [number, number],
        tl: [number, number],
        color: string,
        rows: number
      ) => {
        ctx.fillStyle = color;
        ctx.beginPath();
        ctx.moveTo(bl[0], bl[1]);
        ctx.lineTo(br[0], br[1]);
        ctx.lineTo(tr[0], tr[1]);
        ctx.lineTo(tl[0], tl[1]);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = "rgba(30,20,10,0.22)";
        ctx.lineWidth = 0.5 * s;
        for (let r = 0; r < rows; r++) {
          const t = (r + 0.5) / rows;
          const lx = bl[0] + (tl[0] - bl[0]) * t;
          const ly = bl[1] + (tl[1] - bl[1]) * t;
          const rx = br[0] + (tr[0] - br[0]) * t;
          const ry = br[1] + (tr[1] - br[1]) * t;
          ctx.beginPath();
          ctx.moveTo(lx, ly);
          ctx.lineTo(rx, ry);
          ctx.stroke();
          const cols = 2 + (r % 2);
          for (let c = 1; c <= cols; c++) {
            const ct = c / (cols + 1) + (r % 2 === 0 ? 0.05 : -0.03);
            const jx = lx + (rx - lx) * ct;
            const jy = ly + (ry - ly) * ct;
            const nextT = Math.min(1, (r + 1.5) / rows);
            const ny = bl[1] + (tl[1] - bl[1]) * nextT;
            ctx.beginPath();
            ctx.moveTo(jx, jy);
            ctx.lineTo(jx + ((tl[0] - bl[0]) / rows) * 0.6, ny);
            ctx.stroke();
          }
        }
      };

      const hutChimney = (chimX: number, chimTop: number, chimH: number) => {
        const cW = 3 * s;
        const cIW = cW * ISO_COS;
        const cID = cW * ISO_SIN;
        const cBot = chimTop + chimH;

        // Left face (shadow)
        ctx.fillStyle = "#4A3A2A";
        ctx.beginPath();
        ctx.moveTo(chimX - cIW, chimTop);
        ctx.lineTo(chimX, chimTop + cID);
        ctx.lineTo(chimX, cBot + cID);
        ctx.lineTo(chimX - cIW, cBot);
        ctx.closePath();
        ctx.fill();
        // Right face (lit)
        ctx.fillStyle = "#5A4A3A";
        ctx.beginPath();
        ctx.moveTo(chimX + cIW, chimTop);
        ctx.lineTo(chimX, chimTop + cID);
        ctx.lineTo(chimX, cBot + cID);
        ctx.lineTo(chimX + cIW, cBot);
        ctx.closePath();
        ctx.fill();
        // Top diamond
        ctx.fillStyle = "#6A5A48";
        ctx.beginPath();
        ctx.moveTo(chimX, chimTop - cID);
        ctx.lineTo(chimX + cIW, chimTop);
        ctx.lineTo(chimX, chimTop + cID);
        ctx.lineTo(chimX - cIW, chimTop);
        ctx.closePath();
        ctx.fill();
        // Cap/lip
        const capIW = cIW * 1.25;
        const capID = cID * 1.25;
        ctx.fillStyle = "#6A5A48";
        ctx.beginPath();
        ctx.moveTo(chimX, chimTop - capID);
        ctx.lineTo(chimX + capIW, chimTop);
        ctx.lineTo(chimX, chimTop + capID);
        ctx.lineTo(chimX - capIW, chimTop);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#5A4A38";
        ctx.beginPath();
        ctx.moveTo(chimX - capIW, chimTop);
        ctx.lineTo(chimX, chimTop + capID);
        ctx.lineTo(chimX, chimTop + capID + 2.5 * s);
        ctx.lineTo(chimX - capIW, chimTop + 2.5 * s);
        ctx.closePath();
        ctx.fill();
        // Mortar lines (iso-aligned)
        ctx.strokeStyle = "rgba(30,20,10,0.2)";
        ctx.lineWidth = 0.4 * s;
        for (let r = 0; r < 3; r++) {
          const ry = chimTop + 3 * s + (r * (chimH - 3 * s)) / 3;
          ctx.beginPath();
          ctx.moveTo(chimX - cIW, ry);
          ctx.lineTo(chimX, ry + cID);
          ctx.lineTo(chimX + cIW, ry);
          ctx.stroke();
        }
        // Smoke puffs
        const drift = Math.sin(decorTime * 2 + decorX) * 3 * s;
        ctx.fillStyle = "rgba(170,160,150,0.2)";
        ctx.beginPath();
        ctx.ellipse(
          chimX + drift * 0.5,
          chimTop - 3 * s,
          3 * s,
          2 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "rgba(150,145,140,0.13)";
        ctx.beginPath();
        ctx.ellipse(
          chimX + drift,
          chimTop - 8 * s,
          4.5 * s,
          2.5 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "rgba(140,135,130,0.07)";
        ctx.beginPath();
        ctx.ellipse(
          chimX + drift * 1.6,
          chimTop - 14 * s,
          6 * s,
          3 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
      };

      if (v === 0) {
        // === STONE COTTAGE ===
        const wW = 24 * s,
          wH = 24 * s,
          rH = 16 * s;
        const iW = wW * ISO_COS,
          iD = iW * ISO_Y_RATIO;

        // Foundation (proper iso diamond with uniform padding)
        const fP0 = 3 * s;
        const fIW0 = iW + fP0 * ISO_COS;
        const fID0 = iD + fP0 * ISO_SIN;
        ctx.fillStyle = "#3E3028";
        ctx.beginPath();
        ctx.moveTo(cx, cy + fID0 + fP0);
        ctx.lineTo(cx + fIW0, cy + fP0);
        ctx.lineTo(cx, cy - fID0 + fP0);
        ctx.lineTo(cx - fIW0, cy + fP0);
        ctx.closePath();
        ctx.fill();

        // Left wall
        hutStoneWall(
          [cx - iW, cy],
          [cx, cy + iD],
          [cx, cy + iD - wH],
          [cx - iW, cy - wH],
          "#5A4A3A",
          7
        );

        // Right wall
        const rwG = ctx.createLinearGradient(cx, cy, cx + iW, cy);
        rwG.addColorStop(0, "#8A7A65");
        rwG.addColorStop(1, "#6A5A48");
        hutStoneWall(
          [cx, cy + iD],
          [cx + iW, cy],
          [cx + iW, cy - wH],
          [cx, cy + iD - wH],
          rwG as unknown as string,
          0
        );
        ctx.fillStyle = rwG;
        ctx.beginPath();
        ctx.moveTo(cx + iW, cy);
        ctx.lineTo(cx, cy + iD);
        ctx.lineTo(cx, cy + iD - wH);
        ctx.lineTo(cx + iW, cy - wH);
        ctx.closePath();
        ctx.fill();
        // Stone texture on right wall
        ctx.strokeStyle = "rgba(30,20,10,0.2)";
        ctx.lineWidth = 0.5 * s;
        for (let r = 0; r < 7; r++) {
          const t = (r + 0.5) / 7;
          const ly = cy + iD + (cy + iD - wH - cy - iD) * t;
          const ry = cy + (cy - wH - cy) * t;
          ctx.beginPath();
          ctx.moveTo(cx + 1 * s, ly);
          ctx.lineTo(cx + iW - 1 * s, ry);
          ctx.stroke();
          const cols = 2 + (r % 2);
          for (let c = 1; c <= cols; c++) {
            const ct = c / (cols + 1);
            const jx = cx + 1 * s + (cx + iW - 1 * s - cx - 1 * s) * ct;
            const jy = ly + (ry - ly) * ct;
            ctx.beginPath();
            ctx.moveTo(jx, jy);
            ctx.lineTo(jx, jy - (wH / 7) * 0.8);
            ctx.stroke();
          }
        }

        // Door — isometric flush with right wall face
        drawIsoFlushDoor(
          ctx,
          cx + iW * 0.35,
          cy + iD * 0.35 - 7 * s,
          7,
          14,
          "right",
          s
        );

        // Windows — isometric gothic flush with walls
        drawIsoGothicWindow(
          ctx,
          cx + iW * 0.72,
          cy - wH * 0.4 + iD * 0.15,
          5,
          8,
          "right",
          s,
          "rgba(255, 200, 100",
          0.35,
          { frame: "#3A2518", sill: "#5A4A38", void: "#1A1008" }
        );
        drawIsoGothicWindow(
          ctx,
          cx - iW * 0.5,
          cy - wH * 0.45,
          4.5,
          7.5,
          "left",
          s,
          "rgba(255, 200, 100",
          0.3,
          { frame: "#3A2518", sill: "#5A4A38", void: "#1A1008" }
        );

        // Roof
        hutRoof(
          cy - wH - rH,
          cy - wH,
          iW,
          iD,
          5 * s,
          "#3A2A1A",
          "#5A4030",
          "#6A5040"
        );

        // Chimney
        hutChimney(cx + iW * 0.35, cy - wH - rH - 5 * s, rH * 0.55);
      } else if (v === 1) {
        // === WATCHTOWER — tall isometric stone tower with battlements ===
        const tW = 18 * s,
          tH = 42 * s,
          batH = 6 * s;
        const tiW = tW * ISO_COS,
          tiD = tiW * ISO_Y_RATIO;

        // Platform — stepped stone base (proper iso diamonds)
        const fP1a = 6 * s;
        const fTIW1a = tiW + fP1a * ISO_COS;
        const fTID1a = tiD + fP1a * ISO_SIN;
        ctx.fillStyle = "#3E3028";
        ctx.beginPath();
        ctx.moveTo(cx, cy + fTID1a + 4 * s);
        ctx.lineTo(cx + fTIW1a, cy + 4 * s);
        ctx.lineTo(cx, cy - fTID1a + 4 * s);
        ctx.lineTo(cx - fTIW1a, cy + 4 * s);
        ctx.closePath();
        ctx.fill();
        // Second step
        const fP1b = 4 * s;
        const fTIW1b = tiW + fP1b * ISO_COS;
        const fTID1b = tiD + fP1b * ISO_SIN;
        ctx.fillStyle = "#4A3A2C";
        ctx.beginPath();
        ctx.moveTo(cx, cy + fTID1b + 2 * s);
        ctx.lineTo(cx + fTIW1b, cy + 2 * s);
        ctx.lineTo(cx, cy - fTID1b + 2 * s);
        ctx.lineTo(cx - fTIW1b, cy + 2 * s);
        ctx.closePath();
        ctx.fill();

        // Left wall with stone texture
        hutStoneWall(
          [cx - tiW, cy],
          [cx, cy + tiD],
          [cx, cy + tiD - tH],
          [cx - tiW, cy - tH],
          "#504840",
          12
        );

        // Right wall with gradient + stone mortar
        const twG = ctx.createLinearGradient(cx, cy, cx + tiW, cy);
        twG.addColorStop(0, "#7A6A58");
        twG.addColorStop(0.5, "#6A5A48");
        twG.addColorStop(1, "#5A4A3C");
        ctx.fillStyle = twG;
        ctx.beginPath();
        ctx.moveTo(cx + tiW, cy);
        ctx.lineTo(cx, cy + tiD);
        ctx.lineTo(cx, cy + tiD - tH);
        ctx.lineTo(cx + tiW, cy - tH);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = "rgba(30,20,10,0.2)";
        ctx.lineWidth = 0.5 * s;
        for (let r = 0; r < 12; r++) {
          const t = (r + 0.5) / 12;
          const ly = cy + tiD + (cy + tiD - tH - cy - tiD) * t;
          const ry = cy + (cy - tH - cy) * t;
          ctx.beginPath();
          ctx.moveTo(cx + 1 * s, ly);
          ctx.lineTo(cx + tiW - 1 * s, ry);
          ctx.stroke();
          const cols = 2 + (r % 2);
          for (let c = 1; c <= cols; c++) {
            const ct = c / (cols + 1);
            const jx = cx + 1 * s + (tiW - 2 * s) * ct;
            const jy = ly + (ry - ly) * ct;
            ctx.beginPath();
            ctx.moveTo(jx, jy);
            ctx.lineTo(jx, jy - (tH / 12) * 0.8);
            ctx.stroke();
          }
        }

        // Stone band dividers at 1/3 and 2/3 height
        for (const frac of [0.33, 0.66]) {
          const bandY = cy - tH * frac;
          // Right band
          ctx.fillStyle = "#4A3A2A";
          ctx.beginPath();
          ctx.moveTo(cx, cy + tiD - tH * frac);
          ctx.lineTo(cx + tiW + 1.5 * s, bandY);
          ctx.lineTo(cx + tiW + 1.5 * s, bandY + 2.5 * s);
          ctx.lineTo(cx, cy + tiD - tH * frac + 2.5 * s);
          ctx.closePath();
          ctx.fill();
          // Left band
          ctx.fillStyle = "#3A2A1A";
          ctx.beginPath();
          ctx.moveTo(cx - tiW - 1.5 * s, bandY);
          ctx.lineTo(cx, cy + tiD - tH * frac);
          ctx.lineTo(cx, cy + tiD - tH * frac + 2.5 * s);
          ctx.lineTo(cx - tiW - 1.5 * s, bandY + 2.5 * s);
          ctx.closePath();
          ctx.fill();
        }

        // Door — isometric flush with right wall face
        drawIsoFlushDoor(
          ctx,
          cx + tiW * 0.4,
          cy + tiD * 0.25 - 5.5 * s,
          5,
          11,
          "right",
          s
        );

        // Isometric arrow slits on right wall (properly skewed)
        for (let w = 0; w < 3; w++) {
          const slitT = 0.2 + w * 0.25;
          const slitCX = cx + tiW * 0.55;
          const slitCY = cy - tH * slitT + tiD * (0.5 - slitT * 0.5);
          drawIsoGothicWindow(
            ctx,
            slitCX,
            slitCY,
            2,
            5,
            "right",
            s,
            "rgba(200, 150, 70",
            0.25,
            { frame: "#2a2018", sill: "#4a3a2a", void: "#1A1008" }
          );
        }

        // Left wall window — proper isometric gothic window
        drawIsoGothicWindow(
          ctx,
          cx - tiW * 0.5,
          cy - tH * 0.55,
          5,
          8,
          "left",
          s,
          "rgba(255, 200, 100",
          0.35
        );

        // Second left wall window (lower)
        drawIsoGothicWindow(
          ctx,
          cx - tiW * 0.5,
          cy - tH * 0.3,
          4,
          6,
          "left",
          s,
          "rgba(255, 180, 80",
          0.25
        );

        // Battlements top (proper iso diamond)
        const batBase = cy - tH;
        const batP = 3 * s;
        const batIW = tiW + batP * ISO_COS;
        const batID = tiD + batP * ISO_SIN;
        // Walkway top face
        ctx.fillStyle = "#6A5A48";
        ctx.beginPath();
        ctx.moveTo(cx, batBase - batH - batID);
        ctx.lineTo(cx + batIW, batBase - batH);
        ctx.lineTo(cx, batBase + batID - batH);
        ctx.lineTo(cx - batIW, batBase - batH);
        ctx.closePath();
        ctx.fill();

        // Walkway inner floor
        ctx.fillStyle = "#5A4A38";
        ctx.beginPath();
        ctx.moveTo(cx, batBase - tiD * 0.5);
        ctx.lineTo(cx + tiW * 0.6, batBase);
        ctx.lineTo(cx, batBase + tiD * 0.5);
        ctx.lineTo(cx - tiW * 0.6, batBase);
        ctx.closePath();
        ctx.fill();

        // Isometric merlons along front edge
        const mW = 3.5 * s,
          mH = 6 * s,
          mD = 2.5 * s;
        for (let m = 0; m < 4; m++) {
          const t = (m + 0.25) / 4;
          const mx = cx - batIW + batIW * 2 * t;
          const my = batBase - batH;
          // Front face
          ctx.fillStyle = "#7A6A58";
          ctx.beginPath();
          ctx.moveTo(mx - mW * 0.5, my);
          ctx.lineTo(mx - mW * 0.5, my - mH);
          ctx.lineTo(mx + mW * 0.5, my - mH);
          ctx.lineTo(mx + mW * 0.5, my);
          ctx.closePath();
          ctx.fill();
          // Top face (isometric diamond)
          ctx.fillStyle = "#8A7A68";
          ctx.beginPath();
          ctx.moveTo(mx - mW * 0.5, my - mH);
          ctx.lineTo(mx, my - mH - mD);
          ctx.lineTo(mx + mW * 0.5, my - mH);
          ctx.lineTo(mx, my - mH + mD * 0.3);
          ctx.closePath();
          ctx.fill();
          // Side face for depth
          ctx.fillStyle = "#6A5A48";
          ctx.beginPath();
          ctx.moveTo(mx + mW * 0.5, my - mH);
          ctx.lineTo(mx + mW * 0.5, my);
          ctx.lineTo(mx + mW * 0.5 + mD * 0.4, my - mD * 0.3);
          ctx.lineTo(mx + mW * 0.5 + mD * 0.4, my - mH - mD * 0.3);
          ctx.closePath();
          ctx.fill();
        }

        // Isometric merlons along right edge
        for (let m = 0; m < 3; m++) {
          const t = (m + 0.3) / 3;
          const mx = cx + batIW * t;
          const my = batBase - batH + batID * (1 - t);
          // Right face (visible side)
          ctx.fillStyle = "#5A4A3C";
          ctx.beginPath();
          ctx.moveTo(mx, my);
          ctx.lineTo(mx, my - mH);
          ctx.lineTo(mx + mW * 0.45, my - mH - mD * 0.5);
          ctx.lineTo(mx + mW * 0.45, my - mD * 0.5);
          ctx.closePath();
          ctx.fill();
          // Front face
          ctx.fillStyle = "#6A5A48";
          ctx.beginPath();
          ctx.moveTo(mx - mW * 0.45, my + mD * 0.3);
          ctx.lineTo(mx - mW * 0.45, my + mD * 0.3 - mH);
          ctx.lineTo(mx, my - mH);
          ctx.lineTo(mx, my);
          ctx.closePath();
          ctx.fill();
          // Top
          ctx.fillStyle = "#7A6A58";
          ctx.beginPath();
          ctx.moveTo(mx - mW * 0.45, my + mD * 0.3 - mH);
          ctx.lineTo(mx, my - mH - mD * 0.5);
          ctx.lineTo(mx + mW * 0.45, my - mH - mD * 0.5);
          ctx.lineTo(mx, my - mH);
          ctx.closePath();
          ctx.fill();
        }

        // Merlons along left edge
        for (let m = 0; m < 3; m++) {
          const t = (m + 0.3) / 3;
          const mx = cx - batIW * t;
          const my = batBase - batH + batID * (1 - t);
          ctx.fillStyle = "#4A3A30";
          ctx.beginPath();
          ctx.moveTo(mx, my);
          ctx.lineTo(mx, my - mH);
          ctx.lineTo(mx - mW * 0.45, my - mH - mD * 0.5);
          ctx.lineTo(mx - mW * 0.45, my - mD * 0.5);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = "#5A4A3C";
          ctx.beginPath();
          ctx.moveTo(mx + mW * 0.45, my + mD * 0.3);
          ctx.lineTo(mx + mW * 0.45, my + mD * 0.3 - mH);
          ctx.lineTo(mx, my - mH);
          ctx.lineTo(mx, my);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = "#6A5A48";
          ctx.beginPath();
          ctx.moveTo(mx + mW * 0.45, my + mD * 0.3 - mH);
          ctx.lineTo(mx, my - mH - mD * 0.5);
          ctx.lineTo(mx - mW * 0.45, my - mH - mD * 0.5);
          ctx.lineTo(mx, my - mH);
          ctx.closePath();
          ctx.fill();
        }

        // Conical roof — proper four-face iso pyramid
        const roofBot = batBase - batH + 6 * s;
        const pk = roofBot - 22 * s;
        const crOv = 5 * s;
        const crIW = tiW + crOv * ISO_COS;
        const crID = tiD + crOv * ISO_SIN;
        const crDrop = 2 * s;

        const crFront = { x: cx, y: roofBot + crID + crDrop };
        const crRight = { x: cx + crIW, y: roofBot + crDrop };
        const crBack = { x: cx, y: roofBot - crID + crDrop };
        const crLeft = { x: cx - crIW, y: roofBot + crDrop };

        // Back-right face (behind)
        ctx.fillStyle = "#4A3828";
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(crRight.x, crRight.y);
        ctx.lineTo(crBack.x, crBack.y);
        ctx.closePath();
        ctx.fill();
        // Back-left face
        ctx.fillStyle = "#3A2E20";
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(crBack.x, crBack.y);
        ctx.lineTo(crLeft.x, crLeft.y);
        ctx.closePath();
        ctx.fill();
        // Front-left face (shadow)
        ctx.fillStyle = "#3A2E20";
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(crLeft.x, crLeft.y);
        ctx.lineTo(crFront.x, crFront.y);
        ctx.closePath();
        ctx.fill();
        // Front-right face (lit) with gradient
        const crG = ctx.createLinearGradient(cx, pk, crRight.x, crRight.y);
        crG.addColorStop(0, "#6A5840");
        crG.addColorStop(1, "#4A3828");
        ctx.fillStyle = crG;
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(crFront.x, crFront.y);
        ctx.lineTo(crRight.x, crRight.y);
        ctx.closePath();
        ctx.fill();

        // Shingle lines
        ctx.strokeStyle = "rgba(20,10,5,0.15)";
        ctx.lineWidth = 0.6 * s;
        for (let r = 0; r < 7; r++) {
          const t = (r + 1) / 8;
          const ry = pk + (roofBot + crDrop - pk) * t;
          const rw = crIW * t;
          ctx.beginPath();
          ctx.moveTo(cx - rw, ry);
          ctx.lineTo(cx + rw, ry);
          ctx.stroke();
        }

        // Ridge highlight
        ctx.strokeStyle = "rgba(140,120,80,0.22)";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(crRight.x, crRight.y);
        ctx.stroke();

        // Flag pole + banner
        ctx.strokeStyle = "#3A2A1A";
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(cx, pk - 12 * s);
        ctx.stroke();
        // Pole finial
        ctx.fillStyle = "#D8A838";
        ctx.beginPath();
        ctx.arc(cx, pk - 12.5 * s, 1.2 * s, 0, Math.PI * 2);
        ctx.fill();
        // Waving banner
        const fWave = Math.sin(decorTime * 3 + decorX * 0.1) * 2.5 * s;
        ctx.fillStyle = "#B03030";
        ctx.beginPath();
        ctx.moveTo(cx + 1 * s, pk - 12 * s);
        ctx.quadraticCurveTo(
          cx + 5 * s + fWave,
          pk - 10 * s,
          cx + 8 * s,
          pk - 9 * s + fWave * 0.5
        );
        ctx.lineTo(cx + 8 * s, pk - 5 * s + fWave * 0.5);
        ctx.quadraticCurveTo(
          cx + 5 * s + fWave * 0.5,
          pk - 6 * s,
          cx + 1 * s,
          pk - 8 * s
        );
        ctx.closePath();
        ctx.fill();
        // Banner stripe
        ctx.fillStyle = "#D8A838";
        ctx.beginPath();
        ctx.moveTo(cx + 1 * s, pk - 10 * s);
        ctx.quadraticCurveTo(
          cx + 5 * s + fWave * 0.8,
          pk - 8.5 * s,
          cx + 8 * s,
          pk - 7.5 * s + fWave * 0.5
        );
        ctx.lineTo(cx + 8 * s, pk - 6.5 * s + fWave * 0.5);
        ctx.quadraticCurveTo(
          cx + 5 * s + fWave * 0.6,
          pk - 7.5 * s,
          cx + 1 * s,
          pk - 9 * s
        );
        ctx.closePath();
        ctx.fill();

        // Brazier on battlements
        const brx = cx - tiW * 0.3,
          bry = batBase - batH;
        ctx.fillStyle = "#4A3A2A";
        ctx.beginPath();
        ctx.moveTo(brx - 2.5 * s, bry);
        ctx.lineTo(brx - 2 * s, bry - 4 * s);
        ctx.lineTo(brx + 2 * s, bry - 4 * s);
        ctx.lineTo(brx + 2.5 * s, bry);
        ctx.closePath();
        ctx.fill();
        // Fire glow
        const firePulse = 0.5 + Math.sin(decorTime * 4) * 0.2;
        ctx.fillStyle = `rgba(255,120,30,${firePulse * 0.6})`;
        ctx.beginPath();
        ctx.ellipse(brx, bry - 5 * s, 2 * s, 3 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = `rgba(255,200,50,${firePulse * 0.4})`;
        ctx.beginPath();
        ctx.ellipse(brx, bry - 6 * s, 1.2 * s, 2 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        // Fire glow on surroundings
        const fireGlowG = ctx.createRadialGradient(
          brx,
          bry - 5 * s,
          0,
          brx,
          bry - 5 * s,
          10 * s
        );
        fireGlowG.addColorStop(0, `rgba(255,150,50,${firePulse * 0.12})`);
        fireGlowG.addColorStop(1, "rgba(255,150,50,0)");
        ctx.fillStyle = fireGlowG;
        ctx.beginPath();
        ctx.arc(brx, bry - 5 * s, 10 * s, 0, Math.PI * 2);
        ctx.fill();
      } else if (v === 2) {
        // === HALF-TIMBER MANOR ===
        const wW = 28 * s,
          wH = 28 * s,
          rH = 20 * s;
        const iW = wW * ISO_COS,
          iD = iW * ISO_Y_RATIO;
        const stoneH = wH * 0.4;

        // Foundation (proper iso diamond with uniform padding)
        const fP2 = 3 * s;
        const fIW2 = iW + fP2 * ISO_COS;
        const fID2 = iD + fP2 * ISO_SIN;
        ctx.fillStyle = "#3E3028";
        ctx.beginPath();
        ctx.moveTo(cx, cy + fID2 + fP2);
        ctx.lineTo(cx + fIW2, cy + fP2);
        ctx.lineTo(cx, cy - fID2 + fP2);
        ctx.lineTo(cx - fIW2, cy + fP2);
        ctx.closePath();
        ctx.fill();

        // Lower left — stone
        hutStoneWall(
          [cx - iW, cy],
          [cx, cy + iD],
          [cx, cy + iD - stoneH],
          [cx - iW, cy - stoneH],
          "#5A4A3A",
          5
        );

        // Lower right — stone with gradient
        const lrG = ctx.createLinearGradient(cx, cy, cx + iW, cy);
        lrG.addColorStop(0, "#8A7A65");
        lrG.addColorStop(1, "#6A5A48");
        ctx.fillStyle = lrG;
        ctx.beginPath();
        ctx.moveTo(cx + iW, cy);
        ctx.lineTo(cx, cy + iD);
        ctx.lineTo(cx, cy + iD - stoneH);
        ctx.lineTo(cx + iW, cy - stoneH);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = "rgba(30,20,10,0.2)";
        ctx.lineWidth = 0.5 * s;
        for (let r = 0; r < 5; r++) {
          const t = (r + 0.5) / 5;
          ctx.beginPath();
          ctx.moveTo(cx + 1 * s, cy + iD - stoneH * t);
          ctx.lineTo(cx + iW - 1 * s, cy - stoneH * t);
          ctx.stroke();
        }

        // Overhang ledge (proper iso extensions)
        const ledgP = 3 * s;
        const ledgIW = iW + ledgP * ISO_COS;
        const ledgID = iD + ledgP * ISO_SIN;
        ctx.fillStyle = "#5D4037";
        ctx.beginPath();
        ctx.moveTo(cx - ledgIW, cy - stoneH);
        ctx.lineTo(cx, cy + ledgID - stoneH);
        ctx.lineTo(cx, cy + ledgID - stoneH + 2.5 * s);
        ctx.lineTo(cx - ledgIW, cy - stoneH + 2.5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#4A3525";
        ctx.beginPath();
        ctx.moveTo(cx + ledgIW, cy - stoneH);
        ctx.lineTo(cx, cy + ledgID - stoneH);
        ctx.lineTo(cx, cy + ledgID - stoneH + 2.5 * s);
        ctx.lineTo(cx + ledgIW, cy - stoneH + 2.5 * s);
        ctx.closePath();
        ctx.fill();

        // Upper left — plaster
        ctx.fillStyle = "#C8B898";
        ctx.beginPath();
        ctx.moveTo(cx - iW, cy - stoneH);
        ctx.lineTo(cx, cy + iD - stoneH);
        ctx.lineTo(cx, cy + iD - wH);
        ctx.lineTo(cx - iW, cy - wH);
        ctx.closePath();
        ctx.fill();

        // Upper right — plaster
        const urG = ctx.createLinearGradient(
          cx,
          cy - stoneH,
          cx + iW,
          cy - stoneH
        );
        urG.addColorStop(0, "#D8C8A8");
        urG.addColorStop(1, "#C0B090");
        ctx.fillStyle = urG;
        ctx.beginPath();
        ctx.moveTo(cx + iW, cy - stoneH);
        ctx.lineTo(cx, cy + iD - stoneH);
        ctx.lineTo(cx, cy + iD - wH);
        ctx.lineTo(cx + iW, cy - wH);
        ctx.closePath();
        ctx.fill();

        // Timber frame right wall
        ctx.strokeStyle = "#3E2A1A";
        ctx.lineWidth = 2.2 * s;
        ctx.beginPath();
        ctx.moveTo(cx + 2 * s, cy + iD - stoneH);
        ctx.lineTo(cx + 2 * s, cy + iD - wH);
        ctx.moveTo(cx + iW - 2 * s, cy - stoneH);
        ctx.lineTo(cx + iW - 2 * s, cy - wH);
        ctx.moveTo(cx + iW * 0.5, cy + iD * 0.5 - stoneH);
        ctx.lineTo(cx + iW * 0.5, cy + iD * 0.5 - wH);
        ctx.stroke();
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(cx + 1 * s, cy + iD - wH * 0.65);
        ctx.lineTo(cx + iW - 1 * s, cy - wH * 0.65);
        ctx.moveTo(cx + 1 * s, cy + iD - wH + 2 * s);
        ctx.lineTo(cx + iW - 1 * s, cy - wH + 2 * s);
        ctx.stroke();
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(cx + 3 * s, cy + iD - stoneH);
        ctx.lineTo(cx + iW * 0.48, cy + iD * 0.52 - wH);
        ctx.moveTo(cx + iW * 0.52, cy + iD * 0.48 - stoneH);
        ctx.lineTo(cx + iW - 3 * s, cy - wH);
        ctx.stroke();

        // Left wall timber
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.moveTo(cx - iW + 2 * s, cy - stoneH);
        ctx.lineTo(cx - iW + 2 * s, cy - wH);
        ctx.moveTo(cx - 2 * s, cy + iD - stoneH);
        ctx.lineTo(cx - 2 * s, cy + iD - wH);
        ctx.stroke();

        // Door — isometric flush with right wall face
        drawIsoFlushDoor(
          ctx,
          cx + iW * 0.2,
          cy + iD * 0.7 - 7.5 * s,
          8,
          15,
          "right",
          s
        );

        // Windows — isometric gothic flush with walls
        for (let wi = 0; wi < 3; wi++) {
          const wwx = cx + iW * (0.15 + wi * 0.32);
          const wwy = cy + iD * (0.65 - wi * 0.32) - wH * 0.58;
          drawIsoGothicWindow(
            ctx,
            wwx,
            wwy,
            4.5,
            7,
            "right",
            s,
            "rgba(255, 200, 100",
            0.3,
            { frame: "#3A2518", sill: "#5A4A38", void: "#1A1008" }
          );
        }
        drawIsoGothicWindow(
          ctx,
          cx - iW * 0.55,
          cy - wH * 0.6,
          5,
          8,
          "left",
          s,
          "rgba(255, 200, 100",
          0.35,
          { frame: "#3A2518", sill: "#5A4A38", void: "#1A1008" }
        );

        // Roof
        hutRoof(
          cy - wH - rH,
          cy - wH,
          iW,
          iD,
          6 * s,
          "#3A2A1A",
          "#5A4030",
          "#6A5040"
        );

        // Isometric dormer
        const dwX = cx + iW * 0.35,
          dwY = cy - wH - rH * 0.4;
        const dW = 5 * s,
          dH = 7 * s,
          dD = 3 * s;
        // Right side wall (depth)
        ctx.fillStyle = "#A89878";
        ctx.beginPath();
        ctx.moveTo(dwX + dW, dwY + dH);
        ctx.lineTo(dwX + dW, dwY + 2 * s);
        ctx.lineTo(dwX + dW + dD * 0.5, dwY + 2 * s - dD * 0.3);
        ctx.lineTo(dwX + dW + dD * 0.5, dwY + dH - dD * 0.3);
        ctx.closePath();
        ctx.fill();
        // Front face wall
        ctx.fillStyle = "#C8B898";
        ctx.beginPath();
        ctx.moveTo(dwX - dW, dwY + dH);
        ctx.lineTo(dwX - dW, dwY + 2 * s);
        ctx.lineTo(dwX + dW, dwY + 2 * s);
        ctx.lineTo(dwX + dW, dwY + dH);
        ctx.closePath();
        ctx.fill();
        // Gable triangle (front)
        ctx.fillStyle = "#C8B898";
        ctx.beginPath();
        ctx.moveTo(dwX - dW, dwY + 2 * s);
        ctx.lineTo(dwX, dwY - 1 * s);
        ctx.lineTo(dwX + dW, dwY + 2 * s);
        ctx.closePath();
        ctx.fill();
        // Roof left slope (shadow)
        ctx.fillStyle = "#3A2A1A";
        ctx.beginPath();
        ctx.moveTo(dwX, dwY - 1 * s);
        ctx.lineTo(dwX - dW - 1 * s, dwY + 2 * s + 0.5 * s);
        ctx.lineTo(
          dwX + dD * 0.5 - dW - 1 * s,
          dwY + 2 * s + 0.5 * s - dD * 0.3
        );
        ctx.lineTo(dwX + dD * 0.5, dwY - 1 * s - dD * 0.3);
        ctx.closePath();
        ctx.fill();
        // Roof right slope (lit)
        ctx.fillStyle = "#5A4030";
        ctx.beginPath();
        ctx.moveTo(dwX, dwY - 1 * s);
        ctx.lineTo(dwX + dW + 1 * s, dwY + 2 * s + 0.5 * s);
        ctx.lineTo(
          dwX + dW + 1 * s + dD * 0.5,
          dwY + 2 * s + 0.5 * s - dD * 0.3
        );
        ctx.lineTo(dwX + dD * 0.5, dwY - 1 * s - dD * 0.3);
        ctx.closePath();
        ctx.fill();
        // Window opening
        ctx.fillStyle = "#1A1008";
        ctx.beginPath();
        ctx.moveTo(dwX - 2.5 * s, dwY + dH - 0.5 * s);
        ctx.lineTo(dwX - 2.5 * s, dwY + 3 * s);
        ctx.lineTo(dwX + 2.5 * s, dwY + 3 * s);
        ctx.lineTo(dwX + 2.5 * s, dwY + dH - 0.5 * s);
        ctx.closePath();
        ctx.fill();
        // Window glow
        const dwGl = ctx.createRadialGradient(
          dwX,
          dwY + 4.5 * s,
          0,
          dwX,
          dwY + 4.5 * s,
          3 * s
        );
        dwGl.addColorStop(0, "rgba(255,200,100,0.4)");
        dwGl.addColorStop(1, "rgba(220,160,60,0.05)");
        ctx.fillStyle = dwGl;
        ctx.fillRect(dwX - 2 * s, dwY + 3.5 * s, 4 * s, 3 * s);
        // Window mullions
        ctx.strokeStyle = "#3A2518";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo(dwX, dwY + 3 * s);
        ctx.lineTo(dwX, dwY + dH - 0.5 * s);
        ctx.moveTo(dwX - 2.5 * s, dwY + 4.5 * s);
        ctx.lineTo(dwX + 2.5 * s, dwY + 4.5 * s);
        ctx.stroke();

        // Chimney
        hutChimney(cx - iW * 0.3, cy - wH - rH - 5 * s, rH * 0.45);
      } else {
        // === ROUND STONE HUT ===
        const hR = 16 * s,
          hH = 18 * s,
          thatchH = 20 * s;

        // Cylinder wall (use ISO_Y_RATIO for proper iso ellipses)
        const hutRt = ISO_Y_RATIO;
        const cwG = ctx.createLinearGradient(cx - hR, cy, cx + hR, cy);
        cwG.addColorStop(0, "#504840");
        cwG.addColorStop(0.35, "#8A7A65");
        cwG.addColorStop(0.7, "#6A5A48");
        cwG.addColorStop(1, "#504840");
        ctx.fillStyle = cwG;
        ctx.beginPath();
        ctx.ellipse(cx, cy, hR, hR * hutRt, 0, 0, Math.PI);
        ctx.lineTo(cx - hR, cy - hH);
        ctx.ellipse(cx, cy - hH, hR, hR * hutRt, 0, Math.PI, Math.PI * 2, true);
        ctx.closePath();
        ctx.fill();

        // Stone mortar
        ctx.strokeStyle = "rgba(30,20,10,0.2)";
        ctx.lineWidth = 0.6 * s;
        for (let r = 0; r < 6; r++) {
          const ry = cy - hH + (r + 0.5) * (hH / 6);
          ctx.beginPath();
          ctx.ellipse(cx, ry, hR + 0.5 * s, hR * hutRt, 0, 0.1, Math.PI - 0.1);
          ctx.stroke();
          const cols = 4 + (r % 2);
          for (let c = 0; c < cols; c++) {
            const ang =
              0.3 + (c / cols) * (Math.PI - 0.6) + (r % 2 === 0 ? 0.15 : 0);
            const jx = cx + Math.cos(ang) * hR;
            const jy = ry + Math.sin(ang) * hR * hutRt * 0.2;
            ctx.beginPath();
            ctx.moveTo(jx, jy);
            ctx.lineTo(jx, Math.min(ry + hH / 6, cy));
            ctx.stroke();
          }
        }

        // Top rim
        ctx.fillStyle = "#6A5A48";
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy - hH,
          hR + 1.5 * s,
          (hR + 1.5 * s) * hutRt,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "#7A6A58";
        ctx.beginPath();
        ctx.ellipse(cx, cy - hH, hR, hR * hutRt, 0, 0, Math.PI * 2);
        ctx.fill();

        // Door — isometric flush with right wall face
        drawIsoFlushDoor(
          ctx,
          cx + Math.cos(0.35) * hR * 0.72,
          cy + Math.sin(0.35) * hR * 0.3 - 6 * s,
          6,
          12,
          "right",
          s
        );

        // Windows — isometric gothic flush with curved wall
        drawIsoGothicWindow(
          ctx,
          cx - hR * 0.62,
          cy - hH * 0.45,
          4.5,
          7,
          "left",
          s,
          "rgba(255, 200, 100",
          0.3,
          { frame: "#3A2518", sill: "#5A4A38", void: "#1A1008" }
        );
        drawIsoGothicWindow(
          ctx,
          cx + hR * 0.12,
          cy - hH * 0.5 + hR * 0.1,
          3.5,
          6,
          "right",
          s,
          "rgba(255, 200, 100",
          0.25,
          { frame: "#3A2518", sill: "#5A4A38", void: "#1A1008" }
        );

        // Thatched roof — proper iso diamond base with four faces
        const roofBase = cy - hH;
        const pk = roofBase - thatchH;
        const thOv = 6 * s;
        const thRIW = hR + thOv * ISO_COS;
        const thRID = (hR + thOv) * hutRt;
        const thDrop = 3 * s;

        const thFront = { x: cx, y: roofBase + thRID + thDrop };
        const thRight = { x: cx + thRIW, y: roofBase + thDrop };
        const thBack = { x: cx, y: roofBase - thRID + thDrop };
        const thLeft = { x: cx - thRIW, y: roofBase + thDrop };

        // Back-right (behind)
        ctx.fillStyle = "#4A3828";
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(thRight.x, thRight.y);
        ctx.lineTo(thBack.x, thBack.y);
        ctx.closePath();
        ctx.fill();
        // Back-left
        ctx.fillStyle = "#3A2A18";
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(thBack.x, thBack.y);
        ctx.lineTo(thLeft.x, thLeft.y);
        ctx.closePath();
        ctx.fill();
        // Front-left (shadow)
        ctx.fillStyle = "#3A2A18";
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(thLeft.x, thLeft.y);
        ctx.lineTo(thFront.x, thFront.y);
        ctx.closePath();
        ctx.fill();
        // Front-right (lit)
        const thG = ctx.createLinearGradient(cx, pk, thRight.x, thRight.y);
        thG.addColorStop(0, "#6A5840");
        thG.addColorStop(1, "#4A3828");
        ctx.fillStyle = thG;
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(thFront.x, thFront.y);
        ctx.lineTo(thRight.x, thRight.y);
        ctx.closePath();
        ctx.fill();

        // Thatch texture lines
        ctx.strokeStyle = "rgba(90,70,40,0.15)";
        ctx.lineWidth = 0.7 * s;
        for (let r = 0; r < 7; r++) {
          const t = (r + 0.5) / 8;
          const ry = pk + (roofBase + thDrop - pk) * t;
          const rw = thRIW * t;
          ctx.beginPath();
          ctx.moveTo(cx - rw, ry);
          ctx.lineTo(cx + rw, ry);
          ctx.stroke();
        }
        ctx.lineWidth = 0.5 * s;
        for (let b = 0; b < 8; b++) {
          const t = (b + 0.5) / 8;
          const bx = cx - thRIW + thRIW * 2 * t;
          const topT = Math.abs(bx - cx) / thRIW;
          const by1 = pk + (roofBase + thDrop - pk) * topT;
          ctx.beginPath();
          ctx.moveTo(bx, by1 + 2 * s);
          ctx.lineTo(bx, roofBase + thDrop);
          ctx.stroke();
        }
        // Ridge highlight
        ctx.strokeStyle = "rgba(140,120,80,0.25)";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(cx, pk);
        ctx.lineTo(thRight.x, thRight.y);
        ctx.stroke();
        // Fascia edge (iso-consistent ellipses)
        const thFasciaRY = thRID;
        ctx.fillStyle = "#4A3A28";
        ctx.beginPath();
        ctx.ellipse(cx, roofBase + thDrop, thRIW, thFasciaRY, 0, 0, Math.PI);
        ctx.lineTo(cx - thRIW, roofBase + thDrop + 2 * s);
        ctx.ellipse(
          cx,
          roofBase + thDrop + 2 * s,
          thRIW,
          thFasciaRY,
          0,
          Math.PI,
          0,
          true
        );
        ctx.closePath();
        ctx.fill();
        // Smoke hole
        ctx.fillStyle = "#2A1A10";
        ctx.beginPath();
        ctx.ellipse(cx, pk + 2 * s, 3 * s, 1.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        const smD = Math.sin(decorTime * 2.2 + decorX * 0.1) * 2.5 * s;
        ctx.fillStyle = "rgba(170,160,150,0.18)";
        ctx.beginPath();
        ctx.ellipse(
          cx + smD * 0.5,
          pk - 3 * s,
          3 * s,
          1.8 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "rgba(150,145,140,0.12)";
        ctx.beginPath();
        ctx.ellipse(
          cx + smD,
          pk - 8 * s,
          4.5 * s,
          2.5 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }
    case "fire": {
      const smokeOff = Math.sin(decorTime * 3) * 3 * s;
      ctx.fillStyle = "rgba(80,80,80,0.25)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + smokeOff,
        screenPos.y - 25 * s,
        8 * s,
        6 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.fillStyle = "rgba(100,100,100,0.15)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - smokeOff * 0.5,
        screenPos.y - 35 * s,
        6 * s,
        4 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      setShadowBlur(ctx, 15 * s, "#ff6600");
      const flameH = 12 + Math.sin(decorTime * 8) * 4;
      const fireGrad = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y - 5 * s,
        0,
        screenPos.x,
        screenPos.y - 5 * s,
        flameH * s
      );
      fireGrad.addColorStop(0, "#ffffff");
      fireGrad.addColorStop(0.2, "#ffff00");
      fireGrad.addColorStop(0.5, "#ff8800");
      fireGrad.addColorStop(0.8, "#ff4400");
      fireGrad.addColorStop(1, "rgba(200,50,0,0)");
      ctx.fillStyle = fireGrad;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 8 * s, screenPos.y);
      ctx.quadraticCurveTo(
        screenPos.x - 10 * s,
        screenPos.y - flameH * 0.5 * s,
        screenPos.x - 3 * s,
        screenPos.y - flameH * s
      );
      ctx.quadraticCurveTo(
        screenPos.x,
        screenPos.y - flameH * 1.2 * s,
        screenPos.x + 3 * s,
        screenPos.y - flameH * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 10 * s,
        screenPos.y - flameH * 0.5 * s,
        screenPos.x + 8 * s,
        screenPos.y
      );
      ctx.closePath();
      ctx.fill();
      clearShadow(ctx);
      ctx.fillStyle = "#5d4037";
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y + 2 * s,
        10 * s,
        5 * s,
        decorX * 4.7 + decorY * 8.1
      );
      ctx.fill();
      break;
    }
    case "sword": {
      const swX = screenPos.x;
      const swY = screenPos.y;
      const swSeed = decorX * 73 + decorY * 137;
      const swHash = (n: number) => {
        const h = Math.sin(swSeed * 0.1 + n * 2.654) * 43_758.5453;
        return h - Math.floor(h);
      };

      // 1-3 swords in a cluster
      const swCount = 1 + Math.floor(swHash(0) * 2.99);
      const swSpread = swCount > 1 ? 6 + swCount * 2 : 0;

      // Cluster ground shadow
      const swShadR = 10 + swSpread;
      const swShadG = ctx.createRadialGradient(
        swX + 2 * s,
        swY + 4 * s,
        0,
        swX + 2 * s,
        swY + 4 * s,
        swShadR * s
      );
      swShadG.addColorStop(0, "rgba(0,0,0,0.25)");
      swShadG.addColorStop(0.5, "rgba(0,0,0,0.08)");
      swShadG.addColorStop(1, "rgba(0,0,0,0)");
      ctx.fillStyle = swShadG;
      ctx.beginPath();
      ctx.ellipse(
        swX + 2 * s,
        swY + 4 * s,
        swShadR * s,
        swShadR * 0.4 * s,
        0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Sword style definitions
      const swStyles = [
        { bW: 3.5, bladeTint: 0, gW: 9, gemCol: "#c62828", gripLen: 10 },
        { bW: 4.5, bladeTint: 1, gW: 10, gemCol: "#1565c0", gripLen: 9 },
        { bW: 2.5, bladeTint: 2, gW: 7, gemCol: "#2e7d32", gripLen: 12 },
        { bW: 3, bladeTint: 0, gW: 8, gemCol: "#ff8f00", gripLen: 8 },
        { bW: 5, bladeTint: 1, gW: 6, gemCol: "#6a1b9a", gripLen: 9 },
      ];

      // Blade tint palettes
      const bladePalettes = [
        { dark: "#707880", hi: "#f0f2f5", lit: "#e0e4e8", mid: "#b8c0c8" },
        { dark: "#505058", hi: "#d0d4d8", lit: "#b0b8c0", mid: "#808890" },
        { dark: "#7a6848", hi: "#e8daa8", lit: "#d8c898", mid: "#b8a878" },
      ];

      // Excalibur-style: blade goes INTO ground, pommel sticks UP
      // Local coords: y=0 is ground level, negative Y = above ground, positive Y = below ground
      const drawExcaliburSword = (
        ox: number,
        oy: number,
        tiltAng: number,
        sc: number,
        styleIdx: number
      ) => {
        const sty = swStyles[styleIdx % swStyles.length];
        const pal = bladePalettes[sty.bladeTint];
        const bW = sty.bW * sc;
        const gW = sty.gW * sc;
        const gripLen = sty.gripLen * sc;

        // How much blade is visible above ground (just a sliver)
        const visibleBlade = 6 * sc;

        ctx.save();
        ctx.translate(ox, oy);
        ctx.rotate(tiltAng);

        // === VISIBLE BLADE STUB above ground ===
        const blG = ctx.createLinearGradient(-bW * s, 0, bW * s, 0);
        blG.addColorStop(0, pal.dark);
        blG.addColorStop(0.25, pal.mid);
        blG.addColorStop(0.45, pal.lit);
        blG.addColorStop(0.55, pal.hi);
        blG.addColorStop(0.75, pal.mid);
        blG.addColorStop(1, pal.dark);

        ctx.fillStyle = blG;
        ctx.beginPath();
        ctx.moveTo(-bW * 0.7 * s, 0);
        ctx.lineTo(-bW * s, -visibleBlade * s);
        ctx.lineTo(bW * s, -visibleBlade * s);
        ctx.lineTo(bW * 0.7 * s, 0);
        ctx.closePath();
        ctx.fill();

        // Fuller on visible blade
        ctx.strokeStyle = "rgba(40,45,55,0.3)";
        ctx.lineWidth = 0.8 * sc * s;
        ctx.beginPath();
        ctx.moveTo(0, -1 * sc * s);
        ctx.lineTo(0, -visibleBlade * s);
        ctx.stroke();
        ctx.strokeStyle = `rgba(${sty.bladeTint === 2 ? "200,190,150" : "180,190,200"},0.2)`;
        ctx.lineWidth = 0.3 * sc * s;
        ctx.beginPath();
        ctx.moveTo(0.3 * sc * s, -1.5 * sc * s);
        ctx.lineTo(0.3 * sc * s, -(visibleBlade - 0.5) * s);
        ctx.stroke();

        // Blade edge highlight
        ctx.strokeStyle = "rgba(255,255,255,0.25)";
        ctx.lineWidth = 0.4 * sc * s;
        ctx.beginPath();
        ctx.moveTo(-bW * 0.85 * s, -1 * sc * s);
        ctx.lineTo(-bW * 0.95 * s, -visibleBlade * s);
        ctx.stroke();

        // Blade outline
        ctx.strokeStyle = "rgba(30,35,45,0.3)";
        ctx.lineWidth = 0.35 * sc * s;
        ctx.beginPath();
        ctx.moveTo(-bW * 0.7 * s, 0);
        ctx.lineTo(-bW * s, -visibleBlade * s);
        ctx.lineTo(bW * s, -visibleBlade * s);
        ctx.lineTo(bW * 0.7 * s, 0);
        ctx.stroke();

        // === CROSS-GUARD ===
        const guardY = -(visibleBlade + 1) * sc * s;
        const guardH = 3 * sc;
        const guardGr = ctx.createLinearGradient(
          -gW * s,
          guardY,
          gW * s,
          guardY
        );
        guardGr.addColorStop(0, "#4a3a28");
        guardGr.addColorStop(0.3, "#8a7a60");
        guardGr.addColorStop(0.5, "#a09070");
        guardGr.addColorStop(0.7, "#8a7a60");
        guardGr.addColorStop(1, "#4a3a28");

        // Guard front face
        ctx.fillStyle = guardGr;
        ctx.beginPath();
        ctx.moveTo(-gW * s, guardY);
        ctx.lineTo(gW * s, guardY);
        ctx.quadraticCurveTo(
          (gW + 1) * s,
          guardY + guardH * 0.5 * s,
          gW * s,
          guardY + guardH * s
        );
        ctx.lineTo(-gW * s, guardY + guardH * s);
        ctx.quadraticCurveTo(
          -(gW + 1) * s,
          guardY + guardH * 0.5 * s,
          -gW * s,
          guardY
        );
        ctx.closePath();
        ctx.fill();

        // Guard top face
        ctx.fillStyle = "#b0a080";
        ctx.beginPath();
        ctx.moveTo(-gW * s, guardY);
        ctx.lineTo((-gW + 1) * s, guardY - 1 * sc * s);
        ctx.lineTo((gW + 1) * s, guardY - 1 * sc * s);
        ctx.lineTo(gW * s, guardY);
        ctx.closePath();
        ctx.fill();

        // Guard end caps
        ctx.fillStyle = "#6a5a40";
        ctx.beginPath();
        ctx.ellipse(
          -gW * s,
          guardY + guardH * 0.5 * s,
          1.3 * sc * s,
          1.8 * sc * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "#7a6a50";
        ctx.beginPath();
        ctx.ellipse(
          gW * s,
          guardY + guardH * 0.5 * s,
          1.3 * sc * s,
          1.8 * sc * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // === GRIP — leather-wrapped handle extending upward ===
        const gripTop = guardY - 1 * sc * s;
        const gripBottom = gripTop - gripLen * s;

        const grpG = ctx.createLinearGradient(
          -2 * sc * s,
          gripTop,
          2 * sc * s,
          gripTop
        );
        grpG.addColorStop(0, "#3a2415");
        grpG.addColorStop(0.4, "#5a3d25");
        grpG.addColorStop(0.6, "#6a4d30");
        grpG.addColorStop(1, "#3a2415");
        ctx.fillStyle = grpG;
        ctx.beginPath();
        ctx.moveTo(-2 * sc * s, gripTop);
        ctx.lineTo(2 * sc * s, gripTop);
        ctx.lineTo(1.6 * sc * s, gripBottom);
        ctx.lineTo(-1.6 * sc * s, gripBottom);
        ctx.closePath();
        ctx.fill();

        // Leather wrapping
        ctx.strokeStyle = "rgba(25,15,8,0.3)";
        ctx.lineWidth = 0.4 * sc * s;
        const wrapCount = Math.floor(gripLen / 2.2);
        for (let lw = 0; lw < wrapCount; lw++) {
          const lwY = gripTop - (1.5 + lw * 2.2) * sc * s;
          ctx.beginPath();
          ctx.moveTo(-1.8 * sc * s, lwY);
          ctx.lineTo(1.8 * sc * s, lwY - 0.8 * sc * s);
          ctx.stroke();
        }

        // === POMMEL at the very top ===
        const pomY = gripBottom - 1 * sc * s;
        const pmG = ctx.createRadialGradient(
          0.3 * sc * s,
          pomY + 0.5 * sc * s,
          0.3 * sc * s,
          0,
          pomY,
          3 * sc * s
        );
        pmG.addColorStop(0, "#c0a878");
        pmG.addColorStop(0.5, "#8a7a55");
        pmG.addColorStop(1, "#4a3a28");
        ctx.fillStyle = pmG;
        ctx.beginPath();
        ctx.ellipse(0, pomY, 2.8 * sc * s, 2.2 * sc * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Gem on pommel
        const gmG = ctx.createRadialGradient(
          0.2 * sc * s,
          pomY + 0.3 * sc * s,
          0.1 * sc * s,
          0,
          pomY,
          1.3 * sc * s
        );
        gmG.addColorStop(0, "#ffffff");
        gmG.addColorStop(0.3, sty.gemCol);
        gmG.addColorStop(1, "#1a1a1a");
        ctx.fillStyle = gmG;
        ctx.beginPath();
        ctx.ellipse(0, pomY, 1.3 * sc * s, 1 * sc * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Pommel outline
        ctx.strokeStyle = "rgba(40,30,20,0.4)";
        ctx.lineWidth = 0.35 * sc * s;
        ctx.beginPath();
        ctx.ellipse(0, pomY, 2.8 * sc * s, 2.2 * sc * s, 0, 0, Math.PI * 2);
        ctx.stroke();

        ctx.restore();
      };

      // Disturbed ground / dirt mound at base of cluster
      if (swCount > 1) {
        ctx.fillStyle = "rgba(70,58,40,0.22)";
        drawOrganicBlobAt(
          ctx,
          swX,
          swY + 3 * s,
          (5 + swCount * 2) * s,
          (2.5 + swCount) * s,
          swSeed * 3.7,
          0.2
        );
        ctx.fill();
      }

      // Generate each sword in the cluster
      for (let si = 0; si < swCount; si++) {
        const h1 = swHash(si * 7 + 1);
        const h2 = swHash(si * 7 + 2);
        const h3 = swHash(si * 7 + 3);
        const h4 = swHash(si * 7 + 4);
        const h5 = swHash(si * 7 + 5);

        // Spread positions — fan out from center
        const sAng = (si / swCount) * Math.PI * 1.4 - 0.7 + (h1 - 0.5) * 0.5;
        const dist = swCount === 1 ? 0 : 3 + h2 * swSpread * 0.5;
        const ox = swX + Math.cos(sAng) * dist * s;
        const oy = swY + Math.sin(sAng) * dist * ISO_Y_RATIO * s;

        // Tilt angle — dramatic lean, ±15° to ±35°
        const tiltDir = h3 < 0.5 ? -1 : 1;
        const tilt = tiltDir * (0.25 + h4 * 0.35);

        // Scale variation — 0.75 to 1.15
        const sc = 0.75 + h4 * 0.4;

        // Style index
        const styleIdx = Math.floor(h5 * swStyles.length);

        // Ground insertion — draw BEFORE sword so sword overlaps
        // Cracked ground / disturbed earth at insertion
        ctx.fillStyle = "rgba(55,45,30,0.35)";
        ctx.beginPath();
        ctx.ellipse(
          ox,
          oy + 1 * s,
          5 * sc * s,
          2.5 * sc * s,
          tilt * 0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "rgba(40,32,20,0.25)";
        ctx.beginPath();
        ctx.ellipse(
          ox,
          oy + 1.5 * s,
          3.5 * sc * s,
          1.5 * sc * s,
          tilt * 0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Small radial cracks emanating from insertion point
        ctx.strokeStyle = "rgba(50,40,28,0.2)";
        ctx.lineWidth = 0.5 * s;
        for (let cr = 0; cr < 4; cr++) {
          const crAng = (cr / 4) * Math.PI * 2 + swHash(si * 5 + cr + 50) * 0.8;
          const crLen = (3 + swHash(si * 5 + cr + 55) * 4) * sc;
          ctx.beginPath();
          ctx.moveTo(ox, oy + 1 * s);
          ctx.lineTo(
            ox + Math.cos(crAng) * crLen * s,
            oy + 1 * s + Math.sin(crAng) * crLen * 0.4 * s
          );
          ctx.stroke();
        }

        // Tiny dirt clumps pushed up around insertion
        ctx.fillStyle = "rgba(75,62,42,0.25)";
        for (let dc = 0; dc < 3; dc++) {
          const dcAng = (dc / 3) * Math.PI * 2 + swHash(si * 3 + dc + 60);
          const dcR = (3 + swHash(si * 3 + dc + 63)) * sc;
          ctx.beginPath();
          ctx.ellipse(
            ox + Math.cos(dcAng) * dcR * s,
            oy + 1.5 * s + Math.sin(dcAng) * dcR * 0.35 * s,
            (0.8 + swHash(si * 3 + dc + 66) * 0.6) * s,
            (0.4 + swHash(si * 3 + dc + 66) * 0.3) * s,
            dcAng * 0.3,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }

        drawExcaliburSword(ox, oy, tilt, sc, styleIdx);
      }
      break;
    }
    case "arrow": {
      const arX = screenPos.x;
      const arY = screenPos.y;
      const arSeed = decorX * 59 + decorY * 113;
      const arHash = (n: number) => {
        const h = Math.sin(arSeed * 0.1 + n * 3.217) * 43_758.5453;
        return h - Math.floor(h);
      };

      // 2-6 arrows in a cluster
      const arCount = 2 + Math.floor(arHash(0) * 4.99);
      const arSpread = 5 + arCount * 2.5;

      // Cluster ground shadow — wider for bigger groups
      const arShadR = 10 + arSpread;
      const arShadG = ctx.createRadialGradient(
        arX + 1 * s,
        arY + 3 * s,
        0,
        arX + 1 * s,
        arY + 3 * s,
        arShadR * s
      );
      arShadG.addColorStop(0, "rgba(0,0,0,0.2)");
      arShadG.addColorStop(0.5, "rgba(0,0,0,0.06)");
      arShadG.addColorStop(1, "rgba(0,0,0,0)");
      ctx.fillStyle = arShadG;
      ctx.beginPath();
      ctx.ellipse(
        arX + 1 * s,
        arY + 3 * s,
        arShadR * s,
        arShadR * 0.4 * s,
        0.12,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Disturbed ground at base of cluster
      if (arCount > 2) {
        ctx.fillStyle = "rgba(65,55,38,0.18)";
        drawOrganicBlobAt(
          ctx,
          arX,
          arY + 2 * s,
          (4 + arCount * 1.5) * s,
          (2 + arCount * 0.6) * s,
          arSeed * 2.9,
          0.2
        );
        ctx.fill();
      }

      // Arrowhead shape variants
      const headShapes = ["broadhead", "bodkin", "barbed", "leaf"];

      // Fletching color palettes
      const fletchPalettes = [
        ["#c62828", "#8b1c1c"],
        ["#e0e0e0", "#9e9e9e"],
        ["#2e7d32", "#1b5e20"],
        ["#1565c0", "#0d47a1"],
        ["#ff8f00", "#e65100"],
        ["#6a1b9a", "#4a148c"],
      ];

      // Shaft wood tones
      const shaftTones = [
        { dark: "#4a3020", hi: "#a08060", lit: "#8a6a50", mid: "#6a4a35" },
        { dark: "#3a2818", hi: "#9a7a5a", lit: "#7a5a42", mid: "#5a4030" },
        { dark: "#554030", hi: "#b09a78", lit: "#957a60", mid: "#755a48" },
      ];

      const drawSingleArrow = (
        ox: number,
        oy: number,
        tiltAng: number,
        len: number,
        sc: number,
        headType: string,
        broken: boolean,
        shaftTone: (typeof shaftTones)[0],
        fletchCol: string[]
      ) => {
        ctx.save();
        ctx.translate(ox, oy);
        ctx.rotate(tiltAng);

        const visLen = broken ? len * (0.35 + arHash(77) * 0.3) : len;

        // Shaft shadow
        ctx.strokeStyle = shaftTone.dark;
        ctx.lineWidth = 2 * sc * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(0, 2 * sc * s);
        ctx.lineTo(0, -visLen * sc * s);
        ctx.stroke();

        // Main shaft
        const shG = ctx.createLinearGradient(-1 * sc * s, 0, 1 * sc * s, 0);
        shG.addColorStop(0, shaftTone.dark);
        shG.addColorStop(0.3, shaftTone.mid);
        shG.addColorStop(0.5, shaftTone.lit);
        shG.addColorStop(0.7, shaftTone.mid);
        shG.addColorStop(1, shaftTone.dark);
        ctx.strokeStyle = shG;
        ctx.lineWidth = 1.4 * sc * s;
        ctx.beginPath();
        ctx.moveTo(0, 2 * sc * s);
        ctx.lineTo(0, -visLen * sc * s);
        ctx.stroke();

        // Shaft highlight
        ctx.strokeStyle = `rgba(${shaftTone.hi === "#a08060" ? "160,128,96" : shaftTone.hi === "#9a7a5a" ? "154,122,90" : "176,154,120"},0.25)`;
        ctx.lineWidth = 0.35 * sc * s;
        ctx.beginPath();
        ctx.moveTo(-0.25 * sc * s, 1 * sc * s);
        ctx.lineTo(-0.25 * sc * s, -(visLen - 1) * sc * s);
        ctx.stroke();

        if (broken) {
          // Splintered break at top
          ctx.strokeStyle = shaftTone.lit;
          ctx.lineWidth = 0.5 * sc * s;
          const brkY = -visLen * sc * s;
          ctx.beginPath();
          ctx.moveTo(-0.6 * sc * s, brkY);
          ctx.lineTo(-0.3 * sc * s, brkY - 1.5 * sc * s);
          ctx.moveTo(0.2 * sc * s, brkY);
          ctx.lineTo(0.5 * sc * s, brkY - 1 * sc * s);
          ctx.moveTo(-0.1 * sc * s, brkY);
          ctx.lineTo(0, brkY - 2 * sc * s);
          ctx.stroke();
        }

        if (!broken) {
          // Arrowhead
          const hY = -visLen * sc * s;
          const hW =
            (headType === "broadhead"
              ? 3
              : headType === "leaf"
                ? 2.5
                : headType === "barbed"
                  ? 2.8
                  : 1.5) * sc;
          const hL =
            (headType === "bodkin" ? 6 : headType === "leaf" ? 4.5 : 5) * sc;

          const hdG = ctx.createLinearGradient(-hW * s, hY, hW * s, hY);
          hdG.addColorStop(0, "#555e68");
          hdG.addColorStop(0.3, "#98a0a8");
          hdG.addColorStop(0.5, "#c0c8d0");
          hdG.addColorStop(0.7, "#98a0a8");
          hdG.addColorStop(1, "#454e58");
          ctx.fillStyle = hdG;

          ctx.beginPath();
          if (headType === "broadhead") {
            ctx.moveTo(0, hY - hL * s);
            ctx.lineTo(hW * s, hY + 0.5 * sc * s);
            ctx.lineTo(hW * 0.5 * s, hY);
            ctx.lineTo(0, hY + 1 * sc * s);
            ctx.lineTo(-hW * 0.5 * s, hY);
            ctx.lineTo(-hW * s, hY + 0.5 * sc * s);
          } else if (headType === "bodkin") {
            ctx.moveTo(0, hY - hL * s);
            ctx.lineTo(hW * 0.8 * s, hY + 0.5 * sc * s);
            ctx.lineTo(0, hY + 1 * sc * s);
            ctx.lineTo(-hW * 0.8 * s, hY + 0.5 * sc * s);
          } else if (headType === "barbed") {
            ctx.moveTo(0, hY - hL * s);
            ctx.lineTo(hW * s, hY - 1 * sc * s);
            ctx.lineTo(hW * 0.5 * s, hY + 0.8 * sc * s);
            ctx.lineTo(hW * 0.8 * s, hY + 1.5 * sc * s);
            ctx.lineTo(0, hY + 1 * sc * s);
            ctx.lineTo(-hW * 0.8 * s, hY + 1.5 * sc * s);
            ctx.lineTo(-hW * 0.5 * s, hY + 0.8 * sc * s);
            ctx.lineTo(-hW * s, hY - 1 * sc * s);
          } else {
            ctx.moveTo(0, hY - hL * s);
            ctx.quadraticCurveTo(
              hW * 1.2 * s,
              hY - hL * 0.3 * s,
              hW * 0.8 * s,
              hY + 0.5 * sc * s
            );
            ctx.lineTo(0, hY + 1 * sc * s);
            ctx.lineTo(-hW * 0.8 * s, hY + 0.5 * sc * s);
            ctx.quadraticCurveTo(
              -hW * 1.2 * s,
              hY - hL * 0.3 * s,
              0,
              hY - hL * s
            );
          }
          ctx.closePath();
          ctx.fill();

          // Head outline
          ctx.strokeStyle = "rgba(25,30,40,0.4)";
          ctx.lineWidth = 0.35 * sc * s;
          ctx.stroke();

          // Center ridge
          ctx.strokeStyle = "rgba(200,210,220,0.35)";
          ctx.lineWidth = 0.3 * sc * s;
          ctx.beginPath();
          ctx.moveTo(0, hY - (hL - 0.5) * s);
          ctx.lineTo(0, hY + 0.5 * sc * s);
          ctx.stroke();
        }

        // Fletching — only if shaft is long enough to show it
        if (visLen > 8) {
          const flBase = 0;
          const [fL, fD] = fletchCol;
          for (let fv = 0; fv < 3; fv++) {
            const fAng = (fv / 3) * Math.PI * 2;
            const fx = Math.cos(fAng) * 2.2 * sc * s;
            const fy = Math.sin(fAng) * 0.7 * sc * s;

            ctx.fillStyle = fv < 2 ? fL : fD;
            ctx.beginPath();
            ctx.moveTo(fx * 0.15, (flBase - 0.8 * sc) * s + fy * 0.15);
            ctx.quadraticCurveTo(
              fx,
              (flBase + 1.8 * sc) * s + fy,
              fx * 0.25,
              (flBase + 4.5 * sc) * s + fy * 0.25
            );
            ctx.quadraticCurveTo(
              0,
              (flBase + 2.5 * sc) * s,
              fx * 0.15,
              (flBase - 0.8 * sc) * s + fy * 0.15
            );
            ctx.fill();

            // Feather spine
            ctx.strokeStyle = "rgba(0,0,0,0.18)";
            ctx.lineWidth = 0.25 * sc * s;
            ctx.beginPath();
            ctx.moveTo(fx * 0.12, (flBase - 0.3 * sc) * s + fy * 0.12);
            ctx.lineTo(fx * 0.2, (flBase + 4 * sc) * s + fy * 0.2);
            ctx.stroke();
          }

          // Nock at very bottom
          ctx.fillStyle = shaftTone.dark;
          ctx.beginPath();
          ctx.ellipse(
            0,
            (flBase + 5 * sc) * s,
            0.8 * sc * s,
            0.5 * sc * s,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }

        ctx.restore();
      };

      // Generate each arrow in the cluster
      for (let ai = 0; ai < arCount; ai++) {
        const h1 = arHash(ai * 9 + 1);
        const h2 = arHash(ai * 9 + 2);
        const h3 = arHash(ai * 9 + 3);
        const h4 = arHash(ai * 9 + 4);
        const h5 = arHash(ai * 9 + 5);
        const h6 = arHash(ai * 9 + 6);
        const h7 = arHash(ai * 9 + 7);

        // Fan positions around center — tight cluster
        const ang = (ai / arCount) * Math.PI * 1.4 - 0.7 + (h1 - 0.5) * 0.5;
        const dist = 2 + h2 * arSpread * 0.5;
        const ox = arX + Math.cos(ang) * dist * s;
        const oy = arY + Math.sin(ang) * dist * ISO_Y_RATIO * s;

        // Tilt — steep angles (jabbed into ground), between 0.15 and 0.7 magnitude
        const tiltDir = h3 < 0.5 ? -1 : 1;
        const tilt = tiltDir * (0.15 + h4 * 0.55);

        // Length 14-26
        const len = 14 + h5 * 12;

        // Scale 0.7-1.15
        const sc = 0.7 + h6 * 0.45;

        // Head shape
        const headIdx = Math.floor(h7 * headShapes.length);
        const headType = headShapes[headIdx];

        // ~20% chance of being a broken shaft
        const broken = h1 < 0.2;

        // Shaft tone & fletching
        const shaftTone = shaftTones[Math.floor(h2 * shaftTones.length)];
        const fletchCol =
          fletchPalettes[Math.floor(h3 * fletchPalettes.length)];

        drawSingleArrow(
          ox,
          oy,
          tilt,
          len,
          sc,
          headType,
          broken,
          shaftTone,
          fletchCol
        );

        // Dirt ring at insertion
        ctx.fillStyle = "rgba(55,45,30,0.28)";
        ctx.beginPath();
        ctx.ellipse(
          ox,
          oy + 2 * sc * s,
          2.5 * sc * s,
          1.2 * sc * s,
          tilt * 0.25,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "rgba(40,32,20,0.15)";
        ctx.beginPath();
        ctx.ellipse(
          ox,
          oy + 2.5 * sc * s,
          1.5 * sc * s,
          0.7 * sc * s,
          tilt * 0.25,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Scatter small dirt clods around cluster
      ctx.fillStyle = "rgba(75,62,42,0.15)";
      for (let cd = 0; cd < Math.min(arCount, 4); cd++) {
        const ca = arHash(cd * 3 + 50) * Math.PI * 2;
        const cr = (3 + arHash(cd * 3 + 51) * arSpread * 0.3) * s;
        ctx.beginPath();
        ctx.ellipse(
          arX + Math.cos(ca) * cr,
          arY + Math.sin(ca) * cr * ISO_Y_RATIO + 2 * s,
          (0.8 + arHash(cd * 3 + 52) * 0.8) * s,
          (0.4 + arHash(cd * 3 + 52) * 0.3) * s,
          ca * 0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }
    case "skeleton": {
      // Lying-down isometric skeleton — rotated along the ground plane
      // Uses ctx.rotate to orient the body along an isometric ground axis
      const bL = "#f0ebe3";
      const bM = "#ddd5c8";
      const bD = "#b8ad9e";
      const bS = "#8a7f72";
      const bSk = "#2d2420";
      const cx = screenPos.x;
      const cy = screenPos.y;
      const sv = variant % 4;

      // Each variant gets a different ground-plane angle + pose
      const skelAngles = [0.3, -0.35, 0.55, -0.15];
      const bodyAng = skelAngles[sv];

      // Ground shadow — rotated ellipse matching body orientation
      const skelShad = ctx.createRadialGradient(
        cx,
        cy + 2 * s,
        0,
        cx,
        cy + 2 * s,
        24 * s
      );
      skelShad.addColorStop(0, "rgba(0,0,0,0.18)");
      skelShad.addColorStop(0.6, "rgba(0,0,0,0.06)");
      skelShad.addColorStop(1, "rgba(0,0,0,0)");
      ctx.fillStyle = skelShad;
      ctx.beginPath();
      ctx.ellipse(cx, cy + 2 * s, 26 * s, 8 * s, bodyAng, 0, Math.PI * 2);
      ctx.fill();

      ctx.save();
      ctx.translate(cx, cy);
      ctx.rotate(bodyAng);

      // Helpers for 3D bone drawing in local rotated coordinates
      // drawBone: straight bone segment with shadow + lit dual-stroke
      const drawBone = (
        x1: number,
        y1: number,
        x2: number,
        y2: number,
        w: number,
        shd: string,
        lit: string
      ) => {
        ctx.strokeStyle = shd;
        ctx.lineWidth = w * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(x1 * s, y1 * s);
        ctx.lineTo(x2 * s, y2 * s);
        ctx.stroke();
        ctx.strokeStyle = lit;
        ctx.lineWidth = w * 0.55 * s;
        ctx.beginPath();
        ctx.moveTo(x1 * s, (y1 - 0.5) * s);
        ctx.lineTo(x2 * s, (y2 - 0.5) * s);
        ctx.stroke();
      };
      // drawCBone: curved bone with quadratic bezier
      const drawCBone = (
        x1: number,
        y1: number,
        cpx: number,
        cpy: number,
        x2: number,
        y2: number,
        w: number,
        shd: string,
        lit: string
      ) => {
        ctx.strokeStyle = shd;
        ctx.lineWidth = w * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(x1 * s, y1 * s);
        ctx.quadraticCurveTo(cpx * s, cpy * s, x2 * s, y2 * s);
        ctx.stroke();
        ctx.strokeStyle = lit;
        ctx.lineWidth = w * 0.55 * s;
        ctx.beginPath();
        ctx.moveTo(x1 * s, (y1 - 0.5) * s);
        ctx.quadraticCurveTo(cpx * s, (cpy - 0.5) * s, x2 * s, (y2 - 0.5) * s);
        ctx.stroke();
      };
      // drawJoint: small 3D joint circle
      const drawJoint = (jx: number, jy: number, jr: number) => {
        ctx.fillStyle = bS;
        ctx.beginPath();
        ctx.ellipse(jx * s, jy * s, jr * s, jr * 0.6 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = bM;
        ctx.beginPath();
        ctx.ellipse(
          (jx + 0.3) * s,
          (jy - 0.3) * s,
          jr * 0.6 * s,
          jr * 0.35 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      };

      // === LOCAL COORDINATES ===
      // X axis: along the body (negative = head, positive = feet)
      // Y axis: perpendicular to body (positive = one side)
      // Y values kept small for isometric ground-plane flatness

      // === LEGS (drawn first — feet end, positive X) ===
      if (sv === 0) {
        // Straight legs, slight spread
        drawBone(9, -2, 19, -3.5, 2.2, bS, bD);
        drawBone(19, -3.5, 28, -2.5, 1.8, bS, bM);
        drawBone(9, 2, 19, 3.5, 2.2, bS, bL);
        drawBone(19, 3.5, 28, 2.5, 1.8, bD, bL);
        drawJoint(19, -3.5, 1.4);
        drawJoint(19, 3.5, 1.4);
        // Feet
        drawBone(28, -2.5, 30.5, -3.5, 0.8, bS, bD);
        drawBone(28, -2.5, 30.5, -1.5, 0.8, bS, bM);
        drawBone(28, 2.5, 30.5, 3.5, 0.8, bD, bL);
        drawBone(28, 2.5, 30.5, 1.5, 0.8, bD, bL);
      } else if (sv === 1) {
        // Legs apart, splayed
        drawBone(9, -2, 20, -5.5, 2.2, bS, bD);
        drawBone(20, -5.5, 29, -5, 1.8, bS, bM);
        drawBone(9, 2, 20, 5.5, 2.2, bS, bL);
        drawBone(20, 5.5, 29, 5, 1.8, bD, bL);
        drawJoint(20, -5.5, 1.4);
        drawJoint(20, 5.5, 1.4);
        drawBone(29, -5, 31.5, -6, 0.8, bS, bD);
        drawBone(29, -5, 31.5, -4, 0.8, bS, bM);
        drawBone(29, 5, 31.5, 6, 0.8, bD, bL);
        drawBone(29, 5, 31.5, 4, 0.8, bD, bL);
      } else if (sv === 2) {
        // One leg bent back
        drawBone(9, -2, 17, -5, 2.2, bS, bD);
        drawBone(17, -5, 14, -9, 1.8, bS, bM);
        drawBone(9, 2, 19, 3.5, 2.2, bS, bL);
        drawBone(19, 3.5, 28, 2.5, 1.8, bD, bL);
        drawJoint(17, -5, 1.4);
        drawJoint(19, 3.5, 1.4);
        drawBone(28, 2.5, 30.5, 3.5, 0.8, bD, bL);
        drawBone(28, 2.5, 30.5, 1.5, 0.8, bD, bL);
      } else {
        // One leg extended, one shorter (staggered)
        drawBone(9, -2, 21, -4, 2.2, bS, bD);
        drawBone(21, -4, 31, -3, 1.8, bS, bM);
        drawBone(9, 2, 16, 4.5, 2.2, bS, bL);
        drawBone(16, 4.5, 23, 5.5, 1.8, bD, bL);
        drawJoint(21, -4, 1.4);
        drawJoint(16, 4.5, 1.4);
        drawBone(31, -3, 33.5, -4, 0.8, bS, bD);
        drawBone(31, -3, 33.5, -2, 0.8, bS, bM);
        drawBone(23, 5.5, 25.5, 6.5, 0.8, bD, bL);
        drawBone(23, 5.5, 25.5, 4.5, 0.8, bD, bL);
      }

      // === PELVIS ===
      const plG = ctx.createRadialGradient(
        8 * s,
        -0.3 * s,
        0,
        8 * s,
        0,
        4.5 * s
      );
      plG.addColorStop(0, bM);
      plG.addColorStop(0.5, bD);
      plG.addColorStop(1, bS);
      ctx.fillStyle = plG;
      ctx.beginPath();
      ctx.ellipse(8 * s, 0, 4.5 * s, 3 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(45,36,32,0.25)";
      ctx.beginPath();
      ctx.ellipse(8 * s, 0, 2 * s, 1.2 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = bS;
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.ellipse(8 * s, 0, 4.5 * s, 3 * s, 0, 0, Math.PI * 2);
      ctx.stroke();

      // === SPINE ===
      for (let sp = 0; sp < 7; sp++) {
        const spx = 6 - sp * 2.5;
        ctx.fillStyle = bS;
        ctx.beginPath();
        ctx.ellipse(spx * s, 0.2 * s, 1.2 * s, 0.8 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = sp % 2 === 0 ? bM : bD;
        ctx.beginPath();
        ctx.ellipse((spx + 0.15) * s, 0, 0.9 * s, 0.6 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = bL;
        ctx.beginPath();
        ctx.ellipse(
          (spx + 0.25) * s,
          -0.25 * s,
          0.5 * s,
          0.3 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // === RIBS ===
      for (let r = 0; r < 4; r++) {
        const rx = 4 - r * 2.5;
        const ribLen = 5 - r * 0.3;
        // Top rib
        ctx.strokeStyle = bS;
        ctx.lineWidth = 1.5 * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(rx * s, 0);
        ctx.quadraticCurveTo(
          rx * s,
          -ribLen * 0.5 * s,
          (rx + 1) * s,
          -ribLen * s
        );
        ctx.stroke();
        ctx.strokeStyle = bD;
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo((rx + 0.2) * s, 0);
        ctx.quadraticCurveTo(
          (rx + 0.2) * s,
          (-ribLen * 0.5 - 0.3) * s,
          (rx + 1.2) * s,
          (-ribLen - 0.3) * s
        );
        ctx.stroke();
        // Bottom rib
        ctx.strokeStyle = bS;
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(rx * s, 0);
        ctx.quadraticCurveTo(
          rx * s,
          ribLen * 0.5 * s,
          (rx + 1) * s,
          ribLen * s
        );
        ctx.stroke();
        ctx.strokeStyle = bL;
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo((rx + 0.2) * s, 0);
        ctx.quadraticCurveTo(
          (rx + 0.2) * s,
          (ribLen * 0.5 + 0.3) * s,
          (rx + 1.2) * s,
          (ribLen + 0.3) * s
        );
        ctx.stroke();
      }

      // === COLLARBONES ===
      drawCBone(-9, 0, -10, -2.5, -11, -4.5, 1.8, bS, bD);
      drawCBone(-9, 0, -10, 2.5, -11, 4.5, 1.8, bS, bL);

      // === ARMS — variant poses ===
      if (sv === 0) {
        // Arms at sides
        drawCBone(-11, -4.5, -7, -7, -3, -8.5, 1.8, bS, bD);
        drawCBone(-3, -8.5, 2, -9, 5, -7.5, 1.4, bS, bM);
        drawCBone(-11, 4.5, -7, 7, -3, 8.5, 1.8, bS, bL);
        drawCBone(-3, 8.5, 2, 9, 5, 7.5, 1.4, bD, bL);
        drawJoint(-3, -8.5, 1.2);
        drawJoint(-3, 8.5, 1.2);
        for (let f = -1; f <= 1; f++) {
          drawBone(5, -7.5 + f, 7, -7.8 + f * 1.3, 0.5, bS, bM);
          drawBone(5, 7.5 + f, 7, 7.8 + f * 1.3, 0.5, bD, bL);
        }
      } else if (sv === 1) {
        // Left arm above head, right at side
        drawCBone(-11, -4.5, -14, -7, -18, -5.5, 1.8, bS, bD);
        drawCBone(-18, -5.5, -22, -3, -24, -1, 1.4, bS, bM);
        drawCBone(-11, 4.5, -7, 7, -3, 8.5, 1.8, bS, bL);
        drawCBone(-3, 8.5, 2, 9, 5, 7.5, 1.4, bD, bL);
        drawJoint(-18, -5.5, 1.2);
        drawJoint(-3, 8.5, 1.2);
        for (let f = -1; f <= 1; f++) {
          drawBone(-24, -1 + f * 0.8, -26, -0.5 + f * 1, 0.5, bS, bM);
          drawBone(5, 7.5 + f, 7, 7.8 + f * 1.3, 0.5, bD, bL);
        }
      } else if (sv === 2) {
        // Arms crossed on chest
        drawCBone(-11, -4.5, -7, -2, -4, 2, 1.8, bS, bD);
        drawCBone(-4, 2, -2, 3.5, -5, 4.5, 1.4, bS, bM);
        drawCBone(-11, 4.5, -7, 2, -4, -2, 1.8, bS, bL);
        drawCBone(-4, -2, -2, -3.5, -5, -4.5, 1.4, bD, bL);
        drawJoint(-4, 2, 1.2);
        drawJoint(-4, -2, 1.2);
      } else {
        // One arm reaching out desperately, one at side
        drawCBone(-11, -4.5, -16, -8, -22, -7, 1.8, bS, bD);
        drawCBone(-22, -7, -27, -4, -30, -2, 1.4, bS, bM);
        drawCBone(-11, 4.5, -7, 7.5, -2, 9, 1.8, bS, bL);
        drawCBone(-2, 9, 3, 9, 6, 7.5, 1.4, bD, bL);
        drawJoint(-22, -7, 1.2);
        drawJoint(-2, 9, 1.2);
        for (let f = -1; f <= 1; f++) {
          drawBone(-30, -2 + f * 0.8, -32, -1.5 + f * 1.1, 0.5, bS, bM);
          drawBone(6, 7.5 + f, 8, 7.8 + f * 1.2, 0.5, bD, bL);
        }
      }

      // === SKULL (drawn last — head end, negative X) ===
      const skx = -15;

      // Skull shadow
      ctx.fillStyle = bS;
      ctx.beginPath();
      ctx.ellipse(skx * s, 0.7 * s, 6 * s, 4 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Main cranium with gradient — circle in local coords becomes isometric on screen
      const skGr = ctx.createRadialGradient(
        (skx + 1) * s,
        -1 * s,
        0.5 * s,
        skx * s,
        0,
        5.5 * s
      );
      skGr.addColorStop(0, bL);
      skGr.addColorStop(0.4, bM);
      skGr.addColorStop(0.8, bD);
      skGr.addColorStop(1, bS);
      ctx.fillStyle = skGr;
      ctx.beginPath();
      ctx.ellipse(skx * s, 0, 5.5 * s, 3.8 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Skull highlight
      ctx.fillStyle = "rgba(255,255,255,0.18)";
      ctx.beginPath();
      ctx.ellipse(
        (skx + 1.2) * s,
        -1.3 * s,
        2.5 * s,
        1.5 * s,
        0.2,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Brow ridge
      ctx.fillStyle = bD;
      ctx.beginPath();
      ctx.ellipse(skx * s, 0.3 * s, 4 * s, 0.6 * s, 0, 0, Math.PI);
      ctx.fill();

      // Eye sockets
      for (const ey of [
        { edge: bS, yo: -1.3 },
        { edge: bD, yo: 1.3 },
      ]) {
        const eGr = ctx.createRadialGradient(
          skx * s,
          ey.yo * s,
          0.15 * s,
          skx * s,
          ey.yo * s,
          1.6 * s
        );
        eGr.addColorStop(0, "#0d0a08");
        eGr.addColorStop(0.5, bSk);
        eGr.addColorStop(1, ey.edge);
        ctx.fillStyle = eGr;
        ctx.beginPath();
        ctx.ellipse(skx * s, ey.yo * s, 1.6 * s, 1.2 * s, 0, 0, Math.PI * 2);
        ctx.fill();
      }

      // Nasal cavity
      ctx.fillStyle = bSk;
      ctx.beginPath();
      ctx.moveTo((skx + 1.5) * s, 0);
      ctx.lineTo((skx + 2.8) * s, -0.6 * s);
      ctx.lineTo((skx + 2.8) * s, 0.5 * s);
      ctx.closePath();
      ctx.fill();

      // Jaw
      const jawG = ctx.createLinearGradient(skx * s, -3 * s, skx * s, 3 * s);
      jawG.addColorStop(0, bS);
      jawG.addColorStop(0.35, bD);
      jawG.addColorStop(0.65, bM);
      jawG.addColorStop(1, bS);
      ctx.fillStyle = jawG;
      ctx.beginPath();
      ctx.moveTo((skx + 1.5) * s, -3.2 * s);
      ctx.quadraticCurveTo((skx + 3.5) * s, -3.5 * s, (skx + 4) * s, -1.5 * s);
      ctx.lineTo((skx + 4) * s, 1.5 * s);
      ctx.quadraticCurveTo((skx + 3.5) * s, 3.5 * s, (skx + 1.5) * s, 3.2 * s);
      ctx.closePath();
      ctx.fill();

      // Teeth
      ctx.fillStyle = bL;
      ctx.strokeStyle = bS;
      ctx.lineWidth = 0.2 * s;
      for (let t = -2; t <= 2; t++) {
        ctx.beginPath();
        ctx.rect((skx + 2.8) * s, (t * 0.9 - 0.3) * s, 0.7 * s, 0.6 * s);
        ctx.fill();
        ctx.stroke();
      }

      // Skull outline
      ctx.strokeStyle = bS;
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.ellipse(skx * s, 0, 5.5 * s, 3.8 * s, 0, 0, Math.PI * 2);
      ctx.stroke();

      // Suture lines
      ctx.strokeStyle = "rgba(138,127,114,0.25)";
      ctx.lineWidth = 0.3 * s;
      ctx.beginPath();
      ctx.moveTo((skx - 3) * s, 0);
      ctx.lineTo((skx - 0.5) * s, 0);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo((skx - 1.5) * s, -2.5 * s);
      ctx.quadraticCurveTo((skx - 2.5) * s, 0, (skx - 1.5) * s, 2.5 * s);
      ctx.stroke();

      ctx.restore();
      break;
    }
    case "barrel": {
      const brx = screenPos.x,
        bry = screenPos.y;
      // Shadow
      ctx.fillStyle = "rgba(0,0,0,0.18)";
      ctx.beginPath();
      ctx.ellipse(brx + 1, bry + 5 * s, 10 * s, 5 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      // Barrel body (curved cylinder front)
      const brG = ctx.createLinearGradient(brx - 8 * s, bry, brx + 8 * s, bry);
      brG.addColorStop(0, "#5D4037");
      brG.addColorStop(0.35, "#8D6E63");
      brG.addColorStop(0.7, "#6D4C41");
      brG.addColorStop(1, "#4E342E");
      ctx.fillStyle = brG;
      ctx.beginPath();
      ctx.moveTo(brx - 6 * s, bry - 12 * s);
      ctx.lineTo(brx + 6 * s, bry - 12 * s);
      ctx.quadraticCurveTo(brx + 9 * s, bry - 5 * s, brx + 8 * s, bry + 3 * s);
      ctx.lineTo(brx - 8 * s, bry + 3 * s);
      ctx.quadraticCurveTo(brx - 9 * s, bry - 5 * s, brx - 6 * s, bry - 12 * s);
      ctx.closePath();
      ctx.fill();
      // Metal hoops
      ctx.strokeStyle = "#4A3525";
      ctx.lineWidth = 1.5 * s;
      for (const hoopY of [bry - 8 * s, bry - 2 * s]) {
        const hw2 = hoopY < bry - 5 * s ? 7 * s : 7.5 * s;
        ctx.beginPath();
        ctx.moveTo(brx - hw2, hoopY);
        ctx.lineTo(brx + hw2, hoopY);
        ctx.stroke();
      }
      // Barrel top (ellipse)
      ctx.fillStyle = "#5D4037";
      ctx.beginPath();
      ctx.ellipse(brx, bry - 12 * s, 6 * s, 3 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      // Top rim highlight
      ctx.strokeStyle = "#8D6E63";
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.ellipse(brx, bry - 12 * s, 6 * s, 3 * s, 0, 0, Math.PI * 2);
      ctx.stroke();
      // Wood grain lines
      ctx.strokeStyle = "rgba(40,25,15,0.15)";
      ctx.lineWidth = 0.5 * s;
      for (let wg = 0; wg < 3; wg++) {
        const wgx = brx - 3 * s + wg * 3 * s;
        ctx.beginPath();
        ctx.moveTo(wgx, bry - 11 * s);
        ctx.lineTo(wgx + 0.5 * s, bry + 1 * s);
        ctx.stroke();
      }
      break;
    }
    case "fence": {
      const fx = screenPos.x;
      const fy = screenPos.y;
      const fv = variant % 4;
      const fLen = 34 * s;

      // Ground shadow
      ctx.fillStyle = "rgba(0,0,0,0.15)";
      ctx.beginPath();
      ctx.ellipse(
        fx + 2 * s,
        fy + 3 * s,
        fLen * 0.52,
        4 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      if (fv === 0) {
        // === STRAIGHT BRICK WALL — single isometric wall segment along right axis ===
        const wallH = 16 * s;
        const wallLen = fLen * 0.88;
        const d = 5 * s;

        const faceCol = "#747468";
        const topCol = "#9a9a8c";
        const capCol = "#8a8a7e";
        const mort = "rgba(30,28,22,0.42)";

        const x1 = fx - wallLen * 0.5;
        const y1 = fy + wallLen * 0.25;
        const x2 = fx + wallLen * 0.5;
        const y2 = fy - wallLen * 0.25;

        // Left end cap (faces left → lit)
        drawBrickFace(
          ctx,
          x1 - d,
          y1 - d * 0.5,
          x1,
          y1,
          wallH,
          capCol,
          "rgba(40,38,30,0.35)",
          s,
          5,
          2
        );

        // Top face
        ctx.fillStyle = topCol;
        ctx.beginPath();
        ctx.moveTo(x1, y1 - wallH);
        ctx.lineTo(x2, y2 - wallH);
        ctx.lineTo(x2 - d, y2 - d * 0.5 - wallH);
        ctx.lineTo(x1 - d, y1 - d * 0.5 - wallH);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = "rgba(30,28,22,0.18)";
        ctx.lineWidth = 0.5 * s;
        for (let i = 1; i < 7; i++) {
          const t = i / 7;
          const mx = x1 + t * (x2 - x1);
          const my = y1 + t * (y2 - y1);
          ctx.beginPath();
          ctx.moveTo(mx, my - wallH);
          ctx.lineTo(mx - d, my - d * 0.5 - wallH);
          ctx.stroke();
        }

        // Front face with bricks
        drawBrickFace(ctx, x1, y1, x2, y2, wallH, faceCol, mort, s, 5, 7);
      } else if (fv === 1) {
        // === L-SHAPED CORNER BRICK WALL — proper 2:1 isometric with running bond ===
        const wallH = 20 * s;
        const armR = fLen * 0.44;
        const armL = fLen * 0.44;
        const d = 5 * s;

        const darkFace = "#686860";
        const litFace = "#8a8a7e";
        const topCol = "#9e9e90";
        const darkMort = "rgba(30,28,22,0.45)";
        const litMort = "rgba(40,38,30,0.35)";
        const topMort = "rgba(30,28,22,0.2)";

        const rEx = fx + armR;
        const rEy = fy - armR * 0.5;
        const lEx = fx - armL;
        const lEy = fy - armL * 0.5;

        // Right arm end cap
        ctx.fillStyle = darkFace;
        ctx.beginPath();
        ctx.moveTo(rEx, rEy);
        ctx.lineTo(rEx - d, rEy - d * 0.5);
        ctx.lineTo(rEx - d, rEy - d * 0.5 - wallH);
        ctx.lineTo(rEx, rEy - wallH);
        ctx.closePath();
        ctx.fill();

        // Left arm end cap
        ctx.fillStyle = litFace;
        ctx.beginPath();
        ctx.moveTo(lEx, lEy);
        ctx.lineTo(lEx + d, lEy - d * 0.5);
        ctx.lineTo(lEx + d, lEy - d * 0.5 - wallH);
        ctx.lineTo(lEx, lEy - wallH);
        ctx.closePath();
        ctx.fill();

        // Right arm top face
        ctx.fillStyle = topCol;
        ctx.beginPath();
        ctx.moveTo(fx, fy - wallH);
        ctx.lineTo(rEx, rEy - wallH);
        ctx.lineTo(rEx - d, rEy - d * 0.5 - wallH);
        ctx.lineTo(fx - d, fy - d * 0.5 - wallH);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = topMort;
        ctx.lineWidth = 0.5 * s;
        for (let i = 1; i < 5; i++) {
          const t = i / 5;
          ctx.beginPath();
          ctx.moveTo(fx + t * armR, fy - t * armR * 0.5 - wallH);
          ctx.lineTo(fx + t * armR - d, fy - t * armR * 0.5 - d * 0.5 - wallH);
          ctx.stroke();
        }

        // Left arm top face
        ctx.fillStyle = topCol;
        ctx.beginPath();
        ctx.moveTo(lEx, lEy - wallH);
        ctx.lineTo(fx, fy - wallH);
        ctx.lineTo(fx + d, fy - d * 0.5 - wallH);
        ctx.lineTo(lEx + d, lEy - d * 0.5 - wallH);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = topMort;
        for (let i = 1; i < 5; i++) {
          const t = i / 5;
          ctx.beginPath();
          ctx.moveTo(fx - t * armL, fy - t * armL * 0.5 - wallH);
          ctx.lineTo(fx - t * armL + d, fy - t * armL * 0.5 - d * 0.5 - wallH);
          ctx.stroke();
        }

        // Right arm front face (faces left → darker) with bricks
        drawBrickFace(
          ctx,
          fx,
          fy,
          rEx,
          rEy,
          wallH,
          darkFace,
          darkMort,
          s,
          6,
          5
        );

        // Left arm front face (faces right → lighter) with bricks
        drawBrickFace(ctx, lEx, lEy, fx, fy, wallH, litFace, litMort, s, 6, 5);

        // Corner edge highlight
        ctx.strokeStyle = "rgba(180,178,165,0.3)";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(fx, fy);
        ctx.lineTo(fx, fy - wallH);
        ctx.stroke();

        // Moss accents at base
        ctx.fillStyle = "rgba(76,100,60,0.25)";
        for (let m = 0; m < 3; m++) {
          const mt = 0.2 + m * 0.3;
          const mx = fx + mt * armR;
          const my = fy - mt * armR * 0.5 + 1 * s;
          ctx.beginPath();
          ctx.ellipse(mx, my, 2 * s, 1 * s, 0, 0, Math.PI * 2);
          ctx.fill();
        }
      } else if (fv === 2) {
        // === TALL BRICK WALL WITH BATTLEMENTS — castle-style defense ===
        const wallH = 26 * s;
        const wallLen = fLen * 0.88;
        const d = 5 * s;
        const merlonH = 6 * s;

        const faceCol = "#6e6e64";
        const topCol = "#9a9a8c";
        const capCol = "#8a8a7e";
        const mort = "rgba(30,28,22,0.42)";

        const x1 = fx - wallLen * 0.5;
        const y1 = fy + wallLen * 0.25;
        const x2 = fx + wallLen * 0.5;
        const y2 = fy - wallLen * 0.25;

        // Left end cap
        drawBrickFace(
          ctx,
          x1 - d,
          y1 - d * 0.5,
          x1,
          y1,
          wallH,
          capCol,
          "rgba(40,38,30,0.35)",
          s,
          7,
          2
        );

        // Top face
        ctx.fillStyle = topCol;
        ctx.beginPath();
        ctx.moveTo(x1, y1 - wallH);
        ctx.lineTo(x2, y2 - wallH);
        ctx.lineTo(x2 - d, y2 - d * 0.5 - wallH);
        ctx.lineTo(x1 - d, y1 - d * 0.5 - wallH);
        ctx.closePath();
        ctx.fill();

        // Front face with bricks
        drawBrickFace(ctx, x1, y1, x2, y2, wallH, faceCol, mort, s, 8, 7);

        // Battlements (merlons along the top)
        const merlonCount = 4;
        for (let m = 0; m < merlonCount; m++) {
          const t1 = (m * 2) / (merlonCount * 2 - 1);
          const t2 = (m * 2 + 0.8) / (merlonCount * 2 - 1);
          const mx1 = x1 + t1 * (x2 - x1);
          const my1 = y1 + t1 * (y2 - y1) - wallH;
          const mx2 = x1 + t2 * (x2 - x1);
          const my2 = y1 + t2 * (y2 - y1) - wallH;

          // Merlon front face
          drawBrickFace(
            ctx,
            mx1,
            my1,
            mx2,
            my2,
            merlonH,
            faceCol,
            mort,
            s,
            2,
            2
          );

          // Merlon top
          ctx.fillStyle = topCol;
          ctx.beginPath();
          ctx.moveTo(mx1, my1 - merlonH);
          ctx.lineTo(mx2, my2 - merlonH);
          ctx.lineTo(mx2 - d, my2 - d * 0.5 - merlonH);
          ctx.lineTo(mx1 - d, my1 - d * 0.5 - merlonH);
          ctx.closePath();
          ctx.fill();

          // Merlon side (depth face)
          ctx.fillStyle = "#5e5e56";
          ctx.beginPath();
          ctx.moveTo(mx2, my2);
          ctx.lineTo(mx2 - d, my2 - d * 0.5);
          ctx.lineTo(mx2 - d, my2 - d * 0.5 - merlonH);
          ctx.lineTo(mx2, my2 - merlonH);
          ctx.closePath();
          ctx.fill();
        }
      } else {
        // === BRICK WALL WITH PILLARS — wall segment with brick pillar posts ===
        const wallH = 12 * s;
        const pillarH = 22 * s;
        const wallLen = fLen * 0.78;
        const d = 5 * s;
        const pillarW = 5 * s;

        const faceCol = "#747468";
        const darkCol = "#5e5e56";
        const litCol = "#8a8a7e";
        const topCol = "#9a9a8c";
        const mort = "rgba(30,28,22,0.42)";
        const litMort = "rgba(40,38,30,0.35)";

        const x1 = fx - wallLen * 0.5;
        const y1 = fy + wallLen * 0.25;
        const x2 = fx + wallLen * 0.5;
        const y2 = fy - wallLen * 0.25;

        // Wall top face
        ctx.fillStyle = topCol;
        ctx.beginPath();
        ctx.moveTo(x1, y1 - wallH);
        ctx.lineTo(x2, y2 - wallH);
        ctx.lineTo(x2 - d, y2 - d * 0.5 - wallH);
        ctx.lineTo(x1 - d, y1 - d * 0.5 - wallH);
        ctx.closePath();
        ctx.fill();

        // Wall front face
        drawBrickFace(ctx, x1, y1, x2, y2, wallH, faceCol, mort, s, 4, 7);

        // Pillar at each end
        for (let pi = 0; pi < 2; pi++) {
          const px = pi === 0 ? x1 : x2;
          const py = pi === 0 ? y1 : y2;
          const pw = pillarW;

          // Pillar right face (lit)
          drawBrickFace(
            ctx,
            px,
            py,
            px + pw,
            py - pw * 0.5,
            pillarH,
            litCol,
            litMort,
            s,
            6,
            2
          );

          // Pillar left face (dark)
          drawBrickFace(
            ctx,
            px - pw,
            py - pw * 0.5,
            px,
            py,
            pillarH,
            darkCol,
            mort,
            s,
            6,
            2
          );

          // Pillar top
          ctx.fillStyle = topCol;
          ctx.beginPath();
          ctx.moveTo(px, py - pillarH);
          ctx.lineTo(px + pw, py - pw * 0.5 - pillarH);
          ctx.lineTo(px, py - pw - pillarH);
          ctx.lineTo(px - pw, py - pw * 0.5 - pillarH);
          ctx.closePath();
          ctx.fill();

          // Cap stone (slightly wider)
          const capOff = 1.5 * s;
          const capH = 2 * s;
          ctx.fillStyle = "#a0a090";
          ctx.beginPath();
          ctx.moveTo(px, py - pillarH + capH);
          ctx.lineTo(
            px + pw + capOff,
            py - pw * 0.5 - capOff * 0.5 - pillarH + capH
          );
          ctx.lineTo(px, py - pw - capOff - pillarH + capH);
          ctx.lineTo(
            px - pw - capOff,
            py - pw * 0.5 - capOff * 0.5 - pillarH + capH
          );
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = "#b0b0a0";
          ctx.beginPath();
          ctx.moveTo(px, py - pillarH);
          ctx.lineTo(px + pw + capOff, py - pw * 0.5 - capOff * 0.5 - pillarH);
          ctx.lineTo(px, py - pw - capOff - pillarH);
          ctx.lineTo(px - pw - capOff, py - pw * 0.5 - capOff * 0.5 - pillarH);
          ctx.closePath();
          ctx.fill();
        }
      }
      break;
    }
    case "gravestone": {
      const gsx = screenPos.x,
        gsy = screenPos.y;
      const gsD = 3 * s;

      // Shadow
      ctx.fillStyle = "rgba(0,0,0,0.18)";
      ctx.beginPath();
      ctx.ellipse(gsx + 1, gsy + 4 * s, 10 * s, 5 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Side face (depth) — visible right edge of the slab
      ctx.fillStyle = "#555555";
      ctx.beginPath();
      ctx.moveTo(gsx + 7 * s, gsy + 2 * s);
      ctx.lineTo(gsx + 7 * s + gsD, gsy + 2 * s - gsD * 0.5);
      ctx.lineTo(gsx + 7 * s + gsD, gsy - 10 * s - gsD * 0.5);
      ctx.arc(
        gsx + gsD,
        gsy - 10 * s - gsD * 0.5,
        7 * s,
        0,
        -Math.PI * 0.5,
        true
      );
      ctx.lineTo(gsx + gsD, gsy - 17 * s - gsD * 0.5);
      ctx.lineTo(gsx, gsy - 17 * s);
      ctx.arc(gsx, gsy - 10 * s, 7 * s, -Math.PI * 0.5, 0);
      ctx.closePath();
      ctx.fill();

      // Front face
      const gsG = ctx.createLinearGradient(gsx - 7 * s, gsy, gsx + 7 * s, gsy);
      gsG.addColorStop(0, "#6E6E6E");
      gsG.addColorStop(0.5, "#8A8A8A");
      gsG.addColorStop(1, "#757575");
      ctx.fillStyle = gsG;
      ctx.beginPath();
      ctx.moveTo(gsx - 7 * s, gsy + 2 * s);
      ctx.lineTo(gsx - 7 * s, gsy - 10 * s);
      ctx.arc(gsx, gsy - 10 * s, 7 * s, Math.PI, 0);
      ctx.lineTo(gsx + 7 * s, gsy + 2 * s);
      ctx.closePath();
      ctx.fill();

      // Top edge (curved isometric thickness)
      ctx.fillStyle = "#9E9E9E";
      ctx.beginPath();
      ctx.arc(gsx, gsy - 10 * s, 7 * s, Math.PI, 0);
      ctx.arc(gsx + gsD, gsy - 10 * s - gsD * 0.5, 7 * s, 0, Math.PI, true);
      ctx.closePath();
      ctx.fill();

      // Crack / cross detail
      ctx.strokeStyle = "#424242";
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(gsx - 2 * s, gsy - 14 * s);
      ctx.lineTo(gsx, gsy - 5 * s);
      ctx.moveTo(gsx - 3 * s, gsy - 10 * s);
      ctx.lineTo(gsx + 2 * s, gsy - 10 * s);
      ctx.stroke();

      // Moss
      ctx.fillStyle = "rgba(76,175,80,0.25)";
      ctx.beginPath();
      ctx.ellipse(gsx - 3 * s, gsy + 0.5 * s, 2 * s, 1 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      break;
    }
    case "tent": {
      drawTent(
        ctx,
        screenPos.x,
        screenPos.y,
        s,
        variant,
        decorTime,
        decorX,
        skipShadow
      );
      break;
    }
    case "flowers": {
      // Enhanced 3D flowers with detailed petals and stems
      const flowerPalettes = [
        {
          center: "#C8A860",
          petals: ["#8A5040", "#9A6A5A", "#B0897A"],
          stem: "#2A4A20",
          stemDark: "#1A3515",
        },
        {
          center: "#8A6030",
          petals: ["#9A8040", "#B09A60", "#C8B880"],
          stem: "#3A5A2A",
          stemDark: "#2A4520",
        },
        {
          center: "#B8A060",
          petals: ["#7A4A6A", "#8A5A7A", "#A07A90"],
          stem: "#2A4A25",
          stemDark: "#1A3515",
        },
        {
          center: "#A09050",
          petals: ["#5A7A8A", "#7A9AA0", "#90B0B8"],
          stem: "#354A30",
          stemDark: "#2A3A25",
        },
      ];
      const fp = flowerPalettes[variant % 4];

      // Ground shadow
      ctx.fillStyle = "rgba(0,0,0,0.12)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 2 * s,
        14 * s,
        5 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Small grass tufts at base
      ctx.strokeStyle = fp.stem;
      ctx.lineWidth = 1.5 * s;
      for (let g = 0; g < 5; g++) {
        const gx = screenPos.x + (g - 2) * 5 * s;
        const sway = Math.sin(decorTime * 2 + g) * 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(gx, screenPos.y);
        ctx.quadraticCurveTo(
          gx + sway * 0.5,
          screenPos.y - 4 * s,
          gx + sway,
          screenPos.y - 7 * s
        );
        ctx.stroke();
      }

      // Draw 5 flowers with different heights and angles
      const flowerPositions = [
        { angle: -0.15, height: 18, x: -8, y: 0 },
        { angle: 0.05, height: 22, x: 0, y: 0 },
        { angle: 0.2, height: 16, x: 8, y: 0 },
        { angle: -0.08, height: 14, x: -4, y: -2 },
        { angle: 0.12, height: 13, x: 5, y: -1 },
      ];

      flowerPositions.forEach((fl, idx) => {
        const fx = screenPos.x + fl.x * s;
        const fy = screenPos.y + fl.y * s;
        const sway = Math.sin(decorTime * 1.5 + idx * 0.7) * 2 * s;

        // Stem with gradient
        const stemGrad = ctx.createLinearGradient(
          fx,
          fy,
          fx,
          fy - fl.height * s
        );
        stemGrad.addColorStop(0, fp.stemDark);
        stemGrad.addColorStop(1, fp.stem);
        ctx.strokeStyle = stemGrad;
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.moveTo(fx, fy);
        ctx.quadraticCurveTo(
          fx + sway * 0.3 + fl.angle * 10 * s,
          fy - fl.height * 0.5 * s,
          fx + sway + fl.angle * 8 * s,
          fy - fl.height * s
        );
        ctx.stroke();

        // Leaf on stem
        if (idx < 3) {
          ctx.fillStyle = fp.stem;
          ctx.save();
          ctx.translate(fx + sway * 0.2, fy - fl.height * 0.4 * s);
          ctx.rotate(fl.angle + 0.3);
          ctx.beginPath();
          ctx.ellipse(3 * s, 0, 4 * s, 1.5 * s, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }

        // Flower head position
        const headX = fx + sway + fl.angle * 8 * s;
        const headY = fy - fl.height * s;
        const petalSize = 3 + (idx === 1 ? 1 : 0);

        // Petals - layered for 3D effect
        for (let layer = 0; layer < 2; layer++) {
          const layerOffset = layer * 0.3;
          const layerSize = petalSize - layer * 0.5;

          for (let p = 0; p < 5; p++) {
            const pa = (p / 5) * Math.PI * 2 + layerOffset + decorTime * 0.3;
            const petalX = headX + Math.cos(pa) * layerSize * s;
            const petalY =
              headY + Math.sin(pa) * layerSize * 0.5 * s - layer * 1.5 * s;

            ctx.fillStyle = layer === 0 ? fp.petals[0] : fp.petals[1];
            ctx.beginPath();
            ctx.ellipse(petalX, petalY, 2.5 * s, 1.8 * s, pa, 0, Math.PI * 2);
            ctx.fill();
          }
        }

        // Flower center with gradient
        const centerGrad = ctx.createRadialGradient(
          headX - 0.5 * s,
          headY - 2 * s,
          0,
          headX,
          headY - 1.5 * s,
          2.5 * s
        );
        centerGrad.addColorStop(0, "#FFF8E1");
        centerGrad.addColorStop(0.5, fp.center);
        centerGrad.addColorStop(1, fp.petals[0]);
        ctx.fillStyle = centerGrad;
        ctx.beginPath();
        ctx.arc(headX, headY - 1.5 * s, 2 * s, 0, Math.PI * 2);
        ctx.fill();

        // Pollen dots on center
        ctx.fillStyle = "#8D6E63";
        for (let d = 0; d < 3; d++) {
          const da = (d / 3) * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(
            headX + Math.cos(da) * 0.8 * s,
            headY - 1.5 * s + Math.sin(da) * 0.4 * s,
            0.4 * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
      });
      break;
    }
    case "signpost": {
      const sx = screenPos.x;
      const sy = screenPos.y;
      const sv = variant % 4;

      // Ground shadow
      drawDirectionalShadow(ctx, sx, sy + 1 * s, s, 8 * s, 4 * s, 24 * s, 0.22);

      if (sv === 0) {
        // === CROSSROADS SIGN — 3 arrow boards at different angles, skull decoration ===
        const pH = 28 * s;

        // Dirt mound
        ctx.fillStyle = "#6D4C41";
        ctx.beginPath();
        ctx.ellipse(sx, sy + 1 * s, 4 * s, 2 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Post with gradient (front face)
        const sPostG = ctx.createLinearGradient(sx - 2 * s, 0, sx + 2 * s, 0);
        sPostG.addColorStop(0, "#795548");
        sPostG.addColorStop(0.5, "#A1887F");
        sPostG.addColorStop(1, "#5D4037");
        ctx.fillStyle = sPostG;
        ctx.beginPath();
        ctx.moveTo(sx - 2 * s, sy);
        ctx.lineTo(sx + 2 * s, sy);
        ctx.lineTo(sx + 2 * s, sy - pH);
        ctx.lineTo(sx - 2 * s, sy - pH);
        ctx.closePath();
        ctx.fill();
        // Side face
        ctx.fillStyle = "#4E342E";
        ctx.beginPath();
        ctx.moveTo(sx + 2 * s, sy);
        ctx.lineTo(sx + 3.5 * s, sy - 1 * s);
        ctx.lineTo(sx + 3.5 * s, sy - pH - 1 * s);
        ctx.lineTo(sx + 2 * s, sy - pH);
        ctx.closePath();
        ctx.fill();
        // Top face
        ctx.fillStyle = "#8D6E63";
        ctx.beginPath();
        ctx.moveTo(sx - 2 * s, sy - pH);
        ctx.lineTo(sx + 2 * s, sy - pH);
        ctx.lineTo(sx + 3.5 * s, sy - pH - 1 * s);
        ctx.lineTo(sx - 0.5 * s, sy - pH - 1 * s);
        ctx.closePath();
        ctx.fill();

        // Three arrow boards at different heights/angles
        const boards = [
          { dir: 1, len: 14, tilt: -0.08, y: sy - pH + 4 * s },
          { dir: -1, len: 12, tilt: 0.05, y: sy - pH + 12 * s },
          { dir: 1, len: 10, tilt: -0.12, y: sy - pH + 19 * s },
        ];
        const boardColors = ["#8D6E63", "#A1887F", "#795548"];

        for (let bi = 0; bi < boards.length; bi++) {
          const b = boards[bi];
          const bLen = b.len * s;
          const bH = 5 * s;
          ctx.save();
          ctx.translate(sx, b.y);
          ctx.rotate(b.tilt);

          // Board thickness
          ctx.fillStyle = "#4E342E";
          ctx.beginPath();
          ctx.moveTo(0, bH);
          ctx.lineTo(b.dir * bLen, bH);
          ctx.lineTo(b.dir * (bLen + 3 * s), bH * 0.5 + 1 * s);
          ctx.lineTo(b.dir * bLen, 1 * s);
          ctx.lineTo(0, 1 * s);
          ctx.closePath();
          ctx.fill();

          // Board face
          const bG = ctx.createLinearGradient(0, 0, 0, bH);
          bG.addColorStop(0, boardColors[bi]);
          bG.addColorStop(1, "#5D4037");
          ctx.fillStyle = bG;
          ctx.beginPath();
          ctx.moveTo(0, 0);
          ctx.lineTo(b.dir * bLen, 0);
          ctx.lineTo(b.dir * (bLen + 3 * s), bH * 0.5);
          ctx.lineTo(b.dir * bLen, bH);
          ctx.lineTo(0, bH);
          ctx.closePath();
          ctx.fill();

          // Nail
          ctx.fillStyle = "#FFD54F";
          ctx.beginPath();
          ctx.arc(b.dir * 2 * s, bH * 0.5, 0.7 * s, 0, Math.PI * 2);
          ctx.fill();

          // Weathered text
          ctx.fillStyle = "rgba(62,39,35,0.4)";
          for (let t = 0; t < 3; t++) {
            ctx.fillRect(b.dir * (4 + t * 3) * s, bH * 0.3, 2 * s, 1.5 * s);
          }
          ctx.restore();
        }

        // Skull decoration on top
        ctx.fillStyle = "#E8E0D0";
        ctx.beginPath();
        ctx.ellipse(sx, sy - pH - 3 * s, 3.5 * s, 3 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "#1a1510";
        ctx.beginPath();
        ctx.ellipse(
          sx - 1.2 * s,
          sy - pH - 3.5 * s,
          0.8 * s,
          1 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          sx + 1.2 * s,
          sy - pH - 3.5 * s,
          0.8 * s,
          1 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(sx - 0.5 * s, sy - pH - 1.5 * s);
        ctx.lineTo(sx + 0.5 * s, sy - pH - 1.5 * s);
        ctx.stroke();
      } else if (sv === 1) {
        // === TAVERN HANGING SIGN — iron bracket with swinging sign ===
        const pH2 = 26 * s;

        // Post (isometric)
        const tavG = ctx.createLinearGradient(sx - 2 * s, 0, sx + 2 * s, 0);
        tavG.addColorStop(0, "#5D4037");
        tavG.addColorStop(0.5, "#795548");
        tavG.addColorStop(1, "#4E342E");
        ctx.fillStyle = tavG;
        ctx.beginPath();
        ctx.moveTo(sx - 2 * s, sy);
        ctx.lineTo(sx + 2 * s, sy);
        ctx.lineTo(sx + 2 * s, sy - pH2);
        ctx.lineTo(sx - 2 * s, sy - pH2);
        ctx.closePath();
        ctx.fill();
        // Post side face
        ctx.fillStyle = "#3E2A1E";
        ctx.beginPath();
        ctx.moveTo(sx + 2 * s, sy);
        ctx.lineTo(sx + 3.5 * s, sy - 1 * s);
        ctx.lineTo(sx + 3.5 * s, sy - pH2 - 1 * s);
        ctx.lineTo(sx + 2 * s, sy - pH2);
        ctx.closePath();
        ctx.fill();

        // Iron bracket (L-shape extending right)
        ctx.strokeStyle = "#37474F";
        ctx.lineWidth = 2 * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(sx + 2 * s, sy - pH2 + 3 * s);
        ctx.lineTo(sx + 12 * s, sy - pH2 + 3 * s);
        ctx.stroke();
        // Decorative scroll on bracket
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.arc(
          sx + 12 * s,
          sy - pH2 + 5 * s,
          2 * s,
          -Math.PI * 0.5,
          Math.PI * 0.5
        );
        ctx.stroke();
        // Bracket support
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(sx + 2 * s, sy - pH2 + 8 * s);
        ctx.lineTo(sx + 8 * s, sy - pH2 + 3 * s);
        ctx.stroke();

        // Hanging chains
        const signSwing = Math.sin(decorTime * 1.5 + decorX * 0.1) * 0.03;
        ctx.save();
        ctx.translate(sx + 7 * s, sy - pH2 + 3 * s);
        ctx.rotate(signSwing);

        ctx.strokeStyle = "#455A64";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo(-4 * s, 0);
        ctx.lineTo(-4 * s, 4 * s);
        ctx.moveTo(4 * s, 0);
        ctx.lineTo(4 * s, 4 * s);
        ctx.stroke();

        // Hanging sign board (isometric)
        const sbW = 12 * s,
          sbH = 10 * s,
          sbD = 2 * s;
        // Board bottom edge (thickness)
        ctx.fillStyle = "#3E2A1E";
        ctx.beginPath();
        ctx.moveTo(-sbW * 0.5, 4 * s + sbH);
        ctx.lineTo(sbW * 0.5, 4 * s + sbH);
        ctx.lineTo(sbW * 0.5 + sbD, 4 * s + sbH - sbD * 0.5);
        ctx.lineTo(-sbW * 0.5 + sbD, 4 * s + sbH - sbD * 0.5);
        ctx.closePath();
        ctx.fill();
        // Board side face (right edge)
        ctx.fillStyle = "#4E342E";
        ctx.beginPath();
        ctx.moveTo(sbW * 0.5, 4 * s);
        ctx.lineTo(sbW * 0.5 + sbD, 4 * s - sbD * 0.5);
        ctx.lineTo(sbW * 0.5 + sbD, 4 * s + sbH - sbD * 0.5);
        ctx.lineTo(sbW * 0.5, 4 * s + sbH);
        ctx.closePath();
        ctx.fill();
        // Board face
        const sbG = ctx.createLinearGradient(0, 4 * s, 0, 4 * s + sbH);
        sbG.addColorStop(0, "#A1887F");
        sbG.addColorStop(0.5, "#8D6E63");
        sbG.addColorStop(1, "#795548");
        ctx.fillStyle = sbG;
        ctx.beginPath();
        ctx.moveTo(-sbW * 0.5, 4 * s);
        ctx.lineTo(sbW * 0.5, 4 * s);
        ctx.lineTo(sbW * 0.5, 4 * s + sbH);
        ctx.lineTo(-sbW * 0.5, 4 * s + sbH);
        ctx.closePath();
        ctx.fill();
        // Gold border
        ctx.strokeStyle = "#FFD54F";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(-sbW * 0.5 + 1 * s, 5 * s);
        ctx.lineTo(sbW * 0.5 - 1 * s, 5 * s);
        ctx.lineTo(sbW * 0.5 - 1 * s, 4 * s + sbH - 1 * s);
        ctx.lineTo(-sbW * 0.5 + 1 * s, 4 * s + sbH - 1 * s);
        ctx.closePath();
        ctx.stroke();

        // Tankard icon on sign
        ctx.fillStyle = "#FFD54F";
        ctx.beginPath();
        ctx.moveTo(-2 * s, 6 * s);
        ctx.lineTo(2 * s, 6 * s);
        ctx.lineTo(2 * s, 11 * s);
        ctx.lineTo(-2 * s, 11 * s);
        ctx.closePath();
        ctx.fill();
        ctx.beginPath();
        ctx.arc(2.5 * s, 8.5 * s, 1.5 * s, -Math.PI * 0.5, Math.PI * 0.5);
        ctx.stroke();
        // Foam top
        ctx.fillStyle = "#FFF8E1";
        ctx.beginPath();
        ctx.ellipse(0, 6 * s, 2.5 * s, 1 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        ctx.restore();
      } else if (sv === 2) {
        // === QUEST BOARD — large notice board with pinned papers ===
        const pH3 = 22 * s;

        // Two support posts (isometric)
        const qpD = 2 * s;
        for (const px of [sx - 10 * s, sx + 10 * s]) {
          const spG = ctx.createLinearGradient(
            px - 1.5 * s,
            0,
            px + 1.5 * s,
            0
          );
          spG.addColorStop(0, "#795548");
          spG.addColorStop(0.5, "#8D6E63");
          spG.addColorStop(1, "#5D4037");
          ctx.fillStyle = spG;
          ctx.beginPath();
          ctx.moveTo(px - 1.5 * s, sy);
          ctx.lineTo(px + 1.5 * s, sy);
          ctx.lineTo(px + 1.5 * s, sy - pH3);
          ctx.lineTo(px - 1.5 * s, sy - pH3);
          ctx.closePath();
          ctx.fill();
          // Side face
          ctx.fillStyle = "#4E342E";
          ctx.beginPath();
          ctx.moveTo(px + 1.5 * s, sy);
          ctx.lineTo(px + 1.5 * s + qpD, sy - qpD * 0.5);
          ctx.lineTo(px + 1.5 * s + qpD, sy - pH3 - qpD * 0.5);
          ctx.lineTo(px + 1.5 * s, sy - pH3);
          ctx.closePath();
          ctx.fill();
        }

        // Board backing (isometric)
        const bbW = 22 * s,
          bbH = 14 * s;
        const bbY = sy - pH3 + 3 * s;
        // Side face (right edge)
        ctx.fillStyle = "#3E2A1E";
        ctx.beginPath();
        ctx.moveTo(sx + bbW * 0.5, bbY);
        ctx.lineTo(sx + bbW * 0.5 + qpD, bbY - qpD * 0.5);
        ctx.lineTo(sx + bbW * 0.5 + qpD, bbY + bbH - qpD * 0.5);
        ctx.lineTo(sx + bbW * 0.5, bbY + bbH);
        ctx.closePath();
        ctx.fill();
        // Bottom edge
        ctx.fillStyle = "#4E342E";
        ctx.beginPath();
        ctx.moveTo(sx - bbW * 0.5, bbY + bbH);
        ctx.lineTo(sx + bbW * 0.5, bbY + bbH);
        ctx.lineTo(sx + bbW * 0.5 + qpD, bbY + bbH - qpD * 0.5);
        ctx.lineTo(sx - bbW * 0.5 + qpD, bbY + bbH - qpD * 0.5);
        ctx.closePath();
        ctx.fill();
        // Face
        const bbG = ctx.createLinearGradient(sx, bbY, sx, bbY + bbH);
        bbG.addColorStop(0, "#A1887F");
        bbG.addColorStop(1, "#8D6E63");
        ctx.fillStyle = bbG;
        ctx.beginPath();
        ctx.moveTo(sx - bbW * 0.5, bbY);
        ctx.lineTo(sx + bbW * 0.5, bbY);
        ctx.lineTo(sx + bbW * 0.5, bbY + bbH);
        ctx.lineTo(sx - bbW * 0.5, bbY + bbH);
        ctx.closePath();
        ctx.fill();
        // Frame border
        ctx.strokeStyle = "#5D4037";
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(sx - bbW * 0.5, bbY);
        ctx.lineTo(sx + bbW * 0.5, bbY);
        ctx.lineTo(sx + bbW * 0.5, bbY + bbH);
        ctx.lineTo(sx - bbW * 0.5, bbY + bbH);
        ctx.closePath();
        ctx.stroke();

        // Pinned papers/parchments
        const papers = [
          { color: "#FFF8E1", h: 8, rot: -0.05, w: 7, x: -7, y: 2 },
          { color: "#EFEBE9", h: 9, rot: 0.03, w: 8, x: 1, y: 1 },
          { color: "#FFF3E0", h: 7, rot: -0.08, w: 6, x: 6, y: 3 },
          { color: "#FFFDE7", h: 5, rot: 0.06, w: 5, x: -2, y: 6 },
        ];
        for (const p of papers) {
          ctx.save();
          ctx.translate(sx + p.x * s, bbY + p.y * s);
          ctx.rotate(p.rot);
          // Paper shadow
          ctx.fillStyle = "rgba(0,0,0,0.1)";
          ctx.fillRect(0.5 * s, 0.5 * s, p.w * s, p.h * s);
          // Paper
          ctx.fillStyle = p.color;
          ctx.fillRect(0, 0, p.w * s, p.h * s);
          // Text lines
          ctx.fillStyle = "rgba(60,40,20,0.3)";
          for (let l = 0; l < 3; l++) {
            ctx.fillRect(0.5 * s, (1 + l * 2) * s, (p.w - 1) * s, 0.8 * s);
          }
          // Pin
          ctx.fillStyle = "#F44336";
          ctx.beginPath();
          ctx.arc(p.w * 0.5 * s, 0, 1 * s, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "#FFCDD2";
          ctx.beginPath();
          ctx.arc(p.w * 0.5 * s - 0.3 * s, -0.3 * s, 0.4 * s, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }

        // "QUESTS" header carved into top frame
        ctx.fillStyle = "rgba(62,39,35,0.5)";
        ctx.fillRect(sx - 6 * s, bbY - 2 * s, 12 * s, 2 * s);

        // Small roof overhang (isometric)
        ctx.fillStyle = "#5D4037";
        ctx.beginPath();
        ctx.moveTo(sx - bbW * 0.55, bbY - 1 * s);
        ctx.lineTo(sx, bbY - 4 * s);
        ctx.lineTo(sx + bbW * 0.55, bbY - 1 * s);
        ctx.closePath();
        ctx.fill();
        // Roof depth
        ctx.fillStyle = "#4E342E";
        ctx.beginPath();
        ctx.moveTo(sx, bbY - 4 * s);
        ctx.lineTo(sx + bbW * 0.55, bbY - 1 * s);
        ctx.lineTo(sx + bbW * 0.55 + qpD, bbY - 1 * s - qpD * 0.5);
        ctx.lineTo(sx + qpD, bbY - 4 * s - qpD * 0.5);
        ctx.closePath();
        ctx.fill();
      } else {
        // === ANCIENT WAYSTONE — imposing runed stone pillar with arcane glow ===
        const wsHeight = 50 * s;
        const wsBaseW = 11 * s;
        const wsTopW = 7 * s;

        const bw86 = wsBaseW * ISO_COS;
        const tw86 = wsTopW * ISO_COS;
        const bwIso = wsBaseW * ISO_SIN;
        const twIso = wsTopW * ISO_SIN;

        // Ground shadow
        ctx.fillStyle = "rgba(0,0,0,0.2)";
        ctx.beginPath();
        ctx.ellipse(
          sx + 4 * s,
          sy + 6 * s,
          20 * s,
          9 * s,
          0.15,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Two-tier stepped stone pedestal
        const wsPed1W = 14 * s;
        const wsPed1H = 4 * s;
        drawIsometricPrism(
          ctx,
          sx,
          sy,
          wsPed1W,
          wsPed1W,
          wsPed1H,
          "#6B7B8A",
          "#4A5A68",
          "#5A6A78"
        );
        const wsPed2W = 12 * s;
        const wsPed2H = 4 * s;
        drawIsometricPrism(
          ctx,
          sx,
          sy - wsPed1H,
          wsPed2W,
          wsPed2W,
          wsPed2H,
          "#7A8A9A",
          "#546E7A",
          "#607D8B"
        );

        const wsBase = sy - wsPed1H - wsPed2H;
        const wsTop = wsBase - wsHeight;

        // --- Main shaft: tapered isometric pillar (3 visible faces + top) ---

        // Left face (shadow side)
        const wsLG = ctx.createLinearGradient(
          sx - bw86,
          wsBase,
          sx - tw86,
          wsTop
        );
        wsLG.addColorStop(0, "#455A64");
        wsLG.addColorStop(0.5, "#4A6070");
        wsLG.addColorStop(1, "#3E5260");
        ctx.fillStyle = wsLG;
        ctx.beginPath();
        ctx.moveTo(sx - bw86, wsBase + bwIso);
        ctx.lineTo(sx, wsBase + bwIso * 2);
        ctx.lineTo(sx, wsTop + twIso * 2);
        ctx.lineTo(sx - tw86, wsTop + twIso);
        ctx.closePath();
        ctx.fill();

        // Right face (lit side)
        const wsRG = ctx.createLinearGradient(
          sx + bw86,
          wsBase,
          sx + tw86,
          wsTop
        );
        wsRG.addColorStop(0, "#607D8B");
        wsRG.addColorStop(0.5, "#6A8898");
        wsRG.addColorStop(1, "#546E7A");
        ctx.fillStyle = wsRG;
        ctx.beginPath();
        ctx.moveTo(sx + bw86, wsBase + bwIso);
        ctx.lineTo(sx, wsBase + bwIso * 2);
        ctx.lineTo(sx, wsTop + twIso * 2);
        ctx.lineTo(sx + tw86, wsTop + twIso);
        ctx.closePath();
        ctx.fill();

        // Top diamond
        ctx.fillStyle = "#78909C";
        ctx.beginPath();
        ctx.moveTo(sx, wsTop);
        ctx.lineTo(sx + tw86, wsTop + twIso);
        ctx.lineTo(sx, wsTop + twIso * 2);
        ctx.lineTo(sx - tw86, wsTop + twIso);
        ctx.closePath();
        ctx.fill();

        ctx.strokeStyle = "rgba(0,0,0,0.07)";
        ctx.lineWidth = 0.5 * s;
        const wsCRows = 7;

        // Stone course lines on left face
        for (let cr = 1; cr < wsCRows; cr++) {
          const ct = cr / wsCRows;
          const crW = bw86 + (tw86 - bw86) * ct;
          const crI = bwIso + (twIso - bwIso) * ct;
          const crY = wsBase + (wsTop - wsBase) * ct;
          ctx.beginPath();
          ctx.moveTo(sx - crW, crY + crI);
          ctx.lineTo(sx, crY + crI * 2);
          ctx.stroke();
        }

        // Stone course lines on right face
        for (let cr = 1; cr < wsCRows; cr++) {
          const ct = cr / wsCRows;
          const crW = bw86 + (tw86 - bw86) * ct;
          const crI = bwIso + (twIso - bwIso) * ct;
          const crY = wsBase + (wsTop - wsBase) * ct;
          ctx.beginPath();
          ctx.moveTo(sx + crW, crY + crI);
          ctx.lineTo(sx, crY + crI * 2);
          ctx.stroke();
        }

        // --- Pyramidion pointed cap ---
        const wsCTip = wsTop - 12 * s;

        // Cap left face
        ctx.fillStyle = "#4A6070";
        ctx.beginPath();
        ctx.moveTo(sx, wsCTip);
        ctx.lineTo(sx - tw86, wsTop + twIso);
        ctx.lineTo(sx, wsTop + twIso * 2);
        ctx.closePath();
        ctx.fill();

        // Cap right face
        ctx.fillStyle = "#6A8898";
        ctx.beginPath();
        ctx.moveTo(sx, wsCTip);
        ctx.lineTo(sx + tw86, wsTop + twIso);
        ctx.lineTo(sx, wsTop + twIso * 2);
        ctx.closePath();
        ctx.fill();

        // Cap front face
        ctx.fillStyle = "#5A7888";
        ctx.beginPath();
        ctx.moveTo(sx, wsCTip);
        ctx.lineTo(sx - tw86, wsTop + twIso);
        ctx.lineTo(sx + tw86, wsTop + twIso);
        ctx.closePath();
        ctx.fill();

        // Cap highlight
        ctx.fillStyle = "#90A4AE";
        ctx.globalAlpha = 0.35;
        ctx.beginPath();
        ctx.moveTo(sx, wsCTip);
        ctx.lineTo(sx + tw86 * 0.5, wsTop + twIso * 0.6);
        ctx.lineTo(sx, wsTop + twIso);
        ctx.closePath();
        ctx.fill();
        ctx.globalAlpha = 1;

        // --- Edge highlights ---
        ctx.strokeStyle = "rgba(255,255,255,0.12)";
        ctx.lineWidth = s;
        ctx.beginPath();
        ctx.moveTo(sx, wsCTip);
        ctx.lineTo(sx + tw86, wsTop + twIso);
        ctx.lineTo(sx + bw86, wsBase + bwIso);
        ctx.stroke();

        ctx.strokeStyle = "rgba(255,255,255,0.06)";
        ctx.beginPath();
        ctx.moveTo(sx, wsCTip);
        ctx.lineTo(sx - tw86, wsTop + twIso);
        ctx.lineTo(sx - bw86, wsBase + bwIso);
        ctx.stroke();

        // --- Glowing Elder Futhark runes ---
        const wsRnGlow = 0.45 + Math.sin(decorTime * 2) * 0.25;
        ctx.lineWidth = 1.3 * s;
        ctx.lineCap = "round";
        ctx.lineJoin = "round";
        ctx.strokeStyle = `rgba(220,180,80,${wsRnGlow})`;

        const wsRuneSlots = [
          { rt: 0, t: 0.15 },
          { rt: 1, t: 0.38 },
          { rt: 2, t: 0.58 },
          { rt: 3, t: 0.78 },
        ];

        for (const rSlot of wsRuneSlots) {
          const rfY =
            wsBase + bwIso + (wsTop + twIso - wsBase - bwIso) * rSlot.t;
          const rcx = sx;
          const rcy = rfY;
          const rs = 3.5 * s;

          if (rSlot.rt === 0) {
            // Tiwaz — upward arrow
            ctx.beginPath();
            ctx.moveTo(rcx, rcy + rs);
            ctx.lineTo(rcx, rcy - rs);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(rcx - rs * 0.65, rcy - rs * 0.25);
            ctx.lineTo(rcx, rcy - rs);
            ctx.lineTo(rcx + rs * 0.65, rcy - rs * 0.25);
            ctx.stroke();
          } else if (rSlot.rt === 1) {
            // Othala — diamond with legs
            ctx.beginPath();
            ctx.moveTo(rcx, rcy - rs);
            ctx.lineTo(rcx + rs * 0.6, rcy - rs * 0.2);
            ctx.lineTo(rcx, rcy + rs * 0.4);
            ctx.lineTo(rcx - rs * 0.6, rcy - rs * 0.2);
            ctx.closePath();
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(rcx - rs * 0.6, rcy - rs * 0.2);
            ctx.lineTo(rcx - rs * 0.35, rcy + rs);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(rcx + rs * 0.6, rcy - rs * 0.2);
            ctx.lineTo(rcx + rs * 0.35, rcy + rs);
            ctx.stroke();
          } else if (rSlot.rt === 2) {
            // Sowilo — lightning bolt
            ctx.beginPath();
            ctx.moveTo(rcx + rs * 0.4, rcy - rs);
            ctx.lineTo(rcx - rs * 0.4, rcy - rs * 0.2);
            ctx.lineTo(rcx + rs * 0.4, rcy + rs * 0.2);
            ctx.lineTo(rcx - rs * 0.4, rcy + rs);
            ctx.stroke();
          } else {
            // Algiz — branching Y
            ctx.beginPath();
            ctx.moveTo(rcx, rcy + rs);
            ctx.lineTo(rcx, rcy - rs * 0.3);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(rcx, rcy - rs * 0.3);
            ctx.lineTo(rcx - rs * 0.6, rcy - rs);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(rcx, rcy - rs * 0.3);
            ctx.lineTo(rcx + rs * 0.6, rcy - rs);
            ctx.stroke();
          }

          // Per-rune glow halo
          const rhG = ctx.createRadialGradient(rcx, rcy, 0, rcx, rcy, rs * 2.5);
          rhG.addColorStop(0, `rgba(220,180,80,${wsRnGlow * 0.1})`);
          rhG.addColorStop(1, "rgba(220,180,80,0)");
          ctx.fillStyle = rhG;
          ctx.beginPath();
          ctx.arc(rcx, rcy, rs * 2.5, 0, Math.PI * 2);
          ctx.fill();
        }

        // --- Moss patches at base ---
        ctx.fillStyle = "rgba(80,120,60,0.25)";
        ctx.beginPath();
        ctx.ellipse(
          sx - bw86 * 0.6,
          wsBase + bwIso * 1.4,
          3 * s,
          1.5 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          sx + bw86 * 0.4,
          wsBase + bwIso * 1.2,
          2 * s,
          1 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // --- Ambient glow around stone ---
        const wsAmbG = ctx.createRadialGradient(
          sx,
          wsBase - wsHeight * 0.4,
          0,
          sx,
          wsBase - wsHeight * 0.4,
          22 * s
        );
        wsAmbG.addColorStop(0, `rgba(220,180,80,${wsRnGlow * 0.08})`);
        wsAmbG.addColorStop(0.5, `rgba(220,180,80,${wsRnGlow * 0.03})`);
        wsAmbG.addColorStop(1, "rgba(220,180,80,0)");
        ctx.fillStyle = wsAmbG;
        ctx.beginPath();
        ctx.arc(sx, wsBase - wsHeight * 0.4, 22 * s, 0, Math.PI * 2);
        ctx.fill();

        // --- Ground glow ring ---
        const wsGndG = ctx.createRadialGradient(
          sx,
          sy + 2 * s,
          2 * s,
          sx,
          sy + 2 * s,
          16 * s
        );
        wsGndG.addColorStop(0, `rgba(220,180,80,${wsRnGlow * 0.06})`);
        wsGndG.addColorStop(0.7, `rgba(220,180,80,${wsRnGlow * 0.02})`);
        wsGndG.addColorStop(1, "rgba(220,180,80,0)");
        ctx.fillStyle = wsGndG;
        ctx.beginPath();
        ctx.ellipse(sx, sy + 2 * s, 16 * s, 7 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // --- Floating arcane particles ---
        for (let pi = 0; pi < 5; pi++) {
          const pPh = decorTime * 0.7 + pi * 1.25;
          const oR = (7 + Math.sin(pPh * 0.4) * 4) * s;
          const ppx = sx + Math.sin(pPh) * oR;
          const rise = ((pPh * 2) % 6) / 6;
          const ppy = wsBase - wsHeight * rise * 0.8;
          const pA = 0.35 + Math.sin(pPh * 2.5) * 0.2;
          const pSz = (0.7 + Math.sin(pPh * 1.8) * 0.3) * s;
          ctx.fillStyle = `rgba(220,190,100,${pA})`;
          ctx.beginPath();
          ctx.arc(ppx, ppy, pSz, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      break;
    }
    case "fountain": {
      // Enhanced 3D isometric ornate fountain with animated water
      const fountainBaseX = screenPos.x;
      const fountainBaseY = screenPos.y;
      const waterTime = decorTime * 3;

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        fountainBaseX,
        fountainBaseY + 2 * s,
        s,
        18 * s,
        9 * s,
        30 * s,
        0.25
      );

      // Outer basin - stone rim
      const stoneGrad = ctx.createLinearGradient(
        fountainBaseX - 22 * s,
        0,
        fountainBaseX + 22 * s,
        0
      );
      stoneGrad.addColorStop(0, "#707880");
      stoneGrad.addColorStop(0.3, "#90A4AE");
      stoneGrad.addColorStop(0.7, "#B0BEC5");
      stoneGrad.addColorStop(1, "#78909C");

      ctx.fillStyle = stoneGrad;
      ctx.beginPath();
      ctx.ellipse(
        fountainBaseX,
        fountainBaseY,
        22 * s,
        11 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Basin inner wall
      ctx.fillStyle = "#607D8B";
      ctx.beginPath();
      ctx.ellipse(
        fountainBaseX,
        fountainBaseY - 2 * s,
        18 * s,
        9 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Water surface with ripple effect
      const waterGrad = ctx.createRadialGradient(
        fountainBaseX,
        fountainBaseY - 3 * s,
        0,
        fountainBaseX,
        fountainBaseY - 3 * s,
        15 * s
      );
      waterGrad.addColorStop(0, "#81D4FA");
      waterGrad.addColorStop(0.5, "#4FC3F7");
      waterGrad.addColorStop(1, "#29B6F6");

      ctx.fillStyle = waterGrad;
      ctx.beginPath();
      ctx.ellipse(
        fountainBaseX,
        fountainBaseY - 3 * s,
        15 * s,
        7.5 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Water ripples
      ctx.strokeStyle = "rgba(255,255,255,0.3)";
      ctx.lineWidth = 1 * s;
      for (let r = 0; r < 3; r++) {
        const ripplePhase = (waterTime + r * 0.7) % 2;
        const rippleSize = 4 + ripplePhase * 5;
        const rippleAlpha = 1 - ripplePhase / 2;
        ctx.strokeStyle = `rgba(255,255,255,${rippleAlpha * 0.4})`;
        ctx.beginPath();
        ctx.ellipse(
          fountainBaseX,
          fountainBaseY - 3 * s,
          rippleSize * s,
          rippleSize * 0.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.stroke();
      }

      // Central pillar/pedestal
      ctx.fillStyle = "#78909C";
      ctx.fillRect(
        fountainBaseX - 3 * s,
        fountainBaseY - 18 * s,
        6 * s,
        15 * s
      );

      // Pillar highlight
      ctx.fillStyle = "rgba(255,255,255,0.2)";
      ctx.fillRect(
        fountainBaseX - 1 * s,
        fountainBaseY - 18 * s,
        2 * s,
        15 * s
      );

      // Top bowl
      ctx.fillStyle = "#90A4AE";
      ctx.beginPath();
      ctx.ellipse(
        fountainBaseX,
        fountainBaseY - 18 * s,
        8 * s,
        4 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Top bowl water
      ctx.fillStyle = "#81D4FA";
      ctx.beginPath();
      ctx.ellipse(
        fountainBaseX,
        fountainBaseY - 19 * s,
        6 * s,
        3 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Water spray from center
      const sprayHeight = 12 + Math.sin(waterTime * 2) * 3;
      const sprayGrad = ctx.createLinearGradient(
        0,
        fountainBaseY - 19 * s - sprayHeight * s,
        0,
        fountainBaseY - 19 * s
      );
      sprayGrad.addColorStop(0, "rgba(255,255,255,0.9)");
      sprayGrad.addColorStop(0.5, "rgba(225,245,254,0.7)");
      sprayGrad.addColorStop(1, "rgba(129,212,250,0.4)");

      ctx.fillStyle = sprayGrad;
      ctx.beginPath();
      ctx.moveTo(fountainBaseX - 2 * s, fountainBaseY - 19 * s);
      ctx.quadraticCurveTo(
        fountainBaseX - 4 * s,
        fountainBaseY - 25 * s,
        fountainBaseX,
        fountainBaseY - 19 * s - sprayHeight * s
      );
      ctx.quadraticCurveTo(
        fountainBaseX + 4 * s,
        fountainBaseY - 25 * s,
        fountainBaseX + 2 * s,
        fountainBaseY - 19 * s
      );
      ctx.closePath();
      ctx.fill();

      // Water droplets
      ctx.fillStyle = "rgba(255,255,255,0.7)";
      for (let d = 0; d < 6; d++) {
        const dropPhase = (waterTime * 1.5 + d * 0.4) % 1;
        const dropAngle = (d / 6) * Math.PI * 2;
        const dropDist = 3 + dropPhase * 8;
        const dropX = fountainBaseX + Math.cos(dropAngle) * dropDist * s;
        const dropY =
          fountainBaseY -
          19 * s +
          dropPhase * 16 * s -
          sprayHeight * (1 - dropPhase) * s * 0.5;
        const dropSize = (1 - dropPhase) * 1.5;

        ctx.beginPath();
        ctx.arc(dropX, dropY, dropSize * s, 0, Math.PI * 2);
        ctx.fill();
      }

      // Water falling from top bowl
      ctx.strokeStyle = "rgba(129,212,250,0.5)";
      ctx.lineWidth = 2 * s;
      for (let w = 0; w < 4; w++) {
        const wAngle = (w / 4) * Math.PI * 2 + waterTime * 0.5;
        const wX = fountainBaseX + Math.cos(wAngle) * 6 * s;
        const waveOffset = Math.sin(waterTime * 4 + w) * 2 * s;

        ctx.beginPath();
        ctx.moveTo(wX, fountainBaseY - 18 * s);
        ctx.quadraticCurveTo(
          wX + waveOffset,
          fountainBaseY - 10 * s,
          fountainBaseX + Math.cos(wAngle) * 10 * s,
          fountainBaseY - 3 * s
        );
        ctx.stroke();
      }

      // Sparkle on water
      const sparklePhase = waterTime % 1;
      if (sparklePhase < 0.3) {
        ctx.fillStyle = `rgba(255,255,255,${1 - sparklePhase / 0.3})`;
        ctx.beginPath();
        ctx.arc(
          fountainBaseX + 5 * s,
          fountainBaseY - 4 * s,
          2 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }
    case "bench": {
      drawBench(ctx, screenPos.x, screenPos.y, s, variant);
      break;
    }

    case "lamppost": {
      const TRUE_ISO = 1 / Math.sqrt(3);
      const metalDark = "#212121";
      const metalMid = "#424242";
      const metalDeep = "#1A1A1A";
      const lpx = screenPos.x;
      const lpy = screenPos.y;

      // Ground shadow
      ctx.fillStyle = "rgba(0,0,0,0.25)";
      ctx.beginPath();
      ctx.ellipse(lpx, lpy, 8 * s, 4 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Base step 1 (larger isometric diamond platform)
      const b1w = 6 * s;
      const b1d = b1w * TRUE_ISO;
      const b1h = 2 * s;
      ctx.fillStyle = metalMid;
      ctx.beginPath();
      ctx.moveTo(lpx - b1w, lpy);
      ctx.lineTo(lpx, lpy + b1d);
      ctx.lineTo(lpx, lpy + b1d - b1h);
      ctx.lineTo(lpx - b1w, lpy - b1h);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = metalDeep;
      ctx.beginPath();
      ctx.moveTo(lpx, lpy + b1d);
      ctx.lineTo(lpx + b1w, lpy);
      ctx.lineTo(lpx + b1w, lpy - b1h);
      ctx.lineTo(lpx, lpy + b1d - b1h);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = metalDark;
      ctx.beginPath();
      ctx.moveTo(lpx - b1w, lpy - b1h);
      ctx.lineTo(lpx, lpy + b1d - b1h);
      ctx.lineTo(lpx + b1w, lpy - b1h);
      ctx.lineTo(lpx, lpy - b1d - b1h);
      ctx.closePath();
      ctx.fill();

      // Base step 2 (smaller isometric diamond platform)
      const b2y = lpy - b1h;
      const b2w = 5 * s;
      const b2d = b2w * TRUE_ISO;
      const b2h = 2 * s;
      ctx.fillStyle = metalDark;
      ctx.beginPath();
      ctx.moveTo(lpx - b2w, b2y);
      ctx.lineTo(lpx, b2y + b2d);
      ctx.lineTo(lpx, b2y + b2d - b2h);
      ctx.lineTo(lpx - b2w, b2y - b2h);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = metalDeep;
      ctx.beginPath();
      ctx.moveTo(lpx, b2y + b2d);
      ctx.lineTo(lpx + b2w, b2y);
      ctx.lineTo(lpx + b2w, b2y - b2h);
      ctx.lineTo(lpx, b2y + b2d - b2h);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = metalMid;
      ctx.beginPath();
      ctx.moveTo(lpx - b2w, b2y - b2h);
      ctx.lineTo(lpx, b2y + b2d - b2h);
      ctx.lineTo(lpx + b2w, b2y - b2h);
      ctx.lineTo(lpx, b2y - b2d - b2h);
      ctx.closePath();
      ctx.fill();

      // Pole — isometric thin column
      const poleHW = 2 * s;
      const poleHD = poleHW * TRUE_ISO;
      const poleBot = b2y - b2h;
      const poleTop = lpy - 35 * s;
      const poleGL = ctx.createLinearGradient(lpx - poleHW, 0, lpx, 0);
      poleGL.addColorStop(0, metalDark);
      poleGL.addColorStop(1, metalMid);
      ctx.fillStyle = poleGL;
      ctx.beginPath();
      ctx.moveTo(lpx - poleHW, poleBot);
      ctx.lineTo(lpx, poleBot + poleHD);
      ctx.lineTo(lpx, poleTop + poleHD);
      ctx.lineTo(lpx - poleHW, poleTop);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = metalDeep;
      ctx.beginPath();
      ctx.moveTo(lpx, poleBot + poleHD);
      ctx.lineTo(lpx + poleHW, poleBot);
      ctx.lineTo(lpx + poleHW, poleTop);
      ctx.lineTo(lpx, poleTop + poleHD);
      ctx.closePath();
      ctx.fill();

      // Lamp fixture — isometric trapezoid prism (widens upward)
      const fixBot = poleTop;
      const fixTop = lpy - 45 * s;
      const fixBotHW = 4 * s;
      const fixTopHW = 6 * s;
      const fixBotHD = fixBotHW * TRUE_ISO;
      const fixTopHD = fixTopHW * TRUE_ISO;
      ctx.fillStyle = metalDark;
      ctx.beginPath();
      ctx.moveTo(lpx - fixBotHW, fixBot);
      ctx.lineTo(lpx, fixBot + fixBotHD);
      ctx.lineTo(lpx, fixTop + fixTopHD);
      ctx.lineTo(lpx - fixTopHW, fixTop);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = metalDeep;
      ctx.beginPath();
      ctx.moveTo(lpx, fixBot + fixBotHD);
      ctx.lineTo(lpx + fixBotHW, fixBot);
      ctx.lineTo(lpx + fixTopHW, fixTop);
      ctx.lineTo(lpx, fixTop + fixTopHD);
      ctx.closePath();
      ctx.fill();

      // Glass/light — isometric prism (warm glow faces)
      const flicker = 0.1 + Math.sin(decorTime * 3) * 0.05;
      const glassBot = fixBot + 1 * s;
      const glassTop = fixTop - 1 * s;
      const glassHW = 3.5 * s;
      const glassHD = glassHW * TRUE_ISO;
      ctx.fillStyle = `rgba(255, 236, 179, ${0.8 + flicker})`;
      ctx.beginPath();
      ctx.moveTo(lpx - glassHW, glassBot);
      ctx.lineTo(lpx, glassBot + glassHD);
      ctx.lineTo(lpx, glassTop + glassHD);
      ctx.lineTo(lpx - glassHW, glassTop);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = `rgba(220, 200, 140, ${0.6 + flicker})`;
      ctx.beginPath();
      ctx.moveTo(lpx, glassBot + glassHD);
      ctx.lineTo(lpx + glassHW, glassBot);
      ctx.lineTo(lpx + glassHW, glassTop);
      ctx.lineTo(lpx, glassTop + glassHD);
      ctx.closePath();
      ctx.fill();

      // Glow effect
      const glowCenterY = (glassBot + glassTop) / 2;
      const glowRad = ctx.createRadialGradient(
        lpx,
        glowCenterY,
        2 * s,
        lpx,
        glowCenterY,
        25 * s
      );
      glowRad.addColorStop(0, `rgba(255, 213, 79, ${0.4 + flicker})`);
      glowRad.addColorStop(1, "rgba(255, 213, 79, 0)");
      ctx.fillStyle = glowRad;
      ctx.beginPath();
      ctx.arc(lpx, glowCenterY, 25 * s, 0, Math.PI * 2);
      ctx.fill();

      // Top cap — isometric pyramid
      const capBase = fixTop;
      const capPeak = lpy - 52 * s;
      const capHW = 7 * s;
      const capHD = capHW * TRUE_ISO;
      ctx.fillStyle = metalDark;
      ctx.beginPath();
      ctx.moveTo(lpx - capHW, capBase);
      ctx.lineTo(lpx, capBase - capHD);
      ctx.lineTo(lpx, capPeak);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = metalMid;
      ctx.beginPath();
      ctx.moveTo(lpx - capHW, capBase);
      ctx.lineTo(lpx, capBase + capHD);
      ctx.lineTo(lpx, capPeak);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = "#333333";
      ctx.beginPath();
      ctx.moveTo(lpx, capBase + capHD);
      ctx.lineTo(lpx + capHW, capBase);
      ctx.lineTo(lpx, capPeak);
      ctx.closePath();
      ctx.fill();
      break;
    }

    case "hedge": {
      drawHedge(ctx, screenPos.x, screenPos.y, s, variant, decorX, decorY);
      break;
    }

    case "campfire": {
      const cfX = screenPos.x;
      const cfY = screenPos.y;
      const cfSeed = decorX * 47 + decorY * 89;

      // Warm ambient ground glow
      const cfGndGlow = ctx.createRadialGradient(
        cfX,
        cfY + 2 * s,
        3 * s,
        cfX,
        cfY + 2 * s,
        35 * s
      );
      const cfGlowPulse = 0.18 + Math.sin(decorTime * 3.5) * 0.06;
      cfGndGlow.addColorStop(0, `rgba(255,140,40,${cfGlowPulse})`);
      cfGndGlow.addColorStop(0.4, `rgba(255,90,20,${cfGlowPulse * 0.45})`);
      cfGndGlow.addColorStop(1, "rgba(255,50,10,0)");
      ctx.fillStyle = cfGndGlow;
      ctx.beginPath();
      ctx.ellipse(cfX, cfY + 2 * s, 35 * s, 18 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Ash / char circle on ground — organic shape
      const ashG = ctx.createRadialGradient(
        cfX,
        cfY + 2 * s,
        2 * s,
        cfX,
        cfY + 2 * s,
        12 * s
      );
      ashG.addColorStop(0, "#2a2220");
      ashG.addColorStop(0.3, "#3a3530");
      ashG.addColorStop(0.6, "#4a4540");
      ashG.addColorStop(1, "#5a5550");
      ctx.fillStyle = ashG;
      drawOrganicWaterShape(
        ctx,
        cfX,
        cfY + 2 * s,
        12 * s,
        6 * s,
        cfSeed + 5,
        0.14
      );
      ctx.fill();

      // Charcoal / soot stain ring around ash
      ctx.fillStyle = "rgba(30,25,20,0.2)";
      drawOrganicWaterShape(
        ctx,
        cfX,
        cfY + 2 * s,
        14 * s,
        7 * s,
        cfSeed + 8,
        0.16
      );
      ctx.fill();

      // Stone ring — 3D individual stones with lit/shadow faces
      const cfStoneCount = 10;
      const stonePalettes = [
        { front: "#6a6560", side: "#4a4540", top: "#8a8580" },
        { front: "#7a7570", side: "#5a5550", top: "#9a9590" },
        { front: "#5a5852", side: "#3a3832", top: "#7a7872" },
      ];
      for (let st = 0; st < cfStoneCount; st++) {
        const stAng = (st / cfStoneCount) * Math.PI * 2 + cfSeed * 0.01;
        const stR = 10 + Math.sin(cfSeed + st * 1.7) * 1.5;
        const stx = cfX + Math.cos(stAng) * stR * s;
        const sty = cfY + 2 * s + Math.sin(stAng) * stR * 0.5 * s;
        const stW = (2 + Math.sin(cfSeed + st * 3.1) * 0.8) * s;
        const stH = (1.5 + Math.cos(cfSeed + st * 2.3) * 0.5) * s;
        const stD = 2 * s;
        const pal = stonePalettes[st % 3];

        // Stone top face
        ctx.fillStyle = pal.top;
        ctx.beginPath();
        ctx.ellipse(stx, sty - stD, stW, stH, stAng * 0.3, 0, Math.PI * 2);
        ctx.fill();
        // Stone front face
        ctx.fillStyle = pal.front;
        ctx.beginPath();
        ctx.ellipse(stx, sty - stD * 0.3, stW, stH, stAng * 0.3, 0, Math.PI);
        ctx.fill();
        // Heat tint on inner face
        const heatTint = 0.12 + Math.sin(decorTime * 2 + st) * 0.04;
        ctx.fillStyle = `rgba(255,100,30,${heatTint})`;
        ctx.beginPath();
        ctx.ellipse(
          stx,
          sty - stD * 0.5,
          stW * 0.7,
          stH * 0.5,
          stAng * 0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Ember bed — glowing coals under the fire
      const coalG = ctx.createRadialGradient(cfX, cfY, 1 * s, cfX, cfY, 8 * s);
      const coalPulse = 0.7 + Math.sin(decorTime * 4) * 0.15;
      coalG.addColorStop(0, `rgba(255,120,20,${coalPulse})`);
      coalG.addColorStop(0.3, `rgba(200,60,10,${coalPulse * 0.7})`);
      coalG.addColorStop(0.6, `rgba(120,30,5,${coalPulse * 0.4})`);
      coalG.addColorStop(1, "rgba(40,10,0,0.3)");
      ctx.fillStyle = coalG;
      drawOrganicWaterShape(
        ctx,
        cfX,
        cfY + 1 * s,
        8 * s,
        4 * s,
        cfSeed + 20,
        0.18
      );
      ctx.fill();

      // Individual hot coals
      for (let co = 0; co < 6; co++) {
        const coAng = (co / 6) * Math.PI * 2 + cfSeed * 0.03;
        const coR = 2 + Math.sin(cfSeed + co * 2.7) * 2;
        const cox = cfX + Math.cos(coAng) * coR * s;
        const coy = cfY + 1 * s + Math.sin(coAng) * coR * 0.5 * s;
        const coPulse = 0.5 + Math.sin(decorTime * 5 + co * 1.3) * 0.3;
        const coG = ctx.createRadialGradient(cox, coy, 0, cox, coy, 1.8 * s);
        coG.addColorStop(0, `rgba(255,200,80,${coPulse})`);
        coG.addColorStop(0.5, `rgba(255,80,10,${coPulse * 0.6})`);
        coG.addColorStop(1, "rgba(80,20,0,0.2)");
        ctx.fillStyle = coG;
        ctx.beginPath();
        ctx.ellipse(cox, coy, 1.8 * s, 1 * s, coAng * 0.2, 0, Math.PI * 2);
        ctx.fill();
      }

      // 3D isometric logs — proper cylinders with bark and char
      const drawCfLog = (
        x1: number,
        y1: number,
        x2: number,
        y2: number,
        radius: number,
        barkDark: string,
        barkLit: string,
        charAmount: number
      ) => {
        const dx = x2 - x1;
        const dy = y2 - y1;
        const ang = Math.atan2(dy, dx);
        const nx = -Math.sin(ang) * radius * s;
        const ny = Math.cos(ang) * radius * s;

        // Log body gradient
        const logG = ctx.createLinearGradient(
          x1 + nx,
          y1 + ny,
          x1 - nx,
          y1 - ny
        );
        logG.addColorStop(0, barkDark);
        logG.addColorStop(0.35, barkLit);
        logG.addColorStop(0.7, barkDark);
        logG.addColorStop(1, "#1a0e08");
        ctx.fillStyle = logG;
        ctx.beginPath();
        ctx.moveTo(x1 + nx, y1 + ny);
        ctx.lineTo(x2 + nx, y2 + ny);
        ctx.lineTo(x2 - nx, y2 - ny);
        ctx.lineTo(x1 - nx, y1 - ny);
        ctx.closePath();
        ctx.fill();

        // Bark texture lines
        ctx.strokeStyle = "rgba(20,12,5,0.2)";
        ctx.lineWidth = 0.3 * s;
        for (let bl = 0; bl < 3; bl++) {
          const t = (bl + 1) / 4;
          ctx.beginPath();
          ctx.moveTo(x1 + nx * (1 - 2 * t), y1 + ny * (1 - 2 * t));
          ctx.lineTo(x2 + nx * (1 - 2 * t), y2 + ny * (1 - 2 * t));
          ctx.stroke();
        }

        // Char on the center-facing side
        if (charAmount > 0) {
          const charG = ctx.createLinearGradient(
            (x1 + x2) * 0.5 - nx * 0.5,
            (y1 + y2) * 0.5 - ny * 0.5,
            (x1 + x2) * 0.5 + nx * 0.5,
            (y1 + y2) * 0.5 + ny * 0.5
          );
          charG.addColorStop(0, `rgba(20,10,5,${charAmount})`);
          charG.addColorStop(0.5, `rgba(40,15,5,${charAmount * 0.5})`);
          charG.addColorStop(1, "rgba(0,0,0,0)");
          ctx.fillStyle = charG;
          ctx.beginPath();
          ctx.moveTo(x1 + nx, y1 + ny);
          ctx.lineTo(x2 + nx, y2 + ny);
          ctx.lineTo(x2 - nx, y2 - ny);
          ctx.lineTo(x1 - nx, y1 - ny);
          ctx.closePath();
          ctx.fill();

          // Ember glow along charred edge
          const embPulse = 0.15 + Math.sin(decorTime * 3 + ang) * 0.08;
          ctx.strokeStyle = `rgba(255,80,10,${embPulse})`;
          ctx.lineWidth = 1 * s;
          ctx.beginPath();
          ctx.moveTo(
            x1 * 0.7 + x2 * 0.3 + nx * 0.3,
            y1 * 0.7 + y2 * 0.3 + ny * 0.3
          );
          ctx.lineTo(
            x1 * 0.3 + x2 * 0.7 + nx * 0.3,
            y1 * 0.3 + y2 * 0.7 + ny * 0.3
          );
          ctx.stroke();
        }

        // End caps (cross-section ellipses showing rings)
        const drawEndCap = (ex: number, ey: number) => {
          const capG = ctx.createRadialGradient(
            ex - nx * 0.2,
            ey - ny * 0.2,
            0,
            ex,
            ey,
            radius * s
          );
          capG.addColorStop(0, "#c8a878");
          capG.addColorStop(0.3, "#a08060");
          capG.addColorStop(0.7, barkDark);
          capG.addColorStop(1, "#2a1a10");
          ctx.fillStyle = capG;
          ctx.beginPath();
          ctx.ellipse(
            ex,
            ey,
            radius * s,
            radius * 0.6 * s,
            ang,
            0,
            Math.PI * 2
          );
          ctx.fill();
          // Tree rings
          ctx.strokeStyle = "rgba(60,40,20,0.25)";
          ctx.lineWidth = 0.3 * s;
          for (let ring = 1; ring < 3; ring++) {
            const rt = ring / 3;
            ctx.beginPath();
            ctx.ellipse(
              ex,
              ey,
              radius * rt * s,
              radius * rt * 0.6 * s,
              ang,
              0,
              Math.PI * 2
            );
            ctx.stroke();
          }
        };
        drawEndCap(x1, y1);
        drawEndCap(x2, y2);
      };

      // Three logs arranged in a triangle/tepee base
      drawCfLog(
        cfX - 8 * s,
        cfY + 3 * s,
        cfX + 5 * s,
        cfY - 1 * s,
        1.8,
        "#4a3020",
        "#7a5a42",
        0.6
      );
      drawCfLog(
        cfX + 7 * s,
        cfY + 3 * s,
        cfX - 4 * s,
        cfY - 1 * s,
        1.6,
        "#3e2723",
        "#6d4c41",
        0.5
      );
      drawCfLog(
        cfX - 3 * s,
        cfY + 4 * s,
        cfX + 8 * s,
        cfY + 1 * s,
        1.4,
        "#5d4037",
        "#8d6e63",
        0.4
      );

      // Multi-layered animated fire
      const cfFP = decorTime * 6;
      const cfFlk1 = Math.sin(cfFP) * 2.5 * s;
      const cfFlk2 = Math.sin(cfFP * 1.3 + 1.2) * 2 * s;
      const cfFlk3 = Math.cos(cfFP * 0.8) * 1.5 * s;
      const cfFlameBase = cfY - 1 * s;

      // Outer dark flame (deep red)
      ctx.fillStyle = "#b71c1c";
      ctx.beginPath();
      ctx.moveTo(cfX - 7 * s, cfFlameBase);
      ctx.quadraticCurveTo(
        cfX - 4 * s + cfFlk2,
        cfFlameBase - 20 * s,
        cfX + cfFlk1 * 0.4,
        cfFlameBase - 28 * s
      );
      ctx.quadraticCurveTo(
        cfX + 4 * s - cfFlk2,
        cfFlameBase - 20 * s,
        cfX + 7 * s,
        cfFlameBase
      );
      ctx.closePath();
      ctx.fill();

      // Middle flame (orange-red)
      ctx.fillStyle = "#e65100";
      ctx.beginPath();
      ctx.moveTo(cfX - 5.5 * s, cfFlameBase);
      ctx.quadraticCurveTo(
        cfX - 3 * s - cfFlk1,
        cfFlameBase - 17 * s,
        cfX + cfFlk3,
        cfFlameBase - 24 * s
      );
      ctx.quadraticCurveTo(
        cfX + 3 * s + cfFlk1,
        cfFlameBase - 17 * s,
        cfX + 5.5 * s,
        cfFlameBase
      );
      ctx.closePath();
      ctx.fill();

      // Bright flame (orange)
      ctx.fillStyle = "#ff8f00";
      ctx.beginPath();
      ctx.moveTo(cfX - 4 * s, cfFlameBase);
      ctx.quadraticCurveTo(
        cfX + cfFlk1 * 0.5,
        cfFlameBase - 14 * s,
        cfX - cfFlk3 * 0.4,
        cfFlameBase - 20 * s
      );
      ctx.quadraticCurveTo(
        cfX - cfFlk1 * 0.5,
        cfFlameBase - 14 * s,
        cfX + 4 * s,
        cfFlameBase
      );
      ctx.closePath();
      ctx.fill();

      // Inner flame (yellow)
      ctx.fillStyle = "#ffc107";
      ctx.beginPath();
      ctx.moveTo(cfX - 2.5 * s, cfFlameBase);
      ctx.quadraticCurveTo(
        cfX - cfFlk2 * 0.3,
        cfFlameBase - 10 * s,
        cfX + cfFlk1 * 0.3,
        cfFlameBase - 15 * s
      );
      ctx.quadraticCurveTo(
        cfX + cfFlk2 * 0.3,
        cfFlameBase - 10 * s,
        cfX + 2.5 * s,
        cfFlameBase
      );
      ctx.closePath();
      ctx.fill();

      // Core (white-yellow)
      const cfCoreG = ctx.createRadialGradient(
        cfX,
        cfFlameBase - 4 * s,
        0.5 * s,
        cfX,
        cfFlameBase - 4 * s,
        5 * s
      );
      cfCoreG.addColorStop(0, "rgba(255,255,230,0.85)");
      cfCoreG.addColorStop(0.4, "rgba(255,230,150,0.5)");
      cfCoreG.addColorStop(1, "rgba(255,180,60,0)");
      ctx.fillStyle = cfCoreG;
      ctx.beginPath();
      ctx.moveTo(cfX - 1.5 * s, cfFlameBase);
      ctx.quadraticCurveTo(cfX, cfFlameBase - 7 * s, cfX, cfFlameBase - 10 * s);
      ctx.quadraticCurveTo(
        cfX,
        cfFlameBase - 7 * s,
        cfX + 1.5 * s,
        cfFlameBase
      );
      ctx.closePath();
      ctx.fill();

      // Secondary side flame tongue
      const sideFl = Math.sin(cfFP * 1.7 + 0.5) * 1.5 * s;
      ctx.fillStyle = "rgba(255,140,0,0.6)";
      ctx.beginPath();
      ctx.moveTo(cfX + 4 * s, cfFlameBase);
      ctx.quadraticCurveTo(
        cfX + 7 * s + sideFl,
        cfFlameBase - 8 * s,
        cfX + 5 * s + sideFl,
        cfFlameBase - 12 * s
      );
      ctx.quadraticCurveTo(
        cfX + 6 * s,
        cfFlameBase - 5 * s,
        cfX + 5 * s,
        cfFlameBase
      );
      ctx.closePath();
      ctx.fill();

      // Flame glow halo
      const cfHaloG = ctx.createRadialGradient(
        cfX,
        cfFlameBase - 10 * s,
        2 * s,
        cfX,
        cfFlameBase - 10 * s,
        22 * s
      );
      const cfHaloPulse = 0.2 + Math.sin(decorTime * 4.5) * 0.06;
      cfHaloG.addColorStop(0, `rgba(255,160,40,${cfHaloPulse})`);
      cfHaloG.addColorStop(0.5, `rgba(255,100,20,${cfHaloPulse * 0.3})`);
      cfHaloG.addColorStop(1, "rgba(255,60,10,0)");
      ctx.fillStyle = cfHaloG;
      ctx.beginPath();
      ctx.arc(cfX, cfFlameBase - 10 * s, 22 * s, 0, Math.PI * 2);
      ctx.fill();

      // Rising embers / sparks
      for (let sp = 0; sp < 7; sp++) {
        const spSeed = cfSeed + sp * 2.3;
        const spLife = ((decorTime * 1.8 + spSeed * 0.1) % 2.5) / 2.5;
        const spAlpha = Math.max(0, 1 - spLife * 1.1) * 0.85;
        const spDrift = Math.sin(spSeed + decorTime * 2.5) * 6 * s * spLife;
        const spX = cfX + spDrift + Math.sin(spSeed * 3) * 3 * s;
        const spY = cfFlameBase - 16 * s - spLife * 22 * s;
        const spSize = (0.6 + Math.sin(spSeed) * 0.3) * s * (1 - spLife * 0.4);
        const spR = Math.floor(255);
        const spG = Math.floor(200 - spLife * 120);
        const spB = Math.floor(50 - spLife * 40);
        ctx.fillStyle = `rgba(${spR},${spG},${spB},${spAlpha})`;
        ctx.beginPath();
        ctx.arc(spX, spY, spSize, 0, Math.PI * 2);
        ctx.fill();
      }

      // Smoke wisps rising from the top
      for (let sm = 0; sm < 3; sm++) {
        const smLife = ((decorTime * 0.7 + sm * 1.4) % 3) / 3;
        const smAlpha = Math.sin(smLife * Math.PI) * 0.08;
        const smX = cfX + Math.sin(decorTime * 0.8 + sm * 2.1) * 8 * s * smLife;
        const smY = cfFlameBase - 25 * s - smLife * 20 * s;
        const smR = (4 + smLife * 6) * s;
        ctx.fillStyle = `rgba(100,90,80,${smAlpha})`;
        ctx.beginPath();
        ctx.ellipse(smX, smY, smR, smR * 0.5, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }

    case "dock": {
      const dockWood = "#6d4c41";
      const dockWoodDark = "#4e342e";
      const dockWoodLight = "#8d6e63";
      const dkSeed = (dec.x || 0) * 17.3 + (dec.y || 0) * 9.1;
      const dx = screenPos.x,
        dy = screenPos.y;

      // Organic water body with depth gradient
      const dkWaterG = ctx.createRadialGradient(
        dx + 6 * s,
        dy + 6 * s,
        2 * s,
        dx + 8 * s,
        dy + 5 * s,
        22 * s
      );
      dkWaterG.addColorStop(0, "rgba(20,65,115,0.72)");
      dkWaterG.addColorStop(0.35, "rgba(40,100,155,0.6)");
      dkWaterG.addColorStop(0.65, "rgba(60,130,180,0.48)");
      dkWaterG.addColorStop(1, "rgba(90,160,205,0.25)");
      ctx.fillStyle = dkWaterG;
      drawOrganicWaterShape(
        ctx,
        dx + 8 * s,
        dy + 5 * s,
        22 * s,
        10 * s,
        dkSeed,
        0.14
      );
      ctx.fill();

      // Deeper center shadow
      ctx.fillStyle = "rgba(10,35,65,0.2)";
      drawOrganicWaterShape(
        ctx,
        dx + 6 * s,
        dy + 6 * s,
        12 * s,
        5 * s,
        dkSeed + 30,
        0.1
      );
      ctx.fill();

      // Surface sheen
      ctx.fillStyle = "rgba(180,225,255,0.1)";
      drawOrganicWaterShape(
        ctx,
        dx + 10 * s,
        dy + 3 * s,
        14 * s,
        6 * s,
        dkSeed + 55,
        0.09
      );
      ctx.fill();

      // Animated caustic ripple bands
      for (let band = 0; band < 3; band++) {
        const bPhase = decorTime * 1.2 + band * 1.1;
        const wobble = 1 + Math.sin(bPhase) * 0.05;
        const bR = (8 + band * 5) * s;
        ctx.strokeStyle = `rgba(160,215,255,${0.12 - band * 0.03})`;
        ctx.lineWidth = (1.5 - band * 0.3) * s;
        drawOrganicWaterShape(
          ctx,
          dx + 8 * s,
          dy + 5 * s,
          bR * wobble,
          bR * 0.45 * wobble,
          dkSeed + band * 23,
          0.08
        );
        ctx.stroke();
      }

      // Pilings — 3D isometric wooden posts rising from water
      const pilings = [
        { px: -8, py: 2 },
        { px: -8, py: -6 },
        { px: 8, py: 2 },
        { px: 8, py: -6 },
      ];
      pilings.forEach((p, pi) => {
        const ppx = dx + p.px * s,
          ppy = dy + p.py * s;
        const postH = 10 * s;
        const postW = 2 * s;
        const postD = 1.2 * s;

        // Water ring around piling
        ctx.strokeStyle = "rgba(150,200,240,0.18)";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.ellipse(
          ppx + postD * 0.5,
          ppy + 4 * s,
          postW + 1 * s,
          (postW + 1 * s) * 0.4,
          0,
          0,
          Math.PI * 2
        );
        ctx.stroke();

        // Dark face (left)
        ctx.fillStyle = "#3a2518";
        ctx.beginPath();
        ctx.moveTo(ppx - postW * 0.5, ppy + 4 * s);
        ctx.lineTo(ppx - postW * 0.5, ppy + 4 * s - postH);
        ctx.lineTo(ppx + postW * 0.5, ppy + 4 * s - postH);
        ctx.lineTo(ppx + postW * 0.5, ppy + 4 * s);
        ctx.closePath();
        ctx.fill();

        // Lit face (right)
        ctx.fillStyle = dockWoodDark;
        ctx.beginPath();
        ctx.moveTo(ppx + postW * 0.5, ppy + 4 * s);
        ctx.lineTo(ppx + postW * 0.5, ppy + 4 * s - postH);
        ctx.lineTo(
          ppx + postW * 0.5 + postD,
          ppy + 4 * s - postH - postD * 0.5
        );
        ctx.lineTo(ppx + postW * 0.5 + postD, ppy + 4 * s - postD * 0.5);
        ctx.closePath();
        ctx.fill();

        // Top cap diamond
        ctx.fillStyle = dockWoodLight;
        ctx.beginPath();
        ctx.moveTo(ppx - postW * 0.5, ppy + 4 * s - postH);
        ctx.lineTo(ppx + postW * 0.5, ppy + 4 * s - postH);
        ctx.lineTo(
          ppx + postW * 0.5 + postD,
          ppy + 4 * s - postH - postD * 0.5
        );
        ctx.lineTo(
          ppx - postW * 0.5 + postD,
          ppy + 4 * s - postH - postD * 0.5
        );
        ctx.closePath();
        ctx.fill();

        // Weathering marks on posts
        if (pi % 2 === 0) {
          ctx.strokeStyle = "rgba(30,20,10,0.15)";
          ctx.lineWidth = 0.4 * s;
          for (let wm = 0; wm < 3; wm++) {
            const wmy = ppy + 4 * s - postH * 0.3 - wm * 2.5 * s;
            ctx.beginPath();
            ctx.moveTo(ppx - postW * 0.4, wmy);
            ctx.lineTo(ppx + postW * 0.4, wmy);
            ctx.stroke();
          }
        }

        // Algae/moss at waterline
        ctx.fillStyle = "rgba(60,120,60,0.25)";
        ctx.beginPath();
        ctx.ellipse(ppx, ppy + 3 * s, postW * 0.8, 1.5 * s, 0, 0, Math.PI);
        ctx.fill();
      });

      // Deck underside shadow
      ctx.fillStyle = "rgba(0,0,0,0.12)";
      ctx.beginPath();
      ctx.moveTo(dx - 10 * s, dy - 1 * s);
      ctx.lineTo(dx, dy - 7 * s);
      ctx.lineTo(dx + 16 * s, dy - 1 * s);
      ctx.lineTo(dx + 6 * s, dy + 5 * s);
      ctx.closePath();
      ctx.fill();

      // Deck planks - isometric rectangle (bottom layer)
      ctx.fillStyle = dockWood;
      ctx.beginPath();
      ctx.moveTo(dx - 10 * s, dy - 2 * s);
      ctx.lineTo(dx, dy - 8 * s);
      ctx.lineTo(dx + 16 * s, dy - 2 * s);
      ctx.lineTo(dx + 6 * s, dy + 4 * s);
      ctx.closePath();
      ctx.fill();

      // Plank gap lines (along iso-right direction)
      ctx.strokeStyle = "rgba(40,25,12,0.35)";
      ctx.lineWidth = 0.6 * s;
      for (let pl = 1; pl < 6; pl++) {
        const t = pl / 6;
        const x0 = dx - 10 * s + t * (dx + 6 * s - (dx - 10 * s));
        const y0 = dy - 2 * s + t * (dy + 4 * s - (dy - 2 * s));
        const x1 = dx + t * (dx + 16 * s - dx);
        const y1 = dy - 8 * s + t * (dy - 2 * s - (dy - 8 * s));
        ctx.beginPath();
        ctx.moveTo(x0, y0);
        ctx.lineTo(x1, y1);
        ctx.stroke();
      }

      // Top plank face (lighter, with wood grain suggestion)
      const deckTopG = ctx.createLinearGradient(
        dx - 5 * s,
        dy - 10 * s,
        dx + 10 * s,
        dy + 2 * s
      );
      deckTopG.addColorStop(0, "#8d6e63");
      deckTopG.addColorStop(0.5, "#7b5b4f");
      deckTopG.addColorStop(1, "#6d4c41");
      ctx.fillStyle = deckTopG;
      ctx.beginPath();
      ctx.moveTo(dx - 10 * s, dy - 4 * s);
      ctx.lineTo(dx, dy - 10 * s);
      ctx.lineTo(dx + 16 * s, dy - 4 * s);
      ctx.lineTo(dx + 6 * s, dy + 2 * s);
      ctx.closePath();
      ctx.fill();

      // Plank gap lines on top face
      ctx.strokeStyle = "rgba(50,30,15,0.25)";
      ctx.lineWidth = 0.5 * s;
      for (let pl = 1; pl < 6; pl++) {
        const t = pl / 6;
        const x0 = dx - 10 * s + t * (dx + 6 * s - (dx - 10 * s));
        const y0 = dy - 4 * s + t * (dy + 2 * s - (dy - 4 * s));
        const x1 = dx + t * (dx + 16 * s - dx);
        const y1 = dy - 10 * s + t * (dy - 4 * s - (dy - 10 * s));
        ctx.beginPath();
        ctx.moveTo(x0, y0);
        ctx.lineTo(x1, y1);
        ctx.stroke();
      }

      // Front edge thickness (left face)
      const edgeDark = "#5d4037";
      ctx.fillStyle = edgeDark;
      ctx.beginPath();
      ctx.moveTo(dx - 10 * s, dy - 4 * s);
      ctx.lineTo(dx + 6 * s, dy + 2 * s);
      ctx.lineTo(dx + 6 * s, dy + 4 * s);
      ctx.lineTo(dx - 10 * s, dy - 2 * s);
      ctx.closePath();
      ctx.fill();

      // Front edge thickness (right face)
      ctx.fillStyle = "#4e342e";
      ctx.beginPath();
      ctx.moveTo(dx + 6 * s, dy + 2 * s);
      ctx.lineTo(dx + 16 * s, dy - 4 * s);
      ctx.lineTo(dx + 16 * s, dy - 2 * s);
      ctx.lineTo(dx + 6 * s, dy + 4 * s);
      ctx.closePath();
      ctx.fill();

      // Bollard (mooring post) on deck
      const blx = dx + 10 * s,
        bly = dy - 6 * s;
      ctx.fillStyle = "#5d4037";
      ctx.fillRect(blx - 1 * s, bly - 3 * s, 2 * s, 3 * s);
      ctx.fillStyle = dockWoodLight;
      ctx.beginPath();
      ctx.ellipse(blx, bly - 3 * s, 1.8 * s, 0.8 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Rope coiled around bollard
      ctx.strokeStyle = "#a1887f";
      ctx.lineWidth = 1 * s;
      for (let rc = 0; rc < 3; rc++) {
        ctx.beginPath();
        ctx.ellipse(
          blx,
          bly - 1 * s - rc * 0.8 * s,
          2.2 * s,
          0.9 * s,
          0,
          0.3,
          Math.PI * 1.7
        );
        ctx.stroke();
      }

      // Rope trailing into water
      ctx.strokeStyle = "rgba(140,110,80,0.5)";
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(blx + 2 * s, bly - 1 * s);
      ctx.quadraticCurveTo(blx + 8 * s, bly + 4 * s, blx + 5 * s, dy + 6 * s);
      ctx.stroke();

      // Water edge foam/shore highlight
      ctx.strokeStyle = "rgba(200,235,255,0.22)";
      ctx.lineWidth = 1.2 * s;
      drawOrganicWaterShape(
        ctx,
        dx + 8 * s,
        dy + 5 * s,
        22 * s,
        10 * s,
        dkSeed,
        0.14
      );
      ctx.stroke();
      break;
    }

    case "gate": {
      const gx = screenPos.x,
        gy = screenPos.y;
      const gD = 6 * s;
      const pillarH = 44 * s;
      const pw = 6 * s;

      const darkFace = "#5e5e56";
      const litFace = "#8a8a7e";
      const topFace = "#9e9e90";
      const darkMort = "rgba(30,28,22,0.45)";
      const litMort = "rgba(40,38,30,0.35)";

      // Shadow
      ctx.fillStyle = "rgba(0,0,0,0.2)";
      ctx.beginPath();
      ctx.ellipse(gx, gy + 6 * s, 22 * s, 8 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Two isometric brick pillars
      const lPx = gx - 14 * s;
      const rPx = gx + 14 * s;

      for (const px of [lPx, rPx]) {
        // Pillar right face (lit)
        drawBrickFace(
          ctx,
          px,
          gy + 2 * s,
          px + pw,
          gy + 2 * s - pw * 0.5,
          pillarH,
          litFace,
          litMort,
          s,
          10,
          2
        );

        // Pillar left face (dark)
        drawBrickFace(
          ctx,
          px - pw,
          gy + 2 * s - pw * 0.5,
          px,
          gy + 2 * s,
          pillarH,
          darkFace,
          darkMort,
          s,
          10,
          2
        );

        // Pillar top diamond
        ctx.fillStyle = topFace;
        ctx.beginPath();
        ctx.moveTo(px, gy + 2 * s - pillarH);
        ctx.lineTo(px + pw, gy + 2 * s - pw * 0.5 - pillarH);
        ctx.lineTo(px, gy + 2 * s - pw - pillarH);
        ctx.lineTo(px - pw, gy + 2 * s - pw * 0.5 - pillarH);
        ctx.closePath();
        ctx.fill();

        // Cap stone
        const capW = pw + 2 * s;
        const capH = 3 * s;
        ctx.fillStyle = "#a0a090";
        ctx.beginPath();
        ctx.moveTo(px, gy + 2 * s - pillarH + capH);
        ctx.lineTo(px + capW, gy + 2 * s - capW * 0.5 - pillarH + capH);
        ctx.lineTo(px, gy + 2 * s - capW * 2 * 0.5 - pillarH + capH);
        ctx.lineTo(px - capW, gy + 2 * s - capW * 0.5 - pillarH + capH);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#b0b0a0";
        ctx.beginPath();
        ctx.moveTo(px, gy + 2 * s - pillarH);
        ctx.lineTo(px + capW, gy + 2 * s - capW * 0.5 - pillarH);
        ctx.lineTo(px, gy + 2 * s - capW * 2 * 0.5 - pillarH);
        ctx.lineTo(px - capW, gy + 2 * s - capW * 0.5 - pillarH);
        ctx.closePath();
        ctx.fill();
      }

      // Arch connecting the pillars
      const archBase = gy + 2 * s - pillarH + 6 * s;
      const archPeak = archBase - 18 * s;
      const archL = lPx + pw;
      const archR = rPx - pw;

      // Arch front face
      ctx.fillStyle = "#8a8a7e";
      ctx.beginPath();
      ctx.moveTo(archL, archBase);
      ctx.quadraticCurveTo((archL + archR) * 0.5, archPeak, archR, archBase);
      ctx.lineTo(archR - 2 * s, archBase + 4 * s);
      ctx.quadraticCurveTo(
        (archL + archR) * 0.5,
        archPeak + 6 * s,
        archL + 2 * s,
        archBase + 4 * s
      );
      ctx.closePath();
      ctx.fill();

      // Arch depth (top surface)
      ctx.fillStyle = topFace;
      ctx.beginPath();
      ctx.moveTo(archL, archBase);
      ctx.quadraticCurveTo((archL + archR) * 0.5, archPeak, archR, archBase);
      ctx.lineTo(archR - gD, archBase - gD * 0.5);
      ctx.quadraticCurveTo(
        (archL + archR) * 0.5 - gD,
        archPeak - gD * 0.5,
        archL - gD,
        archBase - gD * 0.5
      );
      ctx.closePath();
      ctx.fill();

      // Keystone at arch apex
      ctx.fillStyle = "#a0a090";
      const ksX = (archL + archR) * 0.5;
      const ksY = archPeak + 1 * s;
      ctx.beginPath();
      ctx.moveTo(ksX - 3 * s, ksY + 5 * s);
      ctx.lineTo(ksX, ksY);
      ctx.lineTo(ksX + 3 * s, ksY + 5 * s);
      ctx.lineTo(ksX + 2 * s, ksY + 8 * s);
      ctx.lineTo(ksX - 2 * s, ksY + 8 * s);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = "rgba(30,28,22,0.3)";
      ctx.lineWidth = 0.6 * s;
      ctx.stroke();

      // Iron gate bars (vertical)
      const barL = archL + 3 * s;
      const barR = archR - 3 * s;
      const barCount = 6;
      for (let bar = 0; bar < barCount; bar++) {
        const t = bar / (barCount - 1);
        const bx = barL + t * (barR - barL);
        const archYAt =
          archBase +
          4 * s -
          Math.sin(t * Math.PI) * (archBase + 4 * s - archPeak - 6 * s);
        const barBot = gy + 3 * s;

        ctx.fillStyle = "#3a3a3a";
        ctx.beginPath();
        ctx.moveTo(bx - 0.7 * s, archYAt);
        ctx.lineTo(bx + 0.7 * s, archYAt);
        ctx.lineTo(bx + 0.7 * s, barBot);
        ctx.lineTo(bx - 0.7 * s, barBot);
        ctx.closePath();
        ctx.fill();

        ctx.fillStyle = "#2a2a2a";
        ctx.beginPath();
        ctx.moveTo(bx + 0.7 * s, archYAt);
        ctx.lineTo(bx + 0.7 * s + 1 * s, archYAt - 0.5 * s);
        ctx.lineTo(bx + 0.7 * s + 1 * s, barBot - 0.5 * s);
        ctx.lineTo(bx + 0.7 * s, barBot);
        ctx.closePath();
        ctx.fill();
      }

      // Horizontal cross bars
      for (const cbY of [gy - 12 * s, gy - 4 * s]) {
        ctx.fillStyle = "#3a3a3a";
        ctx.beginPath();
        ctx.moveTo(barL, cbY);
        ctx.lineTo(barR, cbY);
        ctx.lineTo(barR, cbY + 1.5 * s);
        ctx.lineTo(barL, cbY + 1.5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#2a2a2a";
        ctx.beginPath();
        ctx.moveTo(barL, cbY);
        ctx.lineTo(barR, cbY);
        ctx.lineTo(barR + 1 * s, cbY - 0.5 * s);
        ctx.lineTo(barL + 1 * s, cbY - 0.5 * s);
        ctx.closePath();
        ctx.fill();
      }
      break;
    }

    case "reeds": {
      const rdSeed = (dec.x || 0) * 13.7 + (dec.y || 0) * 7.3;
      const rx0 = screenPos.x,
        ry0 = screenPos.y;

      // Marshy ground shadow
      ctx.fillStyle = "rgba(30,50,20,0.12)";
      drawOrganicWaterShape(
        ctx,
        rx0,
        ry0 + 5 * s,
        16 * s,
        8 * s,
        rdSeed + 5,
        0.18
      );
      ctx.fill();

      // Organic water body with depth gradient
      const rdWaterG = ctx.createRadialGradient(
        rx0 - 1 * s,
        ry0 + 4 * s,
        1 * s,
        rx0,
        ry0 + 3 * s,
        14 * s
      );
      rdWaterG.addColorStop(0, "rgba(25,70,120,0.6)");
      rdWaterG.addColorStop(0.35, "rgba(45,105,155,0.5)");
      rdWaterG.addColorStop(0.65, "rgba(65,135,180,0.4)");
      rdWaterG.addColorStop(1, "rgba(90,165,200,0.2)");
      ctx.fillStyle = rdWaterG;
      drawOrganicWaterShape(ctx, rx0, ry0 + 3 * s, 14 * s, 6 * s, rdSeed, 0.15);
      ctx.fill();

      // Deeper center
      ctx.fillStyle = "rgba(15,45,80,0.18)";
      drawOrganicWaterShape(
        ctx,
        rx0 - 1 * s,
        ry0 + 4 * s,
        7 * s,
        3 * s,
        rdSeed + 20,
        0.1
      );
      ctx.fill();

      // Surface sheen highlight
      ctx.fillStyle = "rgba(180,225,255,0.09)";
      drawOrganicWaterShape(
        ctx,
        rx0 + 2 * s,
        ry0 + 1 * s,
        8 * s,
        3.5 * s,
        rdSeed + 40,
        0.08
      );
      ctx.fill();

      // Animated ripple bands
      for (let rb = 0; rb < 2; rb++) {
        const rbPhase = decorTime * 1 + rb * 1.3;
        const wobble = 1 + Math.sin(rbPhase) * 0.04;
        const rbR = (5 + rb * 4) * s;
        ctx.strokeStyle = `rgba(150,210,250,${0.1 - rb * 0.03})`;
        ctx.lineWidth = (1.2 - rb * 0.3) * s;
        drawOrganicWaterShape(
          ctx,
          rx0,
          ry0 + 3 * s,
          rbR * wobble,
          rbR * 0.45 * wobble,
          rdSeed + rb * 17,
          0.07
        );
        ctx.stroke();
      }

      // Mud/silt patches at edge
      const mudPositions = [
        { mx: -10, my: 5 },
        { mx: 8, my: 4 },
        { mx: -3, my: 7 },
      ];
      mudPositions.forEach((mp, mi) => {
        ctx.fillStyle = `rgba(80,65,40,${0.15 + mi * 0.03})`;
        drawOrganicWaterShape(
          ctx,
          rx0 + mp.mx * s,
          ry0 + mp.my * s,
          4 * s,
          2 * s,
          rdSeed + mi * 31,
          0.2
        );
        ctx.fill();
      });

      // Reed stalks with varied thickness, draping leaves, and 3D cattails
      const reedCount = 9;
      for (let r = 0; r < reedCount; r++) {
        const spread = (r - (reedCount - 1) * 0.5) * 2.8 * s;
        const rx = rx0 + spread + Math.sin(r * 1.7 + rdSeed) * 2 * s;
        const rh = (18 + Math.sin(r * 2.3 + rdSeed) * 10) * s;
        const sway = Math.sin(decorTime * 1.5 + r * 0.8 + rdSeed * 0.1) * 2 * s;
        const thick = (1.2 + Math.sin(r * 3.1) * 0.4) * s;
        const rBase = ry0 + 2 * s + Math.sin(r * 1.9) * 0.5 * s;

        // Stalk color varies
        const stalkHue =
          r % 3 === 0 ? "#558b2f" : r % 3 === 1 ? "#33691e" : "#4a7a28";

        // Main stalk (thicker at base, tapering)
        ctx.strokeStyle = stalkHue;
        ctx.lineWidth = thick;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(rx, rBase);
        ctx.quadraticCurveTo(
          rx + sway * 0.4,
          rBase - rh * 0.5,
          rx + sway,
          rBase - rh
        );
        ctx.stroke();

        // Thin upper stalk (tapers further)
        ctx.lineWidth = thick * 0.5;
        ctx.strokeStyle = "#6a9f3a";
        ctx.beginPath();
        ctx.moveTo(rx + sway * 0.7, rBase - rh * 0.7);
        ctx.quadraticCurveTo(
          rx + sway * 0.9,
          rBase - rh * 0.85,
          rx + sway,
          rBase - rh
        );
        ctx.stroke();

        // Cattail head on every ~3rd reed
        if (r % 3 === 0) {
          const ctX = rx + sway;
          const ctY = rBase - rh;
          const ctH = 6 * s;
          const ctW = 1.8 * s;
          const ctTilt = sway * 0.04;

          // Cattail body (dark brown oblong with subtle shading)
          const ctG = ctx.createLinearGradient(
            ctX - ctW,
            ctY - ctH * 0.5,
            ctX + ctW,
            ctY + ctH * 0.3
          );
          ctG.addColorStop(0, "#5d4037");
          ctG.addColorStop(0.4, "#795548");
          ctG.addColorStop(0.7, "#5d4037");
          ctG.addColorStop(1, "#4e342e");
          ctx.fillStyle = ctG;
          ctx.beginPath();
          ctx.ellipse(
            ctX,
            ctY - ctH * 0.15,
            ctW,
            ctH * 0.5,
            ctTilt,
            0,
            Math.PI * 2
          );
          ctx.fill();

          // Fuzzy texture dots on cattail
          ctx.fillStyle = "rgba(100,70,50,0.3)";
          for (let fz = 0; fz < 4; fz++) {
            const fzY = ctY - ctH * 0.35 + fz * ctH * 0.2;
            const fzX = ctX + Math.sin(fz * 2.7 + rdSeed) * ctW * 0.4;
            ctx.beginPath();
            ctx.arc(fzX, fzY, 0.6 * s, 0, Math.PI * 2);
            ctx.fill();
          }

          // Spike tip above cattail
          ctx.strokeStyle = "#7cb342";
          ctx.lineWidth = 0.6 * s;
          ctx.beginPath();
          ctx.moveTo(ctX, ctY - ctH * 0.55);
          ctx.lineTo(ctX + sway * 0.15, ctY - ctH * 0.55 - 4 * s);
          ctx.stroke();
        }

        // Drooping leaves at stalk mid-point (1-2 per stalk)
        const leafCount = r % 2 === 0 ? 2 : 1;
        for (let lf = 0; lf < leafCount; lf++) {
          const lfFrac = 0.3 + lf * 0.25;
          const lfX = rx + sway * lfFrac;
          const lfY = rBase - rh * lfFrac;
          const lfDir = (r + lf) % 2 === 0 ? 1 : -1;
          const lfSway =
            Math.sin(decorTime * 1.8 + r * 1.1 + lf * 2.3) * 1.5 * s;

          ctx.strokeStyle = "#7cb342";
          ctx.lineWidth = 1.2 * s;
          ctx.beginPath();
          ctx.moveTo(lfX, lfY);
          ctx.quadraticCurveTo(
            lfX + lfDir * 5 * s + lfSway,
            lfY + 2 * s,
            lfX + lfDir * 8 * s + lfSway * 1.3,
            lfY + 5 * s
          );
          ctx.stroke();

          // Leaf blade (thin filled shape)
          ctx.fillStyle = "rgba(100,160,50,0.3)";
          ctx.beginPath();
          ctx.moveTo(lfX, lfY);
          ctx.quadraticCurveTo(
            lfX + lfDir * 5 * s + lfSway,
            lfY + 1 * s,
            lfX + lfDir * 8 * s + lfSway * 1.3,
            lfY + 5 * s
          );
          ctx.quadraticCurveTo(
            lfX + lfDir * 5 * s + lfSway,
            lfY + 3 * s,
            lfX,
            lfY + 0.5 * s
          );
          ctx.fill();
        }
      }

      // Water edge foam/shore line
      ctx.strokeStyle = "rgba(200,235,255,0.2)";
      ctx.lineWidth = 1 * s;
      drawOrganicWaterShape(ctx, rx0, ry0 + 3 * s, 14 * s, 6 * s, rdSeed, 0.15);
      ctx.stroke();
      ctx.lineCap = "butt";
      break;
    }

    case "fishing_spot": {
      const fx = screenPos.x,
        fy = screenPos.y;
      const fishSeed = (dec.x || 0) * 21.7;

      // Water body — organic shape with depth gradient
      const waterG = ctx.createRadialGradient(
        fx - 2 * s,
        fy + 4 * s,
        2 * s,
        fx,
        fy + 5 * s,
        20 * s
      );
      waterG.addColorStop(0, "rgba(30,80,130,0.65)");
      waterG.addColorStop(0.4, "rgba(50,110,160,0.55)");
      waterG.addColorStop(0.7, "rgba(70,140,190,0.45)");
      waterG.addColorStop(1, "rgba(100,170,210,0.3)");
      ctx.fillStyle = waterG;
      drawOrganicWaterShape(ctx, fx, fy + 5 * s, 20 * s, 9 * s, fishSeed, 0.12);
      ctx.fill();

      // Water surface sheen
      ctx.fillStyle = "rgba(180,220,255,0.12)";
      drawOrganicWaterShape(
        ctx,
        fx + 3 * s,
        fy + 3 * s,
        12 * s,
        5 * s,
        fishSeed + 15,
        0.1
      );
      ctx.fill();

      // Fish shadow under water
      const fishPhase = decorTime * 0.8;
      const fishSx = fx - 6 * s + Math.sin(fishPhase) * 8 * s;
      const fishSy = fy + 6 * s + Math.cos(fishPhase * 0.7) * 2 * s;
      ctx.fillStyle = "rgba(20,50,80,0.2)";
      ctx.save();
      ctx.translate(fishSx, fishSy);
      ctx.rotate(Math.cos(fishPhase) * 0.3);
      ctx.beginPath();
      ctx.ellipse(0, 0, 4 * s, 1.5 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(4 * s, 0);
      ctx.lineTo(6 * s, -1.5 * s);
      ctx.lineTo(6 * s, 1.5 * s);
      ctx.closePath();
      ctx.fill();
      ctx.restore();

      // Lily pads
      for (let lp = 0; lp < 3; lp++) {
        const lpSeed = decorX * 31 + lp * 47;
        const lpx = fx - 10 * s + lp * 8 * s + Math.sin(lpSeed) * 3 * s;
        const lpy = fy + 2 * s + lp * 2 * s + Math.cos(lpSeed) * 1.5 * s;
        ctx.fillStyle = lp % 2 === 0 ? "#2e7d32" : "#388e3c";
        ctx.beginPath();
        ctx.ellipse(lpx, lpy, 3 * s, 1.5 * s, lpSeed * 0.2, 0.2, Math.PI * 1.9);
        ctx.fill();
        if (lp === 1) {
          ctx.fillStyle = "#e91e63";
          ctx.beginPath();
          ctx.arc(lpx, lpy - 1 * s, 1 * s, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "#ffc107";
          ctx.beginPath();
          ctx.arc(lpx, lpy - 1 * s, 0.4 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // Reeds/cattails on far side
      for (let r = 0; r < 4; r++) {
        const rx = fx - 14 * s + r * 5 * s;
        const rBase = fy + 1 * s - r * 0.5 * s;
        const rSway = Math.sin(decorTime * 1.5 + r * 1.2) * 1.5 * s;
        ctx.strokeStyle = "#558b2f";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(rx, rBase);
        ctx.quadraticCurveTo(
          rx + rSway * 0.3,
          rBase - 12 * s,
          rx + rSway,
          rBase - 22 * s
        );
        ctx.stroke();
        if (r % 2 === 0) {
          ctx.fillStyle = "#5d4037";
          ctx.beginPath();
          ctx.ellipse(
            rx + rSway * 0.8,
            rBase - 20 * s,
            1.2 * s,
            3 * s,
            rSway * 0.05,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
        ctx.fillStyle = "#7cb342";
        ctx.beginPath();
        ctx.ellipse(
          rx + rSway * 0.3,
          rBase - 6 * s,
          1 * s,
          5 * s,
          rSway * 0.03,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Dock platform — isometric with plank detail
      const dW = 10 * s,
        dD = 5 * s,
        dH = 3 * s;
      const dkx = fx + 4 * s,
        dky = fy - 1 * s;

      // Support posts
      ctx.fillStyle = "#4e342e";
      for (let p = 0; p < 3; p++) {
        const pt = p / 2;
        const ppx = dkx - dW * ISO_COS * (1 - pt) + dW * ISO_COS * pt;
        const ppy = dky - dD * 0.5 + dD * pt;
        ctx.fillRect(ppx - 1 * s, ppy, 2 * s, 6 * s);
      }

      // Dock top face
      ctx.fillStyle = "#6d4c41";
      ctx.beginPath();
      ctx.moveTo(dkx, dky - dD);
      ctx.lineTo(dkx + dW * ISO_COS, dky - dD * 0.5);
      ctx.lineTo(dkx, dky);
      ctx.lineTo(dkx - dW * ISO_COS, dky - dD * 0.5);
      ctx.closePath();
      ctx.fill();

      // Plank lines on dock
      ctx.strokeStyle = "rgba(30,20,10,0.3)";
      ctx.lineWidth = 0.5 * s;
      for (let pl = 1; pl < 4; pl++) {
        const pt = pl / 4;
        ctx.beginPath();
        ctx.moveTo(dkx - dW * ISO_COS * (1 - pt), dky - dD * (1 - pt * 0.5));
        ctx.lineTo(dkx + dW * ISO_COS * pt, dky - dD * pt * 0.5);
        ctx.stroke();
      }

      // Dock front edge (thickness)
      ctx.fillStyle = "#5d4037";
      ctx.beginPath();
      ctx.moveTo(dkx - dW * ISO_COS, dky - dD * 0.5);
      ctx.lineTo(dkx, dky);
      ctx.lineTo(dkx, dky + dH);
      ctx.lineTo(dkx - dW * ISO_COS, dky - dD * 0.5 + dH);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = "#4e342e";
      ctx.beginPath();
      ctx.moveTo(dkx, dky);
      ctx.lineTo(dkx + dW * ISO_COS, dky - dD * 0.5);
      ctx.lineTo(dkx + dW * ISO_COS, dky - dD * 0.5 + dH);
      ctx.lineTo(dkx, dky + dH);
      ctx.closePath();
      ctx.fill();

      // Fishing rod propped on dock
      ctx.strokeStyle = "#5d4037";
      ctx.lineWidth = 1.8 * s;
      ctx.beginPath();
      ctx.moveTo(dkx + 3 * s, dky - 2 * s);
      ctx.quadraticCurveTo(
        dkx + 14 * s,
        dky - 28 * s,
        dkx + 20 * s,
        dky - 22 * s
      );
      ctx.stroke();
      // Rod handle
      ctx.strokeStyle = "#3e2723";
      ctx.lineWidth = 2.5 * s;
      ctx.beginPath();
      ctx.moveTo(dkx + 1 * s, dky - 1 * s);
      ctx.lineTo(dkx + 3 * s, dky - 2 * s);
      ctx.stroke();
      // Reel
      ctx.fillStyle = "#616161";
      ctx.beginPath();
      ctx.arc(dkx + 5 * s, dky - 5 * s, 1.8 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#9e9e9e";
      ctx.beginPath();
      ctx.arc(dkx + 5 * s, dky - 5 * s, 0.8 * s, 0, Math.PI * 2);
      ctx.fill();

      // Fishing line
      ctx.strokeStyle = "rgba(200,200,200,0.55)";
      ctx.lineWidth = 0.5 * s;
      const bobSway = Math.sin(decorTime * 2) * 2 * s;
      ctx.beginPath();
      ctx.moveTo(dkx + 20 * s, dky - 22 * s);
      ctx.quadraticCurveTo(
        dkx + 18 * s + bobSway * 0.5,
        dky - 5 * s,
        dkx + 14 * s + bobSway,
        fy + 3 * s
      );
      ctx.stroke();

      // Bobber with detail
      const bobberY = fy + 3 * s + Math.sin(decorTime * 2) * 1.5 * s;
      const bobberX = dkx + 14 * s + bobSway;
      ctx.fillStyle = "#d32f2f";
      ctx.beginPath();
      ctx.arc(bobberX, bobberY, 1.8 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#ffffff";
      ctx.beginPath();
      ctx.arc(bobberX, bobberY - 1.2 * s, 1 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#f44336";
      ctx.beginPath();
      ctx.arc(bobberX, bobberY - 0.3 * s, 0.4 * s, 0, Math.PI * 2);
      ctx.fill();

      // Bucket with fish
      const bkx = dkx - 6 * s,
        bky = dky - 1 * s;
      ctx.fillStyle = "#5d4037";
      ctx.beginPath();
      ctx.moveTo(bkx - 2.5 * s, bky);
      ctx.lineTo(bkx - 2 * s, bky - 5 * s);
      ctx.lineTo(bkx + 2 * s, bky - 5 * s);
      ctx.lineTo(bkx + 2.5 * s, bky);
      ctx.closePath();
      ctx.fill();
      // Bucket rim
      ctx.fillStyle = "#8d6e63";
      ctx.beginPath();
      ctx.ellipse(bkx, bky - 5 * s, 2.5 * s, 1 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      // Water in bucket
      ctx.fillStyle = "rgba(70,130,180,0.5)";
      ctx.beginPath();
      ctx.ellipse(bkx, bky - 4.5 * s, 2 * s, 0.7 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      // Fish tail peeking out
      ctx.fillStyle = "#90a4ae";
      ctx.beginPath();
      ctx.moveTo(bkx + 0.5 * s, bky - 5.5 * s);
      ctx.lineTo(bkx + 2.5 * s, bky - 7 * s);
      ctx.lineTo(bkx + 2 * s, bky - 5 * s);
      ctx.closePath();
      ctx.fill();
      // Bucket handle
      ctx.strokeStyle = "#4e342e";
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.arc(bkx, bky - 7 * s, 2.5 * s, Math.PI * 0.15, Math.PI * 0.85);
      ctx.stroke();

      // Multiple ripple rings from bobber
      for (let rp = 0; rp < 3; rp++) {
        const ripPhase = ((decorTime + rp * 0.6) % 2) / 2;
        const ripAlpha = 0.3 - ripPhase * 0.3;
        if (ripAlpha > 0) {
          ctx.strokeStyle = `rgba(150,200,255,${ripAlpha})`;
          ctx.lineWidth = 0.7 * s;
          ctx.beginPath();
          ctx.ellipse(
            bobberX,
            fy + 4 * s,
            3 * s * ripPhase + 2 * s,
            1.2 * s * ripPhase + 0.8 * s,
            0,
            0,
            Math.PI * 2
          );
          ctx.stroke();
        }
      }

      // Water edge highlight
      ctx.strokeStyle = "rgba(180,220,255,0.2)";
      ctx.lineWidth = 1 * s;
      drawOrganicWaterShape(ctx, fx, fy + 5 * s, 20 * s, 9 * s, fishSeed, 0.12);
      ctx.stroke();
      break;
    }

    // === DESERT DECORATIONS ===
    case "palm": {
      const px = screenPos.x;
      const py = screenPos.y;
      const palmVariant = variant || 0;
      const pSway = 0;
      const pSeed = dec.x || 0;
      const pAOff = (((pSeed * 7.3 + 13) % 100) / 100) * 0.6 - 0.3;

      const pb0x = px,
        pb0y = py + 3 * s;
      const pb1x = px + 4 * s + pSway * 0.15,
        pb1y = py - 16 * s;
      const pb2x = px + 8 * s + pSway * 0.35,
        pb2y = py - 36 * s;
      const pb3x = px + 5 * s + pSway,
        pb3y = py - 56 * s;
      const pCrownX = pb3x,
        pCrownY = pb3y;

      // Ground shadow
      drawDirectionalShadow(ctx, px, py + 2 * s, s, 10 * s, 4 * s, 40 * s, 0.2);

      // Trunk: 6 segments, 3 faces each
      const pWBase = 12 * s,
        pWTop = 7 * s;
      for (let i = 5; i >= 0; i--) {
        const t0 = i / 6,
          t1 = (i + 1) / 6;
        const mt0 = 1 - t0,
          mt1 = 1 - t1;
        const cx0 =
          mt0 * mt0 * mt0 * pb0x +
          3 * mt0 * mt0 * t0 * pb1x +
          3 * mt0 * t0 * t0 * pb2x +
          t0 * t0 * t0 * pb3x;
        const cy0 =
          mt0 * mt0 * mt0 * pb0y +
          3 * mt0 * mt0 * t0 * pb1y +
          3 * mt0 * t0 * t0 * pb2y +
          t0 * t0 * t0 * pb3y;
        const cx1 =
          mt1 * mt1 * mt1 * pb0x +
          3 * mt1 * mt1 * t1 * pb1x +
          3 * mt1 * t1 * t1 * pb2x +
          t1 * t1 * t1 * pb3x;
        const cy1 =
          mt1 * mt1 * mt1 * pb0y +
          3 * mt1 * mt1 * t1 * pb1y +
          3 * mt1 * t1 * t1 * pb2y +
          t1 * t1 * t1 * pb3y;
        const w0 = pWBase + (pWTop - pWBase) * t0;
        const w1 = pWBase + (pWTop - pWBase) * t1;
        const lip0 = w0 * 0.22;
        // Left (shadow) face
        ctx.fillStyle = i % 2 === 0 ? "#5a4510" : "#4e3c0e";
        ctx.beginPath();
        ctx.moveTo(cx0 - w0 * 0.5, cy0);
        ctx.lineTo(cx1 - w1 * 0.5, cy1);
        ctx.lineTo(cx1, cy1 + w1 * 0.2);
        ctx.lineTo(cx0, cy0 + lip0);
        ctx.closePath();
        ctx.fill();
        // Right (lit) face
        ctx.fillStyle = i % 2 === 0 ? "#b8891e" : "#a07a18";
        ctx.beginPath();
        ctx.moveTo(cx0 + w0 * 0.5, cy0);
        ctx.lineTo(cx1 + w1 * 0.5, cy1);
        ctx.lineTo(cx1, cy1 + w1 * 0.2);
        ctx.lineTo(cx0, cy0 + lip0);
        ctx.closePath();
        ctx.fill();
        // Front lip
        ctx.fillStyle = i % 2 === 0 ? "#6b5012" : "#5a4210";
        ctx.beginPath();
        ctx.moveTo(cx0 - w0 * 0.5, cy0);
        ctx.lineTo(cx0 + w0 * 0.5, cy0);
        ctx.lineTo(cx0, cy0 + lip0);
        ctx.closePath();
        ctx.fill();
      }

      // Crown hub
      ctx.fillStyle = "#3a5a12";
      ctx.beginPath();
      ctx.ellipse(pCrownX, pCrownY + 2 * s, 8 * s, 4 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Pre-baked frond palettes (4 back + 6 front entries)
      const _FP = [
        {
          back: [
            { dark: "#0c460c", lit: "#2a6a2a", rib: "#1e5a1e" },
            { dark: "#104e10", lit: "#306e30", rib: "#226422" },
            { dark: "#0a3e0a", lit: "#266226", rib: "#1a5218" },
            { dark: "#0c460c", lit: "#2a6a2a", rib: "#1e5a1e" },
          ],
          front: [
            { dark: "#228b22", lit: "#38a138", rib: "#2e972e" },
            { dark: "#2e8b57", lit: "#44a16d", rib: "#3a9763" },
            { dark: "#32a852", lit: "#48be68", rib: "#3eb45e" },
            { dark: "#2e9b47", lit: "#44b15d", rib: "#3aa753" },
            { dark: "#228b22", lit: "#38a138", rib: "#2e972e" },
            { dark: "#32a852", lit: "#48be68", rib: "#3eb45e" },
          ],
        },
        {
          back: [
            { dark: "#105626", lit: "#2c763e", rib: "#226c3c" },
            { dark: "#0e4e1e", lit: "#2a6e36", rib: "#206434" },
            { dark: "#0c4c1c", lit: "#28663a", rib: "#1e5c30" },
            { dark: "#105626", lit: "#2c763e", rib: "#226c3c" },
          ],
          front: [
            { dark: "#2a9040", lit: "#40a656", rib: "#369c4c" },
            { dark: "#30985a", lit: "#46ae70", rib: "#3ca466" },
            { dark: "#36a858", lit: "#4cbe6e", rib: "#42b464" },
            { dark: "#30985a", lit: "#46ae70", rib: "#3ca466" },
            { dark: "#2a9040", lit: "#40a656", rib: "#369c4c" },
            { dark: "#36a858", lit: "#4cbe6e", rib: "#42b464" },
          ],
        },
        {
          back: [
            { dark: "#004016", lit: "#20602e", rib: "#16562c" },
            { dark: "#04481e", lit: "#246836", rib: "#1a5e34" },
            { dark: "#003a12", lit: "#1e5a2c", rib: "#145028" },
            { dark: "#004016", lit: "#20602e", rib: "#16562c" },
          ],
          front: [
            { dark: "#1a7828", lit: "#308e3e", rib: "#268434" },
            { dark: "#228b40", lit: "#38a156", rib: "#2e974c" },
            { dark: "#28a04a", lit: "#3eb660", rib: "#34ac56" },
            { dark: "#228b40", lit: "#38a156", rib: "#2e974c" },
            { dark: "#1a7828", lit: "#308e3e", rib: "#268434" },
            { dark: "#28a04a", lit: "#3eb660", rib: "#34ac56" },
          ],
        },
      ];
      const fPal = _FP[palmVariant % 3];

      // Back fronds (4, half sway)
      const _bkL = [
        { a: -2.5, l: 36, p: 0 },
        { a: -1.2, l: 33, p: 1.5 },
        { a: 0.4, l: 31, p: 3.2 },
        { a: 1.9, l: 35, p: 5 },
      ];
      for (let fi = 0; fi < 4; fi++) {
        const f = _bkL[fi];
        _drawPalmFrondItem(
          ctx,
          pCrownX,
          pCrownY,
          f.a + pAOff,
          f.l * s,
          fPal.back[fi],
          s,
          0
        );
      }
      // Front fronds (6, static)
      const _ftL = [
        { a: -2.3, l: 42, p: 0.5 },
        { a: -1.3, l: 40, p: 1.8 },
        { a: -0.2, l: 44, p: 3 },
        { a: 0.7, l: 42, p: 4.2 },
        { a: 1.6, l: 39, p: 5.5 },
        { a: 2.5, l: 36, p: 0.8 },
      ];
      for (let fi = 0; fi < 6; fi++) {
        const f = _ftL[fi];
        _drawPalmFrondItem(
          ctx,
          pCrownX,
          pCrownY,
          f.a + pAOff,
          f.l * s,
          fPal.front[fi],
          s,
          0
        );
      }

      // Coconuts (variant 0 and 2)
      if (palmVariant === 0 || palmVariant === 2) {
        const cBase = palmVariant === 2 ? "#b89830" : "#5a3a1a";
        const cHi = palmVariant === 2 ? "#e8c860" : "#8a6a3a";
        ctx.fillStyle = cBase;
        ctx.beginPath();
        ctx.arc(pCrownX - 3.5 * s, pCrownY + 4 * s, 3.5 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.arc(pCrownX + 3 * s, pCrownY + 3.5 * s, 3.2 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.arc(pCrownX, pCrownY + 5.5 * s, 3 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = cHi;
        ctx.beginPath();
        ctx.arc(pCrownX - 4.5 * s, pCrownY + 3 * s, 1.6 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.arc(pCrownX + 2 * s, pCrownY + 2.5 * s, 1.4 * s, 0, Math.PI * 2);
        ctx.fill();
      }

      // Flowers (variant 1 and 2)
      if (palmVariant === 1 || palmVariant === 2) {
        const pPetalCol = palmVariant === 2 ? "#ff9eb0" : "#f8bbd0";
        const pCenterCol = palmVariant === 2 ? "#ffd54f" : "#ffeb3b";
        ctx.fillStyle = pPetalCol;
        for (let bi = 0; bi < 2; bi++) {
          const bfx = pCrownX + (bi === 0 ? -5 : 4) * s;
          const bfy = pCrownY + (bi === 0 ? 2 : 1.5) * s;
          for (let p = 0; p < 3; p++) {
            const pa = (p / 3) * Math.PI * 2 + bi;
            ctx.beginPath();
            ctx.ellipse(
              bfx + Math.cos(pa) * 3 * s,
              bfy + Math.sin(pa) * 1.5 * s,
              2.5 * s,
              1.3 * s,
              pa,
              0,
              Math.PI * 2
            );
            ctx.fill();
          }
        }
        ctx.fillStyle = pCenterCol;
        ctx.beginPath();
        ctx.arc(pCrownX - 5 * s, pCrownY + 2 * s, 1.5 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.arc(pCrownX + 4 * s, pCrownY + 1.5 * s, 1.5 * s, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }
    case "cactus": {
      // Enhanced 3D isometric cactus with saguaro style
      const cacBaseX = screenPos.x;
      const cacBaseY = screenPos.y;

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        cacBaseX,
        cacBaseY + 2 * s,
        s,
        14 * s,
        6 * s,
        38 * s,
        0.25
      );

      // Main body gradient for 3D roundness
      const cacGrad = ctx.createLinearGradient(
        cacBaseX - 8 * s,
        0,
        cacBaseX + 8 * s,
        0
      );
      cacGrad.addColorStop(0, "#1a4a1a");
      cacGrad.addColorStop(0.3, "#2d6a2d");
      cacGrad.addColorStop(0.5, "#3a8a3a");
      cacGrad.addColorStop(0.7, "#2d6a2d");
      cacGrad.addColorStop(1, "#1a4a1a");

      // Main body - rounded pillar shape
      ctx.fillStyle = cacGrad;
      ctx.beginPath();
      ctx.moveTo(cacBaseX - 7 * s, cacBaseY + 3 * s);
      ctx.quadraticCurveTo(
        cacBaseX - 8 * s,
        cacBaseY - 15 * s,
        cacBaseX - 6 * s,
        cacBaseY - 30 * s
      );
      ctx.quadraticCurveTo(
        cacBaseX,
        cacBaseY - 38 * s,
        cacBaseX + 6 * s,
        cacBaseY - 30 * s
      );
      ctx.quadraticCurveTo(
        cacBaseX + 8 * s,
        cacBaseY - 15 * s,
        cacBaseX + 7 * s,
        cacBaseY + 3 * s
      );
      ctx.closePath();
      ctx.fill();

      // Vertical ridges for texture
      ctx.strokeStyle = "rgba(0,50,0,0.3)";
      ctx.lineWidth = 1 * s;
      for (let r = -2; r <= 2; r++) {
        ctx.beginPath();
        ctx.moveTo(cacBaseX + r * 2.5 * s, cacBaseY + 2 * s);
        ctx.quadraticCurveTo(
          cacBaseX + r * 2 * s,
          cacBaseY - 15 * s,
          cacBaseX + r * 1.5 * s,
          cacBaseY - 32 * s
        );
        ctx.stroke();
      }

      // Arms for larger variants
      if (variant > 0) {
        // Left arm
        const armGrad = ctx.createLinearGradient(
          cacBaseX - 20 * s,
          0,
          cacBaseX - 10 * s,
          0
        );
        armGrad.addColorStop(0, "#1a4a1a");
        armGrad.addColorStop(0.5, "#3a8a3a");
        armGrad.addColorStop(1, "#2d6a2d");

        ctx.fillStyle = armGrad;
        ctx.beginPath();
        ctx.moveTo(cacBaseX - 6 * s, cacBaseY - 12 * s);
        ctx.quadraticCurveTo(
          cacBaseX - 18 * s,
          cacBaseY - 14 * s,
          cacBaseX - 18 * s,
          cacBaseY - 22 * s
        );
        ctx.quadraticCurveTo(
          cacBaseX - 18 * s,
          cacBaseY - 30 * s,
          cacBaseX - 14 * s,
          cacBaseY - 30 * s
        );
        ctx.quadraticCurveTo(
          cacBaseX - 12 * s,
          cacBaseY - 30 * s,
          cacBaseX - 12 * s,
          cacBaseY - 22 * s
        );
        ctx.quadraticCurveTo(
          cacBaseX - 12 * s,
          cacBaseY - 16 * s,
          cacBaseX - 6 * s,
          cacBaseY - 15 * s
        );
        ctx.closePath();
        ctx.fill();

        // Right arm for variant > 1
        if (variant > 1) {
          ctx.fillStyle = armGrad;
          ctx.beginPath();
          ctx.moveTo(cacBaseX + 6 * s, cacBaseY - 8 * s);
          ctx.quadraticCurveTo(
            cacBaseX + 15 * s,
            cacBaseY - 10 * s,
            cacBaseX + 15 * s,
            cacBaseY - 18 * s
          );
          ctx.quadraticCurveTo(
            cacBaseX + 15 * s,
            cacBaseY - 26 * s,
            cacBaseX + 11 * s,
            cacBaseY - 26 * s
          );
          ctx.quadraticCurveTo(
            cacBaseX + 9 * s,
            cacBaseY - 26 * s,
            cacBaseX + 9 * s,
            cacBaseY - 18 * s
          );
          ctx.quadraticCurveTo(
            cacBaseX + 9 * s,
            cacBaseY - 12 * s,
            cacBaseX + 6 * s,
            cacBaseY - 11 * s
          );
          ctx.closePath();
          ctx.fill();
        }
      }

      // Spines (small dots)
      ctx.fillStyle = "#e8e0c0";
      for (let sy = 0; sy < 6; sy++) {
        for (let sx = -1; sx <= 1; sx++) {
          const spineX = cacBaseX + sx * 4 * s + (sy % 2) * 2 * s;
          const spineY = cacBaseY - 5 * s - sy * 5 * s;
          ctx.beginPath();
          ctx.arc(spineX, spineY, 0.8 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // Top highlight
      ctx.fillStyle = "rgba(255,255,255,0.15)";
      ctx.beginPath();
      ctx.ellipse(cacBaseX, cacBaseY - 33 * s, 4 * s, 2 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      break;
    }
    case "dune": {
      const duneBaseX = screenPos.x;
      const duneBaseY = screenPos.y;
      const duneSeed = decorX * 3.7 + decorY * 8.9;

      // Ground shadow - wide, soft organic blob
      const groundShadowGrad = ctx.createRadialGradient(
        duneBaseX,
        duneBaseY + 14 * s,
        0,
        duneBaseX,
        duneBaseY + 14 * s,
        70 * s
      );
      groundShadowGrad.addColorStop(0, "rgba(110,80,35,0.3)");
      groundShadowGrad.addColorStop(0.5, "rgba(100,75,30,0.15)");
      groundShadowGrad.addColorStop(1, "transparent");
      ctx.fillStyle = groundShadowGrad;
      drawOrganicBlobAt(
        ctx,
        duneBaseX,
        duneBaseY + 14 * s,
        68 * s,
        22 * s,
        duneSeed,
        0.12,
        32
      );
      ctx.fill();

      // Base sand spread - wide blob that grounds the dune into terrain
      const baseSandGrad = ctx.createRadialGradient(
        duneBaseX,
        duneBaseY + 8 * s,
        0,
        duneBaseX,
        duneBaseY + 8 * s,
        62 * s
      );
      baseSandGrad.addColorStop(0, "#c9a040");
      baseSandGrad.addColorStop(0.5, "#bfa040");
      baseSandGrad.addColorStop(0.8, "#b08830");
      baseSandGrad.addColorStop(1, "rgba(168,128,48,0)");
      ctx.fillStyle = baseSandGrad;
      drawOrganicBlobAt(
        ctx,
        duneBaseX,
        duneBaseY + 8 * s,
        58 * s,
        20 * s,
        duneSeed * 1.3,
        0.15,
        32
      );
      ctx.fill();

      // Main dune body - large organic blob with radial gradient for 3D depth
      const duneBodyGrad = ctx.createRadialGradient(
        duneBaseX - 8 * s,
        duneBaseY - 10 * s,
        0,
        duneBaseX,
        duneBaseY,
        50 * s
      );
      duneBodyGrad.addColorStop(0, "#f0d070");
      duneBodyGrad.addColorStop(0.35, "#e8c060");
      duneBodyGrad.addColorStop(0.65, "#d4a84b");
      duneBodyGrad.addColorStop(1, "#b89035");
      ctx.fillStyle = duneBodyGrad;
      drawOrganicBlobAt(
        ctx,
        duneBaseX - 5 * s,
        duneBaseY - 2 * s,
        48 * s,
        22 * s,
        duneSeed * 2.1,
        0.18,
        32
      );
      ctx.fill();

      // Primary peak mound - taller blob offset upward
      const peakGrad = ctx.createRadialGradient(
        duneBaseX - 12 * s,
        duneBaseY - 20 * s,
        0,
        duneBaseX - 6 * s,
        duneBaseY - 8 * s,
        30 * s
      );
      peakGrad.addColorStop(0, "#f5db78");
      peakGrad.addColorStop(0.5, "#e8c060");
      peakGrad.addColorStop(1, "#d4a84b");
      ctx.fillStyle = peakGrad;
      drawOrganicBlobAt(
        ctx,
        duneBaseX - 8 * s,
        duneBaseY - 10 * s,
        32 * s,
        18 * s,
        duneSeed * 3.7,
        0.2,
        32
      );
      ctx.fill();

      // Secondary ridge blob offset to the right
      const ridgeGrad = ctx.createRadialGradient(
        duneBaseX + 16 * s,
        duneBaseY - 6 * s,
        0,
        duneBaseX + 16 * s,
        duneBaseY + 2 * s,
        26 * s
      );
      ridgeGrad.addColorStop(0, "#ddb855");
      ridgeGrad.addColorStop(0.6, "#d4a84b");
      ridgeGrad.addColorStop(1, "#b89035");
      ctx.fillStyle = ridgeGrad;
      drawOrganicBlobAt(
        ctx,
        duneBaseX + 16 * s,
        duneBaseY,
        26 * s,
        14 * s,
        duneSeed * 5.3,
        0.18,
        28
      );
      ctx.fill();

      // Sunlit highlight blob on the peak
      const highlightGrad = ctx.createRadialGradient(
        duneBaseX - 14 * s,
        duneBaseY - 18 * s,
        0,
        duneBaseX - 10 * s,
        duneBaseY - 14 * s,
        18 * s
      );
      highlightGrad.addColorStop(0, "rgba(255,235,160,0.5)");
      highlightGrad.addColorStop(0.6, "rgba(245,220,130,0.2)");
      highlightGrad.addColorStop(1, "transparent");
      ctx.fillStyle = highlightGrad;
      drawOrganicBlobAt(
        ctx,
        duneBaseX - 12 * s,
        duneBaseY - 14 * s,
        20 * s,
        12 * s,
        duneSeed * 7.1,
        0.12,
        28
      );
      ctx.fill();

      // Shadow blob on the lee side for depth
      const shadowGrad = ctx.createRadialGradient(
        duneBaseX + 18 * s,
        duneBaseY + 4 * s,
        0,
        duneBaseX + 18 * s,
        duneBaseY + 4 * s,
        22 * s
      );
      shadowGrad.addColorStop(0, "rgba(140,100,40,0.35)");
      shadowGrad.addColorStop(0.7, "rgba(130,95,35,0.15)");
      shadowGrad.addColorStop(1, "transparent");
      ctx.fillStyle = shadowGrad;
      drawOrganicBlobAt(
        ctx,
        duneBaseX + 18 * s,
        duneBaseY + 4 * s,
        22 * s,
        10 * s,
        duneSeed * 9.3,
        0.14,
        24
      );
      ctx.fill();

      // Wind-blown crest highlight with glow
      setShadowBlur(ctx, 4 * s, "rgba(255,230,150,0.35)");
      ctx.strokeStyle = "rgba(245,225,140,0.7)";
      ctx.lineWidth = 1.5 * s;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(duneBaseX - 25 * s, duneBaseY - 6 * s);
      ctx.bezierCurveTo(
        duneBaseX - 15 * s,
        duneBaseY - 18 * s,
        duneBaseX - 5 * s,
        duneBaseY - 22 * s,
        duneBaseX + 8 * s,
        duneBaseY - 10 * s
      );
      ctx.stroke();
      clearShadow(ctx);

      // Wind ripple texture across the dune face
      ctx.strokeStyle = "rgba(160,120,50,0.25)";
      ctx.lineWidth = 0.7 * s;
      for (let r = 0; r < 6; r++) {
        const ry = duneBaseY + r * 2.8 * s - 2 * s;
        const rxOff = r * 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(duneBaseX - 35 * s + rxOff, ry + 2 * s);
        ctx.bezierCurveTo(
          duneBaseX - 18 * s + rxOff,
          ry - 1.5 * s,
          duneBaseX + 2 * s + rxOff,
          ry - 1.5 * s,
          duneBaseX + 22 * s + rxOff * 0.5,
          ry + 2 * s
        );
        ctx.stroke();
      }

      // Sand grain texture highlights
      ctx.fillStyle = "rgba(255,235,180,0.2)";
      for (let g = 0; g < 10; g++) {
        const gx =
          duneBaseX - 30 * s + g * 8 * s + Math.sin(g * 1.7 + duneSeed) * 5 * s;
        const gy = duneBaseY - 5 * s + Math.cos(g * 2.3 + duneSeed) * 8 * s;
        ctx.beginPath();
        ctx.ellipse(gx, gy, 2.2 * s, 0.8 * s, 0.5, 0, Math.PI * 2);
        ctx.fill();
      }

      // Animated wind-blown sand particles
      ctx.fillStyle = "rgba(240,215,130,0.35)";
      const windOffset = Math.sin(decorTime * 2.5) * 6 * s;
      const windOffset2 = Math.cos(decorTime * 1.8) * 4 * s;
      for (let p = 0; p < 5; p++) {
        const px =
          duneBaseX - 15 * s + p * 10 * s + windOffset + (p % 2) * windOffset2;
        const py =
          duneBaseY - 16 * s + p * 3 * s + Math.sin(decorTime * 3 + p) * 2 * s;
        ctx.beginPath();
        ctx.ellipse(px, py, 4 * s, 1.2 * s, 0.4, 0, Math.PI * 2);
        ctx.fill();
      }

      // Scattered sand at the base edges for natural blending
      ctx.fillStyle = "rgba(200,160,80,0.25)";
      for (let sp = 0; sp < 14; sp++) {
        const angle = (sp / 14) * Math.PI * 2;
        const dist = 52 * s + Math.sin(sp * 2.7 + duneSeed) * 8 * s;
        const spx = duneBaseX + Math.cos(angle) * dist * 0.9;
        const spy = duneBaseY + 10 * s + Math.sin(angle) * dist * 0.3;
        const spSize =
          1.5 * s + Math.abs(Math.sin(sp * 1.3 + duneSeed)) * 2 * s;
        ctx.beginPath();
        ctx.ellipse(
          spx,
          spy,
          spSize,
          spSize * 0.4,
          angle * 0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }
    case "pyramid": {
      const pyrX = screenPos.x;
      const pyrY = screenPos.y;

      // Proper 2:1 isometric pyramid geometry
      const pBase = 56 * s;
      const pHeight = 65 * s;
      const pyrHW = pBase * ISO_COS;
      const pyrHD = pBase * ISO_SIN;
      const baseCy = pyrY + 3 * s;

      const frontX = pyrX;
      const frontY = baseCy + pyrHD;
      const rightX = pyrX + pyrHW;
      const rightY = baseCy;
      const leftX = pyrX - pyrHW;
      const leftY = baseCy;
      const tipY = baseCy - pHeight;

      // Triangular cast shadow (light from upper-left, shadow to lower-right)
      const backX = pyrX;
      const backY = baseCy - pyrHD;
      const shadowApexX = pyrX + 42 * s;
      const shadowApexY = baseCy + 18 * s;

      if (!landmarkSkipShadow) {
        // Soft outer penumbra layer
        ctx.beginPath();
        ctx.moveTo(backX - 1 * s, backY - 2 * s);
        ctx.lineTo(leftX - 4 * s, leftY + 2 * s);
        ctx.lineTo(frontX - 1 * s, frontY + 5 * s);
        ctx.lineTo(shadowApexX + 7 * s, shadowApexY + 4 * s);
        ctx.lineTo(rightX + 7 * s, rightY - 1 * s);
        ctx.lineTo(backX + 3 * s, backY - 3 * s);
        ctx.closePath();
        ctx.fillStyle = "rgba(0,0,0,0.05)";
        ctx.fill();

        // Mid penumbra layer
        ctx.beginPath();
        ctx.moveTo(backX, backY - 1 * s);
        ctx.lineTo(leftX - 2 * s, leftY + 1 * s);
        ctx.lineTo(frontX, frontY + 3 * s);
        ctx.lineTo(shadowApexX + 4 * s, shadowApexY + 2 * s);
        ctx.lineTo(rightX + 4 * s, rightY);
        ctx.lineTo(backX + 1 * s, backY - 2 * s);
        ctx.closePath();
        ctx.fillStyle = "rgba(0,0,0,0.08)";
        ctx.fill();

        // Core shadow with gradient fade toward tip
        ctx.beginPath();
        ctx.moveTo(backX, backY);
        ctx.lineTo(leftX, leftY);
        ctx.lineTo(frontX, frontY);
        ctx.lineTo(shadowApexX, shadowApexY);
        ctx.lineTo(rightX, rightY);
        ctx.closePath();
        const pyrShadowGrad = ctx.createLinearGradient(
          pyrX - pyrHW * 0.5,
          baseCy,
          shadowApexX,
          shadowApexY
        );
        pyrShadowGrad.addColorStop(0, "rgba(0,0,0,0.30)");
        pyrShadowGrad.addColorStop(0.35, "rgba(0,0,0,0.22)");
        pyrShadowGrad.addColorStop(0.6, "rgba(0,0,0,0.10)");
        pyrShadowGrad.addColorStop(1, "rgba(0,0,0,0)");
        ctx.fillStyle = pyrShadowGrad;
        ctx.fill();
      }

      // Left face (shadow) with gradient
      const leftFaceGrad = ctx.createLinearGradient(leftX, leftY, pyrX, tipY);
      leftFaceGrad.addColorStop(0, "#7a6030");
      leftFaceGrad.addColorStop(0.4, "#8a7035");
      leftFaceGrad.addColorStop(0.8, "#9a7a3a");
      leftFaceGrad.addColorStop(1, "#a8843f");
      ctx.fillStyle = leftFaceGrad;
      ctx.beginPath();
      ctx.moveTo(pyrX, tipY);
      ctx.lineTo(leftX, leftY);
      ctx.lineTo(frontX, frontY);
      ctx.closePath();
      ctx.fill();

      // Right face (lit) with gradient
      const rightFaceGrad = ctx.createLinearGradient(
        pyrX,
        tipY,
        rightX,
        rightY
      );
      rightFaceGrad.addColorStop(0, "#e8c860");
      rightFaceGrad.addColorStop(0.3, "#d4a84b");
      rightFaceGrad.addColorStop(0.7, "#c9983f");
      rightFaceGrad.addColorStop(1, "#b88832");
      ctx.fillStyle = rightFaceGrad;
      ctx.beginPath();
      ctx.moveTo(pyrX, tipY);
      ctx.lineTo(rightX, rightY);
      ctx.lineTo(frontX, frontY);
      ctx.closePath();
      ctx.fill();

      // Stone block courses (10 rows)
      ctx.strokeStyle = "rgba(0,0,0,0.10)";
      ctx.lineWidth = 0.7 * s;
      for (let row = 1; row <= 10; row++) {
        const t = row / 11;
        const rowTipToLeft_X = pyrX + (leftX - pyrX) * t;
        const rowTipToLeft_Y = tipY + (leftY - tipY) * t;
        const rowTipToRight_X = pyrX + (rightX - pyrX) * t;
        const rowTipToRight_Y = tipY + (rightY - tipY) * t;
        const rowTipToFront_X = pyrX + (frontX - pyrX) * t;
        const rowTipToFront_Y = tipY + (frontY - tipY) * t;

        // Left face course line
        ctx.beginPath();
        ctx.moveTo(rowTipToLeft_X, rowTipToLeft_Y);
        ctx.lineTo(rowTipToFront_X, rowTipToFront_Y);
        ctx.stroke();
        // Right face course line
        ctx.beginPath();
        ctx.moveTo(rowTipToFront_X, rowTipToFront_Y);
        ctx.lineTo(rowTipToRight_X, rowTipToRight_Y);
        ctx.stroke();

        // Vertical block joints (perspective-correct)
        const blocks = Math.max(2, Math.floor(row * 1.3));
        ctx.strokeStyle = "rgba(0,0,0,0.06)";
        for (let b = 1; b < blocks; b++) {
          const bf = b / blocks;
          // Left face joints
          const jlx = rowTipToLeft_X + (rowTipToFront_X - rowTipToLeft_X) * bf;
          const jly = rowTipToLeft_Y + (rowTipToFront_Y - rowTipToLeft_Y) * bf;
          const prevT = (row - 1) / 11;
          const prevLX = pyrX + (leftX - pyrX) * prevT;
          const prevLY = tipY + (leftY - tipY) * prevT;
          const prevFX = pyrX + (frontX - pyrX) * prevT;
          const prevFY = tipY + (frontY - tipY) * prevT;
          const jlxTop = prevLX + (prevFX - prevLX) * bf;
          const jlyTop = prevLY + (prevFY - prevLY) * bf;
          ctx.beginPath();
          ctx.moveTo(jlx, jly);
          ctx.lineTo(jlxTop, jlyTop);
          ctx.stroke();
          // Right face joints
          const jrx =
            rowTipToFront_X + (rowTipToRight_X - rowTipToFront_X) * bf;
          const jry =
            rowTipToFront_Y + (rowTipToRight_Y - rowTipToFront_Y) * bf;
          const prevRX = pyrX + (rightX - pyrX) * prevT;
          const prevRY = tipY + (rightY - tipY) * prevT;
          const jrxTop = prevFX + (prevRX - prevFX) * bf;
          const jryTop = prevFY + (prevRY - prevFY) * bf;
          ctx.beginPath();
          ctx.moveTo(jrx, jry);
          ctx.lineTo(jrxTop, jryTop);
          ctx.stroke();
        }
        ctx.strokeStyle = "rgba(0,0,0,0.10)";
      }

      // Visible base edge
      ctx.strokeStyle = "rgba(60, 45, 25, 0.3)";
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(leftX, leftY);
      ctx.lineTo(frontX, frontY);
      ctx.lineTo(rightX, rightY);
      ctx.stroke();

      // Side edge lines
      ctx.strokeStyle = "rgba(60, 45, 25, 0.15)";
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(pyrX, tipY);
      ctx.lineTo(frontX, frontY);
      ctx.stroke();

      // Edge highlight (front ridge)
      ctx.strokeStyle = "rgba(255,220,140,0.4)";
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(pyrX, tipY + 12 * s);
      ctx.lineTo(frontX, frontY);
      ctx.stroke();

      // Weathering pitting
      ctx.fillStyle = "rgba(80, 60, 30, 0.08)";
      for (let pit = 0; pit < 8; pit++) {
        const pitT = 0.3 + ((pit * 47) % 7) / 10;
        const pitSide = pit % 2 === 0 ? 1 : -1;
        const pitBf = 0.2 + ((pit * 31) % 6) / 10;
        const baseX = pitSide > 0 ? rightX : leftX;
        const baseY = pitSide > 0 ? rightY : leftY;
        const edgeX = pyrX + (baseX - pyrX) * pitT;
        const edgeY = tipY + (baseY - tipY) * pitT;
        const frontEdgeX = pyrX + (frontX - pyrX) * pitT;
        const frontEdgeY = tipY + (frontY - tipY) * pitT;
        const px = edgeX + (frontEdgeX - edgeX) * pitBf;
        const py = edgeY + (frontEdgeY - edgeY) * pitBf;
        ctx.beginPath();
        ctx.arc(px, py, (0.5 + ((pit * 23) % 4) / 4) * s, 0, Math.PI * 2);
        ctx.fill();
      }

      // === GOLDEN PYRAMIDION (TIP) ===
      const capHeight = 18 * s;
      const capFrac = capHeight / pHeight;
      const capFrontX = pyrX + (frontX - pyrX) * capFrac;
      const capFrontY = tipY + (frontY - tipY) * capFrac;
      const capRightX = pyrX + (rightX - pyrX) * capFrac;
      const capRightY = tipY + (rightY - tipY) * capFrac;
      const capLeftX = pyrX + (leftX - pyrX) * capFrac;
      const capLeftY = tipY + (leftY - tipY) * capFrac;

      setShadowBlur(ctx, 20 * s, "rgba(255,215,100,0.8)");

      // Cap left face
      const capLeftGrad = ctx.createLinearGradient(
        capLeftX,
        capLeftY,
        pyrX,
        tipY
      );
      capLeftGrad.addColorStop(0, "#b8860b");
      capLeftGrad.addColorStop(0.3, "#d4a020");
      capLeftGrad.addColorStop(0.7, "#e8b830");
      capLeftGrad.addColorStop(1, "#f5d050");
      ctx.fillStyle = capLeftGrad;
      ctx.beginPath();
      ctx.moveTo(pyrX, tipY);
      ctx.lineTo(capLeftX, capLeftY);
      ctx.lineTo(capFrontX, capFrontY);
      ctx.closePath();
      ctx.fill();

      // Cap right face
      const capRightGrad = ctx.createLinearGradient(
        pyrX,
        tipY,
        capRightX,
        capRightY
      );
      capRightGrad.addColorStop(0, "#fff8e0");
      capRightGrad.addColorStop(0.2, "#ffd700");
      capRightGrad.addColorStop(0.5, "#f0c030");
      capRightGrad.addColorStop(1, "#d4a020");
      ctx.fillStyle = capRightGrad;
      ctx.beginPath();
      ctx.moveTo(pyrX, tipY);
      ctx.lineTo(capRightX, capRightY);
      ctx.lineTo(capFrontX, capFrontY);
      ctx.closePath();
      ctx.fill();

      // Front edge highlight on cap
      ctx.strokeStyle = "rgba(255,255,255,0.5)";
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(pyrX, tipY);
      ctx.lineTo(capFrontX, capFrontY);
      ctx.stroke();

      clearShadow(ctx);

      // Bright tip highlight
      const tipHighlightGrad = ctx.createRadialGradient(
        pyrX,
        tipY + 3 * s,
        0,
        pyrX,
        tipY + 3 * s,
        8 * s
      );
      tipHighlightGrad.addColorStop(0, "rgba(255,255,255,0.9)");
      tipHighlightGrad.addColorStop(0.3, "rgba(255,250,200,0.7)");
      tipHighlightGrad.addColorStop(0.6, "rgba(255,230,150,0.3)");
      tipHighlightGrad.addColorStop(1, "transparent");
      ctx.fillStyle = tipHighlightGrad;
      ctx.beginPath();
      ctx.arc(pyrX, tipY + 3 * s, 8 * s, 0, Math.PI * 2);
      ctx.fill();

      // Animated sparkle
      const sparklePhase = decorTime * 3;
      const sparkleIntensity = (Math.sin(sparklePhase) + 1) / 2;
      ctx.fillStyle = `rgba(255,255,255,${0.4 + sparkleIntensity * 0.5})`;
      ctx.beginPath();
      ctx.moveTo(pyrX, tipY - 5 * s * sparkleIntensity);
      ctx.lineTo(pyrX + 2 * s, tipY + 3 * s);
      ctx.lineTo(pyrX, tipY + 8 * s * sparkleIntensity);
      ctx.lineTo(pyrX - 2 * s, tipY + 3 * s);
      ctx.closePath();
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(pyrX - 6 * s * sparkleIntensity, tipY + 2 * s);
      ctx.lineTo(pyrX, tipY + 4 * s);
      ctx.lineTo(pyrX + 6 * s * sparkleIntensity, tipY + 2 * s);
      ctx.lineTo(pyrX, tipY);
      ctx.closePath();
      ctx.fill();

      // Secondary sparkles
      const sparkle2Phase = decorTime * 2.3;
      const sparkle2Int = (Math.sin(sparkle2Phase + 1) + 1) / 2;
      ctx.fillStyle = `rgba(255,250,200,${0.3 + sparkle2Int * 0.4})`;
      ctx.beginPath();
      ctx.arc(pyrX + 5 * s, tipY + 8 * s, 2 * s * sparkle2Int, 0, Math.PI * 2);
      ctx.fill();
      const sparkle3Int = (Math.sin(sparkle2Phase + 2.5) + 1) / 2;
      ctx.fillStyle = `rgba(255,250,200,${0.3 + sparkle3Int * 0.4})`;
      ctx.beginPath();
      ctx.arc(
        pyrX - 4 * s,
        tipY + 10 * s,
        1.8 * s * sparkle3Int,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Lens flare
      if (sparkleIntensity > 0.7) {
        const flareAlpha = (sparkleIntensity - 0.7) * 2;
        ctx.fillStyle = `rgba(255,255,240,${flareAlpha * 0.15})`;
        ctx.beginPath();
        ctx.ellipse(
          pyrX + 15 * s,
          tipY + 15 * s,
          12 * s,
          4 * s,
          0.5,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          pyrX - 10 * s,
          tipY + 20 * s,
          8 * s,
          3 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }
    case "obelisk": {
      const ox = screenPos.x;
      const oy = screenPos.y;

      // Regional palettes — each region has 3 sub-variants
      const obelRegionPals: Record<
        string,
        {
          left: string;
          right: string;
          front: string;
          top: string;
          cap: string;
          capDk: string;
          capFr: string;
          capHi: string;
          glyph: string;
          pedTop: string;
          pedLeft: string;
          pedRight: string;
        }[]
      > = {
        desert: [
          {
            cap: "#e8c040",
            capDk: "#d0a830",
            capFr: "#dab438",
            capHi: "#f8e060",
            front: "#c0aa78",
            glyph: "#7a6a48",
            left: "#b09a68",
            pedLeft: "#9a8a58",
            pedRight: "#b0a068",
            pedTop: "#c8b888",
            right: "#c8b280",
            top: "#d8c898",
          },
          {
            cap: "#dab040",
            capDk: "#c09830",
            capFr: "#cca438",
            capHi: "#f0d858",
            front: "#b09868",
            glyph: "#6a5a38",
            left: "#a08858",
            pedLeft: "#8a7a48",
            pedRight: "#a09058",
            pedTop: "#b8a878",
            right: "#b8a070",
            top: "#c8b888",
          },
          {
            cap: "#c8a838",
            capDk: "#b09028",
            capFr: "#bc9c30",
            capHi: "#e0c850",
            front: "#9a8858",
            glyph: "#5a4a28",
            left: "#907848",
            pedLeft: "#7a6a38",
            pedRight: "#908048",
            pedTop: "#a89868",
            right: "#a89060",
            top: "#b8a878",
          },
        ],
        grassland: [
          {
            cap: "#d4a840",
            capDk: "#be9428",
            capFr: "#ca9e3b",
            capHi: "#f0d060",
            front: "#b0a068",
            glyph: "#5a4a38",
            left: "#8a7a58",
            pedLeft: "#807048",
            pedRight: "#988860",
            pedTop: "#b8a878",
            right: "#a89870",
            top: "#c8b888",
          },
          {
            cap: "#c4c0b0",
            capDk: "#b2aea0",
            capFr: "#bdb9ab",
            capHi: "#e4e0d4",
            front: "#666258",
            glyph: "#3a3830",
            left: "#58544a",
            pedLeft: "#4a4840",
            pedRight: "#605e54",
            pedTop: "#7a766a",
            right: "#706c5e",
            top: "#8a8678",
          },
          {
            cap: "#c0a048",
            capDk: "#a88a38",
            capFr: "#b49840",
            capHi: "#dcc060",
            front: "#7a7258",
            glyph: "#4a4230",
            left: "#6a6248",
            pedLeft: "#605838",
            pedRight: "#786848",
            pedTop: "#9a9068",
            right: "#8a8268",
            top: "#a09878",
          },
        ],
        swamp: [
          {
            cap: "#40a848",
            capDk: "#2a8a30",
            capFr: "#38983c",
            capHi: "#60d868",
            front: "#3e5e3c",
            glyph: "#1a3a18",
            left: "#3a5a38",
            pedLeft: "#2a4a28",
            pedRight: "#3a5a38",
            pedTop: "#4a6a48",
            right: "#4a6a48",
            top: "#5a7a58",
          },
          {
            cap: "#58886a",
            capDk: "#3a6a4a",
            capFr: "#487a5a",
            capHi: "#78b88a",
            front: "#2e3e2c",
            glyph: "#0a2a08",
            left: "#2a3a28",
            pedLeft: "#1a2a18",
            pedRight: "#2a3a28",
            pedTop: "#3a4a38",
            right: "#3a4a38",
            top: "#4a5a48",
          },
          {
            cap: "#4a8a3a",
            capDk: "#2a6a22",
            capFr: "#3a7a2e",
            capHi: "#6ac04a",
            front: "#3c4c34",
            glyph: "#1a2a10",
            left: "#3a4a30",
            pedLeft: "#2a3a20",
            pedRight: "#3a4a30",
            pedTop: "#4a5a40",
            right: "#4a5a40",
            top: "#5a6a50",
          },
        ],
        volcanic: [
          {
            cap: "#c84020",
            capDk: "#a03018",
            capFr: "#b4381c",
            capHi: "#f06030",
            front: "#503030",
            glyph: "#2a1010",
            left: "#4a2828",
            pedLeft: "#3a2020",
            pedRight: "#4a2828",
            pedTop: "#583838",
            right: "#5a3838",
            top: "#684848",
          },
          {
            cap: "#d85828",
            capDk: "#b04018",
            capFr: "#c44820",
            capHi: "#f87838",
            front: "#402828",
            glyph: "#1a0808",
            left: "#3a2020",
            pedLeft: "#2a1818",
            pedRight: "#3a2020",
            pedTop: "#482828",
            right: "#4a3030",
            top: "#583838",
          },
          {
            cap: "#a83820",
            capDk: "#882818",
            capFr: "#98301c",
            capHi: "#d05028",
            front: "#302020",
            glyph: "#180808",
            left: "#2a1818",
            pedLeft: "#201010",
            pedRight: "#2a1818",
            pedTop: "#382020",
            right: "#3a2828",
            top: "#483030",
          },
        ],
        winter: [
          {
            cap: "#b0d0e8",
            capDk: "#90b0c8",
            capFr: "#a0c0d8",
            capHi: "#d0e8f8",
            front: "#7a8898",
            glyph: "#4a5868",
            left: "#6a7888",
            pedLeft: "#5a6878",
            pedRight: "#6a7888",
            pedTop: "#8898a8",
            right: "#8090a0",
            top: "#98a8b8",
          },
          {
            cap: "#a0c4d8",
            capDk: "#80a4b8",
            capFr: "#90b4c8",
            capHi: "#c0dce8",
            front: "#647888",
            glyph: "#3a4858",
            left: "#5a6878",
            pedLeft: "#4a5868",
            pedRight: "#5a6878",
            pedTop: "#788898",
            right: "#708090",
            top: "#8898a8",
          },
          {
            cap: "#c0dae8",
            capDk: "#a0bac8",
            capFr: "#b0cad8",
            capHi: "#e0f0f8",
            front: "#8898a8",
            glyph: "#5a6878",
            left: "#788898",
            pedLeft: "#687888",
            pedRight: "#788898",
            pedTop: "#98a8b8",
            right: "#90a0b0",
            top: "#a8b8c8",
          },
        ],
      };
      const obelPals = obelRegionPals[mapTheme] ?? obelRegionPals.grassland;
      const op = obelPals[variant % obelPals.length];

      const oH = 55 * s;
      const oBaseW = 10 * s;
      const oTopW = 6 * s;

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        ox,
        oy + 3 * s,
        s,
        12 * s,
        6 * s,
        55 * s,
        0.25
      );

      // Single merged pedestal
      const pedW = 13 * s;
      const pedH = 8 * s;
      const pedIso = pedW * ISO_COS;
      const pedD = pedW * ISO_SIN;
      ctx.fillStyle = op.pedTop;
      ctx.beginPath();
      ctx.moveTo(ox, oy - pedH);
      ctx.lineTo(ox + pedIso, oy - pedH + pedD);
      ctx.lineTo(ox, oy - pedH + pedD * 2);
      ctx.lineTo(ox - pedIso, oy - pedH + pedD);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = op.pedLeft;
      ctx.beginPath();
      ctx.moveTo(ox - pedIso, oy - pedH + pedD);
      ctx.lineTo(ox, oy - pedH + pedD * 2);
      ctx.lineTo(ox, oy + pedD * 2);
      ctx.lineTo(ox - pedIso, oy + pedD);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = op.pedRight;
      ctx.beginPath();
      ctx.moveTo(ox + pedIso, oy - pedH + pedD);
      ctx.lineTo(ox, oy - pedH + pedD * 2);
      ctx.lineTo(ox, oy + pedD * 2);
      ctx.lineTo(ox + pedIso, oy + pedD);
      ctx.closePath();
      ctx.fill();

      // Shaft
      const sBase = oy - pedH;
      const sTop = sBase - oH;
      const bw86 = oBaseW * ISO_COS;
      const tw86 = oTopW * ISO_COS;
      const bwIso = oBaseW * ISO_SIN;
      const twIso = oTopW * ISO_SIN;

      ctx.fillStyle = op.left;
      ctx.beginPath();
      ctx.moveTo(ox - bw86, sBase + bwIso);
      ctx.lineTo(ox, sBase + bwIso * 2);
      ctx.lineTo(ox, sTop + twIso * 2);
      ctx.lineTo(ox - tw86, sTop + twIso);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = op.right;
      ctx.beginPath();
      ctx.moveTo(ox + bw86, sBase + bwIso);
      ctx.lineTo(ox, sBase + bwIso * 2);
      ctx.lineTo(ox, sTop + twIso * 2);
      ctx.lineTo(ox + tw86, sTop + twIso);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = op.top;
      ctx.beginPath();
      ctx.moveTo(ox, sTop);
      ctx.lineTo(ox + tw86, sTop + twIso);
      ctx.lineTo(ox, sTop + twIso * 2);
      ctx.lineTo(ox - tw86, sTop + twIso);
      ctx.closePath();
      ctx.fill();

      // Pyramidion cap
      const cTip = sTop - 10 * s;
      ctx.fillStyle = op.capDk;
      ctx.beginPath();
      ctx.moveTo(ox, cTip);
      ctx.lineTo(ox - tw86, sTop + twIso);
      ctx.lineTo(ox, sTop + twIso * 2);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = op.cap;
      ctx.beginPath();
      ctx.moveTo(ox, cTip);
      ctx.lineTo(ox + tw86, sTop + twIso);
      ctx.lineTo(ox, sTop + twIso * 2);
      ctx.closePath();
      ctx.fill();
      // Cap highlight
      ctx.fillStyle = op.capHi;
      ctx.globalAlpha = 0.4;
      ctx.beginPath();
      ctx.moveTo(ox, cTip);
      ctx.lineTo(ox + oTopW * 0.5, sTop + twIso * 0.6);
      ctx.lineTo(ox, sTop + twIso);
      ctx.closePath();
      ctx.fill();
      ctx.globalAlpha = 1;

      // Hieroglyphs (reduced: 3 left, 2 right, simple rects)
      ctx.fillStyle = op.glyph;
      for (let g = 0; g < 3; g++) {
        const gt = 0.2 + g * 0.3;
        const gy = sBase + (sTop - sBase) * gt;
        const gx = ox - (oBaseW + (oTopW - oBaseW) * gt) * 0.43;
        ctx.fillRect(gx - 2 * s, gy - 0.8 * s, 4 * s, 1.6 * s);
      }
      for (let g = 0; g < 2; g++) {
        const gt = 0.3 + g * 0.4;
        const gy = sBase + (sTop - sBase) * gt;
        const gx = ox + (oBaseW + (oTopW - oBaseW) * gt) * 0.43;
        ctx.fillRect(gx - 1.5 * s, gy - 0.6 * s, 3 * s, 1.2 * s);
      }

      // Edge highlight
      ctx.strokeStyle = "rgba(255,255,255,0.12)";
      ctx.lineWidth = s;
      ctx.beginPath();
      ctx.moveTo(ox, cTip);
      ctx.lineTo(ox + tw86, sTop + twIso);
      ctx.lineTo(ox + bw86, sBase + bwIso);
      ctx.stroke();

      // === REGIONAL TOPPER & OVERLAYS ===
      if (mapTheme === "grassland" || !mapTheme) {
        // Golden sun medallion floating above cap
        const medY = cTip - 5 * s;
        const medR = 4 * s;
        const sunGlow = ctx.createRadialGradient(
          ox,
          medY,
          0,
          ox,
          medY,
          medR * 1.5
        );
        sunGlow.addColorStop(0, "rgba(240,210,80,0.5)");
        sunGlow.addColorStop(0.6, "rgba(220,180,40,0.2)");
        sunGlow.addColorStop(1, "rgba(200,160,30,0)");
        ctx.fillStyle = sunGlow;
        ctx.beginPath();
        ctx.arc(ox, medY, medR * 1.5, 0, Math.PI * 2);
        ctx.fill();
        // Solid medallion disc
        ctx.fillStyle = op.cap;
        ctx.beginPath();
        ctx.arc(ox, medY, medR, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = op.capHi;
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.arc(ox, medY, medR, 0, Math.PI * 2);
        ctx.stroke();
        // Sun rays
        ctx.strokeStyle = op.capHi;
        ctx.lineWidth = 0.6 * s;
        ctx.globalAlpha = 0.5;
        for (let r = 0; r < 8; r++) {
          const ang = (r / 8) * Math.PI * 2;
          ctx.beginPath();
          ctx.moveTo(
            ox + Math.cos(ang) * medR * 1.1,
            medY + Math.sin(ang) * medR * 1.1
          );
          ctx.lineTo(
            ox + Math.cos(ang) * medR * 1.7,
            medY + Math.sin(ang) * medR * 1.7
          );
          ctx.stroke();
        }
        ctx.globalAlpha = 1;
        // Ivy tendrils on shaft
        ctx.globalAlpha = 0.4;
        ctx.strokeStyle = "#4a6a3a";
        ctx.lineWidth = 1 * s;
        const ivyBase1 = sBase - oH * 0.2;
        ctx.beginPath();
        ctx.moveTo(ox - bw86 + 1 * s, ivyBase1);
        ctx.bezierCurveTo(
          ox - bw86 - 1 * s,
          ivyBase1 - 10 * s,
          ox - tw86 + 2 * s,
          ivyBase1 - 20 * s,
          ox - tw86,
          ivyBase1 - 28 * s
        );
        ctx.stroke();
        ctx.globalAlpha = 1;
      } else if (mapTheme === "swamp") {
        // Serpent eye ring on top — glowing eye motif
        const eyeY = cTip - 4 * s;
        const eyeR = 4.5 * s;
        // Outer ring
        ctx.strokeStyle = op.cap;
        ctx.lineWidth = 1.8 * s;
        ctx.beginPath();
        ctx.arc(ox, eyeY, eyeR, 0, Math.PI * 2);
        ctx.stroke();
        // Inner glow
        const eyeGlow = ctx.createRadialGradient(
          ox,
          eyeY,
          0,
          ox,
          eyeY,
          eyeR * 0.8
        );
        eyeGlow.addColorStop(0, "rgba(80,220,80,0.6)");
        eyeGlow.addColorStop(0.5, "rgba(40,160,40,0.3)");
        eyeGlow.addColorStop(1, "rgba(20,100,20,0)");
        ctx.fillStyle = eyeGlow;
        ctx.beginPath();
        ctx.arc(ox, eyeY, eyeR * 0.8, 0, Math.PI * 2);
        ctx.fill();
        // Slit pupil
        ctx.fillStyle = "#0a2a08";
        ctx.beginPath();
        ctx.ellipse(ox, eyeY, 1 * s, 3 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        // Vine wrap spiraling up shaft
        ctx.globalAlpha = 0.5;
        ctx.strokeStyle = "#1a3a12";
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        for (let vt = 0; vt <= 1; vt += 0.02) {
          const vy = sBase + (sTop - sBase) * vt;
          const vw = oBaseW + (oTopW - oBaseW) * vt;
          const vx = ox + Math.sin(vt * 12) * vw * ISO_COS * 0.7;
          if (vt === 0) {
            ctx.moveTo(vx, vy);
          } else {
            ctx.lineTo(vx, vy);
          }
        }
        ctx.stroke();
        ctx.globalAlpha = 1;
        // Moss splotches on pedestal
        ctx.globalAlpha = 0.4;
        ctx.fillStyle = "#2a4a20";
        ctx.beginPath();
        ctx.ellipse(ox - 6 * s, oy - 2 * s, 4 * s, 2 * s, 0.2, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          ox + 5 * s,
          oy - 1 * s,
          3 * s,
          1.5 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;
      } else if (mapTheme === "desert") {
        // Large golden sun disc ring on top
        const discY = cTip - 6 * s;
        const discR = 6 * s;
        // Outer glow
        const discGlow = ctx.createRadialGradient(
          ox,
          discY,
          discR * 0.5,
          ox,
          discY,
          discR * 2
        );
        discGlow.addColorStop(0, "rgba(240,200,60,0.3)");
        discGlow.addColorStop(0.7, "rgba(220,180,40,0.1)");
        discGlow.addColorStop(1, "rgba(200,160,20,0)");
        ctx.fillStyle = discGlow;
        ctx.beginPath();
        ctx.arc(ox, discY, discR * 2, 0, Math.PI * 2);
        ctx.fill();
        // Solid disc
        const discFill = ctx.createRadialGradient(
          ox - 1 * s,
          discY - 1 * s,
          0,
          ox,
          discY,
          discR
        );
        discFill.addColorStop(0, "#f0d860");
        discFill.addColorStop(0.7, "#d4a840");
        discFill.addColorStop(1, "#be9428");
        ctx.fillStyle = discFill;
        ctx.beginPath();
        ctx.arc(ox, discY, discR, 0, Math.PI * 2);
        ctx.fill();
        // Inner ring cutout
        ctx.fillStyle = op.capFr;
        ctx.beginPath();
        ctx.arc(ox, discY, discR * 0.55, 0, Math.PI * 2);
        ctx.fill();
        // Ring highlight
        ctx.strokeStyle = "#f8e880";
        ctx.lineWidth = 0.8 * s;
        ctx.globalAlpha = 0.6;
        ctx.beginPath();
        ctx.arc(ox, discY, discR * 0.78, -0.8, 0.8);
        ctx.stroke();
        ctx.globalAlpha = 1;
        // Hieroglyphic band wrapping shaft midpoint
        const bandY = sBase + (sTop - sBase) * 0.5;
        const bandW = oBaseW + (oTopW - oBaseW) * 0.5;
        ctx.fillStyle = op.cap;
        ctx.globalAlpha = 0.3;
        ctx.fillRect(
          ox - bandW * ISO_COS,
          bandY - 2 * s,
          bandW * ISO_COS * 2,
          4 * s
        );
        ctx.globalAlpha = 1;
        // Sand accumulation at base
        ctx.globalAlpha = 0.35;
        ctx.fillStyle = "rgba(190,170,130,0.5)";
        ctx.beginPath();
        ctx.ellipse(ox + 3 * s, oy + 4 * s, 16 * s, 5 * s, 0.1, 0, Math.PI * 2);
        ctx.fill();
        ctx.globalAlpha = 1;
      } else if (mapTheme === "winter") {
        // Ice crown with crystalline horns
        const crownY = cTip;
        // Left horn
        const lhG = ctx.createLinearGradient(
          ox - 6 * s,
          crownY - 12 * s,
          ox - 2 * s,
          crownY
        );
        lhG.addColorStop(0, "rgba(200,230,255,0.8)");
        lhG.addColorStop(0.5, "rgba(160,200,240,0.6)");
        lhG.addColorStop(1, "rgba(120,170,220,0.3)");
        ctx.fillStyle = lhG;
        ctx.beginPath();
        ctx.moveTo(ox - 2 * s, crownY);
        ctx.lineTo(ox - 7 * s, crownY - 14 * s);
        ctx.lineTo(ox - 5 * s, crownY - 12 * s);
        ctx.lineTo(ox - 1 * s, crownY - 2 * s);
        ctx.closePath();
        ctx.fill();
        // Right horn
        const rhG = ctx.createLinearGradient(
          ox + 6 * s,
          crownY - 12 * s,
          ox + 2 * s,
          crownY
        );
        rhG.addColorStop(0, "rgba(180,215,245,0.8)");
        rhG.addColorStop(0.5, "rgba(140,185,230,0.6)");
        rhG.addColorStop(1, "rgba(110,160,210,0.3)");
        ctx.fillStyle = rhG;
        ctx.beginPath();
        ctx.moveTo(ox + 2 * s, crownY);
        ctx.lineTo(ox + 7 * s, crownY - 14 * s);
        ctx.lineTo(ox + 5 * s, crownY - 12 * s);
        ctx.lineTo(ox + 1 * s, crownY - 2 * s);
        ctx.closePath();
        ctx.fill();
        // Center spike
        ctx.fillStyle = "rgba(210,235,255,0.7)";
        ctx.beginPath();
        ctx.moveTo(ox, crownY - 18 * s);
        ctx.lineTo(ox - 1.5 * s, crownY - 2 * s);
        ctx.lineTo(ox + 1.5 * s, crownY - 2 * s);
        ctx.closePath();
        ctx.fill();
        // Frost ring connecting horns
        ctx.strokeStyle = "rgba(180,220,250,0.5)";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.ellipse(ox, crownY - 4 * s, 5 * s, 2 * s, 0, 0, Math.PI * 2);
        ctx.stroke();
        // Snow cap on pedestal
        ctx.fillStyle = "#e4ecf6";
        ctx.globalAlpha = 0.7;
        const obelSnowSeed = Math.abs(decorX * 41 + decorY * 67);
        drawOrganicBlobAt(
          ctx,
          ox,
          oy - pedH + pedD - 1 * s,
          pedIso,
          pedD,
          obelSnowSeed,
          0.2,
          14
        );
        ctx.fill();
        ctx.globalAlpha = 1;
        // Icicles from shaft bands
        ctx.globalAlpha = 0.5;
        for (let ii = 0; ii < 3; ii++) {
          const iit = 0.25 + ii * 0.25;
          const iiy = sBase + (sTop - sBase) * iit;
          const iiw = oBaseW + (oTopW - oBaseW) * iit;
          const iix = ox - iiw * ISO_COS;
          const iiLen = (3 + ii) * s;
          const icicleG = ctx.createLinearGradient(iix, iiy, iix, iiy + iiLen);
          icicleG.addColorStop(0, "rgba(180,210,240,0.5)");
          icicleG.addColorStop(1, "rgba(160,200,240,0)");
          ctx.fillStyle = icicleG;
          ctx.beginPath();
          ctx.moveTo(iix - 0.8 * s, iiy);
          ctx.lineTo(iix, iiy + iiLen);
          ctx.lineTo(iix + 0.8 * s, iiy);
          ctx.closePath();
          ctx.fill();
        }
        ctx.globalAlpha = 1;
        // Frost sparkle specks
        ctx.globalAlpha = 0.5;
        ctx.fillStyle = "#d0e8f8";
        const frostSeed = Math.abs(decorX * 41 + decorY * 67);
        for (let fi = 0; fi < 4; fi++) {
          const fx = ox + (((frostSeed + fi * 29) % 16) - 8) * s;
          const fy = sTop + ((frostSeed + fi * 17) % 40) * s * 0.8;
          ctx.beginPath();
          ctx.arc(fx, fy, 0.7 * s, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.globalAlpha = 1;
      } else if (mapTheme === "volcanic") {
        // Demon horns — thick curved horns rising from top
        // Left horn
        ctx.fillStyle = "#2a1010";
        ctx.beginPath();
        ctx.moveTo(ox - 2 * s, cTip);
        ctx.bezierCurveTo(
          ox - 5 * s,
          cTip - 6 * s,
          ox - 10 * s,
          cTip - 10 * s,
          ox - 8 * s,
          cTip - 18 * s
        );
        ctx.lineTo(ox - 6 * s, cTip - 16 * s);
        ctx.bezierCurveTo(
          ox - 8 * s,
          cTip - 10 * s,
          ox - 3 * s,
          cTip - 5 * s,
          ox,
          cTip
        );
        ctx.closePath();
        ctx.fill();
        // Right horn
        ctx.beginPath();
        ctx.moveTo(ox + 2 * s, cTip);
        ctx.bezierCurveTo(
          ox + 5 * s,
          cTip - 6 * s,
          ox + 10 * s,
          cTip - 10 * s,
          ox + 8 * s,
          cTip - 18 * s
        );
        ctx.lineTo(ox + 6 * s, cTip - 16 * s);
        ctx.bezierCurveTo(
          ox + 8 * s,
          cTip - 10 * s,
          ox + 3 * s,
          cTip - 5 * s,
          ox,
          cTip
        );
        ctx.closePath();
        ctx.fill();
        // Horn tips glow
        ctx.globalAlpha = 0.7;
        const ltGlow = ctx.createRadialGradient(
          ox - 8 * s,
          cTip - 18 * s,
          0,
          ox - 8 * s,
          cTip - 18 * s,
          3 * s
        );
        ltGlow.addColorStop(0, "rgba(255,100,20,0.6)");
        ltGlow.addColorStop(1, "rgba(200,50,10,0)");
        ctx.fillStyle = ltGlow;
        ctx.beginPath();
        ctx.arc(ox - 8 * s, cTip - 18 * s, 3 * s, 0, Math.PI * 2);
        ctx.fill();
        const rtGlow = ctx.createRadialGradient(
          ox + 8 * s,
          cTip - 18 * s,
          0,
          ox + 8 * s,
          cTip - 18 * s,
          3 * s
        );
        rtGlow.addColorStop(0, "rgba(255,100,20,0.6)");
        rtGlow.addColorStop(1, "rgba(200,50,10,0)");
        ctx.fillStyle = rtGlow;
        ctx.beginPath();
        ctx.arc(ox + 8 * s, cTip - 18 * s, 3 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.globalAlpha = 1;
        // Pulsing ember core in cap
        const embPulse = 0.4 + Math.sin(decorTime * 2.5) * 0.2;
        const embG = ctx.createRadialGradient(
          ox,
          cTip - 2 * s,
          0,
          ox,
          cTip - 2 * s,
          5 * s
        );
        embG.addColorStop(0, `rgba(255,120,30,${embPulse})`);
        embG.addColorStop(0.5, `rgba(200,60,10,${embPulse * 0.5})`);
        embG.addColorStop(1, "rgba(100,30,5,0)");
        ctx.fillStyle = embG;
        ctx.beginPath();
        ctx.arc(ox, cTip - 2 * s, 5 * s, 0, Math.PI * 2);
        ctx.fill();
        // Lava crack glow lines on shaft
        ctx.globalAlpha = 0.45;
        ctx.strokeStyle = "#d86a20";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo(ox - 3 * s, sBase - 10 * s);
        ctx.lineTo(ox - 1 * s, sBase - 25 * s);
        ctx.lineTo(ox - 4 * s, sBase - 38 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(ox + 2 * s, sBase - 15 * s);
        ctx.lineTo(ox + 4 * s, sBase - 30 * s);
        ctx.stroke();
        // Glow halos
        ctx.globalAlpha = 0.12;
        ctx.strokeStyle = "#ff8040";
        ctx.lineWidth = 3 * s;
        ctx.beginPath();
        ctx.moveTo(ox - 3 * s, sBase - 10 * s);
        ctx.lineTo(ox - 1 * s, sBase - 25 * s);
        ctx.lineTo(ox - 4 * s, sBase - 38 * s);
        ctx.stroke();
        ctx.globalAlpha = 1;
        // Ash particles around base
        ctx.globalAlpha = 0.3;
        ctx.fillStyle = "rgba(60,40,30,0.5)";
        ctx.beginPath();
        ctx.ellipse(ox + 2 * s, oy + 3 * s, 14 * s, 5 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.globalAlpha = 1;
      }

      break;
    }
    case "giant_sphinx": {
      const time = Date.now() / 1000;

      // Color palette - aged sandstone
      const sandLight = "#d4c490";
      const sandBase = "#c2b280";
      const sandMid = "#b0a070";
      const sandDark = "#8a7a55";
      const sandShadow = "#6a5a40";
      const goldAccent = "#d4a850";
      const goldDark = "#a08030";
      const eyeGlow = "#40d0ff";

      // Subtle mystical pulse
      const mysticPulse = 0.6 + Math.sin(time * 1.5) * 0.2;

      // ========== GROUND SHADOW ==========
      if (!landmarkSkipShadow) {
        drawDirectionalShadow(
          ctx,
          screenPos.x,
          screenPos.y + 12 * s,
          s,
          42 * s,
          17 * s,
          60 * s,
          0.35
        );
      }

      // ========== SAND AROUND BASE ==========
      const sandGrad = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y + 10 * s,
        20 * s,
        screenPos.x,
        screenPos.y + 10 * s,
        60 * s
      );
      sandGrad.addColorStop(0, "rgba(194, 178, 128, 0.4)");
      sandGrad.addColorStop(0.5, "rgba(194, 178, 128, 0.2)");
      sandGrad.addColorStop(1, "rgba(194, 178, 128, 0)");
      ctx.fillStyle = sandGrad;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 10 * s,
        60 * s,
        25 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // ========== GRAND PEDESTAL BASE ==========
      // 2:1 isometric multi-tiered base (hd = hw * 0.5)
      const cx = screenPos.x;

      // Bottom tier (largest)
      const b1HW = 45 * s;
      const b1HD = b1HW * 0.5;
      const b1CY = screenPos.y + 12 * s;
      const b1WH = 6 * s;
      ctx.fillStyle = sandDark;
      ctx.beginPath();
      ctx.moveTo(cx, b1CY - b1HD);
      ctx.lineTo(cx + b1HW, b1CY);
      ctx.lineTo(cx, b1CY + b1HD);
      ctx.lineTo(cx - b1HW, b1CY);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = sandShadow;
      ctx.beginPath();
      ctx.moveTo(cx - b1HW, b1CY);
      ctx.lineTo(cx, b1CY + b1HD);
      ctx.lineTo(cx, b1CY + b1HD + b1WH);
      ctx.lineTo(cx - b1HW, b1CY + b1WH);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = sandDark;
      ctx.beginPath();
      ctx.moveTo(cx, b1CY + b1HD);
      ctx.lineTo(cx + b1HW, b1CY);
      ctx.lineTo(cx + b1HW, b1CY + b1WH);
      ctx.lineTo(cx, b1CY + b1HD + b1WH);
      ctx.closePath();
      ctx.fill();

      // Middle tier
      const b2HW = 38 * s;
      const b2HD = b2HW * 0.5;
      const b2CY = screenPos.y + 5 * s;
      const b2WH = 7 * s;
      ctx.fillStyle = sandMid;
      ctx.beginPath();
      ctx.moveTo(cx, b2CY - b2HD);
      ctx.lineTo(cx + b2HW, b2CY);
      ctx.lineTo(cx, b2CY + b2HD);
      ctx.lineTo(cx - b2HW, b2CY);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = sandDark;
      ctx.beginPath();
      ctx.moveTo(cx - b2HW, b2CY);
      ctx.lineTo(cx, b2CY + b2HD);
      ctx.lineTo(cx, b2CY + b2HD + b2WH);
      ctx.lineTo(cx - b2HW, b2CY + b2WH);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = sandMid;
      ctx.beginPath();
      ctx.moveTo(cx, b2CY + b2HD);
      ctx.lineTo(cx + b2HW, b2CY);
      ctx.lineTo(cx + b2HW, b2CY + b2WH);
      ctx.lineTo(cx, b2CY + b2HD + b2WH);
      ctx.closePath();
      ctx.fill();

      // Top tier (main platform)
      const b3HW = 32 * s;
      const b3HD = b3HW * 0.5;
      const b3CY = screenPos.y - 2 * s;
      const b3WH = 7 * s;
      ctx.fillStyle = sandBase;
      ctx.beginPath();
      ctx.moveTo(cx, b3CY - b3HD);
      ctx.lineTo(cx + b3HW, b3CY);
      ctx.lineTo(cx, b3CY + b3HD);
      ctx.lineTo(cx - b3HW, b3CY);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = sandMid;
      ctx.beginPath();
      ctx.moveTo(cx - b3HW, b3CY);
      ctx.lineTo(cx, b3CY + b3HD);
      ctx.lineTo(cx, b3CY + b3HD + b3WH);
      ctx.lineTo(cx - b3HW, b3CY + b3WH);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = sandBase;
      ctx.beginPath();
      ctx.moveTo(cx, b3CY + b3HD);
      ctx.lineTo(cx + b3HW, b3CY);
      ctx.lineTo(cx + b3HW, b3CY + b3WH);
      ctx.lineTo(cx, b3CY + b3HD + b3WH);
      ctx.closePath();
      ctx.fill();

      // ========== HIEROGLYPHIC CARVINGS ON BASE ==========
      ctx.strokeStyle = sandShadow;
      ctx.lineWidth = 1 * s;

      // Front face hieroglyphics
      const glyphY = screenPos.y + 8 * s;
      // Ankh symbol
      ctx.beginPath();
      ctx.arc(screenPos.x - 20 * s, glyphY - 3 * s, 2.5 * s, 0, Math.PI * 2);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 20 * s, glyphY - 0.5 * s);
      ctx.lineTo(screenPos.x - 20 * s, glyphY + 6 * s);
      ctx.moveTo(screenPos.x - 23 * s, glyphY + 2 * s);
      ctx.lineTo(screenPos.x - 17 * s, glyphY + 2 * s);
      ctx.stroke();

      // Eye of Horus
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - 8 * s,
        glyphY + 1 * s,
        4 * s,
        2.5 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.stroke();
      ctx.beginPath();
      ctx.arc(screenPos.x - 8 * s, glyphY + 1 * s, 1.5 * s, 0, Math.PI * 2);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 8 * s, glyphY + 3.5 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 10 * s,
        glyphY + 6 * s,
        screenPos.x - 12 * s,
        glyphY + 5 * s
      );
      ctx.stroke();

      // Cartouche border on side
      ctx.strokeStyle = sandDark;
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.roundRect(
        screenPos.x + 8 * s,
        screenPos.y + 3 * s,
        18 * s,
        8 * s,
        3 * s
      );
      ctx.stroke();

      // ========== BACK LEGS (haunches tucked under body) ==========
      // Left back haunch (shadow side)
      ctx.fillStyle = sandShadow;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 22 * s, screenPos.y - 5 * s);
      ctx.bezierCurveTo(
        screenPos.x - 28 * s,
        screenPos.y - 2 * s,
        screenPos.x - 28 * s,
        screenPos.y + 8 * s,
        screenPos.x - 20 * s,
        screenPos.y + 6 * s
      );
      ctx.bezierCurveTo(
        screenPos.x - 14 * s,
        screenPos.y + 8 * s,
        screenPos.x - 12 * s,
        screenPos.y + 2 * s,
        screenPos.x - 16 * s,
        screenPos.y - 3 * s
      );
      ctx.closePath();
      ctx.fill();

      // Back paw (left, tucked)
      ctx.fillStyle = sandDark;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - 17 * s,
        screenPos.y + 6 * s,
        5 * s,
        3 * s,
        0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Right back haunch (lit side)
      ctx.fillStyle = sandDark;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 15 * s, screenPos.y - 12 * s);
      ctx.bezierCurveTo(
        screenPos.x - 21 * s,
        screenPos.y - 9 * s,
        screenPos.x - 21 * s,
        screenPos.y + 1 * s,
        screenPos.x - 13 * s,
        screenPos.y - 1 * s
      );
      ctx.bezierCurveTo(
        screenPos.x - 7 * s,
        screenPos.y + 1 * s,
        screenPos.x - 5 * s,
        screenPos.y - 5 * s,
        screenPos.x - 9 * s,
        screenPos.y - 10 * s
      );
      ctx.closePath();
      ctx.fill();

      // Back paw (right, tucked)
      ctx.fillStyle = sandMid;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - 10 * s,
        screenPos.y - 1 * s,
        5 * s,
        3 * s,
        0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // ========== EXTENDED FRONT PAWS ==========
      // Right paw (front, lower)
      const pawGrad = ctx.createLinearGradient(
        screenPos.x + 15 * s,
        screenPos.y + 3 * s,
        screenPos.x + 35 * s,
        screenPos.y + 13 * s
      );
      pawGrad.addColorStop(0, sandBase);
      pawGrad.addColorStop(0.5, sandLight);
      pawGrad.addColorStop(1, sandMid);
      ctx.fillStyle = pawGrad;

      ctx.beginPath();
      ctx.moveTo(screenPos.x + 8 * s, screenPos.y);
      ctx.lineTo(screenPos.x + 35 * s, screenPos.y + 6 * s);
      ctx.lineTo(screenPos.x + 38 * s, screenPos.y + 10 * s);
      ctx.lineTo(screenPos.x + 35 * s, screenPos.y + 13 * s);
      ctx.lineTo(screenPos.x + 8 * s, screenPos.y + 8 * s);
      ctx.closePath();
      ctx.fill();

      // Paw details - toes
      ctx.strokeStyle = sandDark;
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 32 * s, screenPos.y + 7 * s);
      ctx.lineTo(screenPos.x + 35 * s, screenPos.y + 10 * s);
      ctx.moveTo(screenPos.x + 29 * s, screenPos.y + 8 * s);
      ctx.lineTo(screenPos.x + 32 * s, screenPos.y + 11 * s);
      ctx.stroke();

      // Left paw (behind, darker)
      ctx.fillStyle = sandMid;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 8 * s, screenPos.y + 2 * s);
      ctx.lineTo(screenPos.x + 28 * s, screenPos.y - 4 * s);
      ctx.lineTo(screenPos.x + 32 * s, screenPos.y - 2 * s);
      ctx.lineTo(screenPos.x + 28 * s, screenPos.y + 2 * s);
      ctx.lineTo(screenPos.x - 8 * s, screenPos.y + 6 * s);
      ctx.closePath();
      ctx.fill();

      // ========== LION BODY ==========
      // Back haunch
      ctx.fillStyle = sandDark;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - 18 * s,
        screenPos.y - 14 * s,
        14 * s,
        12 * s,
        -0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Main body gradient
      const bodyGrad = ctx.createLinearGradient(
        screenPos.x - 25 * s,
        screenPos.y - 30 * s,
        screenPos.x + 20 * s,
        screenPos.y
      );
      bodyGrad.addColorStop(0, sandLight);
      bodyGrad.addColorStop(0.3, sandBase);
      bodyGrad.addColorStop(0.6, sandMid);
      bodyGrad.addColorStop(1, sandDark);
      ctx.fillStyle = bodyGrad;

      ctx.beginPath();
      ctx.moveTo(screenPos.x - 25 * s, screenPos.y - 4 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 20 * s,
        screenPos.y - 35 * s,
        screenPos.x - 5 * s,
        screenPos.y - 32 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 15 * s,
        screenPos.y - 28 * s,
        screenPos.x + 22 * s,
        screenPos.y - 18 * s
      );
      ctx.lineTo(screenPos.x + 25 * s, screenPos.y - 2 * s);
      ctx.lineTo(screenPos.x + 8 * s, screenPos.y);
      ctx.lineTo(screenPos.x - 8 * s, screenPos.y + 2 * s);
      ctx.lineTo(screenPos.x - 25 * s, screenPos.y);
      ctx.closePath();
      ctx.fill();

      // Body contour lines (enhanced muscle definition)
      ctx.strokeStyle = sandDark;
      ctx.lineWidth = 1 * s;
      // Spine line
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 20 * s, screenPos.y - 28 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 10 * s,
        screenPos.y - 32 * s,
        screenPos.x + 5 * s,
        screenPos.y - 30 * s
      );
      ctx.stroke();
      // Shoulder blade
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 5 * s, screenPos.y - 28 * s);
      ctx.quadraticCurveTo(
        screenPos.x + 12 * s,
        screenPos.y - 25 * s,
        screenPos.x + 18 * s,
        screenPos.y - 20 * s
      );
      ctx.stroke();
      // Upper body contour
      ctx.strokeStyle = "rgba(100, 80, 50, 0.25)";
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 15 * s, screenPos.y - 25 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 5 * s,
        screenPos.y - 20 * s,
        screenPos.x + 5 * s,
        screenPos.y - 22 * s
      );
      ctx.stroke();
      // Ribcage suggestion (multiple ribs)
      ctx.strokeStyle = "rgba(100, 80, 50, 0.18)";
      ctx.lineWidth = 0.8 * s;
      for (let rib = 0; rib < 3; rib++) {
        const ribOff = rib * 3.5;
        ctx.beginPath();
        ctx.moveTo(
          screenPos.x - 12 * s + ribOff * s,
          screenPos.y - 18 * s + ribOff * s
        );
        ctx.quadraticCurveTo(
          screenPos.x - 8 * s + ribOff * s,
          screenPos.y - 14 * s + ribOff * s,
          screenPos.x - 5 * s + ribOff * s,
          screenPos.y - 12 * s + ribOff * s
        );
        ctx.stroke();
      }
      // Haunch muscle
      ctx.strokeStyle = sandDark;
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 22 * s, screenPos.y - 8 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 18 * s,
        screenPos.y - 18 * s,
        screenPos.x - 10 * s,
        screenPos.y - 22 * s
      );
      ctx.stroke();
      // Body highlight (top surface lit)
      ctx.fillStyle = "rgba(220, 200, 150, 0.12)";
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 18 * s, screenPos.y - 30 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 10 * s,
        screenPos.y - 34 * s,
        screenPos.x + 5 * s,
        screenPos.y - 32 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 12 * s,
        screenPos.y - 30 * s,
        screenPos.x + 18 * s,
        screenPos.y - 24 * s
      );
      ctx.lineTo(screenPos.x + 8 * s, screenPos.y - 26 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 5 * s,
        screenPos.y - 28 * s,
        screenPos.x - 18 * s,
        screenPos.y - 30 * s
      );
      ctx.fill();

      // ========== FRONT LEGS ==========
      // Right front leg (lit side, visible)
      const flegGrad = ctx.createLinearGradient(
        screenPos.x + 10 * s,
        screenPos.y - 18 * s,
        screenPos.x + 22 * s,
        screenPos.y
      );
      flegGrad.addColorStop(0, sandBase);
      flegGrad.addColorStop(1, sandMid);
      ctx.fillStyle = flegGrad;
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 12 * s, screenPos.y - 18 * s);
      ctx.lineTo(screenPos.x + 22 * s, screenPos.y - 16 * s);
      ctx.lineTo(screenPos.x + 22 * s, screenPos.y - 2 * s);
      ctx.lineTo(screenPos.x + 12 * s, screenPos.y);
      ctx.closePath();
      ctx.fill();

      // Right front shoulder bulge
      ctx.fillStyle = sandBase;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 17 * s,
        screenPos.y - 17 * s,
        6 * s,
        4 * s,
        0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Left front leg (shadow side)
      ctx.fillStyle = sandShadow;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 4 * s, screenPos.y - 16 * s);
      ctx.lineTo(screenPos.x + 6 * s, screenPos.y - 14 * s);
      ctx.lineTo(screenPos.x + 6 * s, screenPos.y + 2 * s);
      ctx.lineTo(screenPos.x - 4 * s, screenPos.y + 4 * s);
      ctx.closePath();
      ctx.fill();

      // Left front shoulder bulge
      ctx.fillStyle = sandDark;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 1 * s,
        screenPos.y - 15 * s,
        6 * s,
        4 * s,
        0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Leg separation line
      ctx.strokeStyle = sandShadow;
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 9 * s, screenPos.y - 17 * s);
      ctx.lineTo(screenPos.x + 9 * s, screenPos.y - 1 * s);
      ctx.stroke();

      // ========== CHEST AND NECK ==========
      // Broad chest
      const chestGrad = ctx.createLinearGradient(
        screenPos.x + 5 * s,
        screenPos.y - 35 * s,
        screenPos.x + 25 * s,
        screenPos.y - 15 * s
      );
      chestGrad.addColorStop(0, sandLight);
      chestGrad.addColorStop(0.5, sandBase);
      chestGrad.addColorStop(1, sandMid);
      ctx.fillStyle = chestGrad;

      ctx.beginPath();
      ctx.moveTo(screenPos.x + 5 * s, screenPos.y - 32 * s);
      ctx.quadraticCurveTo(
        screenPos.x + 18 * s,
        screenPos.y - 35 * s,
        screenPos.x + 25 * s,
        screenPos.y - 25 * s
      );
      ctx.lineTo(screenPos.x + 22 * s, screenPos.y - 18 * s);
      ctx.quadraticCurveTo(
        screenPos.x + 15 * s,
        screenPos.y - 25 * s,
        screenPos.x + 5 * s,
        screenPos.y - 28 * s
      );
      ctx.closePath();
      ctx.fill();

      // ========== NECK (isometric prism) ==========
      const neckCx = screenPos.x + 4 * s;
      const neckTopY = screenPos.y - 41 * s;
      const neckH = 10 * s;
      const neckHW = 10 * s;
      const neckHD = neckHW * 0.5;

      // Top face (rhombus)
      ctx.fillStyle = sandBase;
      ctx.beginPath();
      ctx.moveTo(neckCx, neckTopY - neckHD);
      ctx.lineTo(neckCx + neckHW, neckTopY);
      ctx.lineTo(neckCx, neckTopY + neckHD);
      ctx.lineTo(neckCx - neckHW, neckTopY);
      ctx.closePath();
      ctx.fill();

      // Left face (shadow side)
      ctx.fillStyle = sandDark;
      ctx.beginPath();
      ctx.moveTo(neckCx - neckHW, neckTopY);
      ctx.lineTo(neckCx, neckTopY + neckHD);
      ctx.lineTo(neckCx, neckTopY + neckHD + neckH);
      ctx.lineTo(neckCx - neckHW, neckTopY + neckH);
      ctx.closePath();
      ctx.fill();

      // Right face (lit side)
      ctx.fillStyle = sandMid;
      ctx.beginPath();
      ctx.moveTo(neckCx + neckHW, neckTopY);
      ctx.lineTo(neckCx, neckTopY + neckHD);
      ctx.lineTo(neckCx, neckTopY + neckHD + neckH);
      ctx.lineTo(neckCx + neckHW, neckTopY + neckH);
      ctx.closePath();
      ctx.fill();

      // ========== NEMES HEADDREs (Back portion first) ==========
      // Back drape of headdres
      ctx.fillStyle = sandMid;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 2 * s, screenPos.y - 55 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 20 * s,
        screenPos.y - 50 * s,
        screenPos.x - 22 * s,
        screenPos.y - 25 * s
      );
      ctx.lineTo(screenPos.x - 18 * s, screenPos.y - 20 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 12 * s,
        screenPos.y - 35 * s,
        screenPos.x,
        screenPos.y - 45 * s
      );
      ctx.closePath();
      ctx.fill();

      // ========== HEAD AND FACE ==========
      const headCenterX = screenPos.x + 8 * s;
      const headCenterY = screenPos.y - 45 * s;

      // Face base shape
      const faceGrad = ctx.createLinearGradient(
        headCenterX - 10 * s,
        headCenterY - 10 * s,
        headCenterX + 10 * s,
        headCenterY + 15 * s
      );
      faceGrad.addColorStop(0, sandLight);
      faceGrad.addColorStop(0.4, sandBase);
      faceGrad.addColorStop(0.8, sandMid);
      faceGrad.addColorStop(1, sandDark);
      ctx.fillStyle = faceGrad;

      // Face shape - more angular/Egyptian
      ctx.beginPath();
      ctx.moveTo(headCenterX - 10 * s, headCenterY - 8 * s);
      ctx.lineTo(headCenterX - 12 * s, headCenterY + 5 * s);
      ctx.quadraticCurveTo(
        headCenterX - 10 * s,
        headCenterY + 12 * s,
        headCenterX,
        headCenterY + 15 * s
      );
      ctx.quadraticCurveTo(
        headCenterX + 10 * s,
        headCenterY + 12 * s,
        headCenterX + 12 * s,
        headCenterY + 5 * s
      );
      ctx.lineTo(headCenterX + 10 * s, headCenterY - 8 * s);
      ctx.closePath();
      ctx.fill();

      // ========== NEMES HEADDREs (Front) ==========
      // Main headdres
      const nemesGrad = ctx.createLinearGradient(
        headCenterX - 18 * s,
        headCenterY - 15 * s,
        headCenterX + 18 * s,
        headCenterY + 10 * s
      );
      nemesGrad.addColorStop(0, goldAccent);
      nemesGrad.addColorStop(0.3, sandLight);
      nemesGrad.addColorStop(0.5, goldAccent);
      nemesGrad.addColorStop(0.7, sandLight);
      nemesGrad.addColorStop(1, goldDark);
      ctx.fillStyle = nemesGrad;

      // Headdres shape
      ctx.beginPath();
      ctx.moveTo(headCenterX, headCenterY - 18 * s); // Top
      ctx.quadraticCurveTo(
        headCenterX - 18 * s,
        headCenterY - 15 * s,
        headCenterX - 20 * s,
        headCenterY
      );
      ctx.lineTo(headCenterX - 18 * s, headCenterY + 20 * s); // Left drape
      ctx.lineTo(headCenterX - 12 * s, headCenterY + 5 * s);
      ctx.lineTo(headCenterX - 10 * s, headCenterY - 8 * s);
      ctx.lineTo(headCenterX + 10 * s, headCenterY - 8 * s);
      ctx.lineTo(headCenterX + 12 * s, headCenterY + 5 * s);
      ctx.lineTo(headCenterX + 18 * s, headCenterY + 20 * s); // Right drape
      ctx.quadraticCurveTo(
        headCenterX + 18 * s,
        headCenterY - 15 * s,
        headCenterX,
        headCenterY - 18 * s
      );
      ctx.closePath();
      ctx.fill();

      // Headdres stripes
      ctx.strokeStyle = goldDark;
      ctx.lineWidth = 1.5 * s;

      // Left side stripes
      for (let i = 0; i < 5; i++) {
        const stripeT = i / 5;
        ctx.beginPath();
        ctx.moveTo(
          headCenterX - 10 * s - stripeT * 8 * s,
          headCenterY - 8 * s + stripeT * 5 * s
        );
        ctx.lineTo(
          headCenterX - 12 * s - stripeT * 6 * s,
          headCenterY + 5 * s + stripeT * 15 * s
        );
        ctx.stroke();
      }

      // Right side stripes
      for (let i = 0; i < 5; i++) {
        const stripeT = i / 5;
        ctx.beginPath();
        ctx.moveTo(
          headCenterX + 10 * s + stripeT * 8 * s,
          headCenterY - 8 * s + stripeT * 5 * s
        );
        ctx.lineTo(
          headCenterX + 12 * s + stripeT * 6 * s,
          headCenterY + 5 * s + stripeT * 15 * s
        );
        ctx.stroke();
      }

      // ========== URAEUS (Cobra on forehead) ==========
      ctx.fillStyle = goldAccent;
      // Cobra body
      ctx.beginPath();
      ctx.moveTo(headCenterX, headCenterY - 18 * s);
      ctx.quadraticCurveTo(
        headCenterX + 2 * s,
        headCenterY - 22 * s,
        headCenterX,
        headCenterY - 26 * s
      );
      ctx.quadraticCurveTo(
        headCenterX - 2 * s,
        headCenterY - 22 * s,
        headCenterX,
        headCenterY - 18 * s
      );
      ctx.fill();

      // Cobra hood
      ctx.beginPath();
      ctx.moveTo(headCenterX - 4 * s, headCenterY - 24 * s);
      ctx.quadraticCurveTo(
        headCenterX,
        headCenterY - 30 * s,
        headCenterX + 4 * s,
        headCenterY - 24 * s
      );
      ctx.quadraticCurveTo(
        headCenterX,
        headCenterY - 26 * s,
        headCenterX - 4 * s,
        headCenterY - 24 * s
      );
      ctx.fill();

      // Cobra eyes (glowing)
      ctx.fillStyle = `rgba(255, 50, 50, ${mysticPulse})`;
      ctx.beginPath();
      ctx.arc(
        headCenterX - 1.5 * s,
        headCenterY - 26 * s,
        0.8 * s,
        0,
        Math.PI * 2
      );
      ctx.arc(
        headCenterX + 1.5 * s,
        headCenterY - 26 * s,
        0.8 * s,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // ========== FACIAL FEATURES ==========

      // Face contouring — forehead shadow
      const foreheadGrad = ctx.createLinearGradient(
        headCenterX,
        headCenterY - 8 * s,
        headCenterX,
        headCenterY - 2 * s
      );
      foreheadGrad.addColorStop(0, "rgba(100, 80, 50, 0.15)");
      foreheadGrad.addColorStop(1, "rgba(100, 80, 50, 0)");
      ctx.fillStyle = foreheadGrad;
      ctx.fillRect(headCenterX - 9 * s, headCenterY - 8 * s, 18 * s, 6 * s);

      // Cheekbone highlights
      ctx.fillStyle = "rgba(220, 200, 160, 0.2)";
      ctx.beginPath();
      ctx.ellipse(
        headCenterX - 6 * s,
        headCenterY + 2 * s,
        3.5 * s,
        1.8 * s,
        -0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(
        headCenterX + 6 * s,
        headCenterY + 2 * s,
        3.5 * s,
        1.8 * s,
        0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Temple shadows
      ctx.fillStyle = "rgba(90, 70, 45, 0.18)";
      ctx.beginPath();
      ctx.ellipse(
        headCenterX - 9 * s,
        headCenterY - 1 * s,
        2 * s,
        3.5 * s,
        -0.1,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(
        headCenterX + 9 * s,
        headCenterY - 1 * s,
        2 * s,
        3.5 * s,
        0.1,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Sculpted brow ridge
      const browGrad = ctx.createLinearGradient(
        headCenterX,
        headCenterY - 4.5 * s,
        headCenterX,
        headCenterY - 1.5 * s
      );
      browGrad.addColorStop(0, sandMid);
      browGrad.addColorStop(1, sandDark);
      ctx.fillStyle = browGrad;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 8.5 * s, headCenterY - 2 * s);
      ctx.quadraticCurveTo(
        headCenterX - 5 * s,
        headCenterY - 4.5 * s,
        headCenterX - 1.5 * s,
        headCenterY - 2.5 * s
      );
      ctx.lineTo(headCenterX - 1.5 * s, headCenterY - 1 * s);
      ctx.quadraticCurveTo(
        headCenterX - 5 * s,
        headCenterY - 3 * s,
        headCenterX - 8.5 * s,
        headCenterY - 0.5 * s
      );
      ctx.closePath();
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(headCenterX + 1.5 * s, headCenterY - 2.5 * s);
      ctx.quadraticCurveTo(
        headCenterX + 5 * s,
        headCenterY - 4.5 * s,
        headCenterX + 8.5 * s,
        headCenterY - 2 * s
      );
      ctx.lineTo(headCenterX + 8.5 * s, headCenterY - 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX + 5 * s,
        headCenterY - 3 * s,
        headCenterX + 1.5 * s,
        headCenterY - 1 * s
      );
      ctx.closePath();
      ctx.fill();

      // Almond eye sockets
      ctx.fillStyle = sandShadow;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 8 * s, headCenterY + 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX - 5 * s,
        headCenterY - 1.5 * s,
        headCenterX - 1.5 * s,
        headCenterY + 0.5 * s
      );
      ctx.quadraticCurveTo(
        headCenterX - 5 * s,
        headCenterY + 2.2 * s,
        headCenterX - 8 * s,
        headCenterY + 0.5 * s
      );
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(headCenterX + 1.5 * s, headCenterY + 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX + 5 * s,
        headCenterY - 1.5 * s,
        headCenterX + 8 * s,
        headCenterY + 0.5 * s
      );
      ctx.quadraticCurveTo(
        headCenterX + 5 * s,
        headCenterY + 2.2 * s,
        headCenterX + 1.5 * s,
        headCenterY + 0.5 * s
      );
      ctx.fill();

      // Glowing almond eyes
      const eyeGlowIntensity = mysticPulse * 0.65;
      setShadowBlur(ctx, 4 * s, eyeGlow);
      ctx.fillStyle = `rgba(64, 208, 255, ${eyeGlowIntensity})`;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 7.5 * s, headCenterY + 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX - 5 * s,
        headCenterY - 0.8 * s,
        headCenterX - 2 * s,
        headCenterY + 0.5 * s
      );
      ctx.quadraticCurveTo(
        headCenterX - 5 * s,
        headCenterY + 1.6 * s,
        headCenterX - 7.5 * s,
        headCenterY + 0.5 * s
      );
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(headCenterX + 2 * s, headCenterY + 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX + 5 * s,
        headCenterY - 0.8 * s,
        headCenterX + 7.5 * s,
        headCenterY + 0.5 * s
      );
      ctx.quadraticCurveTo(
        headCenterX + 5 * s,
        headCenterY + 1.6 * s,
        headCenterX + 2 * s,
        headCenterY + 0.5 * s
      );
      ctx.fill();
      clearShadow(ctx);

      // Kohl liner (Egyptian eye-line)
      ctx.strokeStyle = "#1a1008";
      ctx.lineWidth = 1.2 * s;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(headCenterX - 8 * s, headCenterY + 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX - 5 * s,
        headCenterY - 1.5 * s,
        headCenterX - 1.5 * s,
        headCenterY + 0.5 * s
      );
      ctx.quadraticCurveTo(
        headCenterX - 5 * s,
        headCenterY + 2.2 * s,
        headCenterX - 8 * s,
        headCenterY + 0.5 * s
      );
      ctx.stroke();
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 8 * s, headCenterY + 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX - 9.5 * s,
        headCenterY + 1.5 * s,
        headCenterX - 10 * s,
        headCenterY + 3 * s
      );
      ctx.stroke();
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.moveTo(headCenterX + 1.5 * s, headCenterY + 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX + 5 * s,
        headCenterY - 1.5 * s,
        headCenterX + 8 * s,
        headCenterY + 0.5 * s
      );
      ctx.quadraticCurveTo(
        headCenterX + 5 * s,
        headCenterY + 2.2 * s,
        headCenterX + 1.5 * s,
        headCenterY + 0.5 * s
      );
      ctx.stroke();
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(headCenterX + 8 * s, headCenterY + 0.5 * s);
      ctx.quadraticCurveTo(
        headCenterX + 9.5 * s,
        headCenterY + 1.5 * s,
        headCenterX + 10 * s,
        headCenterY + 3 * s
      );
      ctx.stroke();
      ctx.lineCap = "butt";

      // Eye highlights
      ctx.fillStyle = "rgba(200, 240, 255, 0.45)";
      ctx.beginPath();
      ctx.arc(headCenterX - 4 * s, headCenterY, 0.7 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.arc(headCenterX + 6 * s, headCenterY, 0.7 * s, 0, Math.PI * 2);
      ctx.fill();

      // Nose bridge
      ctx.fillStyle = sandBase;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 1.2 * s, headCenterY + 2 * s);
      ctx.lineTo(headCenterX + 1.2 * s, headCenterY + 2 * s);
      ctx.lineTo(headCenterX + 2 * s, headCenterY + 6.5 * s);
      ctx.lineTo(headCenterX - 2 * s, headCenterY + 6.5 * s);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = "rgba(90, 70, 45, 0.2)";
      ctx.beginPath();
      ctx.moveTo(headCenterX - 1.2 * s, headCenterY + 2 * s);
      ctx.lineTo(headCenterX, headCenterY + 2.5 * s);
      ctx.lineTo(headCenterX, headCenterY + 6.5 * s);
      ctx.lineTo(headCenterX - 2 * s, headCenterY + 6.5 * s);
      ctx.closePath();
      ctx.fill();

      // Nose tip and nostrils
      ctx.fillStyle = sandMid;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 2.8 * s, headCenterY + 7 * s);
      ctx.quadraticCurveTo(
        headCenterX,
        headCenterY + 8 * s,
        headCenterX + 2.8 * s,
        headCenterY + 7 * s
      );
      ctx.lineTo(headCenterX + 2 * s, headCenterY + 6.5 * s);
      ctx.lineTo(headCenterX - 2 * s, headCenterY + 6.5 * s);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = sandShadow;
      ctx.beginPath();
      ctx.ellipse(
        headCenterX - 1.3 * s,
        headCenterY + 7.2 * s,
        0.5 * s,
        0.35 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(
        headCenterX + 1.3 * s,
        headCenterY + 7.2 * s,
        0.5 * s,
        0.35 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Nasolabial folds
      ctx.strokeStyle = "rgba(100, 80, 50, 0.18)";
      ctx.lineWidth = 0.6 * s;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 2.5 * s, headCenterY + 7.5 * s);
      ctx.quadraticCurveTo(
        headCenterX - 3.5 * s,
        headCenterY + 9 * s,
        headCenterX - 4 * s,
        headCenterY + 10 * s
      );
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(headCenterX + 2.5 * s, headCenterY + 7.5 * s);
      ctx.quadraticCurveTo(
        headCenterX + 3.5 * s,
        headCenterY + 9 * s,
        headCenterX + 4 * s,
        headCenterY + 10 * s
      );
      ctx.stroke();

      // Mouth (enigmatic, barely curved)
      ctx.strokeStyle = sandShadow;
      ctx.lineWidth = 1.3 * s;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 4 * s, headCenterY + 10.5 * s);
      ctx.quadraticCurveTo(
        headCenterX - 1.5 * s,
        headCenterY + 11.2 * s,
        headCenterX,
        headCenterY + 10.8 * s
      );
      ctx.quadraticCurveTo(
        headCenterX + 1.5 * s,
        headCenterY + 11.2 * s,
        headCenterX + 4 * s,
        headCenterY + 10.5 * s
      );
      ctx.stroke();
      ctx.strokeStyle = "rgba(190, 170, 130, 0.15)";
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 3 * s, headCenterY + 10 * s);
      ctx.quadraticCurveTo(
        headCenterX,
        headCenterY + 9.5 * s,
        headCenterX + 3 * s,
        headCenterY + 10 * s
      );
      ctx.stroke();

      // Chin and jaw definition
      ctx.fillStyle = "rgba(180, 160, 120, 0.12)";
      ctx.beginPath();
      ctx.moveTo(headCenterX - 7 * s, headCenterY + 12 * s);
      ctx.quadraticCurveTo(
        headCenterX,
        headCenterY + 16 * s,
        headCenterX + 7 * s,
        headCenterY + 12 * s
      );
      ctx.quadraticCurveTo(
        headCenterX,
        headCenterY + 14 * s,
        headCenterX - 7 * s,
        headCenterY + 12 * s
      );
      ctx.fill();
      ctx.strokeStyle = sandDark;
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 4 * s, headCenterY + 13 * s);
      ctx.quadraticCurveTo(
        headCenterX,
        headCenterY + 15 * s,
        headCenterX + 4 * s,
        headCenterY + 13 * s
      );
      ctx.stroke();

      // ========== CEREMONIAL BEARD ==========
      ctx.fillStyle = goldDark;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 2 * s, headCenterY + 15 * s);
      ctx.lineTo(headCenterX - 3 * s, headCenterY + 28 * s);
      ctx.quadraticCurveTo(
        headCenterX,
        headCenterY + 30 * s,
        headCenterX + 3 * s,
        headCenterY + 28 * s
      );
      ctx.lineTo(headCenterX + 2 * s, headCenterY + 15 * s);
      ctx.closePath();
      ctx.fill();

      // Beard stripes
      ctx.strokeStyle = sandDark;
      ctx.lineWidth = 1 * s;
      for (let i = 0; i < 4; i++) {
        const beardY = headCenterY + 18 * s + i * 3 * s;
        ctx.beginPath();
        ctx.moveTo(headCenterX - 2.5 * s, beardY);
        ctx.lineTo(headCenterX + 2.5 * s, beardY);
        ctx.stroke();
      }

      // ========== COLLAR/BROAD COLLAR ==========
      // Decorative collar at neck
      const collarY = headCenterY + 15 * s;
      ctx.fillStyle = goldAccent;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 15 * s, collarY + 5 * s);
      ctx.quadraticCurveTo(
        headCenterX,
        collarY + 12 * s,
        headCenterX + 15 * s,
        collarY + 5 * s
      );
      ctx.quadraticCurveTo(
        headCenterX,
        collarY + 8 * s,
        headCenterX - 15 * s,
        collarY + 5 * s
      );
      ctx.fill();

      // Collar details
      ctx.strokeStyle = goldDark;
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(headCenterX - 12 * s, collarY + 6 * s);
      ctx.quadraticCurveTo(
        headCenterX,
        collarY + 10 * s,
        headCenterX + 12 * s,
        collarY + 6 * s
      );
      ctx.stroke();

      // Gem on collar
      ctx.fillStyle = `rgba(64, 208, 255, ${mysticPulse})`;
      setShadowBlur(ctx, 6 * s, eyeGlow);
      ctx.beginPath();
      ctx.arc(headCenterX, collarY + 8 * s, 2.5 * s, 0, Math.PI * 2);
      ctx.fill();
      clearShadow(ctx);

      // ========== WEATHERING AND CRACKS ==========
      ctx.strokeStyle = "rgba(80, 60, 40, 0.25)";
      ctx.lineWidth = 0.5 * s;

      // Face crack with branch
      ctx.beginPath();
      ctx.moveTo(headCenterX + 8 * s, headCenterY + 3 * s);
      ctx.lineTo(headCenterX + 10 * s, headCenterY + 8 * s);
      ctx.lineTo(headCenterX + 9 * s, headCenterY + 12 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(headCenterX + 10 * s, headCenterY + 8 * s);
      ctx.lineTo(headCenterX + 12 * s, headCenterY + 9 * s);
      ctx.stroke();

      // Body cracks
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 5 * s, screenPos.y - 20 * s);
      ctx.lineTo(screenPos.x + 8 * s, screenPos.y - 15 * s);
      ctx.lineTo(screenPos.x + 6 * s, screenPos.y - 10 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 8 * s, screenPos.y - 15 * s);
      ctx.lineTo(screenPos.x - 5 * s, screenPos.y - 12 * s);
      ctx.lineTo(screenPos.x - 7 * s, screenPos.y - 8 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 15 * s, screenPos.y - 8 * s);
      ctx.lineTo(screenPos.x + 18 * s, screenPos.y - 5 * s);
      ctx.stroke();

      // Erosion pitting
      ctx.fillStyle = "rgba(80, 60, 40, 0.15)";
      for (let pit = 0; pit < 12; pit++) {
        const pitAngle = (pit / 12) * Math.PI * 2 + 0.3;
        const pitDist = (12 + ((pit * 73) % 15)) * s;
        const pitX = screenPos.x + Math.cos(pitAngle) * pitDist;
        const pitY = screenPos.y - 15 * s + Math.sin(pitAngle) * pitDist * 0.5;
        const pitR = (0.5 + ((pit * 37) % 5) / 5) * s;
        ctx.beginPath();
        ctx.arc(pitX, pitY, pitR, 0, Math.PI * 2);
        ctx.fill();
      }

      // Stone block joint lines
      ctx.strokeStyle = "rgba(80, 60, 40, 0.12)";
      ctx.lineWidth = 0.4 * s;
      for (let joint = 0; joint < 3; joint++) {
        const jY = screenPos.y - 5 * s - joint * 8 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 15 * s + joint * 3 * s, jY);
        ctx.lineTo(screenPos.x + 15 * s - joint * 5 * s, jY - 2 * s);
        ctx.stroke();
      }

      // ========== MYSTICAL AURA ==========
      // Subtle glow around the sphinx
      const auraGrad = ctx.createRadialGradient(
        screenPos.x + 5 * s,
        screenPos.y - 30 * s,
        10 * s,
        screenPos.x + 5 * s,
        screenPos.y - 30 * s,
        50 * s
      );
      auraGrad.addColorStop(0, `rgba(64, 208, 255, ${mysticPulse * 0.1})`);
      auraGrad.addColorStop(0.5, `rgba(64, 208, 255, ${mysticPulse * 0.05})`);
      auraGrad.addColorStop(1, "rgba(64, 208, 255, 0)");
      ctx.fillStyle = auraGrad;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 5 * s,
        screenPos.y - 25 * s,
        50 * s,
        35 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // ========== FLOATING DUST PARTICLES ==========
      ctx.fillStyle = `rgba(212, 196, 144, 0.4)`;
      for (let i = 0; i < 6; i++) {
        const dustTime = (time * 0.3 + i * 0.8) % 3;
        const dustAngle = (i / 6) * Math.PI * 2 + time * 0.2;
        const dustDist = 35 + Math.sin(time + i * 2) * 10;
        const dustX = screenPos.x + Math.cos(dustAngle) * dustDist * s;
        const dustY =
          screenPos.y -
          20 * s +
          Math.sin(dustAngle) * dustDist * 0.4 * s -
          dustTime * 8 * s;
        const dustAlpha = 0.3 + Math.sin(time * 2 + i) * 0.15;

        ctx.fillStyle = `rgba(212, 196, 144, ${dustAlpha})`;
        ctx.beginPath();
        ctx.arc(dustX, dustY, 1.5 * s, 0, Math.PI * 2);
        ctx.fill();
      }

      break;
    }
    case "sphinx": {
      const spxV = variant || 0;
      const sx = screenPos.x;
      const sy = screenPos.y;

      // Variant palettes (pre-baked, no runtime color ops)
      const spxPals = [
        {
          accent: "#d4a840",
          base: "#c4a574",
          dark: "#9a7a4a",
          lit: "#d4b584",
          mid: "#b49564",
          shadow: "#7a5a30",
          stripe: "#8a6a3a",
        },
        {
          accent: "#c0c8d0",
          base: "#b8a090",
          dark: "#8a7060",
          lit: "#d0c0a8",
          mid: "#a89080",
          shadow: "#6a5040",
          stripe: "#6a5a4a",
        },
        {
          accent: "#e8c040",
          base: "#c8a060",
          dark: "#9a7030",
          lit: "#e0c070",
          mid: "#b89050",
          shadow: "#7a5010",
          stripe: "#7a5a20",
        },
      ];
      const p = spxPals[spxV % spxPals.length];

      const bodyW = 44 * s;
      const bodyH = 18 * s;
      const hw = bodyW * 0.5 * ISO_COS;
      const hd = bodyW * 0.5 * ISO_SIN * 0.5;

      // === GROUND SHADOW ===
      if (!landmarkSkipShadow) {
        drawDirectionalShadow(
          ctx,
          sx,
          sy + 3 * s,
          s,
          28 * s,
          10 * s,
          35 * s,
          0.25
        );
      }

      // === PEDESTAL ===
      const pdW = 32 * s,
        pdHt = 8 * s;
      const pdIso = pdW * ISO_COS * 0.5;
      const pdD = pdW * ISO_SIN * 0.5;
      ctx.fillStyle = p.mid;
      ctx.beginPath();
      ctx.moveTo(sx, sy - pdHt - pdD);
      ctx.lineTo(sx + pdIso, sy - pdHt);
      ctx.lineTo(sx, sy - pdHt + pdD);
      ctx.lineTo(sx - pdIso, sy - pdHt);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = p.dark;
      ctx.beginPath();
      ctx.moveTo(sx - pdIso, sy - pdHt);
      ctx.lineTo(sx, sy - pdHt + pdD);
      ctx.lineTo(sx, sy + pdD);
      ctx.lineTo(sx - pdIso, sy);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = p.base;
      ctx.beginPath();
      ctx.moveTo(sx + pdIso, sy - pdHt);
      ctx.lineTo(sx, sy - pdHt + pdD);
      ctx.lineTo(sx, sy + pdD);
      ctx.lineTo(sx + pdIso, sy);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = p.accent;
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.moveTo(sx - pdIso, sy - pdHt);
      ctx.lineTo(sx, sy - pdHt - pdD);
      ctx.lineTo(sx + pdIso, sy - pdHt);
      ctx.stroke();

      const bt = sy - pdHt;

      // === TAIL (behind body) ===
      ctx.strokeStyle = p.mid;
      ctx.lineWidth = 3 * s;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(sx - hw + 4 * s, bt - bodyH * 0.5);
      ctx.bezierCurveTo(
        sx - hw - 10 * s,
        bt - bodyH * 0.7,
        sx - hw - 12 * s,
        bt - bodyH * 1.3,
        sx - hw - 6 * s,
        bt - bodyH * 1.5
      );
      ctx.stroke();
      ctx.fillStyle = p.dark;
      ctx.beginPath();
      ctx.arc(sx - hw - 6 * s, bt - bodyH * 1.5, 2.5 * s, 0, Math.PI * 2);
      ctx.fill();

      // === LION BODY (isometric box with rounded back) ===
      // Body left face (shadow) with raised hump
      ctx.fillStyle = p.shadow;
      ctx.beginPath();
      ctx.moveTo(sx - hw, bt);
      ctx.lineTo(sx - hw, bt - bodyH * 0.8);
      ctx.bezierCurveTo(
        sx - hw + 6 * s,
        bt - bodyH * 1.4,
        sx - 4 * s,
        bt - bodyH * 1.4,
        sx,
        bt - bodyH
      );
      ctx.lineTo(sx + hw, bt - bodyH);
      ctx.lineTo(sx + hw, bt);
      ctx.lineTo(sx, bt + hd);
      ctx.lineTo(sx - hw, bt);
      ctx.closePath();
      ctx.fill();

      // Body right face (lit)
      ctx.fillStyle = p.lit;
      ctx.beginPath();
      ctx.moveTo(sx + hw, bt);
      ctx.lineTo(sx + hw, bt - bodyH);
      ctx.lineTo(sx, bt - bodyH - hd);
      ctx.bezierCurveTo(
        sx - 4 * s,
        bt - bodyH * 1.4 - hd,
        sx - hw + 6 * s,
        bt - bodyH * 1.4 - hd,
        sx - hw,
        bt - bodyH * 0.8 - hd
      );
      ctx.lineTo(sx - hw, bt - hd);
      ctx.lineTo(sx, bt + hd);
      ctx.lineTo(sx + hw, bt);
      ctx.closePath();
      ctx.fill();

      // Body top face
      ctx.fillStyle = p.base;
      ctx.beginPath();
      ctx.moveTo(sx - hw, bt - bodyH * 0.8);
      ctx.bezierCurveTo(
        sx - hw + 6 * s,
        bt - bodyH * 1.4,
        sx - 4 * s,
        bt - bodyH * 1.4,
        sx,
        bt - bodyH
      );
      ctx.lineTo(sx + hw, bt - bodyH);
      ctx.lineTo(sx, bt - bodyH - hd);
      ctx.bezierCurveTo(
        sx - 4 * s,
        bt - bodyH * 1.4 - hd,
        sx - hw + 6 * s,
        bt - bodyH * 1.4 - hd,
        sx - hw,
        bt - bodyH * 0.8 - hd
      );
      ctx.closePath();
      ctx.fill();

      // Front face
      ctx.fillStyle = p.mid;
      ctx.beginPath();
      ctx.moveTo(sx + hw, bt);
      ctx.lineTo(sx + hw, bt - bodyH);
      ctx.lineTo(sx, bt - bodyH - hd);
      ctx.lineTo(sx, bt + hd);
      ctx.closePath();
      ctx.fill();

      // Body contour lines
      ctx.strokeStyle = p.dark;
      ctx.lineWidth = 0.8 * s;
      // Spine line
      ctx.beginPath();
      ctx.moveTo(sx - hw + 4 * s, bt - bodyH * 0.85);
      ctx.quadraticCurveTo(
        sx - 4 * s,
        bt - bodyH * 1.3,
        sx + 4 * s,
        bt - bodyH * 1.1
      );
      ctx.stroke();
      // Haunch muscle
      ctx.beginPath();
      ctx.moveTo(sx - hw + 2 * s, bt - bodyH * 0.3);
      ctx.quadraticCurveTo(
        sx - hw + 8 * s,
        bt - bodyH * 0.7,
        sx - 2 * s,
        bt - bodyH * 0.9
      );
      ctx.stroke();

      // === FRONT PAWS ===
      const pawL = 16 * s;
      const pawH2 = 5 * s;
      const pawFX = sx + hw;
      const pawIso = 6 * s * ISO_COS * 0.5;

      // Left paw
      const lpY = bt - 2 * s;
      ctx.fillStyle = p.dark;
      ctx.beginPath();
      ctx.moveTo(pawFX, lpY);
      ctx.lineTo(pawFX + pawL * 0.5, lpY + pawL * 0.25);
      ctx.lineTo(pawFX + pawL * 0.5, lpY + pawL * 0.25 + pawH2);
      ctx.lineTo(pawFX, lpY + pawH2);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = p.mid;
      ctx.beginPath();
      ctx.moveTo(pawFX, lpY);
      ctx.lineTo(pawFX + pawL * 0.5, lpY + pawL * 0.25);
      ctx.lineTo(pawFX + pawL * 0.5 - pawIso, lpY + pawL * 0.25 - 6 * s * 0.25);
      ctx.lineTo(pawFX - pawIso, lpY - 6 * s * 0.25);
      ctx.closePath();
      ctx.fill();

      // Right paw
      const rpY = bt - bodyH + 2 * s;
      ctx.fillStyle = p.shadow;
      ctx.beginPath();
      ctx.moveTo(pawFX, rpY);
      ctx.lineTo(pawFX + pawL * 0.5, rpY + pawL * 0.25);
      ctx.lineTo(pawFX + pawL * 0.5, rpY + pawL * 0.25 + pawH2);
      ctx.lineTo(pawFX, rpY + pawH2);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = p.lit;
      ctx.beginPath();
      ctx.moveTo(pawFX, rpY);
      ctx.lineTo(pawFX + pawL * 0.5, rpY + pawL * 0.25);
      ctx.lineTo(pawFX + pawL * 0.5 + pawIso, rpY + pawL * 0.25 - 6 * s * 0.25);
      ctx.lineTo(pawFX + pawIso, rpY - 6 * s * 0.25);
      ctx.closePath();
      ctx.fill();

      // === HEAD ===
      const hx = sx + hw - 4 * s;
      const hy = bt - bodyH - 6 * s;
      const headW2 = 12 * s;
      const headH2 = 18 * s;
      const hdIso = headW2 * ISO_COS * 0.5;
      const hdDep = headW2 * ISO_SIN * 0.25;

      // Nemes lappets
      ctx.fillStyle = p.mid;
      ctx.beginPath();
      ctx.moveTo(hx - hdIso - 2 * s, hy);
      ctx.lineTo(hx - hdIso - 4 * s, hy + headH2 + 6 * s);
      ctx.lineTo(hx - hdIso, hy + headH2 + 6 * s);
      ctx.lineTo(hx - hdIso + 2 * s, hy + 2 * s);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = p.lit;
      ctx.beginPath();
      ctx.moveTo(hx + hdIso + 2 * s, hy);
      ctx.lineTo(hx + hdIso + 4 * s, hy + headH2 + 6 * s);
      ctx.lineTo(hx + hdIso, hy + headH2 + 6 * s);
      ctx.lineTo(hx + hdIso - 2 * s, hy + 2 * s);
      ctx.closePath();
      ctx.fill();

      // Head left face
      ctx.fillStyle = p.dark;
      ctx.beginPath();
      ctx.moveTo(hx - hdIso, hy);
      ctx.lineTo(hx, hy + hdDep);
      ctx.lineTo(hx, hy + headH2 + hdDep - 3 * s);
      ctx.lineTo(hx - hdIso + 2 * s, hy + headH2);
      ctx.closePath();
      ctx.fill();
      // Head right face
      ctx.fillStyle = p.lit;
      ctx.beginPath();
      ctx.moveTo(hx + hdIso, hy);
      ctx.lineTo(hx, hy + hdDep);
      ctx.lineTo(hx, hy + headH2 + hdDep - 3 * s);
      ctx.lineTo(hx + hdIso - 2 * s, hy + headH2);
      ctx.closePath();
      ctx.fill();
      // Head front face
      ctx.fillStyle = p.base;
      ctx.beginPath();
      ctx.moveTo(hx - hdIso, hy);
      ctx.lineTo(hx + hdIso, hy);
      ctx.lineTo(hx + hdIso - 2 * s, hy + headH2);
      ctx.lineTo(hx - hdIso + 2 * s, hy + headH2);
      ctx.closePath();
      ctx.fill();

      // Nemes dome
      ctx.fillStyle = p.lit;
      ctx.beginPath();
      ctx.moveTo(hx - hdIso - 2 * s, hy);
      ctx.bezierCurveTo(
        hx - hdIso - 3 * s,
        hy - 9 * s,
        hx - 4 * s,
        hy - 14 * s,
        hx,
        hy - 16 * s
      );
      ctx.bezierCurveTo(
        hx + 4 * s,
        hy - 14 * s,
        hx + hdIso + 3 * s,
        hy - 9 * s,
        hx + hdIso + 2 * s,
        hy
      );
      ctx.closePath();
      ctx.fill();

      // Nemes stripes
      ctx.strokeStyle = p.stripe;
      ctx.lineWidth = 1.2 * s;
      for (let ns = 0; ns < 4; ns++) {
        const nsFrac = (ns + 1) / 5;
        const nsY = hy - 16 * s + nsFrac * 16 * s;
        const nsSpread = (hdIso + 2 * s) * Math.sin(nsFrac * Math.PI * 0.9);
        ctx.beginPath();
        ctx.moveTo(hx - nsSpread, nsY + nsFrac * 2 * s);
        ctx.quadraticCurveTo(
          hx,
          nsY - 1.5 * s,
          hx + nsSpread,
          nsY + nsFrac * 2 * s
        );
        ctx.stroke();
      }

      // Brow ridges
      ctx.fillStyle = p.dark;
      ctx.beginPath();
      ctx.moveTo(hx - 6.5 * s, hy + 2.5 * s);
      ctx.quadraticCurveTo(hx - 3.5 * s, hy + 1 * s, hx - 1 * s, hy + 2.2 * s);
      ctx.lineTo(hx - 1 * s, hy + 3 * s);
      ctx.quadraticCurveTo(
        hx - 3.5 * s,
        hy + 2 * s,
        hx - 6.5 * s,
        hy + 3.3 * s
      );
      ctx.closePath();
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(hx + 1 * s, hy + 2.2 * s);
      ctx.quadraticCurveTo(
        hx + 3.5 * s,
        hy + 1 * s,
        hx + 6.5 * s,
        hy + 2.5 * s
      );
      ctx.lineTo(hx + 6.5 * s, hy + 3.3 * s);
      ctx.quadraticCurveTo(hx + 3.5 * s, hy + 2 * s, hx + 1 * s, hy + 3 * s);
      ctx.closePath();
      ctx.fill();

      // Almond eye sockets
      ctx.fillStyle = p.shadow;
      ctx.beginPath();
      ctx.moveTo(hx - 6 * s, hy + 4 * s);
      ctx.quadraticCurveTo(hx - 3.5 * s, hy + 2.8 * s, hx - 1 * s, hy + 4 * s);
      ctx.quadraticCurveTo(hx - 3.5 * s, hy + 5 * s, hx - 6 * s, hy + 4 * s);
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(hx + 1 * s, hy + 4 * s);
      ctx.quadraticCurveTo(hx + 3.5 * s, hy + 2.8 * s, hx + 6 * s, hy + 4 * s);
      ctx.quadraticCurveTo(hx + 3.5 * s, hy + 5 * s, hx + 1 * s, hy + 4 * s);
      ctx.fill();

      // Almond eyes (lit)
      ctx.fillStyle = p.lit;
      ctx.beginPath();
      ctx.moveTo(hx - 5.5 * s, hy + 4 * s);
      ctx.quadraticCurveTo(
        hx - 3.5 * s,
        hy + 3.2 * s,
        hx - 1.5 * s,
        hy + 4 * s
      );
      ctx.quadraticCurveTo(
        hx - 3.5 * s,
        hy + 4.6 * s,
        hx - 5.5 * s,
        hy + 4 * s
      );
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(hx + 1.5 * s, hy + 4 * s);
      ctx.quadraticCurveTo(
        hx + 3.5 * s,
        hy + 3.2 * s,
        hx + 5.5 * s,
        hy + 4 * s
      );
      ctx.quadraticCurveTo(
        hx + 3.5 * s,
        hy + 4.6 * s,
        hx + 1.5 * s,
        hy + 4 * s
      );
      ctx.fill();

      // Kohl liner
      ctx.strokeStyle = "#1a1008";
      ctx.lineWidth = 0.8 * s;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(hx - 6 * s, hy + 4 * s);
      ctx.quadraticCurveTo(hx - 3.5 * s, hy + 2.8 * s, hx - 1 * s, hy + 4 * s);
      ctx.quadraticCurveTo(hx - 3.5 * s, hy + 5 * s, hx - 6 * s, hy + 4 * s);
      ctx.stroke();
      ctx.lineWidth = 0.6 * s;
      ctx.beginPath();
      ctx.moveTo(hx - 6 * s, hy + 4 * s);
      ctx.lineTo(hx - 7 * s, hy + 5 * s);
      ctx.stroke();
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(hx + 1 * s, hy + 4 * s);
      ctx.quadraticCurveTo(hx + 3.5 * s, hy + 2.8 * s, hx + 6 * s, hy + 4 * s);
      ctx.quadraticCurveTo(hx + 3.5 * s, hy + 5 * s, hx + 1 * s, hy + 4 * s);
      ctx.stroke();
      ctx.lineWidth = 0.6 * s;
      ctx.beginPath();
      ctx.moveTo(hx + 6 * s, hy + 4 * s);
      ctx.lineTo(hx + 7 * s, hy + 5 * s);
      ctx.stroke();
      ctx.lineCap = "butt";

      // Nose bridge and tip
      ctx.fillStyle = p.base;
      ctx.beginPath();
      ctx.moveTo(hx - 0.8 * s, hy + 5.5 * s);
      ctx.lineTo(hx + 0.8 * s, hy + 5.5 * s);
      ctx.lineTo(hx + 1.2 * s, hy + 8.5 * s);
      ctx.lineTo(hx - 1.2 * s, hy + 8.5 * s);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = p.dark;
      ctx.beginPath();
      ctx.moveTo(hx - 1.2 * s, hy + 8.5 * s);
      ctx.quadraticCurveTo(hx, hy + 9.5 * s, hx + 1.2 * s, hy + 8.5 * s);
      ctx.closePath();
      ctx.fill();

      // Subtle mouth
      ctx.strokeStyle = p.dark;
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(hx - 2.5 * s, hy + 11.5 * s);
      ctx.quadraticCurveTo(hx, hy + 12.5 * s, hx + 2.5 * s, hy + 11.5 * s);
      ctx.stroke();

      // Chin line
      ctx.strokeStyle = "rgba(100, 80, 50, 0.2)";
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.moveTo(hx - 4 * s, hy + 13 * s);
      ctx.quadraticCurveTo(hx, hy + 14.5 * s, hx + 4 * s, hy + 13 * s);
      ctx.stroke();

      // Uraeus
      ctx.fillStyle = p.accent;
      ctx.beginPath();
      ctx.moveTo(hx, hy - 12 * s);
      ctx.bezierCurveTo(
        hx - 2 * s,
        hy - 16 * s,
        hx + 2 * s,
        hy - 20 * s,
        hx,
        hy - 22 * s
      );
      ctx.bezierCurveTo(
        hx + 3 * s,
        hy - 19 * s,
        hx + 3 * s,
        hy - 15 * s,
        hx + 1 * s,
        hy - 12 * s
      );
      ctx.closePath();
      ctx.fill();

      break;
    }
    case "oasis_pool": {
      const time = Date.now() / 1000;
      const oasisSeed = (dec.x || 0) * 14.7;

      // Color palette
      const sandLight = "#E8D4A8";
      const sandMid = "#D4C090";
      const sandDark = "#B8A070";
      const sandWet = "#9A8860";
      const waterDeep = "#1565C0";
      const waterMid = "#1E88E5";
      const waterShallow = "#42A5F5";
      const waterSurface = "#64B5F6";
      const foamWhite = "#E3F2FD";

      // ========== GROUND SHADOW ==========
      const lakeShadowGrad = ctx.createRadialGradient(
        screenPos.x + 3 * s,
        screenPos.y + 5 * s,
        0,
        screenPos.x + 3 * s,
        screenPos.y + 5 * s,
        50 * s
      );
      lakeShadowGrad.addColorStop(0, "rgba(0,0,0,0.25)");
      lakeShadowGrad.addColorStop(0.6, "rgba(0,0,0,0.1)");
      lakeShadowGrad.addColorStop(1, "rgba(0,0,0,0)");
      ctx.fillStyle = lakeShadowGrad;
      drawOrganicWaterShape(
        ctx,
        screenPos.x + 3 * s,
        screenPos.y + 8 * s,
        45 * s,
        20 * s,
        oasisSeed - 10,
        0.1
      );
      ctx.fill();

      // ========== OUTER SAND BANK (BEACH) ==========
      // Gradient from dry sand to wet sand
      const sandGrad = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y + 2 * s,
        25 * s,
        screenPos.x,
        screenPos.y + 2 * s,
        42 * s
      );
      sandGrad.addColorStop(0, sandWet);
      sandGrad.addColorStop(0.4, sandMid);
      sandGrad.addColorStop(0.8, sandLight);
      sandGrad.addColorStop(1, sandLight);

      // Sand bank 3D edge (thickness illusion) — drawn first so main bank covers top
      ctx.fillStyle = sandDark;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y + 5 * s,
        43 * s,
        19 * s,
        oasisSeed + 3,
        0.13
      );
      ctx.fill();

      // Main sand bank with organic edges
      ctx.fillStyle = sandGrad;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y + 1 * s,
        43 * s,
        19 * s,
        oasisSeed + 3,
        0.13
      );
      ctx.fill();

      // Sand texture dots
      ctx.fillStyle = "rgba(180, 160, 100, 0.4)";
      for (let i = 0; i < 25; i++) {
        const sandAngle = (i / 25) * Math.PI * 2;
        const sandDist = 32 + Math.sin(i * 7.3) * 8;
        const sandX = screenPos.x + Math.cos(sandAngle) * sandDist * s * 1.1;
        const sandY =
          screenPos.y + 2 * s + Math.sin(sandAngle) * sandDist * 0.42 * s;
        ctx.beginPath();
        ctx.arc(
          sandX,
          sandY,
          (1 + Math.abs(Math.sin(i * 3.7))) * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // ========== WATER BODY ==========
      // Multi-layered water depth gradient
      const waterGrad = ctx.createRadialGradient(
        screenPos.x - 5 * s,
        screenPos.y - 2 * s,
        0,
        screenPos.x,
        screenPos.y + 2 * s,
        30 * s
      );
      waterGrad.addColorStop(0, waterDeep);
      waterGrad.addColorStop(0.3, waterMid);
      waterGrad.addColorStop(0.7, waterShallow);
      waterGrad.addColorStop(0.9, waterSurface);
      waterGrad.addColorStop(1, "#90CAF9");

      ctx.fillStyle = waterGrad;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y + 2 * s,
        30 * s,
        13 * s,
        oasisSeed,
        0.14
      );
      ctx.fill();

      // ========== UNDERWATER CAUSTICS ==========
      // Animated light patterns on the lake bed
      ctx.globalAlpha = 0.15;
      for (let c = 0; c < 8; c++) {
        const causticTime = time * 0.8 + c * 0.9;
        const causticX = screenPos.x + Math.sin(causticTime + c * 2.1) * 18 * s;
        const causticY =
          screenPos.y + 2 * s + Math.cos(causticTime * 0.7 + c) * 6 * s;
        const causticSize = (4 + Math.sin(causticTime * 1.5) * 2) * s;

        const causticGrad = ctx.createRadialGradient(
          causticX,
          causticY,
          0,
          causticX,
          causticY,
          causticSize
        );
        causticGrad.addColorStop(0, "#BBDEFB");
        causticGrad.addColorStop(0.5, "rgba(144, 202, 249, 0.5)");
        causticGrad.addColorStop(1, "rgba(100, 181, 246, 0)");

        ctx.fillStyle = causticGrad;
        ctx.beginPath();
        ctx.ellipse(
          causticX,
          causticY,
          causticSize * 1.5,
          causticSize * 0.7,
          c * 0.5,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      ctx.globalAlpha = 1;

      // ========== WATER SURFACE EFFECTS ==========
      // Animated ripples
      const rippleCount = 3;
      for (let r = 0; r < rippleCount; r++) {
        const ripplePhase = ((time * 0.4 + r * 1.2) % 3) / 3;
        const rippleAlpha = 0.25 * (1 - ripplePhase);
        const rippleSize = ripplePhase * 20;

        ctx.strokeStyle = `rgba(255, 255, 255, ${rippleAlpha})`;
        ctx.lineWidth = (1.5 - ripplePhase) * s;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 8 * s + r * 8 * s,
          screenPos.y + r * 2 * s,
          (5 + rippleSize) * s,
          (2 + rippleSize * 0.4) * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.stroke();
      }

      // Surface shimmer/sparkles
      ctx.fillStyle = foamWhite;
      for (let sp = 0; sp < 12; sp++) {
        const sparkleTime = time * 2 + sp * 0.7;
        const sparkleAlpha = 0.3 + Math.sin(sparkleTime * 3) * 0.3;
        if (sparkleAlpha > 0.35) {
          const spAngle = sp * 0.52 + Math.sin(time * 0.5) * 0.3;
          const spDist = 8 + sp * 1.8;
          const spX = screenPos.x + Math.cos(spAngle) * spDist * s;
          const spY =
            screenPos.y + 2 * s + Math.sin(spAngle) * spDist * 0.4 * s;

          ctx.globalAlpha = sparkleAlpha;
          ctx.beginPath();
          ctx.arc(
            spX,
            spY,
            (0.8 + Math.sin(sparkleTime) * 0.3) * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
      }
      ctx.globalAlpha = 1;

      // ========== LILY PADS ==========
      const lilyPositions = [
        { rot: 0.3, size: 1, x: -15, y: 4 },
        { rot: -0.5, size: 0.8, x: -10, y: -3 },
        { rot: 0.8, size: 0.9, x: 12, y: 5 },
        { rot: -0.2, size: 0.7, x: 18, y: -1 },
        { rot: 1.2, size: 0.85, x: -5, y: 7 },
      ];

      lilyPositions.forEach((lily, i) => {
        const lilyX = screenPos.x + lily.x * s;
        const lilyY = screenPos.y + lily.y * s;
        const lilySway = Math.sin(time * 0.8 + i * 1.5) * 0.5 * s;
        const lilySize = 5 * lily.size * s;

        // Lily pad shadow
        ctx.fillStyle = "rgba(0, 80, 120, 0.2)";
        ctx.beginPath();
        ctx.ellipse(
          lilyX + lilySway + 1 * s,
          lilyY + 1 * s,
          lilySize * 1.1,
          lilySize * 0.5,
          lily.rot,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Lily pad
        ctx.fillStyle = "#2E7D32";
        ctx.beginPath();
        ctx.ellipse(
          lilyX + lilySway,
          lilyY,
          lilySize,
          lilySize * 0.45,
          lily.rot,
          0.15,
          Math.PI * 2 - 0.15
        );
        ctx.lineTo(lilyX + lilySway, lilyY);
        ctx.fill();

        // Lily pad highlight
        ctx.fillStyle = "#4CAF50";
        ctx.beginPath();
        ctx.ellipse(
          lilyX + lilySway - 1 * s,
          lilyY - 0.5 * s,
          lilySize * 0.5,
          lilySize * 0.2,
          lily.rot + 0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Flower on some pads
        if (i % 2 === 0) {
          const flowerX = lilyX + lilySway;
          const flowerY = lilyY - 2 * s;

          // Petals
          ctx.fillStyle = i === 0 ? "#F8BBD9" : "#FFF9C4";
          for (let p = 0; p < 6; p++) {
            const petalAngle = (p / 6) * Math.PI * 2 + time * 0.2;
            const petalX = flowerX + Math.cos(petalAngle) * 2.5 * s;
            const petalY = flowerY + Math.sin(petalAngle) * 1.2 * s;
            ctx.beginPath();
            ctx.ellipse(
              petalX,
              petalY,
              2 * s,
              1 * s,
              petalAngle,
              0,
              Math.PI * 2
            );
            ctx.fill();
          }

          // Flower center
          ctx.fillStyle = "#FFC107";
          ctx.beginPath();
          ctx.arc(flowerX, flowerY, 1.5 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      });

      // ========== CATTAILS/REEDS ==========
      const reedPositions = [
        { count: 3, x: -28, y: 8 },
        { count: 2, x: 28, y: 6 },
        { count: 2, x: -22, y: -8 },
        { count: 2, x: 25, y: -6 },
      ];

      reedPositions.forEach((reedGroup, gi) => {
        for (let ri = 0; ri < reedGroup.count; ri++) {
          const reedX = screenPos.x + (reedGroup.x + ri * 3) * s;
          const reedY = screenPos.y + reedGroup.y * s;
          const reedSway = Math.sin(time * 1.2 + gi + ri * 0.5) * 2 * s;
          const reedHeight = (18 + ri * 4) * s;

          // Reed stalk
          ctx.strokeStyle = "#5D4037";
          ctx.lineWidth = 1.5 * s;
          ctx.beginPath();
          ctx.moveTo(reedX, reedY);
          ctx.quadraticCurveTo(
            reedX + reedSway * 0.3,
            reedY - reedHeight * 0.5,
            reedX + reedSway,
            reedY - reedHeight
          );
          ctx.stroke();

          // Cattail head
          ctx.fillStyle = "#4E342E";
          ctx.beginPath();
          ctx.ellipse(
            reedX + reedSway * 0.85,
            reedY - reedHeight + 4 * s,
            1.8 * s,
            5 * s,
            reedSway * 0.05,
            0,
            Math.PI * 2
          );
          ctx.fill();

          // Reed leaves
          ctx.strokeStyle = "#7CB342";
          ctx.lineWidth = 2 * s;
          ctx.beginPath();
          ctx.moveTo(reedX, reedY - 2 * s);
          ctx.quadraticCurveTo(
            reedX + 6 * s + reedSway * 0.5,
            reedY - 8 * s,
            reedX + 10 * s + reedSway,
            reedY - 6 * s
          );
          ctx.stroke();
        }
      });

      // ========== EDGE FOAM/SHORE LINE ==========
      ctx.strokeStyle = "rgba(227, 242, 253, 0.5)";
      ctx.lineWidth = 2 * s;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y + 2 * s,
        29 * s,
        12.5 * s,
        oasisSeed + 5,
        0.14
      );
      ctx.stroke();

      // Subtle animated foam at edges
      const foamAlpha = 0.3 + Math.sin(time * 2) * 0.1;
      ctx.fillStyle = `rgba(255, 255, 255, ${foamAlpha})`;
      for (let f = 0; f < 8; f++) {
        const foamAngle = (f / 8) * Math.PI * 2 + time * 0.15;
        const foamDist = 28 + Math.sin(time * 1.5 + f * 2) * 1.5;
        const foamX = screenPos.x + Math.cos(foamAngle) * foamDist * s;
        const foamY =
          screenPos.y + 2 * s + Math.sin(foamAngle) * foamDist * 0.43 * s;
        ctx.beginPath();
        ctx.ellipse(
          foamX,
          foamY,
          2.5 * s,
          1 * s,
          foamAngle + 0.5,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // ========== SMALL FISH SHADOWS ==========
      ctx.fillStyle = "rgba(0, 60, 100, 0.2)";
      for (let fish = 0; fish < 3; fish++) {
        const fishTime = time * 0.6 + fish * 2.1;
        const fishAngle = fishTime * 0.4;
        const fishDist = 10 + fish * 5;
        const fishX = screenPos.x + Math.cos(fishAngle) * fishDist * s;
        const fishY =
          screenPos.y + 2 * s + Math.sin(fishAngle) * fishDist * 0.35 * s;

        ctx.beginPath();
        ctx.ellipse(
          fishX,
          fishY,
          2.5 * s,
          1 * s,
          fishAngle + Math.PI / 2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Fish tail
        ctx.beginPath();
        ctx.moveTo(
          fishX - Math.cos(fishAngle + Math.PI / 2) * 2.5 * s,
          fishY - Math.sin(fishAngle + Math.PI / 2) * 1 * s
        );
        ctx.lineTo(
          fishX -
            Math.cos(fishAngle + Math.PI / 2) * 4 * s -
            Math.sin(fishAngle) * 1.5 * s,
          fishY - Math.sin(fishAngle + Math.PI / 2) * 1.5 * s
        );
        ctx.lineTo(
          fishX -
            Math.cos(fishAngle + Math.PI / 2) * 4 * s +
            Math.sin(fishAngle) * 1.5 * s,
          fishY - Math.sin(fishAngle + Math.PI / 2) * 1.5 * s
        );
        ctx.closePath();
        ctx.fill();
      }

      break;
    }
    case "carnegie_lake": {
      const time = Date.now() / 1000;
      const cx = screenPos.x;
      const cy = screenPos.y;
      const ISO = ISO_COS;
      const lakeSeed = (dec.x || 0) * 17.3;

      // --- Background pass: ground, embankment, water, and all flat effects ---
      if (!skipShadow) {
        // === OUTER GROUND BLEND (feathers into map ground) ===
        const outerBlend = ctx.createRadialGradient(
          cx,
          cy + 4 * s,
          22 * s,
          cx,
          cy + 4 * s,
          56 * s
        );
        outerBlend.addColorStop(0, "rgba(58,47,31,0.25)");
        outerBlend.addColorStop(0.6, "rgba(58,47,31,0.12)");
        outerBlend.addColorStop(1, "transparent");
        ctx.fillStyle = outerBlend;
        drawOrganicWaterShape(
          ctx,
          cx,
          cy + 4 * s,
          56 * s,
          24 * s,
          lakeSeed - 15,
          0.08
        );
        ctx.fill();

        // === STONE EMBANKMENT ===
        // Edge = region ground (#3a2f1f), center = brighter earthy brown
        const embankGrad = ctx.createRadialGradient(
          cx,
          cy + 2 * s,
          4 * s,
          cx,
          cy + 2 * s,
          40 * s
        );
        embankGrad.addColorStop(0, "#5a4a35");
        embankGrad.addColorStop(0.35, "#4f4230");
        embankGrad.addColorStop(0.65, "#453828");
        embankGrad.addColorStop(1, "#3a2f1f");

        // Stone wall edge (3D depth)
        const wallGrad = ctx.createLinearGradient(
          cx,
          cy + 13 * s,
          cx,
          cy + 20 * s
        );
        wallGrad.addColorStop(0, "#3a2f1f");
        wallGrad.addColorStop(0.5, "#352a1a");
        wallGrad.addColorStop(1, "#2a1f0f");
        ctx.fillStyle = wallGrad;
        drawOrganicWaterShape(
          ctx,
          cx,
          cy + 4 * s,
          40 * s,
          17 * s,
          lakeSeed + 3,
          0.11
        );
        ctx.fill();

        // Main embankment with organic edges
        ctx.fillStyle = embankGrad;
        drawOrganicWaterShape(
          ctx,
          cx,
          cy + 1 * s,
          40 * s,
          17 * s,
          lakeSeed + 3,
          0.11
        );
        ctx.fill();

        // Subtle earth texture
        ctx.strokeStyle = "rgba(0,0,0,0.05)";
        ctx.lineWidth = 0.8 * s;
        for (let st = 0; st < 16; st++) {
          const sa = (st / 16) * Math.PI * 2;
          const sd = 32 + Math.sin(st * 3.1) * 4;
          const stx = cx + Math.cos(sa) * sd * s;
          const sty = cy + 4 * s + Math.sin(sa) * sd * 0.42 * s;
          ctx.beginPath();
          ctx.moveTo(stx - 3 * s, sty);
          ctx.lineTo(stx + 3 * s, sty);
          ctx.stroke();
        }

        // Dirt/gravel patches
        for (let dp = 0; dp < 8; dp++) {
          const dpa = dp * 0.8 + 0.3;
          const dpr = 28 + Math.sin(dp * 3.7 + lakeSeed) * 7;
          const dpx = cx + Math.cos(dpa) * dpr * s;
          const dpy = cy + 2 * s + Math.sin(dpa) * dpr * 0.42 * s;
          ctx.fillStyle = `rgba(58,47,31,${0.15 + Math.sin(dp * 2.1) * 0.05})`;
          drawOrganicWaterShape(
            ctx,
            dpx,
            dpy,
            5 * s,
            2.5 * s,
            lakeSeed + dp * 13,
            0.2
          );
          ctx.fill();
        }

        // Cobblestone path along right shore
        ctx.fillStyle = "#4a3f2d";
        ctx.beginPath();
        ctx.moveTo(cx + 28 * s, cy - 4 * s);
        ctx.bezierCurveTo(
          cx + 36 * s,
          cy + 1 * s,
          cx + 34 * s,
          cy + 7 * s,
          cx + 26 * s,
          cy + 10 * s
        );
        ctx.bezierCurveTo(
          cx + 32 * s,
          cy + 6 * s,
          cx + 33 * s,
          cy,
          cx + 27 * s,
          cy - 3 * s
        );
        ctx.fill();
        ctx.fillStyle = "#3f3522";
        for (let cb = 0; cb < 8; cb++) {
          const cba = cb * 0.28 - 0.3;
          const cbd = 30 + Math.sin(cb * 2.3) * 2;
          const cbx = cx + Math.cos(cba) * cbd * s * 0.9;
          const cby = cy + 3 * s + Math.sin(cba) * cbd * 0.38 * s;
          ctx.beginPath();
          ctx.ellipse(cbx, cby, 2.5 * s, 1.5 * s, cba + 0.3, 0, Math.PI * 2);
          ctx.fill();
        }
        // === ENCHANTED WATER BODY ===
        // Teal/emerald magical water
        const wGrad = ctx.createRadialGradient(
          cx - 4 * s,
          cy - 2 * s,
          0,
          cx,
          cy + 2 * s,
          28 * s
        );
        wGrad.addColorStop(0, "#004d6b");
        wGrad.addColorStop(0.15, "#006080");
        wGrad.addColorStop(0.35, "#00838f");
        wGrad.addColorStop(0.55, "#0097a7");
        wGrad.addColorStop(0.75, "#00acc1");
        wGrad.addColorStop(0.92, "#26c6da");
        wGrad.addColorStop(1, "#4dd0e1");
        ctx.fillStyle = wGrad;
        drawOrganicWaterShape(
          ctx,
          cx,
          cy + 2 * s,
          28 * s,
          12 * s,
          lakeSeed,
          0.13
        );
        ctx.fill();

        // Magical glow under water surface
        const glowPulse = 0.15 + Math.sin(time * 1.5) * 0.06;
        const wGlow = ctx.createRadialGradient(
          cx,
          cy + 2 * s,
          0,
          cx,
          cy + 2 * s,
          22 * s
        );
        wGlow.addColorStop(0, `rgba(100,255,220,${glowPulse})`);
        wGlow.addColorStop(0.5, `rgba(80,220,200,${glowPulse * 0.5})`);
        wGlow.addColorStop(1, "transparent");
        ctx.fillStyle = wGlow;
        drawOrganicWaterShape(
          ctx,
          cx,
          cy + 2 * s,
          22 * s,
          9 * s,
          lakeSeed + 20,
          0.13
        );
        ctx.fill();

        // Underwater caustics (animated light network)
        ctx.globalAlpha = 0.14;
        for (let c = 0; c < 12; c++) {
          const ct = time * 0.6 + c * 0.7;
          const ccx = cx + Math.sin(ct + c * 1.6) * 20 * s;
          const ccy = cy + 2 * s + Math.cos(ct * 0.5 + c) * 7 * s;
          const ccs = (4 + Math.sin(ct * 1.2) * 2.5) * s;
          const cg = ctx.createRadialGradient(ccx, ccy, 0, ccx, ccy, ccs);
          cg.addColorStop(0, "#a7ffeb");
          cg.addColorStop(0.4, "rgba(128,255,219,0.5)");
          cg.addColorStop(1, "transparent");
          ctx.fillStyle = cg;
          ctx.beginPath();
          ctx.ellipse(ccx, ccy, ccs * 1.6, ccs * 0.7, c * 0.35, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.globalAlpha = 1;

        // Animated concentric ripples
        for (let r = 0; r < 5; r++) {
          const rp = ((time * 0.3 + r * 0.8) % 3.5) / 3.5;
          const ra = 0.2 * (1 - rp);
          const rs = rp * 20;
          ctx.strokeStyle = `rgba(180,255,240,${ra})`;
          ctx.lineWidth = (2 - rp * 1.2) * s;
          ctx.beginPath();
          ctx.ellipse(
            cx - 6 * s + r * 3.5 * s,
            cy + 2 * s + r * 0.8 * s,
            (5 + rs) * s,
            (2 + rs * 0.38) * s,
            0.06,
            0,
            Math.PI * 2
          );
          ctx.stroke();
        }

        // Sparkles on water (magical)
        for (let sp = 0; sp < 18; sp++) {
          const st2 = time * 2.5 + sp * 0.55;
          const sa2 = 0.3 + Math.sin(st2 * 3.5) * 0.35;
          if (sa2 > 0.35) {
            const sang2 = sp * 0.4 + Math.sin(time * 0.35) * 0.2;
            const sd2 = 4 + sp * 1.3;
            const spx2 = cx + Math.cos(sang2) * sd2 * s;
            const spy2 = cy + 3 * s + Math.sin(sang2) * sd2 * 0.37 * s;
            ctx.globalAlpha = sa2;
            const sparkCol =
              sp % 3 === 0 ? "#e0f7fa" : sp % 3 === 1 ? "#b2ebf2" : "#a7ffeb";
            ctx.fillStyle = sparkCol;
            ctx.beginPath();
            ctx.arc(
              spx2,
              spy2,
              (0.8 + Math.sin(st2) * 0.4) * s,
              0,
              Math.PI * 2
            );
            ctx.fill();
          }
        }
        ctx.globalAlpha = 1;

        // Shore glow line
        const shoreGlow = 0.35 + Math.sin(time * 1.8) * 0.1;
        ctx.strokeStyle = `rgba(100,255,220,${shoreGlow})`;
        ctx.lineWidth = 2.5 * s;
        drawOrganicWaterShape(
          ctx,
          cx,
          cy + 2 * s,
          27.5 * s,
          11.5 * s,
          lakeSeed + 5,
          0.13
        );
        ctx.stroke();
        ctx.strokeStyle = "rgba(255,255,255,0.25)";
        ctx.lineWidth = 1 * s;
        drawOrganicWaterShape(
          ctx,
          cx,
          cy + 2 * s,
          27 * s,
          11.2 * s,
          lakeSeed + 5,
          0.13
        );
        ctx.stroke();
      } // end !skipShadow
      if (shadowOnly) {
        break;
      }

      // === LAKESIDE TREES (on left and right sides of lake) ===
      {
        const lakeTrees: {
          x: number;
          y: number;
          sc: number;
          variant: number;
        }[] = [
          { sc: 0.45, variant: 0, x: -34, y: -4 },
          { sc: 0.5, variant: 2, x: -30, y: 6 },
          { sc: 0.4, variant: 3, x: -36, y: 10 },
          { sc: 0.48, variant: 1, x: 34, y: -2 },
          { sc: 0.42, variant: 0, x: 32, y: 8 },
        ];
        const treePalettes = [
          {
            foliage: ["#2e7d32", "#388e3c", "#43a047", "#4caf50"],
            trunk: "#5d4037",
            trunkDark: "#3e2723",
          },
          {
            foliage: ["#1b5e20", "#2e7d32", "#388e3c", "#33691e"],
            trunk: "#4a3728",
            trunkDark: "#2d1f14",
          },
          {
            foliage: ["#33691e", "#558b2f", "#689f38", "#7cb342"],
            trunk: "#6d4c41",
            trunkDark: "#4e342e",
          },
          {
            foliage: ["#2e7d32", "#43a047", "#66bb6a", "#81c784"],
            trunk: "#5d4037",
            trunkDark: "#3e2723",
          },
        ];

        for (const lt of lakeTrees) {
          const ltx = cx + lt.x * s;
          const lty = cy + lt.y * s;
          const lts = lt.sc * s;
          const pal = treePalettes[lt.variant % 4];

          // Shadow
          ctx.fillStyle = "rgba(0,0,0,0.18)";
          ctx.beginPath();
          ctx.ellipse(
            ltx + 3 * lts,
            lty + 6 * lts,
            16 * lts,
            8 * lts,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();

          // Trunk left face
          ctx.fillStyle = pal.trunkDark;
          ctx.beginPath();
          ctx.moveTo(ltx - 3 * lts, lty + 3 * lts);
          ctx.lineTo(ltx - 2.5 * lts, lty - 14 * lts);
          ctx.lineTo(ltx, lty - 16 * lts);
          ctx.lineTo(ltx, lty + 1.5 * lts);
          ctx.closePath();
          ctx.fill();

          // Trunk right face
          const tG = ctx.createLinearGradient(ltx, lty, ltx + 4 * lts, lty);
          tG.addColorStop(0, pal.trunk);
          tG.addColorStop(1, pal.trunkDark);
          ctx.fillStyle = tG;
          ctx.beginPath();
          ctx.moveTo(ltx + 3 * lts, lty + 3 * lts);
          ctx.lineTo(ltx + 2.5 * lts, lty - 14 * lts);
          ctx.lineTo(ltx, lty - 16 * lts);
          ctx.lineTo(ltx, lty + 1.5 * lts);
          ctx.closePath();
          ctx.fill();

          // Foliage layers (back to front)
          const foliageLayers = [
            { dx: 0, dy: -28, rx: 14, ry: 10 },
            { dx: -5, dy: -22, rx: 12, ry: 9 },
            { dx: 4, dy: -24, rx: 11, ry: 8 },
            { dx: -2, dy: -18, rx: 13, ry: 10 },
            { dx: 2, dy: -20, rx: 10, ry: 7 },
          ];

          for (let fi = 0; fi < foliageLayers.length; fi++) {
            const fl = foliageLayers[fi];
            const fc = pal.foliage[fi % pal.foliage.length];
            const flG = ctx.createRadialGradient(
              ltx + fl.dx * lts,
              lty + fl.dy * lts,
              0,
              ltx + fl.dx * lts,
              lty + fl.dy * lts,
              fl.rx * lts
            );
            flG.addColorStop(0, fc);
            flG.addColorStop(0.7, fc);
            flG.addColorStop(1, pal.foliage[0]);
            ctx.fillStyle = flG;
            ctx.beginPath();
            ctx.ellipse(
              ltx + fl.dx * lts,
              lty + fl.dy * lts,
              fl.rx * lts,
              fl.ry * lts,
              0,
              0,
              Math.PI * 2
            );
            ctx.fill();
          }

          // Foliage highlight
          ctx.fillStyle = "rgba(255,255,255,0.08)";
          ctx.beginPath();
          ctx.ellipse(
            ltx + 3 * lts,
            lty - 26 * lts,
            6 * lts,
            4 * lts,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
      }

      // === BOATHOUSE (back-right) ===
      drawCarnegieLakeBoathouse(ctx, cx, cy, s, time);

      // === GRANDSTAND / COLUMNED PAVILION (back-left) ===
      drawCarnegieLakePavilion(ctx, cx, cy, s, time);

      // === CLOCK TOWER (back-center) ===
      drawCarnegieLakeClockTower(ctx, cx, cy, s, time);

      // === ROWBOAT bobbing ===
      drawCarnegieLakeRowboat(ctx, cx, cy, s, time);

      // === LILY PADS ===
      const clLilies = [
        { rot: 0.3, sz: 0.85, x: -15, y: 5 },
        { rot: -0.4, sz: 0.65, x: -10, y: -1 },
        { rot: 0.7, sz: 0.7, x: 16, y: 6 },
        { rot: 1.1, sz: 0.75, x: -5, y: 7 },
        { rot: -0.2, sz: 0.55, x: 20, y: 1 },
      ];
      for (let li = 0; li < clLilies.length; li++) {
        const lil = clLilies[li];
        const lx = cx + lil.x * s;
        const ly = cy + lil.y * s + Math.sin(time * 0.7 + li * 1.3) * 0.5 * s;
        const lsz = 5 * lil.sz * s;
        ctx.fillStyle = "rgba(0,80,100,0.15)";
        ctx.beginPath();
        ctx.ellipse(
          lx + 1 * s,
          ly + 1 * s,
          lsz * 1.1,
          lsz * 0.5,
          lil.rot,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "#1A3A18";
        ctx.beginPath();
        ctx.ellipse(lx, ly, lsz, lsz * 0.45, lil.rot, 0.15, Math.PI * 2 - 0.15);
        ctx.lineTo(lx, ly);
        ctx.fill();
        ctx.fillStyle = "#2A4A22";
        ctx.beginPath();
        ctx.ellipse(
          lx - 1 * s,
          ly - 0.5 * s,
          lsz * 0.45,
          lsz * 0.18,
          lil.rot + 0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Flowers on every other lily pad
        if (li % 2 === 0) {
          const flx = lx;
          const fly = ly - 2.5 * s;
          const petalColors = ["#6A4A50", "#5A4060", "#7A6A40"];
          ctx.fillStyle = petalColors[li % petalColors.length];
          for (let p = 0; p < 6; p++) {
            const pa = (p / 6) * Math.PI * 2 + time * 0.15;
            ctx.beginPath();
            ctx.ellipse(
              flx + Math.cos(pa) * 2.2 * s,
              fly + Math.sin(pa) * 1.1 * s,
              2 * s,
              1 * s,
              pa,
              0,
              Math.PI * 2
            );
            ctx.fill();
          }
          ctx.fillStyle = "#8A7040";
          ctx.beginPath();
          ctx.arc(flx, fly, 1.3 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // === FISH SHADOWS ===
      ctx.fillStyle = "rgba(0,60,80,0.15)";
      for (let f = 0; f < 4; f++) {
        const ft = time * 0.45 + f * 1.7;
        const fa = ft * 0.3;
        const fd = 10 + f * 7;
        const fx = cx + Math.cos(fa) * fd * s;
        const fy = cy + 3 * s + Math.sin(fa) * fd * 0.32 * s;
        ctx.beginPath();
        ctx.ellipse(fx, fy, 2.5 * s, 1 * s, fa + Math.PI / 2, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(
          fx - Math.cos(fa + Math.PI / 2) * 2.5 * s,
          fy - Math.sin(fa + Math.PI / 2) * 1 * s
        );
        ctx.lineTo(
          fx - Math.cos(fa + Math.PI / 2) * 4 * s - Math.sin(fa) * 1.5 * s,
          fy - Math.sin(fa + Math.PI / 2) * 1.5 * s
        );
        ctx.lineTo(
          fx - Math.cos(fa + Math.PI / 2) * 4 * s + Math.sin(fa) * 1.5 * s,
          fy - Math.sin(fa + Math.PI / 2) * 1.5 * s
        );
        ctx.closePath();
        ctx.fill();
      }

      // === MAGICAL FIREFLIES ===
      for (let ff = 0; ff < 6; ff++) {
        const ffPhase = time * 1.2 + ff * 1.1;
        const ffAlpha = 0.4 + Math.sin(ffPhase * 3) * 0.35;
        if (ffAlpha > 0.3) {
          const ffx = cx + Math.sin(ffPhase * 0.7 + ff * 2) * 45 * s;
          const ffy = cy - 10 * s + Math.cos(ffPhase * 0.5 + ff) * 15 * s;
          ctx.globalAlpha = ffAlpha;
          const ffGlow = ctx.createRadialGradient(ffx, ffy, 0, ffx, ffy, 3 * s);
          ffGlow.addColorStop(0, "rgba(200,160,80,0.7)");
          ffGlow.addColorStop(0.4, "rgba(180,140,60,0.2)");
          ffGlow.addColorStop(1, "transparent");
          ctx.fillStyle = ffGlow;
          ctx.beginPath();
          ctx.arc(ffx, ffy, 3 * s, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(220,180,100,0.8)";
          ctx.beginPath();
          ctx.arc(ffx, ffy, 0.8 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      ctx.globalAlpha = 1;

      break;
    }

    case "sarcophagus": {
      const sarcStone = "#c8b080";
      const sarcDark = "#8a7050";
      const sarcGold = "#d4a840";
      const sarcGoldDark = "#a08030";

      // Shadow
      drawDirectionalShadow(
        ctx,
        screenPos.x,
        screenPos.y + 3 * s,
        s,
        16 * s,
        7 * s,
        28 * s,
        0.28
      );

      // Base - isometric box
      const bw = 12 * s,
        bh = 8 * s;
      // Top face
      ctx.fillStyle = sarcStone;
      ctx.beginPath();
      ctx.moveTo(screenPos.x, screenPos.y - bh);
      ctx.lineTo(screenPos.x + bw, screenPos.y - bh + bw * 0.5);
      ctx.lineTo(screenPos.x, screenPos.y - bh + bw);
      ctx.lineTo(screenPos.x - bw, screenPos.y - bh + bw * 0.5);
      ctx.closePath();
      ctx.fill();
      // Left face
      ctx.fillStyle = sarcDark;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - bw, screenPos.y - bh + bw * 0.5);
      ctx.lineTo(screenPos.x, screenPos.y - bh + bw);
      ctx.lineTo(screenPos.x, screenPos.y + bw);
      ctx.lineTo(screenPos.x - bw, screenPos.y + bw * 0.5);
      ctx.closePath();
      ctx.fill();
      // Right face
      ctx.fillStyle = "#a89060";
      ctx.beginPath();
      ctx.moveTo(screenPos.x + bw, screenPos.y - bh + bw * 0.5);
      ctx.lineTo(screenPos.x, screenPos.y - bh + bw);
      ctx.lineTo(screenPos.x, screenPos.y + bw);
      ctx.lineTo(screenPos.x + bw, screenPos.y + bw * 0.5);
      ctx.closePath();
      ctx.fill();

      // Lid - slightly raised trapezoid
      const lh = 28 * s;
      ctx.fillStyle = sarcDark;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - bw * 0.8, screenPos.y - bh + bw * 0.4);
      ctx.lineTo(screenPos.x, screenPos.y - bh);
      ctx.lineTo(screenPos.x, screenPos.y - lh);
      ctx.lineTo(screenPos.x - bw * 0.6, screenPos.y - lh + 3 * s);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = sarcStone;
      ctx.beginPath();
      ctx.moveTo(screenPos.x + bw * 0.8, screenPos.y - bh + bw * 0.4);
      ctx.lineTo(screenPos.x, screenPos.y - bh);
      ctx.lineTo(screenPos.x, screenPos.y - lh);
      ctx.lineTo(screenPos.x + bw * 0.6, screenPos.y - lh + 3 * s);
      ctx.closePath();
      ctx.fill();

      // Face mask on lid
      ctx.fillStyle = sarcGold;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y - lh + 8 * s,
        5 * s,
        6 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      // Eyes
      ctx.fillStyle = "#1a1a1a";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - 2 * s,
        screenPos.y - lh + 7 * s,
        1.2 * s,
        0.8 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.ellipse(
        screenPos.x + 2 * s,
        screenPos.y - lh + 7 * s,
        1.2 * s,
        0.8 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Gold trim bands
      ctx.fillStyle = sarcGoldDark;
      ctx.fillRect(
        screenPos.x - bw * 0.7,
        screenPos.y - 18 * s,
        bw * 1.4,
        2 * s
      );
      ctx.fillRect(
        screenPos.x - bw * 0.6,
        screenPos.y - 12 * s,
        bw * 1.2,
        1.5 * s
      );

      // Hieroglyph details on right face
      ctx.fillStyle = "rgba(90,70,40,0.4)";
      for (let g = 0; g < 3; g++) {
        const gy = screenPos.y + 2 * s + g * 4 * s;
        ctx.fillRect(screenPos.x + 3 * s, gy, 6 * s, 1.5 * s);
      }
      break;
    }

    case "cobra_statue": {
      const cx = screenPos.x;
      const cy = screenPos.y;
      const csLight = "#b8a880";
      const csMid = "#a09070";
      const csDark = "#706040";
      const csDeep = "#504828";
      const csGold = "#d4a830";
      const csGoldDark = "#a08020";

      // Ground shadow
      drawDirectionalShadow(ctx, cx, cy + 3 * s, s, 15 * s, 7 * s, 35 * s, 0.3);

      // === 3D ISOMETRIC PEDESTAL (2-tier) ===
      const pedW1 = 14 * s;
      const pedH1 = 5 * s;
      // Bottom tier top
      ctx.fillStyle = csLight;
      ctx.beginPath();
      ctx.moveTo(cx - pedW1, cy - pedH1);
      ctx.lineTo(cx, cy - pedH1 - pedW1 * 0.5);
      ctx.lineTo(cx + pedW1, cy - pedH1);
      ctx.lineTo(cx, cy - pedH1 + pedW1 * 0.5);
      ctx.closePath();
      ctx.fill();
      // Left face
      ctx.fillStyle = csMid;
      ctx.beginPath();
      ctx.moveTo(cx - pedW1, cy - pedH1);
      ctx.lineTo(cx - pedW1, cy);
      ctx.lineTo(cx, cy + pedW1 * 0.5);
      ctx.lineTo(cx, cy - pedH1 + pedW1 * 0.5);
      ctx.closePath();
      ctx.fill();
      // Right face
      ctx.fillStyle = csDark;
      ctx.beginPath();
      ctx.moveTo(cx + pedW1, cy - pedH1);
      ctx.lineTo(cx + pedW1, cy);
      ctx.lineTo(cx, cy + pedW1 * 0.5);
      ctx.lineTo(cx, cy - pedH1 + pedW1 * 0.5);
      ctx.closePath();
      ctx.fill();
      // Top tier
      const ped2Y = cy - pedH1;
      const pedW2 = 10 * s;
      const pedH2 = 3 * s;
      ctx.fillStyle = csLight;
      ctx.beginPath();
      ctx.moveTo(cx - pedW2, ped2Y - pedH2);
      ctx.lineTo(cx, ped2Y - pedH2 - pedW2 * 0.5);
      ctx.lineTo(cx + pedW2, ped2Y - pedH2);
      ctx.lineTo(cx, ped2Y - pedH2 + pedW2 * 0.5);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = csMid;
      ctx.beginPath();
      ctx.moveTo(cx - pedW2, ped2Y - pedH2);
      ctx.lineTo(cx - pedW2, ped2Y);
      ctx.lineTo(cx, ped2Y + pedW2 * 0.5);
      ctx.lineTo(cx, ped2Y - pedH2 + pedW2 * 0.5);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = csDark;
      ctx.beginPath();
      ctx.moveTo(cx + pedW2, ped2Y - pedH2);
      ctx.lineTo(cx + pedW2, ped2Y);
      ctx.lineTo(cx, ped2Y + pedW2 * 0.5);
      ctx.lineTo(cx, ped2Y - pedH2 + pedW2 * 0.5);
      ctx.closePath();
      ctx.fill();
      // Gold decorative band on pedestal
      ctx.strokeStyle = csGoldDark;
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.moveTo(cx - pedW2 + 1 * s, ped2Y - pedH2 * 0.5);
      ctx.lineTo(cx, ped2Y - pedH2 * 0.5 + pedW2 * 0.45);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(cx + pedW2 - 1 * s, ped2Y - pedH2 * 0.5);
      ctx.lineTo(cx, ped2Y - pedH2 * 0.5 + pedW2 * 0.45);
      ctx.stroke();

      // === COBRA BODY COILS (3D stacked) ===
      const coilBase = ped2Y - pedH2;
      // Bottom coil
      ctx.fillStyle = csMid;
      ctx.beginPath();
      ctx.ellipse(cx, coilBase - 2 * s, 9 * s, 6 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = csDark;
      ctx.beginPath();
      ctx.ellipse(
        cx + 1 * s,
        coilBase - 1 * s,
        7 * s,
        4 * s,
        0.2,
        0,
        Math.PI * 2
      );
      ctx.fill();
      // Top coil (smaller, offset)
      ctx.fillStyle = csLight;
      ctx.beginPath();
      ctx.ellipse(
        cx - 1 * s,
        coilBase - 7 * s,
        7 * s,
        4.5 * s,
        -0.1,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.fillStyle = csMid;
      ctx.beginPath();
      ctx.ellipse(cx, coilBase - 6 * s, 5 * s, 3 * s, 0.1, 0, Math.PI * 2);
      ctx.fill();
      // Scale pattern on coils
      ctx.strokeStyle = csDeep;
      ctx.lineWidth = 0.5 * s;
      for (let row = 0; row < 2; row++) {
        const scy = coilBase - 1 * s - row * 5.5 * s;
        const sr = row === 0 ? 7 : 5;
        for (let sc = 0; sc < 8; sc++) {
          const sa = (sc / 8) * Math.PI * 2;
          const ssx = cx + Math.cos(sa) * sr * s * 0.7;
          const ssy = scy + Math.sin(sa) * sr * s * 0.35;
          ctx.beginPath();
          ctx.arc(ssx, ssy, 1.5 * s, 0, Math.PI, true);
          ctx.stroke();
        }
      }

      // === COBRA NECK (rising, S-curve) ===
      // Back of neck (darker, drawn first)
      ctx.fillStyle = csDark;
      ctx.beginPath();
      ctx.moveTo(cx + 4 * s, coilBase - 9 * s);
      ctx.bezierCurveTo(
        cx + 6 * s,
        coilBase - 22 * s,
        cx + 3 * s,
        coilBase - 35 * s,
        cx + 1 * s,
        coilBase - 44 * s
      );
      ctx.lineTo(cx - 1 * s, coilBase - 44 * s);
      ctx.bezierCurveTo(
        cx - 3 * s,
        coilBase - 35 * s,
        cx - 4 * s,
        coilBase - 22 * s,
        cx - 4 * s,
        coilBase - 9 * s
      );
      ctx.closePath();
      ctx.fill();
      // Front of neck (lighter)
      ctx.fillStyle = csMid;
      ctx.beginPath();
      ctx.moveTo(cx + 3 * s, coilBase - 9 * s);
      ctx.bezierCurveTo(
        cx + 5 * s,
        coilBase - 22 * s,
        cx + 2 * s,
        coilBase - 35 * s,
        cx,
        coilBase - 44 * s
      );
      ctx.bezierCurveTo(
        cx - 2 * s,
        coilBase - 35 * s,
        cx - 3 * s,
        coilBase - 22 * s,
        cx - 3 * s,
        coilBase - 9 * s
      );
      ctx.closePath();
      ctx.fill();
      // Neck belly scales (lighter underside)
      ctx.fillStyle = csLight;
      ctx.beginPath();
      ctx.moveTo(cx + 1.5 * s, coilBase - 10 * s);
      ctx.bezierCurveTo(
        cx + 2.5 * s,
        coilBase - 22 * s,
        cx + 1 * s,
        coilBase - 34 * s,
        cx,
        coilBase - 42 * s
      );
      ctx.bezierCurveTo(
        cx - 1 * s,
        coilBase - 34 * s,
        cx - 1.5 * s,
        coilBase - 22 * s,
        cx - 1.5 * s,
        coilBase - 10 * s
      );
      ctx.closePath();
      ctx.fill();
      // Horizontal scale lines on neck
      ctx.strokeStyle = csDeep;
      ctx.lineWidth = 0.4 * s;
      for (let nl = 0; nl < 7; nl++) {
        const ny2 = coilBase - 12 * s - nl * 4.5 * s;
        const nw = 3 - nl * 0.2;
        ctx.beginPath();
        ctx.moveTo(cx - nw * s, ny2);
        ctx.lineTo(cx + nw * s, ny2);
        ctx.stroke();
      }

      // === COBRA HOOD (spread wide, detailed) ===
      const hoodTop = coilBase - 44 * s;
      // Hood back (darker)
      ctx.fillStyle = csDark;
      ctx.beginPath();
      ctx.moveTo(cx, hoodTop);
      ctx.bezierCurveTo(
        cx - 8 * s,
        hoodTop + 2 * s,
        cx - 15 * s,
        hoodTop + 6 * s,
        cx - 13 * s,
        hoodTop + 18 * s
      );
      ctx.quadraticCurveTo(cx - 8 * s, hoodTop + 22 * s, cx, hoodTop + 16 * s);
      ctx.quadraticCurveTo(
        cx + 8 * s,
        hoodTop + 22 * s,
        cx + 13 * s,
        hoodTop + 18 * s
      );
      ctx.bezierCurveTo(
        cx + 15 * s,
        hoodTop + 6 * s,
        cx + 8 * s,
        hoodTop + 2 * s,
        cx,
        hoodTop
      );
      ctx.closePath();
      ctx.fill();
      // Hood front (lighter, slightly smaller)
      ctx.fillStyle = csMid;
      ctx.beginPath();
      ctx.moveTo(cx, hoodTop + 1 * s);
      ctx.bezierCurveTo(
        cx - 6 * s,
        hoodTop + 3 * s,
        cx - 12 * s,
        hoodTop + 7 * s,
        cx - 11 * s,
        hoodTop + 17 * s
      );
      ctx.quadraticCurveTo(cx - 6 * s, hoodTop + 20 * s, cx, hoodTop + 15 * s);
      ctx.quadraticCurveTo(
        cx + 6 * s,
        hoodTop + 20 * s,
        cx + 11 * s,
        hoodTop + 17 * s
      );
      ctx.bezierCurveTo(
        cx + 12 * s,
        hoodTop + 7 * s,
        cx + 6 * s,
        hoodTop + 3 * s,
        cx,
        hoodTop + 1 * s
      );
      ctx.closePath();
      ctx.fill();

      // Hood markings - gold diamond pattern
      ctx.fillStyle = csGold;
      ctx.beginPath();
      ctx.moveTo(cx, hoodTop + 4 * s);
      ctx.lineTo(cx - 5 * s, hoodTop + 12 * s);
      ctx.lineTo(cx, hoodTop + 16 * s);
      ctx.lineTo(cx + 5 * s, hoodTop + 12 * s);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = csGoldDark;
      ctx.beginPath();
      ctx.moveTo(cx, hoodTop + 6 * s);
      ctx.lineTo(cx - 3 * s, hoodTop + 12 * s);
      ctx.lineTo(cx, hoodTop + 14 * s);
      ctx.lineTo(cx + 3 * s, hoodTop + 12 * s);
      ctx.closePath();
      ctx.fill();
      // Smaller gold accents on hood sides
      ctx.fillStyle = csGold;
      for (const side of [-1, 1]) {
        ctx.beginPath();
        ctx.moveTo(cx + side * 8 * s, hoodTop + 8 * s);
        ctx.lineTo(cx + side * 6 * s, hoodTop + 12 * s);
        ctx.lineTo(cx + side * 8 * s, hoodTop + 14 * s);
        ctx.lineTo(cx + side * 10 * s, hoodTop + 11 * s);
        ctx.closePath();
        ctx.fill();
      }
      // Hood edge highlight
      ctx.strokeStyle = csLight;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(cx - 13 * s, hoodTop + 18 * s);
      ctx.bezierCurveTo(
        cx - 15 * s,
        hoodTop + 6 * s,
        cx - 8 * s,
        hoodTop + 2 * s,
        cx,
        hoodTop
      );
      ctx.stroke();

      // === COBRA HEAD (on top of hood) ===
      const headY2 = hoodTop + 2 * s;
      // Head shape
      ctx.fillStyle = csLight;
      ctx.beginPath();
      ctx.ellipse(cx, headY2, 4 * s, 3.5 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = csMid;
      ctx.beginPath();
      ctx.ellipse(
        cx + 0.5 * s,
        headY2 + 0.5 * s,
        3 * s,
        2.5 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Snout
      ctx.fillStyle = csLight;
      ctx.beginPath();
      ctx.ellipse(cx, headY2 + 3 * s, 2.5 * s, 1.5 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      // Nostrils
      ctx.fillStyle = csDeep;
      ctx.beginPath();
      ctx.arc(cx - 1 * s, headY2 + 3.5 * s, 0.5 * s, 0, Math.PI * 2);
      ctx.arc(cx + 1 * s, headY2 + 3.5 * s, 0.5 * s, 0, Math.PI * 2);
      ctx.fill();

      // Forked tongue (animated)
      const tongueExt = (Math.sin(decorTime * 4) + 1) * 0.5;
      if (tongueExt > 0.3) {
        ctx.strokeStyle = "#c04040";
        ctx.lineWidth = 0.7 * s;
        const tLen = tongueExt * 6 * s;
        ctx.beginPath();
        ctx.moveTo(cx, headY2 + 4.5 * s);
        ctx.lineTo(cx, headY2 + 4.5 * s + tLen);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(cx, headY2 + 4.5 * s + tLen);
        ctx.lineTo(cx - 1.5 * s, headY2 + 5 * s + tLen);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(cx, headY2 + 4.5 * s + tLen);
        ctx.lineTo(cx + 1.5 * s, headY2 + 5 * s + tLen);
        ctx.stroke();
      }

      // Eyes (jewel-like with glow)
      const cobraEyeGlow = 0.5 + Math.sin(decorTime * 3) * 0.3;
      // Eye socket shadow
      ctx.fillStyle = csDeep;
      ctx.beginPath();
      ctx.ellipse(
        cx - 2.5 * s,
        headY2 - 0.5 * s,
        2 * s,
        1.8 * s,
        -0.15,
        0,
        Math.PI * 2
      );
      ctx.ellipse(
        cx + 2.5 * s,
        headY2 - 0.5 * s,
        2 * s,
        1.8 * s,
        0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();
      // Outer glow
      ctx.fillStyle = `rgba(229,57,53,${cobraEyeGlow * 0.3})`;
      ctx.beginPath();
      ctx.arc(cx - 2.5 * s, headY2 - 0.5 * s, 3.5 * s, 0, Math.PI * 2);
      ctx.arc(cx + 2.5 * s, headY2 - 0.5 * s, 3.5 * s, 0, Math.PI * 2);
      ctx.fill();
      // Ruby eyes
      const eyeGrad = ctx.createRadialGradient(
        cx - 3 * s,
        headY2 - 1 * s,
        0,
        cx - 2.5 * s,
        headY2 - 0.5 * s,
        2 * s
      );
      eyeGrad.addColorStop(0, "#ff6060");
      eyeGrad.addColorStop(0.4, "#e53935");
      eyeGrad.addColorStop(1, "#8b1a1a");
      ctx.fillStyle = eyeGrad;
      ctx.beginPath();
      ctx.ellipse(
        cx - 2.5 * s,
        headY2 - 0.5 * s,
        1.5 * s,
        1.3 * s,
        -0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();
      const eyeGrad2 = ctx.createRadialGradient(
        cx + 2 * s,
        headY2 - 1 * s,
        0,
        cx + 2.5 * s,
        headY2 - 0.5 * s,
        2 * s
      );
      eyeGrad2.addColorStop(0, "#ff6060");
      eyeGrad2.addColorStop(0.4, "#e53935");
      eyeGrad2.addColorStop(1, "#8b1a1a");
      ctx.fillStyle = eyeGrad2;
      ctx.beginPath();
      ctx.ellipse(
        cx + 2.5 * s,
        headY2 - 0.5 * s,
        1.5 * s,
        1.3 * s,
        0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();
      // Eye specular highlight
      ctx.fillStyle = `rgba(255,200,200,${cobraEyeGlow * 0.7})`;
      ctx.beginPath();
      ctx.arc(cx - 3 * s, headY2 - 1 * s, 0.5 * s, 0, Math.PI * 2);
      ctx.arc(cx + 2 * s, headY2 - 1 * s, 0.5 * s, 0, Math.PI * 2);
      ctx.fill();

      // Gold crown/diadem on head
      ctx.fillStyle = csGold;
      ctx.beginPath();
      ctx.moveTo(cx - 3 * s, headY2 - 2 * s);
      ctx.lineTo(cx - 2 * s, headY2 - 5 * s);
      ctx.lineTo(cx, headY2 - 3.5 * s);
      ctx.lineTo(cx + 2 * s, headY2 - 5 * s);
      ctx.lineTo(cx + 3 * s, headY2 - 2 * s);
      ctx.closePath();
      ctx.fill();
      // Gem in crown
      ctx.fillStyle = "#2196f3";
      ctx.beginPath();
      ctx.arc(cx, headY2 - 3.8 * s, 0.8 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(100,200,255,0.5)";
      ctx.beginPath();
      ctx.arc(cx - 0.2 * s, headY2 - 4 * s, 0.3 * s, 0, Math.PI * 2);
      ctx.fill();

      // Sand/dust particles drifting
      for (let p = 0; p < 3; p++) {
        const pp = (decorTime * 0.5 + p * 1.1) % 2.5;
        const pa = Math.sin((pp / 2.5) * Math.PI) * 0.2;
        const px = cx + Math.sin(decorTime * 0.3 + p * 2) * 14 * s;
        const py = cy - 10 * s - pp * 12 * s;
        ctx.fillStyle = `rgba(200,180,130,${pa})`;
        ctx.beginPath();
        ctx.arc(px, py, 1 * s, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }

    case "hieroglyph_wall": {
      const hx = screenPos.x;
      const hy = screenPos.y;
      const wallLen = 38 * s;
      const depth = 5 * s;
      const hwSeed = decorX * 61 + decorY * 97;
      const hwHash = (n: number) => {
        const v = Math.sin(hwSeed * 0.1 + n * 2.891) * 43_758.5453;
        return v - Math.floor(v);
      };

      const x1 = hx - wallLen * 0.5;
      const y1 = hy + wallLen * 0.25;
      const x2 = hx + wallLen * 0.5;
      const y2 = hy - wallLen * 0.25;

      // Jagged broken-top profile (tall left, crumbles toward right)
      const hwProfile = [34, 38, 36, 34, 30, 24, 16, 8, 4, 0].map((h) => h * s);

      // Ground shadow
      ctx.fillStyle = "rgba(0,0,0,0.18)";
      ctx.beginPath();
      ctx.ellipse(hx, hy + 4 * s, 24 * s, 8 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      const sandColors = {
        front: "#c8b888",
        side: "#b0a070",
        top: "#d0c098",
        mortar: "rgba(80,68,40,0.25)",
        inner: "#a89868",
      };

      drawIsoJaggedWall(
        ctx,
        x1,
        y1,
        x2,
        y2,
        -depth,
        -depth * 0.5,
        hwProfile,
        sandColors,
        s,
        9,
        7
      );

      // Carved hieroglyph symbols on front face (clipped to profile)
      const maxH = Math.max(...hwProfile);
      const drawHieroglyph = (
        gx: number,
        gy: number,
        type: number,
        sz: number
      ) => {
        ctx.lineCap = "round";
        ctx.lineJoin = "round";

        const drawSymbol = (ox: number, oy: number) => {
          const sx = gx + ox;
          const sy = gy + oy;
          if (type === 0) {
            ctx.beginPath();
            ctx.ellipse(sx, sy - 2.2 * sz, 1.6 * sz, 2 * sz, 0, 0, Math.PI * 2);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx, sy - 0.2 * sz);
            ctx.lineTo(sx, sy + 3.5 * sz);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx - 2 * sz, sy + 1 * sz);
            ctx.lineTo(sx + 2 * sz, sy + 1 * sz);
            ctx.stroke();
          } else if (type === 1) {
            ctx.beginPath();
            ctx.ellipse(sx, sy, 2.8 * sz, 1.6 * sz, 0, 0, Math.PI * 2);
            ctx.stroke();
            ctx.beginPath();
            ctx.arc(sx, sy, 0.9 * sz, 0, Math.PI * 2);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx + 2.8 * sz, sy);
            ctx.quadraticCurveTo(
              sx + 3.5 * sz,
              sy + 0.5 * sz,
              sx + 3 * sz,
              sy + 2.5 * sz
            );
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx - 0.5 * sz, sy + 1.6 * sz);
            ctx.quadraticCurveTo(
              sx - 1.5 * sz,
              sy + 3 * sz,
              sx - 2.5 * sz,
              sy + 2.5 * sz
            );
            ctx.stroke();
          } else if (type === 2) {
            ctx.beginPath();
            ctx.arc(sx, sy - 2 * sz, 1.3 * sz, 0, Math.PI * 2);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx + 1.3 * sz, sy - 2 * sz);
            ctx.lineTo(sx + 3 * sz, sy - 1.5 * sz);
            ctx.lineTo(sx + 1.3 * sz, sy - 1.5 * sz);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx - 0.5 * sz, sy - 0.7 * sz);
            ctx.quadraticCurveTo(sx + 1 * sz, sy + 1 * sz, sx, sy + 3 * sz);
            ctx.moveTo(sx + 0.5 * sz, sy - 0.7 * sz);
            ctx.quadraticCurveTo(
              sx + 2.2 * sz,
              sy + 0.5 * sz,
              sx + 1 * sz,
              sy + 3 * sz
            );
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx, sy + 3 * sz);
            ctx.lineTo(sx - 0.5 * sz, sy + 4.5 * sz);
            ctx.moveTo(sx + 1 * sz, sy + 3 * sz);
            ctx.lineTo(sx + 1.5 * sz, sy + 4.5 * sz);
            ctx.stroke();
          } else if (type === 3) {
            ctx.beginPath();
            ctx.ellipse(sx, sy, 2 * sz, 2.8 * sz, 0, 0, Math.PI * 2);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx - 2 * sz, sy);
            ctx.lineTo(sx + 2 * sz, sy);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx, sy - 2.8 * sz);
            ctx.lineTo(sx, sy + 0.5 * sz);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx - 1 * sz, sy - 2.8 * sz);
            ctx.quadraticCurveTo(
              sx - 2.5 * sz,
              sy - 4 * sz,
              sx - 0.5 * sz,
              sy - 4.2 * sz
            );
            ctx.moveTo(sx + 1 * sz, sy - 2.8 * sz);
            ctx.quadraticCurveTo(
              sx + 2.5 * sz,
              sy - 4 * sz,
              sx + 0.5 * sz,
              sy - 4.2 * sz
            );
            ctx.stroke();
          } else if (type === 4) {
            ctx.beginPath();
            ctx.moveTo(sx, sy + 3.5 * sz);
            ctx.lineTo(sx, sy - 3.5 * sz);
            ctx.stroke();
            for (let ri = 0; ri < 4; ri++) {
              const ry = sy - 3 * sz + ri * 1.8 * sz;
              ctx.beginPath();
              ctx.moveTo(sx, ry);
              ctx.quadraticCurveTo(
                sx - 1.8 * sz,
                ry + 0.3 * sz,
                sx - 2 * sz,
                ry + 1 * sz
              );
              ctx.stroke();
              ctx.beginPath();
              ctx.moveTo(sx, ry);
              ctx.quadraticCurveTo(
                sx + 1.8 * sz,
                ry + 0.3 * sz,
                sx + 2 * sz,
                ry + 1 * sz
              );
              ctx.stroke();
            }
          } else {
            ctx.beginPath();
            ctx.moveTo(sx, sy + 3 * sz);
            ctx.quadraticCurveTo(sx - 1 * sz, sy + 1 * sz, sx, sy - 1 * sz);
            ctx.quadraticCurveTo(sx + 0.5 * sz, sy - 2.5 * sz, sx, sy - 3 * sz);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(sx - 2 * sz, sy - 2 * sz);
            ctx.quadraticCurveTo(sx, sy - 4 * sz, sx + 2 * sz, sy - 2 * sz);
            ctx.stroke();
            ctx.beginPath();
            ctx.arc(sx, sy - 2.8 * sz, 0.6 * sz, 0, Math.PI * 2);
            ctx.stroke();
          }
        };

        ctx.strokeStyle = "rgba(220,210,180,0.3)";
        ctx.lineWidth = 0.8 * s;
        drawSymbol(-0.3 * s, -0.3 * s);

        ctx.strokeStyle = "rgba(85,68,35,0.55)";
        ctx.lineWidth = 1 * s;
        drawSymbol(0, 0);

        ctx.strokeStyle = "rgba(50,40,20,0.2)";
        ctx.lineWidth = 0.6 * s;
        drawSymbol(0.4 * s, 0.4 * s);
      };

      // Clip glyphs + register lines to the front face jagged profile
      const hwN = hwProfile.length;
      const hwDx = x2 - x1;
      const hwDy = y2 - y1;
      ctx.save();
      ctx.beginPath();
      ctx.moveTo(x1, y1);
      ctx.lineTo(x2, y2);
      for (let pi = hwN - 1; pi >= 0; pi--) {
        const pt = pi / (hwN - 1);
        ctx.lineTo(x1 + pt * hwDx, y1 + pt * hwDy - hwProfile[pi]);
      }
      ctx.closePath();
      ctx.clip();

      const glyphDefs = [
        { row: 0.25, t: 0.12, type: 0 },
        { row: 0.25, t: 0.32, type: 1 },
        { row: 0.25, t: 0.52, type: 2 },
        { row: 0.55, t: 0.12, type: 3 },
        { row: 0.55, t: 0.32, type: 5 },
        { row: 0.55, t: 0.52, type: 4 },
        { row: 0.8, t: 0.12, type: 4 },
        { row: 0.8, t: 0.32, type: 0 },
      ];

      // Register lines
      ctx.strokeStyle = "rgba(80,65,35,0.12)";
      ctx.lineWidth = 0.5 * s;
      for (const rowY of [0.35, 0.65]) {
        ctx.beginPath();
        ctx.moveTo(x1, y1 - rowY * maxH);
        ctx.lineTo(x2, y2 - rowY * maxH);
        ctx.stroke();
      }

      glyphDefs.forEach((g, gi) => {
        const gx = x1 + g.t * hwDx;
        const gy = y1 + g.t * hwDy - g.row * maxH;
        const gType = (g.type + Math.floor(hwHash(gi + 30) * 2)) % 6;
        drawHieroglyph(gx, gy, gType, 1.1 * s);
      });

      ctx.restore();

      // Rubble pile at the crumbled right side
      const rubblePalette = [
        { left: "#8a7a60", right: "#a09070", top: "#c0b090" },
        { left: "#7a6a50", right: "#908060", top: "#b0a080" },
        { left: "#9a8a68", right: "#b8a878", top: "#d0c098" },
      ];
      drawRubblePile(
        ctx,
        hx + 12 * s,
        hy + 1 * s,
        [
          { dx: 4, dy: 2, w: 3.5, h: 3 },
          { dx: 10, dy: 0, w: 2.5, h: 2.5 },
          { dx: -2, dy: 4, w: 2, h: 2 },
          { dx: 7, dy: 3, w: 2, h: 1.5 },
          { dx: 14, dy: -1, w: 1.8, h: 2 },
          { dx: 1, dy: 1, w: 1.5, h: 1.2 },
          { dx: 12, dy: 2, w: 1.5, h: 1.8 },
        ],
        s,
        rubblePalette
      );
      break;
    }

    case "pottery": {
      const ptX = screenPos.x;
      const ptY = screenPos.y;
      const ptSeed = decorX * 61 + decorY * 97;
      const ptHash = (n: number) => {
        const h = Math.sin(ptSeed * 0.1 + n * 2.891) * 43_758.5453;
        return h - Math.floor(h);
      };
      const ptV = variant % 4;

      // Color palettes for pots
      const potPalettes = [
        {
          base: "#b07040",
          dark: "#7a4828",
          light: "#d09060",
          pattern: "#5a3018",
          rim: "#c8a070",
        },
        {
          base: "#a06038",
          dark: "#6a3820",
          light: "#c88050",
          pattern: "#4a2810",
          rim: "#b89060",
        },
        {
          base: "#c08858",
          dark: "#8a5a38",
          light: "#d8a878",
          pattern: "#6a4028",
          rim: "#d0b880",
        },
        {
          base: "#8a5838",
          dark: "#5a3020",
          light: "#b08050",
          pattern: "#3a1810",
          rim: "#a87848",
        },
      ];

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        ptX,
        ptY + 2 * s,
        s,
        15 * s,
        6 * s,
        22 * s,
        0.22
      );

      // Scattered sand/dirt under pots
      ctx.fillStyle = "rgba(180,150,100,0.12)";
      drawOrganicWaterShape(
        ctx,
        ptX,
        ptY + 3 * s,
        18 * s,
        8 * s,
        ptSeed + 50,
        0.16
      );
      ctx.fill();

      // Helper: draw a single organic pot
      const drawPot = (
        px: number,
        py: number,
        width: number,
        height: number,
        bulge: number,
        neckRatio: number,
        pal: (typeof potPalettes)[0],
        broken: boolean,
        tilt: number,
        hasSand: boolean
      ) => {
        ctx.save();
        ctx.translate(px, py);
        ctx.rotate(tilt);

        const w = width * s;
        const h = height * s;
        const neckW = w * neckRatio;
        const bulgeW = w * bulge;
        const blobSeed = px * 0.1 + py * 0.2;

        if (broken) {
          // Broken pot — jagged top half with organic shape for base
          const breakH = h * (0.3 + ptHash(77) * 0.3);

          // Base body using organic blob
          const potBodyG = ctx.createLinearGradient(-bulgeW, 0, bulgeW, 0);
          potBodyG.addColorStop(0, pal.dark);
          potBodyG.addColorStop(0.3, pal.base);
          potBodyG.addColorStop(0.5, pal.light);
          potBodyG.addColorStop(0.7, pal.base);
          potBodyG.addColorStop(1, pal.dark);
          ctx.fillStyle = potBodyG;

          drawOrganicWaterShape(
            ctx,
            0,
            -breakH * 0.5,
            bulgeW,
            breakH * 0.5,
            blobSeed,
            0.08
          );
          ctx.fill();

          // Jagged break edge at top
          ctx.strokeStyle = pal.dark;
          ctx.lineWidth = 0.6 * s;
          ctx.beginPath();
          const jagPts = 6;
          for (let j = 0; j <= jagPts; j++) {
            const jt = j / jagPts;
            const jx = -bulgeW * 0.8 + jt * bulgeW * 1.6;
            const jy = -breakH + Math.sin(blobSeed + j * 2.3) * 2 * s;
            if (j === 0) {
              ctx.moveTo(jx, jy);
            } else {
              ctx.lineTo(jx, jy);
            }
          }
          ctx.stroke();

          // Inner shadow at break
          ctx.fillStyle = "rgba(40,20,10,0.35)";
          ctx.beginPath();
          ctx.ellipse(
            0,
            -breakH + 1 * s,
            bulgeW * 0.6,
            2 * s,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();
        } else {
          // Full intact pot with organic body shape

          // Pot body — using stacked organic slices for a natural hand-thrown look
          const bodyG = ctx.createLinearGradient(-bulgeW, 0, bulgeW, 0);
          bodyG.addColorStop(0, pal.dark);
          bodyG.addColorStop(0.25, pal.base);
          bodyG.addColorStop(0.45, pal.light);
          bodyG.addColorStop(0.55, pal.light);
          bodyG.addColorStop(0.75, pal.base);
          bodyG.addColorStop(1, pal.dark);

          // Draw pot profile as a series of organic slices
          ctx.fillStyle = bodyG;
          ctx.beginPath();
          const slices = 16;
          // Right side going up
          for (let sl = 0; sl <= slices; sl++) {
            const t = sl / slices;
            const slY = -t * h;
            // Profile: wide at belly, narrow at base and neck
            const belly = Math.sin(t * Math.PI) * (bulge - 1) + 1;
            const neckTaper =
              t > 0.7 ? 1 - ((t - 0.7) / 0.3) * (1 - neckRatio) : 1;
            const baseW = 0.6 + t * 0.4;
            const slW = w * belly * neckTaper * baseW;
            const wobble = Math.sin(blobSeed + sl * 1.7) * 0.5 * s;
            if (sl === 0) {
              ctx.moveTo(slW + wobble, slY);
            } else {
              ctx.lineTo(slW + wobble, slY);
            }
          }
          // Left side going down
          for (let sl = slices; sl >= 0; sl--) {
            const t = sl / slices;
            const slY = -t * h;
            const belly = Math.sin(t * Math.PI) * (bulge - 1) + 1;
            const neckTaper =
              t > 0.7 ? 1 - ((t - 0.7) / 0.3) * (1 - neckRatio) : 1;
            const baseW = 0.6 + t * 0.4;
            const slW = w * belly * neckTaper * baseW;
            const wobble = Math.sin(blobSeed + sl * 1.7 + 3) * 0.5 * s;
            ctx.lineTo(-slW + wobble, slY);
          }
          ctx.closePath();
          ctx.fill();

          // Throwing rings (horizontal lines from wheel)
          ctx.strokeStyle = "rgba(0,0,0,0.08)";
          ctx.lineWidth = 0.3 * s;
          for (let ring = 2; ring < 8; ring++) {
            const rt = ring / 10;
            const ry = -rt * h;
            const belly = Math.sin(rt * Math.PI) * (bulge - 1) + 1;
            const neckTaper =
              rt > 0.7 ? 1 - ((rt - 0.7) / 0.3) * (1 - neckRatio) : 1;
            const baseW = 0.6 + rt * 0.4;
            const rW = w * belly * neckTaper * baseW;
            ctx.beginPath();
            ctx.ellipse(0, ry, rW, rW * 0.12, 0, 0, Math.PI * 2);
            ctx.stroke();
          }

          // Decorative pattern band at widest point
          const bandT = 0.5;
          const bandY = -bandT * h;
          const bandBelly = Math.sin(bandT * Math.PI) * (bulge - 1) + 1;
          const bandW = w * bandBelly * (0.6 + bandT * 0.4);
          ctx.strokeStyle = pal.pattern;
          ctx.lineWidth = 1.2 * s;
          ctx.beginPath();
          ctx.ellipse(0, bandY, bandW, bandW * 0.12, 0, 0, Math.PI);
          ctx.stroke();

          // Zigzag pattern within the band
          ctx.strokeStyle = pal.pattern;
          ctx.lineWidth = 0.5 * s;
          ctx.beginPath();
          const zigCount = 8;
          for (let z = 0; z <= zigCount; z++) {
            const zt = z / zigCount;
            const zAng = (zt - 0.5) * Math.PI;
            const zx = Math.sin(zAng) * bandW;
            const zy =
              bandY +
              Math.cos(zAng) * bandW * 0.12 +
              (z % 2 === 0 ? -1.5 : 1.5) * s;
            if (z === 0) {
              ctx.moveTo(zx, zy);
            } else {
              ctx.lineTo(zx, zy);
            }
          }
          ctx.stroke();

          // Rim — 3D ellipse with depth
          const rimW = neckW * (0.6 + 1 * 0.4);
          // Rim outer
          ctx.fillStyle = pal.rim;
          ctx.beginPath();
          ctx.ellipse(
            0,
            -h,
            rimW + 1 * s,
            (rimW + 1 * s) * 0.3,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();
          // Rim inner (dark opening)
          ctx.fillStyle = "rgba(30,15,5,0.6)";
          ctx.beginPath();
          ctx.ellipse(0, -h, rimW * 0.7, rimW * 0.2, 0, 0, Math.PI * 2);
          ctx.fill();
          // Rim highlight
          ctx.strokeStyle = "rgba(255,255,255,0.15)";
          ctx.lineWidth = 0.4 * s;
          ctx.beginPath();
          ctx.ellipse(
            0,
            -h,
            rimW + 0.5 * s,
            (rimW + 0.5 * s) * 0.3,
            0,
            Math.PI * 1.1,
            Math.PI * 1.8
          );
          ctx.stroke();

          // Specular highlight on body
          ctx.fillStyle = "rgba(255,255,255,0.1)";
          ctx.beginPath();
          ctx.ellipse(
            -w * 0.3,
            -h * 0.55,
            w * 0.2,
            h * 0.25,
            -0.2,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }

        if (hasSand) {
          // Sand spilling from pot or around base
          ctx.fillStyle = "rgba(200,170,120,0.25)";
          drawOrganicWaterShape(
            ctx,
            2 * s,
            1 * s,
            4 * s,
            2 * s,
            blobSeed + 40,
            0.2
          );
          ctx.fill();
        }

        ctx.restore();
      };

      // Main large pot
      const pal0 = potPalettes[ptV];
      drawPot(ptX, ptY, 7, 20, 1.35, 0.6, pal0, false, 0, false);

      // Second pot — smaller, different palette, slightly offset
      const pal1 = potPalettes[(ptV + 1) % potPalettes.length];
      drawPot(
        ptX - 13 * s,
        ptY + 2 * s,
        4.5,
        12,
        1.25,
        0.55,
        pal1,
        ptHash(10) < 0.3,
        ptHash(11) * 0.15 - 0.07,
        false
      );

      // Third element — broken shards with organic shapes
      const shardPal = potPalettes[(ptV + 2) % potPalettes.length];
      const shardX = ptX + 12 * s;
      const shardY = ptY + 2 * s;

      // Shard scatter using organic shapes
      for (let sh = 0; sh < 3; sh++) {
        const sha = ptHash(20 + sh * 3) * Math.PI * 2;
        const shr = 2 + ptHash(21 + sh * 3) * 3;
        const shx = shardX + Math.cos(sha) * shr * s;
        const shy = shardY + Math.sin(sha) * shr * 0.4 * s;
        const shW = (2 + ptHash(22 + sh * 3) * 2) * s;
        const shH = (1 + ptHash(22 + sh * 3)) * s;

        ctx.fillStyle = sh % 2 === 0 ? shardPal.base : shardPal.dark;
        drawOrganicWaterShape(ctx, shx, shy, shW, shH, ptSeed + sh * 17, 0.25);
        ctx.fill();

        // Inner face (visible on broken edge)
        ctx.fillStyle = shardPal.light;
        drawOrganicWaterShape(
          ctx,
          shx + 0.3 * s,
          shy - 0.3 * s,
          shW * 0.6,
          shH * 0.6,
          ptSeed + sh * 17 + 5,
          0.2
        );
        ctx.fill();
      }

      // Optional: small intact pot near shards
      if (ptHash(40) > 0.4) {
        const pal2 = potPalettes[(ptV + 3) % potPalettes.length];
        drawPot(
          ptX + 10 * s,
          ptY + 1 * s,
          3,
          8,
          1.4,
          0.5,
          pal2,
          false,
          -0.08,
          true
        );
      }

      break;
    }

    case "sand_pile": {
      const sandLight = "#e8d8b0";
      const sandMid = "#d8c898";
      const sandDark = "#c0a878";

      // Shadow
      ctx.fillStyle = "rgba(0,0,0,0.15)";
      drawOrganicBlobAt(
        ctx,
        screenPos.x + 3 * s,
        screenPos.y + 5 * s,
        22 * s,
        10 * s,
        decorX * 4.3 + decorY * 7.7
      );
      ctx.fill();

      // Main dune shape (isometric mound)
      const duneGrad = ctx.createRadialGradient(
        screenPos.x - 5 * s,
        screenPos.y - 8 * s,
        0,
        screenPos.x,
        screenPos.y,
        22 * s
      );
      duneGrad.addColorStop(0, sandLight);
      duneGrad.addColorStop(0.5, sandMid);
      duneGrad.addColorStop(1, sandDark);
      ctx.fillStyle = duneGrad;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 20 * s, screenPos.y + 3 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 15 * s,
        screenPos.y - 12 * s,
        screenPos.x - 3 * s,
        screenPos.y - 15 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 10 * s,
        screenPos.y - 12 * s,
        screenPos.x + 18 * s,
        screenPos.y + 3 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 5 * s,
        screenPos.y + 6 * s,
        screenPos.x - 10 * s,
        screenPos.y + 5 * s
      );
      ctx.closePath();
      ctx.fill();

      // Wind ripple lines
      ctx.strokeStyle = "rgba(255,255,255,0.2)";
      ctx.lineWidth = 0.8 * s;
      for (let r = 0; r < 4; r++) {
        const ry = screenPos.y - 4 * s + r * 3 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 14 * s + r * 3 * s, ry);
        ctx.quadraticCurveTo(
          screenPos.x - 2 * s,
          ry - 2 * s,
          screenPos.x + 12 * s - r * 2 * s,
          ry + 1 * s
        );
        ctx.stroke();
      }

      // Shadow on lee side
      ctx.fillStyle = "rgba(0,0,0,0.08)";
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 5 * s, screenPos.y - 8 * s);
      ctx.quadraticCurveTo(
        screenPos.x + 15 * s,
        screenPos.y - 4 * s,
        screenPos.x + 18 * s,
        screenPos.y + 3 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 10 * s,
        screenPos.y + 2 * s,
        screenPos.x + 5 * s,
        screenPos.y - 8 * s
      );
      ctx.fill();
      break;
    }

    case "treasure_chest": {
      const chestWood = "#6b4226";
      const chestWoodDark = "#4a2a16";
      const chestGold = "#d4a840";
      const chestMetal = "#8a8a70";

      // Shadow
      ctx.fillStyle = "rgba(0,0,0,0.25)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 5 * s,
        14 * s,
        7 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Sand around half-buried chest
      ctx.fillStyle = "#d8c898";
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y + 3 * s,
        12 * s,
        5 * s,
        decorX * 9.1 + decorY * 6.3
      );
      ctx.fill();

      // Chest body - left face
      ctx.fillStyle = chestWoodDark;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 10 * s, screenPos.y);
      ctx.lineTo(screenPos.x - 10 * s, screenPos.y - 10 * s);
      ctx.lineTo(screenPos.x, screenPos.y - 6 * s);
      ctx.lineTo(screenPos.x, screenPos.y + 4 * s);
      ctx.closePath();
      ctx.fill();
      // Right face
      ctx.fillStyle = chestWood;
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 10 * s, screenPos.y);
      ctx.lineTo(screenPos.x + 10 * s, screenPos.y - 10 * s);
      ctx.lineTo(screenPos.x, screenPos.y - 6 * s);
      ctx.lineTo(screenPos.x, screenPos.y + 4 * s);
      ctx.closePath();
      ctx.fill();

      // Lid (open at angle)
      ctx.fillStyle = chestWood;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 10 * s, screenPos.y - 10 * s);
      ctx.lineTo(screenPos.x - 8 * s, screenPos.y - 20 * s);
      ctx.lineTo(screenPos.x + 8 * s, screenPos.y - 16 * s);
      ctx.lineTo(screenPos.x + 10 * s, screenPos.y - 10 * s);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = chestWoodDark;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 10 * s, screenPos.y - 10 * s);
      ctx.lineTo(screenPos.x - 8 * s, screenPos.y - 20 * s);
      ctx.lineTo(screenPos.x - 6 * s, screenPos.y - 18 * s);
      ctx.lineTo(screenPos.x - 8 * s, screenPos.y - 10 * s);
      ctx.closePath();
      ctx.fill();

      // Metal bands
      ctx.fillStyle = chestMetal;
      ctx.fillRect(screenPos.x - 9 * s, screenPos.y - 8 * s, 2 * s, 8 * s);
      ctx.fillRect(screenPos.x + 7 * s, screenPos.y - 8 * s, 2 * s, 8 * s);

      // Gold glow from inside
      const goldGlow = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y - 10 * s,
        0,
        screenPos.x,
        screenPos.y - 10 * s,
        10 * s
      );
      const glowPulse = 0.4 + Math.sin(decorTime * 2) * 0.15;
      goldGlow.addColorStop(0, `rgba(255,200,50,${glowPulse})`);
      goldGlow.addColorStop(1, "transparent");
      ctx.fillStyle = goldGlow;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y - 10 * s,
        10 * s,
        6 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Gold coins visible
      ctx.fillStyle = chestGold;
      ctx.beginPath();
      ctx.arc(screenPos.x - 2 * s, screenPos.y - 8 * s, 2 * s, 0, Math.PI * 2);
      ctx.arc(
        screenPos.x + 3 * s,
        screenPos.y - 9 * s,
        1.8 * s,
        0,
        Math.PI * 2
      );
      ctx.arc(screenPos.x, screenPos.y - 11 * s, 1.5 * s, 0, Math.PI * 2);
      ctx.fill();

      // Lock
      ctx.fillStyle = chestMetal;
      ctx.beginPath();
      ctx.arc(screenPos.x, screenPos.y - 5 * s, 2 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#5a5a40";
      ctx.beginPath();
      ctx.arc(screenPos.x, screenPos.y - 5 * s, 1 * s, 0, Math.PI * 2);
      ctx.fill();
      break;
    }

    // === WINTER DECORATIONS ===
    case "pine": {
      drawPine(ctx, screenPos.x, screenPos.y, s, variant, decorX, decorY);
      break;
    }
    case "snowman": {
      const smVar = variant % 3;
      const snowBase = "#f5f5f5";
      const snowShade = "#e0e0e0";
      const snowHighlight = "#ffffff";
      const snowBlueShade = "#e3f2fd";

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        screenPos.x,
        screenPos.y + 4 * s,
        s,
        16 * s,
        8 * s,
        35 * s,
        0.25
      );

      // Shared snow-ball helper with surface detail
      const drawSnowBall = (bx: number, by: number, br: number) => {
        const sbGrad = ctx.createRadialGradient(
          bx - br * 0.35,
          by - br * 0.35,
          0,
          bx,
          by,
          br
        );
        sbGrad.addColorStop(0, snowHighlight);
        sbGrad.addColorStop(0.3, snowBase);
        sbGrad.addColorStop(0.65, snowShade);
        sbGrad.addColorStop(0.9, snowBlueShade);
        sbGrad.addColorStop(1, "#ccdce8");
        ctx.fillStyle = sbGrad;
        ctx.beginPath();
        ctx.arc(bx, by, br, 0, Math.PI * 2);
        ctx.fill();
        // Rim shadow for roundness
        ctx.strokeStyle = "rgba(0,0,0,0.1)";
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.arc(bx, by, br - 1 * s, 0.2, Math.PI - 0.2);
        ctx.stroke();
        // Top rim highlight
        ctx.strokeStyle = "rgba(255,255,255,0.5)";
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.arc(bx, by, br - 1 * s, Math.PI + 0.5, Math.PI * 2 - 0.5);
        ctx.stroke();
        // Surface sparkle specks
        ctx.fillStyle = "rgba(255,255,255,0.7)";
        ctx.beginPath();
        ctx.arc(bx - br * 0.25, by - br * 0.35, br * 0.08, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.arc(bx + br * 0.15, by - br * 0.45, br * 0.05, 0, Math.PI * 2);
        ctx.fill();
      };

      // Shared eyes
      const drawSnowEyes = (ex: number, ey: number, esp: number) => {
        ctx.fillStyle = "#1a1a1a";
        ctx.beginPath();
        ctx.ellipse(ex - esp, ey, 2 * s, 2.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(ex + esp, ey, 2 * s, 2.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "rgba(255,255,255,0.6)";
        ctx.beginPath();
        ctx.arc(ex - esp - 0.5 * s, ey - 1 * s, 0.8 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.arc(ex + esp - 0.5 * s, ey - 1 * s, 0.8 * s, 0, Math.PI * 2);
        ctx.fill();
      };

      // Shared carrot nose
      const drawCarrotNose = (nx: number, ny: number, nLen: number) => {
        const snNoseGrad = ctx.createLinearGradient(
          nx,
          ny,
          nx + nLen,
          ny + 1 * s
        );
        snNoseGrad.addColorStop(0, "#ff8c00");
        snNoseGrad.addColorStop(0.5, "#ff6b00");
        snNoseGrad.addColorStop(1, "#e65100");
        ctx.fillStyle = snNoseGrad;
        ctx.beginPath();
        ctx.moveTo(nx, ny - 2 * s);
        ctx.lineTo(nx + nLen, ny + 1 * s);
        ctx.lineTo(nx, ny + 3 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "rgba(255,255,255,0.3)";
        ctx.beginPath();
        ctx.moveTo(nx + 1 * s, ny - 1 * s);
        ctx.lineTo(nx + nLen * 0.5, ny);
        ctx.lineTo(nx + 1 * s, ny + 1 * s);
        ctx.closePath();
        ctx.fill();
      };

      if (smVar === 0) {
        // Classic top-hat snowman
        drawSnowBall(screenPos.x, screenPos.y - 5 * s, 16 * s);
        drawSnowBall(screenPos.x, screenPos.y - 22 * s, 12 * s);
        // Buttons
        ctx.fillStyle = "#37474f";
        for (let btn = 0; btn < 3; btn++) {
          ctx.beginPath();
          ctx.arc(
            screenPos.x + 2 * s,
            screenPos.y - 16 * s - btn * 6 * s,
            1.8 * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
        // Stick arms with twigs
        ctx.strokeStyle = "#5d4037";
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 10 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x - 22 * s, screenPos.y - 28 * s);
        ctx.stroke();
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 18 * s, screenPos.y - 26 * s);
        ctx.lineTo(screenPos.x - 22 * s, screenPos.y - 32 * s);
        ctx.moveTo(screenPos.x - 20 * s, screenPos.y - 27 * s);
        ctx.lineTo(screenPos.x - 24 * s, screenPos.y - 25 * s);
        ctx.stroke();
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 10 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x + 20 * s, screenPos.y - 18 * s);
        ctx.stroke();
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 17 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 20 * s, screenPos.y - 14 * s);
        ctx.moveTo(screenPos.x + 19 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 24 * s, screenPos.y - 20 * s);
        ctx.stroke();
        // Head
        drawSnowBall(screenPos.x, screenPos.y - 38 * s, 9 * s);
        // Top hat with gradient
        ctx.fillStyle = "#1a1a1a";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 46 * s,
          10 * s,
          4 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        const hatG = ctx.createLinearGradient(
          screenPos.x - 6 * s,
          screenPos.y - 62 * s,
          screenPos.x + 6 * s,
          screenPos.y - 46 * s
        );
        hatG.addColorStop(0, "#263238");
        hatG.addColorStop(0.3, "#37474f");
        hatG.addColorStop(0.7, "#1a1a1a");
        hatG.addColorStop(1, "#0d0d0d");
        ctx.fillStyle = hatG;
        ctx.fillRect(screenPos.x - 6 * s, screenPos.y - 62 * s, 12 * s, 16 * s);
        // Hat top
        ctx.fillStyle = "#37474f";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 62 * s,
          6 * s,
          2.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Hat highlight
        ctx.fillStyle = "rgba(255,255,255,0.08)";
        ctx.fillRect(screenPos.x - 5 * s, screenPos.y - 61 * s, 3 * s, 14 * s);
        // Hat ribbon
        const ribbonG = ctx.createLinearGradient(
          screenPos.x - 6 * s,
          screenPos.y - 55 * s,
          screenPos.x + 6 * s,
          screenPos.y - 52 * s
        );
        ribbonG.addColorStop(0, "#b71c1c");
        ribbonG.addColorStop(0.5, "#e53935");
        ribbonG.addColorStop(1, "#c62828");
        ctx.fillStyle = ribbonG;
        ctx.fillRect(screenPos.x - 6 * s, screenPos.y - 54 * s, 12 * s, 3 * s);
        // Snow dusting on hat brim
        ctx.fillStyle = "rgba(240,248,255,0.35)";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 2 * s,
          screenPos.y - 46 * s,
          6 * s,
          2 * s,
          0.2,
          Math.PI,
          Math.PI * 2
        );
        ctx.fill();
        // Face
        drawSnowEyes(screenPos.x, screenPos.y - 40 * s, 3 * s);
        drawCarrotNose(screenPos.x, screenPos.y - 37 * s, 10 * s);
        // Smile
        ctx.fillStyle = "#37474f";
        for (let i = 0; i < 5; i++) {
          const smA = Math.PI * 0.15 + (i / 4) * Math.PI * 0.3;
          ctx.beginPath();
          ctx.arc(
            screenPos.x + Math.cos(smA) * 5 * s,
            screenPos.y - 34 * s + Math.sin(smA) * 3 * s,
            1 * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
        // Red scarf with gradient
        const sm0ScarfG = ctx.createLinearGradient(
          screenPos.x - 10 * s,
          screenPos.y - 30 * s,
          screenPos.x + 10 * s,
          screenPos.y - 30 * s
        );
        sm0ScarfG.addColorStop(0, "#b71c1c");
        sm0ScarfG.addColorStop(0.4, "#e53935");
        sm0ScarfG.addColorStop(0.7, "#c62828");
        sm0ScarfG.addColorStop(1, "#b71c1c");
        ctx.fillStyle = sm0ScarfG;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 30 * s,
          10 * s,
          4 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "#c62828";
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 6 * s, screenPos.y - 30 * s);
        ctx.quadraticCurveTo(
          screenPos.x + 12 * s,
          screenPos.y - 28 * s,
          screenPos.x + 10 * s,
          screenPos.y - 20 * s
        );
        ctx.lineTo(screenPos.x + 6 * s, screenPos.y - 21 * s);
        ctx.quadraticCurveTo(
          screenPos.x + 8 * s,
          screenPos.y - 27 * s,
          screenPos.x + 4 * s,
          screenPos.y - 29 * s
        );
        ctx.closePath();
        ctx.fill();
        // Scarf fringe
        ctx.strokeStyle = "#c62828";
        ctx.lineWidth = 1 * s;
        for (let sf = 0; sf < 3; sf++) {
          ctx.beginPath();
          ctx.moveTo(screenPos.x + (7 + sf * 1.5) * s, screenPos.y - 20 * s);
          ctx.lineTo(screenPos.x + (7 + sf * 1.5) * s, screenPos.y - 17 * s);
          ctx.stroke();
        }
        ctx.fillStyle = "#ffeb3b";
        ctx.fillRect(screenPos.x - 8 * s, screenPos.y - 31 * s, 2 * s, 3 * s);
        ctx.fillRect(screenPos.x + 6 * s, screenPos.y - 31 * s, 2 * s, 3 * s);
      } else if (smVar === 1) {
        // Chubby snowman with earmuffs and broom
        drawSnowBall(screenPos.x, screenPos.y - 3 * s, 18 * s);
        drawSnowBall(screenPos.x, screenPos.y - 22 * s, 13 * s);
        drawSnowBall(screenPos.x, screenPos.y - 40 * s, 10 * s);
        // Buttons - wooden toggles
        ctx.fillStyle = "#5d4037";
        for (let btn = 0; btn < 2; btn++) {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + 2 * s,
            screenPos.y - 16 * s - btn * 7 * s,
            2.5 * s,
            1.5 * s,
            0.2,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
        // Earmuffs band
        ctx.strokeStyle = "#1565C0";
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.arc(screenPos.x, screenPos.y - 42 * s, 10 * s, Math.PI + 0.3, -0.3);
        ctx.stroke();
        // Earmuff puffs
        ctx.fillStyle = "#1565C0";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 9 * s,
          screenPos.y - 39 * s,
          3.5 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.arc(
          screenPos.x + 9 * s,
          screenPos.y - 39 * s,
          3.5 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Fluffy trim on earmuffs
        ctx.fillStyle = "#E3F2FD";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 9 * s,
          screenPos.y - 39 * s,
          2 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.arc(
          screenPos.x + 9 * s,
          screenPos.y - 39 * s,
          2 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Face
        drawSnowEyes(screenPos.x, screenPos.y - 42 * s, 3 * s);
        drawCarrotNose(screenPos.x, screenPos.y - 39 * s, 9 * s);
        // Buck-tooth smile
        ctx.fillStyle = "#37474f";
        for (let i = 0; i < 4; i++) {
          const smA = Math.PI * 0.2 + (i / 3) * Math.PI * 0.25;
          ctx.beginPath();
          ctx.arc(
            screenPos.x + Math.cos(smA) * 5 * s,
            screenPos.y - 36 * s + Math.sin(smA) * 2.5 * s,
            1 * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
        // Green scarf
        ctx.fillStyle = "#2E7D32";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 31 * s,
          11 * s,
          4 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 5 * s, screenPos.y - 31 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 14 * s,
          screenPos.y - 28 * s,
          screenPos.x - 12 * s,
          screenPos.y - 20 * s
        );
        ctx.lineTo(screenPos.x - 8 * s, screenPos.y - 21 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 10 * s,
          screenPos.y - 27 * s,
          screenPos.x - 3 * s,
          screenPos.y - 30 * s
        );
        ctx.closePath();
        ctx.fill();
        // Broom in right arm
        ctx.strokeStyle = "#5d4037";
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 11 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x + 22 * s, screenPos.y - 48 * s);
        ctx.stroke();
        // Broom head
        ctx.strokeStyle = "#8D6E63";
        ctx.lineWidth = 1.5 * s;
        for (let br = -3; br <= 3; br++) {
          ctx.beginPath();
          ctx.moveTo(screenPos.x + 22 * s + br * 1.5 * s, screenPos.y - 48 * s);
          ctx.lineTo(screenPos.x + 20 * s + br * 2 * s, screenPos.y - 55 * s);
          ctx.stroke();
        }
        // Left arm waving
        ctx.strokeStyle = "#5d4037";
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 11 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x - 20 * s, screenPos.y - 30 * s);
        ctx.stroke();
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 17 * s, screenPos.y - 28 * s);
        ctx.lineTo(screenPos.x - 21 * s, screenPos.y - 34 * s);
        ctx.moveTo(screenPos.x - 19 * s, screenPos.y - 29 * s);
        ctx.lineTo(screenPos.x - 24 * s, screenPos.y - 28 * s);
        ctx.stroke();
      } else {
        // Small jolly snowman with bucket hat and mittens
        drawSnowBall(screenPos.x, screenPos.y - 2 * s, 14 * s);
        drawSnowBall(screenPos.x, screenPos.y - 18 * s, 10 * s);
        drawSnowBall(screenPos.x, screenPos.y - 32 * s, 8 * s);
        // Pebble buttons
        ctx.fillStyle = "#455A64";
        for (let btn = 0; btn < 2; btn++) {
          ctx.beginPath();
          ctx.arc(
            screenPos.x + 1 * s,
            screenPos.y - 13 * s - btn * 5 * s,
            1.5 * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
        // Bucket hat
        ctx.fillStyle = "#546E7A";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 39 * s,
          9 * s,
          3.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "#455A64";
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 7 * s, screenPos.y - 39 * s);
        ctx.lineTo(screenPos.x - 5 * s, screenPos.y - 50 * s);
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 50 * s);
        ctx.lineTo(screenPos.x + 7 * s, screenPos.y - 39 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#607D8B";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 50 * s,
          5 * s,
          2 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Face
        drawSnowEyes(screenPos.x, screenPos.y - 34 * s, 2.5 * s);
        drawCarrotNose(screenPos.x, screenPos.y - 32 * s, 8 * s);
        // Happy grin
        ctx.strokeStyle = "#37474f";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.arc(
          screenPos.x + 1 * s,
          screenPos.y - 30 * s,
          3.5 * s,
          0.2,
          Math.PI - 0.2
        );
        ctx.stroke();
        // Blue scarf
        ctx.fillStyle = "#1976D2";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 25 * s,
          9 * s,
          3.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 5 * s, screenPos.y - 25 * s);
        ctx.quadraticCurveTo(
          screenPos.x + 10 * s,
          screenPos.y - 23 * s,
          screenPos.x + 9 * s,
          screenPos.y - 17 * s
        );
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 18 * s);
        ctx.quadraticCurveTo(
          screenPos.x + 7 * s,
          screenPos.y - 23 * s,
          screenPos.x + 3 * s,
          screenPos.y - 24 * s
        );
        ctx.closePath();
        ctx.fill();
        // Arms with mittens
        ctx.strokeStyle = "#5d4037";
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 8 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x - 18 * s, screenPos.y - 22 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 8 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 18 * s, screenPos.y - 14 * s);
        ctx.stroke();
        // Mittens
        ctx.fillStyle = "#E53935";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 18 * s,
          screenPos.y - 22 * s,
          3 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.arc(
          screenPos.x + 18 * s,
          screenPos.y - 14 * s,
          3 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Mitten thumbs
        ctx.fillStyle = "#C62828";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 19 * s,
          screenPos.y - 20 * s,
          1.5 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.arc(
          screenPos.x + 19 * s,
          screenPos.y - 12 * s,
          1.5 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      // Ambient snow flurry particles around all snowmen
      ctx.fillStyle = "rgba(230,240,255,0.65)";
      for (let snp = 0; snp < 6; snp++) {
        const snpA = (snp / 6) * Math.PI * 2 + decorTime * 0.3;
        const snpR = (18 + Math.sin(decorTime * 0.5 + snp * 1.4) * 6) * s;
        const snpY =
          screenPos.y - 20 * s + Math.cos(decorTime * 0.35 + snp) * 10 * s;
        const snpSize = (0.6 + Math.sin(decorTime + snp) * 0.3) * s;
        ctx.beginPath();
        ctx.arc(
          screenPos.x + Math.cos(snpA) * snpR,
          snpY,
          snpSize,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }
    case "ice_crystal": {
      // Enhanced 3D isometric ice crystal formation
      const crystalLight = "#e3f2fd";
      const crystalMid = "#90caf9";
      const crystalDark = "#42a5f5";
      const crystalDeep = "#1976d2";
      const crystalGlow = "#bbdefb";

      // Ground glow/reflection
      const crystalGlowGrad = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y + 3 * s,
        0,
        screenPos.x,
        screenPos.y + 3 * s,
        25 * s
      );
      crystalGlowGrad.addColorStop(0, "rgba(144,202,249,0.35)");
      crystalGlowGrad.addColorStop(0.5, "rgba(144,202,249,0.15)");
      crystalGlowGrad.addColorStop(1, "rgba(144,202,249,0)");
      ctx.fillStyle = crystalGlowGrad;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 3 * s,
        25 * s,
        12 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Ice base mound
      ctx.fillStyle = crystalGlow;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 2 * s,
        10 * s,
        4 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Main crystal spires - proper isometric 4-faced crystals
      const spires = [
        { base: 8, h: 35, tilt: 0, x: 0, y: 0 },
        { base: 5, h: 22, tilt: -0.15, x: -8, y: 2 },
        { base: 6, h: 25, tilt: 0.12, x: 10, y: 1 },
        { base: 4, h: 18, tilt: -0.08, x: -4, y: 3 },
        { base: 4, h: 16, tilt: 0.2, x: 6, y: 3 },
        { base: 3, h: 14, tilt: -0.25, x: -12, y: 4 },
      ];

      spires.sort((a, b) => a.y - b.y);

      for (const spire of spires) {
        drawIsometricCrystalSpire(
          ctx,
          screenPos.x + spire.x * s,
          screenPos.y + spire.y * s,
          spire.base * s,
          spire.h * s,
          crystalLight,
          crystalMid,
          crystalDeep,
          spire.tilt
        );
      }

      // Inner glow at center
      const centerGlow = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y - 10 * s,
        0,
        screenPos.x,
        screenPos.y - 10 * s,
        12 * s
      );
      centerGlow.addColorStop(0, "rgba(255,255,255,0.5)");
      centerGlow.addColorStop(0.5, "rgba(144,202,249,0.3)");
      centerGlow.addColorStop(1, "rgba(144,202,249,0)");
      ctx.fillStyle = centerGlow;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y - 10 * s,
        12 * s,
        8 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Sparkle particles
      ctx.fillStyle = "#ffffff";
      const sparkleTime = decorTime * 2;
      for (let sp = 0; sp < 6; sp++) {
        const sparkleAngle = (sp * Math.PI) / 3 + sparkleTime;
        const sparkleR = 8 + Math.sin(sparkleTime + sp) * 4;
        const sparkleX = screenPos.x + Math.cos(sparkleAngle) * sparkleR * s;
        const sparkleY =
          screenPos.y - 15 * s + Math.sin(sparkleAngle) * sparkleR * 0.4 * s;
        const sparkleSize = 1 + Math.sin(sparkleTime * 2 + sp) * 0.5;
        ctx.beginPath();
        ctx.arc(sparkleX, sparkleY, sparkleSize * s, 0, Math.PI * 2);
        ctx.fill();
      }

      // Frost particles floating up
      ctx.fillStyle = "rgba(255,255,255,0.6)";
      for (let fp = 0; fp < 4; fp++) {
        const frostY = screenPos.y - ((decorTime * 20 + fp * 15) % 40) * s;
        const frostX = screenPos.x + Math.sin(decorTime + fp * 1.5) * 8 * s;
        ctx.beginPath();
        ctx.arc(frostX, frostY, 1.2 * s, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }
    case "snow_pile": {
      const snowBaseX = screenPos.x;
      const snowBaseY = screenPos.y;
      const snowSeed = (dec.x || 0) * 7.3 + (dec.y || 0) * 13.1;

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        snowBaseX,
        snowBaseY + 2 * s,
        s,
        40 * s,
        20 * s,
        32 * s,
        0.25,
        "70,90,120"
      );

      // Wide snow spread on ground - soft fade into terrain
      const groundSnowGrad = ctx.createRadialGradient(
        snowBaseX,
        snowBaseY + 6 * s,
        0,
        snowBaseX,
        snowBaseY + 6 * s,
        50 * s
      );
      groundSnowGrad.addColorStop(0, "#e8f0f5");
      groundSnowGrad.addColorStop(0.5, "#e0eaf2");
      groundSnowGrad.addColorStop(0.8, "rgba(220,235,245,0.4)");
      groundSnowGrad.addColorStop(1, "transparent");
      ctx.fillStyle = groundSnowGrad;
      drawOrganicBlobAt(
        ctx,
        snowBaseX,
        snowBaseY + 6 * s,
        48 * s,
        17 * s,
        snowSeed,
        0.12,
        32
      );
      ctx.fill();

      // Main snow body - large organic blob with blue shadow tones
      const mainSnowGrad = ctx.createRadialGradient(
        snowBaseX + 5 * s,
        snowBaseY - 12 * s,
        0,
        snowBaseX,
        snowBaseY,
        40 * s
      );
      mainSnowGrad.addColorStop(0, "#f0f5fa");
      mainSnowGrad.addColorStop(0.4, "#e5edf4");
      mainSnowGrad.addColorStop(0.7, "#dce6ef");
      mainSnowGrad.addColorStop(1, "#d0dce8");
      ctx.fillStyle = mainSnowGrad;
      drawOrganicBlobAt(
        ctx,
        snowBaseX - 2 * s,
        snowBaseY - 3 * s,
        40 * s,
        20 * s,
        snowSeed * 1.7,
        0.15,
        32
      );
      ctx.fill();

      // Primary mound - brighter, higher organic blob
      const moundGrad = ctx.createRadialGradient(
        snowBaseX - 6 * s,
        snowBaseY - 20 * s,
        0,
        snowBaseX - 3 * s,
        snowBaseY - 8 * s,
        30 * s
      );
      moundGrad.addColorStop(0, "#ffffff");
      moundGrad.addColorStop(0.3, "#f8fbfd");
      moundGrad.addColorStop(0.6, "#f0f5fa");
      moundGrad.addColorStop(1, "#e5edf4");
      ctx.fillStyle = moundGrad;
      drawOrganicBlobAt(
        ctx,
        snowBaseX - 5 * s,
        snowBaseY - 8 * s,
        32 * s,
        18 * s,
        snowSeed * 3.1,
        0.18,
        32
      );
      ctx.fill();

      // Peak highlight blob - bright white cap
      const peakSnowGrad = ctx.createRadialGradient(
        snowBaseX - 8 * s,
        snowBaseY - 22 * s,
        0,
        snowBaseX - 5 * s,
        snowBaseY - 14 * s,
        18 * s
      );
      peakSnowGrad.addColorStop(0, "#ffffff");
      peakSnowGrad.addColorStop(0.5, "rgba(255,255,255,0.8)");
      peakSnowGrad.addColorStop(1, "rgba(248,251,253,0.3)");
      ctx.fillStyle = peakSnowGrad;
      drawOrganicBlobAt(
        ctx,
        snowBaseX - 6 * s,
        snowBaseY - 16 * s,
        20 * s,
        11 * s,
        snowSeed * 5.3,
        0.2,
        28
      );
      ctx.fill();

      // Secondary mound offset to the right
      const mound2SnowGrad = ctx.createRadialGradient(
        snowBaseX + 14 * s,
        snowBaseY - 10 * s,
        0,
        snowBaseX + 14 * s,
        snowBaseY - 2 * s,
        20 * s
      );
      mound2SnowGrad.addColorStop(0, "#ffffff");
      mound2SnowGrad.addColorStop(0.4, "#f5f9fc");
      mound2SnowGrad.addColorStop(1, "#e8f0f5");
      ctx.fillStyle = mound2SnowGrad;
      drawOrganicBlobAt(
        ctx,
        snowBaseX + 14 * s,
        snowBaseY - 3 * s,
        20 * s,
        13 * s,
        snowSeed * 7.7,
        0.17,
        28
      );
      ctx.fill();

      // Blue shadow blob in the crevice between mounds
      const creviceGrad = ctx.createRadialGradient(
        snowBaseX + 3 * s,
        snowBaseY - 7 * s,
        0,
        snowBaseX + 3 * s,
        snowBaseY - 5 * s,
        12 * s
      );
      creviceGrad.addColorStop(0, "rgba(150,180,215,0.35)");
      creviceGrad.addColorStop(1, "transparent");
      ctx.fillStyle = creviceGrad;
      drawOrganicBlobAt(
        ctx,
        snowBaseX + 3 * s,
        snowBaseY - 5 * s,
        12 * s,
        7 * s,
        snowSeed * 9.1,
        0.1,
        24
      );
      ctx.fill();

      // Highlight ridge on the lit crest
      setShadowBlur(ctx, 3 * s, "rgba(255,255,255,0.3)");
      ctx.strokeStyle = "rgba(255,255,255,0.85)";
      ctx.lineWidth = 2 * s;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(snowBaseX - 18 * s, snowBaseY - 10 * s);
      ctx.bezierCurveTo(
        snowBaseX - 10 * s,
        snowBaseY - 22 * s,
        snowBaseX - 2 * s,
        snowBaseY - 24 * s,
        snowBaseX + 6 * s,
        snowBaseY - 14 * s
      );
      ctx.stroke();
      clearShadow(ctx);

      // Secondary highlight on the right mound
      ctx.strokeStyle = "rgba(255,255,255,0.55)";
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(snowBaseX + 8 * s, snowBaseY - 8 * s);
      ctx.bezierCurveTo(
        snowBaseX + 14 * s,
        snowBaseY - 14 * s,
        snowBaseX + 20 * s,
        snowBaseY - 12 * s,
        snowBaseX + 22 * s,
        snowBaseY - 4 * s
      );
      ctx.stroke();

      // Snow texture bumps on surfaces
      ctx.fillStyle = "rgba(255,255,255,0.45)";
      for (let bump = 0; bump < 10; bump++) {
        const bumpAngle = (bump / 10) * Math.PI * 2 + snowSeed;
        const bumpDist = (12 + Math.sin(snowSeed + bump * 2) * 8) * s;
        const bumpX = snowBaseX + Math.cos(bumpAngle) * bumpDist * 0.8;
        const bumpY = snowBaseY - 10 * s + Math.sin(bumpAngle) * bumpDist * 0.4;
        const bumpSize = (1.5 + Math.sin(snowSeed + bump) * 0.8) * s;
        ctx.beginPath();
        ctx.ellipse(
          bumpX,
          bumpY,
          bumpSize * 1.8,
          bumpSize * 0.7,
          bumpAngle * 0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Frost crystals scattered on surface — tiny isometric spires
      const crystalSpots = [
        { x: -20, y: -18 },
        { x: -8, y: -24 },
        { x: 5, y: -20 },
        { x: 18, y: -12 },
        { x: 25, y: -8 },
        { x: -15, y: -8 },
      ];
      for (let idx = 0; idx < crystalSpots.length; idx++) {
        const cp = crystalSpots[idx];
        const cBase = (1.2 + Math.sin(snowSeed + idx * 1.7) * 0.4) * s;
        const cHeight = cBase * (3 + Math.sin(snowSeed + idx * 2.3) * 1.2);
        drawIsometricCrystalSpire(
          ctx,
          snowBaseX + cp.x * s,
          snowBaseY + cp.y * s,
          cBase,
          cHeight,
          "rgba(220, 240, 255, 0.85)",
          "rgba(180, 215, 240, 0.7)",
          "rgba(130, 175, 210, 0.6)"
        );
      }

      // Animated sparkles
      const sparkleTime = decorTime * 2.5;
      for (let sp = 0; sp < 7; sp++) {
        const sparklePhase =
          (sparkleTime + sp * 0.9 + Math.sin(snowSeed + sp) * 0.5) % 2;
        if (sparklePhase < 0.5) {
          const sparkleAlpha = Math.sin((sparklePhase * Math.PI) / 0.5) * 0.95;
          const spAngle =
            (sp / 7) * Math.PI * 1.5 - Math.PI * 0.5 + snowSeed * 0.1;
          const spDist = (15 + sp * 3) * s;
          const spx = snowBaseX + Math.cos(spAngle) * spDist * 0.7;
          const spy = snowBaseY - 15 * s + Math.sin(spAngle) * spDist * 0.35;

          ctx.fillStyle = `rgba(255,255,255,${sparkleAlpha})`;
          const starSize = (1.2 + Math.sin(sparklePhase * 6) * 0.4) * s;

          ctx.beginPath();
          ctx.moveTo(spx, spy - starSize * 1.8);
          ctx.lineTo(spx + starSize * 0.25, spy - starSize * 0.25);
          ctx.lineTo(spx + starSize * 1.8, spy);
          ctx.lineTo(spx + starSize * 0.25, spy + starSize * 0.25);
          ctx.lineTo(spx, spy + starSize * 1.8);
          ctx.lineTo(spx - starSize * 0.25, spy + starSize * 0.25);
          ctx.lineTo(spx - starSize * 1.8, spy);
          ctx.lineTo(spx - starSize * 0.25, spy - starSize * 0.25);
          ctx.closePath();
          ctx.fill();

          ctx.beginPath();
          ctx.arc(spx, spy, starSize * 0.35, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // Floating snow particles
      ctx.fillStyle = "rgba(255,255,255,0.45)";
      for (let p = 0; p < 4; p++) {
        const particlePhase = (decorTime * 0.7 + p * 0.8 + snowSeed) % 2.5;
        const px =
          snowBaseX -
          12 * s +
          p * 10 * s +
          Math.sin(decorTime * 1.5 + p) * 4 * s;
        const py = snowBaseY - 20 * s - particlePhase * 10 * s;
        const pSize = (0.7 + Math.sin(particlePhase + p) * 0.25) * s;
        if (particlePhase < 2) {
          ctx.globalAlpha = 0.35 * (1 - particlePhase / 2);
          ctx.beginPath();
          ctx.arc(px, py, pSize, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      ctx.globalAlpha = 1;

      // Rim light on the lit edge
      ctx.strokeStyle = "rgba(255, 252, 245, 0.5)";
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.moveTo(snowBaseX + 5 * s, snowBaseY - 16 * s);
      ctx.bezierCurveTo(
        snowBaseX - 3 * s,
        snowBaseY - 24 * s,
        snowBaseX + 18 * s,
        snowBaseY - 16 * s,
        snowBaseX + 24 * s,
        snowBaseY - 6 * s
      );
      ctx.stroke();
      break;
    }
    case "glacier": {
      const gVar = variant % 3;

      // Ground shadow
      if (!landmarkSkipShadow) {
        drawDirectionalShadow(
          ctx,
          screenPos.x,
          screenPos.y + 3 * s,
          s,
          32 * s,
          14 * s,
          45 * s,
          0.3,
          "0,70,100"
        );
      }

      // Snow mound base with gradient
      const glSnowG = ctx.createRadialGradient(
        screenPos.x - 4 * s,
        screenPos.y - 2 * s,
        0,
        screenPos.x,
        screenPos.y + 2 * s,
        35 * s
      );
      glSnowG.addColorStop(0, "#ffffff");
      glSnowG.addColorStop(0.4, "#edf4f8");
      glSnowG.addColorStop(0.8, "#d4e4ee");
      glSnowG.addColorStop(1, "#b8d4e8");
      ctx.fillStyle = glSnowG;
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y + 2 * s,
        35 * s,
        12 * s,
        decorX * 6.7 + decorY * 4.9
      );
      ctx.fill();

      // Frost bloom glow at base
      const glBloom = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y - 20 * s,
        0,
        screenPos.x,
        screenPos.y - 20 * s,
        40 * s
      );
      glBloom.addColorStop(0, "rgba(144,202,249,0.18)");
      glBloom.addColorStop(0.5, "rgba(144,202,249,0.06)");
      glBloom.addColorStop(1, "transparent");
      ctx.fillStyle = glBloom;
      ctx.beginPath();
      ctx.arc(screenPos.x, screenPos.y - 20 * s, 40 * s, 0, Math.PI * 2);
      ctx.fill();

      const drawGlSpire = (
        gx: number,
        gy: number,
        gh: number,
        gw: number,
        seed: number
      ) => {
        // Left face gradient
        const lG = ctx.createLinearGradient(gx - gw, gy, gx, gy - gh);
        lG.addColorStop(0, "#4fc3f7");
        lG.addColorStop(0.3, "#81d4fa");
        lG.addColorStop(0.7, "#b3e5fc");
        lG.addColorStop(1, "#e1f5fe");
        ctx.fillStyle = lG;
        ctx.beginPath();
        ctx.moveTo(gx, gy);
        ctx.lineTo(gx - gw, gy - gh * 0.18);
        ctx.lineTo(gx - gw * 0.3, gy - gh * 0.7);
        ctx.lineTo(gx, gy - gh);
        ctx.lineTo(gx + gw * 0.4, gy - gh * 0.82);
        ctx.closePath();
        ctx.fill();
        // Right face gradient
        const rG = ctx.createLinearGradient(gx, gy - gh, gx + gw, gy);
        rG.addColorStop(0, "#b3e5fc");
        rG.addColorStop(0.3, "#4fc3f7");
        rG.addColorStop(0.6, "#29b6f6");
        rG.addColorStop(1, "#0288d1");
        ctx.fillStyle = rG;
        ctx.beginPath();
        ctx.moveTo(gx, gy);
        ctx.lineTo(gx + gw, gy - gh * 0.18);
        ctx.lineTo(gx + gw * 0.4, gy - gh * 0.82);
        ctx.closePath();
        ctx.fill();
        // Bright top cap
        ctx.fillStyle = "#e1f5fe";
        ctx.beginPath();
        ctx.moveTo(gx, gy - gh);
        ctx.lineTo(gx - gw * 0.3, gy - gh * 0.7);
        ctx.lineTo(gx + gw * 0.4, gy - gh * 0.82);
        ctx.closePath();
        ctx.fill();
        // Internal fracture veins
        ctx.strokeStyle = "rgba(255,255,255,0.35)";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo(gx, gy - gh);
        ctx.lineTo(gx - gw * 0.15, gy - gh * 0.4);
        ctx.lineTo(gx + gw * 0.1, gy);
        ctx.stroke();
        ctx.strokeStyle = "rgba(200,230,255,0.25)";
        ctx.beginPath();
        ctx.moveTo(gx - gw * 0.5, gy - gh * 0.35);
        ctx.lineTo(gx - gw * 0.1, gy - gh * 0.55);
        ctx.lineTo(gx + gw * 0.2, gy - gh * 0.45);
        ctx.stroke();
        // Edge highlight
        ctx.strokeStyle = "rgba(255,255,255,0.55)";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(gx, gy - gh);
        ctx.lineTo(gx, gy);
        ctx.stroke();
        // Snow cap on ledge
        if (gh > 35 * s) {
          ctx.fillStyle = "rgba(240,248,255,0.7)";
          ctx.beginPath();
          ctx.ellipse(
            gx - gw * 0.15,
            gy - gh * 0.65,
            gw * 0.35,
            gw * 0.12,
            -0.2,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
        // Icicle drips from ledges
        ctx.fillStyle = "rgba(179,229,252,0.6)";
        const icicleCount = Math.max(2, Math.floor(gw / (5 * s)));
        for (let ic = 0; ic < icicleCount; ic++) {
          const icx = gx - gw * 0.6 + (ic / (icicleCount - 1)) * gw * 0.8;
          const icy = gy - gh * (0.15 + (((seed + ic) * 0.03) % 0.1));
          const icLen = (4 + (((seed + ic) * 1.7) % 6)) * s;
          ctx.beginPath();
          ctx.moveTo(icx - 1 * s, icy);
          ctx.lineTo(icx, icy + icLen);
          ctx.lineTo(icx + 1 * s, icy);
          ctx.closePath();
          ctx.fill();
        }
      };

      if (gVar === 0) {
        drawGlSpire(
          screenPos.x - 20 * s,
          screenPos.y - 5 * s,
          52 * s,
          16 * s,
          1
        );
        drawGlSpire(
          screenPos.x + 16 * s,
          screenPos.y - 8 * s,
          47 * s,
          13 * s,
          2
        );
        drawGlSpire(screenPos.x, screenPos.y, 68 * s, 21 * s, 3);
        drawGlSpire(
          screenPos.x - 10 * s,
          screenPos.y + 5 * s,
          32 * s,
          11 * s,
          4
        );
        drawGlSpire(
          screenPos.x + 6 * s,
          screenPos.y - 12 * s,
          25 * s,
          8 * s,
          5
        );
      } else if (gVar === 1) {
        drawGlSpire(
          screenPos.x - 15 * s,
          screenPos.y + 2 * s,
          58 * s,
          19 * s,
          6
        );
        drawGlSpire(
          screenPos.x + 10 * s,
          screenPos.y - 3 * s,
          50 * s,
          15 * s,
          7
        );
        drawGlSpire(
          screenPos.x + 24 * s,
          screenPos.y + 4 * s,
          36 * s,
          12 * s,
          8
        );
        drawGlSpire(
          screenPos.x - 5 * s,
          screenPos.y - 6 * s,
          63 * s,
          17 * s,
          9
        );
        drawGlSpire(
          screenPos.x - 24 * s,
          screenPos.y + 6 * s,
          28 * s,
          9 * s,
          10
        );
      } else {
        drawGlSpire(screenPos.x, screenPos.y - 2 * s, 72 * s, 23 * s, 11);
        drawGlSpire(
          screenPos.x - 24 * s,
          screenPos.y + 3 * s,
          42 * s,
          14 * s,
          12
        );
        drawGlSpire(
          screenPos.x + 20 * s,
          screenPos.y + 1 * s,
          44 * s,
          13 * s,
          13
        );
        drawGlSpire(
          screenPos.x - 8 * s,
          screenPos.y + 6 * s,
          30 * s,
          10 * s,
          14
        );
        drawGlSpire(
          screenPos.x + 8 * s,
          screenPos.y - 10 * s,
          40 * s,
          11 * s,
          15
        );
        drawGlSpire(
          screenPos.x - 16 * s,
          screenPos.y - 8 * s,
          22 * s,
          7 * s,
          16
        );
      }

      // Ambient ice particles
      ctx.fillStyle = "rgba(200,235,255,0.7)";
      for (let gp = 0; gp < 10; gp++) {
        const gpA = (gp / 10) * Math.PI * 2 + decorTime * 0.25;
        const gpR = (28 + Math.sin(decorTime * 0.6 + gp * 1.1) * 10) * s;
        const gpY =
          screenPos.y - 25 * s + Math.cos(decorTime * 0.4 + gp * 0.9) * 12 * s;
        const gpSize = (0.8 + Math.sin(decorTime + gp) * 0.4) * s;
        ctx.beginPath();
        ctx.arc(screenPos.x + Math.cos(gpA) * gpR, gpY, gpSize, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }

    case "fortress": {
      renderFortress({
        ctx,
        mapTheme: (mapTheme || undefined) as
          | import("../../types").MapTheme
          | undefined,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        seedX: decorX,
        seedY: decorY,
        shadowOnly,
        skipShadow: landmarkSkipShadow,
        time: decorTime,
        variant,
        zoom,
      });
      break;
    }

    case "ice_spire": {
      const isVar = variant % 3;
      const cx = screenPos.x;
      const cy = screenPos.y;

      // Ice color palette
      const iceLight = "#e1f5fe";
      const iceMid = "#81d4fa";
      const iceDark = "#0277BD";
      const iceDeep = "#01579B";

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        cx,
        cy + 6 * s,
        s,
        18 * s,
        8 * s,
        50 * s,
        0.3,
        "0,60,80"
      );

      // Isometric base platform using standard ISO ratios
      const drawIceBase = (baseHalf: number) => {
        const platformH = 8 * s;
        drawIsometricPrism(
          ctx,
          cx,
          cy + 4 * s,
          baseHalf,
          baseHalf,
          platformH,
          "#b3e5fc",
          "#4fc3f7",
          "#0288D1"
        );
      };

      if (isVar === 0) {
        drawIceBase(16 * s);
        drawIsometricCrystalSpire(
          ctx,
          cx - 10 * s,
          cy,
          7 * s,
          32 * s,
          iceLight,
          iceMid,
          iceDark
        );
        drawIsometricCrystalSpire(
          ctx,
          cx + 6 * s,
          cy - 2 * s,
          7 * s,
          30 * s,
          iceLight,
          iceMid,
          iceDeep
        );
        drawIsometricCrystalSpire(
          ctx,
          cx,
          cy - 4 * s,
          8 * s,
          58 * s,
          iceLight,
          iceMid,
          iceDark
        );
        // Rune marking on main spire
        ctx.strokeStyle = "rgba(128,222,234,0.3)";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(cx - 2 * s, cy - 20 * s);
        ctx.lineTo(cx + 2 * s, cy - 25 * s);
        ctx.lineTo(cx - 1 * s, cy - 30 * s);
        ctx.stroke();
      } else if (isVar === 1) {
        drawIceBase(19 * s);
        // Twin spires
        drawIsometricCrystalSpire(
          ctx,
          cx - 10 * s,
          cy,
          7 * s,
          48 * s,
          iceLight,
          iceMid,
          iceDark
        );
        drawIsometricCrystalSpire(
          ctx,
          cx + 10 * s,
          cy - 2 * s,
          7 * s,
          48 * s,
          iceLight,
          iceMid,
          iceDark
        );
        drawIsometricCrystalSpire(
          ctx,
          cx - 3 * s,
          cy + 2 * s,
          5 * s,
          22 * s,
          iceMid,
          iceDark,
          iceDeep
        );
        drawIsometricCrystalSpire(
          ctx,
          cx + 15 * s,
          cy + 1 * s,
          4 * s,
          20 * s,
          iceMid,
          iceDark,
          iceDeep
        );
        // Connecting arch with gradient
        const archG = ctx.createLinearGradient(
          cx - 10 * s,
          cy - 28 * s,
          cx + 10 * s,
          cy - 28 * s
        );
        archG.addColorStop(0, "#b3e5fc");
        archG.addColorStop(0.5, "#e1f5fe");
        archG.addColorStop(1, "#b3e5fc");
        ctx.strokeStyle = archG;
        ctx.lineWidth = 3.5 * s;
        ctx.beginPath();
        ctx.moveTo(cx - 10 * s, cy - 28 * s);
        ctx.quadraticCurveTo(cx, cy - 38 * s, cx + 10 * s, cy - 28 * s);
        ctx.stroke();
        // Rune on arch
        ctx.strokeStyle = "rgba(128,222,234,0.35)";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.arc(cx, cy - 32 * s, 3 * s, 0, Math.PI * 2);
        ctx.stroke();
      } else {
        // Clustered crystal formation
        drawIceBase(17 * s);
        drawIsometricCrystalSpire(
          ctx,
          cx,
          cy - 2 * s,
          7 * s,
          54 * s,
          iceLight,
          iceMid,
          iceDark
        );
        const csAngles = [0.7, 1.9, 3.3, 4.8];
        const csHeights = [34, 28, 38, 24];
        const csBases = [5, 4, 6, 4];
        csAngles.forEach((ca, ci) => {
          drawIsometricCrystalSpire(
            ctx,
            cx + Math.cos(ca) * 11 * s,
            cy + 2 * s + Math.sin(ca) * 5 * s,
            csBases[ci] * s,
            csHeights[ci] * s,
            ci % 2 === 0 ? iceLight : iceMid,
            iceMid,
            iceDeep
          );
        });
        // Glowing core
        const isCore = ctx.createRadialGradient(
          cx,
          cy - 16 * s,
          0,
          cx,
          cy - 16 * s,
          14 * s
        );
        isCore.addColorStop(0, "rgba(128,222,234,0.4)");
        isCore.addColorStop(0.4, "rgba(179,229,252,0.15)");
        isCore.addColorStop(1, "transparent");
        ctx.fillStyle = isCore;
        ctx.beginPath();
        ctx.arc(cx, cy - 16 * s, 14 * s, 0, Math.PI * 2);
        ctx.fill();
      }

      // Frost sparkles
      ctx.fillStyle = "rgba(220,245,255,0.75)";
      for (let isp = 0; isp < 6; isp++) {
        const spA = (isp / 6) * Math.PI * 2 + decorTime * 0.5;
        const spR = (14 + Math.sin(decorTime * 0.8 + isp * 1.3) * 5) * s;
        const spSize = (0.8 + Math.sin(decorTime + isp) * 0.3) * s;
        ctx.beginPath();
        ctx.arc(
          cx + Math.cos(spA) * spR,
          cy - 22 * s + Math.sin(spA) * spR * 0.35,
          spSize,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }

    case "ice_throne": {
      const itVar = variant % 3;
      const TRUE_ISO = 1 / Math.sqrt(3);

      // Ground shadow
      if (!landmarkSkipShadow) {
        drawDirectionalShadow(
          ctx,
          screenPos.x,
          screenPos.y + 10 * s,
          s,
          32 * s,
          14 * s,
          55 * s,
          0.35,
          "0,50,70"
        );
      }

      // Snow base mound
      const itSnowG = ctx.createRadialGradient(
        screenPos.x - 4 * s,
        screenPos.y + 4 * s,
        0,
        screenPos.x,
        screenPos.y + 10 * s,
        34 * s
      );
      itSnowG.addColorStop(0, "#ffffff");
      itSnowG.addColorStop(0.4, "#eaf2f8");
      itSnowG.addColorStop(0.8, "#cde0ec");
      itSnowG.addColorStop(1, "#b0cede");
      ctx.fillStyle = itSnowG;
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y + 10 * s,
        34 * s,
        12 * s,
        decorX * 5.7 + decorY * 9.3
      );
      ctx.fill();

      // Dais lower step - top (proper 30° isometric diamond)
      const itD1W = 28 * s;
      const itD1D = itD1W * TRUE_ISO;
      const itD1G = ctx.createLinearGradient(
        screenPos.x - itD1W,
        screenPos.y,
        screenPos.x + itD1W,
        screenPos.y + itD1D + 4 * s
      );
      itD1G.addColorStop(0, "#81d4fa");
      itD1G.addColorStop(0.5, "#b3e5fc");
      itD1G.addColorStop(1, "#4fc3f7");
      ctx.fillStyle = itD1G;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - itD1W, screenPos.y + 4 * s);
      ctx.lineTo(screenPos.x, screenPos.y + 4 * s + itD1D);
      ctx.lineTo(screenPos.x + itD1W, screenPos.y + 4 * s);
      ctx.lineTo(screenPos.x, screenPos.y + 4 * s - itD1D);
      ctx.closePath();
      ctx.fill();
      // Dais lower step - left face
      const itD1L = ctx.createLinearGradient(
        screenPos.x - itD1W,
        screenPos.y,
        screenPos.x,
        screenPos.y + 4 * s + itD1D + 8 * s
      );
      itD1L.addColorStop(0, "#29B6F6");
      itD1L.addColorStop(1, "#0277BD");
      ctx.fillStyle = itD1L;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - itD1W, screenPos.y + 4 * s);
      ctx.lineTo(screenPos.x, screenPos.y + 4 * s + itD1D);
      ctx.lineTo(screenPos.x, screenPos.y + 4 * s + itD1D + 8 * s);
      ctx.lineTo(screenPos.x - itD1W, screenPos.y + 12 * s);
      ctx.closePath();
      ctx.fill();
      // Dais lower step - right face
      ctx.fillStyle = "#01579B";
      ctx.beginPath();
      ctx.moveTo(screenPos.x + itD1W, screenPos.y + 4 * s);
      ctx.lineTo(screenPos.x, screenPos.y + 4 * s + itD1D);
      ctx.lineTo(screenPos.x, screenPos.y + 4 * s + itD1D + 8 * s);
      ctx.lineTo(screenPos.x + itD1W, screenPos.y + 12 * s);
      ctx.closePath();
      ctx.fill();
      // Rune engravings on dais face
      ctx.strokeStyle = "rgba(128,222,234,0.2)";
      ctx.lineWidth = 0.8 * s;
      for (let dr = 0; dr < 3; dr++) {
        const drx = screenPos.x - 18 * s + dr * 12 * s;
        const dry = screenPos.y + 8 * s + dr * 2 * s;
        ctx.beginPath();
        ctx.moveTo(drx, dry);
        ctx.lineTo(drx + 3 * s, dry - 2 * s);
        ctx.lineTo(drx + 6 * s, dry);
        ctx.lineTo(drx + 3 * s, dry + 2 * s);
        ctx.closePath();
        ctx.stroke();
      }

      // Upper step - top (proper 30° isometric diamond)
      const itD2W = 20 * s;
      const itD2D = itD2W * TRUE_ISO;
      const itD2G = ctx.createLinearGradient(
        screenPos.x - itD2W,
        screenPos.y - itD2D,
        screenPos.x + itD2W,
        screenPos.y + itD2D
      );
      itD2G.addColorStop(0, "#b3e5fc");
      itD2G.addColorStop(0.5, "#e1f5fe");
      itD2G.addColorStop(1, "#81d4fa");
      ctx.fillStyle = itD2G;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - itD2W, screenPos.y);
      ctx.lineTo(screenPos.x, screenPos.y + itD2D);
      ctx.lineTo(screenPos.x + itD2W, screenPos.y);
      ctx.lineTo(screenPos.x, screenPos.y - itD2D);
      ctx.closePath();
      ctx.fill();
      // Upper step - left face
      ctx.fillStyle = "#4fc3f7";
      ctx.beginPath();
      ctx.moveTo(screenPos.x - itD2W, screenPos.y);
      ctx.lineTo(screenPos.x, screenPos.y + itD2D);
      ctx.lineTo(screenPos.x, screenPos.y + itD2D + 5 * s);
      ctx.lineTo(screenPos.x - itD2W, screenPos.y + 4 * s);
      ctx.closePath();
      ctx.fill();

      if (itVar === 0) {
        // Grand spiked throne
        // Seat diamond (proper 30° isometric)
        const itSeatW = 13 * s;
        const itSeatD = itSeatW * TRUE_ISO;
        const itSeatCY = screenPos.y - 6 * s;
        const itSeatG = ctx.createLinearGradient(
          screenPos.x,
          itSeatCY - itSeatD,
          screenPos.x,
          itSeatCY + itSeatD
        );
        itSeatG.addColorStop(0, "#4fc3f7");
        itSeatG.addColorStop(1, "#0288D1");
        ctx.fillStyle = itSeatG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - itSeatW, itSeatCY);
        ctx.lineTo(screenPos.x, itSeatCY + itSeatD);
        ctx.lineTo(screenPos.x + itSeatW, itSeatCY);
        ctx.lineTo(screenPos.x, itSeatCY - itSeatD);
        ctx.closePath();
        ctx.fill();
        // Fur cushion on seat
        const itFurG = ctx.createRadialGradient(
          screenPos.x,
          screenPos.y - 7 * s,
          0,
          screenPos.x,
          screenPos.y - 7 * s,
          10 * s
        );
        itFurG.addColorStop(0, "rgba(230,240,250,0.6)");
        itFurG.addColorStop(0.6, "rgba(200,220,235,0.3)");
        itFurG.addColorStop(1, "transparent");
        ctx.fillStyle = itFurG;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 7 * s,
          10 * s,
          10 * s * TRUE_ISO,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Armrests - gradient pillars
        const itArmLG = ctx.createLinearGradient(
          screenPos.x - 15 * s,
          screenPos.y - 4 * s,
          screenPos.x - 15 * s,
          screenPos.y - 20 * s
        );
        itArmLG.addColorStop(0, "#0288D1");
        itArmLG.addColorStop(0.5, "#4fc3f7");
        itArmLG.addColorStop(1, "#b3e5fc");
        ctx.fillStyle = itArmLG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 16 * s, screenPos.y - 4 * s);
        ctx.lineTo(screenPos.x - 16 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x - 12 * s, screenPos.y - 20 * s);
        ctx.lineTo(screenPos.x - 12 * s, screenPos.y - 4 * s);
        ctx.closePath();
        ctx.fill();
        const itArmRG = ctx.createLinearGradient(
          screenPos.x + 12 * s,
          screenPos.y - 4 * s,
          screenPos.x + 12 * s,
          screenPos.y - 20 * s
        );
        itArmRG.addColorStop(0, "#01579B");
        itArmRG.addColorStop(0.5, "#0288D1");
        itArmRG.addColorStop(1, "#4fc3f7");
        ctx.fillStyle = itArmRG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 12 * s, screenPos.y - 4 * s);
        ctx.lineTo(screenPos.x + 12 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 16 * s, screenPos.y - 20 * s);
        ctx.lineTo(screenPos.x + 16 * s, screenPos.y - 4 * s);
        ctx.closePath();
        ctx.fill();
        // Armrest caps
        ctx.fillStyle = "#e1f5fe";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 14 * s,
          screenPos.y - 20 * s,
          3 * s,
          1.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 14 * s,
          screenPos.y - 20 * s,
          3 * s,
          1.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        drawIsometricCrystalSpire(
          ctx,
          screenPos.x,
          screenPos.y - 10 * s,
          24 * s,
          62 * s,
          "#e1f5fe",
          "#4fc3f7",
          "#0277bd"
        );
        // Frost rune carvings on the front crystal face
        ctx.strokeStyle = "rgba(128,222,234,0.3)";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 4 * s, screenPos.y - 25 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 30 * s);
        ctx.lineTo(screenPos.x + 4 * s, screenPos.y - 25 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 20 * s);
        ctx.closePath();
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x, screenPos.y - 38 * s);
        ctx.lineTo(screenPos.x - 3 * s, screenPos.y - 42 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 46 * s);
        ctx.lineTo(screenPos.x + 3 * s, screenPos.y - 42 * s);
        ctx.closePath();
        ctx.stroke();
        // Crown jewel with glow
        const itJewelG = ctx.createRadialGradient(
          screenPos.x,
          screenPos.y - 61 * s,
          0,
          screenPos.x,
          screenPos.y - 61 * s,
          6 * s
        );
        itJewelG.addColorStop(0, "rgba(128,222,234,0.7)");
        itJewelG.addColorStop(0.4, "rgba(128,222,234,0.2)");
        itJewelG.addColorStop(1, "transparent");
        ctx.fillStyle = itJewelG;
        ctx.beginPath();
        ctx.arc(screenPos.x, screenPos.y - 61 * s, 6 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "#80DEEA";
        ctx.beginPath();
        ctx.arc(screenPos.x, screenPos.y - 61 * s, 3 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "rgba(255,255,255,0.7)";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 1 * s,
          screenPos.y - 62 * s,
          1.2 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
      } else if (itVar === 1) {
        // Curved elegant throne
        // Seat
        const itSeatG2 = ctx.createRadialGradient(
          screenPos.x,
          screenPos.y - 8 * s,
          0,
          screenPos.x,
          screenPos.y - 8 * s,
          13 * s
        );
        itSeatG2.addColorStop(0, "#4fc3f7");
        itSeatG2.addColorStop(0.7, "#0288D1");
        itSeatG2.addColorStop(1, "#01579B");
        ctx.fillStyle = itSeatG2;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 8 * s,
          13 * s,
          13 * s * TRUE_ISO,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Fur cushion
        ctx.fillStyle = "rgba(230,240,250,0.45)";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 9 * s,
          9 * s,
          9 * s * TRUE_ISO,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Curved backrest with gradient
        const itCurveG = ctx.createLinearGradient(
          screenPos.x,
          screenPos.y - 8 * s,
          screenPos.x,
          screenPos.y - 62 * s
        );
        itCurveG.addColorStop(0, "#0288D1");
        itCurveG.addColorStop(0.4, "#4fc3f7");
        itCurveG.addColorStop(0.7, "#81d4fa");
        itCurveG.addColorStop(1, "#e1f5fe");
        ctx.fillStyle = itCurveG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 15 * s, screenPos.y - 8 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 19 * s,
          screenPos.y - 42 * s,
          screenPos.x - 7 * s,
          screenPos.y - 62 * s
        );
        ctx.lineTo(screenPos.x, screenPos.y - 57 * s);
        ctx.lineTo(screenPos.x + 7 * s, screenPos.y - 62 * s);
        ctx.quadraticCurveTo(
          screenPos.x + 19 * s,
          screenPos.y - 42 * s,
          screenPos.x + 15 * s,
          screenPos.y - 8 * s
        );
        ctx.closePath();
        ctx.fill();
        // Inner backrest highlight
        ctx.fillStyle = "rgba(225,245,254,0.4)";
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 11 * s, screenPos.y - 10 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 13 * s,
          screenPos.y - 37 * s,
          screenPos.x - 5 * s,
          screenPos.y - 52 * s
        );
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 52 * s);
        ctx.quadraticCurveTo(
          screenPos.x + 13 * s,
          screenPos.y - 37 * s,
          screenPos.x + 11 * s,
          screenPos.y - 10 * s
        );
        ctx.closePath();
        ctx.fill();
        // Sweeping armrests with gradient
        ctx.strokeStyle = "#29B6F6";
        ctx.lineWidth = 3.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 15 * s, screenPos.y - 8 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 22 * s,
          screenPos.y - 13 * s,
          screenPos.x - 20 * s,
          screenPos.y - 3 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 15 * s, screenPos.y - 8 * s);
        ctx.quadraticCurveTo(
          screenPos.x + 22 * s,
          screenPos.y - 13 * s,
          screenPos.x + 20 * s,
          screenPos.y - 3 * s
        );
        ctx.stroke();
        // Armrest tips
        ctx.fillStyle = "#e1f5fe";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 20 * s,
          screenPos.y - 3 * s,
          2.5 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.arc(
          screenPos.x + 20 * s,
          screenPos.y - 3 * s,
          2.5 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Snowflake emblem
        ctx.strokeStyle = "rgba(255,255,255,0.55)";
        ctx.lineWidth = 1 * s;
        for (let sf = 0; sf < 6; sf++) {
          const sfAngle = (sf / 6) * Math.PI * 2;
          const sfR = 6 * s;
          ctx.beginPath();
          ctx.moveTo(screenPos.x, screenPos.y - 37 * s);
          ctx.lineTo(
            screenPos.x + Math.cos(sfAngle) * sfR,
            screenPos.y - 37 * s + Math.sin(sfAngle) * sfR
          );
          ctx.stroke();
          // Branch tips
          const bx = screenPos.x + Math.cos(sfAngle) * sfR * 0.6;
          const by = screenPos.y - 37 * s + Math.sin(sfAngle) * sfR * 0.6;
          ctx.beginPath();
          ctx.moveTo(bx, by);
          ctx.lineTo(
            bx + Math.cos(sfAngle + 0.8) * 2 * s,
            by + Math.sin(sfAngle + 0.8) * 2 * s
          );
          ctx.stroke();
        }
      } else {
        // Jagged crystal throne
        // Seat (proper 30° isometric)
        const itSeat3W = 13 * s;
        const itSeat3D = itSeat3W * TRUE_ISO;
        const itSeat3CY = screenPos.y - 4 * s;
        const itSeatG3 = ctx.createLinearGradient(
          screenPos.x - itSeat3W,
          itSeat3CY - itSeat3D,
          screenPos.x + itSeat3W,
          itSeat3CY + itSeat3D
        );
        itSeatG3.addColorStop(0, "#4fc3f7");
        itSeatG3.addColorStop(0.5, "#0288D1");
        itSeatG3.addColorStop(1, "#01579B");
        ctx.fillStyle = itSeatG3;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - itSeat3W, itSeat3CY);
        ctx.lineTo(screenPos.x, itSeat3CY + itSeat3D);
        ctx.lineTo(screenPos.x + itSeat3W, itSeat3CY);
        ctx.lineTo(screenPos.x, itSeat3CY - itSeat3D);
        ctx.closePath();
        ctx.fill();
        // Jagged backrest - left bright face
        const itJagLG = ctx.createLinearGradient(
          screenPos.x - 16 * s,
          screenPos.y - 8 * s,
          screenPos.x,
          screenPos.y - 64 * s
        );
        itJagLG.addColorStop(0, "#29B6F6");
        itJagLG.addColorStop(0.4, "#4fc3f7");
        itJagLG.addColorStop(0.7, "#81d4fa");
        itJagLG.addColorStop(1, "#e1f5fe");
        ctx.fillStyle = itJagLG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 15 * s, screenPos.y - 8 * s);
        ctx.lineTo(screenPos.x - 17 * s, screenPos.y - 42 * s);
        ctx.lineTo(screenPos.x - 11 * s, screenPos.y - 50 * s);
        ctx.lineTo(screenPos.x - 5 * s, screenPos.y - 40 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 64 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 8 * s);
        ctx.closePath();
        ctx.fill();
        // Right dark face
        const itJagRG = ctx.createLinearGradient(
          screenPos.x,
          screenPos.y - 64 * s,
          screenPos.x + 15 * s,
          screenPos.y - 8 * s
        );
        itJagRG.addColorStop(0, "#81d4fa");
        itJagRG.addColorStop(0.3, "#29B6F6");
        itJagRG.addColorStop(0.6, "#0288D1");
        itJagRG.addColorStop(1, "#01579B");
        ctx.fillStyle = itJagRG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x, screenPos.y - 8 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 64 * s);
        ctx.lineTo(screenPos.x + 6 * s, screenPos.y - 44 * s);
        ctx.lineTo(screenPos.x + 13 * s, screenPos.y - 54 * s);
        ctx.lineTo(screenPos.x + 15 * s, screenPos.y - 36 * s);
        ctx.lineTo(screenPos.x + 15 * s, screenPos.y - 8 * s);
        ctx.closePath();
        ctx.fill();
        // Edge highlights
        ctx.strokeStyle = "rgba(255,255,255,0.45)";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 11 * s, screenPos.y - 50 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 64 * s);
        ctx.lineTo(screenPos.x + 13 * s, screenPos.y - 54 * s);
        ctx.stroke();
        // Fracture veins
        ctx.strokeStyle = "rgba(200,235,255,0.25)";
        ctx.lineWidth = 0.7 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 8 * s, screenPos.y - 20 * s);
        ctx.lineTo(screenPos.x - 3 * s, screenPos.y - 35 * s);
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 25 * s);
        ctx.stroke();
        // Icy armrests with gradient
        const itArmJG = ctx.createLinearGradient(
          screenPos.x - 19 * s,
          screenPos.y - 15 * s,
          screenPos.x - 15 * s,
          screenPos.y - 5 * s
        );
        itArmJG.addColorStop(0, "#e1f5fe");
        itArmJG.addColorStop(1, "#81d4fa");
        ctx.fillStyle = itArmJG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 15 * s, screenPos.y - 8 * s);
        ctx.lineTo(screenPos.x - 19 * s, screenPos.y - 15 * s);
        ctx.lineTo(screenPos.x - 17 * s, screenPos.y - 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#4fc3f7";
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 15 * s, screenPos.y - 8 * s);
        ctx.lineTo(screenPos.x + 19 * s, screenPos.y - 15 * s);
        ctx.lineTo(screenPos.x + 17 * s, screenPos.y - 5 * s);
        ctx.closePath();
        ctx.fill();
      }

      // Magical frost aura
      const itAuraG = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y - 32 * s,
        0,
        screenPos.x,
        screenPos.y - 32 * s,
        28 * s
      );
      itAuraG.addColorStop(0, "rgba(128,222,234,0.18)");
      itAuraG.addColorStop(0.5, "rgba(128,222,234,0.06)");
      itAuraG.addColorStop(1, "transparent");
      ctx.fillStyle = itAuraG;
      ctx.beginPath();
      ctx.arc(screenPos.x, screenPos.y - 32 * s, 28 * s, 0, Math.PI * 2);
      ctx.fill();

      // Floating ice motes
      ctx.fillStyle = "rgba(220,245,255,0.7)";
      for (let tm = 0; tm < 8; tm++) {
        const tmA = (tm / 8) * Math.PI * 2 + decorTime * 0.35;
        const tmR = (20 + Math.sin(decorTime * 0.6 + tm * 1.2) * 6) * s;
        const tmY =
          screenPos.y - 32 * s + Math.cos(decorTime * 0.45 + tm) * 10 * s;
        const tmSize = (0.8 + Math.sin(decorTime + tm * 0.7) * 0.4) * s;
        ctx.beginPath();
        ctx.arc(screenPos.x + Math.cos(tmA) * tmR, tmY, tmSize, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }

    case "icicles": {
      const icicleBlue = "#b3e5fc";
      const icicleMid = "#81d4fa";
      const icicleDark = "#4fc3f7";
      const icicleDeep = "#29b6f6";
      const rockGray = "#6b7b8a";
      const rockDark = "#4a5a6a";
      const rockLight = "#8a9aaa";

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        screenPos.x,
        screenPos.y + 4 * s,
        s,
        22 * s,
        10 * s,
        35 * s,
        0.25
      );

      // Snow mound at base
      ctx.fillStyle = "#f0f4f8";
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y + 4 * s,
        22 * s,
        8 * s,
        decorX * 6.1 + decorY * 3.7
      );
      ctx.fill();
      ctx.fillStyle = "#ffffff";
      drawOrganicBlobAt(
        ctx,
        screenPos.x - 3 * s,
        screenPos.y + 2 * s,
        16 * s,
        5 * s,
        decorX * 8.3 + decorY * 5.1,
        0.12
      );
      ctx.fill();

      // Rock overhang - back face
      ctx.fillStyle = rockDark;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 22 * s, screenPos.y - 15 * s);
      ctx.lineTo(screenPos.x - 18 * s, screenPos.y - 30 * s);
      ctx.lineTo(screenPos.x + 20 * s, screenPos.y - 28 * s);
      ctx.lineTo(screenPos.x + 24 * s, screenPos.y - 15 * s);
      ctx.closePath();
      ctx.fill();

      // Rock overhang - top face
      ctx.fillStyle = rockLight;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 18 * s, screenPos.y - 30 * s);
      ctx.lineTo(screenPos.x - 12 * s, screenPos.y - 36 * s);
      ctx.lineTo(screenPos.x + 22 * s, screenPos.y - 34 * s);
      ctx.lineTo(screenPos.x + 20 * s, screenPos.y - 28 * s);
      ctx.closePath();
      ctx.fill();

      // Snow on top of overhang
      ctx.fillStyle = "#ffffff";
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 15 * s, screenPos.y - 34 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 5 * s,
        screenPos.y - 40 * s,
        screenPos.x + 5 * s,
        screenPos.y - 37 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 15 * s,
        screenPos.y - 39 * s,
        screenPos.x + 22 * s,
        screenPos.y - 34 * s
      );
      ctx.lineTo(screenPos.x + 20 * s, screenPos.y - 28 * s);
      ctx.lineTo(screenPos.x - 18 * s, screenPos.y - 30 * s);
      ctx.closePath();
      ctx.fill();

      // Rock overhang - front face
      ctx.fillStyle = rockGray;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 22 * s, screenPos.y - 15 * s);
      ctx.lineTo(screenPos.x - 18 * s, screenPos.y - 30 * s);
      ctx.lineTo(screenPos.x - 12 * s, screenPos.y - 36 * s);
      ctx.lineTo(screenPos.x - 16 * s, screenPos.y - 18 * s);
      ctx.closePath();
      ctx.fill();

      // Individual icicles hanging from the overhang
      const icicles = [
        { dx: -16, h: 28, w: 3.5 },
        { dx: -11, h: 35, w: 4 },
        { dx: -6, h: 22, w: 3 },
        { dx: -1, h: 40, w: 4.5 },
        { dx: 4, h: 30, w: 3.5 },
        { dx: 9, h: 25, w: 3 },
        { dx: 14, h: 32, w: 4 },
        { dx: 19, h: 18, w: 2.5 },
      ];

      icicles.forEach((ic, idx) => {
        const ix = screenPos.x + ic.dx * s;
        const iy = screenPos.y - 15 * s;
        const ih = ic.h * s;
        const iw = ic.w * s;

        // Icicle left face
        const leftGrad = ctx.createLinearGradient(ix - iw, iy, ix, iy + ih);
        leftGrad.addColorStop(0, icicleMid);
        leftGrad.addColorStop(0.4, icicleBlue);
        leftGrad.addColorStop(1, "rgba(179,229,252,0.3)");
        ctx.fillStyle = leftGrad;
        ctx.beginPath();
        ctx.moveTo(ix - iw, iy);
        ctx.lineTo(ix, iy + 2 * s);
        ctx.lineTo(ix, iy + ih);
        ctx.lineTo(ix - iw * 0.3, iy + ih * 0.7);
        ctx.closePath();
        ctx.fill();

        // Icicle right face
        const rightGrad = ctx.createLinearGradient(ix, iy, ix + iw, iy + ih);
        rightGrad.addColorStop(0, icicleDark);
        rightGrad.addColorStop(0.5, icicleDeep);
        rightGrad.addColorStop(1, "rgba(41,182,246,0.2)");
        ctx.fillStyle = rightGrad;
        ctx.beginPath();
        ctx.moveTo(ix + iw, iy);
        ctx.lineTo(ix, iy + 2 * s);
        ctx.lineTo(ix, iy + ih);
        ctx.lineTo(ix + iw * 0.3, iy + ih * 0.7);
        ctx.closePath();
        ctx.fill();

        // Center highlight
        ctx.fillStyle = "rgba(255,255,255,0.5)";
        ctx.beginPath();
        ctx.moveTo(ix - iw * 0.2, iy + 3 * s);
        ctx.lineTo(ix + iw * 0.1, iy + 4 * s);
        ctx.lineTo(ix, iy + ih * 0.6);
        ctx.closePath();
        ctx.fill();

        // Drip at tip
        if (idx % 3 === 0) {
          const dripPhase = (decorTime * 2 + idx) % 3;
          if (dripPhase < 1) {
            const dripY = iy + ih + dripPhase * 12 * s;
            const dripAlpha = 1 - dripPhase;
            ctx.fillStyle = `rgba(179,229,252,${dripAlpha * 0.8})`;
            ctx.beginPath();
            ctx.ellipse(ix, dripY, 1.5 * s, 2 * s, 0, 0, Math.PI * 2);
            ctx.fill();
          }
        }
      });

      // Prismatic light sparkles
      ctx.fillStyle = "rgba(255,255,255,0.9)";
      const iceSparkleTime = decorTime * 1.5;
      for (let sp = 0; sp < 5; sp++) {
        const phase = (iceSparkleTime + sp * 1.2) % 3;
        if (phase < 0.6) {
          const alpha = Math.sin((phase / 0.6) * Math.PI);
          const spx = screenPos.x + (sp * 8 - 16) * s;
          const spy = screenPos.y - 15 * s + Math.sin(sp * 2.1) * 10 * s;
          ctx.fillStyle = `rgba(255,255,255,${alpha * 0.9})`;
          ctx.beginPath();
          ctx.arc(spx, spy, 1.2 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      break;
    }

    case "frozen_pond": {
      const pondIceLight = "#e8f4fd";
      const pondIceMid = "#b3e5fc";
      const pondIceDark = "#81d4fa";
      const pondDeep = "#4fc3f7";
      const snowEdge = "#f5f9fc";
      const pondSeed = (dec.x || 0) * 19.3;

      const pondW = 38 * s;
      const pondD = 18 * s;

      // Ambient glow beneath ice
      const pondGlow = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y,
        0,
        screenPos.x,
        screenPos.y,
        pondW * 1.3
      );
      pondGlow.addColorStop(0, "rgba(100,180,240,0.2)");
      pondGlow.addColorStop(0.6, "rgba(100,180,240,0.08)");
      pondGlow.addColorStop(1, "transparent");
      ctx.fillStyle = pondGlow;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        pondW * 1.3,
        pondD * 1.3,
        pondSeed - 10,
        0.1
      );
      ctx.fill();

      // Snow bank around pond
      ctx.fillStyle = snowEdge;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        pondW + 6 * s,
        pondD + 4 * s,
        pondSeed + 30,
        0.16
      );
      ctx.fill();
      ctx.fillStyle = "#ffffff";
      drawOrganicWaterShape(
        ctx,
        screenPos.x - 2 * s,
        screenPos.y - 1 * s,
        pondW + 4 * s,
        pondD + 2 * s,
        pondSeed + 30,
        0.16
      );
      ctx.fill();

      // Ice surface - main gradient
      const iceGrad = ctx.createRadialGradient(
        screenPos.x - 8 * s,
        screenPos.y - 4 * s,
        0,
        screenPos.x,
        screenPos.y,
        pondW
      );
      iceGrad.addColorStop(0, pondIceLight);
      iceGrad.addColorStop(0.3, pondIceMid);
      iceGrad.addColorStop(0.7, pondIceDark);
      iceGrad.addColorStop(1, pondDeep);
      ctx.fillStyle = iceGrad;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        pondW,
        pondD,
        pondSeed,
        0.15
      );
      ctx.fill();

      // Dark depth beneath ice
      ctx.fillStyle = "rgba(25,118,210,0.2)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 5 * s,
        screenPos.y + 2 * s,
        pondW * 0.6,
        pondD * 0.5,
        0.2,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Ice cracks pattern
      ctx.strokeStyle = "rgba(255,255,255,0.6)";
      ctx.lineWidth = 1.2 * s;
      const crackSeed = (dec.x || 0) * 5.3;
      // Main crack
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 12 * s, screenPos.y - 4 * s);
      ctx.lineTo(screenPos.x - 3 * s, screenPos.y + 1 * s);
      ctx.lineTo(screenPos.x + 8 * s, screenPos.y - 2 * s);
      ctx.lineTo(screenPos.x + 18 * s, screenPos.y + 3 * s);
      ctx.stroke();
      // Branch cracks
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 3 * s, screenPos.y + 1 * s);
      ctx.lineTo(screenPos.x - 8 * s, screenPos.y + 6 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 8 * s, screenPos.y - 2 * s);
      ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 8 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 8 * s, screenPos.y - 2 * s);
      ctx.lineTo(screenPos.x + 14 * s, screenPos.y + 6 * s);
      ctx.stroke();

      // Surface frost patches
      ctx.fillStyle = "rgba(255,255,255,0.35)";
      const frostPatches = [
        { dx: -18, dy: -2, rx: 8, ry: 4 },
        { dx: 10, dy: -6, rx: 6, ry: 3 },
        { dx: -5, dy: 5, rx: 10, ry: 4 },
        { dx: 20, dy: 2, rx: 7, ry: 3 },
      ];
      frostPatches.forEach((fp) => {
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + fp.dx * s,
          screenPos.y + fp.dy * s,
          fp.rx * s,
          fp.ry * s,
          crackSeed * 0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // Specular reflection
      ctx.fillStyle = "rgba(255,255,255,0.5)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - 12 * s,
        screenPos.y - 5 * s,
        8 * s,
        3.5 * s,
        -0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,0.3)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 14 * s,
        screenPos.y - 2 * s,
        5 * s,
        2 * s,
        0.2,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Subtle pulsing glow from beneath
      const pondPulse = 0.15 + Math.sin(decorTime * 1.5) * 0.08;
      ctx.fillStyle = `rgba(100,180,255,${pondPulse})`;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y,
        pondW * 0.5,
        pondD * 0.4,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Tiny snow particles on ice
      ctx.fillStyle = "rgba(255,255,255,0.7)";
      for (let p = 0; p < 8; p++) {
        const px =
          screenPos.x + Math.cos(crackSeed + p * 0.8) * (10 + p * 3) * s;
        const py =
          screenPos.y + Math.sin(crackSeed + p * 1.1) * (5 + p * 1.5) * s;
        ctx.beginPath();
        ctx.arc(px, py, 0.8 * s, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }

    case "frozen_soldier": {
      const fsVar = variant % 3;

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        screenPos.x,
        screenPos.y + 4 * s,
        s,
        18 * s,
        8 * s,
        40 * s,
        0.3,
        "0,60,80"
      );

      // Snow mound base
      const fsSnowG = ctx.createRadialGradient(
        screenPos.x - 3 * s,
        screenPos.y,
        0,
        screenPos.x,
        screenPos.y + 3 * s,
        20 * s
      );
      fsSnowG.addColorStop(0, "#ffffff");
      fsSnowG.addColorStop(0.4, "#eaf2f8");
      fsSnowG.addColorStop(0.8, "#d0dfe8");
      fsSnowG.addColorStop(1, "#b8ceda");
      ctx.fillStyle = fsSnowG;
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y + 3 * s,
        20 * s,
        8 * s,
        decorX * 5.3 + decorY * 7.9
      );
      ctx.fill();

      // Isometric ice block prism with seed-based variation
      const TRUE_ISO = 1 / Math.sqrt(3);
      const iceSeed = decorX * 13.7 + decorY * 29.3;
      const srand = (n: number) => {
        const x = Math.sin(iceSeed * 127.1 + n * 311.7) * 43_758.5453;
        return x - Math.floor(x);
      };
      const iceRough = srand(40) * 2.5 * s;

      const fsDrawIce = (
        bx: number,
        by: number,
        hw: number,
        bh: number,
        depthR: number,
        tiltX: number
      ) => {
        const hd = hw * depthR * TRUE_ISO;

        const perturb = (idx: number) => ({
          x: (srand(50 + idx * 2) - 0.5) * iceRough,
          y: (srand(50 + idx * 2 + 1) - 0.5) * iceRough,
        });
        const p0 = perturb(0),
          p1 = perturb(1),
          p2 = perturb(2);
        const p4 = perturb(4),
          p5 = perturb(5),
          p6 = perturb(6),
          p7 = perturb(7);

        const bL = { x: bx - hw + p0.x, y: by + p0.y };
        const bF = { x: bx + p1.x, y: by + hd + p1.y };
        const bR = { x: bx + hw + p2.x, y: by + p2.y };
        const tL = { x: bx - hw + tiltX + p4.x, y: by - bh + p4.y };
        const tF = { x: bx + tiltX + p5.x, y: by + hd - bh + p5.y };
        const tR = { x: bx + hw + tiltX + p6.x, y: by - bh + p6.y };
        const tB = { x: bx + tiltX + p7.x, y: by - hd - bh + p7.y };

        // Left face (lit)
        const lG = ctx.createLinearGradient(bL.x, bL.y, tF.x, tF.y);
        lG.addColorStop(0, "#29B6F6");
        lG.addColorStop(0.3, "#4fc3f7");
        lG.addColorStop(0.65, "#81d4fa");
        lG.addColorStop(1, "#e1f5fe");
        ctx.fillStyle = lG;
        ctx.beginPath();
        ctx.moveTo(bL.x, bL.y);
        ctx.lineTo(bF.x, bF.y);
        ctx.lineTo(tF.x, tF.y);
        ctx.lineTo(tL.x, tL.y);
        ctx.closePath();
        ctx.fill();

        // Right face (shadow)
        const rG = ctx.createLinearGradient(bF.x, bF.y, tR.x, tR.y);
        rG.addColorStop(0, "#0288D1");
        rG.addColorStop(0.45, "#01579B");
        rG.addColorStop(1, "#003c5e");
        ctx.fillStyle = rG;
        ctx.beginPath();
        ctx.moveTo(bF.x, bF.y);
        ctx.lineTo(bR.x, bR.y);
        ctx.lineTo(tR.x, tR.y);
        ctx.lineTo(tF.x, tF.y);
        ctx.closePath();
        ctx.fill();

        // Top face (diamond)
        const tG = ctx.createLinearGradient(tL.x, tL.y, tR.x, tR.y);
        tG.addColorStop(0, "#e1f5fe");
        tG.addColorStop(0.5, "#e3f2fd");
        tG.addColorStop(1, "#b3e5fc");
        ctx.fillStyle = tG;
        ctx.beginPath();
        ctx.moveTo(tL.x, tL.y);
        ctx.lineTo(tF.x, tF.y);
        ctx.lineTo(tR.x, tR.y);
        ctx.lineTo(tB.x, tB.y);
        ctx.closePath();
        ctx.fill();

        // Translucency overlay on left face
        ctx.fillStyle = "rgba(255,255,255,0.08)";
        ctx.beginPath();
        ctx.moveTo(bL.x, bL.y);
        ctx.lineTo(bF.x, bF.y);
        ctx.lineTo(tF.x, tF.y);
        ctx.lineTo(tL.x, tL.y);
        ctx.closePath();
        ctx.fill();

        // Primary highlight streak (left face, seeded position)
        ctx.fillStyle = "rgba(255,255,255,0.38)";
        const ht0 = 0.12 + srand(300) * 0.1;
        const ht1 = ht0 + 0.08 + srand(301) * 0.06;
        ctx.beginPath();
        ctx.moveTo(
          bL.x + (bF.x - bL.x) * ht0 + (tL.x - bL.x) * 0.15,
          bL.y + (bF.y - bL.y) * ht0 - bh * 0.82
        );
        ctx.lineTo(
          bL.x + (bF.x - bL.x) * ht1 + (tL.x - bL.x) * 0.15,
          bL.y + (bF.y - bL.y) * ht1 - bh * 0.82
        );
        ctx.lineTo(
          bL.x + (bF.x - bL.x) * ht1 + (tL.x - bL.x) * 0.05,
          bL.y + (bF.y - bL.y) * ht1 - bh * 0.18
        );
        ctx.lineTo(
          bL.x + (bF.x - bL.x) * ht0 + (tL.x - bL.x) * 0.05,
          bL.y + (bF.y - bL.y) * ht0 - bh * 0.18
        );
        ctx.closePath();
        ctx.fill();

        // Secondary highlight (right face)
        ctx.fillStyle = "rgba(255,255,255,0.15)";
        const rht0 = 0.6 + srand(302) * 0.15;
        const rht1 = rht0 + 0.06 + srand(303) * 0.04;
        ctx.beginPath();
        ctx.moveTo(
          bF.x + (bR.x - bF.x) * rht0 + (tF.x - bF.x) * 0.2,
          bF.y + (bR.y - bF.y) * rht0 - bh * 0.75
        );
        ctx.lineTo(
          bF.x + (bR.x - bF.x) * rht1 + (tF.x - bF.x) * 0.2,
          bF.y + (bR.y - bF.y) * rht1 - bh * 0.75
        );
        ctx.lineTo(
          bF.x + (bR.x - bF.x) * rht1 + (tF.x - bF.x) * 0.08,
          bF.y + (bR.y - bF.y) * rht1 - bh * 0.15
        );
        ctx.lineTo(
          bF.x + (bR.x - bF.x) * rht0 + (tF.x - bF.x) * 0.08,
          bF.y + (bR.y - bF.y) * rht0 - bh * 0.15
        );
        ctx.closePath();
        ctx.fill();

        // Seeded crack patterns
        ctx.strokeStyle = "rgba(200,235,255,0.28)";
        ctx.lineWidth = 0.7 * s;
        const numCracks = 2 + Math.floor(srand(400) * 5);
        for (let cr = 0; cr < numCracks; cr++) {
          const onLeft = srand(410 + cr * 7) > 0.5;
          const startT = 0.15 + srand(410 + cr * 7 + 1) * 0.7;
          const startHf = 0.15 + srand(410 + cr * 7 + 2) * 0.7;
          let sx: number, sy: number;
          if (onLeft) {
            sx = bL.x + (bF.x - bL.x) * startT;
            sy = bL.y + (bF.y - bL.y) * startT - bh * startHf;
          } else {
            sx = bF.x + (bR.x - bF.x) * startT;
            sy = bF.y + (bR.y - bF.y) * startT - bh * startHf;
          }
          ctx.beginPath();
          ctx.moveTo(sx, sy);
          const segs = 2 + Math.floor(srand(410 + cr * 7 + 3) * 3);
          let cx = sx,
            cy = sy;
          for (let sg = 0; sg < segs; sg++) {
            cx += (srand(410 + cr * 7 + sg * 3 + 4) - 0.5) * hw * 0.35;
            cy += (srand(410 + cr * 7 + sg * 3 + 5) - 0.5) * bh * 0.25;
            ctx.lineTo(cx, cy);
            if (srand(410 + cr * 7 + sg * 3 + 6) > 0.55) {
              const bx2 = cx + (srand(500 + cr * sg) - 0.5) * hw * 0.2;
              const by2 = cy + (srand(501 + cr * sg) - 0.5) * bh * 0.12;
              ctx.lineTo(bx2, by2);
              ctx.moveTo(cx, cy);
            }
          }
          ctx.stroke();
        }

        // Internal glow
        const glowR = Math.max(hw, bh * 0.25);
        const iGlow = ctx.createRadialGradient(
          bx,
          by - bh * 0.5,
          0,
          bx,
          by - bh * 0.5,
          glowR
        );
        iGlow.addColorStop(0, "rgba(144,202,249,0.12)");
        iGlow.addColorStop(1, "transparent");
        ctx.fillStyle = iGlow;
        ctx.beginPath();
        ctx.ellipse(bx, by - bh * 0.5, hw * 0.65, bh * 0.3, 0, 0, Math.PI * 2);
        ctx.fill();

        // Frost crystal formations on top edges
        const numCrystals = 1 + Math.floor(srand(600) * 5);
        for (let fc = 0; fc < numCrystals; fc++) {
          const edgeT = srand(610 + fc * 4);
          const edgeIdx = Math.floor(srand(610 + fc * 4 + 1) * 4);
          let ex: number, ey: number;
          if (edgeIdx === 0) {
            ex = tL.x + (tF.x - tL.x) * edgeT;
            ey = tL.y + (tF.y - tL.y) * edgeT;
          } else if (edgeIdx === 1) {
            ex = tF.x + (tR.x - tF.x) * edgeT;
            ey = tF.y + (tR.y - tF.y) * edgeT;
          } else if (edgeIdx === 2) {
            ex = tR.x + (tB.x - tR.x) * edgeT;
            ey = tR.y + (tB.y - tR.y) * edgeT;
          } else {
            ex = tB.x + (tL.x - tB.x) * edgeT;
            ey = tB.y + (tL.y - tB.y) * edgeT;
          }
          const cH = (3 + srand(610 + fc * 4 + 2) * 9) * s;
          const cW = (0.8 + srand(610 + fc * 4 + 3) * 2) * s;
          const cG = ctx.createLinearGradient(ex, ey, ex, ey - cH);
          cG.addColorStop(0, "rgba(129,212,250,0.5)");
          cG.addColorStop(1, "rgba(225,245,254,0.25)");
          ctx.fillStyle = cG;
          ctx.beginPath();
          ctx.moveTo(ex - cW, ey);
          ctx.lineTo(ex + (srand(620 + fc) - 0.5) * cW * 0.5, ey - cH);
          ctx.lineTo(ex + cW, ey);
          ctx.closePath();
          ctx.fill();
        }

        // Edge frost lines along vertical edges
        ctx.strokeStyle = "rgba(255,255,255,0.15)";
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(bF.x, bF.y);
        ctx.lineTo(tF.x, tF.y);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(bL.x, bL.y);
        ctx.lineTo(tL.x, tL.y);
        ctx.stroke();
      };

      // Seed-varied ice block dimensions per instance
      const iceHW = (12 + srand(0) * 7) * s;
      const iceH = (48 + srand(1) * 18) * s;
      const iceDepthR = 0.7 + srand(2) * 0.6;
      const iceTilt = (srand(3) - 0.5) * 5 * s;

      if (fsVar === 0) {
        fsDrawIce(screenPos.x, screenPos.y, iceHW, iceH, iceDepthR, iceTilt);
      } else if (fsVar === 1) {
        fsDrawIce(
          screenPos.x,
          screenPos.y,
          iceHW * 0.85,
          iceH * 1.08,
          iceDepthR * 0.8,
          iceTilt + 2 * s
        );
      } else {
        fsDrawIce(
          screenPos.x,
          screenPos.y,
          iceHW * 1.1,
          iceH * 1.05,
          iceDepthR * 1.15,
          iceTilt - 1 * s
        );
      }

      if (fsVar === 0) {
        // Knight with sword and shield
        // Body torso with gradient
        const fsTorsoG = ctx.createLinearGradient(
          screenPos.x,
          screenPos.y - 7 * s,
          screenPos.x,
          screenPos.y - 29 * s
        );
        fsTorsoG.addColorStop(0, "rgba(55,75,95,0.55)");
        fsTorsoG.addColorStop(0.5, "rgba(75,95,115,0.5)");
        fsTorsoG.addColorStop(1, "rgba(95,115,135,0.4)");
        ctx.fillStyle = fsTorsoG;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 18 * s,
          6.5 * s,
          11.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Chainmail texture
        ctx.strokeStyle = "rgba(130,150,170,0.3)";
        ctx.lineWidth = 0.5 * s;
        for (let cm = 0; cm < 5; cm++) {
          ctx.beginPath();
          ctx.arc(
            screenPos.x,
            screenPos.y - 12 * s - cm * 3 * s,
            4.5 * s,
            0.4,
            Math.PI - 0.4
          );
          ctx.stroke();
        }
        // Belt
        ctx.strokeStyle = "rgba(80,60,40,0.4)";
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 12 * s,
          5.5 * s,
          2 * s,
          0,
          0.2,
          Math.PI - 0.2
        );
        ctx.stroke();
        // Belt buckle
        ctx.fillStyle = "rgba(180,160,100,0.35)";
        ctx.beginPath();
        ctx.rect(screenPos.x - 1.5 * s, screenPos.y - 13 * s, 3 * s, 2 * s);
        ctx.fill();
        // Full helm with gradient
        const fsHelmG = ctx.createRadialGradient(
          screenPos.x - 1 * s,
          screenPos.y - 36 * s,
          0,
          screenPos.x,
          screenPos.y - 34 * s,
          6 * s
        );
        fsHelmG.addColorStop(0, "rgba(120,140,160,0.45)");
        fsHelmG.addColorStop(0.6, "rgba(80,100,120,0.5)");
        fsHelmG.addColorStop(1, "rgba(55,75,95,0.55)");
        ctx.fillStyle = fsHelmG;
        ctx.beginPath();
        ctx.arc(screenPos.x, screenPos.y - 34 * s, 6 * s, 0, Math.PI * 2);
        ctx.fill();
        // Visor slit with interior glow
        ctx.fillStyle = "rgba(15,25,35,0.5)";
        ctx.fillRect(screenPos.x - 4 * s, screenPos.y - 35.5 * s, 8 * s, 2 * s);
        ctx.fillStyle = "rgba(80,160,200,0.1)";
        ctx.fillRect(screenPos.x - 3 * s, screenPos.y - 35 * s, 6 * s, 1 * s);
        // Helmet crest
        const fsCrestG = ctx.createLinearGradient(
          screenPos.x,
          screenPos.y - 43 * s,
          screenPos.x,
          screenPos.y - 37 * s
        );
        fsCrestG.addColorStop(0, "rgba(100,120,140,0.5)");
        fsCrestG.addColorStop(1, "rgba(65,85,105,0.55)");
        ctx.fillStyle = fsCrestG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x, screenPos.y - 43 * s);
        ctx.lineTo(screenPos.x - 2.5 * s, screenPos.y - 37 * s);
        ctx.lineTo(screenPos.x + 2.5 * s, screenPos.y - 37 * s);
        ctx.closePath();
        ctx.fill();
        // Sword arm
        ctx.strokeStyle = "rgba(65,85,105,0.55)";
        ctx.lineWidth = 3 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 5.5 * s, screenPos.y - 23 * s);
        ctx.lineTo(screenPos.x + 11 * s, screenPos.y - 31 * s);
        ctx.stroke();
        // Sword with gradient
        const fsSwordG = ctx.createLinearGradient(
          screenPos.x + 10 * s,
          screenPos.y - 31 * s,
          screenPos.x + 8 * s,
          screenPos.y - 48 * s
        );
        fsSwordG.addColorStop(0, "rgba(160,180,200,0.5)");
        fsSwordG.addColorStop(0.5, "rgba(200,215,230,0.45)");
        fsSwordG.addColorStop(1, "rgba(220,235,250,0.4)");
        ctx.strokeStyle = fsSwordG;
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 10 * s, screenPos.y - 31 * s);
        ctx.lineTo(screenPos.x + 8 * s, screenPos.y - 48 * s);
        ctx.stroke();
        // Fuller line
        ctx.strokeStyle = "rgba(220,240,255,0.3)";
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 9.5 * s, screenPos.y - 33 * s);
        ctx.lineTo(screenPos.x + 8.3 * s, screenPos.y - 46 * s);
        ctx.stroke();
        // Crossguard
        ctx.strokeStyle = "rgba(170,150,100,0.4)";
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 8 * s, screenPos.y - 31 * s);
        ctx.lineTo(screenPos.x + 12 * s, screenPos.y - 30 * s);
        ctx.stroke();
        // Shield with gradient
        const fsShieldG = ctx.createLinearGradient(
          screenPos.x - 12 * s,
          screenPos.y - 28 * s,
          screenPos.x - 5 * s,
          screenPos.y - 14 * s
        );
        fsShieldG.addColorStop(0, "rgba(100,120,140,0.45)");
        fsShieldG.addColorStop(0.5, "rgba(80,100,120,0.5)");
        fsShieldG.addColorStop(1, "rgba(60,80,100,0.55)");
        ctx.fillStyle = fsShieldG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 8 * s, screenPos.y - 29 * s);
        ctx.lineTo(screenPos.x - 13 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x - 8 * s, screenPos.y - 13 * s);
        ctx.lineTo(screenPos.x - 4 * s, screenPos.y - 22 * s);
        ctx.closePath();
        ctx.fill();
        // Shield emblem (cross)
        ctx.strokeStyle = "rgba(140,160,180,0.35)";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 8 * s, screenPos.y - 25 * s);
        ctx.lineTo(screenPos.x - 8 * s, screenPos.y - 19 * s);
        ctx.moveTo(screenPos.x - 11 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x - 5 * s, screenPos.y - 22 * s);
        ctx.stroke();
        // Legs with greaves
        ctx.strokeStyle = "rgba(65,85,105,0.55)";
        ctx.lineWidth = 3 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 2 * s, screenPos.y - 7 * s);
        ctx.lineTo(screenPos.x - 4.5 * s, screenPos.y + 2 * s);
        ctx.moveTo(screenPos.x + 2 * s, screenPos.y - 7 * s);
        ctx.lineTo(screenPos.x + 4.5 * s, screenPos.y + 2 * s);
        ctx.stroke();
      } else if (fsVar === 1) {
        // Archer frozen mid-draw
        const fsArchTorso = ctx.createLinearGradient(
          screenPos.x,
          screenPos.y - 8 * s,
          screenPos.x,
          screenPos.y - 28 * s
        );
        fsArchTorso.addColorStop(0, "rgba(55,75,95,0.55)");
        fsArchTorso.addColorStop(0.5, "rgba(70,90,110,0.5)");
        fsArchTorso.addColorStop(1, "rgba(85,105,125,0.4)");
        ctx.fillStyle = fsArchTorso;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 1 * s,
          screenPos.y - 18 * s,
          5.5 * s,
          10.5 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Hooded head with gradient
        const fsHoodG = ctx.createRadialGradient(
          screenPos.x - 1 * s,
          screenPos.y - 35 * s,
          0,
          screenPos.x,
          screenPos.y - 33 * s,
          6 * s
        );
        fsHoodG.addColorStop(0, "rgba(105,125,145,0.45)");
        fsHoodG.addColorStop(0.7, "rgba(75,95,115,0.5)");
        fsHoodG.addColorStop(1, "rgba(55,75,95,0.55)");
        ctx.fillStyle = fsHoodG;
        ctx.beginPath();
        ctx.arc(screenPos.x, screenPos.y - 33 * s, 5.5 * s, 0, Math.PI * 2);
        ctx.fill();
        // Hood peak
        ctx.fillStyle = "rgba(60,80,100,0.55)";
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 5.5 * s, screenPos.y - 34 * s);
        ctx.quadraticCurveTo(
          screenPos.x,
          screenPos.y - 43 * s,
          screenPos.x + 3 * s,
          screenPos.y - 36 * s
        );
        ctx.closePath();
        ctx.fill();
        // Face shadow
        ctx.fillStyle = "rgba(20,35,50,0.3)";
        ctx.beginPath();
        ctx.arc(screenPos.x, screenPos.y - 33 * s, 3.5 * s, 0, Math.PI * 2);
        ctx.fill();
        // Bow arm extended
        ctx.strokeStyle = "rgba(65,85,105,0.55)";
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 4 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x - 13 * s, screenPos.y - 25 * s);
        ctx.stroke();
        // Bow with gradient
        ctx.strokeStyle = "rgba(110,80,50,0.45)";
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 13 * s, screenPos.y - 37 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 17 * s,
          screenPos.y - 25 * s,
          screenPos.x - 13 * s,
          screenPos.y - 13 * s
        );
        ctx.stroke();
        // Bow highlight
        ctx.strokeStyle = "rgba(160,130,90,0.2)";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 12.5 * s, screenPos.y - 35 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 15 * s,
          screenPos.y - 25 * s,
          screenPos.x - 12.5 * s,
          screenPos.y - 15 * s
        );
        ctx.stroke();
        // Bowstring
        ctx.strokeStyle = "rgba(200,200,200,0.35)";
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 13 * s, screenPos.y - 37 * s);
        ctx.lineTo(screenPos.x - 13 * s, screenPos.y - 13 * s);
        ctx.stroke();
        // Draw arm pulled back
        ctx.strokeStyle = "rgba(65,85,105,0.55)";
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 4 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x + 3 * s, screenPos.y - 28 * s);
        ctx.stroke();
        // Arrow
        ctx.strokeStyle = "rgba(140,105,65,0.45)";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 3 * s, screenPos.y - 28 * s);
        ctx.lineTo(screenPos.x - 15 * s, screenPos.y - 25 * s);
        ctx.stroke();
        // Arrowhead
        ctx.fillStyle = "rgba(180,200,220,0.4)";
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 15 * s, screenPos.y - 25 * s);
        ctx.lineTo(screenPos.x - 13 * s, screenPos.y - 26.5 * s);
        ctx.lineTo(screenPos.x - 13 * s, screenPos.y - 23.5 * s);
        ctx.closePath();
        ctx.fill();
        // Quiver with gradient
        const fsQuiverG = ctx.createLinearGradient(
          screenPos.x + 3 * s,
          screenPos.y - 30 * s,
          screenPos.x + 7 * s,
          screenPos.y - 14 * s
        );
        fsQuiverG.addColorStop(0, "rgba(90,70,50,0.4)");
        fsQuiverG.addColorStop(1, "rgba(70,50,30,0.35)");
        ctx.fillStyle = fsQuiverG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 4 * s, screenPos.y - 31 * s);
        ctx.lineTo(screenPos.x + 7.5 * s, screenPos.y - 31 * s);
        ctx.lineTo(screenPos.x + 6.5 * s, screenPos.y - 13 * s);
        ctx.lineTo(screenPos.x + 3 * s, screenPos.y - 13 * s);
        ctx.closePath();
        ctx.fill();
        // Arrow fletching peeking
        ctx.strokeStyle = "rgba(170,180,190,0.35)";
        ctx.lineWidth = 0.8 * s;
        for (let af = 0; af < 3; af++) {
          ctx.beginPath();
          ctx.moveTo(screenPos.x + (4 + af * 1.2) * s, screenPos.y - 31 * s);
          ctx.lineTo(screenPos.x + (4 + af * 1.2) * s, screenPos.y - 34 * s);
          ctx.stroke();
        }
        // Legs
        ctx.strokeStyle = "rgba(65,85,105,0.55)";
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 1 * s, screenPos.y - 8 * s);
        ctx.lineTo(screenPos.x - 3 * s, screenPos.y + 2 * s);
        ctx.moveTo(screenPos.x + 3 * s, screenPos.y - 8 * s);
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y + 2 * s);
        ctx.stroke();
      } else {
        // Spearman with banner
        const fsSpearTorso = ctx.createLinearGradient(
          screenPos.x,
          screenPos.y - 7 * s,
          screenPos.x,
          screenPos.y - 29 * s
        );
        fsSpearTorso.addColorStop(0, "rgba(55,75,95,0.55)");
        fsSpearTorso.addColorStop(0.5, "rgba(70,90,110,0.5)");
        fsSpearTorso.addColorStop(1, "rgba(85,105,125,0.42)");
        ctx.fillStyle = fsSpearTorso;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 18 * s,
          7 * s,
          11.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Shoulder pauldrons with gradient
        const fsPauld = "rgba(85,105,125,0.48)";
        ctx.fillStyle = fsPauld;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 6.5 * s,
          screenPos.y - 26 * s,
          3.5 * s,
          2.5 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 6.5 * s,
          screenPos.y - 26 * s,
          3.5 * s,
          2.5 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Pauldron rivet dots
        ctx.fillStyle = "rgba(140,160,180,0.3)";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 6.5 * s,
          screenPos.y - 26 * s,
          0.8 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.arc(
          screenPos.x + 6.5 * s,
          screenPos.y - 26 * s,
          0.8 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Bucket helm gradient
        const fsBucketG = ctx.createLinearGradient(
          screenPos.x - 5.5 * s,
          screenPos.y - 41 * s,
          screenPos.x + 5.5 * s,
          screenPos.y - 28 * s
        );
        fsBucketG.addColorStop(0, "rgba(100,120,140,0.45)");
        fsBucketG.addColorStop(0.4, "rgba(80,100,120,0.5)");
        fsBucketG.addColorStop(1, "rgba(60,80,100,0.55)");
        ctx.fillStyle = fsBucketG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 5.5 * s, screenPos.y - 28 * s);
        ctx.lineTo(screenPos.x - 5.5 * s, screenPos.y - 41 * s);
        ctx.lineTo(screenPos.x + 5.5 * s, screenPos.y - 41 * s);
        ctx.lineTo(screenPos.x + 5.5 * s, screenPos.y - 28 * s);
        ctx.closePath();
        ctx.fill();
        // Helm slit with interior glow
        ctx.fillStyle = "rgba(15,25,35,0.5)";
        ctx.fillRect(screenPos.x - 4.5 * s, screenPos.y - 37 * s, 9 * s, 2 * s);
        ctx.fillStyle = "rgba(80,160,200,0.08)";
        ctx.fillRect(
          screenPos.x - 3.5 * s,
          screenPos.y - 36.5 * s,
          7 * s,
          1 * s
        );
        // Helm highlight
        ctx.fillStyle = "rgba(255,255,255,0.06)";
        ctx.fillRect(
          screenPos.x - 4 * s,
          screenPos.y - 40 * s,
          2.5 * s,
          11 * s
        );
        // Spear shaft with gradient
        const fsShaftG = ctx.createLinearGradient(
          screenPos.x + 6 * s,
          screenPos.y + 2 * s,
          screenPos.x + 5 * s,
          screenPos.y - 50 * s
        );
        fsShaftG.addColorStop(0, "rgba(100,75,50,0.45)");
        fsShaftG.addColorStop(0.5, "rgba(130,100,70,0.4)");
        fsShaftG.addColorStop(1, "rgba(100,75,50,0.45)");
        ctx.strokeStyle = fsShaftG;
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 6 * s, screenPos.y + 2 * s);
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 50 * s);
        ctx.stroke();
        // Spearhead gradient
        const fsSpearH = ctx.createLinearGradient(
          screenPos.x + 5 * s,
          screenPos.y - 50 * s,
          screenPos.x + 5 * s,
          screenPos.y - 44 * s
        );
        fsSpearH.addColorStop(0, "rgba(210,225,240,0.5)");
        fsSpearH.addColorStop(1, "rgba(170,190,210,0.45)");
        ctx.fillStyle = fsSpearH;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 5 * s, screenPos.y - 50 * s);
        ctx.lineTo(screenPos.x + 2.5 * s, screenPos.y - 44 * s);
        ctx.lineTo(screenPos.x + 7.5 * s, screenPos.y - 44 * s);
        ctx.closePath();
        ctx.fill();
        // Tattered banner
        const fsBannerG = ctx.createLinearGradient(
          screenPos.x + 5 * s,
          screenPos.y - 45 * s,
          screenPos.x + 13 * s,
          screenPos.y - 39 * s
        );
        fsBannerG.addColorStop(0, "rgba(100,45,45,0.38)");
        fsBannerG.addColorStop(0.5, "rgba(120,55,55,0.32)");
        fsBannerG.addColorStop(1, "rgba(80,35,35,0.28)");
        ctx.fillStyle = fsBannerG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 5 * s, screenPos.y - 45 * s);
        ctx.lineTo(screenPos.x + 13 * s, screenPos.y - 43 * s);
        ctx.lineTo(screenPos.x + 11 * s, screenPos.y - 39 * s);
        ctx.lineTo(screenPos.x + 13 * s, screenPos.y - 37 * s);
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 39 * s);
        ctx.closePath();
        ctx.fill();
        // Shield with gradient
        const fsSpShieldG = ctx.createLinearGradient(
          screenPos.x - 13 * s,
          screenPos.y - 27 * s,
          screenPos.x - 4 * s,
          screenPos.y - 12 * s
        );
        fsSpShieldG.addColorStop(0, "rgba(90,110,130,0.48)");
        fsSpShieldG.addColorStop(0.5, "rgba(75,95,115,0.5)");
        fsSpShieldG.addColorStop(1, "rgba(60,80,100,0.52)");
        ctx.fillStyle = fsSpShieldG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 7 * s, screenPos.y - 27 * s);
        ctx.lineTo(screenPos.x - 13 * s, screenPos.y - 20 * s);
        ctx.lineTo(screenPos.x - 7 * s, screenPos.y - 11 * s);
        ctx.lineTo(screenPos.x - 3 * s, screenPos.y - 20 * s);
        ctx.closePath();
        ctx.fill();
        // Shield boss
        ctx.fillStyle = "rgba(130,150,170,0.3)";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 7.5 * s,
          screenPos.y - 20 * s,
          2.5 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "rgba(160,180,200,0.2)";
        ctx.beginPath();
        ctx.arc(
          screenPos.x - 7.5 * s,
          screenPos.y - 20 * s,
          1.2 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Legs
        ctx.strokeStyle = "rgba(65,85,105,0.55)";
        ctx.lineWidth = 3 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 2 * s, screenPos.y - 7 * s);
        ctx.lineTo(screenPos.x - 4.5 * s, screenPos.y + 2 * s);
        ctx.moveTo(screenPos.x + 2 * s, screenPos.y - 7 * s);
        ctx.lineTo(screenPos.x + 4.5 * s, screenPos.y + 2 * s);
        ctx.stroke();
      }

      // Frost bloom around ice block
      const fsFrostB = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y - 25 * s,
        0,
        screenPos.x,
        screenPos.y - 25 * s,
        20 * s
      );
      fsFrostB.addColorStop(0, "rgba(144,202,249,0.1)");
      fsFrostB.addColorStop(1, "transparent");
      ctx.fillStyle = fsFrostB;
      ctx.beginPath();
      ctx.arc(screenPos.x, screenPos.y - 25 * s, 20 * s, 0, Math.PI * 2);
      ctx.fill();

      // Frost particles
      ctx.fillStyle = "rgba(210,240,255,0.65)";
      for (let fsp = 0; fsp < 8; fsp++) {
        const frostA = (fsp / 8) * Math.PI * 2 + decorTime * 0.4;
        const frostRad = (16 + Math.sin(decorTime * 0.7 + fsp * 1.1) * 4) * s;
        const fspSize = (0.7 + Math.sin(decorTime + fsp * 0.8) * 0.3) * s;
        ctx.beginPath();
        ctx.arc(
          screenPos.x + Math.cos(frostA) * frostRad * 0.8,
          screenPos.y - 25 * s + Math.sin(frostA) * frostRad * 0.4,
          fspSize,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }

    case "frozen_gate": {
      const gateStone = "#7a8a9a";
      const gateStoneDark = "#5a6a7a";
      const gateStoneLight = "#9aaaba";
      const gateIce = "#b3e5fc";
      const gateIceDark = "#4fc3f7";

      // Ground shadow
      ctx.fillStyle = "rgba(0,40,60,0.25)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 10 * s,
        35 * s,
        14 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Snow base
      ctx.fillStyle = "#eef4f8";
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y + 5 * s,
        30 * s,
        10 * s,
        decorX * 7.1 + decorY * 5.3
      );
      ctx.fill();

      // Isometric brick pillars
      const fpw = 6 * s;
      const fpH = 55 * s;
      for (const fpx of [screenPos.x - 16 * s, screenPos.x + 16 * s]) {
        const fpy = screenPos.y + 3 * s;
        drawBrickFace(
          ctx,
          fpx,
          fpy,
          fpx + fpw,
          fpy - fpw * 0.5,
          fpH,
          gateStoneLight,
          "rgba(60,70,80,0.3)",
          s,
          12,
          2
        );
        drawBrickFace(
          ctx,
          fpx - fpw,
          fpy - fpw * 0.5,
          fpx,
          fpy,
          fpH,
          gateStoneDark,
          "rgba(40,50,60,0.35)",
          s,
          12,
          2
        );
        ctx.fillStyle = gateStone;
        ctx.beginPath();
        ctx.moveTo(fpx, fpy - fpH);
        ctx.lineTo(fpx + fpw, fpy - fpw * 0.5 - fpH);
        ctx.lineTo(fpx, fpy - fpw - fpH);
        ctx.lineTo(fpx - fpw, fpy - fpw * 0.5 - fpH);
        ctx.closePath();
        ctx.fill();
      }

      // Stone archway
      ctx.fillStyle = gateStoneDark;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 22 * s, screenPos.y - 52 * s);
      ctx.quadraticCurveTo(
        screenPos.x,
        screenPos.y - 70 * s,
        screenPos.x + 22 * s,
        screenPos.y - 52 * s
      );
      ctx.lineTo(screenPos.x + 22 * s, screenPos.y - 48 * s);
      ctx.quadraticCurveTo(
        screenPos.x,
        screenPos.y - 64 * s,
        screenPos.x - 22 * s,
        screenPos.y - 48 * s
      );
      ctx.closePath();
      ctx.fill();

      // Arch highlight
      ctx.strokeStyle = gateStoneLight;
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 20 * s, screenPos.y - 50 * s);
      ctx.quadraticCurveTo(
        screenPos.x,
        screenPos.y - 66 * s,
        screenPos.x + 20 * s,
        screenPos.y - 50 * s
      );
      ctx.stroke();

      // Frozen portcullis bars
      for (let bar = 0; bar < 4; bar++) {
        const barX = screenPos.x - 10 * s + bar * 7 * s;
        const barTop = screenPos.y - 48 * s + Math.abs(bar - 1.5) * 5 * s;
        const barBottom = screenPos.y + 2 * s - bar * 2 * s;

        // Ice-covered bar
        const barGrad = ctx.createLinearGradient(
          barX - 2 * s,
          barTop,
          barX + 2 * s,
          barBottom
        );
        barGrad.addColorStop(0, gateIce);
        barGrad.addColorStop(0.5, gateIceDark);
        barGrad.addColorStop(1, gateIce);
        ctx.fillStyle = barGrad;
        ctx.fillRect(barX - 1.5 * s, barTop, 3 * s, barBottom - barTop);

        // Bar highlight
        ctx.fillStyle = "rgba(255,255,255,0.3)";
        ctx.fillRect(barX - 1 * s, barTop, 1 * s, (barBottom - barTop) * 0.8);
      }

      // Horizontal frozen bar across portcullis
      ctx.fillStyle = gateIceDark;
      ctx.fillRect(screenPos.x - 12 * s, screenPos.y - 25 * s, 24 * s, 3 * s);
      ctx.fillStyle = "rgba(255,255,255,0.25)";
      ctx.fillRect(screenPos.x - 12 * s, screenPos.y - 25 * s, 24 * s, 1 * s);

      // Ice buildup on pillars
      const drawIceOnPillar = (px: number, side: number) => {
        ctx.fillStyle = gateIce;
        ctx.beginPath();
        ctx.moveTo(px, screenPos.y - 45 * s);
        ctx.quadraticCurveTo(
          px + side * 6 * s,
          screenPos.y - 35 * s,
          px + side * 3 * s,
          screenPos.y - 20 * s
        );
        ctx.quadraticCurveTo(
          px + side * 5 * s,
          screenPos.y - 10 * s,
          px + side * 2 * s,
          screenPos.y
        );
        ctx.lineTo(px, screenPos.y);
        ctx.closePath();
        ctx.fill();

        ctx.fillStyle = "rgba(255,255,255,0.3)";
        ctx.beginPath();
        ctx.moveTo(px, screenPos.y - 42 * s);
        ctx.quadraticCurveTo(
          px + side * 3 * s,
          screenPos.y - 32 * s,
          px + side * 1 * s,
          screenPos.y - 18 * s
        );
        ctx.lineTo(px, screenPos.y - 20 * s);
        ctx.closePath();
        ctx.fill();
      };
      drawIceOnPillar(screenPos.x - 14 * s, -1);
      drawIceOnPillar(screenPos.x + 14 * s, 1);

      // Snow on top of arch
      ctx.fillStyle = "#ffffff";
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 20 * s, screenPos.y - 52 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 10 * s,
        screenPos.y - 58 * s,
        screenPos.x,
        screenPos.y - 68 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 5 * s,
        screenPos.y - 72 * s,
        screenPos.x,
        screenPos.y - 73 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x - 5 * s,
        screenPos.y - 72 * s,
        screenPos.x,
        screenPos.y - 68 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 10 * s,
        screenPos.y - 58 * s,
        screenPos.x + 20 * s,
        screenPos.y - 52 * s
      );
      ctx.lineTo(screenPos.x + 22 * s, screenPos.y - 52 * s);
      ctx.quadraticCurveTo(
        screenPos.x,
        screenPos.y - 74 * s,
        screenPos.x - 22 * s,
        screenPos.y - 52 * s
      );
      ctx.closePath();
      ctx.fill();

      // Pillar cap snow
      ctx.fillStyle = "#ffffff";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - 19 * s,
        screenPos.y - 55 * s,
        7 * s,
        3 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 19 * s,
        screenPos.y - 55 * s,
        7 * s,
        3 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Stone detail - cracks
      ctx.strokeStyle = "rgba(0,0,0,0.15)";
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 20 * s, screenPos.y - 30 * s);
      ctx.lineTo(screenPos.x - 16 * s, screenPos.y - 28 * s);
      ctx.moveTo(screenPos.x + 18 * s, screenPos.y - 40 * s);
      ctx.lineTo(screenPos.x + 22 * s, screenPos.y - 36 * s);
      ctx.stroke();
      break;
    }

    case "broken_wall": {
      drawBrokenWallDecoration(
        ctx,
        screenPos,
        s,
        variant,
        decorX,
        decorY,
        mapTheme
      );
      break;
    }

    case "frozen_waterfall": {
      const fwIceWhite = "#eef6ff";
      const fwIceLight = "#d6eaf8";
      const fwIceMid = "#a8d8ea";
      const fwIceDark = "#5dade2";
      const fwIceDeep = "#2e86c1";
      const fwIceAbyss = "#1a5276";
      const fwRock = "#5d6d7e";
      const fwRockMid = "#4a5a6a";
      const fwRockDark = "#2c3e50";
      const fwSnow = "#f0f4f8";
      const cx = screenPos.x;
      const cy = screenPos.y;

      // --- Frost tendrils radiating into ground (environment integration) ---
      const tendrilCount = 12;
      for (let t = 0; t < tendrilCount; t++) {
        const angle = (t / tendrilCount) * Math.PI * 2;
        const tendrilLen = (22 + Math.sin(decorX * 3 + t * 2.1) * 10) * s;
        const tendrilAlpha = 0.08 + Math.sin(decorTime * 0.4 + t * 1.3) * 0.03;
        const tx = cx + Math.cos(angle) * tendrilLen;
        const ty = cy + 6 * s + Math.sin(angle) * tendrilLen * 0.4;
        const tendrilGrad = ctx.createLinearGradient(cx, cy + 6 * s, tx, ty);
        tendrilGrad.addColorStop(0, `rgba(180,220,255,${tendrilAlpha + 0.04})`);
        tendrilGrad.addColorStop(0.6, `rgba(200,235,255,${tendrilAlpha})`);
        tendrilGrad.addColorStop(1, "rgba(200,235,255,0)");
        ctx.strokeStyle = tendrilGrad;
        ctx.lineWidth = (1.5 + Math.sin(t * 1.7) * 0.5) * s;
        ctx.beginPath();
        ctx.moveTo(cx, cy + 6 * s);
        const cpx = cx + Math.cos(angle) * tendrilLen * 0.5;
        const cpy =
          cy +
          6 * s +
          Math.sin(angle) * tendrilLen * 0.2 +
          Math.sin(t * 3.1) * 4 * s;
        ctx.quadraticCurveTo(cpx, cpy, tx, ty);
        ctx.stroke();
      }

      // --- Large ambient glow (frost integration) ---
      const fwGlow = ctx.createRadialGradient(
        cx,
        cy - 15 * s,
        0,
        cx,
        cy - 15 * s,
        55 * s
      );
      const glowPulse = 0.12 + Math.sin(decorTime * 0.5) * 0.03;
      fwGlow.addColorStop(0, `rgba(100,180,240,${glowPulse})`);
      fwGlow.addColorStop(0.4, `rgba(130,200,255,${glowPulse * 0.5})`);
      fwGlow.addColorStop(1, "rgba(130,200,255,0)");
      ctx.fillStyle = fwGlow;
      ctx.beginPath();
      ctx.ellipse(cx, cy - 15 * s, 55 * s, 40 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // --- Frozen pool with cracked ice ---
      const poolCx = cx + 1 * s;
      const poolCy = cy + 7 * s;
      const poolRx = 28 * s;
      const poolRy = 11 * s;

      // Pool outer frost ring
      const frostRingGrad = ctx.createRadialGradient(
        poolCx,
        poolCy,
        poolRx * 0.7,
        poolCx,
        poolCy,
        poolRx * 1.3
      );
      frostRingGrad.addColorStop(0, "rgba(200,230,255,0.15)");
      frostRingGrad.addColorStop(0.5, "rgba(210,235,255,0.08)");
      frostRingGrad.addColorStop(1, "rgba(220,240,255,0)");
      ctx.fillStyle = frostRingGrad;
      ctx.beginPath();
      ctx.ellipse(
        poolCx,
        poolCy,
        poolRx * 1.3,
        poolRy * 1.3,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Pool base
      const poolGrad = ctx.createRadialGradient(
        poolCx - 3 * s,
        poolCy - 2 * s,
        0,
        poolCx,
        poolCy,
        poolRx
      );
      poolGrad.addColorStop(0, fwIceWhite);
      poolGrad.addColorStop(0.3, fwIceLight);
      poolGrad.addColorStop(0.7, fwIceMid);
      poolGrad.addColorStop(1, fwIceDark);
      ctx.fillStyle = poolGrad;
      ctx.beginPath();
      ctx.ellipse(poolCx, poolCy, poolRx, poolRy, 0, 0, Math.PI * 2);
      ctx.fill();

      // Pool ice cracks
      ctx.strokeStyle = "rgba(255,255,255,0.35)";
      ctx.lineWidth = 0.8 * s;
      const crackSeeds = [
        { a: -0.4, l: 0.8 },
        { a: 0.3, l: 0.7 },
        { a: -1.2, l: 0.6 },
        { a: 1, l: 0.9 },
        { a: 2.2, l: 0.5 },
        { a: -2.5, l: 0.65 },
        { a: 0.8, l: 0.55 },
      ];
      crackSeeds.forEach((crack) => {
        const cl = crack.l * poolRx;
        const endX = poolCx + Math.cos(crack.a) * cl;
        const endY = poolCy + Math.sin(crack.a) * cl * (poolRy / poolRx);
        const midX =
          poolCx + Math.cos(crack.a) * cl * 0.5 + Math.sin(crack.a * 3) * 3 * s;
        const midY =
          poolCy +
          Math.sin(crack.a) * cl * 0.5 * (poolRy / poolRx) +
          Math.cos(crack.a * 2) * 2 * s;
        ctx.beginPath();
        ctx.moveTo(poolCx, poolCy);
        ctx.quadraticCurveTo(midX, midY, endX, endY);
        ctx.stroke();
      });

      // Pool highlight
      ctx.fillStyle = "rgba(255,255,255,0.25)";
      ctx.beginPath();
      ctx.ellipse(
        poolCx - 7 * s,
        poolCy - 2 * s,
        10 * s,
        4 * s,
        -0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // --- Rocky cliff face (layered, textured) ---
      const cliffTop = cy - 72 * s;
      const cliffBottom = cy;

      // Back cliff face
      const backCliffGrad = ctx.createLinearGradient(
        cx,
        cliffTop,
        cx,
        cliffBottom
      );
      backCliffGrad.addColorStop(0, fwRockDark);
      backCliffGrad.addColorStop(0.4, fwRockMid);
      backCliffGrad.addColorStop(1, fwRock);
      ctx.fillStyle = backCliffGrad;
      ctx.beginPath();
      ctx.moveTo(cx - 20 * s, cliffBottom);
      ctx.lineTo(cx - 22 * s, cliffTop + 8 * s);
      ctx.lineTo(cx - 18 * s, cliffTop + 3 * s);
      ctx.lineTo(cx - 5 * s, cliffTop);
      ctx.lineTo(cx + 8 * s, cliffTop + 2 * s);
      ctx.lineTo(cx + 20 * s, cliffTop + 5 * s);
      ctx.lineTo(cx + 24 * s, cliffTop + 10 * s);
      ctx.lineTo(cx + 20 * s, cliffBottom);
      ctx.closePath();
      ctx.fill();

      // Left cliff face (lighter)
      const leftCliffGrad = ctx.createLinearGradient(
        cx - 28 * s,
        cliffTop,
        cx - 20 * s,
        cliffBottom
      );
      leftCliffGrad.addColorStop(0, fwRockMid);
      leftCliffGrad.addColorStop(0.5, fwRock);
      leftCliffGrad.addColorStop(1, "#6b7b8b");
      ctx.fillStyle = leftCliffGrad;
      ctx.beginPath();
      ctx.moveTo(cx - 26 * s, cliffBottom + 3 * s);
      ctx.lineTo(cx - 28 * s, cliffTop + 5 * s);
      ctx.lineTo(cx - 22 * s, cliffTop + 8 * s);
      ctx.lineTo(cx - 20 * s, cliffBottom);
      ctx.closePath();
      ctx.fill();

      // Right cliff face (darker)
      const rightCliffGrad = ctx.createLinearGradient(
        cx + 20 * s,
        cliffTop,
        cx + 28 * s,
        cliffBottom
      );
      rightCliffGrad.addColorStop(0, fwRockDark);
      rightCliffGrad.addColorStop(1, "#3a4a5a");
      ctx.fillStyle = rightCliffGrad;
      ctx.beginPath();
      ctx.moveTo(cx + 26 * s, cliffBottom + 3 * s);
      ctx.lineTo(cx + 28 * s, cliffTop + 7 * s);
      ctx.lineTo(cx + 24 * s, cliffTop + 10 * s);
      ctx.lineTo(cx + 20 * s, cliffBottom);
      ctx.closePath();
      ctx.fill();

      // Rock texture crevices
      ctx.strokeStyle = "rgba(0,0,0,0.15)";
      ctx.lineWidth = 0.7 * s;
      const crevices = [
        { x1: -16, x2: -14, y1: -20, y2: -8 },
        { x1: -10, x2: -12, y1: -45, y2: -30 },
        { x1: 8, x2: 6, y1: -55, y2: -38 },
        { x1: 15, x2: 17, y1: -35, y2: -18 },
        { x1: -6, x2: -3, y1: -15, y2: -5 },
        { x1: 12, x2: 10, y1: -12, y2: -3 },
      ];
      crevices.forEach((cr) => {
        ctx.beginPath();
        ctx.moveTo(cx + cr.x1 * s, cy + cr.y1 * s);
        ctx.lineTo(cx + cr.x2 * s, cy + cr.y2 * s);
        ctx.stroke();
      });

      // Ice patches on cliff (frozen seepage)
      const icePatches = [
        { rx: 4, ry: 8, x: -15, y: -50 },
        { rx: 3, ry: 10, x: 12, y: -40 },
        { rx: 3, ry: 6, x: -8, y: -25 },
        { rx: 2.5, ry: 7, x: 18, y: -22 },
      ];
      icePatches.forEach((ip) => {
        const ipGrad = ctx.createLinearGradient(
          cx + ip.x * s,
          cy + (ip.y - ip.ry) * s,
          cx + ip.x * s,
          cy + (ip.y + ip.ry) * s
        );
        ipGrad.addColorStop(0, "rgba(180,220,248,0.3)");
        ipGrad.addColorStop(0.5, "rgba(200,235,255,0.2)");
        ipGrad.addColorStop(1, "rgba(180,220,248,0.05)");
        ctx.fillStyle = ipGrad;
        ctx.beginPath();
        ctx.ellipse(
          cx + ip.x * s,
          cy + ip.y * s,
          ip.rx * s,
          ip.ry * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // --- Snow cap with organic drifts ---
      ctx.fillStyle = fwSnow;
      ctx.beginPath();
      ctx.moveTo(cx - 28 * s, cliffTop + 5 * s);
      ctx.quadraticCurveTo(
        cx - 22 * s,
        cliffTop - 6 * s,
        cx - 12 * s,
        cliffTop - 3 * s
      );
      ctx.quadraticCurveTo(cx - 5 * s, cliffTop - 8 * s, cx, cliffTop - 5 * s);
      ctx.quadraticCurveTo(
        cx + 8 * s,
        cliffTop - 9 * s,
        cx + 16 * s,
        cliffTop - 4 * s
      );
      ctx.quadraticCurveTo(
        cx + 24 * s,
        cliffTop - 2 * s,
        cx + 28 * s,
        cliffTop + 7 * s
      );
      ctx.lineTo(cx + 24 * s, cliffTop + 10 * s);
      ctx.lineTo(cx + 20 * s, cliffTop + 5 * s);
      ctx.lineTo(cx - 5 * s, cliffTop);
      ctx.lineTo(cx - 18 * s, cliffTop + 3 * s);
      ctx.lineTo(cx - 22 * s, cliffTop + 8 * s);
      ctx.closePath();
      ctx.fill();

      // Snow drift shadow
      ctx.fillStyle = "rgba(180,200,220,0.25)";
      ctx.beginPath();
      ctx.moveTo(cx - 22 * s, cliffTop + 8 * s);
      ctx.quadraticCurveTo(
        cx - 10 * s,
        cliffTop + 3 * s,
        cx + 5 * s,
        cliffTop + 4 * s
      );
      ctx.quadraticCurveTo(
        cx + 18 * s,
        cliffTop + 6 * s,
        cx + 24 * s,
        cliffTop + 10 * s
      );
      ctx.lineTo(cx + 20 * s, cliffTop + 5 * s);
      ctx.lineTo(cx - 5 * s, cliffTop);
      ctx.lineTo(cx - 18 * s, cliffTop + 3 * s);
      ctx.closePath();
      ctx.fill();

      // --- Multi-layered ice cascade ---
      const cascW = 18 * s;
      const cascTop = cy - 62 * s;
      const cascBot = cy + 2 * s;
      const cascH = cascBot - cascTop;

      // Deep back layer (darkest ice visible through translucent front)
      const deepGrad = ctx.createLinearGradient(cx, cascTop, cx, cascBot);
      deepGrad.addColorStop(0, fwIceAbyss);
      deepGrad.addColorStop(0.3, fwIceDeep);
      deepGrad.addColorStop(0.6, fwIceDark);
      deepGrad.addColorStop(1, fwIceMid);
      ctx.fillStyle = deepGrad;
      ctx.beginPath();
      ctx.moveTo(cx - cascW * 0.6, cascTop + 4 * s);
      ctx.bezierCurveTo(
        cx - cascW * 0.8,
        cascTop + cascH * 0.2,
        cx - cascW * 1,
        cascTop + cascH * 0.4,
        cx - cascW * 0.85,
        cascTop + cascH * 0.55
      );
      ctx.bezierCurveTo(
        cx - cascW * 1.1,
        cascTop + cascH * 0.7,
        cx - cascW * 0.9,
        cascTop + cascH * 0.9,
        cx - cascW * 0.7,
        cascBot
      );
      ctx.lineTo(cx + cascW * 0.7, cascBot);
      ctx.bezierCurveTo(
        cx + cascW * 0.9,
        cascTop + cascH * 0.9,
        cx + cascW * 1.1,
        cascTop + cascH * 0.7,
        cx + cascW * 0.85,
        cascTop + cascH * 0.55
      );
      ctx.bezierCurveTo(
        cx + cascW * 1,
        cascTop + cascH * 0.4,
        cx + cascW * 0.8,
        cascTop + cascH * 0.2,
        cx + cascW * 0.6,
        cascTop + 4 * s
      );
      ctx.closePath();
      ctx.fill();

      // Mid ice shelf — a frozen ledge where water pooled
      const shelfY = cascTop + cascH * 0.42;
      ctx.fillStyle = fwIceMid;
      ctx.beginPath();
      ctx.moveTo(cx - cascW * 1.15, shelfY);
      ctx.quadraticCurveTo(cx, shelfY + 4 * s, cx + cascW * 1.15, shelfY);
      ctx.quadraticCurveTo(cx, shelfY + 7 * s, cx - cascW * 1.15, shelfY);
      ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,0.2)";
      ctx.beginPath();
      ctx.moveTo(cx - cascW * 0.9, shelfY + 1 * s);
      ctx.quadraticCurveTo(
        cx,
        shelfY + 3 * s,
        cx + cascW * 0.9,
        shelfY + 1 * s
      );
      ctx.quadraticCurveTo(
        cx,
        shelfY + 5 * s,
        cx - cascW * 0.9,
        shelfY + 1 * s
      );
      ctx.fill();

      // Front translucent ice layer
      const frontGrad = ctx.createLinearGradient(cx, cascTop, cx, cascBot);
      frontGrad.addColorStop(0, "rgba(210,235,255,0.5)");
      frontGrad.addColorStop(0.2, "rgba(180,220,248,0.45)");
      frontGrad.addColorStop(0.4, "rgba(160,210,240,0.35)");
      frontGrad.addColorStop(0.55, "rgba(180,225,250,0.4)");
      frontGrad.addColorStop(0.7, "rgba(200,235,255,0.35)");
      frontGrad.addColorStop(1, "rgba(230,245,255,0.25)");
      ctx.fillStyle = frontGrad;
      ctx.beginPath();
      ctx.moveTo(cx - cascW * 0.5, cascTop + 5 * s);
      ctx.bezierCurveTo(
        cx - cascW * 0.65,
        cascTop + cascH * 0.15,
        cx - cascW * 0.75,
        cascTop + cascH * 0.35,
        cx - cascW * 0.65,
        shelfY
      );
      ctx.bezierCurveTo(
        cx - cascW * 0.8,
        shelfY + cascH * 0.15,
        cx - cascW * 0.7,
        cascBot - 8 * s,
        cx - cascW * 0.55,
        cascBot
      );
      ctx.lineTo(cx + cascW * 0.55, cascBot);
      ctx.bezierCurveTo(
        cx + cascW * 0.7,
        cascBot - 8 * s,
        cx + cascW * 0.8,
        shelfY + cascH * 0.15,
        cx + cascW * 0.65,
        shelfY
      );
      ctx.bezierCurveTo(
        cx + cascW * 0.75,
        cascTop + cascH * 0.35,
        cx + cascW * 0.65,
        cascTop + cascH * 0.15,
        cx + cascW * 0.5,
        cascTop + 5 * s
      );
      ctx.closePath();
      ctx.fill();

      // Frozen flow ridges (horizontal wave-lines across ice)
      ctx.lineWidth = 1 * s;
      for (let ridge = 0; ridge < 10; ridge++) {
        const ry = cascTop + (cascH * (ridge + 0.5)) / 10;
        const rw = cascW * (0.55 + ridge * 0.03);
        const ridgeAlpha = 0.15 + Math.sin(ridge * 1.3 + decorX) * 0.08;
        ctx.strokeStyle = `rgba(255,255,255,${ridgeAlpha})`;
        ctx.beginPath();
        const segments = 8;
        for (let seg = 0; seg <= segments; seg++) {
          const frac = seg / segments;
          const rx = cx - rw + frac * rw * 2;
          const ryOff = Math.sin(frac * Math.PI * 2 + ridge * 0.9) * 2 * s;
          if (seg === 0) {
            ctx.moveTo(rx, ry + ryOff);
          } else {
            ctx.lineTo(rx, ry + ryOff);
          }
        }
        ctx.stroke();
      }

      // Ice veins (internal frozen cracks in the cascade)
      ctx.lineWidth = 0.6 * s;
      const veins = [
        { x1: -4, x2: -6, x3: -3, y1: -55, y2: -30, y3: -10 },
        { x1: 3, x2: 5, x3: 2, y1: -58, y2: -35, y3: -15 },
        { x1: -8, x2: -10, x3: -7, y1: -40, y2: -20, y3: -5 },
        { x1: 8, x2: 10, x3: 7, y1: -45, y2: -25, y3: -8 },
        { x1: 0, x2: -2, x3: 1, y1: -50, y2: -28, y3: -6 },
      ];
      veins.forEach((v) => {
        ctx.strokeStyle = "rgba(200,235,255,0.18)";
        ctx.beginPath();
        ctx.moveTo(cx + v.x1 * s, cy + v.y1 * s);
        ctx.quadraticCurveTo(
          cx + v.x2 * s,
          cy + v.y2 * s,
          cx + v.x3 * s,
          cy + v.y3 * s
        );
        ctx.stroke();
      });

      // Center highlight — bright refraction strip
      const highlightGrad = ctx.createLinearGradient(
        cx - 2 * s,
        cascTop,
        cx + 2 * s,
        cascBot
      );
      highlightGrad.addColorStop(0, "rgba(255,255,255,0.1)");
      highlightGrad.addColorStop(0.2, "rgba(255,255,255,0.4)");
      highlightGrad.addColorStop(0.45, "rgba(255,255,255,0.15)");
      highlightGrad.addColorStop(0.6, "rgba(255,255,255,0.35)");
      highlightGrad.addColorStop(0.8, "rgba(255,255,255,0.1)");
      highlightGrad.addColorStop(1, "rgba(255,255,255,0.3)");
      ctx.fillStyle = highlightGrad;
      ctx.beginPath();
      ctx.moveTo(cx - 3 * s, cascTop + 8 * s);
      ctx.bezierCurveTo(
        cx - 4 * s,
        cascTop + cascH * 0.3,
        cx - 5 * s,
        shelfY,
        cx - 3 * s,
        shelfY + 4 * s
      );
      ctx.bezierCurveTo(
        cx - 4 * s,
        cascBot - 15 * s,
        cx - 3 * s,
        cascBot - 5 * s,
        cx - 1 * s,
        cascBot
      );
      ctx.lineTo(cx + 2 * s, cascBot);
      ctx.bezierCurveTo(
        cx + 2 * s,
        cascBot - 5 * s,
        cx + 3 * s,
        cascBot - 15 * s,
        cx + 2 * s,
        shelfY + 4 * s
      );
      ctx.bezierCurveTo(
        cx + 3 * s,
        shelfY,
        cx + 2 * s,
        cascTop + cascH * 0.3,
        cx + 1 * s,
        cascTop + 8 * s
      );
      ctx.closePath();
      ctx.fill();

      // --- Icicle clusters (varied sizes, 3 groups) ---
      const icicleGroups = [
        {
          baseX: -14,
          baseY: -42,
          icicles: [
            { dx: 0, h: 16, w: 2.5 },
            { dx: -3, h: 11, w: 2 },
            { dx: 3, h: 13, w: 1.8 },
            { dx: -5.5, h: 8, w: 1.5 },
            { dx: 5, h: 9, w: 1.4 },
          ],
        },
        {
          baseX: 0,
          baseY: -60,
          icicles: [
            { dx: 0, h: 14, w: 2.2 },
            { dx: -3.5, h: 10, w: 1.8 },
            { dx: 3, h: 12, w: 2 },
            { dx: -6, h: 7, w: 1.3 },
            { dx: 6, h: 8, w: 1.5 },
            { dx: -1.5, h: 11, w: 1.6 },
          ],
        },
        {
          baseX: 14,
          baseY: -35,
          icicles: [
            { dx: 0, h: 14, w: 2.3 },
            { dx: -3, h: 10, w: 1.7 },
            { dx: 4, h: 12, w: 2 },
            { dx: -5, h: 7, w: 1.2 },
            { dx: 6, h: 6, w: 1.1 },
          ],
        },
      ];
      icicleGroups.forEach((group) => {
        const gx = cx + group.baseX * s;
        const gy = cy + group.baseY * s;
        group.icicles.forEach((ic) => {
          const ix = gx + ic.dx * s;
          const icGrad = ctx.createLinearGradient(ix, gy, ix, gy + ic.h * s);
          icGrad.addColorStop(0, fwIceMid);
          icGrad.addColorStop(0.4, fwIceLight);
          icGrad.addColorStop(0.7, fwIceMid);
          icGrad.addColorStop(1, "rgba(180,220,248,0.3)");
          ctx.fillStyle = icGrad;
          ctx.beginPath();
          ctx.moveTo(ix - ic.w * s, gy);
          ctx.quadraticCurveTo(
            ix - ic.w * 0.3 * s,
            gy + ic.h * 0.7 * s,
            ix,
            gy + ic.h * s
          );
          ctx.quadraticCurveTo(
            ix + ic.w * 0.3 * s,
            gy + ic.h * 0.7 * s,
            ix + ic.w * s,
            gy
          );
          ctx.closePath();
          ctx.fill();

          // Icicle highlight
          ctx.fillStyle = "rgba(255,255,255,0.5)";
          ctx.beginPath();
          ctx.moveTo(ix - ic.w * 0.3 * s, gy + 1 * s);
          ctx.quadraticCurveTo(
            ix - ic.w * 0.1 * s,
            gy + ic.h * 0.5 * s,
            ix,
            gy + ic.h * 0.7 * s
          );
          ctx.lineTo(ix + ic.w * 0.15 * s, gy + 1 * s);
          ctx.closePath();
          ctx.fill();
        });
      });

      // --- Snow accumulation on ledges ---
      const snowDrifts = [
        { rx: 6, ry: 2, x: -22, y: -20 },
        { rx: 5, ry: 1.8, x: 20, y: -25 },
        { rx: 4, ry: 1.5, x: -18, y: -48 },
        { rx: 4.5, ry: 1.5, x: 18, y: -50 },
        { rx: 5, ry: 2, x: -24, y: -10 },
        { rx: 4, ry: 1.8, x: 22, y: -12 },
      ];
      snowDrifts.forEach((sd) => {
        ctx.fillStyle = fwSnow;
        ctx.beginPath();
        ctx.ellipse(
          cx + sd.x * s,
          cy + sd.y * s,
          sd.rx * s,
          sd.ry * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = "rgba(200,215,230,0.3)";
        ctx.beginPath();
        ctx.ellipse(
          cx + sd.x * s + 1 * s,
          cy + sd.y * s + 0.5 * s,
          sd.rx * 0.6 * s,
          sd.ry * 0.4 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // --- Animated shimmer sparkles (light catching ice facets) ---
      const sparkleTime = decorTime * 1.5;
      for (let sp = 0; sp < 14; sp++) {
        const spPhase = (sparkleTime + sp * 0.55 + decorX * 0.3) % 3;
        if (spPhase < 0.6) {
          const spAlpha = Math.sin((spPhase / 0.6) * Math.PI) * 0.85;
          const spSeed = decorX * 7.3 + sp * 13.7;
          const spx = cx + (Math.sin(spSeed) * 20 - 10) * s;
          const spy =
            cascTop + (((sp * 37 + Math.floor(decorX * 11)) % 60) + 2) * s;
          const spSize = (1 + Math.sin(spSeed * 2.3) * 0.5) * s;

          // Star-shaped sparkle
          ctx.fillStyle = `rgba(255,255,255,${spAlpha})`;
          ctx.beginPath();
          ctx.moveTo(spx, spy - spSize * 2);
          ctx.lineTo(spx + spSize * 0.4, spy - spSize * 0.4);
          ctx.lineTo(spx + spSize * 2, spy);
          ctx.lineTo(spx + spSize * 0.4, spy + spSize * 0.4);
          ctx.lineTo(spx, spy + spSize * 2);
          ctx.lineTo(spx - spSize * 0.4, spy + spSize * 0.4);
          ctx.lineTo(spx - spSize * 2, spy);
          ctx.lineTo(spx - spSize * 0.4, spy - spSize * 0.4);
          ctx.closePath();
          ctx.fill();
        }
      }

      // Prismatic light refraction dots on the cascade
      for (let pr = 0; pr < 6; pr++) {
        const prPhase = (decorTime * 0.8 + pr * 1.1) % 4;
        if (prPhase < 0.4) {
          const prAlpha = Math.sin((prPhase / 0.4) * Math.PI) * 0.6;
          const hue = (pr * 60 + decorTime * 25) % 360;
          const prx = cx + (pr * 5 - 12) * s;
          const pry = cascTop + (pr * 9 + 8) * s;
          ctx.fillStyle = `hsla(${hue}, 65%, 80%, ${prAlpha})`;
          ctx.beginPath();
          ctx.arc(prx, pry, 1.2 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // --- Layered mist at base ---
      for (let m = 0; m < 3; m++) {
        const mPhase = decorTime * 0.3 + m * 2.1;
        const mAlpha = 0.06 + Math.sin(mPhase) * 0.03;
        const mx = cx + Math.sin(mPhase * 0.7 + m) * 6 * s;
        const my = cy + (3 + m * 3) * s;
        const mrx = (24 + m * 4) * s;
        const mry = (6 + m * 2) * s;
        ctx.fillStyle = `rgba(200,230,255,${mAlpha})`;
        ctx.beginPath();
        ctx.ellipse(mx, my, mrx, mry, 0, 0, Math.PI * 2);
        ctx.fill();
      }

      // Mist wisps rising from impact point
      for (let w = 0; w < 4; w++) {
        const wPhase = decorTime * 0.6 + w * 1.5;
        const wAlpha =
          (0.1 + Math.sin(wPhase) * 0.04) * Math.max(0, 1 - (wPhase % 5) / 5);
        const wRise = ((wPhase % 5) / 5) * 20 * s;
        const wx = cx + (w * 6 - 9) * s + Math.sin(wPhase * 1.3) * 3 * s;
        const wy = cy - wRise;
        ctx.fillStyle = `rgba(210,235,255,${wAlpha})`;
        ctx.beginPath();
        ctx.ellipse(wx, wy, 5 * s, 3 * s, 0, 0, Math.PI * 2);
        ctx.fill();
      }

      break;
    }

    case "aurora_crystal": {
      const auroraBase = screenPos.y;

      // Ground glow with aurora colors
      const auroraGlowGrad = ctx.createRadialGradient(
        screenPos.x,
        auroraBase + 3 * s,
        0,
        screenPos.x,
        auroraBase + 3 * s,
        35 * s
      );
      const auroraHue = (decorTime * 20) % 360;
      auroraGlowGrad.addColorStop(0, `hsla(${auroraHue}, 60%, 60%, 0.3)`);
      auroraGlowGrad.addColorStop(
        0.5,
        `hsla(${(auroraHue + 60) % 360}, 50%, 50%, 0.12)`
      );
      auroraGlowGrad.addColorStop(1, "transparent");
      ctx.fillStyle = auroraGlowGrad;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        auroraBase + 3 * s,
        35 * s,
        16 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Snow mound base
      ctx.fillStyle = "#eef4f8";
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        auroraBase + 2 * s,
        14 * s,
        5 * s,
        decorX * 8.7 + decorY * 3.1
      );
      ctx.fill();

      // Crystal formation - main spire and satellites
      const auroraCrystals = [
        { angle: 0, dx: 0, dy: 0, h: 45, w: 8 },
        { angle: -0.12, dx: -10, dy: 2, h: 28, w: 5 },
        { angle: 0.1, dx: 8, dy: 1, h: 32, w: 6 },
        { angle: -0.2, dx: -5, dy: 3, h: 20, w: 4 },
        { angle: 0.18, dx: 12, dy: 3, h: 18, w: 3.5 },
      ];

      auroraCrystals.forEach((crystal, idx) => {
        const cx = screenPos.x + crystal.dx * s;
        const cy = auroraBase + crystal.dy * s;
        const ch = crystal.h * s;
        const cw = crystal.w * s;

        ctx.save();
        ctx.translate(cx, cy);
        ctx.rotate(crystal.angle);

        // Crystal hue shifts over time
        const crystalHue = (auroraHue + idx * 40) % 360;
        const crystalHue2 = (crystalHue + 120) % 360;

        // Left face
        const leftAuroraGrad = ctx.createLinearGradient(-cw, 0, 0, -ch);
        leftAuroraGrad.addColorStop(0, `hsla(${crystalHue}, 50%, 40%, 0.8)`);
        leftAuroraGrad.addColorStop(0.5, `hsla(${crystalHue2}, 60%, 60%, 0.7)`);
        leftAuroraGrad.addColorStop(1, `hsla(${crystalHue}, 70%, 80%, 0.9)`);
        ctx.fillStyle = leftAuroraGrad;
        ctx.beginPath();
        ctx.moveTo(-cw, 0);
        ctx.lineTo(-cw * 0.3, -ch * 0.15);
        ctx.lineTo(0, -ch);
        ctx.lineTo(-cw * 0.3, 0);
        ctx.closePath();
        ctx.fill();

        // Right face
        const rightAuroraGrad = ctx.createLinearGradient(0, -ch, cw, 0);
        rightAuroraGrad.addColorStop(0, `hsla(${crystalHue2}, 70%, 80%, 0.9)`);
        rightAuroraGrad.addColorStop(0.5, `hsla(${crystalHue}, 50%, 55%, 0.7)`);
        rightAuroraGrad.addColorStop(1, `hsla(${crystalHue2}, 45%, 35%, 0.8)`);
        ctx.fillStyle = rightAuroraGrad;
        ctx.beginPath();
        ctx.moveTo(cw, 0);
        ctx.lineTo(cw * 0.3, -ch * 0.15);
        ctx.lineTo(0, -ch);
        ctx.lineTo(cw * 0.3, 0);
        ctx.closePath();
        ctx.fill();

        // Center facet highlight
        ctx.fillStyle = `hsla(${crystalHue}, 80%, 90%, 0.5)`;
        ctx.beginPath();
        ctx.moveTo(0, -ch);
        ctx.lineTo(-cw * 0.15, -ch * 0.55);
        ctx.lineTo(cw * 0.1, -ch * 0.5);
        ctx.closePath();
        ctx.fill();

        // Edge glow
        ctx.strokeStyle = `hsla(${crystalHue}, 80%, 85%, 0.6)`;
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(0, -ch);
        ctx.lineTo(-cw * 0.3, -ch * 0.15);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(0, -ch);
        ctx.lineTo(cw * 0.3, -ch * 0.15);
        ctx.stroke();

        ctx.restore();
      });

      // Aurora light beams emanating upward
      const beamCount = 5;
      for (let beam = 0; beam < beamCount; beam++) {
        const beamPhase = (decorTime * 0.8 + beam * 1.3) % 4;
        if (beamPhase < 2.5) {
          const beamAlpha = Math.sin((beamPhase / 2.5) * Math.PI) * 0.25;
          const beamHue = (auroraHue + beam * 35) % 360;
          const beamX =
            screenPos.x + Math.sin(decorTime * 0.5 + beam * 1.2) * 8 * s;
          const beamTop = auroraBase - 50 * s - beam * 8 * s;
          const beamW = (3 + Math.sin(decorTime + beam) * 1.5) * s;

          ctx.fillStyle = `hsla(${beamHue}, 70%, 70%, ${beamAlpha})`;
          ctx.beginPath();
          ctx.moveTo(beamX - beamW, auroraBase - 20 * s);
          ctx.lineTo(beamX - beamW * 0.3, beamTop);
          ctx.lineTo(beamX + beamW * 0.3, beamTop);
          ctx.lineTo(beamX + beamW, auroraBase - 20 * s);
          ctx.closePath();
          ctx.fill();
        }
      }

      // Floating aurora particles
      for (let p = 0; p < 8; p++) {
        const pPhase = (decorTime * 1.2 + p * 0.7) % 3;
        const pAlpha = Math.sin((pPhase / 3) * Math.PI) * 0.7;
        const pHue = (auroraHue + p * 45) % 360;
        const px = screenPos.x + Math.sin(decorTime * 0.8 + p * 1.5) * 15 * s;
        const py = auroraBase - 10 * s - pPhase * 15 * s;

        ctx.fillStyle = `hsla(${pHue}, 70%, 75%, ${pAlpha})`;
        ctx.beginPath();
        ctx.arc(px, py, (1.2 + Math.sin(pPhase * 3) * 0.4) * s, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }

    case "snow_lantern": {
      const TRUE_ISO = 1 / Math.sqrt(3);
      const lanternStone = "#8a9aaa";
      const lanternStoneDark = "#6a7a8a";
      const lanternStoneLight = "#b0bfcf";
      const slx = screenPos.x;
      const sly = screenPos.y;

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        slx,
        sly + 4 * s,
        s,
        14 * s,
        7 * s,
        30 * s,
        0.25
      );

      // Snow around base
      ctx.fillStyle = "#f0f4f8";
      drawOrganicBlobAt(
        ctx,
        slx,
        sly + 4 * s,
        14 * s,
        6 * s,
        decorX * 3.3 + decorY * 10.7
      );
      ctx.fill();

      // Base pedestal — isometric diamond platform with TRUE_ISO
      const slBW = 8 * s;
      const slBD = slBW * TRUE_ISO;
      const slBH = 3 * s;
      const slBY = sly + 2 * s;
      // Top diamond
      ctx.fillStyle = lanternStoneDark;
      ctx.beginPath();
      ctx.moveTo(slx - slBW, slBY);
      ctx.lineTo(slx, slBY + slBD);
      ctx.lineTo(slx + slBW, slBY);
      ctx.lineTo(slx, slBY - slBD);
      ctx.closePath();
      ctx.fill();
      // Left face
      ctx.fillStyle = lanternStone;
      ctx.beginPath();
      ctx.moveTo(slx - slBW, slBY);
      ctx.lineTo(slx, slBY + slBD);
      ctx.lineTo(slx, slBY + slBD + slBH);
      ctx.lineTo(slx - slBW, slBY + slBH);
      ctx.closePath();
      ctx.fill();
      // Right face
      ctx.fillStyle = lanternStoneDark;
      ctx.beginPath();
      ctx.moveTo(slx, slBY + slBD);
      ctx.lineTo(slx + slBW, slBY);
      ctx.lineTo(slx + slBW, slBY + slBH);
      ctx.lineTo(slx, slBY + slBD + slBH);
      ctx.closePath();
      ctx.fill();

      // Pillar/post — isometric column
      const pilHW = 3 * s;
      const pilHD = pilHW * TRUE_ISO;
      const pilBot = slBY;
      const pilTop = sly - 22 * s;
      const pilGL = ctx.createLinearGradient(slx - pilHW, 0, slx, 0);
      pilGL.addColorStop(0, lanternStoneLight);
      pilGL.addColorStop(1, lanternStone);
      ctx.fillStyle = pilGL;
      ctx.beginPath();
      ctx.moveTo(slx - pilHW, pilBot);
      ctx.lineTo(slx, pilBot + pilHD);
      ctx.lineTo(slx, pilTop + pilHD);
      ctx.lineTo(slx - pilHW, pilTop);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = lanternStoneDark;
      ctx.beginPath();
      ctx.moveTo(slx, pilBot + pilHD);
      ctx.lineTo(slx + pilHW, pilBot);
      ctx.lineTo(slx + pilHW, pilTop);
      ctx.lineTo(slx, pilTop + pilHD);
      ctx.closePath();
      ctx.fill();

      // Lantern housing — isometric box with open sides
      const hTop = sly - 38 * s;
      const hBot = pilTop;
      const hHW = 10 * s;
      const hHD = hHW * TRUE_ISO;

      // Warm light glow (behind housing)
      const glowPulse = 0.6 + Math.sin(decorTime * 3) * 0.15;
      const glowMidY = (hTop + hBot) / 2;
      const warmGlow = ctx.createRadialGradient(
        slx,
        glowMidY,
        0,
        slx,
        glowMidY,
        18 * s
      );
      warmGlow.addColorStop(0, `rgba(255,200,100,${glowPulse * 0.5})`);
      warmGlow.addColorStop(0.3, `rgba(255,180,80,${glowPulse * 0.25})`);
      warmGlow.addColorStop(1, "transparent");
      ctx.fillStyle = warmGlow;
      ctx.beginPath();
      ctx.ellipse(slx, glowMidY, 18 * s, 12 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Housing left face (lit side with opening for light)
      ctx.fillStyle = lanternStone;
      ctx.beginPath();
      ctx.moveTo(slx - hHW, hBot);
      ctx.lineTo(slx, hBot + hHD);
      ctx.lineTo(slx, hTop + hHD);
      ctx.lineTo(slx - hHW, hTop);
      ctx.closePath();
      ctx.fill();
      // Warm glow opening on left face
      ctx.fillStyle = `rgba(255,200,100,${glowPulse * 0.7})`;
      ctx.beginPath();
      const loInset = 2.5 * s;
      ctx.moveTo(slx - hHW + loInset, hBot - loInset);
      ctx.lineTo(slx - loInset, hBot + hHD - loInset);
      ctx.lineTo(slx - loInset, hTop + hHD + loInset);
      ctx.lineTo(slx - hHW + loInset, hTop + loInset);
      ctx.closePath();
      ctx.fill();

      // Housing right face (shadow side with opening)
      ctx.fillStyle = lanternStoneDark;
      ctx.beginPath();
      ctx.moveTo(slx, hBot + hHD);
      ctx.lineTo(slx + hHW, hBot);
      ctx.lineTo(slx + hHW, hTop);
      ctx.lineTo(slx, hTop + hHD);
      ctx.closePath();
      ctx.fill();
      // Warm glow opening on right face
      ctx.fillStyle = `rgba(255,180,80,${glowPulse * 0.55})`;
      ctx.beginPath();
      ctx.moveTo(slx + loInset, hBot + hHD - loInset);
      ctx.lineTo(slx + hHW - loInset, hBot - loInset);
      ctx.lineTo(slx + hHW - loInset, hTop + loInset);
      ctx.lineTo(slx + loInset, hTop + hHD + loInset);
      ctx.closePath();
      ctx.fill();

      // Housing corner posts (isometric thin columns at front vertex)
      ctx.fillStyle = lanternStone;
      ctx.fillRect(slx - 1.5 * s, hTop, 3 * s, hBot - hTop);

      // Roof — isometric pyramid with TRUE_ISO
      const roofHW = hHW + 3 * s;
      const roofHD = roofHW * TRUE_ISO;
      const roofPeak = hTop - 12 * s;
      // Back face
      ctx.fillStyle = lanternStoneDark;
      ctx.beginPath();
      ctx.moveTo(slx - roofHW, hTop);
      ctx.lineTo(slx, hTop - roofHD);
      ctx.lineTo(slx, roofPeak);
      ctx.closePath();
      ctx.fill();
      // Left face
      ctx.fillStyle = lanternStone;
      ctx.beginPath();
      ctx.moveTo(slx - roofHW, hTop);
      ctx.lineTo(slx, hTop + roofHD);
      ctx.lineTo(slx, roofPeak);
      ctx.closePath();
      ctx.fill();
      // Right face
      ctx.fillStyle = lanternStoneLight;
      ctx.beginPath();
      ctx.moveTo(slx, hTop + roofHD);
      ctx.lineTo(slx + roofHW, hTop);
      ctx.lineTo(slx, roofPeak);
      ctx.closePath();
      ctx.fill();

      // Roof top ornament — small isometric diamond
      ctx.fillStyle = lanternStone;
      ctx.beginPath();
      ctx.moveTo(slx - 2 * s, roofPeak);
      ctx.lineTo(slx, roofPeak + 2 * s * TRUE_ISO);
      ctx.lineTo(slx + 2 * s, roofPeak);
      ctx.lineTo(slx, roofPeak - 2 * s * TRUE_ISO);
      ctx.closePath();
      ctx.fill();

      // Snow on roof
      ctx.fillStyle = "#ffffff";
      ctx.beginPath();
      ctx.moveTo(slx - roofHW, hTop + 1 * s);
      ctx.quadraticCurveTo(slx - 4 * s, hTop - 5 * s, slx, roofPeak);
      ctx.quadraticCurveTo(
        slx + 2 * s,
        roofPeak - 2 * s,
        slx,
        roofPeak - 3 * s
      );
      ctx.quadraticCurveTo(slx - 2 * s, roofPeak - 2 * s, slx, roofPeak);
      ctx.quadraticCurveTo(
        slx + 4 * s,
        hTop - 5 * s,
        slx + roofHW,
        hTop + 1 * s
      );
      ctx.lineTo(slx + roofHW, hTop);
      ctx.lineTo(slx, roofPeak);
      ctx.lineTo(slx - roofHW, hTop);
      ctx.closePath();
      ctx.fill();

      // Light cone downward
      ctx.fillStyle = `rgba(255,200,100,${glowPulse * 0.12})`;
      ctx.beginPath();
      ctx.moveTo(slx - hHW * 0.6, hBot);
      ctx.lineTo(slx - hHW * 1.2, sly + 5 * s);
      ctx.lineTo(slx + hHW * 1.2, sly + 5 * s);
      ctx.lineTo(slx + hHW * 0.6, hBot);
      ctx.closePath();
      ctx.fill();

      // Floating warm particles
      for (let p = 0; p < 3; p++) {
        const warmPhase = (decorTime * 1.5 + p * 1.1) % 2;
        if (warmPhase < 1.2) {
          const warmAlpha = Math.sin((warmPhase / 1.2) * Math.PI) * 0.6;
          const warmX = slx + Math.sin(decorTime + p * 2) * 4 * s;
          const warmY = glowMidY - warmPhase * 8 * s;
          ctx.fillStyle = `rgba(255,200,100,${warmAlpha})`;
          ctx.beginPath();
          ctx.arc(warmX, warmY, 1 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      break;
    }

    // === VOLCANIC DECORATIONS ===
    case "lava_pool": {
      // Enhanced 3D isometric lava pool with animated molten effects
      const lavaYellow = "#ffeb3b";
      const lavaOrange = "#ff9800";
      const lavaRed = "#f44336";
      const lavaDark = "#bf360c";
      const rockDark = "#1a1210";
      const rockMid = "#2a1a10";
      const lavaSeed = (dec.x || 0) * 23.1;

      // Ambient heat glow
      const heatGlow = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y,
        0,
        screenPos.x,
        screenPos.y,
        45 * s
      );
      const heatPulse = 0.25 + Math.sin(decorTime * 2) * 0.1;
      heatGlow.addColorStop(0, `rgba(255,100,0,${heatPulse})`);
      heatGlow.addColorStop(0.4, `rgba(255,60,0,${heatPulse * 0.5})`);
      heatGlow.addColorStop(1, "rgba(255,60,0,0)");
      ctx.fillStyle = heatGlow;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        45 * s,
        25 * s,
        lavaSeed - 10,
        0.1
      );
      ctx.fill();

      // Outer rock rim with 3D depth
      // Rock rim back
      ctx.fillStyle = rockDark;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y - 3 * s,
        32 * s,
        16 * s,
        lavaSeed + 50,
        0.18
      );
      ctx.fill();

      // Rock rim front edge
      ctx.fillStyle = rockMid;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        32 * s,
        16 * s,
        lavaSeed + 50,
        0.18
      );
      ctx.fill();

      // Inner dark crater
      ctx.fillStyle = "#0a0505";
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y - 1 * s,
        26 * s,
        13 * s,
        lavaSeed + 70,
        0.15
      );
      ctx.fill();

      // Lava surface with animated gradient
      const lavaGrad = ctx.createRadialGradient(
        screenPos.x + Math.sin(decorTime * 1.5) * 5 * s,
        screenPos.y + Math.cos(decorTime * 1.2) * 2 * s,
        0,
        screenPos.x,
        screenPos.y,
        24 * s
      );
      lavaGrad.addColorStop(0, lavaYellow);
      lavaGrad.addColorStop(0.2, lavaOrange);
      lavaGrad.addColorStop(0.5, lavaRed);
      lavaGrad.addColorStop(0.8, lavaDark);
      lavaGrad.addColorStop(1, "#1a0a00");
      ctx.fillStyle = lavaGrad;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y - 1 * s,
        24 * s,
        12 * s,
        lavaSeed,
        0.14
      );
      ctx.fill();

      // Cooling crust patterns (darker streaks)
      ctx.strokeStyle = "rgba(40,10,0,0.4)";
      ctx.lineWidth = 2 * s;
      for (let c = 0; c < 4; c++) {
        const crustAngle = (c * Math.PI) / 2 + decorTime * 0.2;
        const crustR = 8 + c * 4;
        ctx.beginPath();
        ctx.arc(
          screenPos.x + Math.cos(crustAngle + 1) * 5 * s,
          screenPos.y + Math.sin(crustAngle + 1) * 2.5 * s,
          crustR * s,
          crustAngle,
          crustAngle + 0.8
        );
        ctx.stroke();
      }

      // Hot spots / bright vents
      const hotSpots = [
        { r: 8, x: 0, y: 0 },
        { r: 5, x: -8, y: -2 },
        { r: 4, x: 10, y: 1 },
        { r: 3, x: -4, y: 3 },
      ];
      hotSpots.forEach((hs, idx) => {
        const hotPulse = 0.6 + Math.sin(decorTime * 3 + idx) * 0.3;
        const hotGrad = ctx.createRadialGradient(
          screenPos.x + hs.x * s,
          screenPos.y + hs.y * s - 1 * s,
          0,
          screenPos.x + hs.x * s,
          screenPos.y + hs.y * s - 1 * s,
          hs.r * s
        );
        hotGrad.addColorStop(0, `rgba(255,255,200,${hotPulse})`);
        hotGrad.addColorStop(0.3, `rgba(255,200,50,${hotPulse * 0.8})`);
        hotGrad.addColorStop(0.7, `rgba(255,100,0,${hotPulse * 0.4})`);
        hotGrad.addColorStop(1, "rgba(255,100,0,0)");
        ctx.fillStyle = hotGrad;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + hs.x * s,
          screenPos.y + hs.y * s - 1 * s,
          hs.r * s,
          hs.r * 0.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // Animated bubbles
      const bubbles = [
        { phase: 0, r: 3, speed: 2, x: 5, y: -1 },
        { phase: 1.5, r: 2.5, speed: 2.5, x: -8, y: 1 },
        { phase: 3, r: 2, speed: 1.8, x: 12, y: 2 },
        { phase: 2, r: 2, speed: 2.2, x: -3, y: -2 },
      ];
      bubbles.forEach((bub) => {
        const bubTime = (decorTime * bub.speed + bub.phase) % 2;
        const bubScale = bubTime < 1 ? bubTime : 2 - bubTime;
        const bubY = bub.y - bubTime * 3;

        if (bubScale > 0.1) {
          ctx.fillStyle = `rgba(255,200,100,${bubScale * 0.8})`;
          ctx.beginPath();
          ctx.arc(
            screenPos.x + bub.x * s,
            screenPos.y + bubY * s,
            bub.r * bubScale * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
          // Bubble highlight
          ctx.fillStyle = `rgba(255,255,200,${bubScale * 0.5})`;
          ctx.beginPath();
          ctx.arc(
            screenPos.x + bub.x * s - bub.r * 0.3 * s,
            screenPos.y + bubY * s - bub.r * 0.3 * s,
            bub.r * bubScale * 0.4 * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
      });

      // Rising heat shimmer / smoke wisps
      ctx.fillStyle = "rgba(100,50,20,0.15)";
      for (let w = 0; w < 3; w++) {
        const wispY = screenPos.y - ((decorTime * 25 + w * 20) % 40) * s;
        const wispX = screenPos.x + Math.sin(decorTime * 2 + w) * 10 * s;
        const wispSize = 4 + (1 - ((decorTime * 25 + w * 20) % 40) / 40) * 4;
        ctx.beginPath();
        ctx.arc(wispX, wispY, wispSize * s, 0, Math.PI * 2);
        ctx.fill();
      }

      // Rock edge details
      ctx.strokeStyle = rockMid;
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y,
        30 * s,
        15 * s,
        0,
        Math.PI * 0.1,
        Math.PI * 0.4
      );
      ctx.stroke();
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y,
        30 * s,
        15 * s,
        0,
        Math.PI * 0.7,
        Math.PI * 0.9
      );
      ctx.stroke();
      break;
    }
    case "obsidian_spike": {
      // Enhanced 3D isometric obsidian crystal spike
      const spikeBaseX = screenPos.x;
      const spikeBaseY = screenPos.y;

      // Lava glow beneath
      const lavaGlow = ctx.createRadialGradient(
        spikeBaseX,
        spikeBaseY + 3 * s,
        0,
        spikeBaseX,
        spikeBaseY + 3 * s,
        15 * s
      );
      lavaGlow.addColorStop(0, "rgba(255,80,0,0.4)");
      lavaGlow.addColorStop(0.5, "rgba(255,40,0,0.2)");
      lavaGlow.addColorStop(1, "transparent");
      ctx.fillStyle = lavaGlow;
      ctx.beginPath();
      ctx.ellipse(
        spikeBaseX,
        spikeBaseY + 3 * s,
        15 * s,
        8 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Ground shadow
      ctx.fillStyle = "rgba(0,0,0,0.35)";
      ctx.beginPath();
      ctx.ellipse(
        spikeBaseX + 4 * s,
        spikeBaseY + 6 * s,
        14 * s,
        7 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Main spike - faceted crystal look
      // Back face (darkest)
      ctx.fillStyle = "#0a0a15";
      ctx.beginPath();
      ctx.moveTo(spikeBaseX - 8 * s, spikeBaseY + 3 * s);
      ctx.lineTo(spikeBaseX - 3 * s, spikeBaseY - 40 * s);
      ctx.lineTo(spikeBaseX + 2 * s, spikeBaseY + 3 * s);
      ctx.closePath();
      ctx.fill();

      // Right face (mid-dark)
      ctx.fillStyle = "#15152a";
      ctx.beginPath();
      ctx.moveTo(spikeBaseX + 2 * s, spikeBaseY + 3 * s);
      ctx.lineTo(spikeBaseX - 3 * s, spikeBaseY - 40 * s);
      ctx.lineTo(spikeBaseX + 10 * s, spikeBaseY + 3 * s);
      ctx.closePath();
      ctx.fill();

      // Sharp glossy highlight facet
      ctx.fillStyle = "rgba(140,140,180,0.5)";
      ctx.beginPath();
      ctx.moveTo(spikeBaseX - 4 * s, spikeBaseY + 3 * s);
      ctx.lineTo(spikeBaseX - 3 * s, spikeBaseY - 40 * s);
      ctx.lineTo(spikeBaseX + 1 * s, spikeBaseY - 25 * s);
      ctx.lineTo(spikeBaseX - 1 * s, spikeBaseY + 3 * s);
      ctx.closePath();
      ctx.fill();

      // Secondary smaller spike
      ctx.fillStyle = "#0a0a15";
      ctx.beginPath();
      ctx.moveTo(spikeBaseX + 5 * s, spikeBaseY + 3 * s);
      ctx.lineTo(spikeBaseX + 8 * s, spikeBaseY - 18 * s);
      ctx.lineTo(spikeBaseX + 12 * s, spikeBaseY + 3 * s);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = "rgba(120,120,160,0.4)";
      ctx.beginPath();
      ctx.moveTo(spikeBaseX + 6 * s, spikeBaseY + 3 * s);
      ctx.lineTo(spikeBaseX + 8 * s, spikeBaseY - 18 * s);
      ctx.lineTo(spikeBaseX + 9 * s, spikeBaseY - 10 * s);
      ctx.lineTo(spikeBaseX + 8 * s, spikeBaseY + 3 * s);
      ctx.closePath();
      ctx.fill();

      // Lava cracks with glow
      ctx.strokeStyle = "#ff4400";
      ctx.lineWidth = 1.5 * s;
      setShadowBlur(ctx, 5 * s, "#ff6600");
      ctx.beginPath();
      ctx.moveTo(spikeBaseX - 2 * s, spikeBaseY + 2 * s);
      ctx.lineTo(spikeBaseX - 1 * s, spikeBaseY - 8 * s);
      ctx.lineTo(spikeBaseX + 1 * s, spikeBaseY - 5 * s);
      ctx.stroke();
      clearShadow(ctx);

      // Tip glint
      ctx.fillStyle = "rgba(200,200,255,0.6)";
      ctx.beginPath();
      ctx.arc(spikeBaseX - 3 * s, spikeBaseY - 39 * s, 1.5 * s, 0, Math.PI * 2);
      ctx.fill();
      break;
    }
    case "charred_tree": {
      drawCharredTree(
        ctx,
        screenPos.x,
        screenPos.y,
        s,
        variant,
        decorX,
        decorY,
        decorTime
      );
      break;
    }
    case "ember": {
      // Enhanced animated floating ember with glow and particles
      const emberBaseX = screenPos.x;
      const emberBaseY = screenPos.y;
      const emberFloat = Math.sin(decorTime * 2 + variant * 1.5) * 5 * s;
      const emberPulse = 0.7 + Math.sin(decorTime * 4 + variant) * 0.3;

      // Outer glow
      const emberGlow = ctx.createRadialGradient(
        emberBaseX,
        emberBaseY + emberFloat,
        0,
        emberBaseX,
        emberBaseY + emberFloat,
        12 * s
      );
      emberGlow.addColorStop(0, `rgba(255,150,0,${0.4 * emberPulse})`);
      emberGlow.addColorStop(0.5, `rgba(255,80,0,${0.2 * emberPulse})`);
      emberGlow.addColorStop(1, "transparent");
      ctx.fillStyle = emberGlow;
      ctx.beginPath();
      ctx.arc(emberBaseX, emberBaseY + emberFloat, 12 * s, 0, Math.PI * 2);
      ctx.fill();

      // Main ember body
      ctx.fillStyle = `rgba(255,${80 + Math.sin(decorTime * 5 + variant) * 40},0,${0.8 * emberPulse})`;
      ctx.beginPath();
      ctx.arc(emberBaseX, emberBaseY + emberFloat, 5 * s, 0, Math.PI * 2);
      ctx.fill();

      // Hot core
      ctx.fillStyle = `rgba(255,${180 + Math.sin(decorTime * 6) * 50},${50 + Math.sin(decorTime * 4) * 30},${emberPulse})`;
      ctx.beginPath();
      ctx.arc(emberBaseX, emberBaseY + emberFloat, 3 * s, 0, Math.PI * 2);
      ctx.fill();

      // White-hot center
      ctx.fillStyle = `rgba(255,255,200,${0.8 * emberPulse})`;
      ctx.beginPath();
      ctx.arc(emberBaseX, emberBaseY + emberFloat, 1.5 * s, 0, Math.PI * 2);
      ctx.fill();

      // Trailing sparks
      for (let sp = 0; sp < 3; sp++) {
        const sparkTime = (decorTime * 3 + sp * 0.5 + variant) % 1;
        const sparkX = emberBaseX + Math.sin(sp * 2 + decorTime) * 3 * s;
        const sparkY = emberBaseY + emberFloat + sparkTime * 15 * s;
        const sparkAlpha = (1 - sparkTime) * 0.8;

        ctx.fillStyle = `rgba(255,${150 + sp * 30},0,${sparkAlpha})`;
        ctx.beginPath();
        ctx.arc(sparkX, sparkY, (1 - sparkTime) * 2 * s, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }
    case "obsidian_castle": {
      const ox = screenPos.x;
      const oy = screenPos.y;
      const cBlack = "#080810";
      const cDark = "#101018";
      const cMid = "#1a1a28";
      const cLight = "#2a2a40";
      const cHi = "#3e3e5a";
      const cEdge = "#505078";
      const lGlow = "#FF3D00";
      const lCore = "#FFAB00";
      const cPulse = 0.5 + Math.sin(decorTime * 2.4) * 0.25;
      const fY = oy - 16 * s;

      if (!landmarkSkipShadow) {
        drawDirectionalShadow(ctx, ox, fY, s, 36 * s, 16 * s, 60 * s, 0.4);
      }
      // Lava underglow at base
      const gGrad = ctx.createRadialGradient(
        ox,
        fY + 2 * s,
        0,
        ox,
        fY + 2 * s,
        28 * s
      );
      gGrad.addColorStop(0, "rgba(255,61,0,0.22)");
      gGrad.addColorStop(0.6, "rgba(255,61,0,0.06)");
      gGrad.addColorStop(1, "transparent");
      ctx.fillStyle = gGrad;
      ctx.beginPath();
      ctx.ellipse(ox, fY + 2 * s, 28 * s, 12 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      drawIsometricPrism(
        ctx,
        ox,
        fY,
        28 * s,
        28 * s,
        6 * s,
        cMid,
        cBlack,
        cDark
      );
      const fI = 28 * s * ISO_COS;
      const fD = 28 * s * ISO_SIN;
      setShadowBlur(ctx, 5 * s, lGlow);
      ctx.strokeStyle = `rgba(255,61,0,${0.2 + cPulse * 0.15})`;
      ctx.lineWidth = 1 * s;
      for (let i = 0; i < 4; i++) {
        const rx = ox - fI * 0.7 + i * fI * 0.45;
        const ry = fY + fD * 0.4 + i * 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(rx, ry);
        ctx.lineTo(rx + 3 * s, ry - 3 * s);
        ctx.stroke();
      }
      clearShadow(ctx);

      const btW = 10 * s;
      const btH = 50 * s;
      const btBase = fY - 10 * s;
      drawIsometricPrism(ctx, ox, btBase, btW, btW, btH, cLight, cBlack, cDark);
      const btI2 = btW * ISO_COS;
      const btD2 = btW * ISO_SIN;
      const btTop = btBase - btH;

      ctx.strokeStyle = "rgba(0,0,0,0.15)";
      ctx.lineWidth = 0.4 * s;
      for (let r = 1; r < 6; r++) {
        const ry = btBase - btH * (r / 6);
        ctx.beginPath();
        ctx.moveTo(ox - btI2, ry + btD2);
        ctx.lineTo(ox, ry + btD2 * 2);
        ctx.lineTo(ox + btI2, ry + btD2);
        ctx.stroke();
      }
      ctx.strokeStyle = cEdge;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(ox, btBase + btD2 * 2);
      ctx.lineTo(ox, btTop + btD2 * 2);
      ctx.stroke();

      drawIsometricPyramid(ctx, ox, btTop, btW, 20 * s, cHi, cBlack, cMid);

      const mW = 16 * s;
      const mH = 45 * s;
      const mBase = fY - 6 * s;
      drawIsometricPrism(ctx, ox, mBase, mW, mW, mH, cLight, cBlack, cDark);
      const mI = mW * ISO_COS;
      const mD = mW * ISO_SIN;
      const mTop = mBase - mH;

      ctx.strokeStyle = "rgba(0,0,0,0.18)";
      ctx.lineWidth = 0.5 * s;
      for (let row = 1; row < 7; row++) {
        const ry = mBase - mH * (row / 7);
        ctx.beginPath();
        ctx.moveTo(ox - mI, ry + mD);
        ctx.lineTo(ox, ry + mD * 2);
        ctx.lineTo(ox + mI, ry + mD);
        ctx.stroke();
      }
      ctx.strokeStyle = cEdge;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(ox, mBase + mD * 2);
      ctx.lineTo(ox, mTop + mD * 2);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(ox - mI, mBase + mD);
      ctx.lineTo(ox - mI, mTop + mD);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(ox + mI, mBase + mD);
      ctx.lineTo(ox + mI, mTop + mD);
      ctx.stroke();

      setShadowBlur(ctx, 8 * s, lGlow);
      ctx.fillStyle = `rgba(255,70,20,${0.35 + cPulse * 0.3})`;
      const mWinY1 = mBase - mH * 0.35;
      traceIsoFlushRect(
        ctx,
        ox - mI * 0.55,
        mWinY1 + mD * 0.5 + 3.5 * s,
        2.4,
        7,
        "left",
        s
      );
      ctx.fill();
      traceIsoFlushRect(
        ctx,
        ox + mI * 0.35,
        mWinY1 + mD * 0.5 + 1.5 * s,
        2,
        7,
        "right",
        s
      );
      ctx.fill();
      const mWinY2 = mBase - mH * 0.6;
      traceIsoFlushRect(
        ctx,
        ox - mI * 0.45,
        mWinY2 + mD * 0.5 + 3 * s,
        2,
        6,
        "left",
        s
      );
      ctx.fill();
      traceIsoFlushRect(
        ctx,
        ox + mI * 0.45,
        mWinY2 + mD * 0.5 + 1 * s,
        2,
        6,
        "right",
        s
      );
      ctx.fill();
      clearShadow(ctx);

      drawIsometricPrism(
        ctx,
        ox,
        mTop,
        mW + 4 * s,
        mW + 4 * s,
        4 * s,
        cHi,
        cMid,
        cLight
      );
      const parI = (mW + 4 * s) * ISO_COS;
      const parD = (mW + 4 * s) * 0.5;
      const parTop = mTop - 4 * s;
      const merlH = 5 * s;
      const merlW = 3 * s;
      for (let i = 0; i < 3; i++) {
        const t = (i + 0.5) / 3;
        const mx = ox - parI * (1 - t);
        const my = parTop + parD + parD * t;
        drawIsometricPrism(
          ctx,
          mx,
          my,
          merlW,
          merlW,
          merlH,
          cEdge,
          cHi,
          cLight
        );
      }
      for (let i = 0; i < 3; i++) {
        const t = (i + 0.5) / 3;
        const mx = ox + parI * t;
        const my = parTop + parD * 2 - parD * t;
        drawIsometricPrism(
          ctx,
          mx,
          my,
          merlW,
          merlW,
          merlH,
          cEdge,
          cHi,
          cLight
        );
      }

      const spW2 = (mW + 4 * s) * 0.5;
      drawIsometricPyramid(ctx, ox, parTop, spW2, 18 * s, cHi, cBlack, cMid);

      [-1, 1].forEach((side) => {
        const tW = 8 * s;
        const tH3 = 38 * s;
        const ttD2 = tW * ISO_SIN;
        const ttx = ox + side * (mI + tW * ISO_COS - 3 * s);
        const tty = fY + 14 * s + side * mD * 0.35;
        const ttI2 = tW * ISO_COS;
        drawIsometricPrism(ctx, ttx, tty, tW, tW, tH3, cLight, cBlack, cDark);
        const ttTop = tty - tH3;

        ctx.strokeStyle = "rgba(0,0,0,0.15)";
        ctx.lineWidth = 0.4 * s;
        for (let r = 1; r < 5; r++) {
          const ry = tty - tH3 * (r / 5);
          ctx.beginPath();
          ctx.moveTo(ttx - ttI2, ry + ttD2);
          ctx.lineTo(ttx, ry + ttD2 * 2);
          ctx.lineTo(ttx + ttI2, ry + ttD2);
          ctx.stroke();
        }
        ctx.strokeStyle = cEdge;
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(ttx, tty + ttD2 * 2);
        ctx.lineTo(ttx, ttTop + ttD2 * 2);
        ctx.stroke();

        drawIsometricPrism(
          ctx,
          ttx,
          ttTop,
          tW + 2 * s,
          tW + 2 * s,
          3 * s,
          cHi,
          cMid,
          cLight
        );
        const tSpBase = ttTop - 3 * s;
        drawIsometricPyramid(
          ctx,
          ttx,
          tSpBase,
          (tW + 2 * s) * 0.5,
          14 * s,
          cHi,
          cBlack,
          cMid
        );

        setShadowBlur(ctx, 7 * s, lGlow);
        ctx.fillStyle = `rgba(255,70,20,${0.4 + cPulse * 0.3})`;
        const wY1 = tty + ttD2 - tH3 * 0.35;
        const tFace = side === -1 ? ("left" as const) : ("right" as const);
        traceIsoFlushRect(ctx, ttx, wY1, 2, 7, tFace, s);
        ctx.fill();
        const wY2 = tty + ttD2 - tH3 * 0.6;
        traceIsoFlushRect(ctx, ttx, wY2, 1.6, 6, tFace, s);
        ctx.fill();
        clearShadow(ctx);
      });

      const gateY = mBase + mD * 2;
      ctx.fillStyle = "#050508";
      ctx.beginPath();
      ctx.moveTo(ox - 5 * s, gateY);
      ctx.lineTo(ox - 5 * s, gateY - 16 * s);
      ctx.quadraticCurveTo(ox, gateY - 22 * s, ox + 5 * s, gateY - 16 * s);
      ctx.lineTo(ox + 5 * s, gateY);
      ctx.closePath();
      ctx.fill();
      setShadowBlur(ctx, 14 * s, lCore);
      ctx.fillStyle = lCore;
      ctx.beginPath();
      ctx.arc(ox, gateY - 9 * s, 3 * s, 0, Math.PI * 2);
      ctx.fill();
      clearShadow(ctx);

      ctx.fillStyle = "#3D3D3D";
      [-6 * s, 6 * s].forEach((xOff) => {
        ctx.beginPath();
        ctx.arc(ox + xOff, gateY - 18 * s, 2.5 * s, 0, Math.PI * 2);
        ctx.fill();
        setShadowBlur(ctx, 4 * s, lGlow);
        ctx.fillStyle = lGlow;
        ctx.beginPath();
        ctx.arc(ox + xOff - 1 * s, gateY - 18.5 * s, 0.7 * s, 0, Math.PI * 2);
        ctx.arc(ox + xOff + 1 * s, gateY - 18.5 * s, 0.7 * s, 0, Math.PI * 2);
        ctx.fill();
        clearShadow(ctx);
        ctx.fillStyle = "#3D3D3D";
      });

      setShadowBlur(ctx, 10 * s, lGlow);
      ctx.strokeStyle = lGlow;
      ctx.lineWidth = 1.8 * s;
      ctx.beginPath();
      ctx.moveTo(ox - mI + 3 * s, mBase + mD - 8 * s);
      ctx.lineTo(ox - mI + 6 * s, mBase + mD - 18 * s);
      ctx.lineTo(ox - mI + 4 * s, mBase + mD - 30 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(ox + mI - 2 * s, mBase + mD - 6 * s);
      ctx.lineTo(ox + mI - 5 * s, mBase + mD - 18 * s);
      ctx.lineTo(ox + mI - 3 * s, mBase + mD - 28 * s);
      ctx.stroke();
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.moveTo(ox - 2 * s, mBase + mD * 2 - 5 * s);
      ctx.lineTo(ox + 1 * s, mBase + mD * 2 - 15 * s);
      ctx.stroke();
      clearShadow(ctx);

      setShadowBlur(ctx, 6 * s, lGlow);
      ctx.strokeStyle = `rgba(255,61,0,${0.25 + cPulse * 0.2})`;
      ctx.lineWidth = 1.2 * s;
      const fRuneY = fY + fD - 2 * s;
      ctx.beginPath();
      ctx.moveTo(ox - fI * 0.4, fRuneY);
      ctx.lineTo(ox - fI * 0.2, fRuneY - 3 * s);
      ctx.lineTo(ox, fRuneY);
      ctx.lineTo(ox + fI * 0.2, fRuneY - 3 * s);
      ctx.lineTo(ox + fI * 0.4, fRuneY);
      ctx.stroke();
      clearShadow(ctx);

      // === CURTAIN WALL SEGMENTS connecting towers to main keep ===
      [-1, 1].forEach((side) => {
        const cwX = ox + side * mI * 0.65;
        const cwY = mBase + mD * (side === -1 ? 0.6 : 1.4);
        const cwW = 6 * s;
        const cwH = 20 * s;
        drawIsometricPrism(ctx, cwX, cwY, cwW, cwW, cwH, cLight, cBlack, cDark);
        const cwI = cwW * ISO_COS;
        const cwD = cwW * ISO_SIN;
        const cwTop = cwY - cwH;
        drawIsometricPrism(
          ctx,
          cwX,
          cwTop,
          cwW + 2 * s,
          cwW + 2 * s,
          2 * s,
          cHi,
          cMid,
          cLight
        );
        const mrl = 2.2 * s;
        for (let i = 0; i < 2; i++) {
          const t = (i + 0.5) / 2;
          const mmx = cwX + (side === -1 ? -cwI * (1 - t) : cwI * t);
          const mmy =
            cwTop - 2 * s + cwD + (side === -1 ? cwD * t : cwD * (1 - t));
          drawIsometricPrism(
            ctx,
            mmx,
            mmy,
            mrl,
            mrl,
            3 * s,
            cEdge,
            cHi,
            cLight
          );
        }
        ctx.strokeStyle = "rgba(0,0,0,0.15)";
        ctx.lineWidth = 0.4 * s;
        for (let r = 1; r < 3; r++) {
          const ry = cwY - cwH * (r / 3);
          ctx.beginPath();
          ctx.moveTo(cwX - cwI, ry + cwD);
          ctx.lineTo(cwX, ry + cwD * 2);
          ctx.lineTo(cwX + cwI, ry + cwD);
          ctx.stroke();
        }
        const wFace = side === -1 ? ("left" as const) : ("right" as const);
        setShadowBlur(ctx, 5 * s, lGlow);
        ctx.fillStyle = `rgba(255,70,20,${0.3 + cPulse * 0.25})`;
        traceIsoFlushRect(
          ctx,
          cwX + (side === -1 ? -cwI * 0.35 : cwI * 0.35),
          cwY - cwH * 0.4 + cwD * 0.5,
          1.4,
          4,
          wFace,
          s
        );
        ctx.fill();
        clearShadow(ctx);
      });

      // === FLYING BUTTRESSES ===
      [-1, 1].forEach((side) => {
        const bStartX = ox + side * mI * 0.9;
        const bStartY = mBase + mD * (side === -1 ? 0.2 : 1.8) - mH * 0.3;
        const bEndX = ox + side * (mI + 8 * s * ISO_COS);
        const bEndY = fY + 14 * s + side * mD * 0.35 - 18 * s;
        ctx.fillStyle = cDark;
        ctx.lineWidth = 3 * s;
        ctx.beginPath();
        ctx.moveTo(bStartX, bStartY);
        ctx.lineTo(bEndX, bEndY);
        ctx.lineTo(bEndX + (side === -1 ? -2 * s : 2 * s), bEndY + 3 * s);
        ctx.lineTo(bStartX + (side === -1 ? 2 * s : -2 * s), bStartY + 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = cMid;
        ctx.beginPath();
        ctx.moveTo(bStartX, bStartY);
        ctx.lineTo(bEndX, bEndY);
        ctx.lineTo(bEndX, bEndY + 1.5 * s);
        ctx.lineTo(bStartX, bStartY + 1.5 * s);
        ctx.closePath();
        ctx.fill();
      });

      // === GARGOYLES on parapet ===
      [-1, 1].forEach((side) => {
        const gx = ox + side * parI * 0.65;
        const gy = parTop + parD * (side === -1 ? 1.35 : 0.65) - 1 * s;
        ctx.fillStyle = cLight;
        ctx.beginPath();
        ctx.moveTo(gx, gy);
        ctx.lineTo(gx + side * 5 * s, gy + 3 * s);
        ctx.lineTo(gx + side * 7 * s, gy + 2 * s);
        ctx.lineTo(gx + side * 8 * s, gy + 5 * s);
        ctx.lineTo(gx + side * 5 * s, gy + 6 * s);
        ctx.lineTo(gx, gy + 4 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = cMid;
        ctx.beginPath();
        ctx.moveTo(gx, gy + 4 * s);
        ctx.lineTo(gx + side * 5 * s, gy + 6 * s);
        ctx.lineTo(gx + side * 5 * s, gy + 8 * s);
        ctx.lineTo(gx, gy + 6 * s);
        ctx.closePath();
        ctx.fill();
        setShadowBlur(ctx, 3 * s, lGlow);
        ctx.fillStyle = `rgba(255,50,10,${0.5 + cPulse * 0.4})`;
        ctx.beginPath();
        ctx.arc(gx + side * 6 * s, gy + 2.5 * s, 0.8 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.arc(gx + side * 7 * s, gy + 2.5 * s, 0.8 * s, 0, Math.PI * 2);
        ctx.fill();
        clearShadow(ctx);
      });

      // === PORTCULLIS GRATE on gate ===
      const gateY2 = mBase + mD * 2;
      ctx.strokeStyle = "#2a2a30";
      ctx.lineWidth = 1 * s;
      for (let gi = 0; gi < 4; gi++) {
        const gx = ox - 3.5 * s + gi * 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(gx, gateY2);
        ctx.lineTo(gx, gateY2 - 14 * s);
        ctx.stroke();
      }
      for (let gi = 0; gi < 3; gi++) {
        const gy = gateY2 - 4 * s - gi * 4 * s;
        ctx.beginPath();
        ctx.moveTo(ox - 4 * s, gy);
        ctx.lineTo(ox + 4 * s, gy);
        ctx.stroke();
      }
      ctx.strokeStyle = cEdge;
      ctx.lineWidth = 0.6 * s;
      for (let gi = 0; gi < 4; gi++) {
        const gx = ox - 3.5 * s + gi * 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(gx + 0.5 * s, gateY2);
        ctx.lineTo(gx + 0.5 * s, gateY2 - 14 * s);
        ctx.stroke();
      }

      // === OBSIDIAN CRYSTALLINE FACET DETAILS ===
      const facetAlpha = 0.12 + cPulse * 0.08;
      ctx.strokeStyle = `rgba(100,100,160,${facetAlpha})`;
      ctx.lineWidth = 0.8 * s;
      [
        [
          -mI * 0.6,
          mBase + mD - mH * 0.15,
          -mI * 0.3,
          mBase + mD - mH * 0.45,
          -mI * 0.7,
          mBase + mD - mH * 0.55,
        ],
        [
          mI * 0.4,
          mBase + mD - mH * 0.1,
          mI * 0.65,
          mBase + mD - mH * 0.35,
          mI * 0.3,
          mBase + mD - mH * 0.5,
        ],
        [
          -mI * 0.2,
          mBase + mD * 2 - mH * 0.2,
          -mI * 0.1,
          mBase + mD * 2 - mH * 0.5,
          mI * 0.15,
          mBase + mD * 2 - mH * 0.45,
        ],
      ].forEach(([x1, y1, x2, y2, x3, y3]) => {
        ctx.beginPath();
        ctx.moveTo(ox + x1, y1);
        ctx.lineTo(ox + x2, y2);
        ctx.lineTo(ox + x3, y3);
        ctx.closePath();
        ctx.stroke();
      });

      // === ADDITIONAL LAVA FISSURES across foundation ===
      setShadowBlur(ctx, 4 * s, lGlow);
      ctx.strokeStyle = `rgba(255,61,0,${0.18 + cPulse * 0.12})`;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(ox - fI * 0.6, fY + fD * 0.3);
      ctx.lineTo(ox - fI * 0.25, fY + fD * 0.8);
      ctx.lineTo(ox - fI * 0.1, fY + fD * 1.2);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(ox + fI * 0.3, fY + fD * 0.5);
      ctx.lineTo(ox + fI * 0.5, fY + fD * 1);
      ctx.lineTo(ox + fI * 0.7, fY + fD * 0.8);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(ox - fI * 0.4, fY + fD * 1.5);
      ctx.lineTo(ox, fY + fD * 1.8);
      ctx.lineTo(ox + fI * 0.3, fY + fD * 1.5);
      ctx.stroke();
      clearShadow(ctx);

      // === RUBBLE & DEBRIS at base ===
      [
        { sz: 3, x: -22, y: 10 },
        { sz: 2.5, x: -14, y: 12 },
        { sz: 3, x: 16, y: 12 },
        { sz: 2.5, x: 22, y: 10 },
        { sz: 2, x: -8, y: 13 },
        { sz: 2, x: 10, y: 14 },
        { sz: 2, x: -18, y: 14 },
        { sz: 2.5, x: 18, y: 14 },
      ].forEach((rb, idx) => {
        const rx = ox + rb.x * s;
        const ry = fY + rb.y * s;
        const rSz = rb.sz * s;
        ctx.fillStyle = idx % 3 === 0 ? cLight : idx % 3 === 1 ? cMid : cDark;
        ctx.beginPath();
        ctx.moveTo(rx, ry - rSz * 0.8);
        ctx.lineTo(rx + rSz, ry - rSz * 0.3);
        ctx.lineTo(rx + rSz * 0.3, ry + rSz * 0.2);
        ctx.lineTo(rx - rSz * 0.7, ry - rSz * 0.2);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = cBlack;
        ctx.beginPath();
        ctx.moveTo(rx + rSz * 0.3, ry + rSz * 0.2);
        ctx.lineTo(rx + rSz, ry - rSz * 0.3);
        ctx.lineTo(rx + rSz * 0.8, ry + rSz * 0.2);
        ctx.lineTo(rx + rSz * 0.2, ry + rSz * 0.5);
        ctx.closePath();
        ctx.fill();
      });

      // === BANNER hanging from main keep ===
      const bannerX = ox + mI * 0.15;
      const bannerTopY = mBase + mD * 2 - mH * 0.7;
      const banWave = Math.sin(decorTime * 1.5) * 2 * s;
      ctx.fillStyle = cBlack;
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(bannerX - 3 * s, bannerTopY);
      ctx.quadraticCurveTo(
        bannerX - 3 * s + banWave,
        bannerTopY + 7 * s,
        bannerX - 4 * s,
        bannerTopY + 14 * s
      );
      ctx.lineTo(bannerX, bannerTopY + 12 * s);
      ctx.lineTo(bannerX + 4 * s, bannerTopY + 14 * s);
      ctx.quadraticCurveTo(
        bannerX + 3 * s - banWave,
        bannerTopY + 7 * s,
        bannerX + 3 * s,
        bannerTopY
      );
      ctx.closePath();
      ctx.fill();
      setShadowBlur(ctx, 4 * s, lGlow);
      ctx.fillStyle = `rgba(255,61,0,${0.6 + cPulse * 0.3})`;
      ctx.beginPath();
      ctx.moveTo(bannerX - 1 * s, bannerTopY + 3 * s);
      ctx.lineTo(bannerX + 1 * s + banWave * 0.3, bannerTopY + 6 * s);
      ctx.lineTo(bannerX - 1 * s, bannerTopY + 9 * s);
      ctx.closePath();
      ctx.fill();
      clearShadow(ctx);
      ctx.fillStyle = cEdge;
      ctx.beginPath();
      ctx.moveTo(bannerX - 4 * s, bannerTopY - 1 * s);
      ctx.lineTo(bannerX + 4 * s, bannerTopY - 1 * s);
      ctx.lineTo(bannerX + 4 * s, bannerTopY + 1 * s);
      ctx.lineTo(bannerX - 4 * s, bannerTopY + 1 * s);
      ctx.closePath();
      ctx.fill();

      // === ENHANCED FIRE PARTICLES ===
      for (let p = 0; p < 14; p++) {
        const pT = decorTime + p * 0.38;
        const pL = (pT * 0.4) % 1;
        const ppx = ox + Math.sin(pT * 2 + p) * 14 * s;
        const ppy = mBase + mD - 8 * s - pL * 55 * s;
        const pA = Math.sin(pL * Math.PI) * 0.8;
        const pR = (2 - pL * 1.5) * s;
        ctx.fillStyle = `rgba(255,${80 + p * 8},0,${pA * 0.6})`;
        ctx.beginPath();
        ctx.arc(ppx, ppy, pR + 1 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = `rgba(255,${120 + p * 6},0,${pA})`;
        ctx.beginPath();
        ctx.arc(ppx, ppy, pR, 0, Math.PI * 2);
        ctx.fill();
        if (pL < 0.3) {
          ctx.fillStyle = `rgba(255,255,180,${(1 - pL / 0.3) * 0.6})`;
          ctx.beginPath();
          ctx.arc(ppx, ppy, pR * 0.5, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // === SMOKE WISPS from towers ===
      for (let sw = 0; sw < 3; sw++) {
        const swTime = (decorTime * 0.3 + sw * 0.7) % 1;
        const swX = ox + Math.sin(sw * 2.5) * 8 * s;
        const swY = mTop - 18 * s - swTime * 30 * s;
        const swAlpha = (1 - swTime) * 0.12;
        ctx.fillStyle = `rgba(80,60,60,${swAlpha})`;
        ctx.beginPath();
        ctx.ellipse(
          swX + Math.sin(decorTime + sw) * 4 * s,
          swY,
          (3 + swTime * 4) * s,
          (2 + swTime * 2) * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      break;
    }

    case "dark_barracks": {
      const bx = screenPos.x;
      const by = screenPos.y;
      const obsBlack = "#0c0c12";
      const obsDark = "#161620";
      const obsMid = "#222234";
      const obsLight = "#383852";
      const obsHi = "#505072";
      const lavaCore = "#ff5a1f";
      const lavaGlow = "#ff8a2a";
      const pulse = 0.55 + Math.sin(decorTime * 2 + variant * 0.8) * 0.25;

      const kH = [50, 44, 58][variant % 3] * s;
      const kW = [22, 26, 20][variant % 3] * s;
      const tH2 = [62, 52, 72][variant % 3] * s;
      const peaked = variant === 2;
      const hasFlag = variant === 2;

      drawDirectionalShadow(
        ctx,
        bx,
        by + 3 * s,
        s,
        28 * s,
        12 * s,
        50 * s,
        0.4
      );

      drawIsometricPrism(
        ctx,
        bx,
        by,
        36 * s,
        36 * s,
        7 * s,
        obsMid,
        obsBlack,
        obsDark
      );

      drawIsometricPrism(
        ctx,
        bx,
        by - 7 * s,
        kW,
        kW,
        kH,
        obsLight,
        obsBlack,
        obsDark
      );

      const kI = kW * ISO_COS;
      const kD = kW * ISO_SIN;
      const kBase = by - 7 * s;
      const kTop = kBase - kH;

      ctx.strokeStyle = "rgba(0,0,0,0.18)";
      ctx.lineWidth = 0.5 * s;
      const nRows = Math.floor(kH / (7 * s));
      for (let row = 1; row < nRows; row++) {
        const ry = kBase - row * 7 * s;
        if (ry < kTop) {
          break;
        }
        ctx.beginPath();
        ctx.moveTo(bx - kI, ry + kD);
        ctx.lineTo(bx, ry + kD * 2);
        ctx.lineTo(bx + kI, ry + kD);
        ctx.stroke();
      }
      ctx.strokeStyle = obsHi;
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(bx, kBase + kD * 2);
      ctx.lineTo(bx, kTop + kD * 2);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(bx - kI, kBase + kD);
      ctx.lineTo(bx - kI, kTop + kD);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(bx + kI, kBase + kD);
      ctx.lineTo(bx + kI, kTop + kD);
      ctx.stroke();

      setShadowBlur(ctx, 6 * s, lavaCore);
      ctx.fillStyle = `rgba(255,90,30,${0.3 + pulse * 0.25})`;
      const bWinY = kBase - kH * 0.4;
      traceIsoFlushRect(
        ctx,
        bx - kI * 0.5,
        bWinY + kD * 0.5 + 3 * s,
        2,
        6,
        "left",
        s
      );
      ctx.fill();
      traceIsoFlushRect(
        ctx,
        bx + kI * 0.4,
        bWinY + kD * 0.5 + 1.5 * s,
        1.6,
        6,
        "right",
        s
      );
      ctx.fill();
      clearShadow(ctx);

      const gW2 = variant === 1 ? 10 * s : 7 * s;
      const gH2 = variant === 1 ? 24 * s : 20 * s;
      const gateY = kBase + kD * 2;
      ctx.fillStyle = "#050508";
      ctx.beginPath();
      ctx.moveTo(bx - gW2 * 0.5, gateY);
      ctx.lineTo(bx - gW2 * 0.5, gateY - gH2 + 6 * s);
      ctx.quadraticCurveTo(
        bx,
        gateY - gH2 - 2 * s,
        bx + gW2 * 0.5,
        gateY - gH2 + 6 * s
      );
      ctx.lineTo(bx + gW2 * 0.5, gateY);
      ctx.closePath();
      ctx.fill();
      const gGl = ctx.createLinearGradient(bx, gateY - gH2, bx, gateY);
      gGl.addColorStop(0, `rgba(255,138,42,${0.65 * pulse})`);
      gGl.addColorStop(1, "rgba(140,40,20,0.12)");
      ctx.fillStyle = gGl;
      ctx.beginPath();
      ctx.moveTo(bx - gW2 * 0.35, gateY - 1 * s);
      ctx.lineTo(bx - gW2 * 0.35, gateY - gH2 + 8 * s);
      ctx.quadraticCurveTo(
        bx,
        gateY - gH2 + 1 * s,
        bx + gW2 * 0.35,
        gateY - gH2 + 8 * s
      );
      ctx.lineTo(bx + gW2 * 0.35, gateY - 1 * s);
      ctx.closePath();
      ctx.fill();

      const slitY = kBase + kD - kH * 0.55;
      [-1, 1].forEach((side) => {
        const wx = bx + side * kI * 0.5;
        const wy = slitY + side * kD * 0.5;
        ctx.fillStyle = "#050508";
        ctx.fillRect(wx - 1.2 * s, wy - 4 * s, 2.4 * s, 8 * s);
        setShadowBlur(ctx, 6 * s, lavaCore);
        ctx.fillStyle = `rgba(255,120,40,${0.35 + pulse * 0.3})`;
        ctx.fillRect(wx - 0.6 * s, wy - 3 * s, 1.2 * s, 6 * s);
        clearShadow(ctx);
      });

      if (!peaked) {
        drawIsometricPrism(
          ctx,
          bx,
          kTop - 1 * s,
          kW + 3 * s,
          kW + 3 * s,
          4 * s,
          obsHi,
          obsMid,
          obsLight
        );
        const parI2 = (kW + 3 * s) * ISO_COS;
        const parD2 = (kW + 3 * s) * 0.5;
        const parTop2 = kTop - 1 * s - 4 * s;
        const merlH2 = 5 * s;
        const merlW2 = 3 * s;
        for (let i = 0; i < 3; i++) {
          const t = (i + 0.5) / 3;
          const mx2 = bx - parI2 * (1 - t);
          const my2 = parTop2 + parD2 + parD2 * t;
          drawIsometricPrism(
            ctx,
            mx2,
            my2,
            merlW2,
            merlW2,
            merlH2,
            obsHi,
            obsLight,
            obsMid
          );
        }
        for (let i = 0; i < 3; i++) {
          const t = (i + 0.5) / 3;
          const mx2 = bx + parI2 * t;
          const my2 = parTop2 + parD2 * 2 - parD2 * t;
          drawIsometricPrism(
            ctx,
            mx2,
            my2,
            merlW2,
            merlW2,
            merlH2,
            obsHi,
            obsLight,
            obsMid
          );
        }
      } else {
        drawIsometricPrism(
          ctx,
          bx,
          kTop - 1 * s,
          kW + 2 * s,
          kW + 2 * s,
          3 * s,
          obsLight,
          obsMid,
          obsMid
        );
        const roofW = kW + 3 * s;
        const roofI = roofW * ISO_COS;
        const roofD = roofW * ISO_SIN;
        const roofBase = kTop - 1 * s - 3 * s;
        const ridgeH = 16 * s;
        const ridgeLen = roofI * 0.8;
        const ridgeTop = roofBase - ridgeH;

        ctx.fillStyle = obsBlack;
        ctx.beginPath();
        ctx.moveTo(bx - ridgeLen, ridgeTop + ridgeLen * 0.5);
        ctx.lineTo(bx - roofI, roofBase + roofD);
        ctx.lineTo(bx, roofBase + roofD * 2);
        ctx.lineTo(bx + ridgeLen, ridgeTop + ridgeLen * 0.5 + roofD * 2);
        ctx.closePath();
        ctx.fill();

        ctx.fillStyle = obsMid;
        ctx.beginPath();
        ctx.moveTo(bx + ridgeLen, ridgeTop + ridgeLen * 0.5 + roofD * 2);
        ctx.lineTo(bx, roofBase + roofD * 2);
        ctx.lineTo(bx + roofI, roofBase + roofD);
        ctx.lineTo(bx + ridgeLen, ridgeTop + ridgeLen * 0.5);
        ctx.closePath();
        ctx.fill();

        ctx.fillStyle = obsLight;
        ctx.beginPath();
        ctx.moveTo(bx - ridgeLen, ridgeTop + ridgeLen * 0.5);
        ctx.lineTo(bx - roofI, roofBase + roofD);
        ctx.lineTo(bx + roofI, roofBase + roofD);
        ctx.lineTo(bx + ridgeLen, ridgeTop + ridgeLen * 0.5);
        ctx.closePath();
        ctx.fill();

        ctx.strokeStyle = obsHi;
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(bx - ridgeLen, ridgeTop + ridgeLen * 0.5);
        ctx.lineTo(bx + ridgeLen, ridgeTop + ridgeLen * 0.5);
        ctx.stroke();
      }

      [-1, 1].forEach((side) => {
        const tW2 = 7 * s;
        const ttx = bx + side * (kI + tW2 * ISO_COS + 2 * s);
        const tty = by - 5 * s + side * (kD * 0.3);
        const tTW = 3 * s;
        const tBase2 = tty;
        const tTop2 = tBase2 - tH2;
        const tbI = tW2 * ISO_COS;
        const tbD = tW2 * ISO_SIN;
        const ttI = tTW * ISO_COS;
        const ttD = tTW * ISO_SIN;

        ctx.fillStyle = obsBlack;
        ctx.beginPath();
        ctx.moveTo(ttx - tbI, tBase2 + tbD);
        ctx.lineTo(ttx, tBase2 + tbD * 2);
        ctx.lineTo(ttx, tTop2 + ttD * 2);
        ctx.lineTo(ttx - ttI, tTop2 + ttD);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = obsDark;
        ctx.beginPath();
        ctx.moveTo(ttx + tbI, tBase2 + tbD);
        ctx.lineTo(ttx, tBase2 + tbD * 2);
        ctx.lineTo(ttx, tTop2 + ttD * 2);
        ctx.lineTo(ttx + ttI, tTop2 + ttD);
        ctx.closePath();
        ctx.fill();

        ctx.fillStyle = obsLight;
        ctx.beginPath();
        ctx.moveTo(ttx, tTop2);
        ctx.lineTo(ttx + ttI, tTop2 + ttD);
        ctx.lineTo(ttx, tTop2 + ttD * 2);
        ctx.lineTo(ttx - ttI, tTop2 + ttD);
        ctx.closePath();
        ctx.fill();

        ctx.strokeStyle = obsMid;
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(ttx, tBase2 + tbD * 2);
        ctx.lineTo(ttx, tTop2 + ttD * 2);
        ctx.stroke();

        drawIsometricPyramid(
          ctx,
          ttx,
          tTop2,
          tTW * 1.3,
          14 * s,
          obsLight,
          obsBlack,
          obsMid
        );

        ctx.strokeStyle = obsHi;
        ctx.lineWidth = 0.5 * s;
        for (let r = 1; r < 4; r++) {
          const rt = r / 4;
          const ry = tBase2 + (tTop2 - tBase2) * rt;
          const gI = tbI + (ttI - tbI) * rt;
          const gD2 = tbD + (ttD - tbD) * rt;
          ctx.beginPath();
          ctx.moveTo(ttx - gI, ry + gD2);
          ctx.lineTo(ttx, ry + gD2 * 2);
          ctx.lineTo(ttx + gI, ry + gD2);
          ctx.stroke();
        }

        setShadowBlur(ctx, 7 * s, lavaCore);
        const twY = tBase2 + tbD - tH2 * 0.4;
        ctx.fillStyle = `rgba(255,110,40,${0.4 + pulse * 0.3})`;
        const t2Face = side === -1 ? ("left" as const) : ("right" as const);
        traceIsoFlushRect(ctx, ttx, twY, 2, 7, t2Face, s);
        ctx.fill();
        const twY2 = tBase2 + tbD - tH2 * 0.65;
        traceIsoFlushRect(ctx, ttx, twY2, 1.6, 6, t2Face, s);
        ctx.fill();
        clearShadow(ctx);
      });

      setShadowBlur(ctx, 5 * s, lavaGlow);
      ctx.strokeStyle = `rgba(255,110,20,${0.2 + pulse * 0.15})`;
      ctx.lineWidth = 1 * s;
      const fI2 = 36 * s * ISO_COS;
      const fD2 = 36 * s * ISO_SIN;
      ctx.beginPath();
      ctx.moveTo(bx - fI2 * 0.5, by + fD2 * 0.6);
      ctx.lineTo(bx, by + fD2 * 1.1);
      ctx.lineTo(bx + fI2 * 0.5, by + fD2 * 0.6);
      ctx.stroke();
      clearShadow(ctx);

      if (hasFlag) {
        const fpB = peaked ? kTop - 20 * s : kTop - 9 * s;
        const fpT = fpB - 18 * s;
        ctx.strokeStyle = "#444";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(bx, fpB);
        ctx.lineTo(bx, fpT);
        ctx.stroke();
        ctx.fillStyle = "#6a1020";
        ctx.beginPath();
        ctx.moveTo(bx, fpT);
        ctx.lineTo(
          bx + 10 * s,
          fpT + 3 * s + Math.sin(decorTime * 3) * 1.5 * s
        );
        ctx.lineTo(bx + 8 * s, fpT + 8 * s + Math.sin(decorTime * 3 + 0.5) * s);
        ctx.lineTo(bx, fpT + 6 * s);
        ctx.closePath();
        ctx.fill();
      }

      setShadowBlur(ctx, 10 * s, lavaGlow);
      ctx.strokeStyle = `rgba(255,110,20,${0.5 + pulse * 0.2})`;
      ctx.lineWidth = 1.3 * s;
      const chevY = kBase + kD - kH * 0.75;
      ctx.beginPath();
      ctx.moveTo(bx - kI * 0.3, chevY + kD * 0.3);
      ctx.lineTo(bx, chevY - 3 * s);
      ctx.lineTo(bx + kI * 0.3, chevY + kD * 0.3);
      ctx.stroke();
      clearShadow(ctx);

      for (let i = 0; i < 5; i++) {
        const t = (decorTime * 0.75 + i * 0.35 + variant * 0.11) % 1.2;
        const ex = bx + (i - 2) * 8 * s + Math.sin(decorTime * 2 + i) * 3 * s;
        const ey = kBase + kD - 14 * s - t * 35 * s;
        const ea = Math.max(0, (1 - t / 1.2) * 0.55);
        ctx.fillStyle = `rgba(255,130,40,${ea})`;
        ctx.beginPath();
        ctx.arc(ex, ey, (1.8 - t) * s, 0, Math.PI * 2);
        ctx.fill();
      }

      break;
    }

    case "dark_throne": {
      const tx = screenPos.x;
      const ty = screenPos.y;
      const obsBlack = "#0c0c14";
      const obsDark = "#181826";
      const obsMid = "#282840";
      const obsLight = "#454568";
      const obsHi = "#5c5c88";
      const velvetDk = "#3a0815";
      const velvetMd = "#641024";
      const velvetLt = "#901838";
      const brass = "#a8854a";
      const brassHi = "#c8a55a";
      const rPulse = 0.5 + Math.sin(decorTime * 2.2 + variant * 0.7) * 0.25;

      drawDirectionalShadow(
        ctx,
        tx,
        ty + 4 * s,
        s,
        24 * s,
        12 * s,
        45 * s,
        0.38
      );

      drawIsometricPrism(
        ctx,
        tx,
        ty,
        32 * s,
        32 * s,
        6 * s,
        obsMid,
        obsBlack,
        obsDark
      );
      drawIsometricPrism(
        ctx,
        tx,
        ty - 6 * s,
        26 * s,
        26 * s,
        5 * s,
        obsLight,
        obsDark,
        obsMid
      );
      ctx.strokeStyle = brass;
      ctx.lineWidth = 0.8 * s;
      const d1I = 26 * s * ISO_COS;
      const d1D = 26 * s * ISO_SIN;
      ctx.beginPath();
      ctx.moveTo(tx - d1I, ty - 6 * s + d1D);
      ctx.lineTo(tx, ty - 6 * s + d1D * 2);
      ctx.lineTo(tx + d1I, ty - 6 * s + d1D);
      ctx.stroke();

      setShadowBlur(ctx, 4 * s, "#c03030");
      ctx.strokeStyle = `rgba(200,50,50,${0.15 + rPulse * 0.12})`;
      ctx.lineWidth = 0.8 * s;
      const d0I = 32 * s * ISO_COS;
      const d0D = 32 * s * ISO_SIN;
      ctx.beginPath();
      ctx.moveTo(tx - d0I * 0.35, ty + d0D * 0.7);
      ctx.lineTo(tx, ty + d0D * 1.05);
      ctx.lineTo(tx + d0I * 0.35, ty + d0D * 0.7);
      ctx.stroke();
      clearShadow(ctx);

      const sW = 16 * s;
      const sH = 14 * s;
      const sBase = ty - 11 * s;
      drawIsometricPrism(ctx, tx, sBase, sW, sW, sH, obsMid, obsBlack, obsDark);

      const sI = sW * ISO_COS;
      const sD2 = sW * ISO_SIN;
      const cushTop = sBase - sH;
      const cG = ctx.createLinearGradient(tx, cushTop, tx, cushTop + 4 * s);
      cG.addColorStop(0, velvetLt);
      cG.addColorStop(0.5, velvetMd);
      cG.addColorStop(1, velvetDk);
      ctx.fillStyle = cG;
      ctx.beginPath();
      ctx.moveTo(tx, cushTop + 1 * s);
      ctx.lineTo(tx + sI * 0.85, cushTop + sD2 * 0.85 + 1 * s);
      ctx.lineTo(tx, cushTop + sD2 * 1.7 + 1 * s);
      ctx.lineTo(tx - sI * 0.85, cushTop + sD2 * 0.85 + 1 * s);
      ctx.closePath();
      ctx.fill();

      [-1, 1].forEach((side) => {
        const ax = tx + side * sI * 1.1;
        const ay = cushTop + sD2 + 2 * s;
        ctx.fillStyle = obsMid;
        ctx.beginPath();
        ctx.moveTo(tx + side * sI * 0.8, cushTop + sD2 * 0.8);
        ctx.lineTo(ax, ay - 3 * s);
        ctx.lineTo(ax, ay - 9 * s);
        ctx.lineTo(tx + side * sI * 0.8, cushTop + sD2 * 0.8 - 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = obsHi;
        ctx.beginPath();
        ctx.arc(ax, ay - 6 * s, 2 * s, 0, Math.PI * 2);
        ctx.fill();
        setShadowBlur(ctx, 5 * s, "#c03030");
        ctx.fillStyle = `rgba(200,50,50,${0.5 + rPulse * 0.3})`;
        ctx.beginPath();
        ctx.arc(ax, ay - 6 * s, 1.2 * s, 0, Math.PI * 2);
        ctx.fill();
        clearShadow(ctx);
      });

      const bkW = 18 * s;
      const bkTW = 10 * s;
      const bkH = 62 * s;
      const bkBase = cushTop;
      const bkTop = bkBase - bkH;
      const bI = bkW * ISO_COS;
      const bD2 = bkW * ISO_SIN;
      const btI = bkTW * ISO_COS;
      const btD = bkTW * ISO_SIN;

      ctx.fillStyle = obsBlack;
      ctx.beginPath();
      ctx.moveTo(tx - bI, bkBase + bD2);
      ctx.lineTo(tx, bkBase + bD2 * 2);
      ctx.lineTo(tx, bkTop + btD * 2);
      ctx.lineTo(tx - btI, bkTop + btD);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = obsDark;
      ctx.beginPath();
      ctx.moveTo(tx + bI, bkBase + bD2);
      ctx.lineTo(tx, bkBase + bD2 * 2);
      ctx.lineTo(tx, bkTop + btD * 2);
      ctx.lineTo(tx + btI, bkTop + btD);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = obsMid;
      ctx.beginPath();
      ctx.moveTo(tx - bI, bkBase + bD2);
      ctx.lineTo(tx + bI, bkBase + bD2);
      ctx.lineTo(tx + btI, bkTop + btD);
      ctx.lineTo(tx, bkTop);
      ctx.lineTo(tx - btI, bkTop + btD);
      ctx.closePath();
      ctx.fill();

      ctx.strokeStyle = obsHi;
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(tx, bkBase + bD2 * 2);
      ctx.lineTo(tx, bkTop + btD * 2);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(tx - bI, bkBase + bD2);
      ctx.lineTo(tx - btI, bkTop + btD);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(tx + bI, bkBase + bD2);
      ctx.lineTo(tx + btI, bkTop + btD);
      ctx.stroke();

      [-1, 1].forEach((side) => {
        const wx = tx + side * bI * 0.9;
        const wy = bkBase + bD2 * 0.9 - 5 * s;
        const wTy = bkTop + 20 * s;
        ctx.fillStyle = side < 0 ? obsBlack : obsDark;
        ctx.beginPath();
        ctx.moveTo(wx, wy);
        ctx.lineTo(wx + side * 14 * s, wy - 8 * s);
        ctx.lineTo(wx + side * 16 * s, (wy + wTy) * 0.5 - 5 * s);
        ctx.lineTo(wx + side * 10 * s, wTy);
        ctx.lineTo(wx, wTy + 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = obsLight;
        ctx.lineWidth = 0.5 * s;
        ctx.stroke();
      });

      const bnG = ctx.createLinearGradient(
        tx,
        bkTop + 14 * s,
        tx,
        bkBase - 8 * s
      );
      bnG.addColorStop(0, velvetDk);
      bnG.addColorStop(0.4, velvetMd);
      bnG.addColorStop(0.7, velvetLt);
      bnG.addColorStop(1, velvetDk);
      ctx.fillStyle = bnG;
      ctx.beginPath();
      ctx.moveTo(tx - btI * 0.8, bkBase + bD2 * 0.4);
      ctx.lineTo(tx - btI * 0.85, bkTop + btD + 10 * s);
      ctx.lineTo(tx, bkTop + 5 * s);
      ctx.lineTo(tx + btI * 0.85, bkTop + btD + 10 * s);
      ctx.lineTo(tx + btI * 0.8, bkBase + bD2 * 0.4);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = brassHi;
      ctx.lineWidth = 1 * s;
      ctx.stroke();

      setShadowBlur(ctx, 4 * s, "#c03030");
      ctx.strokeStyle = `rgba(200,50,50,${0.2 + rPulse * 0.15})`;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(tx - btI * 0.6, bkBase + bD2 * 0.35);
      ctx.lineTo(tx, bkBase + bD2 * 0.35 + 4 * s);
      ctx.lineTo(tx + btI * 0.6, bkBase + bD2 * 0.35);
      ctx.stroke();
      clearShadow(ctx);

      [-12, -6, 0, 6, 12].forEach((off, idx) => {
        const spkH = idx === 2 ? 14 * s : 9 * s;
        const spkW = idx === 2 ? 2.5 * s : 1.8 * s;
        const sy = bkTop + btD + (idx === 2 ? -2 * s : 4 * s);
        drawIsometricPyramid(
          ctx,
          tx + off * s,
          sy,
          spkW,
          spkH,
          brassHi,
          obsBlack,
          obsLight
        );
      });

      ctx.fillStyle = "#505060";
      ctx.beginPath();
      ctx.ellipse(tx, bkTop + btD + 8 * s, 5 * s, 4.5 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      setShadowBlur(ctx, 12 * s, "#c03030");
      ctx.fillStyle = `rgba(255,60,60,${0.55 + rPulse * 0.3})`;
      ctx.beginPath();
      ctx.arc(tx - 1.8 * s, bkTop + btD + 7 * s, 1.2 * s, 0, Math.PI * 2);
      ctx.arc(tx + 1.8 * s, bkTop + btD + 7 * s, 1.2 * s, 0, Math.PI * 2);
      ctx.fill();
      clearShadow(ctx);

      setShadowBlur(ctx, 8 * s, "#8a1010");
      ctx.strokeStyle = `rgba(220,40,40,${0.5 + rPulse * 0.3})`;
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(tx, bkBase + bD2 * 0.3);
      ctx.lineTo(tx, bkTop + btD + 16 * s);
      ctx.stroke();
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(tx - 3 * s, bkTop + btD + 20 * s);
      ctx.lineTo(tx, bkTop + btD + 14 * s);
      ctx.lineTo(tx + 3 * s, bkTop + btD + 20 * s);
      ctx.stroke();
      clearShadow(ctx);

      for (let i = 0; i < 6; i++) {
        const t = (decorTime * 0.6 + i * 0.45) % 2;
        const ang = (i / 6) * Math.PI * 2 + decorTime * 0.3;
        const dr = 18 * s + Math.sin(t * Math.PI) * 8 * s;
        const ex = tx + Math.cos(ang) * dr;
        const ey = bkTop + 5 * s - t * 12 * s;
        const ea = Math.max(0, (1 - t / 2) * 0.4);
        ctx.fillStyle = `rgba(180,30,30,${ea})`;
        ctx.beginPath();
        ctx.arc(ex, ey, (2 - t * 0.6) * s, 0, Math.PI * 2);
        ctx.fill();
      }

      break;
    }

    case "dark_spire": {
      const dx = screenPos.x;
      const dy = screenPos.y;
      const dsBlack = "#0a0a14";
      const dsDark = "#141422";
      const dsMid = "#222238";
      const dsLight = "#3a3a58";
      const dsHi = "#505078";
      const dsRune = "#cc3030";
      const dsPulse = 0.5 + Math.sin(decorTime * 2.8 + variant * 1.1) * 0.25;
      const dsHM = [1, 0.85, 1.15][variant % 3];
      const dsWM = [1, 1.1, 0.9][variant % 3];

      drawDirectionalShadow(
        ctx,
        dx,
        dy + 2 * s,
        s,
        14 * s * dsWM,
        6 * s,
        55 * s * dsHM,
        0.35
      );

      drawIsometricPrism(
        ctx,
        dx,
        dy,
        14 * s * dsWM,
        14 * s * dsWM,
        6 * s,
        dsMid,
        dsBlack,
        dsDark
      );

      const spH = 55 * s * dsHM;
      const spBW = 7 * s * dsWM;
      const spTW2 = 2.5 * s * dsWM;
      const spBase = dy - 6 * s;
      const spTop = spBase - spH;
      const spBI = spBW * ISO_COS;
      const spBD = spBW * ISO_SIN;
      const spTI = spTW2 * ISO_COS;
      const spTD = spTW2 * ISO_SIN;

      ctx.fillStyle = dsBlack;
      ctx.beginPath();
      ctx.moveTo(dx - spBI, spBase + spBD);
      ctx.lineTo(dx, spBase + spBD * 2);
      ctx.lineTo(dx, spTop + spTD * 2);
      ctx.lineTo(dx - spTI, spTop + spTD);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = dsDark;
      ctx.beginPath();
      ctx.moveTo(dx + spBI, spBase + spBD);
      ctx.lineTo(dx, spBase + spBD * 2);
      ctx.lineTo(dx, spTop + spTD * 2);
      ctx.lineTo(dx + spTI, spTop + spTD);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = dsLight;
      ctx.beginPath();
      ctx.moveTo(dx, spTop);
      ctx.lineTo(dx + spTI, spTop + spTD);
      ctx.lineTo(dx, spTop + spTD * 2);
      ctx.lineTo(dx - spTI, spTop + spTD);
      ctx.closePath();
      ctx.fill();

      ctx.strokeStyle = dsHi;
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(dx, spBase + spBD * 2);
      ctx.lineTo(dx, spTop + spTD * 2);
      ctx.stroke();

      ctx.strokeStyle = "rgba(0,0,0,0.15)";
      ctx.lineWidth = 0.4 * s;
      for (let b = 1; b < 6; b++) {
        const bt = b / 6;
        const by2 = spBase + (spTop - spBase) * bt;
        const bgI = spBI + (spTI - spBI) * bt;
        const bgD = spBD + (spTD - spBD) * bt;
        ctx.beginPath();
        ctx.moveTo(dx - bgI, by2 + bgD);
        ctx.lineTo(dx, by2 + bgD * 2);
        ctx.lineTo(dx + bgI, by2 + bgD);
        ctx.stroke();
      }

      ctx.fillStyle = "rgba(100,100,180,0.08)";
      ctx.beginPath();
      ctx.moveTo(dx + spBI * 0.15, spBase + spBD * 1.7);
      ctx.lineTo(dx + spBI * 0.8, spBase + spBD * 0.85);
      ctx.lineTo(dx + spTI * 0.8, spTop + spTD * 0.85);
      ctx.lineTo(dx + spTI * 0.15, spTop + spTD * 1.7);
      ctx.closePath();
      ctx.fill();

      [-1, 1].forEach((bSide) => {
        const buttH = 12 * s;
        const buttW = 3 * s * dsWM;
        const buttX = dx + bSide * spBI * 0.85;
        const buttY = spBase + spBD * (bSide < 0 ? 0.85 : 0.85);
        ctx.fillStyle = bSide < 0 ? dsBlack : dsDark;
        ctx.beginPath();
        ctx.moveTo(buttX, buttY);
        ctx.lineTo(buttX + bSide * buttW, buttY - buttW * 0.3);
        ctx.lineTo(buttX + bSide * buttW * 0.3, buttY - buttH);
        ctx.lineTo(buttX, buttY - buttH + 2 * s);
        ctx.closePath();
        ctx.fill();
      });

      drawIsometricPyramid(
        ctx,
        dx,
        spTop,
        spTW2,
        16 * s * dsHM,
        dsHi,
        dsBlack,
        dsMid
      );

      setShadowBlur(ctx, 8 * s, dsRune);
      ctx.strokeStyle = `rgba(200,50,50,${0.3 + dsPulse * 0.4})`;
      ctx.lineWidth = 1.2 * s;
      for (let i = 0; i < 3; i++) {
        const rt = 0.2 + (i / 3) * 0.55;
        const ry = spBase + (spTop - spBase) * rt;
        const gI = spBI + (spTI - spBI) * rt;
        const gDy = spBD + (spTD - spBD) * rt;
        ctx.beginPath();
        ctx.moveTo(dx - gI * 0.4, ry + gDy);
        ctx.lineTo(dx, ry + gDy - 2.5 * s);
        ctx.lineTo(dx + gI * 0.4, ry + gDy);
        ctx.lineTo(dx, ry + gDy + 2.5 * s);
        ctx.closePath();
        ctx.stroke();
      }
      clearShadow(ctx);

      const winY = spBase + (spTop - spBase) * 0.4;
      const winGD = spBD + (spTD - spBD) * 0.4;
      setShadowBlur(ctx, 8 * s, dsRune);
      ctx.fillStyle = `rgba(220,60,60,${0.4 + dsPulse * 0.3})`;
      ctx.fillRect(dx - 1 * s, winY + winGD * 2 - 4 * s, 2 * s, 8 * s);
      clearShadow(ctx);

      const winY2 = spBase + (spTop - spBase) * 0.65;
      const winGD2 = spBD + (spTD - spBD) * 0.65;
      setShadowBlur(ctx, 6 * s, dsRune);
      ctx.fillStyle = `rgba(220,60,60,${0.3 + dsPulse * 0.2})`;
      ctx.fillRect(dx - 0.8 * s, winY2 + winGD2 * 2 - 3 * s, 1.6 * s, 6 * s);
      clearShadow(ctx);

      setShadowBlur(ctx, 5 * s, dsRune);
      ctx.strokeStyle = `rgba(200,50,50,${0.2 + dsPulse * 0.15})`;
      ctx.lineWidth = 0.8 * s;
      const baseI = 14 * s * dsWM * ISO_COS;
      const baseD = 14 * s * dsWM * ISO_SIN;
      ctx.beginPath();
      ctx.moveTo(dx - baseI * 0.4, dy + baseD * 0.7);
      ctx.lineTo(dx, dy + baseD * 1.1);
      ctx.lineTo(dx + baseI * 0.4, dy + baseD * 0.7);
      ctx.stroke();
      clearShadow(ctx);

      for (let i = 0; i < 4; i++) {
        const t = (decorTime * 0.7 + i * 0.5 + variant * 0.4) % 1.5;
        const ex = dx + Math.sin(decorTime * 1.3 + i * 2) * 6 * s;
        const ey = spTop - 5 * s - t * 18 * s;
        const ea = Math.max(0, (1 - t / 1.5) * 0.55);
        ctx.fillStyle = `rgba(200,60,60,${ea})`;
        ctx.beginPath();
        ctx.arc(ex, ey, (1.3 - t * 0.7) * s, 0, Math.PI * 2);
        ctx.fill();
      }

      break;
    }

    case "lava_fall": {
      const lx = screenPos.x;
      const ly = screenPos.y;
      const rkBlack = "#120a06";
      const rkDark = "#1a120c";
      const rkMid = "#2d1e14";
      const rkLight = "#3e2a1a";
      const rkHi = "#54382a";
      const rkEdge = "#6a4a38";
      const rkTop = "#7a5842";
      const magYel = "#ffeb3b";
      const magOrg = "#ff9800";
      const magRed = "#f44336";
      const magDk = "#bf360c";
      const poolP = 0.7 + Math.sin(decorTime * 3 + variant) * 0.15;
      const wide = variant === 0;

      const cW = wide ? 18 * s : 12 * s;
      const cH = wide ? 52 * s : 62 * s;
      const cI = cW * ISO_COS;
      const cD = cW * ISO_SIN;
      const cascHalf = wide ? 5 * s : 3.5 * s;
      const poolRx = wide ? 16 * s : 11 * s;
      const poolRy = wide ? 7 * s : 5 * s;

      const hG = ctx.createRadialGradient(lx, ly, 0, lx, ly, 45 * s);
      const hp = 0.2 + Math.sin(decorTime * 2 + variant) * 0.08;
      hG.addColorStop(0, `rgba(255,100,0,${hp})`);
      hG.addColorStop(1, "transparent");
      ctx.fillStyle = hG;
      ctx.beginPath();
      ctx.ellipse(lx, ly, 45 * s, 22 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      drawDirectionalShadow(
        ctx,
        lx,
        ly + 3 * s,
        s,
        22 * s,
        10 * s,
        52 * s,
        0.32
      );

      const debrisData: number[][] = wide
        ? [
            [-24, 4, 5],
            [22, 6, 4.5],
            [-17, 9, 3],
            [26, 2, 3.5],
            [-9, 11, 2.5],
            [16, 10, 2.8],
          ]
        : [
            [-18, 5, 4],
            [16, 7, 3.5],
            [-12, 10, 2.5],
            [20, 3, 3],
            [-6, 12, 2],
          ];
      debrisData.forEach(([rx, ry, rSz]) => {
        drawIsometricPrism(
          ctx,
          lx + rx * s,
          ly + ry * s,
          rSz * s,
          rSz * s,
          rSz * 0.7 * s,
          rkLight,
          rkBlack,
          rkDark
        );
      });

      ctx.fillStyle = rkDark;
      ctx.beginPath();
      ctx.ellipse(
        lx,
        ly + 3 * s,
        poolRx + 4 * s,
        poolRy + 3 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      const pG = ctx.createRadialGradient(
        lx,
        ly + 3 * s,
        0,
        lx,
        ly + 3 * s,
        poolRx
      );
      pG.addColorStop(0, magYel);
      pG.addColorStop(0.3, magOrg);
      pG.addColorStop(0.6, magRed);
      pG.addColorStop(1, magDk);
      ctx.fillStyle = pG;
      ctx.beginPath();
      ctx.ellipse(lx, ly + 3 * s, poolRx, poolRy, 0, 0, Math.PI * 2);
      ctx.fill();

      ctx.fillStyle = `rgba(255,235,59,${poolP * 0.4})`;
      ctx.beginPath();
      ctx.ellipse(
        lx - 2 * s,
        ly + 2 * s,
        poolRx * 0.35,
        poolRy * 0.3,
        -0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();

      ctx.strokeStyle = rkMid;
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.arc(lx, ly + 3 * s, poolRx - 1 * s, Math.PI * 0.55, Math.PI * 0.9);
      ctx.stroke();
      ctx.beginPath();
      ctx.arc(lx, ly + 3 * s, poolRx - 1.5 * s, Math.PI * 1.15, Math.PI * 1.45);
      ctx.stroke();

      const clBase = ly - 2 * s;
      const clTop = clBase - cH;
      const topBk = clTop;
      const topLt = clTop + cD * 0.4;
      const topFr = clTop + cD;
      const topRt = clTop + cD * 0.3;

      ctx.fillStyle = rkBlack;
      ctx.beginPath();
      ctx.moveTo(lx - cI, clBase + cD);
      ctx.lineTo(lx, clBase + cD * 2);
      ctx.lineTo(lx, topFr);
      ctx.lineTo(lx - cI * 0.9, topLt);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = rkMid;
      ctx.beginPath();
      ctx.moveTo(lx + cI, clBase + cD);
      ctx.lineTo(lx, clBase + cD * 2);
      ctx.lineTo(lx, topFr);
      ctx.lineTo(lx + cI * 0.95, topRt);
      ctx.closePath();
      ctx.fill();

      ctx.strokeStyle = rkLight;
      ctx.lineWidth = 0.5 * s;
      for (let r = 1; r < 7; r++) {
        const rt = r / 7;
        const ry = clBase + (clTop - clBase) * rt;
        const rj = Math.sin(r * 4.2 + variant * 2) * 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(
          lx - cI * (0.9 + (1 - 0.9) * (1 - rt)),
          ry + cD * (1 - rt * 0.6) + rj
        );
        ctx.lineTo(lx, ry + cD * (2 - rt) + rj * 0.5);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(lx, ry + cD * (2 - rt) + rj * 0.5);
        ctx.lineTo(
          lx + cI * (0.95 + (1 - 0.95) * (1 - rt)),
          ry + cD * (1 - rt * 0.7) - rj * 0.3
        );
        ctx.stroke();
      }

      ctx.strokeStyle = rkEdge;
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(lx, clBase + cD * 2);
      ctx.lineTo(lx, topFr);
      ctx.stroke();

      ctx.fillStyle = "rgba(80,60,40,0.06)";
      ctx.beginPath();
      ctx.moveTo(lx + cI * 0.15, clBase + cD * 1.7);
      ctx.lineTo(lx + cI * 0.8, clBase + cD * 0.85);
      ctx.lineTo(lx + cI * 0.85, topRt + cD * 0.3);
      ctx.lineTo(lx + cI * 0.15, topFr - 2 * s);
      ctx.closePath();
      ctx.fill();

      const ledges: number[][] = wide
        ? [
            [0.3, -0.7, 6],
            [0.55, 0.5, 5],
            [0.75, -0.4, 4],
          ]
        : [
            [0.25, -0.6, 5],
            [0.5, 0.4, 4.5],
            [0.7, -0.5, 3.5],
          ];
      ledges.forEach(([ht, side, sz]) => {
        const ledgeY = clBase + (clTop - clBase) * ht;
        const ledgeX =
          side < 0
            ? lx - cI * 0.85 + side * sz * s * 0.3
            : lx + cI * 0.85 + side * sz * s * 0.3;
        const ledgeD = cD * (1 - ht * 0.5);
        drawIsometricPrism(
          ctx,
          ledgeX,
          ledgeY + ledgeD * 0.5,
          sz * s,
          sz * s,
          sz * 0.5 * s,
          rkHi,
          rkBlack,
          rkDark
        );
      });

      ctx.fillStyle = rkEdge;
      ctx.beginPath();
      ctx.moveTo(lx - cI * 0.9, topLt);
      ctx.lineTo(lx, topBk - 1 * s);
      ctx.lineTo(lx + cI * 0.95, topRt);
      ctx.lineTo(lx, topFr);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = rkTop;
      ctx.beginPath();
      ctx.moveTo(lx - cI * 0.9, topLt);
      ctx.lineTo(lx, topBk - 1 * s);
      ctx.lineTo(lx + cI * 0.95, topRt);
      ctx.lineTo(lx + cI * 0.3, topRt + cD * 0.4);
      ctx.lineTo(lx - cI * 0.3, topLt + cD * 0.5);
      ctx.closePath();
      ctx.fill();

      ctx.strokeStyle = rkHi;
      ctx.lineWidth = 0.6 * s;
      ctx.beginPath();
      ctx.moveTo(lx - cI * 0.85, topLt + 1 * s);
      ctx.lineTo(lx - cI * 0.4, topLt + cD * 0.3);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(lx + cI * 0.4, topRt + cD * 0.2);
      ctx.lineTo(lx + cI * 0.9, topRt + 1 * s);
      ctx.stroke();

      const srcY = topFr - 2 * s;
      const srcGrad = ctx.createRadialGradient(
        lx,
        srcY,
        0,
        lx,
        srcY,
        cascHalf * 2.5
      );
      srcGrad.addColorStop(0, magYel);
      srcGrad.addColorStop(0.5, magOrg);
      srcGrad.addColorStop(1, magRed);
      ctx.fillStyle = srcGrad;
      ctx.beginPath();
      ctx.ellipse(lx, srcY, cascHalf * 1.8, cascHalf * 0.8, 0, 0, Math.PI * 2);
      ctx.fill();

      const cascGrad = ctx.createLinearGradient(lx, srcY, lx, ly + 3 * s);
      cascGrad.addColorStop(0, magYel);
      cascGrad.addColorStop(0.15, magOrg);
      cascGrad.addColorStop(0.4, magRed);
      cascGrad.addColorStop(0.8, magDk);
      cascGrad.addColorStop(1, magOrg);
      ctx.fillStyle = cascGrad;
      const wob1 = Math.sin(decorTime * 3.5) * 0.8 * s;
      const wob2 = Math.sin(decorTime * 4.2 + 1) * 0.6 * s;
      ctx.beginPath();
      ctx.moveTo(lx - cascHalf, srcY);
      ctx.quadraticCurveTo(
        lx - cascHalf - 1 * s + wob1,
        clBase * 0.5 + srcY * 0.5,
        lx - cascHalf * 0.8 - 0.5 * s,
        ly + 1 * s
      );
      ctx.lineTo(lx + cascHalf * 0.8 + 0.5 * s, ly + 1 * s);
      ctx.quadraticCurveTo(
        lx + cascHalf + 1 * s + wob2,
        clBase * 0.5 + srcY * 0.5,
        lx + cascHalf,
        srcY
      );
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = `rgba(255,235,59,${poolP * 0.45})`;
      const hlW = cascHalf * 0.35;
      ctx.beginPath();
      ctx.moveTo(lx - hlW, srcY + 2 * s);
      ctx.quadraticCurveTo(
        lx - hlW - 0.5 * s,
        (srcY + ly) * 0.5,
        lx - hlW * 0.6,
        ly
      );
      ctx.lineTo(lx + hlW * 0.6, ly);
      ctx.quadraticCurveTo(
        lx + hlW + 0.5 * s,
        (srcY + ly) * 0.5,
        lx + hlW,
        srcY + 2 * s
      );
      ctx.closePath();
      ctx.fill();

      setShadowBlur(ctx, 8 * s, magOrg);
      ctx.strokeStyle = `rgba(255,152,0,${0.25 + poolP * 0.15})`;
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(lx - cascHalf * 1.2, srcY + 1 * s);
      ctx.lineTo(lx - cascHalf * 0.6, srcY - 2 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(lx + cascHalf * 1.2, srcY + 1 * s);
      ctx.lineTo(lx + cascHalf * 0.6, srcY - 2 * s);
      ctx.stroke();
      clearShadow(ctx);

      if (!wide) {
        const s2x = lx + cI * 0.6;
        const s2y = clBase - cH * 0.45;
        const s2Grad = ctx.createLinearGradient(s2x, s2y, s2x, ly);
        s2Grad.addColorStop(0, magOrg);
        s2Grad.addColorStop(0.5, magRed);
        s2Grad.addColorStop(1, magDk);
        ctx.fillStyle = s2Grad;
        const s2w = 2 * s;
        ctx.beginPath();
        ctx.moveTo(s2x - s2w, s2y);
        ctx.quadraticCurveTo(
          s2x - s2w - 0.5 * s,
          (s2y + ly) * 0.5,
          s2x - s2w * 0.7,
          ly
        );
        ctx.lineTo(s2x + s2w * 0.7, ly);
        ctx.quadraticCurveTo(
          s2x + s2w + 0.5 * s,
          (s2y + ly) * 0.5,
          s2x + s2w,
          s2y
        );
        ctx.closePath();
        ctx.fill();
      }

      setShadowBlur(ctx, 6 * s, magOrg);
      ctx.fillStyle = `rgba(255,200,50,${poolP * 0.5})`;
      ctx.beginPath();
      ctx.ellipse(
        lx,
        ly + 2 * s,
        cascHalf * 1.2 + Math.sin(decorTime * 4) * 0.5 * s,
        cascHalf * 0.5,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      clearShadow(ctx);

      for (let e = 0; e < 8; e++) {
        const ePh = (decorTime * 1.5 + e * 0.65) % 2;
        const ex = lx + Math.sin(decorTime + e * 1.7 + variant) * 12 * s;
        const ey = ly - ePh * 38 * s;
        const ea = Math.max(0, (1 - ePh / 2) * 0.7);
        ctx.fillStyle = `rgba(255,200,50,${ea})`;
        ctx.beginPath();
        ctx.arc(ex, ey, (1.3 + Math.sin(ePh * 3)) * s, 0, Math.PI * 2);
        ctx.fill();
      }

      for (let m = 0; m < 5; m++) {
        const mPh = (decorTime * 0.45 + m * 0.55 + variant * 0.3) % 1.5;
        const mx = lx + (m - 2) * 7 * s + Math.sin(decorTime * 0.8 + m) * 4 * s;
        const my = ly + 2 * s - mPh * 20 * s;
        const ma = Math.max(0, (1 - mPh / 1.5) * 0.25);
        ctx.fillStyle = `rgba(200,180,160,${ma})`;
        ctx.beginPath();
        ctx.ellipse(
          mx,
          my,
          (3 + mPh * 2) * s,
          (2 + mPh) * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      break;
    }

    case "obsidian_pillar": {
      const px = screenPos.x;
      const py = screenPos.y;
      const obsBlack = "#0a0a14";
      const obsDark = "#151524";
      const obsMid = "#252540";
      const obsLight = "#404068";
      const obsHi = "#5a5a90";
      const runeCol = "#ff6a3a";
      const pulse = 0.45 + Math.sin(decorTime * 2.6 + variant * 0.9) * 0.22;
      const hM = variant === 0 ? 1 : 0.82;
      const wM = variant === 0 ? 1 : 1.18;

      drawDirectionalShadow(
        ctx,
        px,
        py + 3 * s,
        s,
        16 * s * wM,
        7 * s,
        50 * s * hM,
        0.35
      );

      drawIsometricPrism(
        ctx,
        px,
        py,
        16 * s * wM,
        16 * s * wM,
        5 * s,
        obsMid,
        obsBlack,
        obsDark
      );
      drawIsometricPrism(
        ctx,
        px,
        py - 5 * s,
        12 * s * wM,
        12 * s * wM,
        4 * s,
        obsLight,
        obsDark,
        obsMid
      );

      const shH = 60 * s * hM;
      const bW = 9 * s * wM;
      const tW2 = 4.5 * s * wM;
      const shBase = py - 9 * s;
      const shTop = shBase - shH;
      const bI = bW * ISO_COS;
      const bD = bW * ISO_SIN;
      const tI = tW2 * ISO_COS;
      const tD = tW2 * ISO_SIN;

      ctx.fillStyle = obsBlack;
      ctx.beginPath();
      ctx.moveTo(px - bI, shBase + bD);
      ctx.lineTo(px, shBase + bD * 2);
      ctx.lineTo(px, shTop + tD * 2);
      ctx.lineTo(px - tI, shTop + tD);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = obsDark;
      ctx.beginPath();
      ctx.moveTo(px + bI, shBase + bD);
      ctx.lineTo(px, shBase + bD * 2);
      ctx.lineTo(px, shTop + tD * 2);
      ctx.lineTo(px + tI, shTop + tD);
      ctx.closePath();
      ctx.fill();

      ctx.fillStyle = obsLight;
      ctx.beginPath();
      ctx.moveTo(px, shTop);
      ctx.lineTo(px + tI, shTop + tD);
      ctx.lineTo(px, shTop + tD * 2);
      ctx.lineTo(px - tI, shTop + tD);
      ctx.closePath();
      ctx.fill();

      ctx.strokeStyle = "rgba(0,0,0,0.15)";
      ctx.lineWidth = 0.5 * s;
      for (let i = 1; i <= 5; i++) {
        const t = i / 6;
        const gy = shBase + (shTop - shBase) * t;
        const gI = bI + (tI - bI) * t;
        const gD2 = bD + (tD - bD) * t;
        const gFy = gy + gD2 * 2;
        ctx.beginPath();
        ctx.moveTo(px - gI, gy + gD2);
        ctx.lineTo(px, gFy);
        ctx.lineTo(px + gI, gy + gD2);
        ctx.stroke();
      }

      ctx.fillStyle = "rgba(120,120,200,0.1)";
      ctx.beginPath();
      ctx.moveTo(px + bI * 0.15, shBase + bD * 1.7);
      ctx.lineTo(px + bI * 0.85, shBase + bD * 0.85);
      ctx.lineTo(px + tI * 0.85, shTop + tD * 0.85);
      ctx.lineTo(px + tI * 0.15, shTop + tD * 1.7);
      ctx.closePath();
      ctx.fill();

      ctx.strokeStyle = obsHi;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(px, shBase + bD * 2);
      ctx.lineTo(px, shTop + tD * 2);
      ctx.stroke();

      setShadowBlur(ctx, 4 * s, runeCol);
      ctx.strokeStyle = `rgba(255,106,58,${0.15 + pulse * 0.12})`;
      ctx.lineWidth = 0.8 * s;
      const baseI2 = 16 * s * wM * ISO_COS;
      const baseD2 = 16 * s * wM * ISO_SIN;
      ctx.beginPath();
      ctx.moveTo(px - baseI2 * 0.4, py + baseD2 * 0.7);
      ctx.lineTo(px, py + baseD2 * 1.1);
      ctx.lineTo(px + baseI2 * 0.4, py + baseD2 * 0.7);
      ctx.stroke();
      clearShadow(ctx);

      const capY = shTop - 12 * s * hM;
      const cW = 6 * s * wM;
      const cD2 = cW * ISO_SIN;

      setShadowBlur(ctx, 8 * s, runeCol);
      ctx.strokeStyle = `rgba(255,120,70,${0.25 + pulse * 0.4})`;
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(px, shTop + tD);
      ctx.lineTo(px, capY + cD2 * 2 + 2 * s);
      ctx.stroke();
      clearShadow(ctx);

      drawIsometricPyramid(ctx, px, capY, cW, 9 * s, obsHi, obsBlack, obsMid);

      ctx.fillStyle = `rgba(255,130,80,${0.25 + pulse * 0.3})`;
      const capCenterY = capY + cD2;
      ctx.beginPath();
      ctx.moveTo(px, capCenterY - 6 * s);
      ctx.lineTo(px - cW * 0.3, capCenterY);
      ctx.lineTo(px, capCenterY + cD2 * 0.6);
      ctx.lineTo(px + cW * 0.3, capCenterY);
      ctx.closePath();
      ctx.fill();

      setShadowBlur(ctx, 14 * s, `rgba(255,100,50,${pulse})`);
      ctx.fillStyle = `rgba(255,140,80,${0.1 + pulse * 0.12})`;
      ctx.beginPath();
      ctx.arc(px, capY, cW * 1.8, 0, Math.PI * 2);
      ctx.fill();
      clearShadow(ctx);

      [-1, 1].forEach((side) => {
        const sx = px + side * 14 * s * wM;
        const sy = py + 3 * s;
        const shH2 = (variant === 0 ? 20 : 15) * s;
        const sdW = 3.5 * s;
        const sdI = sdW * ISO_COS;
        const sdD = sdW * ISO_SIN;
        ctx.fillStyle = obsBlack;
        ctx.beginPath();
        ctx.moveTo(sx - sdI, sy + sdD);
        ctx.lineTo(sx, sy + sdD * 2);
        ctx.lineTo(sx, sy - shH2);
        ctx.lineTo(sx - sdI, sy - shH2 + sdD);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = obsDark;
        ctx.beginPath();
        ctx.moveTo(sx + sdI, sy + sdD);
        ctx.lineTo(sx, sy + sdD * 2);
        ctx.lineTo(sx, sy - shH2);
        ctx.lineTo(sx + sdI, sy - shH2 + sdD);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = obsLight;
        ctx.beginPath();
        ctx.moveTo(sx, sy - shH2);
        ctx.lineTo(sx - sdI, sy - shH2 + sdD);
        ctx.lineTo(sx + sdI, sy - shH2 + sdD);
        ctx.closePath();
        ctx.fill();
      });

      setShadowBlur(ctx, 10 * s, runeCol);
      ctx.strokeStyle = `rgba(255,120,70,${0.3 + pulse * 0.45})`;
      ctx.lineWidth = 1.3 * s;
      const nR = variant === 0 ? 4 : 3;
      for (let i = 0; i < nR; i++) {
        const rt = 0.2 + (i / nR) * 0.6;
        const ry = shBase + (shTop - shBase) * rt;
        const gI = bI + (tI - bI) * rt;
        const gDy = bD + (tD - bD) * rt;
        const rw = gI * 0.35;
        ctx.beginPath();
        ctx.moveTo(px - rw, ry + gDy);
        ctx.lineTo(px, ry + gDy - 3.5 * s);
        ctx.lineTo(px + rw, ry + gDy);
        ctx.lineTo(px, ry + gDy + 3.5 * s);
        ctx.closePath();
        ctx.stroke();
      }
      clearShadow(ctx);

      if (variant === 1) {
        ctx.strokeStyle = `rgba(255,80,40,${0.2 + pulse * 0.15})`;
        ctx.lineWidth = 0.7 * s;
        ctx.beginPath();
        ctx.moveTo(px - bI * 0.6, shBase + bD * 0.7);
        ctx.quadraticCurveTo(
          px - (bI + tI) * 0.2,
          shBase + (shTop - shBase) * 0.4,
          px - tI * 0.4,
          shTop + tD * 1.2
        );
        ctx.stroke();
      }

      for (let i = 0; i < 5; i++) {
        const t = (decorTime * 0.8 + i * 0.5 + variant * 0.3) % 1.5;
        const ex = px + Math.sin(decorTime * 1.5 + i * 2.2) * 10 * s;
        const ey = shTop - 5 * s - t * 20 * s;
        const ea = Math.max(0, (1 - t / 1.5) * 0.65);
        ctx.fillStyle = `rgba(255,140,60,${ea})`;
        ctx.beginPath();
        ctx.arc(ex, ey, (1.5 - t * 0.8) * s, 0, Math.PI * 2);
        ctx.fill();
      }

      break;
    }

    case "fire_crystal": {
      const fcOrange = "#ff9800";
      const fcRed = "#f44336";
      const fcYellow = "#ffeb3b";
      const fcDark = "#bf360c";

      // Heat glow
      const fcGlow = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y - 10 * s,
        0,
        screenPos.x,
        screenPos.y - 10 * s,
        30 * s
      );
      const fcPulse = 0.25 + Math.sin(decorTime * 3) * 0.1;
      fcGlow.addColorStop(0, `rgba(255,150,0,${fcPulse})`);
      fcGlow.addColorStop(1, "transparent");
      ctx.fillStyle = fcGlow;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y - 10 * s,
        30 * s,
        18 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Crystal spires
      const fcSpires = [
        { dx: 0, h: 40, w: 7 },
        { dx: -9, h: 25, w: 5 },
        { dx: 8, h: 28, w: 5 },
        { dx: -5, h: 18, w: 3.5 },
        { dx: 11, h: 16, w: 3 },
      ];
      fcSpires.forEach((sp, idx) => {
        const sx = screenPos.x + sp.dx * s;
        const sy = screenPos.y;
        const sh = sp.h * s;
        const sw = sp.w * s;
        // Left face
        const fcLGrad = ctx.createLinearGradient(sx - sw, sy, sx, sy - sh);
        fcLGrad.addColorStop(0, fcDark);
        fcLGrad.addColorStop(0.5, fcRed);
        fcLGrad.addColorStop(1, fcOrange);
        ctx.fillStyle = fcLGrad;
        ctx.beginPath();
        ctx.moveTo(sx - sw, sy);
        ctx.lineTo(sx, sy - sh);
        ctx.lineTo(sx, sy + 2 * s);
        ctx.closePath();
        ctx.fill();

        // Right face
        const fcRGrad = ctx.createLinearGradient(sx, sy - sh, sx + sw, sy);
        fcRGrad.addColorStop(0, fcYellow);
        fcRGrad.addColorStop(0.5, fcOrange);
        fcRGrad.addColorStop(1, fcRed);
        ctx.fillStyle = fcRGrad;
        ctx.beginPath();
        ctx.moveTo(sx + sw, sy);
        ctx.lineTo(sx, sy - sh);
        ctx.lineTo(sx, sy + 2 * s);
        ctx.closePath();
        ctx.fill();

        // Inner glow
        ctx.fillStyle = `rgba(255,235,59,${0.3 + Math.sin(decorTime * 4 + idx) * 0.15})`;
        ctx.beginPath();
        ctx.moveTo(sx, sy - sh);
        ctx.lineTo(sx - sw * 0.3, sy - sh * 0.5);
        ctx.lineTo(sx + sw * 0.2, sy - sh * 0.55);
        ctx.closePath();
        ctx.fill();
      });

      // Floating embers
      for (let e = 0; e < 5; e++) {
        const ep = (decorTime * 1.2 + e * 0.6) % 2;
        const ea = Math.sin((ep / 2) * Math.PI) * 0.7;
        const ex = screenPos.x + Math.sin(decorTime + e * 1.3) * 10 * s;
        const ey = screenPos.y - 5 * s - ep * 20 * s;
        ctx.fillStyle = `rgba(255,200,50,${ea})`;
        ctx.beginPath();
        ctx.arc(ex, ey, 1.2 * s, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }

    case "skull_throne": {
      renderSkullThrone({
        ctx,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        seedX: decorX,
        seedY: decorY,
        shadowOnly,
        skipShadow,
        time: decorTime,
        variant,
        zoom,
      });
      break;
    }

    case "ember_rock": {
      const erDark = "#2a1810";
      const erMid = "#3a2818";

      // Shadow
      ctx.fillStyle = "rgba(0,0,0,0.25)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 2 * s,
        screenPos.y + 5 * s,
        18 * s,
        8 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Heat glow from cracks
      const erGlow = 0.2 + Math.sin(decorTime * 2) * 0.1;
      ctx.fillStyle = `rgba(255,100,0,${erGlow})`;
      ctx.beginPath();
      ctx.ellipse(screenPos.x, screenPos.y, 16 * s, 8 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Rock body - angular
      ctx.fillStyle = erDark;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 14 * s, screenPos.y + 2 * s);
      ctx.lineTo(screenPos.x - 10 * s, screenPos.y - 14 * s);
      ctx.lineTo(screenPos.x + 2 * s, screenPos.y - 18 * s);
      ctx.lineTo(screenPos.x + 14 * s, screenPos.y - 10 * s);
      ctx.lineTo(screenPos.x + 12 * s, screenPos.y + 2 * s);
      ctx.closePath();
      ctx.fill();

      // Top face
      ctx.fillStyle = erMid;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 10 * s, screenPos.y - 14 * s);
      ctx.lineTo(screenPos.x + 2 * s, screenPos.y - 18 * s);
      ctx.lineTo(screenPos.x + 14 * s, screenPos.y - 10 * s);
      ctx.lineTo(screenPos.x + 4 * s, screenPos.y - 8 * s);
      ctx.closePath();
      ctx.fill();

      // Glowing cracks
      const crackGlow = 0.6 + Math.sin(decorTime * 3) * 0.2;
      ctx.strokeStyle = `rgba(255,150,30,${crackGlow})`;
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 8 * s, screenPos.y - 2 * s);
      ctx.lineTo(screenPos.x - 2 * s, screenPos.y - 10 * s);
      ctx.lineTo(screenPos.x + 6 * s, screenPos.y - 6 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(screenPos.x + 2 * s, screenPos.y - 14 * s);
      ctx.lineTo(screenPos.x + 8 * s, screenPos.y - 4 * s);
      ctx.stroke();

      // Bright crack centers
      ctx.strokeStyle = `rgba(255,235,59,${crackGlow * 0.7})`;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 6 * s, screenPos.y - 4 * s);
      ctx.lineTo(screenPos.x - 1 * s, screenPos.y - 9 * s);
      ctx.stroke();

      // Small ember particles
      for (let e = 0; e < 3; e++) {
        const ep = (decorTime * 1.5 + e * 0.9) % 2;
        if (ep < 1) {
          const ea = (1 - ep) * 0.6;
          const ex =
            screenPos.x + (e * 6 - 6) * s + Math.sin(decorTime + e) * 2 * s;
          const ey = screenPos.y - 10 * s - ep * 12 * s;
          ctx.fillStyle = `rgba(255,180,50,${ea})`;
          ctx.beginPath();
          ctx.arc(ex, ey, 1 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      break;
    }

    case "volcano_rim": {
      const rimDark = "#1a1210";
      const rimMid = "#3a2818";
      const rimLight = "#5a4028";
      const rimSeed = decorX * 7 + decorY * 13;

      // Outer shadow
      ctx.fillStyle = "rgba(0,0,0,0.25)";
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y + 5 * s,
        28 * s,
        14 * s,
        rimSeed,
        0.12
      );
      ctx.fill();

      // Crater rim - back half (clipped to top semicircle)
      ctx.save();
      ctx.beginPath();
      ctx.rect(screenPos.x - 30 * s, screenPos.y - 30 * s, 60 * s, 24 * s);
      ctx.clip();
      ctx.fillStyle = rimDark;
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y - 6 * s,
        24 * s,
        12 * s,
        rimSeed + 1,
        0.1
      );
      ctx.fill();
      ctx.restore();

      // Rim sides (raised ring)
      ctx.fillStyle = rimMid;
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y,
        24 * s,
        12 * s,
        rimSeed + 2,
        0.1
      );
      ctx.fill();

      // Inner crater hole
      const innerGrad = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y,
        0,
        screenPos.x,
        screenPos.y,
        16 * s
      );
      const lavaPulse = 0.6 + Math.sin(decorTime * 2) * 0.2;
      innerGrad.addColorStop(0, `rgba(255,235,59,${lavaPulse})`);
      innerGrad.addColorStop(0.3, `rgba(255,152,0,${lavaPulse * 0.8})`);
      innerGrad.addColorStop(0.6, `rgba(244,67,54,${lavaPulse * 0.6})`);
      innerGrad.addColorStop(1, "#1a0a00");
      ctx.fillStyle = innerGrad;
      drawOrganicBlobAt(
        ctx,
        screenPos.x,
        screenPos.y,
        16 * s,
        8 * s,
        rimSeed + 3,
        0.08
      );
      ctx.fill();

      // Rim texture - jagged rocks
      ctx.fillStyle = rimLight;
      const rimRocks = [
        { a: 0, h: 8, r: 22 },
        { a: 0.7, h: 6, r: 23 },
        { a: 1.4, h: 9, r: 22 },
        { a: 2.1, h: 5, r: 24 },
        { a: 2.8, h: 7, r: 22 },
        { a: 3.5, h: 6, r: 23 },
        { a: 4.2, h: 8, r: 22 },
        { a: 4.9, h: 5, r: 24 },
        { a: 5.6, h: 7, r: 22 },
      ];
      rimRocks.forEach((rr) => {
        const rx = screenPos.x + Math.cos(rr.a) * rr.r * s;
        const ry = screenPos.y + Math.sin(rr.a) * rr.r * 0.5 * s;
        ctx.fillStyle = rr.h > 7 ? rimLight : rimMid;
        ctx.beginPath();
        ctx.moveTo(rx - 3 * s, ry);
        ctx.lineTo(rx, ry - rr.h * s);
        ctx.lineTo(rx + 3 * s, ry);
        ctx.closePath();
        ctx.fill();
      });

      // Smoke wisps
      for (let sm = 0; sm < 3; sm++) {
        const smokePhase = (decorTime * 0.5 + sm * 1.2) % 3;
        const smokeAlpha = Math.sin((smokePhase / 3) * Math.PI) * 0.15;
        const smx = screenPos.x + Math.sin(decorTime * 0.8 + sm) * 6 * s;
        const smy = screenPos.y - 8 * s - smokePhase * 10 * s;
        ctx.fillStyle = `rgba(80,60,40,${smokeAlpha})`;
        ctx.beginPath();
        ctx.ellipse(
          smx,
          smy,
          (4 + smokePhase * 2) * s,
          (2 + smokePhase) * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }

    // === SWAMP DECORATIONS ===
    case "swamp_tree": {
      drawSwampTree(
        ctx,
        screenPos.x,
        screenPos.y,
        s,
        variant,
        decorX,
        decorY,
        decorTime
      );
      break;
    }
    case "mushroom": {
      // Enhanced 3D isometric fantasy mushroom with glowing effects
      const mushroomPalettes = [
        {
          cap: "#b71c1c",
          capDark: "#7f0000",
          capLight: "#e53935",
          glow: null,
          spots: "#ffebee",
          stem: "#efebe9",
          stemDark: "#d7ccc8",
        },
        {
          cap: "#6a1b9a",
          capDark: "#4a148c",
          capLight: "#9c27b0",
          glow: "rgba(156,39,176,0.4)",
          spots: "#f3e5f5",
          stem: "#ede7f6",
          stemDark: "#d1c4e9",
        },
        {
          cap: "#1b5e20",
          capDark: "#0d3d11",
          capLight: "#2e7d32",
          glow: "rgba(76,175,80,0.3)",
          spots: "#e8f5e9",
          stem: "#e8f5e9",
          stemDark: "#c8e6c9",
        },
        {
          cap: "#bf360c",
          capDark: "#8d2804",
          capLight: "#e64a19",
          glow: null,
          spots: "#fff3e0",
          stem: "#efebe9",
          stemDark: "#d7ccc8",
        },
      ];
      const mp = mushroomPalettes[variant % 4];

      // Bioluminescent glow for magical mushrooms
      if (mp.glow) {
        const glowPulse = 0.6 + Math.sin(decorTime * 2) * 0.3;
        const mushroomGlow = ctx.createRadialGradient(
          screenPos.x,
          screenPos.y - 8 * s,
          0,
          screenPos.x,
          screenPos.y - 8 * s,
          22 * s
        );
        mushroomGlow.addColorStop(
          0,
          mp.glow.replace("0.4", String(glowPulse * 0.5))
        );
        mushroomGlow.addColorStop(
          0.5,
          mp.glow.replace("0.4", String(glowPulse * 0.2))
        );
        mushroomGlow.addColorStop(1, "rgba(0,0,0,0)");
        ctx.fillStyle = mushroomGlow;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y - 8 * s,
          22 * s,
          14 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Ground shadow
      ctx.fillStyle = "rgba(0,0,0,0.2)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 2 * s,
        screenPos.y + 4 * s,
        12 * s,
        6 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Stem with 3D cylinder shading
      const stemGrad = ctx.createLinearGradient(
        screenPos.x - 5 * s,
        screenPos.y,
        screenPos.x + 5 * s,
        screenPos.y
      );
      stemGrad.addColorStop(0, mp.stemDark);
      stemGrad.addColorStop(0.3, mp.stem);
      stemGrad.addColorStop(0.7, mp.stem);
      stemGrad.addColorStop(1, mp.stemDark);
      ctx.fillStyle = stemGrad;

      // Curved stem shape
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 4 * s, screenPos.y + 2 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 5 * s,
        screenPos.y - 4 * s,
        screenPos.x - 3.5 * s,
        screenPos.y - 10 * s
      );
      ctx.lineTo(screenPos.x + 3.5 * s, screenPos.y - 10 * s);
      ctx.quadraticCurveTo(
        screenPos.x + 5 * s,
        screenPos.y - 4 * s,
        screenPos.x + 4 * s,
        screenPos.y + 2 * s
      );
      ctx.closePath();
      ctx.fill();

      // Stem ring detail
      ctx.strokeStyle = mp.stemDark;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y - 5 * s,
        4 * s,
        1.5 * s,
        0,
        0,
        Math.PI
      );
      ctx.stroke();

      // Gills under cap
      ctx.fillStyle = mp.stemDark;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y - 10 * s,
        10 * s,
        3 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      // Gill lines
      ctx.strokeStyle = mp.stem;
      ctx.lineWidth = 0.5 * s;
      for (let g = 0; g < 8; g++) {
        const gillAngle = (g / 8) * Math.PI - Math.PI / 2;
        ctx.beginPath();
        ctx.moveTo(screenPos.x, screenPos.y - 10 * s);
        ctx.lineTo(
          screenPos.x + Math.cos(gillAngle) * 9 * s,
          screenPos.y - 10 * s + Math.sin(gillAngle) * 2.5 * s
        );
        ctx.stroke();
      }

      // Cap with 3D dome shading
      const capGrad = ctx.createRadialGradient(
        screenPos.x - 4 * s,
        screenPos.y - 18 * s,
        0,
        screenPos.x,
        screenPos.y - 12 * s,
        14 * s
      );
      capGrad.addColorStop(0, mp.capLight);
      capGrad.addColorStop(0.4, mp.cap);
      capGrad.addColorStop(0.8, mp.capDark);
      capGrad.addColorStop(1, mp.capDark);
      ctx.fillStyle = capGrad;

      // Cap shape - more mushroom-like dome
      ctx.beginPath();
      ctx.moveTo(screenPos.x - 13 * s, screenPos.y - 10 * s);
      ctx.quadraticCurveTo(
        screenPos.x - 14 * s,
        screenPos.y - 18 * s,
        screenPos.x - 8 * s,
        screenPos.y - 22 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x,
        screenPos.y - 26 * s,
        screenPos.x + 8 * s,
        screenPos.y - 22 * s
      );
      ctx.quadraticCurveTo(
        screenPos.x + 14 * s,
        screenPos.y - 18 * s,
        screenPos.x + 13 * s,
        screenPos.y - 10 * s
      );
      ctx.closePath();
      ctx.fill();

      // Spots on cap with 3D appearance
      const spots = [
        { r: 2.5, x: -6, y: -18 },
        { r: 2, x: 3, y: -20 },
        { r: 1.8, x: -2, y: -14 },
        { r: 1.5, x: 7, y: -16 },
        { r: 1.3, x: -8, y: -14 },
        { r: 1.2, x: 1, y: -22 },
      ];

      spots.forEach((spot) => {
        // Spot shadow
        ctx.fillStyle = "rgba(0,0,0,0.15)";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + spot.x * s + 0.5 * s,
          screenPos.y + spot.y * s + 0.5 * s,
          spot.r * s,
          spot.r * 0.7 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Spot
        const spotGrad = ctx.createRadialGradient(
          screenPos.x + spot.x * s - spot.r * 0.3 * s,
          screenPos.y + spot.y * s - spot.r * 0.3 * s,
          0,
          screenPos.x + spot.x * s,
          screenPos.y + spot.y * s,
          spot.r * s
        );
        spotGrad.addColorStop(0, "#ffffff");
        spotGrad.addColorStop(0.5, mp.spots);
        spotGrad.addColorStop(1, mp.stemDark);
        ctx.fillStyle = spotGrad;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + spot.x * s,
          screenPos.y + spot.y * s,
          spot.r * s,
          spot.r * 0.7 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // Highlight on cap
      ctx.fillStyle = "rgba(255,255,255,0.2)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x - 4 * s,
        screenPos.y - 20 * s,
        5 * s,
        3 * s,
        -0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Spore particles for magical mushrooms
      if (mp.glow) {
        const sporeAlpha = 0.3 + Math.sin(decorTime * 1.5) * 0.2;
        ctx.fillStyle = mp.glow.replace("0.4", String(sporeAlpha));
        for (let sp = 0; sp < 5; sp++) {
          const sporeY =
            screenPos.y - 8 * s - ((decorTime * 15 + sp * 8) % 25) * s;
          const sporeX = screenPos.x + Math.sin(decorTime + sp * 1.3) * 8 * s;
          ctx.beginPath();
          ctx.arc(sporeX, sporeY, 1 * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      break;
    }
    case "lily_pad": {
      // Enhanced 3D isometric lily pad with water reflections
      const lilyBaseX = screenPos.x;
      const lilyBaseY = screenPos.y;
      const lilyBob = Math.sin(decorTime * 1.5 + variant * 2) * 1.5 * s;

      // Water ripple beneath
      ctx.strokeStyle = "rgba(100,150,100,0.3)";
      ctx.lineWidth = 1 * s;
      const rippleSize = 16 + Math.sin(decorTime * 2 + variant) * 2;
      ctx.beginPath();
      ctx.ellipse(
        lilyBaseX,
        lilyBaseY + 2 * s,
        rippleSize * s,
        rippleSize * 0.4 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.stroke();

      // Main pad shadow on water
      ctx.fillStyle = "rgba(20,60,20,0.3)";
      ctx.beginPath();
      ctx.ellipse(
        lilyBaseX + 1 * s,
        lilyBaseY + 3 * s + lilyBob,
        14 * s,
        7 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Main lily pad - elliptical for isometric view with notch
      ctx.fillStyle = "#2a6a2a";
      ctx.beginPath();
      ctx.ellipse(
        lilyBaseX,
        lilyBaseY + lilyBob,
        14 * s,
        7 * s,
        0,
        0.15,
        Math.PI * 2 - 0.15
      );
      ctx.lineTo(lilyBaseX, lilyBaseY + lilyBob);
      ctx.closePath();
      ctx.fill();

      // Pad surface gradient (lighter center)
      const padGrad = ctx.createRadialGradient(
        lilyBaseX - 2 * s,
        lilyBaseY - 2 * s + lilyBob,
        0,
        lilyBaseX,
        lilyBaseY + lilyBob,
        12 * s
      );
      padGrad.addColorStop(0, "#4a9a4a");
      padGrad.addColorStop(0.5, "#3a7a3a");
      padGrad.addColorStop(1, "#2a5a2a");
      ctx.fillStyle = padGrad;
      ctx.beginPath();
      ctx.ellipse(
        lilyBaseX,
        lilyBaseY + lilyBob,
        12 * s,
        6 * s,
        0,
        0.2,
        Math.PI * 2 - 0.2
      );
      ctx.lineTo(lilyBaseX, lilyBaseY + lilyBob);
      ctx.closePath();
      ctx.fill();

      // Vein lines radiating from center
      ctx.strokeStyle = "rgba(60,100,60,0.5)";
      ctx.lineWidth = 0.8 * s;
      for (let v = 0; v < 6; v++) {
        const vAngle = (v / 6) * Math.PI * 2 + 0.3;
        if (vAngle > 0.1 && vAngle < Math.PI * 2 - 0.1) {
          ctx.beginPath();
          ctx.moveTo(lilyBaseX, lilyBaseY + lilyBob);
          ctx.lineTo(
            lilyBaseX + Math.cos(vAngle) * 10 * s,
            lilyBaseY + lilyBob + Math.sin(vAngle) * 5 * s
          );
          ctx.stroke();
        }
      }

      // Water droplet highlight
      ctx.fillStyle = "rgba(200,255,200,0.4)";
      ctx.beginPath();
      ctx.ellipse(
        lilyBaseX - 4 * s,
        lilyBaseY - 2 * s + lilyBob,
        2 * s,
        1 * s,
        -0.3,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Flower for variant 0
      if (variant === 0) {
        const flowerY = lilyBaseY - 5 * s + lilyBob;
        // Outer petals
        ctx.fillStyle = "#ff7ab5";
        for (let p = 0; p < 6; p++) {
          const pa = (p / 6) * Math.PI * 2 + decorTime * 0.2;
          const petalX = lilyBaseX + Math.cos(pa) * 5 * s;
          const petalY = flowerY + Math.sin(pa) * 2.5 * s;
          ctx.beginPath();
          ctx.ellipse(petalX, petalY, 4 * s, 2.5 * s, pa, 0, Math.PI * 2);
          ctx.fill();
        }
        // Inner petals
        ctx.fillStyle = "#ffaad5";
        for (let p = 0; p < 5; p++) {
          const pa = (p / 5) * Math.PI * 2 + 0.3;
          const petalX = lilyBaseX + Math.cos(pa) * 3 * s;
          const petalY = flowerY + Math.sin(pa) * 1.5 * s;
          ctx.beginPath();
          ctx.ellipse(petalX, petalY, 2.5 * s, 1.5 * s, pa, 0, Math.PI * 2);
          ctx.fill();
        }
        // Center
        ctx.fillStyle = "#ffe033";
        ctx.beginPath();
        ctx.arc(lilyBaseX, flowerY, 2.5 * s, 0, Math.PI * 2);
        ctx.fill();
        // Pollen dots
        ctx.fillStyle = "#ffaa00";
        for (let d = 0; d < 4; d++) {
          const da = (d / 4) * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(
            lilyBaseX + Math.cos(da) * 1.2 * s,
            flowerY + Math.sin(da) * 0.8 * s,
            0.5 * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
      }
      break;
    }
    case "fog_wisp": {
      // Enhanced ethereal swamp fog wisp with layered effects
      const fogBaseX = screenPos.x;
      const fogBaseY = screenPos.y;

      // Multiple layers of drifting fog
      const driftX = Math.sin(decorTime * 0.4 + variant * 1.5) * 15 * s;
      const driftY = Math.cos(decorTime * 0.25 + variant) * 8 * s;
      const breathe = 0.8 + Math.sin(decorTime * 0.6 + variant * 2) * 0.2;

      // Deepest layer (largest, most transparent)
      const fogGrad1 = ctx.createRadialGradient(
        fogBaseX + driftX * 0.5,
        fogBaseY + driftY * 0.5,
        0,
        fogBaseX + driftX * 0.5,
        fogBaseY + driftY * 0.5,
        45 * s * breathe
      );
      fogGrad1.addColorStop(0, `rgba(80,130,80,${0.12 * breathe})`);
      fogGrad1.addColorStop(0.6, `rgba(100,150,100,${0.08 * breathe})`);
      fogGrad1.addColorStop(1, "transparent");
      ctx.fillStyle = fogGrad1;
      ctx.beginPath();
      ctx.ellipse(
        fogBaseX + driftX * 0.5,
        fogBaseY + driftY * 0.5,
        45 * s * breathe,
        22 * s * breathe,
        driftX * 0.01,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Middle layer
      const fogGrad2 = ctx.createRadialGradient(
        fogBaseX + driftX,
        fogBaseY + driftY,
        0,
        fogBaseX + driftX,
        fogBaseY + driftY,
        32 * s * breathe
      );
      fogGrad2.addColorStop(0, `rgba(120,180,120,${0.15 * breathe})`);
      fogGrad2.addColorStop(0.5, `rgba(100,160,100,${0.1 * breathe})`);
      fogGrad2.addColorStop(1, "transparent");
      ctx.fillStyle = fogGrad2;
      ctx.beginPath();
      ctx.ellipse(
        fogBaseX + driftX,
        fogBaseY + driftY,
        32 * s * breathe,
        16 * s * breathe,
        -driftX * 0.02,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Top layer (densest core)
      const fogGrad3 = ctx.createRadialGradient(
        fogBaseX + driftX * 1.3,
        fogBaseY + driftY * 0.8,
        0,
        fogBaseX + driftX * 1.3,
        fogBaseY + driftY * 0.8,
        20 * s * breathe
      );
      fogGrad3.addColorStop(0, `rgba(150,200,150,${0.18 * breathe})`);
      fogGrad3.addColorStop(0.7, `rgba(130,180,130,${0.1 * breathe})`);
      fogGrad3.addColorStop(1, "transparent");
      ctx.fillStyle = fogGrad3;
      ctx.beginPath();
      ctx.ellipse(
        fogBaseX + driftX * 1.3,
        fogBaseY + driftY * 0.8,
        20 * s * breathe,
        10 * s * breathe,
        driftX * 0.015,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Subtle ghostly wisps rising
      for (let w = 0; w < 3; w++) {
        const wispPhase = (decorTime * 0.8 + w * 1.2 + variant) % 3;
        const wispAlpha = Math.sin((wispPhase * Math.PI) / 3) * 0.12;
        const wispRise = wispPhase * 12 * s;
        const wispDrift = Math.sin(wispPhase + w) * 8 * s;

        ctx.fillStyle = `rgba(180,220,180,${wispAlpha})`;
        ctx.beginPath();
        ctx.ellipse(
          fogBaseX + driftX + wispDrift,
          fogBaseY + driftY - wispRise,
          (8 - wispPhase * 2) * s,
          (4 - wispPhase) * s,
          wispDrift * 0.05,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      break;
    }
    case "witch_cottage": {
      const wt = decorTime;
      const ox = screenPos.x;
      const oy = screenPos.y;

      const wcGlowGreen = "#50ff60";
      const wcGlowPurple = "#b060ff";
      const wcPulse = 0.6 + Math.sin(wt * 2) * 0.15;

      // ========== GROUND SHADOW ==========
      if (!landmarkSkipShadow) {
        drawDirectionalShadow(
          ctx,
          ox,
          oy + 2 * s,
          s,
          40 * s,
          20 * s,
          55 * s,
          0.5
        );
      }

      // ========== CORRUPTED GROUND ==========
      const wcGroundG = ctx.createRadialGradient(
        ox,
        oy + 6 * s,
        3 * s,
        ox,
        oy + 6 * s,
        58 * s
      );
      wcGroundG.addColorStop(0, "rgba(15, 8, 25, 0.8)");
      wcGroundG.addColorStop(0.2, "rgba(10, 20, 10, 0.5)");
      wcGroundG.addColorStop(0.5, "rgba(8, 12, 8, 0.25)");
      wcGroundG.addColorStop(1, "rgba(0,0,0,0)");
      ctx.fillStyle = wcGroundG;
      ctx.beginPath();
      ctx.ellipse(ox, oy + 6 * s, 58 * s, 30 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // ========== SLUDGE POOLS ==========
      const wcSludgePools = [
        { dx: -18, dy: 12, rx: 14, ry: 6 },
        { dx: 12, dy: 16, rx: 10, ry: 5 },
        { dx: -6, dy: 20, rx: 8, ry: 4 },
        { dx: 22, dy: 8, rx: 7, ry: 3.5 },
      ];
      wcSludgePools.forEach((sp, i) => {
        const spx = ox + sp.dx * s;
        const spy = oy + sp.dy * s;
        const sludgeG = ctx.createRadialGradient(
          spx,
          spy,
          1 * s,
          spx,
          spy,
          sp.rx * s
        );
        const pulse = 0.5 + Math.sin(wt * 1.5 + i * 1.3) * 0.15;
        sludgeG.addColorStop(0, `rgba(40, 120, 30, ${pulse * 0.6})`);
        sludgeG.addColorStop(0.4, `rgba(30, 80, 20, ${pulse * 0.4})`);
        sludgeG.addColorStop(0.7, `rgba(20, 50, 15, ${pulse * 0.2})`);
        sludgeG.addColorStop(1, "rgba(20, 50, 15, 0)");
        ctx.fillStyle = sludgeG;
        ctx.beginPath();
        ctx.ellipse(spx, spy, sp.rx * s, sp.ry * s, 0.1 * i, 0, Math.PI * 2);
        ctx.fill();
        // Sludge surface sheen
        ctx.fillStyle = `rgba(80, 200, 60, ${pulse * 0.15})`;
        ctx.beginPath();
        ctx.ellipse(
          spx - 2 * s,
          spy - 1 * s,
          sp.rx * 0.4 * s,
          sp.ry * 0.3 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // Sludge bubbles in pools
      for (let i = 0; i < 8; i++) {
        const bPhase = (wt * 1.8 + i * 2.7) % 4;
        const poolIdx = i % wcSludgePools.length;
        const pool = wcSludgePools[poolIdx];
        const bx = ox + pool.dx * s + Math.sin(i * 3.1) * pool.rx * 0.5 * s;
        const by = oy + pool.dy * s - bPhase * 1.5 * s;
        const bSz = (1.2 + Math.sin(i * 1.7) * 0.5) * s;
        const bA = Math.max(0, 0.5 - bPhase * 0.13);
        if (bA > 0) {
          ctx.fillStyle = `rgba(100, 220, 80, ${bA * 0.6})`;
          ctx.beginPath();
          ctx.arc(bx, by, bSz, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // ========== SCATTERED WOOD DEBRIS ==========
      const wcDebrisColor = (i: number) =>
        i % 3 === 0 ? "#2a1c10" : i % 3 === 1 ? "#3a2818" : "#1e1408";
      [
        { a: 0.4, l: 12, x: -32, y: 14 },
        { a: -0.6, l: 9, x: 28, y: 16 },
        { a: 1.1, l: 7, x: -20, y: 22 },
        { a: -0.2, l: 10, x: 14, y: 20 },
        { a: 0.7, l: 8, x: 34, y: 6 },
        { a: -0.9, l: 6, x: -36, y: 8 },
        { a: 0.15, l: 11, x: 6, y: 24 },
        { a: -0.45, l: 8, x: -8, y: -6 },
        { a: 0.2, l: 5, x: -42, y: 6 },
        { a: -1.1, l: 7, x: 40, y: 10 },
        { a: 0.85, l: 6, x: -16, y: 26 },
        { a: -0.35, l: 8, x: 18, y: 24 },
      ].forEach((d, i) => {
        ctx.strokeStyle = wcDebrisColor(i);
        ctx.lineWidth = (1.5 + (i % 3) * 0.5) * s;
        ctx.beginPath();
        const dx = ox + d.x * s;
        const dy = oy + d.y * s;
        ctx.moveTo(dx, dy);
        ctx.lineTo(
          dx + Math.cos(d.a) * d.l * s,
          dy + Math.sin(d.a) * d.l * 0.5 * s
        );
        ctx.stroke();
        if (i % 3 === 0) {
          ctx.lineWidth = 0.8 * s;
          ctx.beginPath();
          ctx.moveTo(
            dx + Math.cos(d.a) * d.l * s,
            dy + Math.sin(d.a) * d.l * 0.5 * s
          );
          ctx.lineTo(
            dx + Math.cos(d.a + 0.3) * (d.l + 3) * s,
            dy + Math.sin(d.a + 0.3) * (d.l + 3) * 0.5 * s
          );
          ctx.stroke();
        }
      });

      // Scattered stone rubble chunks
      ctx.fillStyle = "#3a3530";
      [
        { r: 2.5, x: -24, y: 16 },
        { r: 2, x: 26, y: 12 },
        { r: 1.8, x: -30, y: 4 },
        { r: 2.2, x: 38, y: 14 },
        { r: 1.5, x: -4, y: 22 },
        { r: 1.6, x: 30, y: 20 },
      ].forEach((rb) => {
        ctx.fillStyle = "#3a3530";
        ctx.beginPath();
        ctx.moveTo(ox + rb.x * s - rb.r * s, oy + rb.y * s);
        ctx.lineTo(ox + rb.x * s, oy + rb.y * s - rb.r * 0.8 * s);
        ctx.lineTo(ox + rb.x * s + rb.r * s, oy + rb.y * s - rb.r * 0.3 * s);
        ctx.lineTo(
          ox + rb.x * s + rb.r * 0.6 * s,
          oy + rb.y * s + rb.r * 0.5 * s
        );
        ctx.lineTo(
          ox + rb.x * s - rb.r * 0.5 * s,
          oy + rb.y * s + rb.r * 0.4 * s
        );
        ctx.closePath();
        ctx.fill();
      });

      // ========== BACK WALL — warped leaning planks ==========
      const wcBackBaseY = oy + 2 * s;
      const wcBackH = 30 * s;
      const wcBackPlanks = [
        { color: "#2a1c10", h: 1, lean: 0.04, w: 10, x: -14 },
        { color: "#3a2818", h: 0.85, lean: -0.06, w: 9, x: -4 },
        { color: "#221610", h: 0.7, lean: 0.08, w: 11, x: 5 },
        { color: "#2e1c0e", h: 0.55, lean: 0.1, w: 8, x: 13 },
      ];
      wcBackPlanks.forEach((p, pi) => {
        const px = ox + p.x * s;
        const pH = wcBackH * p.h;
        ctx.save();
        ctx.translate(px, wcBackBaseY);
        ctx.rotate(p.lean);
        const plG = ctx.createLinearGradient(
          (-p.w / 2) * s,
          0,
          (p.w / 2) * s,
          0
        );
        plG.addColorStop(0, p.color);
        plG.addColorStop(0.5, "#4a3820");
        plG.addColorStop(1, p.color);
        ctx.fillStyle = plG;
        ctx.beginPath();
        ctx.moveTo((-p.w / 2) * s, 0);
        ctx.lineTo((p.w / 2) * s + 1 * s, 2 * s);
        ctx.lineTo((p.w / 2) * s, -pH + 3 * s);
        ctx.lineTo((p.w / 4) * s, -pH);
        ctx.lineTo(0, -pH + 2 * s);
        ctx.lineTo((-p.w / 4) * s, -pH - 1 * s);
        ctx.lineTo((-p.w / 2) * s, -pH + 4 * s);
        ctx.closePath();
        ctx.fill();
        // Wood grain
        ctx.strokeStyle = "rgba(0,0,0,0.2)";
        ctx.lineWidth = 0.5 * s;
        for (let g = 1; g < 3; g++) {
          const gx = (-p.w / 2 + (g * p.w) / 3) * s;
          ctx.beginPath();
          ctx.moveTo(gx, 0);
          ctx.quadraticCurveTo(
            gx + Math.sin(g) * 2 * s,
            -pH * 0.5,
            gx - 1 * s,
            -pH + 4 * s
          );
          ctx.stroke();
        }
        // Nail heads
        ctx.fillStyle = "#3a3630";
        if (pi < 3) {
          ctx.beginPath();
          ctx.arc(0, -pH * 0.3, 0.8 * s, 0, Math.PI * 2);
          ctx.arc(0, -pH * 0.6, 0.8 * s, 0, Math.PI * 2);
          ctx.fill();
        }
        // Rot holes on some planks
        if (pi % 2 === 0) {
          ctx.fillStyle = "#0e0a06";
          ctx.beginPath();
          ctx.ellipse(
            p.w * 0.15 * s,
            -pH * 0.4,
            1.5 * s,
            2.5 * s,
            0.3,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
        ctx.restore();
      });

      // Cross-braces on back wall
      ctx.strokeStyle = "#2a1a0c";
      ctx.lineWidth = 2 * s;
      ctx.beginPath();
      ctx.moveTo(ox - 18 * s, oy - 8 * s);
      ctx.lineTo(ox + 2 * s, oy - 18 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(ox - 4 * s, oy - 6 * s);
      ctx.lineTo(ox + 16 * s, oy - 14 * s);
      ctx.stroke();

      // ========== LEFT WALL — tall warped planks ==========
      const wcLeftX = ox - 16 * s;
      const wcLeftPlanks = [
        { color: "#2e1e12", dy: -4, h: 34, lean: 0.05, w: 3 },
        { color: "#3a2818", dy: -2, h: 30, lean: 0.03, w: 3.5 },
        { color: "#221408", dy: 0, h: 26, lean: 0.07, w: 3 },
        { color: "#2a1c10", dy: 2, h: 22, lean: 0.04, w: 3.5 },
        { color: "#321e10", dy: 4, h: 18, lean: 0.06, w: 3 },
      ];
      wcLeftPlanks.forEach((p, i) => {
        const py = wcBackBaseY + p.dy * s;
        ctx.save();
        ctx.translate(wcLeftX - i * 0.5 * s, py);
        ctx.rotate(p.lean);
        ctx.fillStyle = p.color;
        ctx.beginPath();
        ctx.moveTo((-p.w / 2) * s, 0);
        ctx.lineTo((p.w / 2) * s, 0);
        ctx.lineTo((p.w / 2) * s + 1 * s, -p.h * s + 2 * s);
        ctx.lineTo(0, -p.h * s);
        ctx.lineTo((-p.w / 2) * s - 0.5 * s, -p.h * s + 3 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = "rgba(0,0,0,0.18)";
        ctx.lineWidth = 0.4 * s;
        ctx.beginPath();
        ctx.moveTo(0, 0);
        ctx.lineTo(Math.sin(i) * 1 * s, -p.h * s + 2 * s);
        ctx.stroke();
        // Nail
        if (i % 2 === 1) {
          ctx.fillStyle = "#3a3630";
          ctx.beginPath();
          ctx.arc(0, -p.h * 0.5 * s, 0.7 * s, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.restore();
      });

      // Broken window shutter on left wall
      ctx.save();
      ctx.translate(ox - 17 * s, oy - 14 * s);
      ctx.rotate(0.35);
      ctx.fillStyle = "#2a1c10";
      ctx.fillRect(0, 0, 6 * s, 8 * s);
      ctx.strokeStyle = "rgba(0,0,0,0.25)";
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.moveTo(3 * s, 0);
      ctx.lineTo(3 * s, 8 * s);
      ctx.moveTo(0, 4 * s);
      ctx.lineTo(6 * s, 4 * s);
      ctx.stroke();
      ctx.fillStyle = "#5a4a30";
      ctx.beginPath();
      ctx.arc(5 * s, 4 * s, 0.8 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();

      // ========== RIGHT WALL — collapsed shorter ==========
      const wcRightX = ox + 16 * s;
      const wcRightPlanks = [
        { color: "#2a1c10", dy: 0, h: 16, lean: -0.08, w: 3.5 },
        { color: "#3a2818", dy: 2, h: 12, lean: -0.12, w: 3 },
        { color: "#1e1408", dy: -2, h: 20, lean: -0.05, w: 3.5 },
        { color: "#2e1c0e", dy: -4, h: 14, lean: -0.09, w: 3 },
      ];
      wcRightPlanks.forEach((p, i) => {
        const py = wcBackBaseY + p.dy * s;
        ctx.save();
        ctx.translate(wcRightX + i * 0.5 * s, py);
        ctx.rotate(p.lean);
        ctx.fillStyle = p.color;
        ctx.beginPath();
        ctx.moveTo((-p.w / 2) * s, 0);
        ctx.lineTo((p.w / 2) * s, 0);
        ctx.lineTo((p.w / 2) * s, -p.h * s + 3 * s);
        ctx.lineTo(0, -p.h * s);
        ctx.lineTo((-p.w / 2) * s, -p.h * s + 1 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = "rgba(0,0,0,0.15)";
        ctx.lineWidth = 0.4 * s;
        ctx.beginPath();
        ctx.moveTo(0, 0);
        ctx.lineTo(0, -p.h * s + 2 * s);
        ctx.stroke();
        ctx.restore();
      });

      // Fallen wall section on the ground (right side)
      ctx.fillStyle = "#2a1c10";
      ctx.save();
      ctx.translate(ox + 24 * s, oy + 10 * s);
      ctx.rotate(0.08);
      ctx.beginPath();
      ctx.moveTo(0, 0);
      ctx.lineTo(14 * s, 2 * s);
      ctx.lineTo(14 * s, -3 * s);
      ctx.quadraticCurveTo(10 * s, -4 * s, 7 * s, -2 * s);
      ctx.lineTo(4 * s, -4 * s);
      ctx.lineTo(0, -2 * s);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = "rgba(0,0,0,0.2)";
      ctx.lineWidth = 0.4 * s;
      ctx.beginPath();
      ctx.moveTo(5 * s, -3 * s);
      ctx.lineTo(5 * s, 1 * s);
      ctx.moveTo(10 * s, -3 * s);
      ctx.lineTo(10 * s, 2 * s);
      ctx.stroke();
      ctx.restore();

      // Collapsed corner post (thick log, broken at top)
      ctx.fillStyle = "#1e1408";
      ctx.save();
      ctx.translate(ox + 15 * s, oy + 3 * s);
      ctx.rotate(-0.15);
      ctx.beginPath();
      ctx.moveTo(-2.5 * s, 0);
      ctx.lineTo(2.5 * s, 0);
      ctx.lineTo(3 * s, -22 * s);
      ctx.lineTo(1 * s, -24 * s);
      ctx.lineTo(-1 * s, -22 * s);
      ctx.lineTo(-2.5 * s, -23 * s);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = "rgba(80, 60, 30, 0.3)";
      ctx.lineWidth = 0.6 * s;
      ctx.beginPath();
      ctx.moveTo(0, 0);
      ctx.lineTo(0, -22 * s);
      ctx.stroke();
      ctx.restore();

      // ========== BROKEN BARREL ==========
      ctx.save();
      ctx.translate(ox + 28 * s, oy + 4 * s);
      ctx.rotate(0.6);
      ctx.fillStyle = "#2a1c10";
      ctx.beginPath();
      ctx.ellipse(0, 0, 5 * s, 3 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(-5 * s, 0);
      ctx.quadraticCurveTo(-5.5 * s, 6 * s, 0, 8 * s);
      ctx.quadraticCurveTo(5.5 * s, 6 * s, 5 * s, 0);
      ctx.fill();
      ctx.strokeStyle = "#4a3820";
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.ellipse(0, 2 * s, 5.2 * s, 3.2 * s, 0, Math.PI * 0.1, Math.PI * 0.9);
      ctx.stroke();
      ctx.beginPath();
      ctx.ellipse(0, 5 * s, 5 * s, 3 * s, 0, Math.PI * 0.1, Math.PI * 0.9);
      ctx.stroke();
      // Crack in barrel
      ctx.strokeStyle = "#0e0a06";
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(-2 * s, -1 * s);
      ctx.lineTo(-1 * s, 4 * s);
      ctx.lineTo(-3 * s, 7 * s);
      ctx.stroke();
      // Sludge leaking from crack
      ctx.fillStyle = `rgba(50, 140, 30, ${wcPulse * 0.5})`;
      ctx.beginPath();
      ctx.ellipse(-2 * s, 8 * s, 3 * s, 1.5 * s, 0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();

      // ========== SCATTERED SHINGLES ==========
      [
        { a: 0.8, x: -28, y: 8 },
        { a: -0.3, x: -22, y: 14 },
        { a: 1.2, x: 20, y: 14 },
        { a: -0.5, x: 32, y: 2 },
        { a: 0.4, x: -10, y: 18 },
        { a: -0.7, x: 8, y: 16 },
      ].forEach((sh) => {
        ctx.fillStyle = "#1e1610";
        ctx.save();
        ctx.translate(ox + sh.x * s, oy + sh.y * s);
        ctx.rotate(sh.a);
        ctx.fillRect(-2.5 * s, -1 * s, 5 * s, 2 * s);
        ctx.restore();
      });

      // ========== GREEN SLUDGE DRIPPING DOWN WALLS ==========
      const wcDrips = [
        { len: 18, w: 2, x: ox - 15 * s, y: oy - 20 * s },
        { len: 12, w: 1.5, x: ox - 12 * s, y: oy - 14 * s },
        { len: 20, w: 2.5, x: ox - 6 * s, y: oy - 22 * s },
        { len: 16, w: 2, x: ox + 2 * s, y: oy - 18 * s },
        { len: 10, w: 1.8, x: ox + 14 * s, y: oy - 10 * s },
        { len: 14, w: 2.2, x: ox + 17 * s, y: oy - 16 * s },
        { len: 8, w: 1.5, x: ox - 2 * s, y: oy - 10 * s },
      ];
      wcDrips.forEach((d, i) => {
        const dripPulse = 0.5 + Math.sin(wt * 1.2 + i * 1.8) * 0.2;
        const dripG = ctx.createLinearGradient(d.x, d.y, d.x, d.y + d.len * s);
        dripG.addColorStop(0, `rgba(50, 140, 30, ${dripPulse * 0.7})`);
        dripG.addColorStop(0.6, `rgba(40, 100, 25, ${dripPulse * 0.5})`);
        dripG.addColorStop(1, `rgba(30, 80, 20, ${dripPulse * 0.15})`);
        ctx.fillStyle = dripG;
        const wobble = Math.sin(wt * 0.8 + i * 2) * 1 * s;
        ctx.beginPath();
        ctx.moveTo(d.x - d.w * 0.5 * s, d.y);
        ctx.quadraticCurveTo(
          d.x - d.w * 0.5 * s + wobble,
          d.y + d.len * 0.5 * s,
          d.x - d.w * 0.3 * s,
          d.y + d.len * s
        );
        // Drip blob at bottom
        ctx.quadraticCurveTo(
          d.x,
          d.y + (d.len + 2) * s,
          d.x + d.w * 0.3 * s,
          d.y + d.len * s
        );
        ctx.quadraticCurveTo(
          d.x + d.w * 0.5 * s - wobble,
          d.y + d.len * 0.5 * s,
          d.x + d.w * 0.5 * s,
          d.y
        );
        ctx.closePath();
        ctx.fill();
      });

      // ========== COLLAPSED ROOF BEAMS ==========
      const wcBeamColor = "#2a1a0e";
      const wcBeamLight = "#4a3420";
      // Main ridge beam — broken, sagging across the top
      ctx.strokeStyle = wcBeamColor;
      ctx.lineWidth = 4 * s;
      ctx.beginPath();
      ctx.moveTo(ox - 20 * s, oy - 28 * s);
      ctx.quadraticCurveTo(ox - 5 * s, oy - 24 * s, ox + 8 * s, oy - 18 * s);
      ctx.stroke();
      // Ridge beam highlight
      ctx.strokeStyle = wcBeamLight;
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(ox - 20 * s, oy - 29 * s);
      ctx.quadraticCurveTo(ox - 5 * s, oy - 25 * s, ox + 8 * s, oy - 19 * s);
      ctx.stroke();
      // Broken end splinter
      ctx.strokeStyle = wcBeamColor;
      ctx.lineWidth = 2 * s;
      ctx.beginPath();
      ctx.moveTo(ox + 8 * s, oy - 18 * s);
      ctx.lineTo(ox + 12 * s, oy - 16 * s);
      ctx.stroke();
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.moveTo(ox + 8 * s, oy - 18 * s);
      ctx.lineTo(ox + 10 * s, oy - 20 * s);
      ctx.stroke();

      // Rafter beams (leaning from ridge down)
      const wcRafters = [
        { ex: -28, ey: -6, sx: -18, sy: -27, w: 3 },
        { ex: -24, ey: 4, sx: -12, sy: -26, w: 2.5 },
        { ex: 10, ey: 6, sx: -6, sy: -25, w: 2.5 },
        { ex: 22, ey: -2, sx: 0, sy: -23, w: 2.8 },
        { ex: 24, ey: 8, sx: 5, sy: -20, w: 2 },
      ];
      wcRafters.forEach((r, i) => {
        ctx.strokeStyle = i % 2 === 0 ? wcBeamColor : "#321e0e";
        ctx.lineWidth = r.w * s;
        ctx.beginPath();
        ctx.moveTo(ox + r.sx * s, oy + r.sy * s);
        const sag = Math.sin(((i + 1) / 6) * Math.PI) * 4 * s;
        ctx.quadraticCurveTo(
          ox + ((r.sx + r.ex) / 2) * s,
          oy + ((r.sy + r.ey) / 2) * s + sag,
          ox + r.ex * s,
          oy + r.ey * s
        );
        ctx.stroke();
        // Highlight edge
        ctx.strokeStyle = "rgba(90, 70, 40, 0.25)";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo(ox + r.sx * s, oy + (r.sy - 1) * s);
        ctx.quadraticCurveTo(
          ox + ((r.sx + r.ex) / 2) * s,
          oy + ((r.sy + r.ey) / 2) * s + sag - 1 * s,
          ox + r.ex * s,
          oy + (r.ey - 1) * s
        );
        ctx.stroke();
      });

      // Collapsed roof panels (thatch fragments on beams)
      [
        { a: 0.15, color: "#1e1610", h: 10, w: 16, x: -20, y: -24 },
        { a: -0.1, color: "#2a1e12", h: 8, w: 14, x: -8, y: -20 },
        { a: 0.2, color: "#1a1208", h: 7, w: 10, x: 4, y: -16 },
      ].forEach((panel) => {
        ctx.save();
        ctx.translate(ox + panel.x * s, oy + panel.y * s);
        ctx.rotate(panel.a);
        ctx.fillStyle = panel.color;
        ctx.beginPath();
        ctx.moveTo(0, 0);
        ctx.lineTo(panel.w * s, panel.h * 0.3 * s);
        ctx.quadraticCurveTo(
          panel.w * 0.8 * s,
          panel.h * 0.7 * s,
          panel.w * 0.6 * s,
          panel.h * s
        );
        ctx.lineTo(-2 * s, panel.h * 0.8 * s);
        ctx.closePath();
        ctx.fill();
        // Thatch texture lines
        ctx.strokeStyle = "rgba(60, 45, 20, 0.3)";
        ctx.lineWidth = 0.5 * s;
        for (let t = 0; t < 4; t++) {
          const ty = ((t + 1) / 5) * panel.h * s;
          ctx.beginPath();
          ctx.moveTo(0, ty);
          ctx.lineTo(panel.w * 0.8 * s, ty + 2 * s);
          ctx.stroke();
        }
        ctx.restore();
      });

      // Green sludge dripping off beams
      [
        { len: 8, x: -22, y: -18 },
        { len: 10, x: -10, y: -20 },
        { len: 6, x: 4, y: -14 },
        { len: 5, x: 16, y: -8 },
      ].forEach((bd, i) => {
        const bdp = 0.4 + Math.sin(wt * 1.5 + i * 2.2) * 0.2;
        ctx.fillStyle = `rgba(50, 150, 30, ${bdp * 0.6})`;
        ctx.beginPath();
        ctx.ellipse(
          ox + bd.x * s,
          oy + bd.y * s,
          2 * s,
          1 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Drip strand
        ctx.strokeStyle = `rgba(50, 150, 30, ${bdp * 0.5})`;
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(ox + bd.x * s, oy + bd.y * s);
        ctx.lineTo(
          ox + bd.x * s + Math.sin(wt + i) * 1 * s,
          oy + (bd.y + bd.len) * s
        );
        ctx.stroke();
        // Blob at tip
        ctx.fillStyle = `rgba(60, 180, 40, ${bdp * 0.7})`;
        ctx.beginPath();
        ctx.arc(
          ox + bd.x * s + Math.sin(wt + i) * 1 * s,
          oy + (bd.y + bd.len) * s,
          1.5 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // ========== DARK INTERIOR VOID ==========
      ctx.fillStyle = "#080604";
      ctx.beginPath();
      ctx.moveTo(ox - 14 * s, oy + 2 * s);
      ctx.quadraticCurveTo(ox - 6 * s, oy + 8 * s, ox + 2 * s, oy + 6 * s);
      ctx.quadraticCurveTo(ox + 12 * s, oy + 4 * s, ox + 14 * s, oy);
      ctx.quadraticCurveTo(ox + 10 * s, oy - 4 * s, ox, oy - 2 * s);
      ctx.quadraticCurveTo(ox - 10 * s, oy - 4 * s, ox - 14 * s, oy + 2 * s);
      ctx.closePath();
      ctx.fill();

      // Interior glow
      const wcIntG = ctx.createRadialGradient(
        ox,
        oy + 1 * s,
        2 * s,
        ox,
        oy + 1 * s,
        18 * s
      );
      wcIntG.addColorStop(0, `rgba(80, 255, 96, ${wcPulse * 0.12})`);
      wcIntG.addColorStop(0.5, `rgba(50, 150, 40, ${wcPulse * 0.05})`);
      wcIntG.addColorStop(1, "rgba(80, 255, 96, 0)");
      ctx.fillStyle = wcIntG;
      ctx.beginPath();
      ctx.ellipse(ox, oy + 1 * s, 18 * s, 10 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // ========== GIANT TIPPED-OVER CAULDRON ==========
      const wcCx = ox + 2 * s;
      const wcCy = oy + 4 * s;
      const wcCTilt = 0.55;
      ctx.save();
      ctx.translate(wcCx, wcCy);
      ctx.rotate(wcCTilt);
      // Cauldron body (large, cracked)
      ctx.fillStyle = "#101010";
      ctx.beginPath();
      ctx.ellipse(0, 0, 12 * s, 6 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(-12 * s, 0);
      ctx.quadraticCurveTo(-12 * s, 10 * s, 0, 12 * s);
      ctx.quadraticCurveTo(12 * s, 10 * s, 12 * s, 0);
      ctx.fill();
      // Rim
      ctx.strokeStyle = "#2a2a2a";
      ctx.lineWidth = 2.5 * s;
      ctx.beginPath();
      ctx.ellipse(0, 0, 12 * s, 6 * s, 0, 0, Math.PI * 2);
      ctx.stroke();
      // Iron rivets
      ctx.fillStyle = "#3a3a3a";
      [-8, -3, 3, 8].forEach((rx) => {
        ctx.beginPath();
        ctx.arc(rx * s, 5 * s, 1.2 * s, 0, Math.PI * 2);
        ctx.fill();
      });
      // Crack line across body
      ctx.strokeStyle = "#1a1a1a";
      ctx.lineWidth = 1 * s;
      ctx.beginPath();
      ctx.moveTo(-6 * s, -2 * s);
      ctx.lineTo(-3 * s, 4 * s);
      ctx.lineTo(0, 8 * s);
      ctx.lineTo(4 * s, 10 * s);
      ctx.stroke();
      // Second crack
      ctx.beginPath();
      ctx.moveTo(3 * s, -1 * s);
      ctx.lineTo(5 * s, 3 * s);
      ctx.lineTo(4 * s, 7 * s);
      ctx.stroke();
      // Green glow from inside the cauldron
      const wcCGlowA = 0.7 + Math.sin(wt * 3) * 0.15;
      ctx.fillStyle = `rgba(80, 255, 96, ${wcCGlowA * 0.7})`;
      setShadowBlur(ctx, 20 * s, wcGlowGreen);
      ctx.beginPath();
      ctx.ellipse(0, -1 * s, 9 * s, 4 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      clearShadow(ctx);
      // Handle stump (broken)
      ctx.strokeStyle = "#2a2a2a";
      ctx.lineWidth = 2 * s;
      ctx.beginPath();
      ctx.moveTo(-12 * s, -1 * s);
      ctx.lineTo(-15 * s, -4 * s);
      ctx.stroke();
      ctx.restore();

      // Sludge spilling out of tipped cauldron
      const wcSpillX = wcCx + Math.cos(wcCTilt) * 14 * s;
      const wcSpillY = wcCy + Math.sin(wcCTilt) * 14 * s;
      const wcSpillG = ctx.createRadialGradient(
        wcSpillX,
        wcSpillY,
        2 * s,
        wcSpillX,
        wcSpillY + 6 * s,
        22 * s
      );
      const wcSpillPulse = 0.6 + Math.sin(wt * 2.5) * 0.15;
      wcSpillG.addColorStop(0, `rgba(60, 200, 50, ${wcSpillPulse * 0.65})`);
      wcSpillG.addColorStop(0.3, `rgba(40, 140, 30, ${wcSpillPulse * 0.45})`);
      wcSpillG.addColorStop(0.6, `rgba(30, 100, 20, ${wcSpillPulse * 0.2})`);
      wcSpillG.addColorStop(1, "rgba(30, 80, 20, 0)");
      ctx.fillStyle = wcSpillG;
      ctx.beginPath();
      ctx.moveTo(wcSpillX - 4 * s, wcSpillY - 2 * s);
      ctx.quadraticCurveTo(
        wcSpillX + 8 * s,
        wcSpillY,
        wcSpillX + 16 * s,
        wcSpillY + 8 * s
      );
      ctx.quadraticCurveTo(
        wcSpillX + 10 * s,
        wcSpillY + 14 * s,
        wcSpillX - 2 * s,
        wcSpillY + 10 * s
      );
      ctx.quadraticCurveTo(
        wcSpillX - 8 * s,
        wcSpillY + 6 * s,
        wcSpillX - 4 * s,
        wcSpillY - 2 * s
      );
      ctx.closePath();
      ctx.fill();
      // Glow from spill
      setShadowBlur(ctx, 15 * s, wcGlowGreen);
      ctx.fillStyle = `rgba(80, 255, 96, ${wcSpillPulse * 0.12})`;
      ctx.beginPath();
      ctx.ellipse(
        wcSpillX + 6 * s,
        wcSpillY + 5 * s,
        14 * s,
        8 * s,
        0.2,
        0,
        Math.PI * 2
      );
      ctx.fill();
      clearShadow(ctx);

      // Cauldron spill bubbles
      for (let i = 0; i < 6; i++) {
        const bT = (wt * 2 + i * 1.4) % 3;
        const bx = wcSpillX + 3 * s + Math.sin(i * 2.5) * 8 * s;
        const by = wcSpillY + 3 * s + Math.cos(i * 1.7) * 4 * s - bT * 2 * s;
        const bA = Math.max(0, 0.6 - bT * 0.2);
        ctx.fillStyle = `rgba(120, 255, 100, ${bA * 0.5})`;
        setShadowBlur(ctx, 3 * s, `rgba(80, 255, 96, ${bA * 0.2})`);
        ctx.beginPath();
        ctx.arc(bx, by, (1.5 + Math.sin(i) * 0.5) * s, 0, Math.PI * 2);
        ctx.fill();
      }
      clearShadow(ctx);

      // ========== BROKEN DOORWAY (gap in front wall) ==========
      // Two leaning planks framing an opening
      ctx.fillStyle = "#2a1c10";
      ctx.save();
      ctx.translate(ox - 5 * s, oy + 4 * s);
      ctx.rotate(0.12);
      ctx.fillRect(-2 * s, 0, 3.5 * s, -20 * s);
      ctx.restore();
      ctx.fillStyle = "#321e0e";
      ctx.save();
      ctx.translate(ox + 5 * s, oy + 6 * s);
      ctx.rotate(-0.08);
      ctx.fillRect(-1.5 * s, 0, 3 * s, -16 * s);
      ctx.restore();
      // Glow from doorway opening
      const wcDoorGlow = ctx.createRadialGradient(
        ox,
        oy + 2 * s,
        1 * s,
        ox,
        oy + 2 * s,
        18 * s
      );
      wcDoorGlow.addColorStop(0, `rgba(80, 255, 96, ${wcPulse * 0.2})`);
      wcDoorGlow.addColorStop(0.4, `rgba(50, 180, 40, ${wcPulse * 0.08})`);
      wcDoorGlow.addColorStop(1, "rgba(80, 255, 96, 0)");
      ctx.fillStyle = wcDoorGlow;
      ctx.beginPath();
      ctx.ellipse(ox, oy + 4 * s, 18 * s, 10 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // ========== HANGING CHAINS / HOOKS ==========
      const wcHooks = [
        { len: 14, x: ox - 16 * s, y: oy - 18 * s },
        { len: 10, x: ox + 10 * s, y: oy - 14 * s },
      ];
      wcHooks.forEach((hk, i) => {
        const swing = Math.sin(wt * 0.7 + i * 2) * 1.5 * s;
        ctx.strokeStyle = "#4a4238";
        ctx.lineWidth = 1.2 * s;
        // Chain links
        for (let c = 0; c < hk.len; c += 3) {
          const cx = hk.x + swing * (c / hk.len);
          const cy = hk.y + c * s;
          ctx.beginPath();
          ctx.ellipse(cx, cy, 1.2 * s, 2 * s, 0, 0, Math.PI * 2);
          ctx.stroke();
        }
        // Hook at bottom
        ctx.lineWidth = 1.5 * s;
        const hookX = hk.x + swing;
        const hookY = hk.y + hk.len * s;
        ctx.beginPath();
        ctx.moveTo(hookX, hookY);
        ctx.quadraticCurveTo(
          hookX + 3 * s,
          hookY + 4 * s,
          hookX,
          hookY + 6 * s
        );
        ctx.stroke();
      });

      // ========== SKULL EMBEDDED IN WALL ==========
      const wcSkX = ox - 14 * s;
      const wcSkY = oy - 12 * s;
      ctx.fillStyle = "#c8b8a8";
      ctx.beginPath();
      ctx.ellipse(wcSkX, wcSkY, 3 * s, 3.5 * s, 0.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#0a0a0a";
      ctx.beginPath();
      ctx.ellipse(
        wcSkX - 1 * s,
        wcSkY - 0.3 * s,
        1 * s,
        1.3 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.ellipse(
        wcSkX + 1.3 * s,
        wcSkY - 0.3 * s,
        1 * s,
        1.3 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.fillStyle = `rgba(80, 255, 96, ${wcPulse * 0.6})`;
      setShadowBlur(ctx, 4 * s, wcGlowGreen);
      ctx.beginPath();
      ctx.arc(wcSkX - 1 * s, wcSkY - 0.3 * s, 0.6 * s, 0, Math.PI * 2);
      ctx.arc(wcSkX + 1.3 * s, wcSkY - 0.3 * s, 0.6 * s, 0, Math.PI * 2);
      ctx.fill();
      clearShadow(ctx);

      // ========== BLACK CAT ==========
      const wcCatX = ox - 22 * s;
      const wcCatY = oy + 8 * s;
      const wcCatBob = Math.sin(wt * 1.2) * 0.4 * s;
      ctx.save();
      ctx.translate(wcCatX, wcCatY + wcCatBob);
      ctx.fillStyle = "#0a0a0e";
      ctx.beginPath();
      ctx.ellipse(0, 0, 4 * s, 2.8 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.arc(4 * s, -2 * s, 2.2 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(2.5 * s, -3.8 * s);
      ctx.lineTo(2 * s, -6.5 * s);
      ctx.lineTo(4 * s, -4.2 * s);
      ctx.fill();
      ctx.beginPath();
      ctx.moveTo(5 * s, -3.8 * s);
      ctx.lineTo(5.5 * s, -6.5 * s);
      ctx.lineTo(6 * s, -3.8 * s);
      ctx.fill();
      ctx.strokeStyle = "#0a0a0e";
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.moveTo(-4 * s, 0);
      ctx.quadraticCurveTo(-7 * s, -4 * s, -5 * s, -6 * s);
      ctx.stroke();
      ctx.fillStyle = `rgba(80, 255, 96, ${wcPulse * 0.85})`;
      setShadowBlur(ctx, 3 * s, wcGlowGreen);
      ctx.beginPath();
      ctx.ellipse(3.2 * s, -2.5 * s, 0.6 * s, 0.9 * s, 0, 0, Math.PI * 2);
      ctx.ellipse(5.3 * s, -2.5 * s, 0.6 * s, 0.9 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      clearShadow(ctx);
      ctx.restore();

      // ========== COOKING SPIT WITH BROKEN CHAIN ==========
      const wcSpitX = ox - 28 * s;
      const wcSpitY = oy + 6 * s;
      // Left upright (standing)
      ctx.strokeStyle = "#3a3228";
      ctx.lineWidth = 2.5 * s;
      ctx.beginPath();
      ctx.moveTo(wcSpitX - 10 * s, wcSpitY + 4 * s);
      ctx.lineTo(wcSpitX - 10 * s, wcSpitY - 16 * s);
      ctx.stroke();
      // Forked top
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(wcSpitX - 10 * s, wcSpitY - 16 * s);
      ctx.lineTo(wcSpitX - 11.5 * s, wcSpitY - 20 * s);
      ctx.moveTo(wcSpitX - 10 * s, wcSpitY - 16 * s);
      ctx.lineTo(wcSpitX - 8.5 * s, wcSpitY - 20 * s);
      ctx.stroke();
      // Right upright (broken, leaning)
      ctx.lineWidth = 2.5 * s;
      ctx.save();
      ctx.translate(wcSpitX + 10 * s, wcSpitY + 4 * s);
      ctx.rotate(-0.25);
      ctx.beginPath();
      ctx.moveTo(0, 0);
      ctx.lineTo(0, -12 * s);
      ctx.stroke();
      // Splintered broken top
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(-1 * s, -12 * s);
      ctx.lineTo(-2 * s, -14 * s);
      ctx.moveTo(0, -12 * s);
      ctx.lineTo(1 * s, -15 * s);
      ctx.moveTo(1 * s, -12 * s);
      ctx.lineTo(2 * s, -13.5 * s);
      ctx.stroke();
      ctx.restore();
      // Crossbar (sagging, resting on left fork, broken end)
      ctx.strokeStyle = "#4a4030";
      ctx.lineWidth = 2 * s;
      ctx.beginPath();
      ctx.moveTo(wcSpitX - 10 * s, wcSpitY - 18 * s);
      ctx.quadraticCurveTo(
        wcSpitX,
        wcSpitY - 15 * s,
        wcSpitX + 6 * s,
        wcSpitY - 14 * s
      );
      ctx.stroke();
      // Broken end dangling
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(wcSpitX + 6 * s, wcSpitY - 14 * s);
      ctx.lineTo(wcSpitX + 8 * s, wcSpitY - 12 * s);
      ctx.stroke();
      // Chain from crossbar (intact left side)
      ctx.strokeStyle = "#5a5248";
      ctx.lineWidth = 1 * s;
      const wcChainSwing = Math.sin(wt * 0.6) * 1 * s;
      for (let c = 0; c < 6; c++) {
        const cx = wcSpitX - 4 * s + wcChainSwing * (c / 6);
        const cy = wcSpitY - 17 * s + c * 2.5 * s;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy,
          1 * s,
          1.8 * s,
          c % 2 === 0 ? 0 : Math.PI / 4,
          0,
          Math.PI * 2
        );
        ctx.stroke();
      }
      // Dangling hook at end of chain
      const wcHookX = wcSpitX - 4 * s + wcChainSwing;
      const wcHookY = wcSpitY - 2 * s;
      ctx.lineWidth = 1.3 * s;
      ctx.beginPath();
      ctx.moveTo(wcHookX, wcHookY);
      ctx.quadraticCurveTo(
        wcHookX + 2 * s,
        wcHookY + 3 * s,
        wcHookX - 0.5 * s,
        wcHookY + 5 * s
      );
      ctx.stroke();
      // Broken chain on right side (few links then gap)
      ctx.strokeStyle = "#5a5248";
      ctx.lineWidth = 1 * s;
      for (let c = 0; c < 3; c++) {
        const cx = wcSpitX + 3 * s;
        const cy = wcSpitY - 16 * s + c * 2.5 * s;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy,
          1 * s,
          1.8 * s,
          c % 2 === 0 ? 0 : Math.PI / 4,
          0,
          Math.PI * 2
        );
        ctx.stroke();
      }
      // Fallen chain links on ground
      ctx.strokeStyle = "#4a4238";
      ctx.lineWidth = 0.8 * s;
      [
        { x: 1, y: 2 },
        { x: 4, y: 3 },
        { x: 6, y: 1 },
      ].forEach((fl) => {
        ctx.beginPath();
        ctx.ellipse(
          wcSpitX + fl.x * s,
          wcSpitY + (4 + fl.y) * s,
          1.2 * s,
          0.8 * s,
          fl.x * 0.3,
          0,
          Math.PI * 2
        );
        ctx.stroke();
      });
      // Soot marks under spit
      ctx.fillStyle = "rgba(10, 8, 5, 0.35)";
      ctx.beginPath();
      ctx.ellipse(wcSpitX, wcSpitY + 4 * s, 10 * s, 4 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      // Charred wood / ash pile
      ctx.fillStyle = "#1a1410";
      ctx.beginPath();
      ctx.ellipse(
        wcSpitX,
        wcSpitY + 3 * s,
        6 * s,
        2.5 * s,
        0.1,
        0,
        Math.PI * 2
      );
      ctx.fill();
      ctx.fillStyle = "#2a2420";
      ctx.beginPath();
      ctx.ellipse(
        wcSpitX - 1 * s,
        wcSpitY + 2.5 * s,
        4 * s,
        1.8 * s,
        -0.1,
        0,
        Math.PI * 2
      );
      ctx.fill();
      // Faint ember glow
      const wcEmberP = 0.3 + Math.sin(wt * 3) * 0.15;
      ctx.fillStyle = `rgba(200, 80, 20, ${wcEmberP * 0.15})`;
      setShadowBlur(ctx, 6 * s, `rgba(200, 80, 20, ${wcEmberP * 0.1})`);
      ctx.beginPath();
      ctx.ellipse(wcSpitX, wcSpitY + 2 * s, 4 * s, 2 * s, 0, 0, Math.PI * 2);
      ctx.fill();
      clearShadow(ctx);

      // ========== PARTIAL LOFT / SECOND FLOOR REMNANT ==========
      // A few boards hanging from the left wall forming a half-collapsed loft
      ctx.fillStyle = "#2a1c10";
      ctx.save();
      ctx.translate(ox - 14 * s, oy - 16 * s);
      ctx.rotate(0.06);
      ctx.fillRect(0, 0, 22 * s, 2.5 * s);
      ctx.restore();
      ctx.fillStyle = "#321e0e";
      ctx.save();
      ctx.translate(ox - 12 * s, oy - 15 * s);
      ctx.rotate(0.12);
      ctx.fillRect(0, 0, 16 * s, 2 * s);
      ctx.restore();
      // Support bracket
      ctx.strokeStyle = "#2a1c10";
      ctx.lineWidth = 2 * s;
      ctx.beginPath();
      ctx.moveTo(ox - 15 * s, oy - 16 * s);
      ctx.lineTo(ox - 15 * s, oy - 10 * s);
      ctx.lineTo(ox - 10 * s, oy - 16 * s);
      ctx.stroke();

      // ========== WOOD MOSS PATCHES ==========
      ctx.fillStyle = "rgba(30, 60, 25, 0.5)";
      [
        { rx: 4, ry: 2, x: -16, y: -8 },
        { rx: 3, ry: 1.5, x: -14, y: -20 },
        { rx: 3.5, ry: 2, x: 16, y: -6 },
        { rx: 5, ry: 2.5, x: -8, y: 0 },
        { rx: 3, ry: 1.8, x: 12, y: -12 },
      ].forEach((mp) => {
        ctx.beginPath();
        ctx.ellipse(
          ox + mp.x * s,
          oy + mp.y * s,
          mp.rx * s,
          mp.ry * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // ========== MUSHROOMS ==========
      const wcMush: {
        dx: number;
        dy: number;
        sz: number;
        purple?: boolean;
      }[] = [
        { dx: 30, dy: 12, sz: 1.3 },
        { dx: -34, dy: 16, purple: true, sz: 1.5 },
        { dx: -26, dy: 20, sz: 0.9 },
        { dx: 22, dy: 18, purple: true, sz: 1.1 },
        { dx: -40, dy: 10, sz: 1.4 },
        { dx: 36, dy: 6, purple: true, sz: 0.8 },
        { dx: -8, dy: 22, sz: 1 },
      ];
      wcMush.forEach((mp, i) => {
        const mx = ox + mp.dx * s;
        const my = oy + mp.dy * s;
        const mg = 0.4 + Math.sin(wt * 1.5 + i * 0.9) * 0.2;
        const msc = mp.sz;
        const isPurple = mp.purple ?? false;
        ctx.fillStyle = isPurple ? "#a098b0" : "#b0a898";
        ctx.beginPath();
        ctx.moveTo(mx - 1.3 * msc * s, my);
        ctx.quadraticCurveTo(
          mx - 0.7 * msc * s,
          my - 3 * msc * s,
          mx - 0.5 * msc * s,
          my - 5 * msc * s
        );
        ctx.lineTo(mx + 0.5 * msc * s, my - 5 * msc * s);
        ctx.quadraticCurveTo(
          mx + 0.7 * msc * s,
          my - 3 * msc * s,
          mx + 1.3 * msc * s,
          my
        );
        ctx.closePath();
        ctx.fill();
        let cr: number, cg: number, cb: number;
        if (isPurple) {
          cr = 80 + (i % 3) * 15;
          cg = 25;
          cb = 110 + (i % 2) * 25;
        } else {
          cr = 130 + (i % 2) * 30;
          cg = 25;
          cb = 25;
        }
        ctx.fillStyle = `rgba(${cr}, ${cg}, ${cb}, 0.9)`;
        ctx.beginPath();
        ctx.ellipse(
          mx,
          my - 5.5 * msc * s,
          3.5 * msc * s,
          2.2 * msc * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        const spC = isPurple ? "180, 160, 210" : "200, 180, 140";
        ctx.fillStyle = `rgba(${spC}, ${mg * 0.5})`;
        [
          { dx: -1.2, dy: -6, r: 0.6 },
          { dx: 0.8, dy: -6.3, r: 0.4 },
        ].forEach((sp) => {
          ctx.beginPath();
          ctx.arc(
            mx + sp.dx * msc * s,
            my + sp.dy * msc * s,
            sp.r * msc * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });
      });

      // ========== RUNE MARKS ==========
      const wcRuneP = 0.4 + Math.sin(wt * 1.5) * 0.25;
      [
        { rgb: "176, 96, 255", x: ox - 13 * s, y: oy - 6 * s },
        { rgb: "80, 255, 96", x: ox + 15 * s, y: oy - 4 * s },
        { rgb: "176, 96, 255", x: ox - 3 * s, y: oy - 24 * s },
      ].forEach((rp) => {
        ctx.strokeStyle = `rgba(${rp.rgb}, ${wcRuneP * 0.5})`;
        setShadowBlur(ctx, 6 * s, `rgba(${rp.rgb}, ${wcRuneP * 0.3})`);
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.arc(rp.x, rp.y, 2 * s, 0, Math.PI * 2);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(rp.x - 1.5 * s, rp.y);
        ctx.lineTo(rp.x + 1.5 * s, rp.y);
        ctx.moveTo(rp.x, rp.y - 1.5 * s);
        ctx.lineTo(rp.x, rp.y + 1.5 * s);
        ctx.stroke();
        clearShadow(ctx);
      });

      // ========== FLOATING PARTICLES ==========
      for (let i = 0; i < 10; i++) {
        const pP = wt * 0.6 + i * 2.5;
        const pR = (16 + i * 3) * s;
        const pA = pP + Math.sin(pP * 0.3) * 0.5;
        const px = ox + Math.cos(pA) * pR * 0.8;
        const py = oy + Math.sin(pA) * pR * 0.35 - Math.sin(pP * 1.2) * 8 * s;
        const pSz = (1 + Math.sin(pP * 2) * 0.4) * s;
        const pAlpha = 0.25 + Math.sin(pP * 3) * 0.12;
        const isGr = i % 3 !== 0;
        ctx.fillStyle = isGr
          ? `rgba(80, 255, 96, ${pAlpha})`
          : `rgba(176, 96, 255, ${pAlpha})`;
        setShadowBlur(ctx, 4 * s, isGr ? wcGlowGreen : wcGlowPurple);
        ctx.beginPath();
        ctx.arc(px, py, pSz, 0, Math.PI * 2);
        ctx.fill();
      }
      clearShadow(ctx);

      // ========== GROUND MIST ==========
      for (let i = 0; i < 6; i++) {
        const mP = wt * 0.3 + i * 0.9;
        const mX = ox + Math.sin(mP + i * 1.7) * 34 * s;
        const mY = oy + 10 * s + Math.cos(mP * 0.6 + i) * 7 * s;
        const mW = (16 + Math.sin(mP) * 5) * s;
        const mH = (5 + Math.sin(mP * 1.2) * 1.5) * s;
        const mA = 0.07 + Math.sin(mP * 2) * 0.03;
        const mC = i % 3 === 0 ? "80, 40, 120" : "50, 160, 50";
        const mG = ctx.createRadialGradient(mX, mY, 2 * s, mX, mY, mW);
        mG.addColorStop(0, `rgba(${mC}, ${mA})`);
        mG.addColorStop(0.6, `rgba(${mC}, ${mA * 0.3})`);
        mG.addColorStop(1, `rgba(${mC}, 0)`);
        ctx.fillStyle = mG;
        ctx.beginPath();
        ctx.ellipse(mX, mY, mW, mH, 0, 0, Math.PI * 2);
        ctx.fill();
      }

      // ========== WILL-O-WISPS ==========
      for (let i = 0; i < 3; i++) {
        const wP = wt * 0.9 + i * 4.1;
        const wR = (22 + i * 9) * s;
        const wX = ox + Math.sin(wP) * wR;
        const wY =
          oy -
          8 * s +
          Math.cos(wP * 0.7) * wR * 0.3 -
          Math.sin(wP * 1.8) * 4 * s;
        const wSz = (1.4 + Math.sin(wP * 2.5) * 0.5) * s;
        const wA = 0.25 + Math.sin(wP * 2) * 0.12;
        const wPurple = i % 2 === 0;
        ctx.fillStyle = wPurple
          ? `rgba(176, 96, 255, ${wA})`
          : `rgba(80, 255, 96, ${wA})`;
        setShadowBlur(ctx, 7 * s, wPurple ? wcGlowPurple : wcGlowGreen);
        ctx.beginPath();
        ctx.arc(wX, wY, wSz, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = wPurple
          ? `rgba(176, 96, 255, ${wA * 0.2})`
          : `rgba(80, 255, 96, ${wA * 0.2})`;
        ctx.beginPath();
        ctx.arc(
          wX - Math.cos(wP) * 3 * s,
          wY + 1.5 * s,
          wSz * 0.5,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      clearShadow(ctx);

      break;
    }

    case "cauldron": {
      const TRUE_ISO = 1 / Math.sqrt(3);
      const cVar = variant % 4;
      const cx = screenPos.x;
      const cy = screenPos.y;
      const cSeed = decorX * 17.3 + decorY * 31.7;
      const csrand = (n: number) => {
        const v = Math.sin(cSeed * 127.1 + n * 311.7) * 43_758.5453;
        return v - Math.floor(v);
      };

      if (cVar <= 1) {
        // ---- ACTUAL CAULDRONS ----
        const iron = cVar === 0 ? "#212121" : "#1a1520";
        const ironLight = cVar === 0 ? "#424242" : "#352840";
        const ironHi = cVar === 0 ? "#555555" : "#4a3858";
        const liquidColor = cVar === 0 ? "#64DD17" : "#9C27B0";
        const liquidBright = cVar === 0 ? "#B2FF59" : "#CE93D8";
        const glowColor = cVar === 0 ? "0,200,50" : "120,30,180";

        const rimR = 16 * s;
        const bulgeR = 18 * s;
        const baseR = 11 * s;
        const potH = 26 * s;
        const rimY = cy - potH;
        const bulgeY = cy - potH * 0.4;

        // Ground shadow
        ctx.fillStyle = "rgba(0,0,0,0.25)";
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy + 4 * s,
          bulgeR + 2 * s,
          (bulgeR + 2 * s) * TRUE_ISO,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Eerie ground glow
        const glowPulse = 0.3 + Math.sin(decorTime * 2) * 0.1;
        const groundGlow = ctx.createRadialGradient(
          cx,
          cy,
          0,
          cx,
          cy,
          bulgeR * 1.5
        );
        groundGlow.addColorStop(0, `rgba(${glowColor},${glowPulse})`);
        groundGlow.addColorStop(1, "transparent");
        ctx.fillStyle = groundGlow;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy + 2 * s,
          bulgeR * 1.5,
          bulgeR * TRUE_ISO,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Legs (isometric — front-left, front-right, back)
        ctx.fillStyle = iron;
        const legW = 2.5 * s;
        const legH = 5 * s;
        ctx.beginPath();
        ctx.moveTo(cx - 8 * s, cy);
        ctx.lineTo(cx - 8 * s + legW, cy + legW * TRUE_ISO * 0.5);
        ctx.lineTo(cx - 8 * s + legW, cy + legH + legW * TRUE_ISO * 0.5);
        ctx.lineTo(cx - 8 * s, cy + legH);
        ctx.closePath();
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(cx + 6 * s, cy);
        ctx.lineTo(cx + 6 * s + legW, cy - legW * TRUE_ISO * 0.5);
        ctx.lineTo(cx + 6 * s + legW, cy + legH - legW * TRUE_ISO * 0.5);
        ctx.lineTo(cx + 6 * s, cy + legH);
        ctx.closePath();
        ctx.fill();

        // Pot body with gradient
        const bodyGrad = ctx.createLinearGradient(
          cx - bulgeR,
          rimY,
          cx + bulgeR,
          rimY
        );
        bodyGrad.addColorStop(0, "#181818");
        bodyGrad.addColorStop(0.3, ironLight);
        bodyGrad.addColorStop(0.5, ironHi);
        bodyGrad.addColorStop(0.7, ironLight);
        bodyGrad.addColorStop(1, "#181818");
        ctx.fillStyle = bodyGrad;
        ctx.beginPath();
        ctx.moveTo(cx - rimR, rimY);
        ctx.bezierCurveTo(
          cx - bulgeR - 1 * s,
          rimY + (bulgeY - rimY) * 0.45,
          cx - bulgeR,
          bulgeY - (bulgeY - rimY) * 0.15,
          cx - bulgeR,
          bulgeY
        );
        ctx.bezierCurveTo(
          cx - bulgeR,
          bulgeY + (cy - bulgeY) * 0.55,
          cx - baseR - 2 * s,
          cy - 2 * s,
          cx - baseR,
          cy
        );
        ctx.ellipse(cx, cy, baseR, baseR * TRUE_ISO, 0, Math.PI, 0, true);
        ctx.bezierCurveTo(
          cx + baseR + 2 * s,
          cy - 2 * s,
          cx + bulgeR,
          bulgeY + (cy - bulgeY) * 0.55,
          cx + bulgeR,
          bulgeY
        );
        ctx.bezierCurveTo(
          cx + bulgeR,
          bulgeY - (bulgeY - rimY) * 0.15,
          cx + bulgeR + 1 * s,
          rimY + (bulgeY - rimY) * 0.45,
          cx + rimR,
          rimY
        );
        ctx.closePath();
        ctx.fill();

        // Rivet details on body
        ctx.fillStyle = ironHi;
        const rivetCount = 5 + Math.floor(csrand(10) * 3);
        for (let rv = 0; rv < rivetCount; rv++) {
          const rvAngle = (rv / rivetCount) * Math.PI + 0.1;
          const rvX = cx + Math.cos(rvAngle) * bulgeR * 0.95;
          const rvY = bulgeY + Math.sin(rvAngle) * 2 * s;
          ctx.beginPath();
          ctx.arc(rvX, rvY, 1.2 * s, 0, Math.PI * 2);
          ctx.fill();
        }

        // Band/ring around belly
        ctx.strokeStyle = ironHi;
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          bulgeY,
          bulgeR * 0.98,
          bulgeR * TRUE_ISO * 0.35,
          0,
          0,
          Math.PI
        );
        ctx.stroke();

        if (cVar === 1) {
          // Skull decoration on front face
          ctx.fillStyle = "rgba(200,200,190,0.4)";
          ctx.beginPath();
          ctx.arc(cx, bulgeY - 3 * s, 4 * s, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(20,0,30,0.6)";
          ctx.beginPath();
          ctx.arc(cx - 1.5 * s, bulgeY - 4 * s, 1 * s, 0, Math.PI * 2);
          ctx.arc(cx + 1.5 * s, bulgeY - 4 * s, 1 * s, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.moveTo(cx - 1 * s, bulgeY - 1 * s);
          ctx.lineTo(cx + 1 * s, bulgeY - 1 * s);
          ctx.stroke();

          // Chains hanging from handles
          ctx.strokeStyle = "rgba(100,80,120,0.5)";
          ctx.lineWidth = 1.5 * s;
          for (let ch = 0; ch < 4; ch++) {
            const chx = cx - rimR + ch * 3 * s - 2 * s;
            ctx.beginPath();
            ctx.moveTo(chx, rimY + 2 * s);
            ctx.quadraticCurveTo(
              chx - 1 * s,
              rimY + 8 * s,
              chx + 1 * s,
              rimY + 12 * s
            );
            ctx.stroke();
          }
        }

        // Handles (isometric arcs on sides)
        ctx.strokeStyle = iron;
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.arc(
          cx - rimR - 3 * s,
          rimY + 6 * s,
          5 * s,
          -Math.PI * 0.3,
          Math.PI * 0.6
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.arc(
          cx + rimR + 3 * s,
          rimY + 6 * s,
          5 * s,
          Math.PI * 0.4,
          Math.PI * 1.3
        );
        ctx.stroke();

        // Rim outer (isometric ellipse)
        ctx.fillStyle = ironLight;
        ctx.beginPath();
        ctx.ellipse(cx, rimY, rimR, rimR * TRUE_ISO, 0, 0, Math.PI * 2);
        ctx.fill();

        // Rim inner hole
        const rimThick = 3 * s;
        const innerR = rimR - rimThick;
        ctx.fillStyle = iron;
        ctx.beginPath();
        ctx.ellipse(cx, rimY, innerR, innerR * TRUE_ISO, 0, 0, Math.PI * 2);
        ctx.fill();

        // Bubbling liquid surface
        const gooH = Math.sin(decorTime * 3) * 2 * s;
        const gooR = innerR - 1 * s;
        ctx.fillStyle = liquidColor;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          rimY + 2 * s + gooH,
          gooR,
          gooR * TRUE_ISO,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Animated bubbles (seeded positions)
        ctx.fillStyle = liquidBright;
        for (let bb = 0; bb < 3; bb++) {
          const bbPhase =
            (decorTime * (3 + bb * 0.7) + csrand(20 + bb) * 6) % 4;
          if (bbPhase < 2) {
            const bbx = cx + (csrand(30 + bb) - 0.5) * gooR * 1.2;
            const bby = rimY + 2 * s + gooH - bbPhase * 3 * s;
            const bbr = (1.5 + csrand(40 + bb) * 2) * s * (1 - bbPhase * 0.3);
            ctx.globalAlpha = 0.7 * (1 - bbPhase / 2);
            ctx.beginPath();
            ctx.arc(bbx, bby, bbr, 0, Math.PI * 2);
            ctx.fill();
          }
        }
        ctx.globalAlpha = 1;

        // Rising fumes
        for (let fm = 0; fm < 3; fm++) {
          const fmPhase =
            (decorTime * 1.2 + fm * 1.3 + csrand(50 + fm) * 4) % 3;
          if (fmPhase < 2) {
            const fmAlpha = 0.25 * (1 - fmPhase / 2);
            const fmX = cx + (csrand(60 + fm) - 0.5) * gooR;
            const fmY = rimY - fmPhase * 12 * s;
            const fmR = (3 + fmPhase * 4) * s;
            ctx.fillStyle = `rgba(${glowColor},${fmAlpha})`;
            ctx.beginPath();
            ctx.arc(
              fmX + Math.sin(decorTime * 2 + fm) * 2 * s,
              fmY,
              fmR,
              0,
              Math.PI * 2
            );
            ctx.fill();
          }
        }
      } else if (cVar === 2) {
        // ---- POISON VOLCANO ----
        const rockDark = "#2a2218";
        const rockMid = "#3d3228";
        const rockLight = "#5a4a38";
        const toxicGreen = "#64DD17";

        // Ground shadow
        ctx.fillStyle = "rgba(0,0,0,0.3)";
        ctx.beginPath();
        ctx.ellipse(cx, cy + 5 * s, 24 * s, 12 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Toxic ground seep
        const seepPulse = 0.15 + Math.sin(decorTime * 1.5) * 0.05;
        const seepGrad = ctx.createRadialGradient(
          cx,
          cy + 3 * s,
          5 * s,
          cx,
          cy + 3 * s,
          22 * s
        );
        seepGrad.addColorStop(0, `rgba(100,221,23,${seepPulse})`);
        seepGrad.addColorStop(1, "transparent");
        ctx.fillStyle = seepGrad;
        ctx.beginPath();
        ctx.ellipse(cx, cy + 3 * s, 22 * s, 11 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Volcano cone — isometric prism (triangular profile)
        const vHW = 20 * s;
        const vHD = vHW * TRUE_ISO;
        const vH = 38 * s;
        const vTopHW = 7 * s;
        const vTopHD = vTopHW * TRUE_ISO;

        // Left face (lit)
        const vlG = ctx.createLinearGradient(cx - vHW, cy, cx, cy - vH);
        vlG.addColorStop(0, rockDark);
        vlG.addColorStop(0.4, rockMid);
        vlG.addColorStop(0.7, rockLight);
        vlG.addColorStop(1, rockMid);
        ctx.fillStyle = vlG;
        ctx.beginPath();
        ctx.moveTo(cx - vHW, cy);
        ctx.lineTo(cx, cy + vHD);
        ctx.lineTo(cx, cy + vTopHD - vH);
        ctx.lineTo(cx - vTopHW, cy - vH);
        ctx.closePath();
        ctx.fill();

        // Right face (shadow)
        const vrG = ctx.createLinearGradient(cx, cy + vHD, cx + vHW, cy - vH);
        vrG.addColorStop(0, "#1a1510");
        vrG.addColorStop(0.5, rockDark);
        vrG.addColorStop(1, rockMid);
        ctx.fillStyle = vrG;
        ctx.beginPath();
        ctx.moveTo(cx, cy + vHD);
        ctx.lineTo(cx + vHW, cy);
        ctx.lineTo(cx + vTopHW, cy - vH);
        ctx.lineTo(cx, cy + vTopHD - vH);
        ctx.closePath();
        ctx.fill();

        // Rock texture lines (seeded)
        ctx.strokeStyle = "rgba(0,0,0,0.15)";
        ctx.lineWidth = 0.8 * s;
        for (let rl = 0; rl < 6; rl++) {
          const rlt = 0.15 + csrand(100 + rl) * 0.7;
          const rlY = cy - vH * rlt;
          const rlHW = vHW * (1 - rlt * 0.65);
          ctx.beginPath();
          ctx.moveTo(cx - rlHW, rlY + (1 - rlt) * vHD * 0.3);
          ctx.lineTo(cx + rlHW * 0.3, rlY + (1 - rlt) * vHD * 0.8);
          ctx.stroke();
        }

        // Crater top face (isometric diamond)
        ctx.fillStyle = "#1a1008";
        ctx.beginPath();
        ctx.moveTo(cx - vTopHW, cy - vH);
        ctx.lineTo(cx, cy + vTopHD - vH);
        ctx.lineTo(cx + vTopHW, cy - vH);
        ctx.lineTo(cx, cy - vTopHD - vH);
        ctx.closePath();
        ctx.fill();

        // Toxic liquid in crater
        const craterPulse = Math.sin(decorTime * 3) * 1.5 * s;
        const crR = vTopHW - 2 * s;
        ctx.fillStyle = toxicGreen;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy - vH + craterPulse,
          crR,
          crR * TRUE_ISO,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Toxic flows down the sides (2-3 seeded streams)
        const numFlows = 2 + Math.floor(csrand(200) * 2);
        for (let fl = 0; fl < numFlows; fl++) {
          const flowT = 0.2 + csrand(210 + fl * 3) * 0.6;
          const onLeft = csrand(210 + fl * 3 + 1) > 0.5;
          const flowW = (2 + csrand(210 + fl * 3 + 2) * 2) * s;
          const startX = onLeft
            ? cx - vTopHW + (cx - (cx - vTopHW)) * flowT * 0.3
            : cx + vTopHW * flowT;
          const startY = cy - vH + vTopHD * flowT;
          const endX = onLeft
            ? cx - vHW * (0.3 + flowT * 0.4)
            : cx + vHW * (0.3 + flowT * 0.4);
          const endY = cy - 3 * s;

          const flowGrad = ctx.createLinearGradient(startX, startY, endX, endY);
          flowGrad.addColorStop(0, toxicGreen);
          flowGrad.addColorStop(0.6, "rgba(100,221,23,0.5)");
          flowGrad.addColorStop(1, "rgba(100,221,23,0.1)");
          ctx.strokeStyle = flowGrad;
          ctx.lineWidth = flowW;
          ctx.lineCap = "round";
          ctx.beginPath();
          ctx.moveTo(startX, startY);
          ctx.quadraticCurveTo(
            (startX + endX) / 2 + (csrand(220 + fl) - 0.5) * 5 * s,
            (startY + endY) / 2,
            endX,
            endY
          );
          ctx.stroke();
          ctx.lineCap = "butt";
        }

        // Rising toxic fumes
        for (let fm = 0; fm < 4; fm++) {
          const fmPhase =
            (decorTime * 0.8 + fm * 0.9 + csrand(300 + fm) * 3) % 3;
          if (fmPhase < 2.5) {
            const fmAlpha = 0.3 * (1 - fmPhase / 2.5);
            const fmX = cx + (csrand(310 + fm) - 0.5) * vTopHW;
            const fmY = cy - vH - fmPhase * 15 * s;
            const fmR = (2 + fmPhase * 5) * s;
            ctx.fillStyle = `rgba(100,220,30,${fmAlpha})`;
            ctx.beginPath();
            ctx.arc(
              fmX + Math.sin(decorTime * 1.5 + fm) * 3 * s,
              fmY,
              fmR,
              0,
              Math.PI * 2
            );
            ctx.fill();
          }
        }

        // Bright glow from crater
        const craterGlow = ctx.createRadialGradient(
          cx,
          cy - vH,
          0,
          cx,
          cy - vH,
          vTopHW * 2
        );
        craterGlow.addColorStop(0, "rgba(100,221,23,0.35)");
        craterGlow.addColorStop(1, "transparent");
        ctx.fillStyle = craterGlow;
        ctx.beginPath();
        ctx.arc(cx, cy - vH, vTopHW * 2, 0, Math.PI * 2);
        ctx.fill();
      } else {
        // ---- TOXIC CRATER / POOL ----
        const crRockDark = "#2a2a20";
        const crRockMid = "#44443a";
        const crRockLight = "#5e5e50";

        // Ground shadow
        ctx.fillStyle = "rgba(0,0,0,0.25)";
        ctx.beginPath();
        ctx.ellipse(cx, cy + 4 * s, 22 * s, 11 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Toxic ground stain
        const stainGrad = ctx.createRadialGradient(
          cx,
          cy,
          3 * s,
          cx,
          cy,
          20 * s
        );
        stainGrad.addColorStop(0, "rgba(100,221,23,0.2)");
        stainGrad.addColorStop(0.5, "rgba(80,180,20,0.08)");
        stainGrad.addColorStop(1, "transparent");
        ctx.fillStyle = stainGrad;
        ctx.beginPath();
        ctx.ellipse(cx, cy + 2 * s, 20 * s, 10 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Rock ring — isometric irregular boulders forming a crater rim
        const crOuterHW = 18 * s;
        const crOuterHD = crOuterHW * TRUE_ISO;
        const crInnerHW = 10 * s;
        const crInnerHD = crInnerHW * TRUE_ISO;
        const crRimH = 10 * s;

        // Back rocks (drawn first, behind pool)
        ctx.fillStyle = crRockDark;
        ctx.beginPath();
        ctx.moveTo(cx - crOuterHW, cy);
        ctx.lineTo(cx, cy - crOuterHD);
        ctx.lineTo(cx + crOuterHW, cy);
        ctx.lineTo(cx + crInnerHW, cy);
        ctx.lineTo(cx, cy - crInnerHD);
        ctx.lineTo(cx - crInnerHW, cy);
        ctx.closePath();
        ctx.fill();

        // Back rim height
        ctx.fillStyle = crRockMid;
        ctx.beginPath();
        ctx.moveTo(cx - crOuterHW, cy);
        ctx.lineTo(cx, cy - crOuterHD);
        ctx.lineTo(cx, cy - crOuterHD - crRimH);
        ctx.lineTo(cx - crOuterHW, cy - crRimH);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = crRockDark;
        ctx.beginPath();
        ctx.moveTo(cx, cy - crOuterHD);
        ctx.lineTo(cx + crOuterHW, cy);
        ctx.lineTo(cx + crOuterHW, cy - crRimH);
        ctx.lineTo(cx, cy - crOuterHD - crRimH);
        ctx.closePath();
        ctx.fill();

        // Toxic pool inside crater
        const poolPulse = Math.sin(decorTime * 2.5) * 1.5 * s;
        const poolGrad = ctx.createRadialGradient(
          cx - 2 * s,
          cy + poolPulse,
          0,
          cx,
          cy,
          crInnerHW
        );
        poolGrad.addColorStop(0, "#B2FF59");
        poolGrad.addColorStop(0.4, "#64DD17");
        poolGrad.addColorStop(0.8, "#33AA00");
        poolGrad.addColorStop(1, "#1a6600");
        ctx.fillStyle = poolGrad;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy + poolPulse * 0.5,
          crInnerHW * 0.9,
          crInnerHD * 0.9,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Animated bubbles in pool
        ctx.fillStyle = "#B2FF59";
        for (let pb = 0; pb < 4; pb++) {
          const pbPhase =
            (decorTime * 2 + pb * 0.8 + csrand(400 + pb) * 5) % 2.5;
          if (pbPhase < 1.5) {
            const pbAlpha = 0.6 * (1 - pbPhase / 1.5);
            const pbx = cx + (csrand(410 + pb) - 0.5) * crInnerHW * 1.2;
            const pby = cy + poolPulse * 0.5 - pbPhase * 2 * s;
            ctx.globalAlpha = pbAlpha;
            ctx.beginPath();
            ctx.arc(pbx, pby, (1.5 + csrand(420 + pb)) * s, 0, Math.PI * 2);
            ctx.fill();
          }
        }
        ctx.globalAlpha = 1;

        // Front rim left face (lit)
        const frG = ctx.createLinearGradient(
          cx - crOuterHW,
          cy,
          cx,
          cy + crOuterHD
        );
        frG.addColorStop(0, crRockLight);
        frG.addColorStop(1, crRockMid);
        ctx.fillStyle = frG;
        ctx.beginPath();
        ctx.moveTo(cx - crOuterHW, cy);
        ctx.lineTo(cx, cy + crOuterHD);
        ctx.lineTo(cx, cy + crOuterHD - crRimH);
        ctx.lineTo(cx - crOuterHW, cy - crRimH);
        ctx.closePath();
        ctx.fill();

        // Front rim right face (shadow)
        ctx.fillStyle = crRockDark;
        ctx.beginPath();
        ctx.moveTo(cx, cy + crOuterHD);
        ctx.lineTo(cx + crOuterHW, cy);
        ctx.lineTo(cx + crOuterHW, cy - crRimH);
        ctx.lineTo(cx, cy + crOuterHD - crRimH);
        ctx.closePath();
        ctx.fill();

        // Rim top face (ring between outer and inner diamonds)
        ctx.fillStyle = crRockLight;
        ctx.beginPath();
        ctx.moveTo(cx - crOuterHW, cy - crRimH);
        ctx.lineTo(cx, cy + crOuterHD - crRimH);
        ctx.lineTo(cx + crOuterHW, cy - crRimH);
        ctx.lineTo(cx, cy - crOuterHD - crRimH);
        ctx.closePath();
        ctx.fill();
        // Cut out inner opening
        ctx.fillStyle = "#1a2a10";
        ctx.beginPath();
        ctx.moveTo(cx - crInnerHW, cy - crRimH);
        ctx.lineTo(cx, cy + crInnerHD - crRimH);
        ctx.lineTo(cx + crInnerHW, cy - crRimH);
        ctx.lineTo(cx, cy - crInnerHD - crRimH);
        ctx.closePath();
        ctx.fill();

        // Seeded rock bumps on rim
        for (let rb = 0; rb < 6; rb++) {
          const rbAngle = (rb / 6) * Math.PI * 2 + csrand(500 + rb) * 0.5;
          const rbDist = crOuterHW * (0.85 + csrand(510 + rb) * 0.15);
          const rbx = cx + Math.cos(rbAngle) * rbDist;
          const rby = cy - crRimH + Math.sin(rbAngle) * rbDist * TRUE_ISO;
          const rbSize = (2 + csrand(520 + rb) * 3) * s;
          const rbGrad = ctx.createRadialGradient(
            rbx - 1 * s,
            rby - 1 * s,
            0,
            rbx,
            rby,
            rbSize
          );
          rbGrad.addColorStop(0, crRockLight);
          rbGrad.addColorStop(1, crRockMid);
          ctx.fillStyle = rbGrad;
          ctx.beginPath();
          ctx.arc(rbx, rby - rbSize * 0.5, rbSize, 0, Math.PI * 2);
          ctx.fill();
        }

        // Green glow from pool
        const poolGlow = ctx.createRadialGradient(cx, cy, 0, cx, cy, crOuterHW);
        poolGlow.addColorStop(
          0,
          `rgba(100,221,23,${0.2 + Math.sin(decorTime * 2) * 0.05})`
        );
        poolGlow.addColorStop(1, "transparent");
        ctx.fillStyle = poolGlow;
        ctx.beginPath();
        ctx.arc(cx, cy - crRimH * 0.5, crOuterHW, 0, Math.PI * 2);
        ctx.fill();

        // Rising toxic fumes
        for (let fm = 0; fm < 3; fm++) {
          const fmPhase = (decorTime * 1 + fm * 1.1 + csrand(600 + fm) * 4) % 3;
          if (fmPhase < 2) {
            const fmAlpha = 0.2 * (1 - fmPhase / 2);
            const fmX = cx + (csrand(610 + fm) - 0.5) * crInnerHW;
            const fmY = cy - crRimH - fmPhase * 14 * s;
            const fmR = (2.5 + fmPhase * 5) * s;
            ctx.fillStyle = `rgba(100,220,30,${fmAlpha})`;
            ctx.beginPath();
            ctx.arc(
              fmX + Math.sin(decorTime * 1.8 + fm) * 2 * s,
              fmY,
              fmR,
              0,
              Math.PI * 2
            );
            ctx.fill();
          }
        }

        // Splatter stains around edges
        ctx.fillStyle = "rgba(100,221,23,0.12)";
        for (let sp = 0; sp < 5; sp++) {
          const spAngle = csrand(700 + sp) * Math.PI * 2;
          const spDist = crOuterHW + 2 * s + csrand(710 + sp) * 6 * s;
          const spx = cx + Math.cos(spAngle) * spDist;
          const spy = cy + Math.sin(spAngle) * spDist * TRUE_ISO;
          ctx.beginPath();
          ctx.arc(spx, spy, (1 + csrand(720 + sp) * 2) * s, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      break;
    }

    case "tentacle": {
      drawTentacle(
        ctx,
        screenPos.x,
        screenPos.y,
        s,
        variant,
        decorX,
        decorY,
        decorTime,
        true
      );
      break;
    }

    case "deep_water": {
      const cx = screenPos.x;
      const cy = screenPos.y;
      const waterSeed = (dec.x || 0) * 11.3 + (dec.y || 0) * 23.7;
      const iso = ISO_Y_RATIO;
      const t = decorTime;

      // 1. Outer wet ground blend (feathers into terrain)
      const wetBlend = ctx.createRadialGradient(
        cx,
        cy + 2 * s,
        22 * s,
        cx,
        cy + 2 * s,
        42 * s
      );
      wetBlend.addColorStop(0, "rgba(10,25,50,0.35)");
      wetBlend.addColorStop(0.5, "rgba(15,30,55,0.18)");
      wetBlend.addColorStop(1, "transparent");
      ctx.fillStyle = wetBlend;
      drawOrganicWaterShape(
        ctx,
        cx,
        cy + 2 * s,
        42 * s,
        42 * s * iso,
        waterSeed,
        0.16
      );
      ctx.fill();

      // 2. Flat stone ring around water (organic)
      const ringGrad = ctx.createRadialGradient(cx, cy, 24 * s, cx, cy, 34 * s);
      ringGrad.addColorStop(0, "rgba(55,50,45,0.92)");
      ringGrad.addColorStop(0.35, "rgba(65,60,54,0.88)");
      ringGrad.addColorStop(0.65, "rgba(58,53,47,0.85)");
      ringGrad.addColorStop(1, "rgba(48,43,38,0.8)");
      ctx.fillStyle = ringGrad;
      drawOrganicWaterShape(
        ctx,
        cx,
        cy,
        32 * s,
        32 * s * iso,
        waterSeed + 10,
        0.15
      );
      ctx.fill();

      // Speckled stone texture on ring
      for (let sp = 0; sp < 18; sp++) {
        const spAng = (sp / 18) * Math.PI * 2 + Math.sin(waterSeed + sp) * 0.3;
        const spDist = (26 + Math.sin(sp * 2.7 + waterSeed) * 3) * s;
        const spX = cx + Math.cos(spAng) * spDist;
        const spY = cy + Math.sin(spAng) * spDist * iso;
        const spSize = (1 + Math.sin(waterSeed + sp * 4.3) * 0.6) * s;
        ctx.fillStyle =
          sp % 2 === 0 ? "rgba(80,75,68,0.35)" : "rgba(40,36,32,0.3)";
        ctx.beginPath();
        ctx.arc(spX, spY, spSize, 0, Math.PI * 2);
        ctx.fill();
      }

      // 3. Abyss layer — rich multi-stop deep water gradient
      const abyssGrad = ctx.createRadialGradient(
        cx,
        cy + 3 * s,
        0,
        cx,
        cy,
        28 * s
      );
      abyssGrad.addColorStop(0, "rgba(2,8,25,0.99)");
      abyssGrad.addColorStop(0.15, "rgba(4,12,35,0.97)");
      abyssGrad.addColorStop(0.3, "rgba(8,22,50,0.95)");
      abyssGrad.addColorStop(0.5, "rgba(12,35,70,0.92)");
      abyssGrad.addColorStop(0.7, "rgba(18,50,95,0.88)");
      abyssGrad.addColorStop(0.85, "rgba(25,65,115,0.85)");
      abyssGrad.addColorStop(1, "rgba(35,80,135,0.8)");
      ctx.fillStyle = abyssGrad;
      drawOrganicWaterShape(
        ctx,
        cx,
        cy,
        28 * s,
        28 * s * iso,
        waterSeed + 20,
        0.12
      );
      ctx.fill();

      // 4. Surface water layer with directional light offset
      const surfGrad = ctx.createRadialGradient(
        cx - 6 * s,
        cy - 3 * s,
        0,
        cx,
        cy + 1 * s,
        26 * s
      );
      surfGrad.addColorStop(0, "rgba(70,140,195,0.65)");
      surfGrad.addColorStop(0.25, "rgba(55,120,180,0.5)");
      surfGrad.addColorStop(0.5, "rgba(40,100,165,0.35)");
      surfGrad.addColorStop(0.75, "rgba(28,75,140,0.2)");
      surfGrad.addColorStop(1, "rgba(18,55,110,0.1)");
      ctx.fillStyle = surfGrad;
      drawOrganicWaterShape(
        ctx,
        cx,
        cy - 1 * s,
        25 * s,
        25 * s * iso,
        waterSeed + 30,
        0.1
      );
      ctx.fill();

      // 5. Underwater caustics (animated light network)
      ctx.globalAlpha = 0.12;
      for (let c = 0; c < 10; c++) {
        const ct = t * 0.5 + c * 0.65;
        const ccx = cx + Math.sin(ct + c * 1.7) * 18 * s;
        const ccy = cy + 2 * s + Math.cos(ct * 0.4 + c) * 6 * s;
        const ccs = (3 + Math.sin(ct * 1.1) * 2) * s;
        const cg = ctx.createRadialGradient(ccx, ccy, 0, ccx, ccy, ccs);
        cg.addColorStop(0, "#b0d8ff");
        cg.addColorStop(0.4, "rgba(140,195,235,0.5)");
        cg.addColorStop(1, "transparent");
        ctx.fillStyle = cg;
        ctx.beginPath();
        ctx.ellipse(ccx, ccy, ccs * 1.5, ccs * 0.6, c * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.globalAlpha = 1;

      // 6. Underwater glow pulse (deep mysterious light)
      const glowPhase = Math.sin(t * 0.6 + waterSeed) * 0.3 + 0.7;
      const glowGrad = ctx.createRadialGradient(
        cx,
        cy + 3 * s,
        0,
        cx,
        cy,
        22 * s
      );
      glowGrad.addColorStop(0, `rgba(40,130,200,${0.22 * glowPhase})`);
      glowGrad.addColorStop(0.35, `rgba(30,100,170,${0.14 * glowPhase})`);
      glowGrad.addColorStop(0.7, `rgba(20,70,140,${0.06 * glowPhase})`);
      glowGrad.addColorStop(1, "transparent");
      ctx.fillStyle = glowGrad;
      ctx.beginPath();
      ctx.ellipse(cx, cy, 20 * s, 20 * s * iso, 0, 0, Math.PI * 2);
      ctx.fill();

      // Secondary color-shifting glow
      const glowPhase2 = Math.sin(t * 0.4 + waterSeed + 2) * 0.5 + 0.5;
      const glow2 = ctx.createRadialGradient(
        cx + Math.sin(t * 0.3) * 4 * s,
        cy + 2 * s,
        0,
        cx,
        cy,
        16 * s
      );
      glow2.addColorStop(0, `rgba(60,180,220,${0.1 * glowPhase2})`);
      glow2.addColorStop(0.5, `rgba(40,150,200,${0.05 * glowPhase2})`);
      glow2.addColorStop(1, "transparent");
      ctx.fillStyle = glow2;
      ctx.beginPath();
      ctx.ellipse(cx, cy, 14 * s, 14 * s * iso, 0, 0, Math.PI * 2);
      ctx.fill();

      // 7. Animated concentric ripples (offset origins, 5 rings)
      for (let rip = 0; rip < 5; rip++) {
        const ripPhase = ((t * 0.35 + rip * 0.7) % 3) / 3;
        const ripAlpha = 0.3 * (1 - ripPhase);
        const ripSize = ripPhase * 18;
        ctx.strokeStyle = `rgba(150,200,255,${ripAlpha})`;
        ctx.lineWidth = (1.8 - ripPhase * 1.2) * s;
        ctx.beginPath();
        ctx.ellipse(
          cx - 4 * s + rip * 2.5 * s,
          cy + 1 * s + rip * 0.5 * s,
          (6 + ripSize) * s,
          (6 + ripSize) * s * iso,
          0.04,
          0,
          Math.PI * 2
        );
        ctx.stroke();
      }

      // 8. Current lines (subtle animated flow beneath surface)
      ctx.globalAlpha = 0.08;
      ctx.strokeStyle = "#90caf9";
      ctx.lineWidth = 1.2 * s;
      for (let cl = 0; cl < 5; cl++) {
        const clY = cy - 8 * s + cl * 4 * s;
        const clOffset = t * 12 + cl * 40 + waterSeed;
        ctx.beginPath();
        for (let p = 0; p <= 20; p++) {
          const px = cx - 18 * s + (p / 20) * 36 * s;
          const py = clY + Math.sin(p * 0.6 + clOffset * 0.04) * 2.5 * s;
          if (p === 0) {
            ctx.moveTo(px, py);
          } else {
            ctx.lineTo(px, py);
          }
        }
        ctx.stroke();
      }
      ctx.globalAlpha = 1;

      // 9. Sparkles on surface (twinkling light)
      for (let sp = 0; sp < 14; sp++) {
        const sparkT = t * 2.2 + sp * 0.6;
        const sparkAlpha = 0.25 + Math.sin(sparkT * 3) * 0.3;
        if (sparkAlpha > 0.3) {
          const spAng = sp * 0.45 + Math.sin(t * 0.3) * 0.15;
          const spDist = 4 + sp * 1.4;
          const spx = cx + Math.cos(spAng) * spDist * s;
          const spy = cy + 1 * s + Math.sin(spAng) * spDist * iso * s;
          ctx.globalAlpha = sparkAlpha;
          ctx.fillStyle =
            sp % 3 === 0 ? "#e3f2fd" : sp % 3 === 1 ? "#bbdefb" : "#cce5ff";
          ctx.beginPath();
          ctx.arc(
            spx,
            spy,
            (0.7 + Math.sin(sparkT) * 0.35) * s,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
      }
      ctx.globalAlpha = 1;

      // 10. Shimmer highlights (broader, animated)
      for (let sh = 0; sh < 6; sh++) {
        const shAngle = sh * 1.05 + t * 0.4 + waterSeed * 0.1;
        const shDist = (9 + Math.sin(t * 0.8 + sh) * 5) * s;
        const shX = cx + Math.cos(shAngle) * shDist * 0.8;
        const shY = cy + Math.sin(shAngle) * shDist * iso - 1.5 * s;
        const shSize = (3.5 + Math.sin(t * 1.6 + sh * 1.8) * 1.8) * s;
        const shAlpha = 0.25 + Math.sin(t * 1.2 + sh * 1.5) * 0.15;
        ctx.fillStyle = `rgba(190,225,255,${shAlpha})`;
        ctx.beginPath();
        ctx.ellipse(
          shX,
          shY,
          shSize,
          shSize * 0.35,
          shAngle * 0.25,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // 11. Bubbles rising (varied sizes, staggered timing)
      for (let bub = 0; bub < 6; bub++) {
        const bubCycle = (t * 0.7 + bub * 0.45 + waterSeed * 0.01) % 2.5;
        if (bubCycle < 1.5) {
          const bubProgress = bubCycle / 1.5;
          const bubX = cx + Math.sin(bub * 2.7 + waterSeed) * 14 * s;
          const bubY = cy + 6 * s - bubProgress * 20 * s;
          const bubWobble = Math.sin(t * 3 + bub * 2) * 1.5 * s;
          const bubSize = (1.5 + bub * 0.4) * s * (1 - bubProgress * 0.6);
          const bubAlpha = 0.55 * (1 - bubProgress * 0.8);
          ctx.fillStyle = `rgba(170,215,255,${bubAlpha})`;
          ctx.beginPath();
          ctx.arc(bubX + bubWobble, bubY, bubSize, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = `rgba(255,255,255,${bubAlpha * 0.7})`;
          ctx.beginPath();
          ctx.arc(
            bubX + bubWobble - bubSize * 0.3,
            bubY - bubSize * 0.3,
            bubSize * 0.3,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
      }

      // 13. Rocks/pebbles at edge (with shadow and variety)
      for (let rock = 0; rock < 8; rock++) {
        const rkAng =
          (rock / 8) * Math.PI * 2 + Math.sin(waterSeed + rock) * 0.35;
        const rkDist = (28 + Math.sin(rock * 2.3 + waterSeed) * 3.5) * s;
        const rkX = cx + Math.cos(rkAng) * rkDist;
        const rkY = cy + Math.sin(rkAng) * rkDist * iso;
        const rkSize = (2.5 + Math.sin(waterSeed + rock * 3.1) * 1.2) * s;
        // Rock shadow
        ctx.fillStyle = "rgba(0,0,0,0.2)";
        ctx.beginPath();
        ctx.ellipse(
          rkX + 0.5 * s,
          rkY + 0.8 * s,
          rkSize * 1.1,
          rkSize * 0.5,
          rkAng,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Rock body
        ctx.fillStyle =
          rock % 3 === 0 ? "#4a4540" : rock % 3 === 1 ? "#3d3832" : "#55504a";
        ctx.beginPath();
        ctx.ellipse(rkX, rkY, rkSize, rkSize * 0.55, rkAng, 0, Math.PI * 2);
        ctx.fill();
        // Rock highlight
        ctx.fillStyle = "rgba(255,255,255,0.08)";
        ctx.beginPath();
        ctx.ellipse(
          rkX - rkSize * 0.2,
          rkY - rkSize * 0.15,
          rkSize * 0.5,
          rkSize * 0.25,
          rkAng,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // 14. Foam patches at shoreline
      ctx.globalAlpha = 0.15 + Math.sin(t * 1.2) * 0.04;
      ctx.fillStyle = "rgba(200,230,255,0.6)";
      for (let fp = 0; fp < 6; fp++) {
        const fpAng = fp * 1.05 + waterSeed * 0.05;
        const fpDist = (25 + Math.sin(t * 0.5 + fp * 1.8) * 2) * s;
        const fpX = cx + Math.cos(fpAng) * fpDist;
        const fpY = cy + Math.sin(fpAng) * fpDist * iso;
        const fpW = (4 + Math.sin(fp * 2.1 + waterSeed) * 1.5) * s;
        const fpH = fpW * 0.3;
        ctx.beginPath();
        ctx.ellipse(fpX, fpY, fpW, fpH, fpAng * 0.3, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.globalAlpha = 1;

      break;
    }

    case "pond": {
      // Teal/emerald pond matching Carnegie Lake's water palette
      const pondSeed = (dec.x || 0) * 11.3 + (dec.y || 0) * 23.7;
      const pondIso = ISO_Y_RATIO;

      // 1. Outer wet ground ring (earthy blend into terrain)
      const pondWetG = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y,
        18 * s,
        screenPos.x,
        screenPos.y,
        36 * s
      );
      pondWetG.addColorStop(0, "transparent");
      pondWetG.addColorStop(0.5, "rgba(58,47,31,0.3)");
      pondWetG.addColorStop(1, "rgba(58,47,31,0.1)");
      ctx.fillStyle = pondWetG;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        36 * s,
        36 * s * pondIso,
        pondSeed,
        0.16
      );
      ctx.fill();

      // 2. Earthy stone embankment rim
      const pondRimG = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y + 2 * s,
        4 * s,
        screenPos.x,
        screenPos.y + 2 * s,
        30 * s
      );
      pondRimG.addColorStop(0, "#5a4a35");
      pondRimG.addColorStop(0.5, "#4a3d2a");
      pondRimG.addColorStop(1, "#3a2f1f");
      ctx.fillStyle = pondRimG;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        30 * s,
        30 * s * pondIso,
        pondSeed + 10,
        0.14
      );
      ctx.fill();

      // 3. Teal water body (Carnegie Lake palette)
      const pondWaterG = ctx.createRadialGradient(
        screenPos.x - 4 * s,
        screenPos.y - 2 * s,
        0,
        screenPos.x,
        screenPos.y + 2 * s,
        26 * s
      );
      pondWaterG.addColorStop(0, "#004d6b");
      pondWaterG.addColorStop(0.2, "#006080");
      pondWaterG.addColorStop(0.4, "#00838f");
      pondWaterG.addColorStop(0.6, "#0097a7");
      pondWaterG.addColorStop(0.8, "#00acc1");
      pondWaterG.addColorStop(1, "#26c6da");
      ctx.fillStyle = pondWaterG;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        26 * s,
        26 * s * pondIso,
        pondSeed + 20,
        0.12
      );
      ctx.fill();

      // 4. Surface reflection layer (lighter teal offset)
      const pondSurfG = ctx.createRadialGradient(
        screenPos.x - 6 * s,
        screenPos.y - 3 * s,
        0,
        screenPos.x,
        screenPos.y,
        22 * s
      );
      pondSurfG.addColorStop(0, "rgba(77,208,225,0.4)");
      pondSurfG.addColorStop(0.4, "rgba(38,198,218,0.25)");
      pondSurfG.addColorStop(1, "rgba(0,172,193,0.1)");
      ctx.fillStyle = pondSurfG;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y - 2 * s,
        22 * s,
        22 * s * pondIso,
        pondSeed + 30,
        0.1
      );
      ctx.fill();

      // 5. Animated ripples (teal-tinted)
      for (let rip = 0; rip < 3; rip++) {
        const ripPh = (decorTime * 0.5 + rip * 0.33) % 1;
        const ripR = (6 + ripPh * 14) * s;
        ctx.strokeStyle = `rgba(100,220,230,${0.35 * (1 - ripPh)})`;
        ctx.lineWidth = (1.3 - ripPh) * s;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y,
          ripR,
          ripR * pondIso,
          0,
          0,
          Math.PI * 2
        );
        ctx.stroke();
      }

      // 6. Shimmer highlights (warm teal)
      ctx.fillStyle = "rgba(180,240,240,0.4)";
      for (let sh = 0; sh < 4; sh++) {
        const shAng = sh * 0.7 + decorTime * 0.4 + pondSeed * 0.1;
        const shDist = (8 + Math.sin(decorTime + sh) * 4) * s;
        const shX = screenPos.x + Math.cos(shAng) * shDist * 0.8;
        const shY = screenPos.y + Math.sin(shAng) * shDist * 0.4 - 2 * s;
        const shSz = (3.5 + Math.sin(decorTime * 2 + sh * 2) * 1.5) * s;
        ctx.beginPath();
        ctx.ellipse(shX, shY, shSz, shSz * 0.4, shAng * 0.3, 0, Math.PI * 2);
        ctx.fill();
      }

      // 7. Subtle magical glow (teal center pulse)
      const pondGlowPh = Math.sin(decorTime * 1.2 + pondSeed) * 0.3 + 0.7;
      const pondGlow = ctx.createRadialGradient(
        screenPos.x,
        screenPos.y + 2 * s,
        0,
        screenPos.x,
        screenPos.y,
        18 * s
      );
      pondGlow.addColorStop(0, `rgba(100,255,220,${0.12 * pondGlowPh})`);
      pondGlow.addColorStop(0.5, `rgba(80,220,200,${0.06 * pondGlowPh})`);
      pondGlow.addColorStop(1, "transparent");
      ctx.fillStyle = pondGlow;
      ctx.beginPath();
      ctx.ellipse(screenPos.x, screenPos.y, 16 * s, 8 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // 8. Natural edge stones/pebbles
      for (let rock = 0; rock < 6; rock++) {
        const rkAng =
          (rock / 6) * Math.PI * 2 + Math.sin(pondSeed + rock) * 0.4;
        const rkDist = (24 + Math.sin(rock * 2 + pondSeed) * 3) * s;
        const rkX = screenPos.x + Math.cos(rkAng) * rkDist;
        const rkY = screenPos.y + Math.sin(rkAng) * rkDist * pondIso;
        const rkSz = (2.5 + Math.sin(pondSeed + rock * 3) * 1) * s;
        ctx.fillStyle = rock % 2 === 0 ? "#5a4a3a" : "#4a3d2d";
        ctx.beginPath();
        ctx.ellipse(rkX, rkY, rkSz, rkSz * 0.6, rkAng, 0, Math.PI * 2);
        ctx.fill();
      }

      // 9. Grass tufts at edges
      ctx.strokeStyle = "rgba(80,120,60,0.4)";
      ctx.lineWidth = 0.8 * s;
      for (let gt = 0; gt < 5; gt++) {
        const gtAng = gt * 1.3 + pondSeed * 0.1;
        const gtDist = (27 + Math.sin(gt * 2.7 + pondSeed) * 3) * s;
        const gtX = screenPos.x + Math.cos(gtAng) * gtDist;
        const gtY = screenPos.y + Math.sin(gtAng) * gtDist * pondIso;
        for (let bl = 0; bl < 3; bl++) {
          const blAng = -Math.PI / 2 + (bl - 1) * 0.3;
          ctx.beginPath();
          ctx.moveTo(gtX, gtY);
          ctx.lineTo(
            gtX + Math.cos(blAng) * 3 * s,
            gtY + Math.sin(blAng) * 3 * s
          );
          ctx.stroke();
        }
      }

      break;
    }

    case "sunken_pillar": {
      const spPalettes: Record<
        string,
        {
          stone: string;
          stoneDark: string;
          brokenTop: string;
          moss: string;
          basePool: string;
          ripple: string;
          bandStroke: string;
        }
      > = {
        desert: {
          bandStroke: "rgba(60,40,10,0.15)",
          basePool: "rgba(160,140,100,0.35)",
          brokenTop: "#a08a68",
          moss: "#7a6a48",
          ripple: "rgba(180,160,120,0.2)",
          stone: "#8a7a58",
          stoneDark: "#6a5a40",
        },
        grassland: {
          bandStroke: "rgba(0,0,0,0.15)",
          basePool: "rgba(60,90,60,0.5)",
          brokenTop: "#7a8a7a",
          moss: "#3a5a2a",
          ripple: "rgba(100,140,100,0.3)",
          stone: "#5a6a5a",
          stoneDark: "#3a4a3a",
        },
        swamp: {
          bandStroke: "rgba(0,20,0,0.2)",
          basePool: "rgba(30,50,20,0.6)",
          brokenTop: "#4a6a48",
          moss: "#1a3a12",
          ripple: "rgba(50,80,40,0.35)",
          stone: "#3a5a38",
          stoneDark: "#1a3a18",
        },
        volcanic: {
          bandStroke: "rgba(40,10,0,0.2)",
          basePool: "rgba(120,40,10,0.45)",
          brokenTop: "#6a4a48",
          moss: "#4a2a1a",
          ripple: "rgba(200,80,20,0.25)",
          stone: "#5a3a38",
          stoneDark: "#3a2218",
        },
        winter: {
          bandStroke: "rgba(0,10,30,0.15)",
          basePool: "rgba(140,180,210,0.45)",
          brokenTop: "#8a9aa8",
          moss: "#8aaa9a",
          ripple: "rgba(180,210,240,0.3)",
          stone: "#6a7a88",
          stoneDark: "#4a5a68",
        },
      };
      const sp = spPalettes[mapTheme] ?? spPalettes.grassland;

      // Pool around base
      ctx.fillStyle = sp.basePool;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 5 * s,
        16 * s,
        7 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();
      // Ripples
      ctx.strokeStyle = sp.ripple;
      ctx.lineWidth = 1 * s;
      const ripPhase = (decorTime % 2) / 2;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 5 * s,
        10 * s * ripPhase,
        4 * s * ripPhase,
        0,
        0,
        Math.PI * 2
      );
      ctx.stroke();

      // Winter: frozen pool surface
      if (mapTheme === "winter") {
        ctx.fillStyle = "rgba(200,220,245,0.3)";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y + 5 * s,
          14 * s,
          6 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.strokeStyle = "rgba(220,235,255,0.4)";
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 10 * s, screenPos.y + 4 * s);
        ctx.lineTo(screenPos.x - 2 * s, screenPos.y + 6 * s);
        ctx.moveTo(screenPos.x + 3 * s, screenPos.y + 3 * s);
        ctx.lineTo(screenPos.x + 9 * s, screenPos.y + 5 * s);
        ctx.stroke();
      }

      // Desert: sand drift instead of water
      if (mapTheme === "desert") {
        ctx.fillStyle = "rgba(190,170,130,0.4)";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 3 * s,
          screenPos.y + 4 * s,
          18 * s,
          6 * s,
          0.15,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Volcanic: bubbling lava glow in pool
      if (mapTheme === "volcanic") {
        const lavaGlow = ctx.createRadialGradient(
          screenPos.x,
          screenPos.y + 5 * s,
          0,
          screenPos.x,
          screenPos.y + 5 * s,
          14 * s
        );
        lavaGlow.addColorStop(0, "rgba(255,120,20,0.25)");
        lavaGlow.addColorStop(0.6, "rgba(200,60,10,0.1)");
        lavaGlow.addColorStop(1, "rgba(100,20,0,0)");
        ctx.fillStyle = lavaGlow;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y + 5 * s,
          14 * s,
          6 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Tilted pillar body
      ctx.save();
      ctx.translate(screenPos.x, screenPos.y);
      ctx.rotate(-0.15);

      // Left face
      ctx.fillStyle = sp.stoneDark;
      ctx.beginPath();
      ctx.moveTo(-6 * s, 5 * s);
      ctx.lineTo(-5 * s, -40 * s);
      ctx.lineTo(0, -42 * s);
      ctx.lineTo(0, 5 * s);
      ctx.closePath();
      ctx.fill();

      // Right face
      ctx.fillStyle = sp.stone;
      ctx.beginPath();
      ctx.moveTo(6 * s, 5 * s);
      ctx.lineTo(5 * s, -40 * s);
      ctx.lineTo(0, -42 * s);
      ctx.lineTo(0, 5 * s);
      ctx.closePath();
      ctx.fill();

      // Broken top
      ctx.fillStyle = sp.brokenTop;
      ctx.beginPath();
      ctx.moveTo(-5 * s, -40 * s);
      ctx.lineTo(-3 * s, -44 * s);
      ctx.lineTo(2 * s, -42 * s);
      ctx.lineTo(5 * s, -40 * s);
      ctx.lineTo(3 * s, -38 * s);
      ctx.closePath();
      ctx.fill();

      // Carved bands
      ctx.strokeStyle = sp.bandStroke;
      ctx.lineWidth = 1 * s;
      for (let b = 0; b < 4; b++) {
        const by = -5 * s - b * 9 * s;
        ctx.beginPath();
        ctx.moveTo(-5.5 * s, by);
        ctx.lineTo(5.5 * s, by - 1 * s);
        ctx.stroke();
      }

      // Moss/growth patches (themed)
      ctx.fillStyle = sp.moss;
      ctx.beginPath();
      ctx.ellipse(-4 * s, -15 * s, 3 * s, 5 * s, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(3 * s, -25 * s, 2.5 * s, 4 * s, 0.2, 0, Math.PI * 2);
      ctx.fill();

      // === REGIONAL OVERLAYS (inside rotated transform) ===
      if (mapTheme === "swamp") {
        // Dripping vines down the pillar
        ctx.globalAlpha = 0.6;
        ctx.strokeStyle = "#1a3a12";
        ctx.lineWidth = 1 * s;
        for (let vi = 0; vi < 3; vi++) {
          const vx = -3 * s + vi * 3 * s;
          const vy0 = -(20 + vi * 8) * s;
          ctx.beginPath();
          ctx.moveTo(vx, vy0);
          ctx.bezierCurveTo(
            vx - 2 * s,
            vy0 + 6 * s,
            vx + 1 * s,
            vy0 + 12 * s,
            vx - 1 * s,
            vy0 + 18 * s
          );
          ctx.stroke();
        }
        // Algae splotches
        ctx.globalAlpha = 0.4;
        ctx.fillStyle = "#2a4a18";
        ctx.beginPath();
        ctx.ellipse(2 * s, -8 * s, 4 * s, 2.5 * s, 0.4, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(-3 * s, -30 * s, 3 * s, 2 * s, -0.2, 0, Math.PI * 2);
        ctx.fill();
        ctx.globalAlpha = 1;
      } else if (mapTheme === "desert") {
        // Sand accumulation on ledges
        ctx.globalAlpha = 0.5;
        ctx.fillStyle = "#c0a878";
        ctx.beginPath();
        ctx.ellipse(0, -5 * s, 5 * s, 1.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(-1 * s, -14 * s, 4 * s, 1 * s, -0.1, 0, Math.PI * 2);
        ctx.fill();
        // Wind erosion scratches
        ctx.globalAlpha = 0.2;
        ctx.strokeStyle = "#a08a60";
        ctx.lineWidth = 0.5 * s;
        for (let ei = 0; ei < 4; ei++) {
          const ey = -(8 + ei * 8) * s;
          ctx.beginPath();
          ctx.moveTo(-5 * s, ey);
          ctx.lineTo(5 * s, ey + 1 * s);
          ctx.stroke();
        }
        ctx.globalAlpha = 1;
      } else if (mapTheme === "winter") {
        // Snow cap on broken top
        ctx.fillStyle = "#e4ecf6";
        ctx.beginPath();
        ctx.moveTo(-5 * s, -40 * s);
        ctx.lineTo(-4 * s, -45 * s);
        ctx.lineTo(2 * s, -43 * s);
        ctx.lineTo(5 * s, -40 * s);
        ctx.lineTo(3 * s, -39 * s);
        ctx.closePath();
        ctx.fill();
        // Frost patches on body
        ctx.globalAlpha = 0.35;
        ctx.fillStyle = "#c8daea";
        ctx.beginPath();
        ctx.ellipse(-3 * s, -20 * s, 3 * s, 4 * s, -0.2, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(2 * s, -10 * s, 2.5 * s, 3 * s, 0.15, 0, Math.PI * 2);
        ctx.fill();
        // Icicles from carved bands
        ctx.globalAlpha = 0.5;
        for (let ii = 0; ii < 3; ii++) {
          const ibY = -5 * s - ii * 9 * s;
          const iceG = ctx.createLinearGradient(0, ibY, 0, ibY + 5 * s);
          iceG.addColorStop(0, "rgba(180,210,240,0.5)");
          iceG.addColorStop(1, "rgba(160,200,240,0)");
          ctx.fillStyle = iceG;
          ctx.beginPath();
          ctx.moveTo(-3 * s + ii * 2 * s, ibY);
          ctx.lineTo(-2.5 * s + ii * 2 * s, ibY + 5 * s);
          ctx.lineTo(-2 * s + ii * 2 * s, ibY);
          ctx.closePath();
          ctx.fill();
        }
        ctx.globalAlpha = 1;
      } else if (mapTheme === "volcanic") {
        // Ash deposits
        ctx.globalAlpha = 0.35;
        ctx.fillStyle = "rgba(50,40,35,0.5)";
        ctx.beginPath();
        ctx.ellipse(0, -6 * s, 5 * s, 2 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(-1 * s, -22 * s, 3.5 * s, 1.5 * s, -0.1, 0, Math.PI * 2);
        ctx.fill();
        // Ember glow cracks in stone
        ctx.globalAlpha = 0.5;
        ctx.strokeStyle = "#d86a20";
        ctx.lineWidth = 0.7 * s;
        ctx.beginPath();
        ctx.moveTo(-2 * s, -12 * s);
        ctx.lineTo(-1 * s, -18 * s);
        ctx.lineTo(1 * s, -22 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(2 * s, -8 * s);
        ctx.lineTo(3 * s, -14 * s);
        ctx.stroke();
        // Glow halo around cracks
        ctx.globalAlpha = 0.15;
        ctx.strokeStyle = "#ff8040";
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(-2 * s, -12 * s);
        ctx.lineTo(-1 * s, -18 * s);
        ctx.lineTo(1 * s, -22 * s);
        ctx.stroke();
        // Heat shimmer above top
        ctx.globalAlpha = 0.06;
        ctx.fillStyle = "#ff6020";
        ctx.beginPath();
        ctx.ellipse(0, -48 * s, 6 * s, 10 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.globalAlpha = 1;
      }

      ctx.restore();
      break;
    }

    case "idol_statue": {
      renderIdolStatue({
        ctx,
        mapTheme,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        time: decorTime,
      });
      break;
    }

    case "glowing_runes": {
      const rv = variant % 4;
      const cx = screenPos.x;
      const cy = screenPos.y;
      const t = decorTime;

      const runeHues = [270, 200, 140, 340];
      const baseHue = runeHues[rv];
      const pulse = 0.5 + Math.sin(t * 2.2) * 0.3;
      const slowPulse = 0.5 + Math.sin(t * 1.1) * 0.35;
      const fastFlicker = 0.7 + Math.sin(t * 5.5) * 0.15;
      const rotAngle = t * 0.3;

      // project a world-space circle onto isometric ground plane
      const isoCircleX = (ang: number, r: number) =>
        cx + Math.cos(ang) * r * ISO_COS;
      const isoCircleY = (ang: number, r: number) =>
        cy + Math.sin(ang) * r * ISO_SIN;

      // ── Shared ground glow ──
      const glowR = rv === 1 ? 28 * s : 24 * s;
      const groundGlow = ctx.createRadialGradient(cx, cy, 0, cx, cy, glowR);
      groundGlow.addColorStop(
        0,
        `hsla(${baseHue}, 80%, 55%, ${0.25 + pulse * 0.12})`
      );
      groundGlow.addColorStop(
        0.4,
        `hsla(${baseHue + 30}, 60%, 40%, ${0.12 + pulse * 0.06})`
      );
      groundGlow.addColorStop(1, "transparent");
      ctx.fillStyle = groundGlow;
      ctx.beginPath();
      ctx.ellipse(cx, cy, glowR, glowR * ISO_Y_RATIO, 0, 0, Math.PI * 2);
      ctx.fill();

      // ── Stone platform (isometric prism, square footprint) ──
      const slabSize = rv === 1 ? 22 * s : 18 * s;
      const slabH = 2.5 * s;
      drawIsometricPrism(
        ctx,
        cx,
        cy,
        slabSize,
        slabSize,
        slabH,
        "#504e46",
        "#3e3c34",
        "#45433a"
      );

      // Subtle edge crack details on top face
      ctx.strokeStyle = "rgba(0,0,0,0.15)";
      ctx.lineWidth = 0.5 * s;
      const crackSeed = decorX * 7 + decorY * 13;
      for (let ci = 0; ci < 3; ci++) {
        const ca = ((ci + crackSeed * 0.1) / 3) * Math.PI * 2 + crackSeed * 0.5;
        const cr = 4 * s;
        const cl = (4 + Math.sin(crackSeed + ci * 2.7) * 2) * s;
        ctx.beginPath();
        ctx.moveTo(
          cx + Math.cos(ca) * cr * ISO_COS,
          cy - slabH + Math.sin(ca) * cr * ISO_SIN
        );
        ctx.lineTo(
          cx + Math.cos(ca) * (cr + cl) * ISO_COS,
          cy - slabH + Math.sin(ca) * (cr + cl) * ISO_SIN
        );
        ctx.stroke();
      }

      if (rv === 0) {
        // ── VARIANT 0: Arcane Circle with Inscribed Pentagram ──
        const circR = 14 * s;
        const runeA = 0.55 + pulse * 0.35;

        // Outer circle on ground
        ctx.strokeStyle = `hsla(${baseHue}, 85%, 65%, ${runeA})`;
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy - slabH,
          circR * ISO_COS,
          circR * ISO_SIN,
          0,
          0,
          Math.PI * 2
        );
        ctx.stroke();

        // Inner circle
        ctx.strokeStyle = `hsla(${baseHue + 20}, 80%, 60%, ${runeA * 0.7})`;
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy - slabH,
          circR * 0.7 * ISO_COS,
          circR * 0.7 * ISO_SIN,
          0,
          0,
          Math.PI * 2
        );
        ctx.stroke();

        // Pentagram (5-pointed star inscribed in circle, rotates slowly)
        ctx.strokeStyle = `hsla(${baseHue}, 90%, 70%, ${runeA * 0.9})`;
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        for (let pi = 0; pi < 5; pi++) {
          const fromAngle = rotAngle + (pi / 5) * Math.PI * 2 - Math.PI / 2;
          const toAngle =
            rotAngle + (((pi + 2) % 5) / 5) * Math.PI * 2 - Math.PI / 2;
          ctx.moveTo(
            isoCircleX(fromAngle, circR),
            isoCircleY(fromAngle, circR) - slabH
          );
          ctx.lineTo(
            isoCircleX(toAngle, circR),
            isoCircleY(toAngle, circR) - slabH
          );
        }
        ctx.stroke();

        // Rune glyphs at each pentagram vertex
        setShadowBlur(ctx, 4 * s, `hsla(${baseHue}, 80%, 60%, 0.6)`);
        ctx.fillStyle = `hsla(${baseHue}, 90%, 75%, ${runeA})`;
        ctx.font = `${3.5 * s}px serif`;
        ctx.textAlign = "center";
        ctx.textBaseline = "middle";
        const glyphs = ["\u16A0", "\u16B1", "\u16C7", "\u16D2", "\u16DA"];
        for (let gi = 0; gi < 5; gi++) {
          const ga = rotAngle + (gi / 5) * Math.PI * 2 - Math.PI / 2;
          const gx = isoCircleX(ga, circR + 3 * s);
          const gy = isoCircleY(ga, circR + 3 * s) - slabH;
          ctx.fillText(glyphs[gi], gx, gy);
        }
        clearShadow(ctx);

        // Center eye / focal glyph
        setShadowBlur(ctx, 6 * s, `hsla(${baseHue}, 85%, 65%, 0.8)`);
        ctx.fillStyle = `hsla(${baseHue}, 90%, 80%, ${fastFlicker})`;
        ctx.beginPath();
        ctx.ellipse(cx, cy - slabH, 2.5 * s, 1.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        clearShadow(ctx);
      } else if (rv === 1) {
        // ── VARIANT 1: Standing Stones Circle with Central Pillar ──
        const stoneCount = 6;
        const ringR = 15 * s;

        // Connecting lines between stones on ground
        ctx.strokeStyle = `hsla(${baseHue}, 70%, 55%, ${0.2 + pulse * 0.15})`;
        ctx.lineWidth = 0.7 * s;
        for (let si = 0; si < stoneCount; si++) {
          const a1 = (si / stoneCount) * Math.PI * 2 - Math.PI / 2;
          const a2 =
            (((si + 1) % stoneCount) / stoneCount) * Math.PI * 2 - Math.PI / 2;
          ctx.beginPath();
          ctx.moveTo(isoCircleX(a1, ringR), isoCircleY(a1, ringR) - slabH);
          ctx.lineTo(isoCircleX(a2, ringR), isoCircleY(a2, ringR) - slabH);
          ctx.stroke();
        }

        // Standing stone pillars
        for (let si = 0; si < stoneCount; si++) {
          const sa = (si / stoneCount) * Math.PI * 2 - Math.PI / 2;
          const sx = isoCircleX(sa, ringR);
          const sy = isoCircleY(sa, ringR) - slabH;
          const stH = (6 + Math.sin(decorX + si * 2.3) * 2) * s;
          const stW = 3.5 * s;
          const shadeOff = si * 17;
          drawIsometricPrism(
            ctx,
            sx,
            sy,
            stW,
            stW,
            stH,
            `hsl(220, 5%, ${38 + (shadeOff % 8)}%)`,
            `hsl(220, 5%, ${28 + (shadeOff % 6)}%)`,
            `hsl(220, 5%, ${33 + (shadeOff % 7)}%)`
          );

          // Rune glyph etched on each stone face
          const glyphA = 0.5 + pulse * 0.4;
          setShadowBlur(
            ctx,
            3 * s,
            `hsla(${baseHue}, 80%, 60%, ${glyphA * 0.5})`
          );
          ctx.fillStyle = `hsla(${baseHue}, 80%, 65%, ${glyphA})`;
          ctx.font = `${2.5 * s}px serif`;
          ctx.textAlign = "center";
          ctx.textBaseline = "middle";
          const stoneGlyphs = [
            "\u16A0",
            "\u16B1",
            "\u16C1",
            "\u16D6",
            "\u16DA",
            "\u16BE",
          ];
          ctx.fillText(
            stoneGlyphs[si % stoneGlyphs.length],
            sx,
            sy - stH * 0.55
          );
          clearShadow(ctx);
        }

        // Central pillar
        const cpH = 10 * s;
        const cpW = 4 * s;
        drawIsometricPrism(
          ctx,
          cx,
          cy - slabH,
          cpW,
          cpW,
          cpH,
          "#5a5852",
          "#424038",
          "#4e4c44"
        );

        // Glowing orb atop central pillar
        const orbR = 2.5 * s;
        const orbY = cy - slabH - cpH - orbR * 0.3;
        setShadowBlur(ctx, 8 * s, `hsla(${baseHue}, 85%, 65%, 0.7)`);
        const orbGrad = ctx.createRadialGradient(cx, orbY, 0, cx, orbY, orbR);
        orbGrad.addColorStop(0, `hsla(${baseHue}, 90%, 90%, ${fastFlicker})`);
        orbGrad.addColorStop(
          0.5,
          `hsla(${baseHue}, 85%, 65%, ${fastFlicker * 0.7})`
        );
        orbGrad.addColorStop(1, "transparent");
        ctx.fillStyle = orbGrad;
        ctx.beginPath();
        ctx.arc(cx, orbY, orbR, 0, Math.PI * 2);
        ctx.fill();
        clearShadow(ctx);

        // Energy arcs from orb to stones
        ctx.strokeStyle = `hsla(${baseHue}, 75%, 60%, ${0.15 + slowPulse * 0.15})`;
        ctx.lineWidth = 0.6 * s;
        for (let si = 0; si < stoneCount; si++) {
          const sa = (si / stoneCount) * Math.PI * 2 - Math.PI / 2;
          const sx = isoCircleX(sa, ringR);
          const sy2 = isoCircleY(sa, ringR) - slabH;
          const stH = (6 + Math.sin(decorX + si * 2.3) * 2) * s;
          ctx.beginPath();
          ctx.moveTo(cx, orbY);
          ctx.quadraticCurveTo(
            (cx + sx) / 2,
            orbY + Math.sin(t * 1.5 + si) * 3 * s,
            sx,
            sy2 - stH * 0.5
          );
          ctx.stroke();
        }
      } else if (rv === 2) {
        // ── VARIANT 2: Concentric Warding Rings with Rotating Glyphs ──
        const rings = [12 * s, 8 * s, 4 * s];
        const ringAlphas = [0.4, 0.55, 0.75];

        // Three concentric ellipses, each rotating at different speed
        for (let ri = 0; ri < rings.length; ri++) {
          const rr = rings[ri];
          const rAlpha = ringAlphas[ri] * (0.7 + pulse * 0.3);
          const rRot = rotAngle * (1 + ri * 0.4) * (ri % 2 === 0 ? 1 : -1);

          ctx.strokeStyle = `hsla(${baseHue + ri * 15}, 80%, ${60 + ri * 5}%, ${rAlpha})`;
          ctx.lineWidth = (1 + ri * 0.3) * s;
          ctx.beginPath();
          ctx.ellipse(
            cx,
            cy - slabH,
            rr * ISO_COS,
            rr * ISO_SIN,
            0,
            0,
            Math.PI * 2
          );
          ctx.stroke();

          // Tick marks around each ring
          const tickCount = 8 + ri * 4;
          ctx.lineWidth = 0.5 * s;
          for (let ti = 0; ti < tickCount; ti++) {
            const ta = rRot + (ti / tickCount) * Math.PI * 2;
            const innerR = rr * 0.85;
            const outerR = rr;
            ctx.beginPath();
            ctx.moveTo(isoCircleX(ta, innerR), isoCircleY(ta, innerR) - slabH);
            ctx.lineTo(isoCircleX(ta, outerR), isoCircleY(ta, outerR) - slabH);
            ctx.stroke();
          }
        }

        // Rotating rune glyphs between outer and middle rings
        setShadowBlur(ctx, 4 * s, `hsla(${baseHue}, 80%, 60%, 0.5)`);
        ctx.fillStyle = `hsla(${baseHue}, 85%, 70%, ${0.5 + pulse * 0.4})`;
        ctx.font = `${2.8 * s}px serif`;
        ctx.textAlign = "center";
        ctx.textBaseline = "middle";
        const wardGlyphs = [
          "\u16A0",
          "\u16B7",
          "\u16C7",
          "\u16D2",
          "\u16DA",
          "\u16C1",
          "\u16B1",
          "\u16BE",
        ];
        for (let gi = 0; gi < 8; gi++) {
          const ga = -rotAngle * 0.6 + (gi / 8) * Math.PI * 2;
          const gr = 10 * s;
          ctx.fillText(
            wardGlyphs[gi],
            isoCircleX(ga, gr),
            isoCircleY(ga, gr) - slabH
          );
        }
        clearShadow(ctx);

        // Central ward sigil — cross within circle
        const sigA = 0.6 + fastFlicker * 0.3;
        ctx.strokeStyle = `hsla(${baseHue}, 90%, 75%, ${sigA})`;
        ctx.lineWidth = 1.2 * s;
        const innerR = 3 * s;
        ctx.beginPath();
        ctx.ellipse(
          cx,
          cy - slabH,
          innerR * ISO_COS,
          innerR * ISO_SIN,
          0,
          0,
          Math.PI * 2
        );
        ctx.stroke();

        // Cross arms
        for (let arm = 0; arm < 4; arm++) {
          const aa = rotAngle * 0.8 + (arm / 4) * Math.PI * 2;
          ctx.beginPath();
          ctx.moveTo(cx, cy - slabH);
          ctx.lineTo(
            isoCircleX(aa, innerR * 1.8),
            isoCircleY(aa, innerR * 1.8) - slabH
          );
          ctx.stroke();
        }
      } else {
        // ── VARIANT 3: Eldritch Sigil — Angular Rune Array ──
        const runeA = 0.5 + pulse * 0.4;
        const sigR = 13 * s;

        // Outer hexagonal boundary
        ctx.strokeStyle = `hsla(${baseHue}, 80%, 60%, ${runeA * 0.6})`;
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        for (let hi = 0; hi <= 6; hi++) {
          const ha = (hi / 6) * Math.PI * 2 - Math.PI / 2;
          const hx = isoCircleX(ha, sigR);
          const hy = isoCircleY(ha, sigR) - slabH;
          if (hi === 0) {
            ctx.moveTo(hx, hy);
          } else {
            ctx.lineTo(hx, hy);
          }
        }
        ctx.stroke();

        // Inner inverted triangle
        ctx.strokeStyle = `hsla(${baseHue}, 85%, 65%, ${runeA * 0.8})`;
        ctx.lineWidth = 0.9 * s;
        ctx.beginPath();
        for (let ti = 0; ti < 3; ti++) {
          const ta = rotAngle + (ti / 3) * Math.PI * 2 + Math.PI / 2;
          const tx = isoCircleX(ta, sigR * 0.7);
          const ty = isoCircleY(ta, sigR * 0.7) - slabH;
          if (ti === 0) {
            ctx.moveTo(tx, ty);
          } else {
            ctx.lineTo(tx, ty);
          }
        }
        ctx.closePath();
        ctx.stroke();

        // Outer triangle (counter-rotating)
        ctx.beginPath();
        for (let ti = 0; ti < 3; ti++) {
          const ta = -rotAngle + (ti / 3) * Math.PI * 2 - Math.PI / 2;
          const tx = isoCircleX(ta, sigR * 0.7);
          const ty = isoCircleY(ta, sigR * 0.7) - slabH;
          if (ti === 0) {
            ctx.moveTo(tx, ty);
          } else {
            ctx.lineTo(tx, ty);
          }
        }
        ctx.closePath();
        ctx.stroke();

        // Connecting spokes from center to hex vertices
        ctx.strokeStyle = `hsla(${baseHue}, 75%, 55%, ${runeA * 0.4})`;
        ctx.lineWidth = 0.6 * s;
        for (let hi = 0; hi < 6; hi++) {
          const ha = (hi / 6) * Math.PI * 2 - Math.PI / 2;
          ctx.beginPath();
          ctx.moveTo(cx, cy - slabH);
          ctx.lineTo(isoCircleX(ha, sigR), isoCircleY(ha, sigR) - slabH);
          ctx.stroke();
        }

        // Rune glyphs at hexagon vertices
        setShadowBlur(ctx, 3 * s, `hsla(${baseHue}, 80%, 60%, 0.5)`);
        ctx.fillStyle = `hsla(${baseHue}, 85%, 70%, ${runeA})`;
        ctx.font = `${2.5 * s}px serif`;
        ctx.textAlign = "center";
        ctx.textBaseline = "middle";
        const hexGlyphs = [
          "\u16A0",
          "\u16BE",
          "\u16C7",
          "\u16D2",
          "\u16DA",
          "\u16B1",
        ];
        for (let gi = 0; gi < 6; gi++) {
          const ga = (gi / 6) * Math.PI * 2 - Math.PI / 2;
          ctx.fillText(
            hexGlyphs[gi],
            isoCircleX(ga, sigR + 3 * s),
            isoCircleY(ga, sigR + 3 * s) - slabH
          );
        }
        clearShadow(ctx);

        // Pulsing center dot
        setShadowBlur(ctx, 6 * s, `hsla(${baseHue}, 90%, 65%, 0.8)`);
        ctx.fillStyle = `hsla(${baseHue}, 95%, 80%, ${fastFlicker})`;
        ctx.beginPath();
        ctx.arc(cx, cy - slabH, 2 * s, 0, Math.PI * 2);
        ctx.fill();
        clearShadow(ctx);
      }

      // ── Shared: Floating rune particles ──
      for (let p = 0; p < 6; p++) {
        const pPhase = (t + p * 0.55 + decorX * 0.3) % 2.5;
        const pAlpha = Math.sin((pPhase / 2.5) * Math.PI) * 0.55;
        const pAngle = t * 0.4 + (p * (Math.PI * 2)) / 6;
        const pDist = (6 + Math.sin(t * 0.8 + p * 1.7) * 5) * s;
        const px = isoCircleX(pAngle, pDist);
        const py = isoCircleY(pAngle, pDist) - slabH - pPhase * 6 * s;
        const pSize = (0.8 + Math.sin(t * 3 + p) * 0.3) * s;
        ctx.fillStyle = `hsla(${(baseHue + p * 25) % 360}, 85%, 75%, ${pAlpha})`;
        ctx.beginPath();
        ctx.arc(px, py, pSize, 0, Math.PI * 2);
        ctx.fill();
      }
      break;
    }

    case "hanging_cage": {
      const cageMetal = "#5a5a50";
      const cageRust = "#7a5a3a";
      const cageDark = "#3a3a30";

      // Shadow
      ctx.fillStyle = "rgba(0,0,0,0.2)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 3 * s,
        screenPos.y + 8 * s,
        12 * s,
        6 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Post
      ctx.fillStyle = "#4a3a2a";
      ctx.fillRect(screenPos.x - 2.5 * s, screenPos.y - 50 * s, 5 * s, 55 * s);
      ctx.fillStyle = "#5a4a3a";
      ctx.fillRect(screenPos.x - 2.5 * s, screenPos.y - 50 * s, 2 * s, 55 * s);

      // Arm extending out
      ctx.fillStyle = "#4a3a2a";
      ctx.fillRect(screenPos.x - 1 * s, screenPos.y - 50 * s, 16 * s, 3 * s);

      // Chain
      ctx.strokeStyle = cageMetal;
      ctx.lineWidth = 1.5 * s;
      const chainX = screenPos.x + 12 * s;
      ctx.beginPath();
      ctx.moveTo(chainX, screenPos.y - 48 * s);
      ctx.lineTo(chainX, screenPos.y - 32 * s);
      ctx.stroke();

      // Cage body
      const cageTop = screenPos.y - 32 * s;
      const cageBot = screenPos.y - 10 * s;
      const cageW = 8 * s;
      const cageSway = Math.sin(decorTime * 1.2) * 2 * s;

      ctx.save();
      ctx.translate(cageSway, 0);

      // Cage bars
      ctx.strokeStyle = cageRust;
      ctx.lineWidth = 1.2 * s;
      for (let b = 0; b < 6; b++) {
        const angle = (b / 6) * Math.PI * 2;
        const bx = chainX + Math.cos(angle) * cageW;
        const by = (cageTop + cageBot) / 2 + Math.sin(angle) * cageW * 0.4;
        ctx.beginPath();
        ctx.moveTo(chainX, cageTop);
        ctx.quadraticCurveTo(bx, by, chainX, cageBot);
        ctx.stroke();
      }

      // Horizontal ring
      ctx.strokeStyle = cageMetal;
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.ellipse(
        chainX,
        (cageTop + cageBot) / 2,
        cageW,
        cageW * 0.4,
        0,
        0,
        Math.PI * 2
      );
      ctx.stroke();

      // Top cap
      ctx.fillStyle = cageDark;
      ctx.beginPath();
      ctx.ellipse(chainX, cageTop, 3 * s, 1.5 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Bottom
      ctx.fillStyle = cageDark;
      ctx.beginPath();
      ctx.ellipse(chainX, cageBot, 4 * s, 2 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Skull inside cage
      ctx.fillStyle = "#d8d0c0";
      ctx.beginPath();
      ctx.arc(chainX, (cageTop + cageBot) / 2 + 2 * s, 3 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#2a2a20";
      ctx.beginPath();
      ctx.arc(
        chainX - 1 * s,
        (cageTop + cageBot) / 2 + 1.5 * s,
        0.8 * s,
        0,
        Math.PI * 2
      );
      ctx.arc(
        chainX + 1 * s,
        (cageTop + cageBot) / 2 + 1.5 * s,
        0.8 * s,
        0,
        Math.PI * 2
      );
      ctx.fill();

      ctx.restore();
      break;
    }

    case "poison_pool": {
      const poisonGreen = "#4caf50";
      const poisonDark = "#2e7d32";
      const poisonBright = "#76ff03";
      const poolSeed = (dec.x || 0) * 19.3;

      // Shadow/stain
      ctx.fillStyle = "rgba(30,80,30,0.25)";
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y + 3 * s,
        24 * s,
        12 * s,
        poolSeed - 10,
        0.14
      );
      ctx.fill();

      // Muddy rim
      ctx.fillStyle = "#3a3a28";
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        22 * s,
        11 * s,
        poolSeed + 3,
        0.12
      );
      ctx.fill();

      // Poison surface
      const poisonGrad = ctx.createRadialGradient(
        screenPos.x - 4 * s,
        screenPos.y - 2 * s,
        0,
        screenPos.x,
        screenPos.y,
        18 * s
      );
      poisonGrad.addColorStop(0, poisonBright);
      poisonGrad.addColorStop(0.4, poisonGreen);
      poisonGrad.addColorStop(1, poisonDark);
      ctx.fillStyle = poisonGrad;
      drawOrganicWaterShape(
        ctx,
        screenPos.x,
        screenPos.y,
        18 * s,
        9 * s,
        poolSeed,
        0.12
      );
      ctx.fill();

      // Animated bubbles
      for (let b = 0; b < 4; b++) {
        const bubblePhase = (decorTime * 1.5 + b * 0.7) % 2;
        if (bubblePhase < 1.2) {
          const bSize = (1.5 + bubblePhase * 1.5) * s;
          const bAlpha = 0.6 - bubblePhase * 0.4;
          const bx = screenPos.x + Math.cos(b * 1.7 + decorTime * 0.3) * 8 * s;
          const by =
            screenPos.y + Math.sin(b * 2.1) * 3 * s - bubblePhase * 3 * s;
          ctx.fillStyle = `rgba(118,255,3,${bAlpha})`;
          ctx.beginPath();
          ctx.arc(bx, by, bSize, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = `rgba(255,255,255,${bAlpha * 0.3})`;
          ctx.beginPath();
          ctx.arc(
            bx - bSize * 0.3,
            by - bSize * 0.3,
            bSize * 0.3,
            0,
            Math.PI * 2
          );
          ctx.fill();
        }
      }

      // Toxic vapors rising
      for (let v = 0; v < 3; v++) {
        const vPhase = (decorTime * 0.6 + v * 1.1) % 2.5;
        const vAlpha = Math.sin((vPhase / 2.5) * Math.PI) * 0.12;
        const vx = screenPos.x + Math.sin(decorTime * 0.5 + v * 2) * 8 * s;
        const vy = screenPos.y - 4 * s - vPhase * 10 * s;
        ctx.fillStyle = `rgba(76,175,80,${vAlpha})`;
        ctx.beginPath();
        ctx.ellipse(
          vx,
          vy,
          (3 + vPhase * 2) * s,
          (1.5 + vPhase) * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }

      // Dead plant matter at edges
      ctx.fillStyle = "#4a4a20";
      drawOrganicWaterShape(
        ctx,
        screenPos.x - 14 * s,
        screenPos.y + 2 * s,
        3 * s,
        1.5 * s,
        poolSeed + 20,
        0.18
      );
      ctx.fill();
      drawOrganicWaterShape(
        ctx,
        screenPos.x + 12 * s,
        screenPos.y - 1 * s,
        2.5 * s,
        1.2 * s,
        poolSeed + 30,
        0.18
      );
      ctx.fill();
      break;
    }

    case "skeleton_pile": {
      const pileWhite = "#e8e0d0";
      const pileShadow = "#c0b0a0";
      const pileDeep = "#2a2520";

      // Shadow
      ctx.fillStyle = "rgba(0,0,0,0.2)";
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x,
        screenPos.y + 4 * s,
        18 * s,
        9 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Bone pile base
      const boneSeeds = [
        { dx: -10, dy: 2, len: 6, rot: 0.3 },
        { dx: -4, dy: 3, len: 5, rot: -0.5 },
        { dx: 4, dy: 2, len: 7, rot: 0.8 },
        { dx: 10, dy: 1, len: 5, rot: -0.2 },
        { dx: -7, dy: 0, len: 4, rot: 1.2 },
        { dx: 6, dy: 0, len: 6, rot: -0.8 },
        { dx: -2, dy: -1, len: 8, rot: 0.1 },
        { dx: 8, dy: -1, len: 5, rot: -1.1 },
      ];
      boneSeeds.forEach((bone, idx) => {
        ctx.save();
        ctx.translate(screenPos.x + bone.dx * s, screenPos.y + bone.dy * s);
        ctx.rotate(bone.rot);
        ctx.fillStyle = idx % 2 === 0 ? pileWhite : pileShadow;
        ctx.fillRect(-bone.len * s, -0.8 * s, bone.len * 2 * s, 1.6 * s);
        ctx.beginPath();
        ctx.arc(-bone.len * s, 0, 1.5 * s, 0, Math.PI * 2);
        ctx.arc(bone.len * s, 0, 1.5 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
      });

      // Skulls on top
      const skulls = [
        { dx: -5, dy: -3, sz: 4 },
        { dx: 4, dy: -4, sz: 3.5 },
        { dx: -1, dy: -6, sz: 4.5 },
      ];
      skulls.forEach((sk) => {
        ctx.fillStyle = pileShadow;
        ctx.beginPath();
        ctx.arc(
          screenPos.x + sk.dx * s,
          screenPos.y + sk.dy * s,
          sk.sz * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = pileWhite;
        ctx.beginPath();
        ctx.arc(
          screenPos.x + sk.dx * s - 0.5 * s,
          screenPos.y + sk.dy * s - 0.5 * s,
          sk.sz * 0.85 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Eye sockets
        ctx.fillStyle = pileDeep;
        ctx.beginPath();
        ctx.arc(
          screenPos.x + sk.dx * s - 1.2 * s,
          screenPos.y + sk.dy * s - 0.5 * s,
          0.8 * s,
          0,
          Math.PI * 2
        );
        ctx.arc(
          screenPos.x + sk.dx * s + 1.2 * s,
          screenPos.y + sk.dy * s - 0.5 * s,
          0.8 * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // Ribcage sticking up
      ctx.strokeStyle = pileShadow;
      ctx.lineWidth = 1.5 * s;
      for (let r = 0; r < 3; r++) {
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 8 * s,
          screenPos.y - 2 * s - r * 3 * s,
          4 * s,
          2 * s,
          0.3,
          Math.PI * 0.2,
          Math.PI * 0.8
        );
        ctx.stroke();
      }
      break;
    }

    // === MISC DECORATIONS ===
    case "ruins": {
      // 7 ruin variants: 0-3 generic, 4 sunken_temple, 5 small pillar (frost), 6 grand temple (swamp)
      const ruinVariant = selectedMap === "sunken_temple" ? 4 : variant % 7;

      const ruinPalettes: Record<
        string,
        {
          stoneDark: string;
          stoneLight: string;
          stoneMid: string;
          stoneShadow: string;
          mossColor: string;
          vineColor: string;
          highlightColor: string;
          accentMoss: string;
        }
      > = {
        desert: {
          accentMoss: "#8a7a58",
          highlightColor: "#c0aa78",
          mossColor: "#8a7a58",
          stoneDark: "#5a4a38",
          stoneLight: "#a08a68",
          stoneMid: "#7a6a50",
          stoneShadow: "#3a3028",
          vineColor: "#7a6a48",
        },
        grassland: {
          accentMoss: "#4a6a5a",
          highlightColor: "#a0a090",
          mossColor: "#4a5d3a",
          stoneDark: "#3a3a3a",
          stoneLight: "#7a7a7a",
          stoneMid: "#5a5a5a",
          stoneShadow: "#2a2a2a",
          vineColor: "#2d5a2d",
        },
        swamp: {
          accentMoss: "#2a4a2a",
          highlightColor: "#6a8a6a",
          mossColor: "#2d4a2d",
          stoneDark: "#2a3a28",
          stoneLight: "#5a7a58",
          stoneMid: "#3a5a38",
          stoneShadow: "#1a2a18",
          vineColor: "#1a3a1a",
        },
        volcanic: {
          accentMoss: "#4a2a1a",
          highlightColor: "#d86a30",
          mossColor: "#5a3a2a",
          stoneDark: "#3a2828",
          stoneLight: "#6a4a4a",
          stoneMid: "#4a3838",
          stoneShadow: "#2a1818",
          vineColor: "#5a2a1a",
        },
        winter: {
          accentMoss: "#6a8a9a",
          highlightColor: "#b8d4e8",
          mossColor: "#8aaa9a",
          stoneDark: "#4a5058",
          stoneLight: "#8a9098",
          stoneMid: "#6a7078",
          stoneShadow: "#2a3038",
          vineColor: "#7a9aaa",
        },
      };
      const rp = ruinPalettes[mapTheme] ?? ruinPalettes.grassland;
      const { stoneDark } = rp;
      const { stoneLight } = rp;
      const { stoneMid } = rp;
      const { stoneShadow } = rp;
      const { mossColor } = rp;
      const { vineColor } = rp;
      const { highlightColor } = rp;
      const { accentMoss } = rp;

      // Ground shadow for all variants
      drawDirectionalShadow(
        ctx,
        screenPos.x,
        screenPos.y + 4 * s,
        s,
        32 * s,
        16 * s,
        40 * s,
        0.28
      );

      if (ruinVariant === 0) {
        // VARIANT 0: Broken column ruins — tall column, wall fragment, rubble field

        // === HEXAGONAL BASE ===
        const hexR = 24 * s;
        const hexH = 5 * s;
        const hexY = screenPos.y + 2 * s;
        const hexPts: { x: number; y: number }[] = [];
        for (let i = 0; i < 6; i++) {
          const ang = (Math.PI / 3) * i - Math.PI / 2;
          hexPts.push({
            x: screenPos.x + Math.cos(ang) * hexR,
            y: hexY + Math.sin(ang) * hexR * 0.5,
          });
        }
        const hexTopGrad = ctx.createLinearGradient(
          screenPos.x - hexR,
          hexY,
          screenPos.x + hexR,
          hexY
        );
        hexTopGrad.addColorStop(0, stoneMid);
        hexTopGrad.addColorStop(0.3, stoneLight);
        hexTopGrad.addColorStop(0.7, stoneMid);
        hexTopGrad.addColorStop(1, stoneDark);
        ctx.fillStyle = hexTopGrad;
        ctx.beginPath();
        ctx.moveTo(hexPts[0].x, hexPts[0].y);
        for (let i = 1; i < 6; i++) {
          ctx.lineTo(hexPts[i].x, hexPts[i].y);
        }
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.4 * s;
        for (let i = 0; i < 6; i++) {
          ctx.beginPath();
          ctx.moveTo(screenPos.x, hexY);
          ctx.lineTo(hexPts[i].x, hexPts[i].y);
          ctx.stroke();
        }
        for (let i = 2; i <= 4; i++) {
          const ni = (i + 1) % 6;
          ctx.fillStyle = i === 3 ? stoneShadow : stoneDark;
          ctx.beginPath();
          ctx.moveTo(hexPts[i].x, hexPts[i].y);
          ctx.lineTo(hexPts[ni].x, hexPts[ni].y);
          ctx.lineTo(hexPts[ni].x, hexPts[ni].y + hexH);
          ctx.lineTo(hexPts[i].x, hexPts[i].y + hexH);
          ctx.closePath();
          ctx.fill();
        }

        // === CRUMBLING WALL (back-left) ===
        const wX = screenPos.x - 24 * s;
        const wTopY = screenPos.y - 36 * s;
        const wW = 28 * s;
        const wThick = 5 * s;
        ctx.fillStyle = stoneShadow;
        ctx.beginPath();
        ctx.moveTo(wX - wThick, screenPos.y + 2 * s);
        ctx.lineTo(wX - wThick, wTopY + 8 * s);
        ctx.lineTo(wX - wThick + 2 * s, wTopY + 4 * s);
        ctx.lineTo(wX, wTopY + 2 * s);
        ctx.lineTo(wX, screenPos.y - 2 * s);
        ctx.closePath();
        ctx.fill();
        const wFG = ctx.createLinearGradient(wX, wTopY, wX + wW, screenPos.y);
        wFG.addColorStop(0, stoneMid);
        wFG.addColorStop(0.4, stoneLight);
        wFG.addColorStop(1, stoneDark);
        ctx.fillStyle = wFG;
        ctx.beginPath();
        ctx.moveTo(wX, screenPos.y - 2 * s);
        ctx.lineTo(wX, wTopY + 2 * s);
        ctx.lineTo(wX + 6 * s, wTopY);
        ctx.lineTo(wX + 14 * s, wTopY + 6 * s);
        ctx.lineTo(wX + 20 * s, wTopY + 2 * s);
        ctx.lineTo(wX + wW, wTopY + 8 * s);
        ctx.lineTo(wX + wW, screenPos.y + 1 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(wX - wThick, wTopY + 8 * s);
        ctx.lineTo(wX, wTopY + 2 * s);
        ctx.lineTo(wX + 6 * s, wTopY);
        ctx.lineTo(wX + 14 * s, wTopY + 6 * s);
        ctx.lineTo(wX + 20 * s, wTopY + 2 * s);
        ctx.lineTo(wX + wW, wTopY + 8 * s);
        ctx.lineTo(wX + wW - wThick, wTopY + 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.5 * s;
        for (let row = 0; row < 5; row++) {
          const rowY = wTopY + 12 * s + row * 6 * s;
          if (rowY < screenPos.y) {
            ctx.beginPath();
            ctx.moveTo(wX + 2 * s, rowY);
            ctx.lineTo(wX + wW - 2 * s, rowY + 1 * s);
            ctx.stroke();
          }
        }

        // === MAIN TALL COLUMN ===
        const colR = 7 * s;
        const colH = 48 * s;
        const colX = screenPos.x + 2 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          colX,
          screenPos.y + 2 * s,
          colR + 3 * s,
          (colR + 3 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          colX,
          screenPos.y,
          colR,
          colR * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(colX + colR, screenPos.y - colH + 10 * s);
        ctx.ellipse(
          colX,
          screenPos.y - colH + 10 * s,
          colR,
          colR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const cGrad = ctx.createLinearGradient(
          colX - colR,
          screenPos.y,
          colX + colR,
          screenPos.y
        );
        cGrad.addColorStop(0, stoneMid);
        cGrad.addColorStop(0.25, stoneLight);
        cGrad.addColorStop(0.6, stoneMid);
        cGrad.addColorStop(1, stoneDark);
        ctx.fillStyle = cGrad;
        ctx.beginPath();
        ctx.ellipse(colX, screenPos.y, colR, colR * 0.5, 0, 0, Math.PI);
        ctx.lineTo(colX - colR, screenPos.y - colH + 10 * s);
        ctx.ellipse(
          colX,
          screenPos.y - colH + 10 * s,
          colR,
          colR * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(colX - colR, screenPos.y - colH + 10 * s);
        ctx.lineTo(colX - colR + 2 * s, screenPos.y - colH + 3 * s);
        ctx.lineTo(colX - 2 * s, screenPos.y - colH + 8 * s);
        ctx.lineTo(colX + 1 * s, screenPos.y - colH);
        ctx.lineTo(colX + colR, screenPos.y - colH + 10 * s);
        ctx.ellipse(
          colX,
          screenPos.y - colH + 10 * s,
          colR,
          colR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.7 * s;
        for (let fl = 0; fl < 5; fl++) {
          const flA = (fl / 5) * Math.PI - Math.PI / 2;
          const flX = colX + Math.cos(flA) * colR * 0.85;
          ctx.beginPath();
          ctx.moveTo(flX, screenPos.y - 1 * s);
          ctx.lineTo(flX, screenPos.y - colH + 13 * s);
          ctx.stroke();
        }
        ctx.strokeStyle = "rgba(255,255,255,0.12)";
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.moveTo(colX - colR * 0.4, screenPos.y - 2 * s);
        ctx.lineTo(colX - colR * 0.4, screenPos.y - colH + 14 * s);
        ctx.stroke();

        // === SECONDARY SHORT COLUMN (right) ===
        const col2X = screenPos.x + 22 * s;
        const col2R = 5 * s;
        const col2H = 22 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          col2X,
          screenPos.y + 4 * s,
          col2R + 2 * s,
          (col2R + 2 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          col2X,
          screenPos.y + 1 * s,
          col2R,
          col2R * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(col2X + col2R, screenPos.y - col2H + 8 * s);
        ctx.ellipse(
          col2X,
          screenPos.y - col2H + 8 * s,
          col2R,
          col2R * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const c2G = ctx.createLinearGradient(
          col2X - col2R,
          0,
          col2X + col2R,
          0
        );
        c2G.addColorStop(0, stoneMid);
        c2G.addColorStop(0.3, stoneLight);
        c2G.addColorStop(1, stoneDark);
        ctx.fillStyle = c2G;
        ctx.beginPath();
        ctx.ellipse(
          col2X,
          screenPos.y + 1 * s,
          col2R,
          col2R * 0.5,
          0,
          0,
          Math.PI
        );
        ctx.lineTo(col2X - col2R, screenPos.y - col2H + 8 * s);
        ctx.ellipse(
          col2X,
          screenPos.y - col2H + 8 * s,
          col2R,
          col2R * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(col2X - col2R, screenPos.y - col2H + 8 * s);
        ctx.lineTo(col2X - col2R + 2 * s, screenPos.y - col2H + 3 * s);
        ctx.lineTo(col2X + col2R, screenPos.y - col2H + 8 * s);
        ctx.ellipse(
          col2X,
          screenPos.y - col2H + 8 * s,
          col2R,
          col2R * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();

        // === FALLEN COLUMN SEGMENT ===
        const fcX = screenPos.x - 6 * s;
        const fcY = screenPos.y + 11 * s;
        const fcR = 4.5 * s;
        const fcLen = 16 * s;
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          fcX + fcLen * 0.65,
          fcY - fcLen * 0.32,
          fcR,
          fcR * 0.5,
          0.6,
          0,
          Math.PI * 2
        );
        ctx.fill();
        const fcG = ctx.createLinearGradient(fcX, fcY - fcR, fcX, fcY + fcR);
        fcG.addColorStop(0, stoneLight);
        fcG.addColorStop(0.5, stoneMid);
        fcG.addColorStop(1, stoneDark);
        ctx.fillStyle = fcG;
        ctx.beginPath();
        ctx.moveTo(fcX, fcY - fcR * 0.5);
        ctx.lineTo(fcX + fcLen * 0.65, fcY - fcLen * 0.32 - fcR * 0.5);
        ctx.ellipse(
          fcX + fcLen * 0.65,
          fcY - fcLen * 0.32,
          fcR,
          fcR * 0.5,
          0.6,
          -Math.PI / 2,
          Math.PI / 2
        );
        ctx.lineTo(fcX, fcY + fcR * 0.5);
        ctx.ellipse(
          fcX,
          fcY,
          fcR,
          fcR * 0.5,
          0,
          Math.PI / 2,
          -Math.PI / 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(fcX, fcY, fcR, fcR * 0.45, 0, 0, Math.PI * 2);
        ctx.fill();

        // === ARCH FRAGMENT (right-back) ===
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 26 * s, screenPos.y);
        ctx.lineTo(screenPos.x + 24 * s, screenPos.y - 16 * s);
        ctx.lineTo(screenPos.x + 28 * s, screenPos.y - 20 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y - 14 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y + 3 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 34 * s, screenPos.y + 3 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y - 14 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 10 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y + 6 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 24 * s, screenPos.y - 16 * s);
        ctx.lineTo(screenPos.x + 28 * s, screenPos.y - 20 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y - 14 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 10 * s);
        ctx.closePath();
        ctx.fill();

        // === 3D RUBBLE FIELD ===
        [
          { sz: 5, x: -34, y: 10 },
          { sz: 4, x: -20, y: 14 },
          { sz: 3.5, x: -12, y: 15 },
          { sz: 4.5, x: 10, y: 14 },
          { sz: 3.5, x: 20, y: 12 },
          { sz: 4, x: 32, y: 9 },
          { sz: 3, x: -28, y: 12 },
          { sz: 3, x: 14, y: 8 },
          { sz: 3, x: 36, y: 5 },
          { sz: 2.5, x: -4, y: 17 },
          { sz: 3, x: 6, y: 18 },
          { sz: 3.5, x: -38, y: 5 },
        ].forEach((rb, idx) => {
          const rx = screenPos.x + rb.x * s;
          const ry = screenPos.y + rb.y * s;
          const rSz = rb.sz * s;
          ctx.fillStyle =
            idx % 3 === 0 ? stoneLight : idx % 3 === 1 ? stoneMid : stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx, ry - rSz * 0.8);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx - rSz * 0.7, ry - rSz * 0.2);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.8, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz * 0.2, ry + rSz * 0.6);
          ctx.closePath();
          ctx.fill();
        });
        ctx.fillStyle = stoneShadow;
        [
          [-30, 15],
          [-16, 17],
          [-2, 19],
          [8, 18],
          [18, 16],
          [28, 12],
          [36, 8],
          [-24, 18],
        ].forEach(([px, py], i) => {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + px * s,
            screenPos.y + py * s,
            (1.5 + (i % 3) * 0.5) * s,
            (0.8 + (i % 2) * 0.3) * s,
            i * 0.6,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });

        // === FLOOR TILES ===
        [
          { d: 3.5, w: 7, x: -16, y: 5 },
          { d: 3, w: 6, x: 12, y: 6 },
          { d: 2.5, w: 5, x: -6, y: 3 },
          { d: 2.5, w: 5, x: 20, y: 3 },
        ].forEach((t) => {
          const tx = screenPos.x + t.x * s;
          const ty = screenPos.y + t.y * s;
          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(tx, ty - t.d * s);
          ctx.lineTo(tx + t.w * s, ty);
          ctx.lineTo(tx, ty + t.d * s);
          ctx.lineTo(tx - t.w * s, ty);
          ctx.closePath();
          ctx.fill();
          ctx.strokeStyle = stoneShadow;
          ctx.lineWidth = 0.3 * s;
          ctx.stroke();
        });

        // === MOSS ===
        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.6;
        ctx.beginPath();
        ctx.ellipse(
          colX - 2 * s,
          screenPos.y - 18 * s,
          4 * s,
          2 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          colX + 4 * s,
          screenPos.y - 6 * s,
          3.5 * s,
          1.8 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          wX + 10 * s,
          screenPos.y - 8 * s,
          4 * s,
          2 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 30 * s,
          screenPos.y - 2 * s,
          3 * s,
          1.5 * s,
          -0.4,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;

        // === CRACKS ===
        ctx.strokeStyle = "#2a2a1a";
        ctx.lineWidth = 0.7 * s;
        ctx.beginPath();
        ctx.moveTo(wX + 12 * s, wTopY + 8 * s);
        ctx.lineTo(wX + 14 * s, screenPos.y - 16 * s);
        ctx.lineTo(wX + 12 * s, screenPos.y - 8 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 6 * s, hexY - 3 * s);
        ctx.lineTo(screenPos.x - 3 * s, hexY + 4 * s);
        ctx.stroke();
      } else if (ruinVariant === 1) {
        // VARIANT 1: Grand crumbling archway with thick pillars

        // === LEFT PILLAR (tall, 3D cylinder) ===
        const lpX = screenPos.x - 18 * s;
        const lpR = 7 * s;
        const lpH = 46 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          lpX,
          screenPos.y + 3 * s,
          lpR + 2 * s,
          (lpR + 2 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          lpX,
          screenPos.y + 1 * s,
          lpR,
          lpR * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(lpX + lpR, screenPos.y - lpH + 10 * s);
        ctx.ellipse(
          lpX,
          screenPos.y - lpH + 10 * s,
          lpR,
          lpR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const lpG = ctx.createLinearGradient(
          lpX - lpR,
          screenPos.y,
          lpX + lpR,
          screenPos.y
        );
        lpG.addColorStop(0, stoneMid);
        lpG.addColorStop(0.25, stoneLight);
        lpG.addColorStop(0.6, stoneMid);
        lpG.addColorStop(1, stoneDark);
        ctx.fillStyle = lpG;
        ctx.beginPath();
        ctx.ellipse(lpX, screenPos.y + 1 * s, lpR, lpR * 0.5, 0, 0, Math.PI);
        ctx.lineTo(lpX - lpR, screenPos.y - lpH + 10 * s);
        ctx.ellipse(
          lpX,
          screenPos.y - lpH + 10 * s,
          lpR,
          lpR * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.ellipse(
          lpX,
          screenPos.y - lpH + 6 * s,
          lpR + 2 * s,
          (lpR + 2 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.6 * s;
        for (let fl = 0; fl < 4; fl++) {
          const flA = (fl / 4) * Math.PI - Math.PI / 2;
          const flX = lpX + Math.cos(flA) * lpR * 0.85;
          ctx.beginPath();
          ctx.moveTo(flX, screenPos.y);
          ctx.lineTo(flX, screenPos.y - lpH + 13 * s);
          ctx.stroke();
        }

        // === RIGHT PILLAR (broken shorter) ===
        const rpX = screenPos.x + 18 * s;
        const rpR = 7 * s;
        const rpH = 28 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          rpX,
          screenPos.y + 3 * s,
          rpR + 2 * s,
          (rpR + 2 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          rpX,
          screenPos.y + 1 * s,
          rpR,
          rpR * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(rpX + rpR, screenPos.y - rpH + 8 * s);
        ctx.ellipse(
          rpX,
          screenPos.y - rpH + 8 * s,
          rpR,
          rpR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const rpG = ctx.createLinearGradient(
          rpX - rpR,
          screenPos.y,
          rpX + rpR,
          screenPos.y
        );
        rpG.addColorStop(0, stoneMid);
        rpG.addColorStop(0.25, stoneLight);
        rpG.addColorStop(0.6, stoneMid);
        rpG.addColorStop(1, stoneDark);
        ctx.fillStyle = rpG;
        ctx.beginPath();
        ctx.ellipse(rpX, screenPos.y + 1 * s, rpR, rpR * 0.5, 0, 0, Math.PI);
        ctx.lineTo(rpX - rpR, screenPos.y - rpH + 8 * s);
        ctx.ellipse(
          rpX,
          screenPos.y - rpH + 8 * s,
          rpR,
          rpR * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(rpX - rpR, screenPos.y - rpH + 8 * s);
        ctx.lineTo(rpX - rpR + 2 * s, screenPos.y - rpH + 3 * s);
        ctx.lineTo(rpX + 1 * s, screenPos.y - rpH + 6 * s);
        ctx.lineTo(rpX + rpR, screenPos.y - rpH + 8 * s);
        ctx.ellipse(
          rpX,
          screenPos.y - rpH + 8 * s,
          rpR,
          rpR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.6 * s;
        for (let fl = 0; fl < 4; fl++) {
          const flA = (fl / 4) * Math.PI - Math.PI / 2;
          const flX = rpX + Math.cos(flA) * rpR * 0.85;
          ctx.beginPath();
          ctx.moveTo(flX, screenPos.y);
          ctx.lineTo(flX, screenPos.y - rpH + 10 * s);
          ctx.stroke();
        }

        // === PARTIAL ARCH (thick, 3D) ===
        ctx.strokeStyle = stoneDark;
        ctx.lineWidth = 8 * s;
        ctx.lineCap = "butt";
        ctx.beginPath();
        ctx.arc(
          screenPos.x,
          screenPos.y - 22 * s,
          20 * s,
          Math.PI * 1.05,
          Math.PI * 1.55
        );
        ctx.stroke();
        ctx.strokeStyle = stoneLight;
        ctx.lineWidth = 6 * s;
        ctx.beginPath();
        ctx.arc(
          screenPos.x,
          screenPos.y - 22 * s,
          20 * s,
          Math.PI * 1.05,
          Math.PI * 1.55
        );
        ctx.stroke();
        ctx.strokeStyle = stoneMid;
        ctx.lineWidth = 4 * s;
        ctx.beginPath();
        ctx.arc(
          screenPos.x,
          screenPos.y - 22 * s,
          20 * s,
          Math.PI * 1.05,
          Math.PI * 1.55
        );
        ctx.stroke();

        // === LINTEL FRAGMENT on left pillar ===
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.moveTo(lpX - lpR - 2 * s, screenPos.y - lpH + 4 * s);
        ctx.lineTo(lpX + lpR + 6 * s, screenPos.y - lpH + 2 * s);
        ctx.lineTo(lpX + lpR + 6 * s, screenPos.y - lpH + 8 * s);
        ctx.lineTo(lpX - lpR - 2 * s, screenPos.y - lpH + 10 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(lpX - lpR - 2 * s, screenPos.y - lpH + 4 * s);
        ctx.lineTo(lpX + lpR + 6 * s, screenPos.y - lpH + 2 * s);
        ctx.lineTo(lpX + lpR + 4 * s, screenPos.y - lpH);
        ctx.lineTo(lpX - lpR - 4 * s, screenPos.y - lpH + 2 * s);
        ctx.closePath();
        ctx.fill();

        // === KEYSTONE on ground ===
        const ksX = screenPos.x + 4 * s;
        const ksY = screenPos.y + 6 * s;
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(ksX, ksY - 5 * s);
        ctx.lineTo(ksX + 8 * s, ksY - 2 * s);
        ctx.lineTo(ksX + 6 * s, ksY + 4 * s);
        ctx.lineTo(ksX - 2 * s, ksY + 2 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(ksX + 8 * s, ksY - 2 * s);
        ctx.lineTo(ksX + 6 * s, ksY + 4 * s);
        ctx.lineTo(ksX + 4 * s, ksY + 2 * s);
        ctx.lineTo(ksX + 6 * s, ksY - 3 * s);
        ctx.closePath();
        ctx.fill();

        // === 3D RUBBLE FIELD ===
        [
          { sz: 4.5, x: -32, y: 10 },
          { sz: 3.5, x: -10, y: 14 },
          { sz: 4, x: 6, y: 15 },
          { sz: 3.5, x: 26, y: 12 },
          { sz: 3, x: -24, y: 13 },
          { sz: 3, x: 14, y: 16 },
          { sz: 4, x: 30, y: 8 },
          { sz: 2.5, x: -16, y: 16 },
          { sz: 3, x: 34, y: 4 },
          { sz: 3, x: -36, y: 6 },
        ].forEach((rb, idx) => {
          const rx = screenPos.x + rb.x * s;
          const ry = screenPos.y + rb.y * s;
          const rSz = rb.sz * s;
          ctx.fillStyle =
            idx % 3 === 0 ? stoneLight : idx % 3 === 1 ? stoneMid : stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx, ry - rSz * 0.8);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx - rSz * 0.7, ry - rSz * 0.2);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.8, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz * 0.2, ry + rSz * 0.6);
          ctx.closePath();
          ctx.fill();
        });
        ctx.fillStyle = stoneShadow;
        [
          [-28, 15],
          [-14, 18],
          [0, 19],
          [12, 17],
          [22, 15],
          [32, 11],
          [-8, 12],
          [18, 8],
        ].forEach(([px, py], i) => {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + px * s,
            screenPos.y + py * s,
            (1.5 + (i % 3) * 0.5) * s,
            (0.8 + (i % 2) * 0.3) * s,
            i * 0.6,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });

        // === FLOOR TILES ===
        [
          { d: 3, w: 6, x: -8, y: 4 },
          { d: 2.5, w: 5, x: 10, y: 3 },
          { d: 3.5, w: 7, x: -20, y: 6 },
        ].forEach((t) => {
          const tx = screenPos.x + t.x * s;
          const ty = screenPos.y + t.y * s;
          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(tx, ty - t.d * s);
          ctx.lineTo(tx + t.w * s, ty);
          ctx.lineTo(tx, ty + t.d * s);
          ctx.lineTo(tx - t.w * s, ty);
          ctx.closePath();
          ctx.fill();
          ctx.strokeStyle = stoneShadow;
          ctx.lineWidth = 0.3 * s;
          ctx.stroke();
        });

        // === MOSS ===
        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.6;
        ctx.beginPath();
        ctx.ellipse(
          lpX + 2 * s,
          screenPos.y - 18 * s,
          4 * s,
          2 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          rpX - 2 * s,
          screenPos.y - 10 * s,
          3.5 * s,
          1.8 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 2 * s,
          screenPos.y - 30 * s,
          3 * s,
          1.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 30 * s,
          screenPos.y + 4 * s,
          4 * s,
          2 * s,
          0.4,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;
      } else if (ruinVariant === 2) {
        // VARIANT 2: L-shaped collapsed wall with vegetation

        // === BACK WALL SECTION ===
        const w1X = screenPos.x - 28 * s;
        const w1TopY = screenPos.y - 34 * s;
        const w1W = 36 * s;
        const w1Th = 5 * s;
        ctx.fillStyle = stoneShadow;
        ctx.beginPath();
        ctx.moveTo(w1X - w1Th, screenPos.y + 2 * s);
        ctx.lineTo(w1X - w1Th, w1TopY + 8 * s);
        ctx.lineTo(w1X - w1Th + 2 * s, w1TopY + 4 * s);
        ctx.lineTo(w1X, w1TopY + 2 * s);
        ctx.lineTo(w1X, screenPos.y - 2 * s);
        ctx.closePath();
        ctx.fill();
        const w1G = ctx.createLinearGradient(
          w1X,
          w1TopY,
          w1X + w1W,
          screenPos.y
        );
        w1G.addColorStop(0, stoneMid);
        w1G.addColorStop(0.4, stoneLight);
        w1G.addColorStop(1, stoneDark);
        ctx.fillStyle = w1G;
        ctx.beginPath();
        ctx.moveTo(w1X, screenPos.y - 2 * s);
        ctx.lineTo(w1X, w1TopY + 2 * s);
        ctx.lineTo(w1X + 8 * s, w1TopY);
        ctx.lineTo(w1X + 16 * s, w1TopY + 6 * s);
        ctx.lineTo(w1X + 24 * s, w1TopY + 2 * s);
        ctx.lineTo(w1X + w1W, w1TopY + 10 * s);
        ctx.lineTo(w1X + w1W, screenPos.y + 1 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(w1X - w1Th, w1TopY + 8 * s);
        ctx.lineTo(w1X, w1TopY + 2 * s);
        ctx.lineTo(w1X + 8 * s, w1TopY);
        ctx.lineTo(w1X + 16 * s, w1TopY + 6 * s);
        ctx.lineTo(w1X + 24 * s, w1TopY + 2 * s);
        ctx.lineTo(w1X + w1W, w1TopY + 10 * s);
        ctx.lineTo(w1X + w1W - w1Th, w1TopY + 7 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.5 * s;
        for (let row = 0; row < 5; row++) {
          const rowY = w1TopY + 14 * s + row * 5 * s;
          if (rowY < screenPos.y) {
            ctx.beginPath();
            ctx.moveTo(w1X + 2 * s, rowY);
            ctx.lineTo(w1X + w1W - 2 * s, rowY + 1 * s);
            ctx.stroke();
          }
        }

        // === PERPENDICULAR WALL (right side) ===
        const w2X = screenPos.x + 10 * s;
        const w2TopY = screenPos.y - 24 * s;
        const w2W = 22 * s;
        const w2Th = 5 * s;
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(w2X, screenPos.y + 2 * s);
        ctx.lineTo(w2X, w2TopY + 4 * s);
        ctx.lineTo(w2X + 5 * s, w2TopY);
        ctx.lineTo(w2X + 12 * s, w2TopY + 6 * s);
        ctx.lineTo(w2X + w2W, w2TopY + 3 * s);
        ctx.lineTo(w2X + w2W, screenPos.y + 4 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.moveTo(w2X + w2W, screenPos.y + 4 * s);
        ctx.lineTo(w2X + w2W, w2TopY + 3 * s);
        ctx.lineTo(w2X + w2W + w2Th, w2TopY + 6 * s);
        ctx.lineTo(w2X + w2W + w2Th, screenPos.y + 7 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(w2X, w2TopY + 4 * s);
        ctx.lineTo(w2X + 5 * s, w2TopY);
        ctx.lineTo(w2X + 12 * s, w2TopY + 6 * s);
        ctx.lineTo(w2X + w2W, w2TopY + 3 * s);
        ctx.lineTo(w2X + w2W + w2Th, w2TopY + 6 * s);
        ctx.closePath();
        ctx.fill();

        // === COLUMN STUMP ===
        const csX = screenPos.x - 6 * s;
        const csR = 5 * s;
        const csH = 12 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          csX,
          screenPos.y + 3 * s,
          csR + 2 * s,
          (csR + 2 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          csX,
          screenPos.y + 1 * s,
          csR,
          csR * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(csX + csR, screenPos.y - csH + 6 * s);
        ctx.ellipse(
          csX,
          screenPos.y - csH + 6 * s,
          csR,
          csR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const csG = ctx.createLinearGradient(csX - csR, 0, csX + csR, 0);
        csG.addColorStop(0, stoneMid);
        csG.addColorStop(0.3, stoneLight);
        csG.addColorStop(1, stoneDark);
        ctx.fillStyle = csG;
        ctx.beginPath();
        ctx.ellipse(csX, screenPos.y + 1 * s, csR, csR * 0.5, 0, 0, Math.PI);
        ctx.lineTo(csX - csR, screenPos.y - csH + 6 * s);
        ctx.ellipse(
          csX,
          screenPos.y - csH + 6 * s,
          csR,
          csR * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(csX - csR, screenPos.y - csH + 6 * s);
        ctx.lineTo(csX - 1 * s, screenPos.y - csH + 2 * s);
        ctx.lineTo(csX + csR, screenPos.y - csH + 6 * s);
        ctx.ellipse(
          csX,
          screenPos.y - csH + 6 * s,
          csR,
          csR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();

        // === VEGETATION ===
        ctx.fillStyle = "#3d5c2a";
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 10 * s, screenPos.y - 16 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 15 * s,
          screenPos.y - 32 * s,
          screenPos.x - 8 * s,
          screenPos.y - 30 * s
        );
        ctx.quadraticCurveTo(
          screenPos.x - 5 * s,
          screenPos.y - 22 * s,
          screenPos.x - 10 * s,
          screenPos.y - 16 * s
        );
        ctx.fill();
        ctx.fillStyle = "#4a7a35";
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 4 * s, screenPos.y - 20 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 1 * s,
          screenPos.y - 34 * s,
          screenPos.x + 4 * s,
          screenPos.y - 27 * s
        );
        ctx.quadraticCurveTo(
          screenPos.x + 1 * s,
          screenPos.y - 22 * s,
          screenPos.x - 4 * s,
          screenPos.y - 20 * s
        );
        ctx.fill();
        ctx.fillStyle = "#5a8a42";
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 2 * s, screenPos.y - 18 * s);
        ctx.quadraticCurveTo(
          screenPos.x + 6 * s,
          screenPos.y - 28 * s,
          screenPos.x + 8 * s,
          screenPos.y - 22 * s
        );
        ctx.quadraticCurveTo(
          screenPos.x + 5 * s,
          screenPos.y - 19 * s,
          screenPos.x + 2 * s,
          screenPos.y - 18 * s
        );
        ctx.fill();

        // === IVY VINES ===
        ctx.strokeStyle = vineColor;
        ctx.lineWidth = 1.5 * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(w1X + 10 * s, w1TopY + 6 * s);
        ctx.quadraticCurveTo(
          w1X + 6 * s,
          screenPos.y - 14 * s,
          w1X + 12 * s,
          screenPos.y - 4 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(w1X + 28 * s, w1TopY + 10 * s);
        ctx.quadraticCurveTo(
          w1X + 32 * s,
          screenPos.y - 10 * s,
          w1X + 26 * s,
          screenPos.y - 2 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(w2X + 8 * s, w2TopY + 4 * s);
        ctx.quadraticCurveTo(
          w2X + 12 * s,
          screenPos.y - 8 * s,
          w2X + 6 * s,
          screenPos.y
        );
        ctx.stroke();
        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.85;
        [
          [w1X + 8 * s, screenPos.y - 10 * s],
          [w1X + 11 * s, screenPos.y - 6 * s],
          [w1X + 28 * s, screenPos.y - 6 * s],
          [w2X + 8 * s, screenPos.y - 4 * s],
        ].forEach(([lx, ly]) => {
          ctx.beginPath();
          ctx.ellipse(lx, ly, 2.5 * s, 1.2 * s, 0.5, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.globalAlpha = 1;

        // === 3D RUBBLE FIELD ===
        [
          { sz: 5, x: -34, y: 10 },
          { sz: 4, x: -18, y: 14 },
          { sz: 3.5, x: 4, y: 15 },
          { sz: 4, x: 18, y: 12 },
          { sz: 3.5, x: 30, y: 10 },
          { sz: 3, x: -24, y: 12 },
          { sz: 3, x: 10, y: 16 },
          { sz: 3, x: 34, y: 7 },
          { sz: 2.5, x: -10, y: 16 },
          { sz: 4, x: 24, y: 14 },
        ].forEach((rb, idx) => {
          const rx = screenPos.x + rb.x * s;
          const ry = screenPos.y + rb.y * s;
          const rSz = rb.sz * s;
          ctx.fillStyle =
            idx % 3 === 0 ? stoneLight : idx % 3 === 1 ? stoneMid : stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx, ry - rSz * 0.8);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx - rSz * 0.7, ry - rSz * 0.2);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.8, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz * 0.2, ry + rSz * 0.6);
          ctx.closePath();
          ctx.fill();
        });
        ctx.fillStyle = stoneShadow;
        [
          [-30, 15],
          [-14, 17],
          [2, 19],
          [14, 18],
          [26, 14],
          [34, 10],
          [-20, 17],
          [8, 12],
        ].forEach(([px, py], i) => {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + px * s,
            screenPos.y + py * s,
            (1.5 + (i % 3) * 0.5) * s,
            (0.8 + (i % 2) * 0.3) * s,
            i * 0.6,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });

        // === FLOOR TILES ===
        [
          { d: 3, w: 6, x: -14, y: 5 },
          { d: 2.5, w: 5, x: 6, y: 4 },
          { d: 2.5, w: 5, x: -24, y: 6 },
        ].forEach((t) => {
          const tx = screenPos.x + t.x * s;
          const ty = screenPos.y + t.y * s;
          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(tx, ty - t.d * s);
          ctx.lineTo(tx + t.w * s, ty);
          ctx.lineTo(tx, ty + t.d * s);
          ctx.lineTo(tx - t.w * s, ty);
          ctx.closePath();
          ctx.fill();
          ctx.strokeStyle = stoneShadow;
          ctx.lineWidth = 0.3 * s;
          ctx.stroke();
        });

        // === CRACKS ===
        ctx.strokeStyle = "#2a2a1a";
        ctx.lineWidth = 0.7 * s;
        ctx.beginPath();
        ctx.moveTo(w1X + 16 * s, w1TopY + 10 * s);
        ctx.lineTo(w1X + 18 * s, screenPos.y - 14 * s);
        ctx.lineTo(w1X + 16 * s, screenPos.y - 6 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(w2X + 14 * s, w2TopY + 8 * s);
        ctx.lineTo(w2X + 16 * s, screenPos.y - 6 * s);
        ctx.stroke();

        // === MOSS ===
        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.6;
        ctx.beginPath();
        ctx.ellipse(
          csX + 2 * s,
          screenPos.y - 4 * s,
          4 * s,
          2 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 30 * s,
          screenPos.y + 2 * s,
          3 * s,
          1.5 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 30 * s,
          screenPos.y + 4 * s,
          3.5 * s,
          1.8 * s,
          0.4,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;
      } else if (ruinVariant === 3) {
        // VARIANT 3: Ruined tower base — circular 3D wall, hollow interior, spiral stairs

        // === CIRCULAR BASE ===
        const bRx = 26 * s;
        const bRy = 14 * s;
        const bH = 5 * s;
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y + 3 * s,
          bRx,
          bRy,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneShadow;
        ctx.beginPath();
        ctx.ellipse(screenPos.x, screenPos.y + 3 * s, bRx, bRy, 0, 0, Math.PI);
        ctx.lineTo(screenPos.x - bRx, screenPos.y + 3 * s + bH);
        ctx.ellipse(
          screenPos.x,
          screenPos.y + 3 * s + bH,
          bRx,
          bRy,
          0,
          Math.PI,
          0,
          true
        );
        ctx.closePath();
        ctx.fill();

        // === TOWER WALL ===
        const wallG = ctx.createLinearGradient(
          screenPos.x - bRx,
          screenPos.y,
          screenPos.x + bRx,
          screenPos.y
        );
        wallG.addColorStop(0, stoneMid);
        wallG.addColorStop(0.3, stoneLight);
        wallG.addColorStop(0.7, stoneMid);
        wallG.addColorStop(1, stoneDark);
        ctx.fillStyle = wallG;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 22 * s, screenPos.y + 3 * s);
        ctx.lineTo(screenPos.x - 20 * s, screenPos.y - 34 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 5 * s,
          screenPos.y - 42 * s,
          screenPos.x + 12 * s,
          screenPos.y - 28 * s
        );
        ctx.lineTo(screenPos.x + 14 * s, screenPos.y + 1 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 22 * s, screenPos.y + 3 * s);
        ctx.lineTo(screenPos.x - 20 * s, screenPos.y - 34 * s);
        ctx.lineTo(screenPos.x - 24 * s, screenPos.y - 32 * s);
        ctx.lineTo(screenPos.x - 26 * s, screenPos.y + 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 24 * s, screenPos.y - 32 * s);
        ctx.lineTo(screenPos.x - 20 * s, screenPos.y - 34 * s);
        ctx.lineTo(screenPos.x - 14 * s, screenPos.y - 37 * s);
        ctx.lineTo(screenPos.x - 8 * s, screenPos.y - 32 * s);
        ctx.lineTo(screenPos.x - 2 * s, screenPos.y - 38 * s);
        ctx.lineTo(screenPos.x + 4 * s, screenPos.y - 34 * s);
        ctx.lineTo(screenPos.x + 12 * s, screenPos.y - 28 * s);
        ctx.lineTo(screenPos.x + 8 * s, screenPos.y - 30 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.5 * s;
        for (let row = 0; row < 5; row++) {
          const rowY = screenPos.y - 28 * s + row * 7 * s;
          if (rowY < screenPos.y) {
            ctx.beginPath();
            ctx.moveTo(screenPos.x - 20 * s, rowY);
            ctx.quadraticCurveTo(
              screenPos.x - 4 * s,
              rowY - 3 * s,
              screenPos.x + 12 * s,
              rowY + 2 * s
            );
            ctx.stroke();
          }
        }

        // === HOLLOW INTERIOR (3D depth) ===
        const holeX = screenPos.x - 2 * s;
        const holeY = screenPos.y + 1 * s;
        const holeRx = 15 * s;
        const holeRy = 9 * s;
        const holeDepth = 7 * s;

        // Inner wall (visible back face going down into the tower)
        const innerWallG = ctx.createLinearGradient(
          holeX - holeRx,
          holeY,
          holeX + holeRx,
          holeY
        );
        innerWallG.addColorStop(0, "#2e2e26");
        innerWallG.addColorStop(0.4, "#1e1e18");
        innerWallG.addColorStop(1, "#121210");
        ctx.fillStyle = innerWallG;
        ctx.beginPath();
        ctx.ellipse(holeX, holeY, holeRx, holeRy, 0, Math.PI, Math.PI * 2);
        ctx.lineTo(holeX + holeRx * 0.88, holeY + holeDepth);
        ctx.ellipse(
          holeX,
          holeY + holeDepth,
          holeRx * 0.88,
          holeRy * 0.88,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();

        // Stone floor at bottom of interior
        const floorG = ctx.createRadialGradient(
          holeX,
          holeY + holeDepth,
          0,
          holeX,
          holeY + holeDepth,
          holeRx * 0.85
        );
        floorG.addColorStop(0, "#22221c");
        floorG.addColorStop(0.7, "#1a1a15");
        floorG.addColorStop(1, "#121210");
        ctx.fillStyle = floorG;
        ctx.beginPath();
        ctx.ellipse(
          holeX,
          holeY + holeDepth,
          holeRx * 0.85,
          holeRy * 0.85,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Floor tile cracks
        ctx.strokeStyle = "#2a2a22";
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(holeX - 8 * s, holeY + holeDepth);
        ctx.lineTo(holeX + 7 * s, holeY + holeDepth + 1 * s);
        ctx.moveTo(holeX - 1 * s, holeY + holeDepth - 5 * s);
        ctx.lineTo(holeX - 3 * s, holeY + holeDepth + 4 * s);
        ctx.moveTo(holeX + 4 * s, holeY + holeDepth - 3 * s);
        ctx.lineTo(holeX + 6 * s, holeY + holeDepth + 2 * s);
        ctx.stroke();

        // Rubble inside the tower
        [
          { dx: -7, dy: -1, sz: 2.2 },
          { dx: 4, dy: 1, sz: 1.8 },
          { dx: -2, dy: -3, sz: 1.5 },
          { dx: 8, dy: -1, sz: 1.4 },
          { dx: -5, dy: 2, sz: 1.2 },
        ].forEach((d) => {
          const rx2 = holeX + d.dx * s;
          const ry2 = holeY + holeDepth + d.dy * s;
          ctx.fillStyle = d.sz > 1.7 ? "#3a3a2e" : "#2e2e26";
          ctx.beginPath();
          ctx.moveTo(rx2, ry2 - d.sz * s * 0.5);
          ctx.lineTo(rx2 + d.sz * s * 0.5, ry2);
          ctx.lineTo(rx2, ry2 + d.sz * s * 0.3);
          ctx.lineTo(rx2 - d.sz * s * 0.5, ry2);
          ctx.closePath();
          ctx.fill();
        });

        // Inner wall mortar lines
        ctx.strokeStyle = "rgba(30,30,25,0.5)";
        ctx.lineWidth = 0.4 * s;
        for (let iw = 0; iw < 3; iw++) {
          const iwY = holeY + (iw + 1) * holeDepth * 0.25;
          ctx.beginPath();
          ctx.ellipse(
            holeX,
            iwY,
            holeRx * (0.92 - iw * 0.02),
            holeRy * (0.92 - iw * 0.02),
            0,
            Math.PI + 0.2,
            Math.PI * 2 - 0.2
          );
          ctx.stroke();
        }

        // Rim highlight (lit edge of the opening)
        ctx.strokeStyle = "rgba(160,160,140,0.25)";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.ellipse(
          holeX,
          holeY,
          holeRx,
          holeRy,
          0,
          Math.PI + 0.3,
          Math.PI * 2 - 0.3
        );
        ctx.stroke();

        // Front rim shadow (bottom lip of opening)
        ctx.strokeStyle = "rgba(10,10,8,0.4)";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.ellipse(holeX, holeY, holeRx, holeRy, 0, 0.3, Math.PI - 0.3);
        ctx.stroke();

        // === SPIRAL STAIRCASE ===
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 9 * s, screenPos.y);
        ctx.lineTo(screenPos.x - 13 * s, screenPos.y - 10 * s);
        ctx.lineTo(screenPos.x - 6 * s, screenPos.y - 12 * s);
        ctx.lineTo(screenPos.x - 3 * s, screenPos.y - 4 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 6 * s, screenPos.y - 12 * s);
        ctx.lineTo(screenPos.x - 9 * s, screenPos.y - 20 * s);
        ctx.lineTo(screenPos.x - 2 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 14 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 2 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x - 4 * s, screenPos.y - 28 * s);
        ctx.lineTo(screenPos.x + 3 * s, screenPos.y - 30 * s);
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 24 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 13 * s, screenPos.y - 10 * s);
        ctx.lineTo(screenPos.x - 3 * s, screenPos.y - 4 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 9 * s, screenPos.y - 20 * s);
        ctx.lineTo(screenPos.x, screenPos.y - 14 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 4 * s, screenPos.y - 28 * s);
        ctx.lineTo(screenPos.x + 5 * s, screenPos.y - 24 * s);
        ctx.stroke();

        // === BROKEN PARAPET ===
        const parY = screenPos.y - 36 * s;
        [
          { x: -16, y: parY },
          { x: -6, y: parY - 4 * s },
          { x: 4, y: parY + 2 * s },
        ].forEach((m) => {
          const mx = screenPos.x + m.x * s;
          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(mx - 2 * s, m.y + 4 * s);
          ctx.lineTo(mx - 2 * s, m.y);
          ctx.lineTo(mx + 2 * s, m.y);
          ctx.lineTo(mx + 2 * s, m.y + 4 * s);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneLight;
          ctx.beginPath();
          ctx.moveTo(mx - 2 * s, m.y);
          ctx.lineTo(mx, m.y - 2 * s);
          ctx.lineTo(mx + 2 * s, m.y);
          ctx.closePath();
          ctx.fill();
        });

        // === COLUMN REMNANT ===
        const crX = screenPos.x + 22 * s;
        const crR = 4 * s;
        const crH = 16 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          crX,
          screenPos.y + 4 * s,
          crR + 1.5 * s,
          (crR + 1.5 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          crX,
          screenPos.y + 2 * s,
          crR,
          crR * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(crX + crR, screenPos.y - crH + 6 * s);
        ctx.ellipse(
          crX,
          screenPos.y - crH + 6 * s,
          crR,
          crR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const crG = ctx.createLinearGradient(crX - crR, 0, crX + crR, 0);
        crG.addColorStop(0, stoneMid);
        crG.addColorStop(0.3, stoneLight);
        crG.addColorStop(1, stoneDark);
        ctx.fillStyle = crG;
        ctx.beginPath();
        ctx.ellipse(crX, screenPos.y + 2 * s, crR, crR * 0.5, 0, 0, Math.PI);
        ctx.lineTo(crX - crR, screenPos.y - crH + 6 * s);
        ctx.ellipse(
          crX,
          screenPos.y - crH + 6 * s,
          crR,
          crR * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();

        // === 3D RUBBLE RING ===
        [
          { sz: 4.5, x: -30, y: 8 },
          { sz: 4, x: -20, y: 13 },
          { sz: 3.5, x: -8, y: 15 },
          { sz: 4, x: 8, y: 14 },
          { sz: 3.5, x: 20, y: 11 },
          { sz: 4, x: 28, y: 7 },
          { sz: 3, x: -26, y: 11 },
          { sz: 3, x: 14, y: 13 },
          { sz: 3, x: 32, y: 4 },
          { sz: 3, x: -34, y: 4 },
          { sz: 2.5, x: -14, y: 14 },
          { sz: 2.5, x: 24, y: 10 },
        ].forEach((rb, idx) => {
          const rx = screenPos.x + rb.x * s;
          const ry = screenPos.y + rb.y * s;
          const rSz = rb.sz * s;
          ctx.fillStyle =
            idx % 3 === 0 ? stoneLight : idx % 3 === 1 ? stoneMid : stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx, ry - rSz * 0.8);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx - rSz * 0.7, ry - rSz * 0.2);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.8, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz * 0.2, ry + rSz * 0.6);
          ctx.closePath();
          ctx.fill();
        });
        ctx.fillStyle = stoneShadow;
        [
          [-28, 12],
          [-16, 16],
          [-2, 18],
          [10, 17],
          [22, 14],
          [30, 9],
          [-8, 10],
          [16, 8],
        ].forEach(([px, py], i) => {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + px * s,
            screenPos.y + py * s,
            (1.5 + (i % 3) * 0.5) * s,
            (0.8 + (i % 2) * 0.3) * s,
            i * 0.6,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });

        // === FLOOR TILES ===
        [
          { d: 2.5, w: 5, x: -18, y: 7 },
          { d: 3, w: 6, x: 16, y: 6 },
          { d: 2.5, w: 5, x: 28, y: 2 },
          { d: 2, w: 4, x: -30, y: 3 },
        ].forEach((t) => {
          const tx = screenPos.x + t.x * s;
          const ty = screenPos.y + t.y * s;
          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(tx, ty - t.d * s);
          ctx.lineTo(tx + t.w * s, ty);
          ctx.lineTo(tx, ty + t.d * s);
          ctx.lineTo(tx - t.w * s, ty);
          ctx.closePath();
          ctx.fill();
          ctx.strokeStyle = stoneShadow;
          ctx.lineWidth = 0.3 * s;
          ctx.stroke();
        });

        // === MOSS ===
        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.6;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 16 * s,
          screenPos.y - 20 * s,
          4 * s,
          2 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 8 * s,
          screenPos.y - 14 * s,
          3.5 * s,
          1.8 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          crX + 2 * s,
          screenPos.y - 4 * s,
          3 * s,
          1.5 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 28 * s,
          screenPos.y + 6 * s,
          4 * s,
          2 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;

        // === CRACKS ===
        ctx.strokeStyle = "#2a2a1a";
        ctx.lineWidth = 0.7 * s;
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 12 * s, screenPos.y - 30 * s);
        ctx.lineTo(screenPos.x - 10 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x - 14 * s, screenPos.y - 10 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 6 * s, screenPos.y - 26 * s);
        ctx.lineTo(screenPos.x + 8 * s, screenPos.y - 16 * s);
        ctx.stroke();
      } else if (ruinVariant === 4) {
        // VARIANT 4: Properly isometric 3D sunken temple ruins
        const vineColor = "#2d4a2d";
        const waterStainColor = "rgba(40, 80, 90, 0.25)";

        // Crumbled foundation — ground shadow
        const v4GroundShadow = ctx.createRadialGradient(
          screenPos.x,
          screenPos.y + 8 * s,
          0,
          screenPos.x,
          screenPos.y + 8 * s,
          44 * s
        );
        v4GroundShadow.addColorStop(0, "rgba(10, 10, 8, 0.35)");
        v4GroundShadow.addColorStop(0.6, "rgba(10, 10, 8, 0.15)");
        v4GroundShadow.addColorStop(1, "rgba(10, 10, 8, 0)");
        ctx.fillStyle = v4GroundShadow;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y + 8 * s,
          46 * s,
          22 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Broken stone slabs at varying heights and angles
        const v4SlabColors = [stoneMid, "#5a5a4a", "#4a4a3a", stoneDark];
        const v4Slabs = [
          { d: 7, h: 3.5, jOff: 0.9, w: 14, x: -28, y: 4 },
          { d: 6, h: 2.5, jOff: 2.1, w: 12, x: -8, y: 2 },
          { d: 5.5, h: 4, jOff: 0.4, w: 11, x: 12, y: 4 },
          { d: 5, h: 3, jOff: 3.3, w: 10, x: 26, y: 7 },
          { d: 4.5, h: 2, jOff: 1.6, w: 9, x: -20, y: 12 },
          { d: 5, h: 1.5, jOff: 0.2, w: 10, x: 4, y: 10 },
          { d: 4, h: 2.5, jOff: 2.7, w: 8, x: 18, y: 12 },
          { d: 3.5, h: 2, jOff: 3.8, w: 7, x: -34, y: 8 },
          { d: 4, h: 2, jOff: 1.3, w: 8, x: 34, y: 3 },
        ];
        v4Slabs.forEach((sl, i) => {
          const sx = screenPos.x + sl.x * s;
          const sy = screenPos.y + sl.y * s;
          const sw = sl.w * s;
          const sd = sl.d * s;
          const sh = sl.h * s;
          const jag = Math.sin(sl.jOff) * 1.5 * s;
          ctx.fillStyle = v4SlabColors[i % v4SlabColors.length];
          ctx.beginPath();
          ctx.moveTo(sx, sy - sd);
          ctx.lineTo(sx + sw * 0.5 + jag, sy - sd * 0.4);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5);
          ctx.lineTo(sx - sw * 0.45, sy - sd * 0.1 + jag);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(sx + sw * 0.5 + jag, sy - sd * 0.4);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5 + sh);
          ctx.lineTo(sx + sw * 0.5 + jag, sy - sd * 0.4 + sh);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneShadow;
          ctx.beginPath();
          ctx.moveTo(sx - sw * 0.45, sy - sd * 0.1 + jag);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5 + sh);
          ctx.lineTo(sx - sw * 0.45, sy - sd * 0.1 + jag + sh);
          ctx.closePath();
          ctx.fill();
          if (i % 2 === 0) {
            ctx.strokeStyle = "#1a1a12";
            ctx.lineWidth = 0.5 * s;
            ctx.beginPath();
            ctx.moveTo(sx - sw * 0.2, sy - sd * 0.6);
            ctx.lineTo(sx + sw * 0.1, sy - sd * 0.1);
            ctx.lineTo(sx + sw * 0.25, sy + sd * 0.1);
            ctx.stroke();
          }
        });

        // Small stone fragments between slabs
        [
          { sz: 2.2, x: -16, y: 8 },
          { sz: 1.8, x: 8, y: 7 },
          { sz: 1.6, x: -4, y: 14 },
          { sz: 2, x: 20, y: 10 },
          { sz: 1.4, x: -26, y: 10 },
          { sz: 1.6, x: 30, y: 9 },
          { sz: 1.3, x: 0, y: 16 },
          { sz: 1.5, x: -12, y: 15 },
          { sz: 1.8, x: 14, y: 14 },
          { sz: 1.4, x: 36, y: 6 },
        ].forEach((f, i) => {
          const fx = screenPos.x + f.x * s;
          const fy = screenPos.y + f.y * s;
          const fSz = f.sz * s;
          ctx.fillStyle =
            i % 3 === 0 ? stoneLight : i % 3 === 1 ? stoneMid : stoneDark;
          ctx.beginPath();
          ctx.moveTo(fx, fy - fSz);
          ctx.lineTo(fx + fSz * 0.7, fy - fSz * 0.15);
          ctx.lineTo(fx + fSz * 0.2, fy + fSz * 0.5);
          ctx.lineTo(fx - fSz * 0.6, fy + fSz * 0.1);
          ctx.closePath();
          ctx.fill();
        });

        // Exposed dirt / waterlogged patches between stones
        ctx.fillStyle = "rgba(30, 45, 35, 0.25)";
        [
          { rx: 5, ry: 2.5, x: -18, y: 9 },
          { rx: 4.5, ry: 2.2, x: 6, y: 12 },
          { rx: 6, ry: 2.8, x: -6, y: 15 },
          { rx: 4, ry: 2, x: 24, y: 10 },
        ].forEach((p) => {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + p.x * s,
            screenPos.y + p.y * s,
            p.rx * s,
            p.ry * s,
            0.15,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });

        // === LEFT WALL (3D isometric box) ===
        const lWallX = screenPos.x - 28 * s;
        const lWallW = 14 * s;
        const lWallD = 7 * s;
        const lWallH = 38 * s;

        // Left wall - LEFT face (darkest, shadow)
        const leftFaceGrad = ctx.createLinearGradient(
          lWallX - lWallD,
          screenPos.y,
          lWallX,
          screenPos.y
        );
        leftFaceGrad.addColorStop(0, stoneShadow);
        leftFaceGrad.addColorStop(1, stoneDark);
        ctx.fillStyle = leftFaceGrad;
        ctx.beginPath();
        ctx.moveTo(lWallX - lWallD, screenPos.y + 8 * s);
        ctx.lineTo(lWallX - lWallD, screenPos.y - lWallH + 12 * s);
        ctx.lineTo(lWallX - lWallD + 3 * s, screenPos.y - lWallH + 8 * s);
        ctx.lineTo(lWallX - lWallD + 5 * s, screenPos.y - lWallH + 14 * s);
        ctx.lineTo(lWallX, screenPos.y - lWallH + 5 * s);
        ctx.lineTo(lWallX, screenPos.y + 5 * s);
        ctx.closePath();
        ctx.fill();

        // Left wall - FRONT face (medium lit)
        const frontFaceGrad = ctx.createLinearGradient(
          lWallX,
          screenPos.y - lWallH,
          lWallX + lWallW,
          screenPos.y
        );
        frontFaceGrad.addColorStop(0, stoneMid);
        frontFaceGrad.addColorStop(0.5, stoneLight);
        frontFaceGrad.addColorStop(1, stoneMid);
        ctx.fillStyle = frontFaceGrad;
        ctx.beginPath();
        ctx.moveTo(lWallX, screenPos.y + 5 * s);
        ctx.lineTo(lWallX, screenPos.y - lWallH + 5 * s);
        ctx.lineTo(lWallX + 4 * s, screenPos.y - lWallH);
        ctx.lineTo(lWallX + 8 * s, screenPos.y - lWallH + 8 * s);
        ctx.lineTo(lWallX + lWallW, screenPos.y - lWallH + 3 * s);
        ctx.lineTo(lWallX + lWallW, screenPos.y + 8 * s);
        ctx.closePath();
        ctx.fill();

        // Left wall - TOP face (brightest)
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(lWallX - lWallD, screenPos.y - lWallH + 12 * s);
        ctx.lineTo(lWallX - lWallD + 3 * s, screenPos.y - lWallH + 8 * s);
        ctx.lineTo(lWallX - lWallD + 5 * s, screenPos.y - lWallH + 14 * s);
        ctx.lineTo(lWallX, screenPos.y - lWallH + 5 * s);
        ctx.lineTo(lWallX + 4 * s, screenPos.y - lWallH);
        ctx.lineTo(lWallX + 8 * s, screenPos.y - lWallH + 8 * s);
        ctx.lineTo(lWallX + lWallW, screenPos.y - lWallH + 3 * s);
        ctx.lineTo(lWallX + lWallW - lWallD, screenPos.y - lWallH);
        ctx.closePath();
        ctx.fill();

        // Brick lines on left wall front face
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.5 * s;
        for (let row = 0; row < 6; row++) {
          const rowY = screenPos.y - lWallH + 15 * s + row * 7 * s;
          if (rowY < screenPos.y + 5 * s) {
            ctx.beginPath();
            ctx.moveTo(lWallX + 1 * s, rowY);
            ctx.lineTo(lWallX + lWallW - 1 * s, rowY + 2 * s);
            ctx.stroke();
          }
        }

        // Water stains on left wall
        ctx.fillStyle = waterStainColor;
        ctx.beginPath();
        ctx.moveTo(lWallX + 3 * s, screenPos.y - 15 * s);
        ctx.quadraticCurveTo(
          lWallX + 5 * s,
          screenPos.y - 5 * s,
          lWallX + 2 * s,
          screenPos.y + 3 * s
        );
        ctx.lineTo(lWallX + 6 * s, screenPos.y + 3 * s);
        ctx.quadraticCurveTo(
          lWallX + 8 * s,
          screenPos.y - 8 * s,
          lWallX + 7 * s,
          screenPos.y - 15 * s
        );
        ctx.closePath();
        ctx.fill();

        // === RIGHT WALL (3D isometric box) ===
        const rWallX = screenPos.x + 15 * s;
        const rWallW = 16 * s;
        const rWallD = 8 * s;
        const rWallH = 32 * s;

        // Right wall - BACK face (visible in iso)
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(rWallX + rWallW, screenPos.y + 5 * s);
        ctx.lineTo(rWallX + rWallW, screenPos.y - rWallH + 8 * s);
        ctx.lineTo(rWallX + rWallW + rWallD, screenPos.y - rWallH + 5 * s);
        ctx.lineTo(rWallX + rWallW + rWallD, screenPos.y + 8 * s);
        ctx.closePath();
        ctx.fill();

        // Right wall - FRONT face (brightest)
        const rFrontGrad = ctx.createLinearGradient(
          rWallX,
          screenPos.y,
          rWallX + rWallW,
          screenPos.y - rWallH
        );
        rFrontGrad.addColorStop(0, stoneMid);
        rFrontGrad.addColorStop(0.4, stoneLight);
        rFrontGrad.addColorStop(1, stoneMid);
        ctx.fillStyle = rFrontGrad;
        ctx.beginPath();
        ctx.moveTo(rWallX, screenPos.y + 8 * s);
        ctx.lineTo(rWallX, screenPos.y - rWallH + 12 * s);
        ctx.lineTo(rWallX + 5 * s, screenPos.y - rWallH + 5 * s);
        ctx.lineTo(rWallX + 10 * s, screenPos.y - rWallH + 10 * s);
        ctx.lineTo(rWallX + rWallW, screenPos.y - rWallH + 8 * s);
        ctx.lineTo(rWallX + rWallW, screenPos.y + 5 * s);
        ctx.closePath();
        ctx.fill();

        // Right wall - TOP face
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(rWallX, screenPos.y - rWallH + 12 * s);
        ctx.lineTo(rWallX + 5 * s, screenPos.y - rWallH + 5 * s);
        ctx.lineTo(rWallX + 10 * s, screenPos.y - rWallH + 10 * s);
        ctx.lineTo(rWallX + rWallW, screenPos.y - rWallH + 8 * s);
        ctx.lineTo(rWallX + rWallW + rWallD, screenPos.y - rWallH + 5 * s);
        ctx.lineTo(rWallX + rWallD, screenPos.y - rWallH + 9 * s);
        ctx.closePath();
        ctx.fill();

        // Brick lines on right wall
        ctx.strokeStyle = stoneShadow;
        for (let row = 0; row < 5; row++) {
          const rowY = screenPos.y - rWallH + 18 * s + row * 7 * s;
          if (rowY < screenPos.y + 5 * s) {
            ctx.beginPath();
            ctx.moveTo(rWallX + 1 * s, rowY + 1 * s);
            ctx.lineTo(rWallX + rWallW - 1 * s, rowY - 1 * s);
            ctx.stroke();
          }
        }

        // Ancient carved eye symbol on right wall
        ctx.strokeStyle = "#4a4a4a";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.ellipse(
          rWallX + 8 * s,
          screenPos.y - 8 * s,
          4 * s,
          2.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.arc(rWallX + 8 * s, screenPos.y - 8 * s, 1.2 * s, 0, Math.PI * 2);
        ctx.stroke();

        // === CENTRAL BROKEN COLUMN (3D cylinder) ===
        const colX = screenPos.x;
        const colY = screenPos.y;
        const colRadius = 7 * s;
        const colHeight = 42 * s;

        // Column shadow
        ctx.fillStyle = "rgba(30, 30, 20, 0.3)";
        ctx.beginPath();
        ctx.ellipse(
          colX + 4 * s,
          colY + 10 * s,
          colRadius + 4 * s,
          (colRadius + 4 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Column base (isometric ellipse)
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          colX,
          colY + 6 * s,
          colRadius + 3 * s,
          (colRadius + 3 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          colX,
          colY + 8 * s,
          colRadius + 3 * s,
          (colRadius + 3 * s) * 0.5,
          0,
          0,
          Math.PI
        );
        ctx.fill();

        // Column body - back half (shadow)
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          colX,
          colY + 4 * s,
          colRadius,
          colRadius * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(colX + colRadius, colY - colHeight + 15 * s);
        ctx.ellipse(
          colX,
          colY - colHeight + 15 * s,
          colRadius,
          colRadius * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();

        // Column body - front half (lit)
        const colFrontGrad = ctx.createLinearGradient(
          colX - colRadius,
          colY,
          colX + colRadius,
          colY
        );
        colFrontGrad.addColorStop(0, stoneMid);
        colFrontGrad.addColorStop(0.3, stoneLight);
        colFrontGrad.addColorStop(0.7, stoneMid);
        colFrontGrad.addColorStop(1, stoneDark);
        ctx.fillStyle = colFrontGrad;
        ctx.beginPath();
        ctx.ellipse(
          colX,
          colY + 4 * s,
          colRadius,
          colRadius * 0.5,
          0,
          0,
          Math.PI
        );
        ctx.lineTo(colX - colRadius, colY - colHeight + 15 * s);
        ctx.ellipse(
          colX,
          colY - colHeight + 15 * s,
          colRadius,
          colRadius * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();

        // Column top (broken, jagged)
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(colX - colRadius, colY - colHeight + 15 * s);
        ctx.lineTo(colX - colRadius + 3 * s, colY - colHeight + 8 * s);
        ctx.lineTo(colX - 2 * s, colY - colHeight + 12 * s);
        ctx.lineTo(colX + 2 * s, colY - colHeight + 5 * s);
        ctx.lineTo(colX + colRadius - 2 * s, colY - colHeight + 10 * s);
        ctx.lineTo(colX + colRadius, colY - colHeight + 15 * s);
        ctx.ellipse(
          colX,
          colY - colHeight + 15 * s,
          colRadius,
          colRadius * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();

        // Column fluting (vertical grooves)
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.8 * s;
        for (let fl = 0; fl < 5; fl++) {
          const flAngle = (fl / 5) * Math.PI - Math.PI / 2;
          const flX = colX + Math.cos(flAngle) * colRadius * 0.85;
          ctx.beginPath();
          ctx.moveTo(flX, colY + 3 * s);
          ctx.lineTo(flX, colY - colHeight + 16 * s);
          ctx.stroke();
        }

        // Highlight on column
        ctx.strokeStyle = "rgba(255, 255, 255, 0.15)";
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.moveTo(colX - colRadius * 0.5, colY + 2 * s);
        ctx.lineTo(colX - colRadius * 0.5, colY - colHeight + 18 * s);
        ctx.stroke();

        // === RUBBLE (3D isometric rocks) ===
        const rubblePositions = [
          { size: 4, x: -20, y: 8 },
          { size: 3, x: -12, y: 10 },
          { size: 5, x: 5, y: 12 },
          { size: 3.5, x: 15, y: 9 },
          { size: 4, x: 25, y: 11 },
          { size: 2.5, x: -8, y: 7 },
          { size: 3, x: 32, y: 7 },
          { size: 3.5, x: -32, y: 6 },
        ];

        rubblePositions.forEach((rb, idx) => {
          const rx = screenPos.x + rb.x * s;
          const ry = screenPos.y + rb.y * s;
          const rSize = rb.size * s;

          ctx.fillStyle = idx % 2 === 0 ? stoneLight : stoneMid;
          ctx.beginPath();
          ctx.moveTo(rx, ry - rSize * 0.8);
          ctx.lineTo(rx + rSize, ry - rSize * 0.3);
          ctx.lineTo(rx + rSize * 0.3, ry + rSize * 0.2);
          ctx.lineTo(rx - rSize * 0.7, ry - rSize * 0.2);
          ctx.closePath();
          ctx.fill();

          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(rx - rSize * 0.7, ry - rSize * 0.2);
          ctx.lineTo(rx + rSize * 0.3, ry + rSize * 0.2);
          ctx.lineTo(rx + rSize * 0.2, ry + rSize * 0.6);
          ctx.lineTo(rx - rSize * 0.6, ry + rSize * 0.3);
          ctx.closePath();
          ctx.fill();

          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx + rSize * 0.3, ry + rSize * 0.2);
          ctx.lineTo(rx + rSize, ry - rSize * 0.3);
          ctx.lineTo(rx + rSize * 0.8, ry + rSize * 0.2);
          ctx.lineTo(rx + rSize * 0.2, ry + rSize * 0.6);
          ctx.closePath();
          ctx.fill();
        });

        // === FALLEN COLUMN SEGMENT ===
        const fallX = screenPos.x + 26 * s;
        const fallY = screenPos.y + 4 * s;
        const fallR = 4 * s;
        const fallLen = 14 * s;

        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          fallX + fallLen * 0.7,
          fallY - fallLen * 0.35,
          fallR,
          fallR * 0.5,
          0.8,
          0,
          Math.PI * 2
        );
        ctx.fill();

        const fallGrad = ctx.createLinearGradient(
          fallX,
          fallY - fallR,
          fallX,
          fallY + fallR
        );
        fallGrad.addColorStop(0, stoneLight);
        fallGrad.addColorStop(0.5, stoneMid);
        fallGrad.addColorStop(1, stoneDark);
        ctx.fillStyle = fallGrad;
        ctx.beginPath();
        ctx.moveTo(fallX, fallY - fallR * 0.5);
        ctx.lineTo(fallX + fallLen * 0.7, fallY - fallLen * 0.35 - fallR * 0.5);
        ctx.ellipse(
          fallX + fallLen * 0.7,
          fallY - fallLen * 0.35,
          fallR,
          fallR * 0.5,
          0.8,
          -Math.PI / 2,
          Math.PI / 2
        );
        ctx.lineTo(fallX, fallY + fallR * 0.5);
        ctx.ellipse(
          fallX,
          fallY,
          fallR,
          fallR * 0.5,
          0,
          Math.PI / 2,
          -Math.PI / 2,
          true
        );
        ctx.closePath();
        ctx.fill();

        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          fallX,
          fallY,
          fallR,
          fallR * ISO_Y_RATIO,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // === MOSS & VINES ===
        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.75;
        ctx.beginPath();
        ctx.ellipse(
          lWallX + 5 * s,
          screenPos.y - 12 * s,
          5 * s,
          2.5 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          colX - 3 * s,
          colY - 20 * s,
          4 * s,
          2 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 8 * s,
          screenPos.y + 5 * s,
          6 * s,
          3 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;

        ctx.strokeStyle = vineColor;
        ctx.lineWidth = 1.5 * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(lWallX + 8 * s, screenPos.y - lWallH + 10 * s);
        ctx.quadraticCurveTo(
          lWallX + 5 * s,
          screenPos.y - 15 * s,
          lWallX + 10 * s,
          screenPos.y - 5 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(colX + 3 * s, colY - colHeight + 10 * s);
        ctx.quadraticCurveTo(
          colX + 10 * s,
          colY - 25 * s,
          colX + 6 * s,
          colY - 10 * s
        );
        ctx.stroke();

        ctx.fillStyle = mossColor;
        const leaves = [
          { x: lWallX + 6 * s, y: screenPos.y - 18 * s },
          { x: lWallX + 9 * s, y: screenPos.y - 10 * s },
          { x: colX + 8 * s, y: colY - 22 * s },
          { x: colX + 7 * s, y: colY - 15 * s },
        ];
        leaves.forEach((lf) => {
          ctx.beginPath();
          ctx.ellipse(lf.x, lf.y, 2.5 * s, 1.2 * s, 0.5, 0, Math.PI * 2);
          ctx.fill();
        });

        ctx.strokeStyle = "#2a2a1a";
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo(lWallX + 3 * s, screenPos.y - 8 * s);
        ctx.lineTo(lWallX + 6 * s, screenPos.y - 3 * s);
        ctx.lineTo(lWallX + 4 * s, screenPos.y + 2 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(rWallX + 5 * s, screenPos.y - 15 * s);
        ctx.lineTo(rWallX + 8 * s, screenPos.y - 10 * s);
        ctx.stroke();

        // === ADDITIONAL BRANCHING CRACKS on walls ===
        ctx.strokeStyle = "rgba(20, 20, 12, 0.55)";
        ctx.lineWidth = 0.4 * s;
        ctx.beginPath();
        ctx.moveTo(lWallX + 6 * s, screenPos.y - 3 * s);
        ctx.lineTo(lWallX + 3 * s, screenPos.y + 4 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(lWallX + 10 * s, screenPos.y - lWallH + 20 * s);
        ctx.lineTo(lWallX + 12 * s, screenPos.y - lWallH + 28 * s);
        ctx.lineTo(lWallX + 9 * s, screenPos.y - lWallH + 34 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(rWallX + 3 * s, screenPos.y - rWallH + 18 * s);
        ctx.lineTo(rWallX + 6 * s, screenPos.y - rWallH + 26 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(rWallX + 12 * s, screenPos.y - 6 * s);
        ctx.lineTo(rWallX + 14 * s, screenPos.y);
        ctx.lineTo(rWallX + 11 * s, screenPos.y + 4 * s);
        ctx.stroke();

        // === COLLAPSED WALL FRAGMENT (lying flat, left side) ===
        const cwfX = screenPos.x - 34 * s;
        const cwfY = screenPos.y + 6 * s;
        const cwfG = ctx.createLinearGradient(
          cwfX,
          cwfY - 3 * s,
          cwfX + 16 * s,
          cwfY + 4 * s
        );
        cwfG.addColorStop(0, stoneLight);
        cwfG.addColorStop(0.5, stoneMid);
        cwfG.addColorStop(1, stoneDark);
        ctx.fillStyle = cwfG;
        ctx.beginPath();
        ctx.moveTo(cwfX, cwfY - 3 * s);
        ctx.lineTo(cwfX + 16 * s, cwfY - 6 * s);
        ctx.lineTo(cwfX + 18 * s, cwfY - 1 * s);
        ctx.lineTo(cwfX + 2 * s, cwfY + 2 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneShadow;
        ctx.beginPath();
        ctx.moveTo(cwfX + 2 * s, cwfY + 2 * s);
        ctx.lineTo(cwfX + 18 * s, cwfY - 1 * s);
        ctx.lineTo(cwfX + 18 * s, cwfY + 1 * s);
        ctx.lineTo(cwfX + 2 * s, cwfY + 4 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = "#1a1a12";
        ctx.lineWidth = 0.4 * s;
        ctx.beginPath();
        ctx.moveTo(cwfX + 5 * s, cwfY - 4 * s);
        ctx.lineTo(cwfX + 10 * s, cwfY - 1 * s);
        ctx.stroke();

        // === BROKEN STEP FRAGMENTS (near left wall base) ===
        const bsX = screenPos.x - 18 * s;
        const bsY = screenPos.y + 4 * s;
        [
          { d: 3, dx: 0, dy: 0, h: 2, w: 7 },
          { d: 2.5, dx: 7, dy: -1, h: 3.5, w: 6 },
          { d: 2, dx: 13, dy: -2.5, h: 5, w: 5 },
        ].forEach((step) => {
          const spx = bsX + step.dx * s;
          const spy = bsY + step.dy * s;
          const spw = step.w * s;
          const spd = step.d * s;
          const sph = step.h * s;
          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(spx, spy - spd);
          ctx.lineTo(spx + spw, spy - spd * 0.3);
          ctx.lineTo(spx + spw * 0.8, spy + spd * 0.5);
          ctx.lineTo(spx - spw * 0.2, spy);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(spx + spw, spy - spd * 0.3);
          ctx.lineTo(spx + spw * 0.8, spy + spd * 0.5);
          ctx.lineTo(spx + spw * 0.8, spy + spd * 0.5 + sph);
          ctx.lineTo(spx + spw, spy - spd * 0.3 + sph);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneShadow;
          ctx.beginPath();
          ctx.moveTo(spx - spw * 0.2, spy);
          ctx.lineTo(spx + spw * 0.8, spy + spd * 0.5);
          ctx.lineTo(spx + spw * 0.8, spy + spd * 0.5 + sph);
          ctx.lineTo(spx - spw * 0.2, spy + sph);
          ctx.closePath();
          ctx.fill();
        });

        // === BROKEN ARCH FRAGMENT (right foreground) ===
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 32 * s, screenPos.y + 2 * s);
        ctx.lineTo(screenPos.x + 30 * s, screenPos.y - 14 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 12 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y + 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 38 * s, screenPos.y + 5 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 12 * s);
        ctx.lineTo(screenPos.x + 42 * s, screenPos.y - 9 * s);
        ctx.lineTo(screenPos.x + 42 * s, screenPos.y + 8 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 30 * s, screenPos.y - 14 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 12 * s);
        ctx.lineTo(screenPos.x + 42 * s, screenPos.y - 9 * s);
        ctx.closePath();
        ctx.fill();

        // === SCATTERED CARVED STONE BLOCK ===
        const v4cbX = screenPos.x + 6 * s;
        const v4cbY = screenPos.y + 16 * s;
        const v4cbG = ctx.createLinearGradient(
          v4cbX - 3 * s,
          v4cbY,
          v4cbX + 4 * s,
          v4cbY
        );
        v4cbG.addColorStop(0, stoneMid);
        v4cbG.addColorStop(0.4, stoneLight);
        v4cbG.addColorStop(1, stoneDark);
        ctx.fillStyle = v4cbG;
        ctx.beginPath();
        ctx.moveTo(v4cbX - 3 * s, v4cbY - 1.5 * s);
        ctx.lineTo(v4cbX + 2 * s, v4cbY - 3 * s);
        ctx.lineTo(v4cbX + 5 * s, v4cbY - 1 * s);
        ctx.lineTo(v4cbX, v4cbY + 1 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(v4cbX + 5 * s, v4cbY - 1 * s);
        ctx.lineTo(v4cbX, v4cbY + 1 * s);
        ctx.lineTo(v4cbX, v4cbY + 3 * s);
        ctx.lineTo(v4cbX + 5 * s, v4cbY + 1 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = "rgba(80, 80, 60, 0.4)";
        ctx.lineWidth = 0.3 * s;
        ctx.beginPath();
        ctx.moveTo(v4cbX - 1 * s, v4cbY - 2 * s);
        ctx.lineTo(v4cbX + 3 * s, v4cbY - 2.2 * s);
        ctx.stroke();

        // === GROUND EROSION MARKS ===
        ctx.strokeStyle = "rgba(15, 15, 10, 0.2)";
        ctx.lineWidth = 1.2 * s;
        ctx.lineCap = "round";
        [
          { x1: -24, x2: -16, y1: 16, y2: 18 },
          { x1: 10, x2: 20, y1: 18, y2: 17 },
          { x1: -6, x2: 4, y1: 19, y2: 21 },
          { x1: 26, x2: 34, y1: 14, y2: 12 },
        ].forEach((e) => {
          ctx.beginPath();
          ctx.moveTo(screenPos.x + e.x1 * s, screenPos.y + e.y1 * s);
          ctx.quadraticCurveTo(
            screenPos.x + ((e.x1 + e.x2) / 2) * s,
            screenPos.y + ((e.y1 + e.y2) / 2 + 1.5) * s,
            screenPos.x + e.x2 * s,
            screenPos.y + e.y2 * s
          );
          ctx.stroke();
        });

        // === ADDITIONAL RUBBLE PILES ===
        [
          { clr: stoneMid, sz: 4.5, x: -38, y: 12 },
          { clr: stoneDark, sz: 3.5, x: -30, y: 16 },
          { clr: stoneMid, sz: 4, x: 36, y: 10 },
          { clr: stoneLight, sz: 3, x: 40, y: 6 },
          { clr: stoneDark, sz: 2.5, x: -8, y: 18 },
          { clr: stoneMid, sz: 3, x: 16, y: 18 },
        ].forEach((rb) => {
          const rx = screenPos.x + rb.x * s;
          const ry = screenPos.y + rb.y * s;
          const rSz = rb.sz * s;
          ctx.fillStyle = rb.clr;
          ctx.beginPath();
          ctx.moveTo(rx, ry - rSz * 0.7);
          ctx.lineTo(rx + rSz * 0.8, ry - rSz * 0.2);
          ctx.lineTo(rx + rSz * 0.5, ry + rSz * 0.3);
          ctx.lineTo(rx - rSz * 0.3, ry + rSz * 0.15);
          ctx.lineTo(rx - rSz * 0.6, ry - rSz * 0.3);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneShadow;
          ctx.beginPath();
          ctx.moveTo(rx + rSz * 0.5, ry + rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.8, ry - rSz * 0.2);
          ctx.lineTo(rx + rSz * 0.7, ry + rSz * 0.1);
          ctx.lineTo(rx + rSz * 0.4, ry + rSz * 0.5);
          ctx.closePath();
          ctx.fill();
        });

        // === BROKEN PEDESTAL STUMP ===
        const v4pdX = screenPos.x - 10 * s;
        const v4pdY = screenPos.y + 14 * s;
        const v4pdR = 3.5 * s;
        const v4pdH = 5 * s;
        const v4pdG = ctx.createLinearGradient(
          v4pdX - v4pdR,
          v4pdY,
          v4pdX + v4pdR,
          v4pdY
        );
        v4pdG.addColorStop(0, stoneMid);
        v4pdG.addColorStop(0.3, stoneLight);
        v4pdG.addColorStop(0.7, stoneMid);
        v4pdG.addColorStop(1, stoneDark);
        ctx.fillStyle = v4pdG;
        ctx.beginPath();
        ctx.ellipse(v4pdX, v4pdY, v4pdR, v4pdR * 0.45, 0, 0, Math.PI);
        ctx.lineTo(v4pdX - v4pdR, v4pdY - v4pdH);
        ctx.ellipse(
          v4pdX,
          v4pdY - v4pdH,
          v4pdR,
          v4pdR * 0.45,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(v4pdX - v4pdR, v4pdY - v4pdH);
        ctx.lineTo(v4pdX - v4pdR + 1.5 * s, v4pdY - v4pdH - 2 * s);
        ctx.lineTo(v4pdX + 1 * s, v4pdY - v4pdH - 0.5 * s);
        ctx.lineTo(v4pdX + v4pdR, v4pdY - v4pdH);
        ctx.ellipse(
          v4pdX,
          v4pdY - v4pdH,
          v4pdR,
          v4pdR * 0.45,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();

        // === ADDITIONAL VINES (more overgrowth) ===
        ctx.strokeStyle = vineColor;
        ctx.lineWidth = 1.2 * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(rWallX + 14 * s, screenPos.y - rWallH + 10 * s);
        ctx.quadraticCurveTo(
          rWallX + 18 * s,
          screenPos.y - 16 * s,
          rWallX + 12 * s,
          screenPos.y - 4 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(lWallX + 2 * s, screenPos.y - lWallH + 16 * s);
        ctx.quadraticCurveTo(
          lWallX - 2 * s,
          screenPos.y - 20 * s,
          lWallX + 4 * s,
          screenPos.y - 8 * s
        );
        ctx.stroke();
        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.8;
        [
          [rWallX + 16 * s, screenPos.y - 12 * s],
          [rWallX + 14 * s, screenPos.y - 6 * s],
          [lWallX, screenPos.y - 16 * s],
          [lWallX + 3 * s, screenPos.y - 10 * s],
        ].forEach(([lx, ly]) => {
          ctx.beginPath();
          ctx.ellipse(lx, ly, 2 * s, 1 * s, 0.4, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.globalAlpha = 1;

        // === WATER STAIN POOLS (sunken theme) ===
        ctx.fillStyle = "rgba(35, 70, 80, 0.12)";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 8 * s,
          screenPos.y + 6 * s,
          10 * s,
          5 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 18 * s,
          screenPos.y + 4 * s,
          8 * s,
          4 * s,
          -0.15,
          0,
          Math.PI * 2
        );
        ctx.fill();
      } else if (ruinVariant === 5) {
        // VARIANT 5: Fortress ruins — hexagonal base, columns, wall fragments, rubble
        const iceHighlight = highlightColor;
        const frostMoss = accentMoss;

        // === HEXAGONAL PRISM BASE ===
        const hexR = 26 * s;
        const hexH = 6 * s;
        const hexY = screenPos.y + 2 * s;
        const hexPts: { x: number; y: number }[] = [];
        for (let i = 0; i < 6; i++) {
          const ang = (Math.PI / 3) * i - Math.PI / 2;
          hexPts.push({
            x: screenPos.x + Math.cos(ang) * hexR,
            y: hexY + Math.sin(ang) * hexR * 0.5,
          });
        }
        const hexTopGrad = ctx.createLinearGradient(
          screenPos.x - hexR,
          hexY,
          screenPos.x + hexR,
          hexY
        );
        hexTopGrad.addColorStop(0, stoneMid);
        hexTopGrad.addColorStop(0.3, stoneLight);
        hexTopGrad.addColorStop(0.7, stoneMid);
        hexTopGrad.addColorStop(1, stoneDark);
        ctx.fillStyle = hexTopGrad;
        ctx.beginPath();
        ctx.moveTo(hexPts[0].x, hexPts[0].y);
        for (let i = 1; i < 6; i++) {
          ctx.lineTo(hexPts[i].x, hexPts[i].y);
        }
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.4 * s;
        for (let i = 0; i < 6; i++) {
          ctx.beginPath();
          ctx.moveTo(screenPos.x, hexY);
          ctx.lineTo(hexPts[i].x, hexPts[i].y);
          ctx.stroke();
        }
        for (let i = 2; i <= 4; i++) {
          const ni = (i + 1) % 6;
          ctx.fillStyle = i === 3 ? stoneShadow : stoneDark;
          ctx.beginPath();
          ctx.moveTo(hexPts[i].x, hexPts[i].y);
          ctx.lineTo(hexPts[ni].x, hexPts[ni].y);
          ctx.lineTo(hexPts[ni].x, hexPts[ni].y + hexH);
          ctx.lineTo(hexPts[i].x, hexPts[i].y + hexH);
          ctx.closePath();
          ctx.fill();
        }

        // === CRUMBLING WALL SECTION (back-left) ===
        const wX = screenPos.x - 24 * s;
        const wTopY = screenPos.y - 38 * s;
        const wW = 30 * s;
        const wThick = 5 * s;
        ctx.fillStyle = stoneShadow;
        ctx.beginPath();
        ctx.moveTo(wX - wThick, screenPos.y + 2 * s);
        ctx.lineTo(wX - wThick, wTopY + 8 * s);
        ctx.lineTo(wX - wThick + 2 * s, wTopY + 4 * s);
        ctx.lineTo(wX, wTopY + 2 * s);
        ctx.lineTo(wX, screenPos.y - 2 * s);
        ctx.closePath();
        ctx.fill();
        const wFG = ctx.createLinearGradient(wX, wTopY, wX + wW, screenPos.y);
        wFG.addColorStop(0, stoneMid);
        wFG.addColorStop(0.4, stoneLight);
        wFG.addColorStop(1, stoneDark);
        ctx.fillStyle = wFG;
        ctx.beginPath();
        ctx.moveTo(wX, screenPos.y - 2 * s);
        ctx.lineTo(wX, wTopY + 2 * s);
        ctx.lineTo(wX + 6 * s, wTopY);
        ctx.lineTo(wX + 14 * s, wTopY + 6 * s);
        ctx.lineTo(wX + 22 * s, wTopY + 2 * s);
        ctx.lineTo(wX + wW, wTopY + 8 * s);
        ctx.lineTo(wX + wW, screenPos.y + 1 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(wX - wThick, wTopY + 8 * s);
        ctx.lineTo(wX, wTopY + 2 * s);
        ctx.lineTo(wX + 6 * s, wTopY);
        ctx.lineTo(wX + 14 * s, wTopY + 6 * s);
        ctx.lineTo(wX + 22 * s, wTopY + 2 * s);
        ctx.lineTo(wX + wW, wTopY + 8 * s);
        ctx.lineTo(wX + wW - wThick, wTopY + 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.5 * s;
        for (let row = 0; row < 5; row++) {
          const rowY = wTopY + 12 * s + row * 6 * s;
          if (rowY < screenPos.y) {
            ctx.beginPath();
            ctx.moveTo(wX + 2 * s, rowY);
            ctx.lineTo(wX + wW - 2 * s, rowY + 1 * s);
            ctx.stroke();
          }
        }
        // Ice patches on wall
        ctx.fillStyle = iceHighlight;
        ctx.globalAlpha = 0.2;
        ctx.beginPath();
        ctx.ellipse(
          wX + 10 * s,
          screenPos.y - 12 * s,
          5 * s,
          3 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          wX + 22 * s,
          screenPos.y - 6 * s,
          4 * s,
          2 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;

        // === MAIN TALL COLUMN (center) ===
        const colR = 7 * s;
        const colH = 52 * s;
        const colX = screenPos.x + 2 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          colX,
          screenPos.y + 2 * s,
          colR + 3 * s,
          (colR + 3 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          colX,
          screenPos.y,
          colR,
          colR * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(colX + colR, screenPos.y - colH + 10 * s);
        ctx.ellipse(
          colX,
          screenPos.y - colH + 10 * s,
          colR,
          colR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const cGrad = ctx.createLinearGradient(
          colX - colR,
          screenPos.y,
          colX + colR,
          screenPos.y
        );
        cGrad.addColorStop(0, stoneMid);
        cGrad.addColorStop(0.25, stoneLight);
        cGrad.addColorStop(0.6, stoneMid);
        cGrad.addColorStop(1, stoneDark);
        ctx.fillStyle = cGrad;
        ctx.beginPath();
        ctx.ellipse(colX, screenPos.y, colR, colR * 0.5, 0, 0, Math.PI);
        ctx.lineTo(colX - colR, screenPos.y - colH + 10 * s);
        ctx.ellipse(
          colX,
          screenPos.y - colH + 10 * s,
          colR,
          colR * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(colX - colR, screenPos.y - colH + 10 * s);
        ctx.lineTo(colX - colR + 2 * s, screenPos.y - colH + 3 * s);
        ctx.lineTo(colX - 2 * s, screenPos.y - colH + 8 * s);
        ctx.lineTo(colX + 1 * s, screenPos.y - colH);
        ctx.lineTo(colX + colR, screenPos.y - colH + 10 * s);
        ctx.ellipse(
          colX,
          screenPos.y - colH + 10 * s,
          colR,
          colR * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.7 * s;
        for (let fl = 0; fl < 5; fl++) {
          const flA = (fl / 5) * Math.PI - Math.PI / 2;
          const flX = colX + Math.cos(flA) * colR * 0.85;
          ctx.beginPath();
          ctx.moveTo(flX, screenPos.y - 1 * s);
          ctx.lineTo(flX, screenPos.y - colH + 13 * s);
          ctx.stroke();
        }
        ctx.strokeStyle = iceHighlight;
        ctx.lineWidth = 2 * s;
        ctx.globalAlpha = 0.15;
        ctx.beginPath();
        ctx.moveTo(colX - colR * 0.4, screenPos.y - 2 * s);
        ctx.lineTo(colX - colR * 0.4, screenPos.y - colH + 14 * s);
        ctx.stroke();
        ctx.globalAlpha = 1;

        // === SECONDARY BROKEN COLUMN (right) ===
        const col2X = screenPos.x + 22 * s;
        const col2R = 5 * s;
        const col2H = 24 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          col2X,
          screenPos.y + 4 * s,
          col2R + 2 * s,
          (col2R + 2 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          col2X,
          screenPos.y + 1 * s,
          col2R,
          col2R * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(col2X + col2R, screenPos.y - col2H + 8 * s);
        ctx.ellipse(
          col2X,
          screenPos.y - col2H + 8 * s,
          col2R,
          col2R * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const c2G = ctx.createLinearGradient(
          col2X - col2R,
          0,
          col2X + col2R,
          0
        );
        c2G.addColorStop(0, stoneMid);
        c2G.addColorStop(0.3, stoneLight);
        c2G.addColorStop(1, stoneDark);
        ctx.fillStyle = c2G;
        ctx.beginPath();
        ctx.ellipse(
          col2X,
          screenPos.y + 1 * s,
          col2R,
          col2R * 0.5,
          0,
          0,
          Math.PI
        );
        ctx.lineTo(col2X - col2R, screenPos.y - col2H + 8 * s);
        ctx.ellipse(
          col2X,
          screenPos.y - col2H + 8 * s,
          col2R,
          col2R * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(col2X - col2R, screenPos.y - col2H + 8 * s);
        ctx.lineTo(col2X - col2R + 2 * s, screenPos.y - col2H + 3 * s);
        ctx.lineTo(col2X + col2R, screenPos.y - col2H + 8 * s);
        ctx.ellipse(
          col2X,
          screenPos.y - col2H + 8 * s,
          col2R,
          col2R * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();

        // === THIRD STUB COLUMN (far left) ===
        const col3X = screenPos.x - 18 * s;
        const col3R = 4 * s;
        const col3H = 14 * s;
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(
          col3X,
          screenPos.y + 3 * s,
          col3R + 1.5 * s,
          (col3R + 1.5 * s) * 0.5,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          col3X,
          screenPos.y + 1 * s,
          col3R,
          col3R * 0.5,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.lineTo(col3X + col3R, screenPos.y - col3H + 6 * s);
        ctx.ellipse(
          col3X,
          screenPos.y - col3H + 6 * s,
          col3R,
          col3R * 0.5,
          0,
          0,
          Math.PI,
          true
        );
        ctx.closePath();
        ctx.fill();
        const c3G = ctx.createLinearGradient(
          col3X - col3R,
          0,
          col3X + col3R,
          0
        );
        c3G.addColorStop(0, stoneMid);
        c3G.addColorStop(0.3, stoneLight);
        c3G.addColorStop(1, stoneDark);
        ctx.fillStyle = c3G;
        ctx.beginPath();
        ctx.ellipse(
          col3X,
          screenPos.y + 1 * s,
          col3R,
          col3R * 0.5,
          0,
          0,
          Math.PI
        );
        ctx.lineTo(col3X - col3R, screenPos.y - col3H + 6 * s);
        ctx.ellipse(
          col3X,
          screenPos.y - col3H + 6 * s,
          col3R,
          col3R * 0.5,
          0,
          Math.PI,
          Math.PI * 2,
          true
        );
        ctx.closePath();
        ctx.fill();

        // === FALLEN COLUMN SEGMENT (foreground) ===
        const fcX = screenPos.x - 6 * s;
        const fcY = screenPos.y + 12 * s;
        const fcR = 4.5 * s;
        const fcLen = 16 * s;
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          fcX + fcLen * 0.65,
          fcY - fcLen * 0.32,
          fcR,
          fcR * 0.5,
          0.6,
          0,
          Math.PI * 2
        );
        ctx.fill();
        const fcG = ctx.createLinearGradient(fcX, fcY - fcR, fcX, fcY + fcR);
        fcG.addColorStop(0, stoneLight);
        fcG.addColorStop(0.5, stoneMid);
        fcG.addColorStop(1, stoneDark);
        ctx.fillStyle = fcG;
        ctx.beginPath();
        ctx.moveTo(fcX, fcY - fcR * 0.5);
        ctx.lineTo(fcX + fcLen * 0.65, fcY - fcLen * 0.32 - fcR * 0.5);
        ctx.ellipse(
          fcX + fcLen * 0.65,
          fcY - fcLen * 0.32,
          fcR,
          fcR * 0.5,
          0.6,
          -Math.PI / 2,
          Math.PI / 2
        );
        ctx.lineTo(fcX, fcY + fcR * 0.5);
        ctx.ellipse(
          fcX,
          fcY,
          fcR,
          fcR * 0.5,
          0,
          Math.PI / 2,
          -Math.PI / 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(fcX, fcY, fcR, fcR * 0.45, 0, 0, Math.PI * 2);
        ctx.fill();

        // === SECOND FALLEN PIECE (back-left, angled opposite) ===
        const fc2X = screenPos.x - 28 * s;
        const fc2Y = screenPos.y + 10 * s;
        const fc2R = 4 * s;
        const fc2Len = 14 * s;
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          fc2X - fc2Len * 0.5,
          fc2Y - fc2Len * 0.2,
          fc2R,
          fc2R * 0.5,
          -0.4,
          0,
          Math.PI * 2
        );
        ctx.fill();
        const fc2G = ctx.createLinearGradient(
          fc2X,
          fc2Y - fc2R,
          fc2X,
          fc2Y + fc2R
        );
        fc2G.addColorStop(0, stoneLight);
        fc2G.addColorStop(0.5, stoneMid);
        fc2G.addColorStop(1, stoneDark);
        ctx.fillStyle = fc2G;
        ctx.beginPath();
        ctx.moveTo(fc2X, fc2Y - fc2R * 0.5);
        ctx.lineTo(fc2X - fc2Len * 0.5, fc2Y - fc2Len * 0.2 - fc2R * 0.5);
        ctx.ellipse(
          fc2X - fc2Len * 0.5,
          fc2Y - fc2Len * 0.2,
          fc2R,
          fc2R * 0.5,
          -0.4,
          -Math.PI / 2,
          Math.PI / 2
        );
        ctx.lineTo(fc2X, fc2Y + fc2R * 0.5);
        ctx.ellipse(
          fc2X,
          fc2Y,
          fc2R,
          fc2R * 0.5,
          0,
          Math.PI / 2,
          -Math.PI / 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(fc2X, fc2Y, fc2R, fc2R * 0.4, 0, 0, Math.PI * 2);
        ctx.fill();

        // === BROKEN ARCH FRAGMENT (right-back) ===
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 26 * s, screenPos.y);
        ctx.lineTo(screenPos.x + 24 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 28 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y - 16 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y + 3 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 34 * s, screenPos.y + 3 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y - 16 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 12 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y + 6 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 24 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 28 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x + 34 * s, screenPos.y - 16 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 12 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.4 * s;
        for (let bRow = 0; bRow < 3; bRow++) {
          const bY = screenPos.y - 14 * s + bRow * 5 * s;
          ctx.beginPath();
          ctx.moveTo(screenPos.x + 25 * s, bY);
          ctx.lineTo(screenPos.x + 33 * s, bY + 1 * s);
          ctx.stroke();
        }

        // === SCATTERED 3D RUBBLE ===
        [
          { sz: 5, x: -34, y: 10 },
          { sz: 4, x: -20, y: 14 },
          { sz: 3.5, x: -12, y: 16 },
          { sz: 4.5, x: 10, y: 14 },
          { sz: 3.5, x: 20, y: 12 },
          { sz: 4, x: 32, y: 9 },
          { sz: 3, x: -28, y: 13 },
          { sz: 3, x: 14, y: 8 },
          { sz: 3, x: 36, y: 5 },
          { sz: 2.5, x: -4, y: 18 },
          { sz: 3, x: 6, y: 18 },
          { sz: 3.5, x: -38, y: 6 },
          { sz: 3, x: 40, y: 2 },
          { sz: 3, x: -16, y: 16 },
        ].forEach((rb, idx) => {
          const rx = screenPos.x + rb.x * s;
          const ry = screenPos.y + rb.y * s;
          const rSz = rb.sz * s;
          ctx.fillStyle =
            idx % 3 === 0 ? stoneLight : idx % 3 === 1 ? stoneMid : stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx, ry - rSz * 0.8);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx - rSz * 0.7, ry - rSz * 0.2);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.8, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz * 0.2, ry + rSz * 0.6);
          ctx.closePath();
          ctx.fill();
        });
        ctx.fillStyle = stoneShadow;
        [
          [-30, 15],
          [-16, 17],
          [-2, 19],
          [8, 18],
          [18, 16],
          [28, 12],
          [36, 8],
          [-24, 18],
          [40, 5],
          [-8, 14],
          [12, 10],
          [-36, 8],
        ].forEach(([px, py], i) => {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + px * s,
            screenPos.y + py * s,
            (1.5 + (i % 3) * 0.5) * s,
            (0.8 + (i % 2) * 0.3) * s,
            i * 0.6,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });

        // === BROKEN FLOOR TILES ===
        [
          { d: 3.5, w: 7, x: -16, y: 5 },
          { d: 3, w: 6, x: 12, y: 6 },
          { d: 2.5, w: 5, x: -6, y: 3 },
          { d: 2.5, w: 5, x: 20, y: 3 },
          { d: 2.5, w: 5, x: -26, y: 4 },
        ].forEach((t) => {
          const tx = screenPos.x + t.x * s;
          const ty = screenPos.y + t.y * s;
          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(tx, ty - t.d * s);
          ctx.lineTo(tx + t.w * s, ty);
          ctx.lineTo(tx, ty + t.d * s);
          ctx.lineTo(tx - t.w * s, ty);
          ctx.closePath();
          ctx.fill();
          ctx.strokeStyle = stoneShadow;
          ctx.lineWidth = 0.3 * s;
          ctx.stroke();
        });

        // === FROST MOSS & ICE DETAILS ===
        ctx.fillStyle = frostMoss;
        ctx.globalAlpha = 0.6;
        ctx.beginPath();
        ctx.ellipse(
          colX - 2 * s,
          screenPos.y - 20 * s,
          4 * s,
          2 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          colX + 4 * s,
          screenPos.y - 8 * s,
          3.5 * s,
          1.8 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          wX + 12 * s,
          screenPos.y - 8 * s,
          4 * s,
          2 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 30 * s,
          screenPos.y - 4 * s,
          3 * s,
          1.5 * s,
          -0.4,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          col3X + 3 * s,
          screenPos.y - 4 * s,
          3 * s,
          1.5 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;
        // Ice frost patches
        ctx.fillStyle = iceHighlight;
        ctx.globalAlpha = 0.15;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 10 * s,
          hexY - 2 * s,
          8 * s,
          4 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 14 * s,
          hexY + 1 * s,
          6 * s,
          3 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          colX,
          screenPos.y - colH + 18 * s,
          4 * s,
          2 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          col2X,
          screenPos.y - col2H + 12 * s,
          3 * s,
          1.5 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;

        // === WALL CRACKS ===
        ctx.strokeStyle = "#2a2a1a";
        ctx.lineWidth = 0.7 * s;
        ctx.beginPath();
        ctx.moveTo(wX + 14 * s, wTopY + 8 * s);
        ctx.lineTo(wX + 16 * s, screenPos.y - 16 * s);
        ctx.lineTo(wX + 14 * s, screenPos.y - 8 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 6 * s, hexY - 3 * s);
        ctx.lineTo(screenPos.x - 3 * s, hexY + 4 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 28 * s, screenPos.y - 14 * s);
        ctx.lineTo(screenPos.x + 30 * s, screenPos.y - 6 * s);
        ctx.stroke();
      } else {
        // VARIANT 6: Grand ruined temple complex

        // Crumbled foundation — ground shadow
        const groundShadow = ctx.createRadialGradient(
          screenPos.x,
          screenPos.y + 8 * s,
          0,
          screenPos.x,
          screenPos.y + 8 * s,
          50 * s
        );
        groundShadow.addColorStop(0, "rgba(10, 10, 8, 0.35)");
        groundShadow.addColorStop(0.6, "rgba(10, 10, 8, 0.15)");
        groundShadow.addColorStop(1, "rgba(10, 10, 8, 0)");
        ctx.fillStyle = groundShadow;
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x,
          screenPos.y + 8 * s,
          52 * s,
          26 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Broken stone slabs at varying heights and angles
        const slabColors = [stoneMid, "#5a5a4a", "#4a4a3a", stoneDark];
        const slabs = [
          { d: 8, h: 4, jOff: 1.2, w: 16, x: -32, y: 4 },
          { d: 7, h: 3, jOff: 2.4, w: 14, x: -12, y: 2 },
          { d: 6, h: 5, jOff: 0.8, w: 12, x: 10, y: 3 },
          { d: 6.5, h: 3.5, jOff: 3.1, w: 13, x: 28, y: 6 },
          { d: 5, h: 2.5, jOff: 1.8, w: 10, x: -24, y: 12 },
          { d: 5.5, h: 2, jOff: 0.5, w: 11, x: 2, y: 10 },
          { d: 4.5, h: 3, jOff: 2.9, w: 9, x: 20, y: 12 },
          { d: 4, h: 2, jOff: 3.6, w: 8, x: -40, y: 8 },
          { d: 4.5, h: 2.5, jOff: 1, w: 9, x: 38, y: 2 },
        ];
        slabs.forEach((sl, i) => {
          const sx = screenPos.x + sl.x * s;
          const sy = screenPos.y + sl.y * s;
          const sw = sl.w * s;
          const sd = sl.d * s;
          const sh = sl.h * s;
          const jag = Math.sin(sl.jOff) * 1.5 * s;
          ctx.fillStyle = slabColors[i % slabColors.length];
          ctx.beginPath();
          ctx.moveTo(sx, sy - sd);
          ctx.lineTo(sx + sw * 0.5 + jag, sy - sd * 0.4);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5);
          ctx.lineTo(sx - sw * 0.45, sy - sd * 0.1 + jag);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(sx + sw * 0.5 + jag, sy - sd * 0.4);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5 + sh);
          ctx.lineTo(sx + sw * 0.5 + jag, sy - sd * 0.4 + sh);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneShadow;
          ctx.beginPath();
          ctx.moveTo(sx - sw * 0.45, sy - sd * 0.1 + jag);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5);
          ctx.lineTo(sx + sw * 0.1, sy + sd * 0.5 + sh);
          ctx.lineTo(sx - sw * 0.45, sy - sd * 0.1 + jag + sh);
          ctx.closePath();
          ctx.fill();
          if (i % 2 === 0) {
            ctx.strokeStyle = "#1a1a12";
            ctx.lineWidth = 0.5 * s;
            ctx.beginPath();
            ctx.moveTo(sx - sw * 0.2, sy - sd * 0.6);
            ctx.lineTo(sx + sw * 0.1, sy - sd * 0.1);
            ctx.lineTo(sx + sw * 0.25, sy + sd * 0.1);
            ctx.stroke();
          }
        });

        // Small stone fragments between slabs
        [
          { sz: 2.5, x: -18, y: 8 },
          { sz: 2, x: 6, y: 7 },
          { sz: 1.8, x: -6, y: 14 },
          { sz: 2.2, x: 16, y: 9 },
          { sz: 1.5, x: -30, y: 10 },
          { sz: 1.8, x: 32, y: 8 },
          { sz: 1.5, x: -2, y: 16 },
          { sz: 2, x: 24, y: 14 },
          { sz: 1.2, x: -14, y: 16 },
          { sz: 1.6, x: 8, y: 15 },
          { sz: 1.8, x: -36, y: 12 },
          { sz: 1.5, x: 42, y: 4 },
        ].forEach((f, i) => {
          const fx = screenPos.x + f.x * s;
          const fy = screenPos.y + f.y * s;
          const fSz = f.sz * s;
          ctx.fillStyle =
            i % 3 === 0 ? stoneLight : i % 3 === 1 ? stoneMid : stoneDark;
          ctx.beginPath();
          ctx.moveTo(fx, fy - fSz);
          ctx.lineTo(fx + fSz * 0.7, fy - fSz * 0.15);
          ctx.lineTo(fx + fSz * 0.2, fy + fSz * 0.5);
          ctx.lineTo(fx - fSz * 0.6, fy + fSz * 0.1);
          ctx.closePath();
          ctx.fill();
        });

        // Exposed dirt patches between broken stones
        ctx.fillStyle = "rgba(30, 35, 25, 0.25)";
        [
          { rx: 6, ry: 3, x: -20, y: 8 },
          { rx: 5, ry: 2.5, x: 8, y: 12 },
          { rx: 7, ry: 3, x: -8, y: 14 },
          { rx: 5, ry: 2.5, x: 28, y: 10 },
        ].forEach((p) => {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + p.x * s,
            screenPos.y + p.y * s,
            p.rx * s,
            p.ry * s,
            0.15,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });

        // Back wall facade
        const wallX = screenPos.x - 34 * s;
        const wallTopY = screenPos.y - 52 * s;
        const wallW = 68 * s;
        const wallThick = 7 * s;

        const wallLeftGrad = ctx.createLinearGradient(
          wallX,
          screenPos.y,
          wallX - wallThick,
          screenPos.y
        );
        wallLeftGrad.addColorStop(0, stoneDark);
        wallLeftGrad.addColorStop(1, stoneShadow);
        ctx.fillStyle = wallLeftGrad;
        ctx.beginPath();
        ctx.moveTo(wallX - wallThick, screenPos.y + 2 * s);
        ctx.lineTo(wallX - wallThick, wallTopY + 14 * s);
        ctx.lineTo(wallX - wallThick + 3 * s, wallTopY + 9 * s);
        ctx.lineTo(wallX - wallThick + 5 * s, wallTopY + 16 * s);
        ctx.lineTo(wallX, wallTopY + 5 * s);
        ctx.lineTo(wallX, screenPos.y - 2 * s);
        ctx.closePath();
        ctx.fill();

        const wallFrontGrad = ctx.createLinearGradient(
          wallX,
          wallTopY,
          wallX + wallW,
          screenPos.y
        );
        wallFrontGrad.addColorStop(0, stoneMid);
        wallFrontGrad.addColorStop(0.3, stoneLight);
        wallFrontGrad.addColorStop(0.7, stoneMid);
        wallFrontGrad.addColorStop(1, stoneDark);
        ctx.fillStyle = wallFrontGrad;
        ctx.beginPath();
        ctx.moveTo(wallX, screenPos.y - 2 * s);
        ctx.lineTo(wallX, wallTopY + 5 * s);
        ctx.lineTo(wallX + 8 * s, wallTopY);
        ctx.lineTo(wallX + 15 * s, wallTopY + 8 * s);
        ctx.lineTo(wallX + 22 * s, wallTopY + 3 * s);
        ctx.lineTo(wallX + 30 * s, wallTopY + 12 * s);
        ctx.lineTo(wallX + 38 * s, wallTopY + 5 * s);
        ctx.lineTo(wallX + 45 * s, wallTopY + 14 * s);
        ctx.lineTo(wallX + 52 * s, wallTopY + 8 * s);
        ctx.lineTo(wallX + 58 * s, wallTopY + 16 * s);
        ctx.lineTo(wallX + wallW, wallTopY + 20 * s);
        ctx.lineTo(wallX + wallW, screenPos.y + 2 * s);
        ctx.closePath();
        ctx.fill();

        // Wall top edge
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(wallX - wallThick, wallTopY + 14 * s);
        ctx.lineTo(wallX - wallThick + 3 * s, wallTopY + 9 * s);
        ctx.lineTo(wallX, wallTopY + 5 * s);
        ctx.lineTo(wallX + 8 * s, wallTopY);
        ctx.lineTo(wallX + 15 * s, wallTopY + 8 * s);
        ctx.lineTo(wallX + 22 * s, wallTopY + 3 * s);
        ctx.lineTo(wallX + 38 * s, wallTopY + 5 * s);
        ctx.lineTo(wallX + 52 * s, wallTopY + 8 * s);
        ctx.lineTo(wallX + wallW, wallTopY + 20 * s);
        ctx.lineTo(wallX + wallW - wallThick, wallTopY + 17 * s);
        ctx.closePath();
        ctx.fill();

        // Brick lines
        ctx.strokeStyle = stoneShadow;
        ctx.lineWidth = 0.5 * s;
        for (let row = 0; row < 7; row++) {
          const rowY = wallTopY + 22 * s + row * 6 * s;
          if (rowY < screenPos.y) {
            ctx.beginPath();
            ctx.moveTo(wallX + 2 * s, rowY);
            ctx.lineTo(wallX + wallW - 2 * s, rowY + 2 * s);
            ctx.stroke();
          }
        }

        // Arched niche
        const nicheX = wallX + 24 * s;
        const nicheY = screenPos.y - 38 * s;
        ctx.fillStyle = "#1a1a15";
        ctx.beginPath();
        ctx.moveTo(nicheX, nicheY + 12 * s);
        ctx.lineTo(nicheX, nicheY + 3 * s);
        ctx.arc(nicheX + 6 * s, nicheY + 3 * s, 6 * s, Math.PI, 0);
        ctx.lineTo(nicheX + 12 * s, nicheY + 12 * s);
        ctx.closePath();
        ctx.fill();

        // Columns at varying heights
        const columns = [
          { broken: false, cx: -28, h: 34, r: 5 },
          { broken: false, cx: -14, h: 46, r: 5.5 },
          { broken: false, cx: 0, h: 40, r: 6 },
          { broken: true, cx: 14, h: 24, r: 5.5 },
          { broken: true, cx: 28, h: 11, r: 5 },
        ];
        columns.forEach((col) => {
          const cx = screenPos.x + col.cx * s;
          const cy = screenPos.y;
          const cr = col.r * s;
          const ch = col.h * s;

          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.ellipse(
            cx,
            cy + 4 * s,
            cr + 3 * s,
            (cr + 3 * s) * 0.5,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();

          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.ellipse(cx, cy + 2 * s, cr, cr * 0.5, 0, Math.PI, Math.PI * 2);
          ctx.lineTo(cx + cr, cy - ch + 10 * s);
          ctx.ellipse(cx, cy - ch + 10 * s, cr, cr * 0.5, 0, 0, Math.PI, true);
          ctx.closePath();
          ctx.fill();

          const cGrad = ctx.createLinearGradient(cx - cr, cy, cx + cr, cy);
          cGrad.addColorStop(0, stoneMid);
          cGrad.addColorStop(0.25, stoneLight);
          cGrad.addColorStop(0.6, stoneMid);
          cGrad.addColorStop(1, stoneDark);
          ctx.fillStyle = cGrad;
          ctx.beginPath();
          ctx.ellipse(cx, cy + 2 * s, cr, cr * 0.5, 0, 0, Math.PI);
          ctx.lineTo(cx - cr, cy - ch + 10 * s);
          ctx.ellipse(
            cx,
            cy - ch + 10 * s,
            cr,
            cr * 0.5,
            0,
            Math.PI,
            Math.PI * 2,
            true
          );
          ctx.closePath();
          ctx.fill();

          ctx.strokeStyle = stoneShadow;
          ctx.lineWidth = 0.6 * s;
          for (let fl = 0; fl < 4; fl++) {
            const flA = (fl / 4) * Math.PI - Math.PI / 2;
            const flX = cx + Math.cos(flA) * cr * 0.85;
            ctx.beginPath();
            ctx.moveTo(flX, cy + 1 * s);
            ctx.lineTo(flX, cy - ch + 12 * s);
            ctx.stroke();
          }

          if (!col.broken) {
            ctx.fillStyle = stoneLight;
            ctx.beginPath();
            ctx.ellipse(
              cx,
              cy - ch + 5 * s,
              cr + 2 * s,
              (cr + 2 * s) * 0.5,
              0,
              0,
              Math.PI * 2
            );
            ctx.fill();
          } else {
            ctx.fillStyle = stoneLight;
            ctx.beginPath();
            ctx.moveTo(cx - cr, cy - ch + 10 * s);
            ctx.lineTo(cx - cr + 2 * s, cy - ch + 5 * s);
            ctx.lineTo(cx + 2 * s, cy - ch + 3 * s);
            ctx.lineTo(cx + cr, cy - ch + 10 * s);
            ctx.ellipse(
              cx,
              cy - ch + 10 * s,
              cr,
              cr * 0.5,
              0,
              0,
              Math.PI,
              true
            );
            ctx.closePath();
            ctx.fill();
          }
        });

        // === FALLEN COLUMN SEGMENT (foreground) ===
        const fcX = screenPos.x + 16 * s;
        const fcY = screenPos.y + 12 * s;
        const fcR = 5 * s;
        const fcLen = 18 * s;
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          fcX + fcLen * 0.6,
          fcY - fcLen * 0.3,
          fcR,
          fcR * 0.5,
          0.5,
          0,
          Math.PI * 2
        );
        ctx.fill();
        const fcG = ctx.createLinearGradient(fcX, fcY - fcR, fcX, fcY + fcR);
        fcG.addColorStop(0, stoneLight);
        fcG.addColorStop(0.5, stoneMid);
        fcG.addColorStop(1, stoneDark);
        ctx.fillStyle = fcG;
        ctx.beginPath();
        ctx.moveTo(fcX, fcY - fcR * 0.5);
        ctx.lineTo(fcX + fcLen * 0.6, fcY - fcLen * 0.3 - fcR * 0.5);
        ctx.ellipse(
          fcX + fcLen * 0.6,
          fcY - fcLen * 0.3,
          fcR,
          fcR * 0.5,
          0.5,
          -Math.PI / 2,
          Math.PI / 2
        );
        ctx.lineTo(fcX, fcY + fcR * 0.5);
        ctx.ellipse(
          fcX,
          fcY,
          fcR,
          fcR * 0.5,
          0,
          Math.PI / 2,
          -Math.PI / 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(fcX, fcY, fcR, fcR * 0.45, 0, 0, Math.PI * 2);
        ctx.fill();

        // === SECOND FALLEN PIECE (back-left) ===
        const fc2X = screenPos.x - 30 * s;
        const fc2Y = screenPos.y + 10 * s;
        const fc2R = 4 * s;
        const fc2Len = 14 * s;
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.ellipse(
          fc2X - fc2Len * 0.5,
          fc2Y - fc2Len * 0.2,
          fc2R,
          fc2R * 0.5,
          -0.4,
          0,
          Math.PI * 2
        );
        ctx.fill();
        const fc2G = ctx.createLinearGradient(
          fc2X,
          fc2Y - fc2R,
          fc2X,
          fc2Y + fc2R
        );
        fc2G.addColorStop(0, stoneLight);
        fc2G.addColorStop(0.5, stoneMid);
        fc2G.addColorStop(1, stoneDark);
        ctx.fillStyle = fc2G;
        ctx.beginPath();
        ctx.moveTo(fc2X, fc2Y - fc2R * 0.5);
        ctx.lineTo(fc2X - fc2Len * 0.5, fc2Y - fc2Len * 0.2 - fc2R * 0.5);
        ctx.ellipse(
          fc2X - fc2Len * 0.5,
          fc2Y - fc2Len * 0.2,
          fc2R,
          fc2R * 0.5,
          -0.4,
          -Math.PI / 2,
          Math.PI / 2
        );
        ctx.lineTo(fc2X, fc2Y + fc2R * 0.5);
        ctx.ellipse(
          fc2X,
          fc2Y,
          fc2R,
          fc2R * 0.5,
          0,
          Math.PI / 2,
          -Math.PI / 2,
          true
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.ellipse(fc2X, fc2Y, fc2R, fc2R * 0.4, 0, 0, Math.PI * 2);
        ctx.fill();

        // === BROKEN ARCH FRAGMENT (right side) ===
        ctx.fillStyle = stoneDark;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 30 * s, screenPos.y + 2 * s);
        ctx.lineTo(screenPos.x + 28 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 32 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 16 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y + 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneMid;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 38 * s, screenPos.y + 5 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 16 * s);
        ctx.lineTo(screenPos.x + 42 * s, screenPos.y - 12 * s);
        ctx.lineTo(screenPos.x + 42 * s, screenPos.y + 8 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(screenPos.x + 28 * s, screenPos.y - 18 * s);
        ctx.lineTo(screenPos.x + 32 * s, screenPos.y - 22 * s);
        ctx.lineTo(screenPos.x + 38 * s, screenPos.y - 16 * s);
        ctx.lineTo(screenPos.x + 42 * s, screenPos.y - 12 * s);
        ctx.closePath();
        ctx.fill();

        // === 3D RUBBLE FIELD ===
        [
          { sz: 5, x: -40, y: 12 },
          { sz: 4, x: -24, y: 15 },
          { sz: 3.5, x: -12, y: 16 },
          { sz: 4.5, x: 8, y: 16 },
          { sz: 3.5, x: 22, y: 14 },
          { sz: 4, x: 36, y: 10 },
          { sz: 3, x: -32, y: 14 },
          { sz: 3, x: 16, y: 10 },
          { sz: 3, x: 40, y: 6 },
          { sz: 2.5, x: -6, y: 18 },
          { sz: 3, x: 4, y: 19 },
          { sz: 3.5, x: -38, y: 8 },
          { sz: 3, x: 44, y: 3 },
          { sz: 4, x: -18, y: 8 },
        ].forEach((rb, idx) => {
          const rx = screenPos.x + rb.x * s;
          const ry = screenPos.y + rb.y * s;
          const rSz = rb.sz * s;
          ctx.fillStyle =
            idx % 3 === 0 ? stoneLight : idx % 3 === 1 ? stoneMid : stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx, ry - rSz * 0.8);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx - rSz * 0.7, ry - rSz * 0.2);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDark;
          ctx.beginPath();
          ctx.moveTo(rx + rSz * 0.3, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz, ry - rSz * 0.3);
          ctx.lineTo(rx + rSz * 0.8, ry + rSz * 0.2);
          ctx.lineTo(rx + rSz * 0.2, ry + rSz * 0.6);
          ctx.closePath();
          ctx.fill();
        });
        ctx.fillStyle = stoneShadow;
        [
          [-36, 16],
          [-20, 18],
          [-4, 20],
          [10, 19],
          [24, 16],
          [38, 12],
          [-28, 18],
          [16, 14],
          [42, 8],
          [-8, 14],
        ].forEach(([px, py], i) => {
          ctx.beginPath();
          ctx.ellipse(
            screenPos.x + px * s,
            screenPos.y + py * s,
            (1.5 + (i % 3) * 0.5) * s,
            (0.8 + (i % 2) * 0.3) * s,
            i * 0.6,
            0,
            Math.PI * 2
          );
          ctx.fill();
        });

        // === FLOOR TILES ===
        [
          { d: 3.5, w: 7, x: -20, y: 6 },
          { d: 3, w: 6, x: 10, y: 7 },
          { d: 2.5, w: 5, x: -4, y: 4 },
          { d: 2.5, w: 5, x: 26, y: 4 },
          { d: 2.5, w: 5, x: -34, y: 4 },
        ].forEach((t) => {
          const tx = screenPos.x + t.x * s;
          const ty = screenPos.y + t.y * s;
          ctx.fillStyle = stoneMid;
          ctx.beginPath();
          ctx.moveTo(tx, ty - t.d * s);
          ctx.lineTo(tx + t.w * s, ty);
          ctx.lineTo(tx, ty + t.d * s);
          ctx.lineTo(tx - t.w * s, ty);
          ctx.closePath();
          ctx.fill();
          ctx.strokeStyle = stoneShadow;
          ctx.lineWidth = 0.3 * s;
          ctx.stroke();
        });

        // === MOSS PATCHES ===
        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.7;
        ctx.beginPath();
        ctx.ellipse(
          wallX + 15 * s,
          screenPos.y - 14 * s,
          6 * s,
          3 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 12 * s,
          screenPos.y + 5 * s,
          7 * s,
          3.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 20 * s,
          screenPos.y + 3 * s,
          5 * s,
          2.5 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 30 * s,
          screenPos.y - 6 * s,
          4 * s,
          2 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.globalAlpha = 1;

        // === HANGING VINES with leaves ===
        ctx.strokeStyle = vineColor;
        ctx.lineWidth = 1.5 * s;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(wallX + 12 * s, wallTopY + 12 * s);
        ctx.quadraticCurveTo(
          wallX + 8 * s,
          screenPos.y - 18 * s,
          wallX + 14 * s,
          screenPos.y - 6 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(screenPos.x - 12 * s, screenPos.y - 38 * s);
        ctx.quadraticCurveTo(
          screenPos.x - 6 * s,
          screenPos.y - 24 * s,
          screenPos.x - 10 * s,
          screenPos.y - 10 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(wallX + 42 * s, wallTopY + 14 * s);
        ctx.quadraticCurveTo(
          wallX + 46 * s,
          screenPos.y - 22 * s,
          wallX + 40 * s,
          screenPos.y - 8 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(wallX + 56 * s, wallTopY + 18 * s);
        ctx.quadraticCurveTo(
          wallX + 52 * s,
          screenPos.y - 16 * s,
          wallX + 58 * s,
          screenPos.y - 4 * s
        );
        ctx.stroke();

        ctx.fillStyle = mossColor;
        ctx.globalAlpha = 0.85;
        [
          [wallX + 10 * s, screenPos.y - 14 * s],
          [wallX + 13 * s, screenPos.y - 8 * s],
          [screenPos.x - 8 * s, screenPos.y - 26 * s],
          [screenPos.x + 6 * s, screenPos.y - 13 * s],
          [wallX + 44 * s, screenPos.y - 16 * s],
          [wallX + 42 * s, screenPos.y - 10 * s],
          [wallX + 54 * s, screenPos.y - 10 * s],
        ].forEach(([lx, ly]) => {
          ctx.beginPath();
          ctx.ellipse(lx, ly, 2.5 * s, 1.2 * s, 0.5, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.globalAlpha = 1;

        // === WATER STAINS (swamp theme) ===
        ctx.fillStyle = "rgba(40, 80, 60, 0.15)";
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x - 10 * s,
          screenPos.y - 8 * s,
          12 * s,
          6 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(
          screenPos.x + 20 * s,
          screenPos.y + 2 * s,
          10 * s,
          5 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // === CRACKS on walls ===
        ctx.strokeStyle = "#1a1a12";
        ctx.lineWidth = 0.7 * s;
        ctx.beginPath();
        ctx.moveTo(wallX + 20 * s, wallTopY + 14 * s);
        ctx.lineTo(wallX + 22 * s, screenPos.y - 20 * s);
        ctx.lineTo(wallX + 18 * s, screenPos.y - 10 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(wallX + 48 * s, wallTopY + 16 * s);
        ctx.lineTo(wallX + 50 * s, screenPos.y - 18 * s);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(wallX + 34 * s, wallTopY + 16 * s);
        ctx.lineTo(wallX + 36 * s, screenPos.y - 14 * s);
        ctx.lineTo(wallX + 32 * s, screenPos.y - 4 * s);
        ctx.stroke();
      }

      // === REGIONAL OVERLAYS (variant-specific, isometric) ===
      drawRuinsRegionalOverlay(
        ctx,
        screenPos.x,
        screenPos.y,
        s,
        ruinVariant,
        mapTheme,
        vineColor,
        decorX,
        decorY
      );

      break;
    }
    case "bones": {
      // Isometric skull-and-crossbones on the ground with 3D shading
      const bnLit = "#f0ebe3";
      const bnMid = "#ddd5c8";
      const bnDrk = "#b8ad9e";
      const bnShd = "#8a7f72";

      // Ground shadow
      const bonesShadow = ctx.createRadialGradient(
        screenPos.x + 1 * s,
        screenPos.y + 3 * s,
        0,
        screenPos.x + 1 * s,
        screenPos.y + 3 * s,
        14 * s
      );
      bonesShadow.addColorStop(0, "rgba(0,0,0,0.2)");
      bonesShadow.addColorStop(0.7, "rgba(0,0,0,0.06)");
      bonesShadow.addColorStop(1, "rgba(0,0,0,0)");
      ctx.fillStyle = bonesShadow;
      ctx.beginPath();
      ctx.ellipse(
        screenPos.x + 1 * s,
        screenPos.y + 3 * s,
        14 * s,
        7 * s,
        0,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // === CROSSBONES — two bones crossed, lying flat ===
      ctx.save();
      ctx.translate(screenPos.x, screenPos.y);
      ctx.rotate(rotation);

      // Bone 1 (bottom-left to top-right)
      // Shadow stroke
      ctx.strokeStyle = bnShd;
      ctx.lineWidth = 2.8 * s;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(-10 * s, 5 * s);
      ctx.lineTo(10 * s, -1 * s);
      ctx.stroke();
      // Lit stroke
      ctx.strokeStyle = bnMid;
      ctx.lineWidth = 1.8 * s;
      ctx.beginPath();
      ctx.moveTo(-10 * s, 4.5 * s);
      ctx.lineTo(10 * s, -1.5 * s);
      ctx.stroke();
      // Highlight
      ctx.strokeStyle = bnLit;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(-9 * s, 4 * s);
      ctx.lineTo(9 * s, -2 * s);
      ctx.stroke();
      // Knobs at ends
      ctx.fillStyle = bnShd;
      ctx.beginPath();
      ctx.ellipse(-10 * s, 5 * s, 2.5 * s, 1.8 * s, 0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = bnMid;
      ctx.beginPath();
      ctx.ellipse(-9.5 * s, 4.5 * s, 1.8 * s, 1.3 * s, 0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = bnShd;
      ctx.beginPath();
      ctx.ellipse(10 * s, -1 * s, 2.5 * s, 1.8 * s, 0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = bnLit;
      ctx.beginPath();
      ctx.ellipse(10.5 * s, -1.5 * s, 1.8 * s, 1.3 * s, 0.3, 0, Math.PI * 2);
      ctx.fill();

      // Bone 2 (top-left to bottom-right)
      ctx.strokeStyle = bnShd;
      ctx.lineWidth = 2.8 * s;
      ctx.beginPath();
      ctx.moveTo(-10 * s, -1 * s);
      ctx.lineTo(10 * s, 5 * s);
      ctx.stroke();
      ctx.strokeStyle = bnDrk;
      ctx.lineWidth = 1.8 * s;
      ctx.beginPath();
      ctx.moveTo(-10 * s, -1.5 * s);
      ctx.lineTo(10 * s, 4.5 * s);
      ctx.stroke();
      ctx.strokeStyle = bnLit;
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(-9 * s, -2 * s);
      ctx.lineTo(9 * s, 4 * s);
      ctx.stroke();
      // Knobs at ends
      ctx.fillStyle = bnShd;
      ctx.beginPath();
      ctx.ellipse(-10 * s, -1 * s, 2.5 * s, 1.8 * s, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = bnDrk;
      ctx.beginPath();
      ctx.ellipse(-9.5 * s, -1.5 * s, 1.8 * s, 1.3 * s, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = bnShd;
      ctx.beginPath();
      ctx.ellipse(10 * s, 5 * s, 2.5 * s, 1.8 * s, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = bnMid;
      ctx.beginPath();
      ctx.ellipse(10.5 * s, 4.5 * s, 1.8 * s, 1.3 * s, -0.3, 0, Math.PI * 2);
      ctx.fill();

      ctx.restore();

      // === SKULL — isometric top-down, sitting on crossbones ===
      const bSkX = screenPos.x;
      const bSkY = screenPos.y - 3 * s;

      // Skull shadow volume
      ctx.fillStyle = bnShd;
      ctx.beginPath();
      ctx.ellipse(bSkX, bSkY + 0.8 * s, 6.5 * s, 4.5 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Main skull with radial gradient
      const bSkGrad = ctx.createRadialGradient(
        bSkX + 1 * s,
        bSkY - 1 * s,
        0.5 * s,
        bSkX,
        bSkY,
        6 * s
      );
      bSkGrad.addColorStop(0, bnLit);
      bSkGrad.addColorStop(0.4, bnMid);
      bSkGrad.addColorStop(0.8, bnDrk);
      bSkGrad.addColorStop(1, bnShd);
      ctx.fillStyle = bSkGrad;
      ctx.beginPath();
      ctx.ellipse(bSkX, bSkY, 6 * s, 4 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Skull specular highlight
      ctx.fillStyle = "rgba(255,255,255,0.18)";
      ctx.beginPath();
      ctx.ellipse(
        bSkX + 1.5 * s,
        bSkY - 1.5 * s,
        2.5 * s,
        1.5 * s,
        0.2,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Brow ridge
      ctx.fillStyle = bnDrk;
      ctx.beginPath();
      ctx.ellipse(bSkX, bSkY + 0.3 * s, 4.5 * s, 0.8 * s, 0, 0, Math.PI);
      ctx.fill();

      // Eye sockets
      const bLEye = ctx.createRadialGradient(
        bSkX - 2 * s,
        bSkY + 0.5 * s,
        0.2 * s,
        bSkX - 2 * s,
        bSkY + 0.5 * s,
        1.8 * s
      );
      bLEye.addColorStop(0, "#0d0a08");
      bLEye.addColorStop(0.5, "#2d2420");
      bLEye.addColorStop(1, bnShd);
      ctx.fillStyle = bLEye;
      ctx.beginPath();
      ctx.ellipse(
        bSkX - 2 * s,
        bSkY + 0.5 * s,
        1.8 * s,
        1.3 * s,
        0.06,
        0,
        Math.PI * 2
      );
      ctx.fill();

      const bREye = ctx.createRadialGradient(
        bSkX + 2 * s,
        bSkY + 0.5 * s,
        0.2 * s,
        bSkX + 2 * s,
        bSkY + 0.5 * s,
        1.8 * s
      );
      bREye.addColorStop(0, "#0d0a08");
      bREye.addColorStop(0.5, "#3d3430");
      bREye.addColorStop(1, bnDrk);
      ctx.fillStyle = bREye;
      ctx.beginPath();
      ctx.ellipse(
        bSkX + 2 * s,
        bSkY + 0.5 * s,
        1.8 * s,
        1.3 * s,
        -0.06,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Nasal cavity
      ctx.fillStyle = "#2d2420";
      ctx.beginPath();
      ctx.moveTo(bSkX, bSkY + 1.5 * s);
      ctx.lineTo(bSkX - 0.8 * s, bSkY + 2.8 * s);
      ctx.lineTo(bSkX + 0.6 * s, bSkY + 2.8 * s);
      ctx.closePath();
      ctx.fill();

      // Teeth
      ctx.fillStyle = bnLit;
      ctx.strokeStyle = bnShd;
      ctx.lineWidth = 0.25 * s;
      for (let t = -2; t <= 2; t++) {
        ctx.beginPath();
        ctx.rect(bSkX + t * 1.1 * s - 0.4 * s, bSkY + 3 * s, 0.8 * s, 0.9 * s);
        ctx.fill();
        ctx.stroke();
      }

      // Skull outline
      ctx.strokeStyle = bnShd;
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.ellipse(bSkX, bSkY, 6 * s, 4 * s, 0, 0, Math.PI * 2);
      ctx.stroke();

      break;
    }
    case "torch": {
      const tcX = screenPos.x;
      const tcY = screenPos.y;
      const tcD = 3 * s;

      // Warm ground glow
      const tcGndGlow = ctx.createRadialGradient(
        tcX,
        tcY + 2 * s,
        2 * s,
        tcX,
        tcY + 2 * s,
        28 * s
      );
      const tcPulse = 0.12 + Math.sin(decorTime * 4) * 0.04;
      tcGndGlow.addColorStop(0, `rgba(255,140,40,${tcPulse})`);
      tcGndGlow.addColorStop(0.4, `rgba(255,100,20,${tcPulse * 0.4})`);
      tcGndGlow.addColorStop(1, "rgba(255,60,10,0)");
      ctx.fillStyle = tcGndGlow;
      ctx.beginPath();
      ctx.ellipse(tcX, tcY + 2 * s, 28 * s, 14 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        tcX,
        tcY + 2 * s,
        s,
        8 * s,
        3 * s,
        30 * s,
        0.22
      );

      // Stone base — isometric slab
      const baseG = ctx.createLinearGradient(
        tcX - 6 * s,
        tcY,
        tcX + 6 * s,
        tcY
      );
      baseG.addColorStop(0, "#5a5550");
      baseG.addColorStop(0.4, "#807870");
      baseG.addColorStop(0.6, "#908880");
      baseG.addColorStop(1, "#4a4540");

      // Base top face
      ctx.fillStyle = "#908880";
      ctx.beginPath();
      ctx.moveTo(tcX, tcY - 2 * s);
      ctx.lineTo(tcX + 7 * s, tcY + 1.5 * s);
      ctx.lineTo(tcX, tcY + 5 * s);
      ctx.lineTo(tcX - 7 * s, tcY + 1.5 * s);
      ctx.closePath();
      ctx.fill();

      // Base front-left face
      ctx.fillStyle = baseG;
      ctx.beginPath();
      ctx.moveTo(tcX - 7 * s, tcY + 1.5 * s);
      ctx.lineTo(tcX, tcY + 5 * s);
      ctx.lineTo(tcX, tcY + 8 * s);
      ctx.lineTo(tcX - 7 * s, tcY + 4.5 * s);
      ctx.closePath();
      ctx.fill();

      // Base front-right face
      ctx.fillStyle = "#4a4540";
      ctx.beginPath();
      ctx.moveTo(tcX, tcY + 5 * s);
      ctx.lineTo(tcX + 7 * s, tcY + 1.5 * s);
      ctx.lineTo(tcX + 7 * s, tcY + 4.5 * s);
      ctx.lineTo(tcX, tcY + 8 * s);
      ctx.closePath();
      ctx.fill();

      // Wooden pole — isometric cylinder
      const poleG = ctx.createLinearGradient(
        tcX - 2.5 * s,
        0,
        tcX + 2.5 * s,
        0
      );
      poleG.addColorStop(0, "#3a2818");
      poleG.addColorStop(0.3, "#5a4030");
      poleG.addColorStop(0.5, "#6a5040");
      poleG.addColorStop(0.7, "#5a4030");
      poleG.addColorStop(1, "#2a1a10");

      // Pole front face
      ctx.fillStyle = poleG;
      ctx.beginPath();
      ctx.moveTo(tcX - 2.5 * s, tcY);
      ctx.lineTo(tcX + 2.5 * s, tcY);
      ctx.lineTo(tcX + 2.5 * s, tcY - 35 * s);
      ctx.lineTo(tcX - 2.5 * s, tcY - 35 * s);
      ctx.closePath();
      ctx.fill();

      // Pole side face
      ctx.fillStyle = "#2a1a10";
      ctx.beginPath();
      ctx.moveTo(tcX + 2.5 * s, tcY);
      ctx.lineTo(tcX + 2.5 * s + tcD * 0.5, tcY - tcD * 0.25);
      ctx.lineTo(tcX + 2.5 * s + tcD * 0.5, tcY - 35 * s - tcD * 0.25);
      ctx.lineTo(tcX + 2.5 * s, tcY - 35 * s);
      ctx.closePath();
      ctx.fill();

      // Pole wood grain
      ctx.strokeStyle = "rgba(20,12,5,0.2)";
      ctx.lineWidth = 0.4 * s;
      for (let pg = 0; pg < 4; pg++) {
        const pgx = tcX + (-1 + pg * 0.8) * s;
        ctx.beginPath();
        ctx.moveTo(pgx, tcY - 2 * s);
        ctx.lineTo(pgx + 0.3 * s, tcY - 33 * s);
        ctx.stroke();
      }

      // Iron bracket/cradle at top
      const brY = tcY - 33 * s;
      ctx.strokeStyle = "#3a3535";
      ctx.lineWidth = 1.5 * s;
      ctx.lineCap = "round";
      // Bracket arms
      ctx.beginPath();
      ctx.moveTo(tcX - 5 * s, brY + 2 * s);
      ctx.quadraticCurveTo(tcX - 6 * s, brY - 3 * s, tcX - 4 * s, brY - 5 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(tcX + 5 * s, brY + 2 * s);
      ctx.quadraticCurveTo(tcX + 6 * s, brY - 3 * s, tcX + 4 * s, brY - 5 * s);
      ctx.stroke();
      // Bracket base ring
      ctx.strokeStyle = "#4a4545";
      ctx.lineWidth = 1.2 * s;
      ctx.beginPath();
      ctx.ellipse(tcX, brY + 2 * s, 5 * s, 2 * s, 0, 0, Math.PI * 2);
      ctx.stroke();

      // Iron highlight
      ctx.strokeStyle = "rgba(120,115,110,0.3)";
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.moveTo(tcX - 4.5 * s, brY + 1 * s);
      ctx.quadraticCurveTo(
        tcX - 5.5 * s,
        brY - 3 * s,
        tcX - 3.5 * s,
        brY - 5 * s
      );
      ctx.stroke();

      // Multi-layer animated flame
      const tcFlicker = Math.sin(decorTime * 8) * 2 * s;
      const tcFlicker2 = Math.sin(decorTime * 11 + 1.5) * 1.5 * s;
      const tcFlicker3 = Math.cos(decorTime * 6) * 1 * s;
      const flameBase = brY - 2 * s;

      // Outer flame (darkest red-orange)
      ctx.fillStyle = "#cc3300";
      ctx.beginPath();
      ctx.moveTo(tcX - 6 * s, flameBase);
      ctx.quadraticCurveTo(
        tcX - 3 * s + tcFlicker2,
        flameBase - 16 * s,
        tcX + tcFlicker * 0.5,
        flameBase - 20 * s
      );
      ctx.quadraticCurveTo(
        tcX + 3 * s - tcFlicker2,
        flameBase - 16 * s,
        tcX + 6 * s,
        flameBase
      );
      ctx.closePath();
      ctx.fill();

      // Middle flame (bright orange)
      ctx.fillStyle = "#ff6600";
      ctx.beginPath();
      ctx.moveTo(tcX - 4.5 * s, flameBase);
      ctx.quadraticCurveTo(
        tcX - 2 * s - tcFlicker,
        flameBase - 13 * s,
        tcX + tcFlicker3,
        flameBase - 17 * s
      );
      ctx.quadraticCurveTo(
        tcX + 2 * s + tcFlicker,
        flameBase - 13 * s,
        tcX + 4.5 * s,
        flameBase
      );
      ctx.closePath();
      ctx.fill();

      // Inner flame (yellow-orange)
      ctx.fillStyle = "#ff9900";
      ctx.beginPath();
      ctx.moveTo(tcX - 3 * s, flameBase);
      ctx.quadraticCurveTo(
        tcX + tcFlicker * 0.6,
        flameBase - 10 * s,
        tcX - tcFlicker3 * 0.5,
        flameBase - 14 * s
      );
      ctx.quadraticCurveTo(
        tcX - tcFlicker * 0.6,
        flameBase - 10 * s,
        tcX + 3 * s,
        flameBase
      );
      ctx.closePath();
      ctx.fill();

      // Core flame (bright yellow-white)
      const coreG = ctx.createRadialGradient(
        tcX,
        flameBase - 5 * s,
        0.5 * s,
        tcX,
        flameBase - 5 * s,
        4 * s
      );
      coreG.addColorStop(0, "rgba(255,255,220,0.9)");
      coreG.addColorStop(0.4, "rgba(255,220,100,0.6)");
      coreG.addColorStop(1, "rgba(255,150,50,0)");
      ctx.fillStyle = coreG;
      ctx.beginPath();
      ctx.moveTo(tcX - 2 * s, flameBase);
      ctx.quadraticCurveTo(
        tcX - tcFlicker2 * 0.3,
        flameBase - 8 * s,
        tcX,
        flameBase - 11 * s
      );
      ctx.quadraticCurveTo(
        tcX + tcFlicker2 * 0.3,
        flameBase - 8 * s,
        tcX + 2 * s,
        flameBase
      );
      ctx.closePath();
      ctx.fill();

      // Flickering embers/sparks
      const tcSeed = decorX * 37 + decorY * 53;
      for (let em = 0; em < 4; em++) {
        const emPhase = tcSeed + em * 1.7;
        const emLife = ((decorTime * 2 + emPhase) % 3) / 3;
        const emX = tcX + Math.sin(emPhase + decorTime * 3) * 4 * s * emLife;
        const emY = flameBase - 14 * s - emLife * 12 * s;
        const emAlpha = Math.max(0, 1 - emLife * 1.3) * 0.7;
        const emSize = (0.8 + Math.sin(emPhase) * 0.3) * s * (1 - emLife * 0.5);

        ctx.fillStyle = `rgba(255,${Math.floor(180 - emLife * 100)},${Math.floor(50 - emLife * 40)},${emAlpha})`;
        ctx.beginPath();
        ctx.arc(emX, emY, emSize, 0, Math.PI * 2);
        ctx.fill();
      }

      // Flame glow halo
      const tcHaloG = ctx.createRadialGradient(
        tcX,
        flameBase - 8 * s,
        2 * s,
        tcX,
        flameBase - 8 * s,
        18 * s
      );
      const haloPulse = 0.2 + Math.sin(decorTime * 5) * 0.06;
      tcHaloG.addColorStop(0, `rgba(255,160,40,${haloPulse})`);
      tcHaloG.addColorStop(0.5, `rgba(255,100,20,${haloPulse * 0.3})`);
      tcHaloG.addColorStop(1, "rgba(255,60,10,0)");
      ctx.fillStyle = tcHaloG;
      ctx.beginPath();
      ctx.arc(tcX, flameBase - 8 * s, 18 * s, 0, Math.PI * 2);
      ctx.fill();

      break;
    }
    case "statue": {
      // Regional stone & figure palettes
      const statuePalettes: Record<
        string,
        {
          stoneLight: string;
          stoneMid: string;
          stoneDark: string;
          stoneShadow: string;
          stoneDeep: string;
          figureHighlight: string;
          figureLight: string;
          figureMid: string;
          figureDark: string;
          figureShadow: string;
          steelLight: string;
          steelDark: string;
        }
      > = {
        desert: {
          figureDark: "#786858",
          figureHighlight: "#d4c4a4",
          figureLight: "#b8a888",
          figureMid: "#988870",
          figureShadow: "#584838",
          steelDark: "#887868",
          steelLight: "#d8c8b0",
          stoneDark: "#887860",
          stoneDeep: "#4a3e30",
          stoneLight: "#c8b898",
          stoneMid: "#a89878",
          stoneShadow: "#685848",
        },
        grassland: {
          figureDark: "#605a50",
          figureHighlight: "#c0bbb0",
          figureLight: "#a09a90",
          figureMid: "#807a70",
          figureShadow: "#403a30",
          steelDark: "#6a6560",
          steelLight: "#d0ccc5",
          stoneDark: "#706b62",
          stoneDeep: "#3a3530",
          stoneLight: "#b5b0a5",
          stoneMid: "#928d82",
          stoneShadow: "#504b42",
        },
        swamp: {
          figureDark: "#485842",
          figureHighlight: "#96a68a",
          figureLight: "#7a8a6e",
          figureMid: "#607058",
          figureShadow: "#30402a",
          steelDark: "#506048",
          steelLight: "#a0aa98",
          stoneDark: "#566652",
          stoneDeep: "#2a3a24",
          stoneLight: "#8a9a80",
          stoneMid: "#708068",
          stoneShadow: "#3e4e38",
        },
        volcanic: {
          figureDark: "#504038",
          figureHighlight: "#b09080",
          figureLight: "#907060",
          figureMid: "#705848",
          figureShadow: "#382828",
          steelDark: "#605048",
          steelLight: "#b09888",
          stoneDark: "#604840",
          stoneDeep: "#302020",
          stoneLight: "#a08070",
          stoneMid: "#806050",
          stoneShadow: "#483430",
        },
        winter: {
          figureDark: "#687078",
          figureHighlight: "#d0d4da",
          figureLight: "#aab0b8",
          figureMid: "#8a9098",
          figureShadow: "#485058",
          steelDark: "#7a8088",
          steelLight: "#dce0e4",
          stoneDark: "#78808a",
          stoneDeep: "#3a4248",
          stoneLight: "#c0c5cc",
          stoneMid: "#98a0aa",
          stoneShadow: "#586068",
        },
      };
      const sp = statuePalettes[mapTheme] ?? statuePalettes.grassland;
      const { stoneLight } = sp;
      const { stoneMid } = sp;
      const { stoneDark } = sp;
      const { stoneShadow } = sp;
      const { stoneDeep } = sp;
      const { figureHighlight } = sp;
      const { figureLight } = sp;
      const { figureMid } = sp;
      const { figureDark } = sp;
      const { figureShadow } = sp;
      const { steelLight } = sp;
      const { steelDark } = sp;
      const px = screenPos.x;
      const py = screenPos.y;

      // Ground shadow
      drawDirectionalShadow(
        ctx,
        px,
        py + 4 * s,
        s,
        18 * s,
        9 * s,
        45 * s,
        0.35
      );

      // Helper: draw a gradient-lit isometric tier
      const drawPedTier = (
        cx2: number,
        ty: number,
        tw: number,
        th: number,
        topCol: string,
        leftCol: string,
        rightCol: string,
        topHi: string,
        leftHi: string
      ) => {
        // Top surface with gradient
        const tg = ctx.createLinearGradient(
          cx2 - tw,
          ty - th,
          cx2 + tw,
          ty - th
        );
        tg.addColorStop(0, topHi);
        tg.addColorStop(0.5, topCol);
        tg.addColorStop(1, topCol);
        ctx.fillStyle = tg;
        ctx.beginPath();
        ctx.moveTo(cx2 - tw, ty - th);
        ctx.lineTo(cx2, ty - th - tw * 0.5);
        ctx.lineTo(cx2 + tw, ty - th);
        ctx.lineTo(cx2, ty - th + tw * 0.5);
        ctx.closePath();
        ctx.fill();
        // Left face gradient (lit)
        const lg = ctx.createLinearGradient(
          cx2 - tw,
          ty - th,
          cx2,
          ty + tw * 0.5
        );
        lg.addColorStop(0, leftHi);
        lg.addColorStop(0.3, leftCol);
        lg.addColorStop(1, leftCol);
        ctx.fillStyle = lg;
        ctx.beginPath();
        ctx.moveTo(cx2 - tw, ty - th);
        ctx.lineTo(cx2 - tw, ty);
        ctx.lineTo(cx2, ty + tw * 0.5);
        ctx.lineTo(cx2, ty - th + tw * 0.5);
        ctx.closePath();
        ctx.fill();
        // Right face (shadow)
        ctx.fillStyle = rightCol;
        ctx.beginPath();
        ctx.moveTo(cx2 + tw, ty - th);
        ctx.lineTo(cx2 + tw, ty);
        ctx.lineTo(cx2, ty + tw * 0.5);
        ctx.lineTo(cx2, ty - th + tw * 0.5);
        ctx.closePath();
        ctx.fill();
        // Bottom edge line (ground contact)
        ctx.strokeStyle = stoneDeep;
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(cx2 - tw, ty);
        ctx.lineTo(cx2, ty + tw * 0.5);
        ctx.lineTo(cx2 + tw, ty);
        ctx.stroke();
      };

      // Helper: draw a cornice molding between tiers
      const drawCornice = (
        cx2: number,
        cy2: number,
        cw: number,
        ch: number
      ) => {
        // Lit chamfer strip on left
        ctx.fillStyle = stoneLight;
        ctx.beginPath();
        ctx.moveTo(cx2 - cw, cy2);
        ctx.lineTo(cx2 - cw - 1.5 * s, cy2 + ch);
        ctx.lineTo(cx2, cy2 + ch + cw * 0.5 + 0.75 * s);
        ctx.lineTo(cx2, cy2 + cw * 0.5);
        ctx.closePath();
        ctx.fill();
        // Shadow chamfer strip on right
        ctx.fillStyle = stoneShadow;
        ctx.beginPath();
        ctx.moveTo(cx2 + cw, cy2);
        ctx.lineTo(cx2 + cw + 1.5 * s, cy2 + ch);
        ctx.lineTo(cx2, cy2 + ch + cw * 0.5 + 0.75 * s);
        ctx.lineTo(cx2, cy2 + cw * 0.5);
        ctx.closePath();
        ctx.fill();
        // Top edge highlight
        ctx.strokeStyle = "#c5c0b5";
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(cx2 - cw, cy2);
        ctx.lineTo(cx2, cy2 - cw * 0.5);
        ctx.stroke();
      };

      // ========== STONE PEDESTAL ==========
      const baseY = py + 5 * s;

      // === BOTTOM PLINTH ===
      const t1W = 16 * s;
      const t1H = 6 * s;
      drawPedTier(
        px,
        baseY,
        t1W,
        t1H,
        stoneLight,
        stoneMid,
        stoneDark,
        "#c5c0b5",
        "#a5a095"
      );
      const pedSeed = Math.abs(decorX * 37 + decorY * 53);

      // Weathered stone patches on bottom tier
      ctx.fillStyle = "rgba(90,85,75,0.15)";
      drawOrganicBlobAt(
        ctx,
        px - 8 * s,
        baseY - 2 * s,
        4 * s,
        2 * s,
        pedSeed + 1.1,
        0.18,
        14
      );
      ctx.fill();
      drawOrganicBlobAt(
        ctx,
        px + 5 * s,
        baseY - 3 * s,
        3 * s,
        1.5 * s,
        pedSeed + 2.3,
        0.18,
        14
      );
      ctx.fill();
      // Chiseled edge bevel on bottom plinth (top-left lit edge)
      ctx.strokeStyle = "rgba(255,255,255,0.12)";
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(px - t1W, baseY - t1H);
      ctx.lineTo(px, baseY - t1H - t1W * 0.5);
      ctx.stroke();
      // Bottom plinth mortar lines
      ctx.strokeStyle = "rgba(50,45,38,0.12)";
      ctx.lineWidth = 0.4 * s;
      ctx.beginPath();
      ctx.moveTo(px - t1W + 5 * s, baseY - t1H * 0.5);
      ctx.lineTo(px + 5 * s, baseY - t1H * 0.5 + t1W * 0.2);
      ctx.stroke();

      // === CORNICE between bottom and middle ===
      const t2Y = baseY - t1H;
      drawCornice(px, t2Y - 2 * s, 13.5 * s, 2 * s);

      // === MIDDLE COLUMN ===
      const t2W = 12 * s;
      const t2H = 10 * s;
      drawPedTier(
        px,
        t2Y,
        t2W,
        t2H,
        stoneLight,
        stoneMid,
        stoneDark,
        "#c0bbb0",
        "#a09a90"
      );

      // Fluting grooves on middle column left face
      ctx.strokeStyle = "rgba(50,45,38,0.12)";
      ctx.lineWidth = 0.5 * s;
      for (let fl = 0; fl < 4; fl++) {
        const ft = (fl + 1) / 5;
        const flx1 = px - t2W + 2 * s + ft * (t2W - 3 * s);
        const fly1 = t2Y - 1 * s + ft * t2W * 0.42;
        const flx2 = flx1 + 0.5 * s;
        const fly2 = fly1 - t2H + 1 * s;
        ctx.beginPath();
        ctx.moveTo(flx1, fly1);
        ctx.lineTo(flx2, fly2);
        ctx.stroke();
      }
      // Fluting grooves on right face
      for (let fl = 0; fl < 3; fl++) {
        const ft = (fl + 1) / 4;
        const flx1 = px + t2W - 2 * s - ft * (t2W - 3 * s);
        const fly1 = t2Y - 1 * s + (1 - ft) * t2W * 0.42 + ft * t2W * 0.1;
        const flx2 = flx1 - 0.5 * s;
        const fly2 = fly1 - t2H + 1 * s;
        ctx.beginPath();
        ctx.moveTo(flx1, fly1);
        ctx.lineTo(flx2, fly2);
        ctx.stroke();
      }

      // Stone grain speckle on middle column
      ctx.globalAlpha = 0.08;
      for (let sp2 = 0; sp2 < 12; sp2++) {
        const spx = px + (((pedSeed + sp2 * 41) % 20) - 10) * s;
        const spy =
          t2Y - t2H * 0.1 - ((pedSeed + sp2 * 29) % ((t2H * 0.8) / s)) * s;
        ctx.fillStyle = sp2 % 3 === 0 ? stoneShadow : stoneLight;
        ctx.beginPath();
        ctx.arc(
          spx,
          spy,
          (0.4 + ((pedSeed + sp2 * 17) % 3) * 0.2) * s,
          0,
          Math.PI * 2
        );
        ctx.fill();
      }
      ctx.globalAlpha = 1;

      // Carved inscription band (left face)
      ctx.strokeStyle = stoneShadow;
      ctx.lineWidth = 0.7 * s;
      const bandY1 = t2Y - t2H * 0.65;
      const bandY2 = t2Y - t2H * 0.35;
      ctx.beginPath();
      ctx.moveTo(px - t2W + 2 * s, bandY1);
      ctx.lineTo(px, bandY1 + t2W * 0.42);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(px - t2W + 2 * s, bandY2);
      ctx.lineTo(px, bandY2 + t2W * 0.42);
      ctx.stroke();
      // Inscription symbols between lines (left face)
      ctx.strokeStyle = "rgba(70,65,55,0.3)";
      ctx.lineWidth = 0.5 * s;
      for (let gi = 0; gi < 5; gi++) {
        const gt = (gi + 0.5) / 5;
        const gx = px - t2W + 3 * s + gt * (t2W - 4 * s);
        const gy = bandY1 + gt * t2W * 0.42 + (bandY2 - bandY1) * 0.5;
        ctx.beginPath();
        ctx.moveTo(gx, gy - 1.5 * s);
        ctx.lineTo(gx + 1 * s, gy);
        ctx.lineTo(gx, gy + 1.5 * s);
        ctx.stroke();
      }
      // Inscription band (right face)
      ctx.strokeStyle = stoneShadow;
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(px + t2W - 2 * s, bandY1);
      ctx.lineTo(px, bandY1 + t2W * 0.42);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(px + t2W - 2 * s, bandY2);
      ctx.lineTo(px, bandY2 + t2W * 0.42);
      ctx.stroke();
      // Inscription band filling (subtle recessed background)
      ctx.globalAlpha = 0.06;
      ctx.fillStyle = stoneDeep;
      ctx.beginPath();
      ctx.moveTo(px - t2W + 2 * s, bandY1);
      ctx.lineTo(px, bandY1 + t2W * 0.42);
      ctx.lineTo(px, bandY2 + t2W * 0.42);
      ctx.lineTo(px - t2W + 2 * s, bandY2);
      ctx.closePath();
      ctx.fill();
      ctx.globalAlpha = 1;

      // Plaque (inset on front face, above inscription)
      const plaqueY = t2Y - t2H * 0.8;
      // Plaque recess shadow
      ctx.fillStyle = stoneDeep;
      ctx.beginPath();
      ctx.moveTo(px - 7.5 * s, plaqueY);
      ctx.lineTo(px, plaqueY + 3.8 * s);
      ctx.lineTo(px + 7.5 * s, plaqueY);
      ctx.lineTo(px, plaqueY - 3.8 * s);
      ctx.closePath();
      ctx.fill();
      // Plaque surface
      const plqG = ctx.createLinearGradient(
        px - 6 * s,
        plaqueY - 3 * s,
        px + 6 * s,
        plaqueY + 3 * s
      );
      plqG.addColorStop(0, stoneMid);
      plqG.addColorStop(0.5, stoneShadow);
      plqG.addColorStop(1, stoneDeep);
      ctx.fillStyle = plqG;
      ctx.beginPath();
      ctx.moveTo(px - 6 * s, plaqueY);
      ctx.lineTo(px, plaqueY + 3 * s);
      ctx.lineTo(px + 6 * s, plaqueY);
      ctx.lineTo(px, plaqueY - 3 * s);
      ctx.closePath();
      ctx.fill();
      // Plaque inner highlight (top-left bevel)
      ctx.strokeStyle = "rgba(200,195,185,0.25)";
      ctx.lineWidth = 0.6 * s;
      ctx.beginPath();
      ctx.moveTo(px - 6 * s, plaqueY);
      ctx.lineTo(px, plaqueY - 3 * s);
      ctx.lineTo(px + 6 * s, plaqueY);
      ctx.stroke();
      // Plaque inner shadow (bottom-right bevel)
      ctx.strokeStyle = "rgba(30,28,22,0.2)";
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.moveTo(px + 6 * s, plaqueY);
      ctx.lineTo(px, plaqueY + 3 * s);
      ctx.lineTo(px - 6 * s, plaqueY);
      ctx.stroke();
      // Engraved text lines on plaque
      ctx.strokeStyle = "rgba(40,35,28,0.2)";
      ctx.lineWidth = 0.4 * s;
      for (let tl = 0; tl < 3; tl++) {
        const tly = plaqueY - 1.5 * s + tl * 1.2 * s;
        const tlw = (3 - tl * 0.5) * s;
        ctx.beginPath();
        ctx.moveTo(px - tlw, tly);
        ctx.lineTo(px + tlw, tly);
        ctx.stroke();
      }

      // === CORNICE between middle and top ===
      const t3Y = t2Y - t2H;
      drawCornice(px, t3Y - 1.5 * s, 9.5 * s, 1.5 * s);

      // === TOP CAP ===
      const t3W = 8 * s;
      const t3H = 4 * s;
      drawPedTier(
        px,
        t3Y,
        t3W,
        t3H,
        "#c0bbb0",
        stoneLight,
        stoneMid,
        "#ccc7bc",
        "#b0aba0"
      );

      // Regional pedestal weathering
      if (mapTheme === "swamp") {
        ctx.fillStyle = "rgba(50,80,30,0.35)";
        drawOrganicBlobAt(
          ctx,
          px - t1W + 4 * s,
          baseY - 1 * s,
          4 * s,
          2 * s,
          pedSeed + 10.1,
          0.2,
          14
        );
        ctx.fill();
        ctx.fillStyle = "rgba(40,70,25,0.3)";
        drawOrganicBlobAt(
          ctx,
          px + 6 * s,
          t2Y + 1 * s,
          3 * s,
          1.5 * s,
          pedSeed + 11.7,
          0.2,
          14
        );
        ctx.fill();
        ctx.fillStyle = "rgba(45,75,28,0.25)";
        drawOrganicBlobAt(
          ctx,
          px - 3 * s,
          t2Y - t2H * 0.3,
          2.5 * s,
          1.2 * s,
          pedSeed + 12.3,
          0.22,
          12
        );
        ctx.fill();
      } else if (mapTheme === "desert") {
        ctx.fillStyle = "rgba(200,180,140,0.2)";
        drawOrganicBlobAt(
          ctx,
          px - t1W + 5 * s,
          baseY - 0.5 * s,
          4 * s,
          1.8 * s,
          pedSeed + 13.1,
          0.18,
          14
        );
        ctx.fill();
      } else if (mapTheme === "winter") {
        ctx.fillStyle = "rgba(220,230,240,0.35)";
        drawOrganicBlobAt(
          ctx,
          px,
          baseY - t1H - 0.5 * s,
          t1W * 0.8,
          3 * s,
          pedSeed + 14.7,
          0.2,
          18
        );
        ctx.fill();
      } else if (mapTheme === "volcanic") {
        ctx.fillStyle = "rgba(40,30,30,0.2)";
        drawOrganicBlobAt(
          ctx,
          px - t1W + 4 * s,
          baseY - 1 * s,
          3.5 * s,
          1.5 * s,
          pedSeed + 15.3,
          0.2,
          14
        );
        ctx.fill();
      } else {
        ctx.fillStyle = "rgba(70,90,55,0.25)";
        drawOrganicBlobAt(
          ctx,
          px - t1W + 4 * s,
          baseY - 1 * s,
          3.5 * s,
          1.5 * s,
          pedSeed + 16.1,
          0.2,
          14
        );
        ctx.fill();
        ctx.fillStyle = "rgba(60,80,45,0.2)";
        drawOrganicBlobAt(
          ctx,
          px + 6 * s,
          t2Y + 1 * s,
          2.5 * s,
          1.2 * s,
          pedSeed + 17.3,
          0.2,
          12
        );
        ctx.fill();
      }

      // Subtle crack line
      ctx.strokeStyle = "rgba(50,45,38,0.2)";
      ctx.lineWidth = 0.4 * s;
      ctx.beginPath();
      ctx.moveTo(px + 3 * s, t2Y - 1 * s);
      ctx.quadraticCurveTo(
        px + 5 * s,
        t2Y - t2H * 0.4,
        px + 4 * s,
        t2Y - t2H + 2 * s
      );
      ctx.stroke();

      // ========== STONE FIGURE (variant-based) ==========
      const figureBase = t3Y - t3H;
      const sv = variant % 8;
      const fx = px;

      if (sv === 0) {
        // === VARIANT 0: KNIGHT with raised sword & shield ===
        // Legs/Robe
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, figureBase);
        ctx.lineTo(fx - 6 * s, figureBase - 18 * s);
        ctx.lineTo(fx, figureBase - 20 * s);
        ctx.lineTo(fx, figureBase);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 5 * s, figureBase);
        ctx.lineTo(fx + 6 * s, figureBase - 18 * s);
        ctx.lineTo(fx, figureBase - 20 * s);
        ctx.lineTo(fx, figureBase);
        ctx.closePath();
        ctx.fill();

        // Torso
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, figureBase - 18 * s);
        ctx.lineTo(fx - 4 * s, figureBase - 32 * s);
        ctx.lineTo(fx, figureBase - 34 * s);
        ctx.lineTo(fx, figureBase - 20 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 5 * s, figureBase - 18 * s);
        ctx.lineTo(fx + 4 * s, figureBase - 32 * s);
        ctx.lineTo(fx, figureBase - 34 * s);
        ctx.lineTo(fx, figureBase - 20 * s);
        ctx.closePath();
        ctx.fill();

        // Cape
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, figureBase - 30 * s);
        ctx.quadraticCurveTo(
          fx - 12 * s,
          figureBase - 25 * s,
          fx - 10 * s,
          figureBase - 12 * s
        );
        ctx.lineTo(fx - 6 * s, figureBase - 18 * s);
        ctx.closePath();
        ctx.fill();

        // Left arm + shield
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, figureBase - 30 * s);
        ctx.lineTo(fx - 8 * s, figureBase - 20 * s);
        ctx.lineTo(fx - 6 * s, figureBase - 19 * s);
        ctx.lineTo(fx - 3 * s, figureBase - 28 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.ellipse(
          fx - 10 * s,
          figureBase - 18 * s,
          5 * s,
          7 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureLight;
        ctx.beginPath();
        ctx.ellipse(
          fx - 10 * s,
          figureBase - 18 * s,
          3 * s,
          4.5 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.arc(fx - 10 * s, figureBase - 18 * s, 1.5 * s, 0, Math.PI * 2);
        ctx.fill();

        // Right arm + sword
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 3 * s, figureBase - 30 * s);
        ctx.lineTo(fx + 10 * s, figureBase - 45 * s);
        ctx.lineTo(fx + 12 * s, figureBase - 43 * s);
        ctx.lineTo(fx + 5 * s, figureBase - 28 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureLight;
        ctx.beginPath();
        ctx.ellipse(
          fx + 11 * s,
          figureBase - 44 * s,
          2 * s,
          1.5 * s,
          0.7,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Blade
        ctx.fillStyle = steelLight;
        ctx.beginPath();
        ctx.moveTo(fx + 10 * s, figureBase - 46 * s);
        ctx.lineTo(fx + 8 * s, figureBase - 70 * s);
        ctx.lineTo(fx + 10 * s, figureBase - 72 * s);
        ctx.lineTo(fx + 12 * s, figureBase - 70 * s);
        ctx.lineTo(fx + 12 * s, figureBase - 46 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = steelDark;
        ctx.beginPath();
        ctx.moveTo(fx + 10 * s, figureBase - 46 * s);
        ctx.lineTo(fx + 8 * s, figureBase - 70 * s);
        ctx.lineTo(fx + 10 * s, figureBase - 72 * s);
        ctx.lineTo(fx + 10 * s, figureBase - 46 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "rgba(255,255,255,0.4)";
        ctx.beginPath();
        ctx.moveTo(fx + 11 * s, figureBase - 48 * s);
        ctx.lineTo(fx + 10 * s, figureBase - 68 * s);
        ctx.lineTo(fx + 11 * s, figureBase - 68 * s);
        ctx.lineTo(fx + 11.5 * s, figureBase - 48 * s);
        ctx.closePath();
        ctx.fill();
        // Crossguard
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx + 7 * s, figureBase - 45 * s);
        ctx.lineTo(fx + 15 * s, figureBase - 47 * s);
        ctx.lineTo(fx + 15 * s, figureBase - 45 * s);
        ctx.lineTo(fx + 7 * s, figureBase - 43 * s);
        ctx.closePath();
        ctx.fill();

        // Head + helmet
        const kHeadY = figureBase - 38 * s;
        const kHG = ctx.createRadialGradient(
          fx - 1 * s,
          kHeadY - 2 * s,
          0,
          fx,
          kHeadY,
          6 * s
        );
        kHG.addColorStop(0, figureHighlight);
        kHG.addColorStop(0.4, figureLight);
        kHG.addColorStop(0.8, figureMid);
        kHG.addColorStop(1, figureDark);
        ctx.fillStyle = kHG;
        ctx.beginPath();
        ctx.ellipse(fx, kHeadY, 5 * s, 6 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.ellipse(
          fx,
          kHeadY - 3 * s,
          5.5 * s,
          3.5 * s,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx, kHeadY - 6 * s);
        ctx.lineTo(fx - 1 * s, kHeadY - 3 * s);
        ctx.lineTo(fx + 1 * s, kHeadY - 3 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.arc(fx - 1.5 * s, kHeadY - 1 * s, 0.7 * s, 0, Math.PI * 2);
        ctx.arc(fx + 1.5 * s, kHeadY - 1 * s, 0.7 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "rgba(255,255,255,0.15)";
        ctx.beginPath();
        ctx.ellipse(
          fx - 2 * s,
          kHeadY - 3 * s,
          2 * s,
          1.5 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
      } else if (sv === 1) {
        // === VARIANT 1: DEMON LORD with horns & wings ===
        // Broad legs
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, figureBase);
        ctx.lineTo(fx - 7 * s, figureBase - 16 * s);
        ctx.lineTo(fx - 2 * s, figureBase - 18 * s);
        ctx.lineTo(fx - 1 * s, figureBase);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 6 * s, figureBase);
        ctx.lineTo(fx + 7 * s, figureBase - 16 * s);
        ctx.lineTo(fx + 2 * s, figureBase - 18 * s);
        ctx.lineTo(fx + 1 * s, figureBase);
        ctx.closePath();
        ctx.fill();

        // Muscular torso (wider)
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, figureBase - 16 * s);
        ctx.lineTo(fx - 7 * s, figureBase - 30 * s);
        ctx.lineTo(fx, figureBase - 33 * s);
        ctx.lineTo(fx, figureBase - 18 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 6 * s, figureBase - 16 * s);
        ctx.lineTo(fx + 7 * s, figureBase - 30 * s);
        ctx.lineTo(fx, figureBase - 33 * s);
        ctx.lineTo(fx, figureBase - 18 * s);
        ctx.closePath();
        ctx.fill();

        // Wings (behind, spread wide)
        ctx.fillStyle = figureShadow;
        // Left wing
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, figureBase - 28 * s);
        ctx.quadraticCurveTo(
          fx - 20 * s,
          figureBase - 45 * s,
          fx - 22 * s,
          figureBase - 35 * s
        );
        ctx.quadraticCurveTo(
          fx - 18 * s,
          figureBase - 28 * s,
          fx - 15 * s,
          figureBase - 18 * s
        );
        ctx.quadraticCurveTo(
          fx - 10 * s,
          figureBase - 22 * s,
          fx - 5 * s,
          figureBase - 20 * s
        );
        ctx.closePath();
        ctx.fill();
        // Right wing
        ctx.beginPath();
        ctx.moveTo(fx + 5 * s, figureBase - 28 * s);
        ctx.quadraticCurveTo(
          fx + 20 * s,
          figureBase - 45 * s,
          fx + 22 * s,
          figureBase - 35 * s
        );
        ctx.quadraticCurveTo(
          fx + 18 * s,
          figureBase - 28 * s,
          fx + 15 * s,
          figureBase - 18 * s
        );
        ctx.quadraticCurveTo(
          fx + 10 * s,
          figureBase - 22 * s,
          fx + 5 * s,
          figureBase - 20 * s
        );
        ctx.closePath();
        ctx.fill();
        // Wing bone highlights
        ctx.strokeStyle = figureDark;
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, figureBase - 28 * s);
        ctx.quadraticCurveTo(
          fx - 14 * s,
          figureBase - 40 * s,
          fx - 22 * s,
          figureBase - 35 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(fx + 5 * s, figureBase - 28 * s);
        ctx.quadraticCurveTo(
          fx + 14 * s,
          figureBase - 40 * s,
          fx + 22 * s,
          figureBase - 35 * s
        );
        ctx.stroke();

        // Arms at sides, clawed
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, figureBase - 28 * s);
        ctx.lineTo(fx - 10 * s, figureBase - 15 * s);
        ctx.lineTo(fx - 8 * s, figureBase - 14 * s);
        ctx.lineTo(fx - 5 * s, figureBase - 26 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 6 * s, figureBase - 28 * s);
        ctx.lineTo(fx + 10 * s, figureBase - 15 * s);
        ctx.lineTo(fx + 8 * s, figureBase - 14 * s);
        ctx.lineTo(fx + 5 * s, figureBase - 26 * s);
        ctx.closePath();
        ctx.fill();
        // Claws
        for (let c = 0; c < 3; c++) {
          ctx.fillStyle = steelLight;
          ctx.beginPath();
          ctx.moveTo(fx - 10 * s + c * 1.2 * s, figureBase - 15 * s);
          ctx.lineTo(fx - 11 * s + c * 1.2 * s, figureBase - 11 * s);
          ctx.lineTo(fx - 9.5 * s + c * 1.2 * s, figureBase - 14 * s);
          ctx.fill();
          ctx.beginPath();
          ctx.moveTo(fx + 8 * s + c * 1.2 * s, figureBase - 15 * s);
          ctx.lineTo(fx + 7 * s + c * 1.2 * s, figureBase - 11 * s);
          ctx.lineTo(fx + 8.5 * s + c * 1.2 * s, figureBase - 14 * s);
          ctx.fill();
        }

        // Demon head
        const dHeadY = figureBase - 37 * s;
        const dHG = ctx.createRadialGradient(
          fx,
          dHeadY - 1 * s,
          0,
          fx,
          dHeadY,
          6 * s
        );
        dHG.addColorStop(0, figureHighlight);
        dHG.addColorStop(0.5, figureMid);
        dHG.addColorStop(1, figureDark);
        ctx.fillStyle = dHG;
        ctx.beginPath();
        ctx.ellipse(fx, dHeadY, 5 * s, 5.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Horns
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, dHeadY - 3 * s);
        ctx.quadraticCurveTo(
          fx - 10 * s,
          dHeadY - 12 * s,
          fx - 8 * s,
          dHeadY - 16 * s
        );
        ctx.quadraticCurveTo(
          fx - 6 * s,
          dHeadY - 10 * s,
          fx - 3 * s,
          dHeadY - 4 * s
        );
        ctx.closePath();
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(fx + 4 * s, dHeadY - 3 * s);
        ctx.quadraticCurveTo(
          fx + 10 * s,
          dHeadY - 12 * s,
          fx + 8 * s,
          dHeadY - 16 * s
        );
        ctx.quadraticCurveTo(
          fx + 6 * s,
          dHeadY - 10 * s,
          fx + 3 * s,
          dHeadY - 4 * s
        );
        ctx.closePath();
        ctx.fill();
        // Horn highlights
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, dHeadY - 4 * s);
        ctx.quadraticCurveTo(
          fx - 8 * s,
          dHeadY - 11 * s,
          fx - 8 * s,
          dHeadY - 15 * s
        );
        ctx.lineTo(fx - 8 * s, dHeadY - 16 * s);
        ctx.quadraticCurveTo(
          fx - 7 * s,
          dHeadY - 11 * s,
          fx - 3.5 * s,
          dHeadY - 5 * s
        );
        ctx.closePath();
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(fx + 4 * s, dHeadY - 4 * s);
        ctx.quadraticCurveTo(
          fx + 8 * s,
          dHeadY - 11 * s,
          fx + 8 * s,
          dHeadY - 15 * s
        );
        ctx.lineTo(fx + 8 * s, dHeadY - 16 * s);
        ctx.quadraticCurveTo(
          fx + 7 * s,
          dHeadY - 11 * s,
          fx + 3.5 * s,
          dHeadY - 5 * s
        );
        ctx.closePath();
        ctx.fill();

        // Glowing eyes
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(
          fx - 2 * s,
          dHeadY - 0.5 * s,
          1.5 * s,
          1 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.ellipse(
          fx + 2 * s,
          dHeadY - 0.5 * s,
          1.5 * s,
          1 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        const dEyeGlow = 0.4 + Math.sin(decorTime * 2.5) * 0.3;
        ctx.fillStyle = `rgba(200,50,30,${dEyeGlow})`;
        ctx.beginPath();
        ctx.arc(fx - 2 * s, dHeadY - 0.5 * s, 2.5 * s, 0, Math.PI * 2);
        ctx.arc(fx + 2 * s, dHeadY - 0.5 * s, 2.5 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "#ff4030";
        ctx.beginPath();
        ctx.arc(fx - 2 * s, dHeadY - 0.5 * s, 0.8 * s, 0, Math.PI * 2);
        ctx.arc(fx + 2 * s, dHeadY - 0.5 * s, 0.8 * s, 0, Math.PI * 2);
        ctx.fill();

        // Snarling mouth
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 3 * s, dHeadY + 2 * s);
        ctx.lineTo(fx, dHeadY + 4 * s);
        ctx.lineTo(fx + 3 * s, dHeadY + 2 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = steelLight;
        ctx.beginPath();
        ctx.moveTo(fx - 1.5 * s, dHeadY + 2.5 * s);
        ctx.lineTo(fx - 0.8 * s, dHeadY + 3.2 * s);
        ctx.lineTo(fx, dHeadY + 2.5 * s);
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(fx, dHeadY + 2.5 * s);
        ctx.lineTo(fx + 0.8 * s, dHeadY + 3.2 * s);
        ctx.lineTo(fx + 1.5 * s, dHeadY + 2.5 * s);
        ctx.fill();
      } else if (sv === 2) {
        // === VARIANT 2: ROBED MAGE with staff & orb ===
        // Long flowing robes (wider at base)
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 7 * s, figureBase);
        ctx.lineTo(fx - 4 * s, figureBase - 30 * s);
        ctx.lineTo(fx, figureBase - 33 * s);
        ctx.lineTo(fx, figureBase);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 7 * s, figureBase);
        ctx.lineTo(fx + 4 * s, figureBase - 30 * s);
        ctx.lineTo(fx, figureBase - 33 * s);
        ctx.lineTo(fx, figureBase);
        ctx.closePath();
        ctx.fill();

        // Robe trim at bottom
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 7 * s, figureBase);
        ctx.quadraticCurveTo(fx, figureBase + 2 * s, fx + 7 * s, figureBase);
        ctx.stroke();

        // Left arm holding book/scroll
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, figureBase - 28 * s);
        ctx.lineTo(fx - 8 * s, figureBase - 18 * s);
        ctx.lineTo(fx - 6 * s, figureBase - 17 * s);
        ctx.lineTo(fx - 3 * s, figureBase - 26 * s);
        ctx.closePath();
        ctx.fill();
        // Book/tome
        ctx.fillStyle = "#5a4a38";
        ctx.fillRect(fx - 11 * s, figureBase - 20 * s, 5 * s, 6 * s);
        ctx.fillStyle = "#7a6a50";
        ctx.fillRect(fx - 10.5 * s, figureBase - 19.5 * s, 4 * s, 5 * s);
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 8.5 * s, figureBase - 19 * s);
        ctx.lineTo(fx - 8.5 * s, figureBase - 15 * s);
        ctx.stroke();

        // Right arm raised holding staff
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 3 * s, figureBase - 28 * s);
        ctx.lineTo(fx + 7 * s, figureBase - 40 * s);
        ctx.lineTo(fx + 9 * s, figureBase - 38 * s);
        ctx.lineTo(fx + 5 * s, figureBase - 26 * s);
        ctx.closePath();
        ctx.fill();

        // Staff
        ctx.fillStyle = "#5a4a3a";
        ctx.fillRect(fx + 7 * s, figureBase - 65 * s, 2.5 * s, 52 * s);
        ctx.fillStyle = "#4a3a2a";
        ctx.fillRect(fx + 7 * s, figureBase - 65 * s, 1 * s, 52 * s);
        // Staff top knot
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.arc(fx + 8.25 * s, figureBase - 65 * s, 2.5 * s, 0, Math.PI * 2);
        ctx.fill();
        // Orb on staff
        const orbGlow = 0.5 + Math.sin(decorTime * 2) * 0.3;
        const orbGrad = ctx.createRadialGradient(
          fx + 7.5 * s,
          figureBase - 69 * s,
          0,
          fx + 8.25 * s,
          figureBase - 68 * s,
          4 * s
        );
        orbGrad.addColorStop(0, `rgba(100,180,255,${orbGlow})`);
        orbGrad.addColorStop(0.5, `rgba(60,120,220,${orbGlow * 0.7})`);
        orbGrad.addColorStop(1, `rgba(30,60,150,${orbGlow * 0.3})`);
        ctx.fillStyle = orbGrad;
        ctx.beginPath();
        ctx.arc(fx + 8.25 * s, figureBase - 68 * s, 3.5 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = `rgba(200,230,255,${orbGlow * 0.6})`;
        ctx.beginPath();
        ctx.arc(fx + 7.5 * s, figureBase - 69 * s, 1.2 * s, 0, Math.PI * 2);
        ctx.fill();

        // Hooded head
        const mHeadY = figureBase - 36 * s;
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, mHeadY + 3 * s);
        ctx.quadraticCurveTo(fx - 7 * s, mHeadY - 6 * s, fx, mHeadY - 9 * s);
        ctx.quadraticCurveTo(
          fx + 7 * s,
          mHeadY - 6 * s,
          fx + 6 * s,
          mHeadY + 3 * s
        );
        ctx.closePath();
        ctx.fill();
        // Hood inner shadow
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, mHeadY + 2 * s);
        ctx.quadraticCurveTo(fx - 5 * s, mHeadY - 3 * s, fx, mHeadY - 5 * s);
        ctx.quadraticCurveTo(
          fx + 5 * s,
          mHeadY - 3 * s,
          fx + 4 * s,
          mHeadY + 2 * s
        );
        ctx.closePath();
        ctx.fill();
        // Shadowed face (barely visible)
        const mFG = ctx.createRadialGradient(fx, mHeadY, 0, fx, mHeadY, 4 * s);
        mFG.addColorStop(0, figureMid);
        mFG.addColorStop(0.5, figureShadow);
        mFG.addColorStop(1, "#1a1a1a");
        ctx.fillStyle = mFG;
        ctx.beginPath();
        ctx.ellipse(fx, mHeadY, 3.5 * s, 4 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        // Faint glowing eyes under hood
        const mEyeGlow = 0.3 + Math.sin(decorTime * 1.8) * 0.2;
        ctx.fillStyle = `rgba(100,180,255,${mEyeGlow})`;
        ctx.beginPath();
        ctx.arc(fx - 1.2 * s, mHeadY - 0.5 * s, 0.8 * s, 0, Math.PI * 2);
        ctx.arc(fx + 1.2 * s, mHeadY - 0.5 * s, 0.8 * s, 0, Math.PI * 2);
        ctx.fill();
      } else if (sv === 3) {
        // === VARIANT 3: WARRIOR with battle axe ===
        // Heavy armored legs
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, figureBase);
        ctx.lineTo(fx - 6 * s, figureBase - 16 * s);
        ctx.lineTo(fx - 1 * s, figureBase - 18 * s);
        ctx.lineTo(fx - 1 * s, figureBase);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 6 * s, figureBase);
        ctx.lineTo(fx + 6 * s, figureBase - 16 * s);
        ctx.lineTo(fx + 1 * s, figureBase - 18 * s);
        ctx.lineTo(fx + 1 * s, figureBase);
        ctx.closePath();
        ctx.fill();

        // Broad torso with pauldrons
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, figureBase - 16 * s);
        ctx.lineTo(fx - 7 * s, figureBase - 30 * s);
        ctx.lineTo(fx, figureBase - 32 * s);
        ctx.lineTo(fx, figureBase - 18 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 6 * s, figureBase - 16 * s);
        ctx.lineTo(fx + 7 * s, figureBase - 30 * s);
        ctx.lineTo(fx, figureBase - 32 * s);
        ctx.lineTo(fx, figureBase - 18 * s);
        ctx.closePath();
        ctx.fill();

        // Pauldrons (shoulder armor)
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(
          fx - 8 * s,
          figureBase - 29 * s,
          4 * s,
          3 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.ellipse(
          fx - 8 * s,
          figureBase - 30 * s,
          3.5 * s,
          2.5 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(
          fx + 8 * s,
          figureBase - 29 * s,
          4 * s,
          3 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.ellipse(
          fx + 8 * s,
          figureBase - 30 * s,
          3.5 * s,
          2.5 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Left arm resting
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 7 * s, figureBase - 28 * s);
        ctx.lineTo(fx - 10 * s, figureBase - 18 * s);
        ctx.lineTo(fx - 8 * s, figureBase - 17 * s);
        ctx.lineTo(fx - 5 * s, figureBase - 26 * s);
        ctx.closePath();
        ctx.fill();
        // Fist
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.arc(fx - 9 * s, figureBase - 16 * s, 2 * s, 0, Math.PI * 2);
        ctx.fill();

        // Right arm raised with axe
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 6 * s, figureBase - 28 * s);
        ctx.lineTo(fx + 12 * s, figureBase - 42 * s);
        ctx.lineTo(fx + 14 * s, figureBase - 40 * s);
        ctx.lineTo(fx + 8 * s, figureBase - 26 * s);
        ctx.closePath();
        ctx.fill();
        // Axe handle
        ctx.fillStyle = "#5a4a3a";
        ctx.save();
        ctx.translate(fx + 13 * s, figureBase - 41 * s);
        ctx.rotate(-0.4);
        ctx.fillRect(-1.2 * s, -20 * s, 2.4 * s, 20 * s);
        ctx.restore();
        // Axe head (large crescent)
        ctx.fillStyle = steelLight;
        ctx.beginPath();
        ctx.moveTo(fx + 10 * s, figureBase - 58 * s);
        ctx.quadraticCurveTo(
          fx + 18 * s,
          figureBase - 62 * s,
          fx + 16 * s,
          figureBase - 52 * s
        );
        ctx.lineTo(fx + 11 * s, figureBase - 52 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = steelDark;
        ctx.beginPath();
        ctx.moveTo(fx + 10 * s, figureBase - 58 * s);
        ctx.quadraticCurveTo(
          fx + 14 * s,
          figureBase - 60 * s,
          fx + 13 * s,
          figureBase - 52 * s
        );
        ctx.lineTo(fx + 11 * s, figureBase - 52 * s);
        ctx.closePath();
        ctx.fill();
        // Axe edge highlight
        ctx.fillStyle = "rgba(255,255,255,0.35)";
        ctx.beginPath();
        ctx.moveTo(fx + 15 * s, figureBase - 60 * s);
        ctx.quadraticCurveTo(
          fx + 17 * s,
          figureBase - 58 * s,
          fx + 16 * s,
          figureBase - 53 * s
        );
        ctx.lineTo(fx + 15.5 * s, figureBase - 53 * s);
        ctx.quadraticCurveTo(
          fx + 16 * s,
          figureBase - 58 * s,
          fx + 14.5 * s,
          figureBase - 59.5 * s
        );
        ctx.closePath();
        ctx.fill();

        // Viking-style head
        const wHeadY = figureBase - 36 * s;
        const wHG = ctx.createRadialGradient(
          fx,
          wHeadY - 1 * s,
          0,
          fx,
          wHeadY,
          5.5 * s
        );
        wHG.addColorStop(0, figureHighlight);
        wHG.addColorStop(0.4, figureLight);
        wHG.addColorStop(0.8, figureMid);
        wHG.addColorStop(1, figureDark);
        ctx.fillStyle = wHG;
        ctx.beginPath();
        ctx.ellipse(fx, wHeadY, 5 * s, 5.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        // Viking helm (flat top, nose guard)
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, wHeadY - 2 * s);
        ctx.lineTo(fx - 5.5 * s, wHeadY - 5 * s);
        ctx.lineTo(fx + 5.5 * s, wHeadY - 5 * s);
        ctx.lineTo(fx + 6 * s, wHeadY - 2 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureDark;
        ctx.fillRect(fx - 0.8 * s, wHeadY - 5 * s, 1.6 * s, 6 * s);
        // Eyes
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.arc(fx - 2 * s, wHeadY - 0.5 * s, 0.7 * s, 0, Math.PI * 2);
        ctx.arc(fx + 2 * s, wHeadY - 0.5 * s, 0.7 * s, 0, Math.PI * 2);
        ctx.fill();
        // Beard
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 3 * s, wHeadY + 2 * s);
        ctx.quadraticCurveTo(fx, wHeadY + 7 * s, fx + 3 * s, wHeadY + 2 * s);
        ctx.closePath();
        ctx.fill();
      } else if (sv === 4) {
        // === VARIANT 4: PRINCETON TIGER (crouching/prowling) ===
        const tB = figureBase - 2 * s;

        // Tail with gradient thickness
        ctx.lineCap = "round";
        ctx.strokeStyle = figureMid;
        ctx.lineWidth = 3 * s;
        ctx.beginPath();
        ctx.moveTo(fx + 7 * s, tB - 8 * s);
        ctx.bezierCurveTo(
          fx + 12 * s,
          tB - 16 * s,
          fx + 15 * s,
          tB - 22 * s,
          fx + 10 * s,
          tB - 28 * s
        );
        ctx.stroke();
        ctx.strokeStyle = figureLight;
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx + 6.5 * s, tB - 8 * s);
        ctx.bezierCurveTo(
          fx + 11 * s,
          tB - 16 * s,
          fx + 14 * s,
          tB - 22 * s,
          fx + 9.5 * s,
          tB - 27 * s
        );
        ctx.stroke();
        // Tail tip dark
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 2.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx + 11 * s, tB - 26 * s);
        ctx.quadraticCurveTo(
          fx + 10.5 * s,
          tB - 28 * s,
          fx + 10 * s,
          tB - 28.5 * s
        );
        ctx.stroke();

        // Hind body with gradient
        const hindG = ctx.createRadialGradient(
          fx + 4 * s,
          tB - 9 * s,
          1 * s,
          fx + 5 * s,
          tB - 8 * s,
          8 * s
        );
        hindG.addColorStop(0, figureLight);
        hindG.addColorStop(0.5, figureMid);
        hindG.addColorStop(1, figureDark);
        ctx.fillStyle = hindG;
        ctx.beginPath();
        ctx.ellipse(
          fx + 5 * s,
          tB - 8 * s,
          7.5 * s,
          5.5 * s,
          0.15,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Haunch muscle highlight
        ctx.fillStyle = "rgba(190,185,175,0.15)";
        ctx.beginPath();
        ctx.ellipse(fx + 3 * s, tB - 10 * s, 3 * s, 3 * s, 0.2, 0, Math.PI * 2);
        ctx.fill();

        // Hind legs (shaped with knee joint)
        const drawTigerLeg = (
          lx: number,
          ly: number,
          kneeOff: number,
          col1: string,
          col2: string
        ) => {
          ctx.fillStyle = col1;
          ctx.beginPath();
          ctx.moveTo(lx - 1.5 * s, ly);
          ctx.quadraticCurveTo(
            lx - 2 * s + kneeOff,
            ly + 4 * s,
            lx - 1 * s + kneeOff * 0.5,
            ly + 7 * s
          );
          ctx.lineTo(lx + 1 * s + kneeOff * 0.5, ly + 7 * s);
          ctx.quadraticCurveTo(
            lx + 1 * s + kneeOff,
            ly + 4 * s,
            lx + 1.5 * s,
            ly
          );
          ctx.closePath();
          ctx.fill();
          // Paw
          ctx.fillStyle = col2;
          ctx.beginPath();
          ctx.ellipse(
            lx + kneeOff * 0.4,
            ly + 7.5 * s,
            2 * s,
            1 * s,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();
        };
        drawTigerLeg(fx + 7 * s, tB - 4 * s, 1.5, figureMid, figureDark);
        drawTigerLeg(fx + 2 * s, tB - 4 * s, -0.5, figureDark, figureShadow);

        // Torso with gradient
        const torsoG = ctx.createLinearGradient(
          fx - 6 * s,
          tB - 14 * s,
          fx + 4 * s,
          tB - 5 * s
        );
        torsoG.addColorStop(0, figureLight);
        torsoG.addColorStop(0.5, figureMid);
        torsoG.addColorStop(1, figureDark);
        ctx.fillStyle = torsoG;
        ctx.beginPath();
        ctx.moveTo(fx + 3 * s, tB - 10 * s);
        ctx.bezierCurveTo(
          fx,
          tB - 14 * s,
          fx - 4 * s,
          tB - 16 * s,
          fx - 7 * s,
          tB - 13 * s
        );
        ctx.lineTo(fx - 6 * s, tB - 5 * s);
        ctx.bezierCurveTo(
          fx - 2 * s,
          tB - 6 * s,
          fx + 2 * s,
          tB - 5 * s,
          fx + 4 * s,
          tB - 5 * s
        );
        ctx.closePath();
        ctx.fill();
        // Spine highlight
        ctx.strokeStyle = "rgba(200,195,185,0.12)";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(fx + 2 * s, tB - 10 * s);
        ctx.bezierCurveTo(
          fx - 1 * s,
          tB - 13 * s,
          fx - 4 * s,
          tB - 15 * s,
          fx - 7 * s,
          tB - 14 * s
        );
        ctx.stroke();

        // Front chest (raised) with gradient
        const chestG = ctx.createRadialGradient(
          fx - 7 * s,
          tB - 15 * s,
          1 * s,
          fx - 6 * s,
          tB - 13 * s,
          7 * s
        );
        chestG.addColorStop(0, figureHighlight);
        chestG.addColorStop(0.4, figureLight);
        chestG.addColorStop(1, figureMid);
        ctx.fillStyle = chestG;
        ctx.beginPath();
        ctx.ellipse(
          fx - 6 * s,
          tB - 14 * s,
          6.5 * s,
          6 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Shoulder muscle
        ctx.fillStyle = "rgba(200,195,185,0.12)";
        ctx.beginPath();
        ctx.ellipse(
          fx - 8 * s,
          tB - 15 * s,
          3 * s,
          2.5 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Front legs (shaped)
        drawTigerLeg(fx - 10 * s, tB - 9 * s, -1, figureLight, figureMid);
        drawTigerLeg(fx - 4 * s, tB - 9 * s, 0, figureMid, figureDark);

        // Tiger stripes (carved into stone, dual-line)
        ctx.lineWidth = 1.2 * s;
        for (let si = 0; si < 6; si++) {
          const sx2 = fx - 5 * s + si * 2.8 * s;
          const sy2 = tB - 12 * s + si * 0.4 * s;
          ctx.strokeStyle = figureShadow;
          ctx.beginPath();
          ctx.moveTo(sx2, sy2 - 2.5 * s);
          ctx.bezierCurveTo(
            sx2 + 0.8 * s,
            sy2 - 1 * s,
            sx2 + 0.8 * s,
            sy2 + 1 * s,
            sx2,
            sy2 + 2.5 * s
          );
          ctx.stroke();
          ctx.strokeStyle = "rgba(200,195,185,0.08)";
          ctx.lineWidth = 0.5 * s;
          ctx.beginPath();
          ctx.moveTo(sx2 + 0.5 * s, sy2 - 2 * s);
          ctx.bezierCurveTo(
            sx2 + 1.2 * s,
            sy2 - 0.5 * s,
            sx2 + 1.2 * s,
            sy2 + 0.5 * s,
            sx2 + 0.5 * s,
            sy2 + 2 * s
          );
          ctx.stroke();
          ctx.lineWidth = 1.2 * s;
        }

        // Head with gradient
        const tHX = fx - 9 * s;
        const tHY = tB - 20 * s;
        const headG = ctx.createRadialGradient(
          tHX - 1 * s,
          tHY - 1 * s,
          0,
          tHX,
          tHY,
          6 * s
        );
        headG.addColorStop(0, figureHighlight);
        headG.addColorStop(0.35, figureLight);
        headG.addColorStop(0.7, figureMid);
        headG.addColorStop(1, figureDark);
        ctx.fillStyle = headG;
        ctx.beginPath();
        ctx.ellipse(tHX, tHY, 5.5 * s, 5 * s, -0.15, 0, Math.PI * 2);
        ctx.fill();

        // Muzzle with gradient
        const muzG = ctx.createRadialGradient(
          tHX - 3 * s,
          tHY + 1 * s,
          0,
          tHX - 3 * s,
          tHY + 1.5 * s,
          3.5 * s
        );
        muzG.addColorStop(0, figureHighlight);
        muzG.addColorStop(0.5, figureLight);
        muzG.addColorStop(1, figureMid);
        ctx.fillStyle = muzG;
        ctx.beginPath();
        ctx.ellipse(
          tHX - 3 * s,
          tHY + 1.5 * s,
          3.5 * s,
          2.8 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Jaw line
        ctx.strokeStyle = figureDark;
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(tHX - 5.5 * s, tHY + 1 * s);
        ctx.quadraticCurveTo(
          tHX - 3 * s,
          tHY + 4.5 * s,
          tHX - 0.5 * s,
          tHY + 3 * s
        );
        ctx.stroke();

        // Open mouth (roaring)
        ctx.fillStyle = stoneDeep;
        ctx.beginPath();
        ctx.moveTo(tHX - 6 * s, tHY + 1 * s);
        ctx.quadraticCurveTo(
          tHX - 4.5 * s,
          tHY + 5 * s,
          tHX - 1.5 * s,
          tHY + 3.5 * s
        );
        ctx.lineTo(tHX - 1.5 * s, tHY + 1 * s);
        ctx.closePath();
        ctx.fill();
        // Tongue cavity
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(
          tHX - 3.5 * s,
          tHY + 2.5 * s,
          1.5 * s,
          1 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Fangs (4 visible)
        ctx.fillStyle = steelLight;
        const fangs = [
          [-5.5, 1],
          [-4, 1],
          [-2.5, 1],
          [-1.8, 1],
        ];
        for (const [fgx, fgy] of fangs) {
          ctx.beginPath();
          ctx.moveTo(tHX + fgx * s, tHY + fgy * s);
          ctx.lineTo(tHX + (fgx + 0.3) * s, tHY + (fgy + 1.8) * s);
          ctx.lineTo(tHX + (fgx + 0.8) * s, tHY + fgy * s);
          ctx.fill();
        }

        // Eyes (deep set with socket shadow)
        ctx.fillStyle = stoneDeep;
        ctx.beginPath();
        ctx.ellipse(
          tHX - 2 * s,
          tHY - 1.5 * s,
          1.6 * s,
          1.1 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.ellipse(
          tHX + 1.5 * s,
          tHY - 1.5 * s,
          1.6 * s,
          1.1 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(
          tHX - 2 * s,
          tHY - 1.5 * s,
          1 * s,
          0.6 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.ellipse(
          tHX + 1.5 * s,
          tHY - 1.5 * s,
          1 * s,
          0.6 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Nose
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(
          tHX - 5 * s,
          tHY + 0.5 * s,
          1 * s,
          0.7 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Ears (with inner shadow)
        for (const [ex, edir] of [
          [-1, -1],
          [2, 1],
        ] as [number, number][]) {
          ctx.fillStyle = figureMid;
          ctx.beginPath();
          ctx.moveTo(tHX + ex * s, tHY - 4 * s);
          ctx.lineTo(tHX + (ex + edir * 2) * s, tHY - 8 * s);
          ctx.lineTo(tHX + (ex + edir * 3.5) * s, tHY - 5 * s);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = figureDark;
          ctx.beginPath();
          ctx.moveTo(tHX + (ex + edir * 0.5) * s, tHY - 4.5 * s);
          ctx.lineTo(tHX + (ex + edir * 2) * s, tHY - 7 * s);
          ctx.lineTo(tHX + (ex + edir * 3) * s, tHY - 5.2 * s);
          ctx.closePath();
          ctx.fill();
        }

        // Forehead stripes
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 0.8 * s;
        for (let fi = 0; fi < 3; fi++) {
          const fsx = tHX - 1.5 * s + fi * 1.8 * s;
          ctx.beginPath();
          ctx.moveTo(fsx, tHY - 4 * s);
          ctx.lineTo(fsx + 0.5 * s, tHY - 1.5 * s);
          ctx.stroke();
        }
        // Whisker indentations
        ctx.strokeStyle = "rgba(80,75,65,0.2)";
        ctx.lineWidth = 0.4 * s;
        for (let wi = 0; wi < 3; wi++) {
          const wy = tHY + 0.5 * s + wi * 0.8 * s;
          ctx.beginPath();
          ctx.moveTo(tHX - 5 * s, wy);
          ctx.lineTo(tHX - 8 * s, wy - 0.5 * s + wi * 0.3 * s);
          ctx.stroke();
        }
      } else if (sv === 5) {
        // === VARIANT 5: MOUNTED CAVALRY (rider on horse) ===
        const hY = figureBase - 2 * s;

        // Flowing tail with volume
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx + 9 * s, hY - 13 * s);
        ctx.bezierCurveTo(
          fx + 13 * s,
          hY - 11 * s,
          fx + 15 * s,
          hY - 5 * s,
          fx + 13 * s,
          hY
        );
        ctx.bezierCurveTo(
          fx + 12 * s,
          hY - 4 * s,
          fx + 10 * s,
          hY - 8 * s,
          fx + 8 * s,
          hY - 12 * s
        );
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx + 11 * s, hY - 10 * s);
        ctx.bezierCurveTo(
          fx + 13 * s,
          hY - 7 * s,
          fx + 14 * s,
          hY - 3 * s,
          fx + 12.5 * s,
          hY - 1 * s
        );
        ctx.stroke();

        // Hind quarters with gradient
        const hqG = ctx.createRadialGradient(
          fx + 3 * s,
          hY - 11 * s,
          1 * s,
          fx + 4 * s,
          hY - 10 * s,
          9 * s
        );
        hqG.addColorStop(0, figureLight);
        hqG.addColorStop(0.5, figureMid);
        hqG.addColorStop(1, figureDark);
        ctx.fillStyle = hqG;
        ctx.beginPath();
        ctx.ellipse(
          fx + 4 * s,
          hY - 10 * s,
          7 * s,
          7.5 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Haunch muscle
        ctx.fillStyle = "rgba(200,195,185,0.12)";
        ctx.beginPath();
        ctx.ellipse(fx + 2 * s, hY - 12 * s, 3 * s, 3 * s, 0.2, 0, Math.PI * 2);
        ctx.fill();

        // Barrel with gradient
        const barG = ctx.createLinearGradient(
          fx - 6 * s,
          hY - 17 * s,
          fx + 3 * s,
          hY - 4 * s
        );
        barG.addColorStop(0, figureLight);
        barG.addColorStop(0.5, figureMid);
        barG.addColorStop(1, figureDark);
        ctx.fillStyle = barG;
        ctx.beginPath();
        ctx.moveTo(fx + 2 * s, hY - 16 * s);
        ctx.bezierCurveTo(
          fx + 1 * s,
          hY - 4 * s,
          fx - 5 * s,
          hY - 4 * s,
          fx - 6 * s,
          hY - 5 * s
        );
        ctx.lineTo(fx - 6 * s, hY - 16 * s);
        ctx.closePath();
        ctx.fill();
        // Belly shadow
        ctx.fillStyle = "rgba(60,55,45,0.08)";
        ctx.beginPath();
        ctx.ellipse(fx - 2 * s, hY - 5 * s, 5 * s, 1.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Chest with gradient
        const chG = ctx.createRadialGradient(
          fx - 7 * s,
          hY - 14 * s,
          1 * s,
          fx - 6 * s,
          hY - 12 * s,
          8 * s
        );
        chG.addColorStop(0, figureHighlight);
        chG.addColorStop(0.4, figureLight);
        chG.addColorStop(1, figureMid);
        ctx.fillStyle = chG;
        ctx.beginPath();
        ctx.ellipse(
          fx - 6 * s,
          hY - 12 * s,
          5.5 * s,
          7.5 * s,
          -0.15,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Horse legs (shaped with joints)
        const drawHorseLeg = (
          lx: number,
          ly: number,
          h: number,
          knee: number,
          col: string
        ) => {
          const lg = ctx.createLinearGradient(
            lx - 1.5 * s,
            ly,
            lx + 1.5 * s,
            ly + h * s
          );
          lg.addColorStop(0, col);
          lg.addColorStop(1, figureShadow);
          ctx.fillStyle = lg;
          ctx.beginPath();
          ctx.moveTo(lx - 1.5 * s, ly);
          ctx.quadraticCurveTo(
            lx - 2 * s + knee * s,
            ly + h * 0.45 * s,
            lx - 1.2 * s + knee * 0.5 * s,
            ly + h * 0.7 * s
          );
          ctx.lineTo(lx - 1.5 * s + knee * 0.3 * s, ly + h * s);
          ctx.lineTo(lx + 1.5 * s + knee * 0.3 * s, ly + h * s);
          ctx.lineTo(lx + 1.2 * s + knee * 0.5 * s, ly + h * 0.7 * s);
          ctx.quadraticCurveTo(
            lx + 1.5 * s + knee * s,
            ly + h * 0.45 * s,
            lx + 1.5 * s,
            ly
          );
          ctx.closePath();
          ctx.fill();
          // Hoof
          ctx.fillStyle = stoneDeep;
          ctx.beginPath();
          ctx.ellipse(
            lx + knee * 0.3 * s,
            ly + (h + 0.5) * s,
            2.2 * s,
            1 * s,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();
        };
        drawHorseLeg(fx + 3 * s, hY - 4 * s, 6, 0.5, figureDark);
        drawHorseLeg(fx + 6.5 * s, hY - 4 * s, 6, 0.8, figureMid);
        drawHorseLeg(fx - 7 * s, hY - 6 * s, 8, -0.5, figureMid);
        drawHorseLeg(fx - 4.5 * s, hY - 6 * s, 8, -0.3, figureLight);

        // Horse neck with gradient
        const neckG = ctx.createLinearGradient(
          fx - 10 * s,
          hY - 30 * s,
          fx - 4 * s,
          hY - 15 * s
        );
        neckG.addColorStop(0, figureLight);
        neckG.addColorStop(0.6, figureMid);
        neckG.addColorStop(1, figureDark);
        ctx.fillStyle = neckG;
        ctx.beginPath();
        ctx.moveTo(fx - 7 * s, hY - 16 * s);
        ctx.bezierCurveTo(
          fx - 9 * s,
          hY - 22 * s,
          fx - 10 * s,
          hY - 28 * s,
          fx - 8 * s,
          hY - 30 * s
        );
        ctx.lineTo(fx - 5 * s, hY - 28 * s);
        ctx.bezierCurveTo(
          fx - 6 * s,
          hY - 24 * s,
          fx - 6 * s,
          hY - 20 * s,
          fx - 4 * s,
          hY - 15 * s
        );
        ctx.closePath();
        ctx.fill();
        // Neck muscle line
        ctx.strokeStyle = "rgba(200,195,185,0.1)";
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, hY - 17 * s);
        ctx.bezierCurveTo(
          fx - 8 * s,
          hY - 22 * s,
          fx - 9 * s,
          hY - 26 * s,
          fx - 7 * s,
          hY - 29 * s
        );
        ctx.stroke();

        // Mane (filled flowing shape)
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 8.5 * s, hY - 30 * s);
        ctx.bezierCurveTo(
          fx - 11 * s,
          hY - 28 * s,
          fx - 12 * s,
          hY - 24 * s,
          fx - 10 * s,
          hY - 20 * s
        );
        ctx.bezierCurveTo(
          fx - 11 * s,
          hY - 18 * s,
          fx - 10 * s,
          hY - 16 * s,
          fx - 8 * s,
          hY - 16 * s
        );
        ctx.bezierCurveTo(
          fx - 9 * s,
          hY - 19 * s,
          fx - 10 * s,
          hY - 23 * s,
          fx - 9 * s,
          hY - 27 * s
        );
        ctx.closePath();
        ctx.fill();
        // Mane strands
        ctx.strokeStyle = figureDark;
        ctx.lineWidth = 0.5 * s;
        for (let mi = 0; mi < 4; mi++) {
          const mt2 = mi / 3;
          const my2 = hY - 28 * s + mt2 * 11 * s;
          const mx2 = fx - 9 * s + mt2 * 1 * s;
          ctx.beginPath();
          ctx.moveTo(mx2, my2);
          ctx.quadraticCurveTo(
            mx2 - 2 * s,
            my2 + 1.5 * s,
            mx2 - 1 * s,
            my2 + 3 * s
          );
          ctx.stroke();
        }

        // Horse head with gradient
        const hhG = ctx.createRadialGradient(
          fx - 10 * s,
          hY - 31 * s,
          1 * s,
          fx - 11 * s,
          hY - 30 * s,
          6 * s
        );
        hhG.addColorStop(0, figureHighlight);
        hhG.addColorStop(0.4, figureLight);
        hhG.addColorStop(1, figureMid);
        ctx.fillStyle = hhG;
        ctx.beginPath();
        ctx.moveTo(fx - 8 * s, hY - 30 * s);
        ctx.bezierCurveTo(
          fx - 9 * s,
          hY - 34 * s,
          fx - 13 * s,
          hY - 35 * s,
          fx - 14 * s,
          hY - 31 * s
        );
        ctx.bezierCurveTo(
          fx - 14 * s,
          hY - 28 * s,
          fx - 11 * s,
          hY - 27 * s,
          fx - 8 * s,
          hY - 28 * s
        );
        ctx.closePath();
        ctx.fill();
        // Jaw line
        ctx.strokeStyle = figureDark;
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 13.5 * s, hY - 30 * s);
        ctx.bezierCurveTo(
          fx - 13 * s,
          hY - 28 * s,
          fx - 11 * s,
          hY - 27.5 * s,
          fx - 9 * s,
          hY - 28 * s
        );
        ctx.stroke();
        // Nostrils
        ctx.fillStyle = stoneDeep;
        ctx.beginPath();
        ctx.ellipse(
          fx - 13.5 * s,
          hY - 30 * s,
          0.6 * s,
          0.4 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Eye (deep set)
        ctx.fillStyle = stoneDeep;
        ctx.beginPath();
        ctx.ellipse(
          fx - 10.5 * s,
          hY - 32 * s,
          1 * s,
          0.7 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(
          fx - 10.5 * s,
          hY - 32 * s,
          0.6 * s,
          0.4 * s,
          -0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();
        // Ears
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx - 9 * s, hY - 33 * s);
        ctx.lineTo(fx - 10 * s, hY - 37 * s);
        ctx.lineTo(fx - 8 * s, hY - 34 * s);
        ctx.closePath();
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(fx - 7.5 * s, hY - 32.5 * s);
        ctx.lineTo(fx - 8 * s, hY - 36 * s);
        ctx.lineTo(fx - 6.5 * s, hY - 33 * s);
        ctx.closePath();
        ctx.fill();

        // Bridle
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 9 * s, hY - 33 * s);
        ctx.lineTo(fx - 13 * s, hY - 31 * s);
        ctx.lineTo(fx - 12 * s, hY - 28 * s);
        ctx.stroke();

        // Saddle
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 3 * s, hY - 17 * s);
        ctx.bezierCurveTo(
          fx - 2 * s,
          hY - 18 * s,
          fx + 2 * s,
          hY - 18 * s,
          fx + 3 * s,
          hY - 17 * s
        );
        ctx.lineTo(fx + 2 * s, hY - 15 * s);
        ctx.bezierCurveTo(
          fx + 1 * s,
          hY - 15.5 * s,
          fx - 1 * s,
          hY - 15.5 * s,
          fx - 2 * s,
          hY - 15 * s
        );
        ctx.closePath();
        ctx.fill();
        // Saddle blanket edge
        ctx.strokeStyle = figureDark;
        ctx.lineWidth = 0.8 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, hY - 15 * s);
        ctx.quadraticCurveTo(fx, hY - 14 * s, fx + 4 * s, hY - 15 * s);
        ctx.stroke();

        // Rider legs (shaped)
        const rY = hY - 17 * s;
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 3.5 * s, rY + 1 * s);
        ctx.quadraticCurveTo(fx - 4 * s, rY + 4 * s, fx - 3 * s, rY + 6 * s);
        ctx.lineTo(fx - 1 * s, rY + 6 * s);
        ctx.quadraticCurveTo(
          fx - 1.5 * s,
          rY + 3 * s,
          fx - 1.5 * s,
          rY + 1 * s
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 1.5 * s, rY + 1 * s);
        ctx.quadraticCurveTo(fx + 2 * s, rY + 4 * s, fx + 2 * s, rY + 6 * s);
        ctx.lineTo(fx + 3.5 * s, rY + 6 * s);
        ctx.quadraticCurveTo(fx + 3.5 * s, rY + 3 * s, fx + 3 * s, rY + 1 * s);
        ctx.closePath();
        ctx.fill();
        // Boots
        ctx.fillStyle = stoneDeep;
        ctx.beginPath();
        ctx.ellipse(fx - 2 * s, rY + 6.5 * s, 2 * s, 1 * s, 0, 0, Math.PI * 2);
        ctx.ellipse(
          fx + 2.5 * s,
          rY + 6.5 * s,
          2 * s,
          1 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Rider torso with armor gradient
        const rtG = ctx.createLinearGradient(
          fx - 3 * s,
          rY - 14 * s,
          fx + 3 * s,
          rY
        );
        rtG.addColorStop(0, figureLight);
        rtG.addColorStop(0.4, figureMid);
        rtG.addColorStop(1, figureDark);
        ctx.fillStyle = rtG;
        ctx.beginPath();
        ctx.moveTo(fx - 3.5 * s, rY + 1 * s);
        ctx.bezierCurveTo(
          fx - 4 * s,
          rY - 5 * s,
          fx - 3.5 * s,
          rY - 12 * s,
          fx - 2 * s,
          rY - 14 * s
        );
        ctx.lineTo(fx + 2 * s, rY - 14 * s);
        ctx.bezierCurveTo(
          fx + 3.5 * s,
          rY - 12 * s,
          fx + 4 * s,
          rY - 5 * s,
          fx + 3.5 * s,
          rY + 1 * s
        );
        ctx.closePath();
        ctx.fill();
        // Armor edge
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx, rY - 14 * s);
        ctx.lineTo(fx, rY + 1 * s);
        ctx.stroke();
        // Shoulder pads
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.ellipse(
          fx - 4 * s,
          rY - 12 * s,
          2.5 * s,
          1.5 * s,
          -0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureLight;
        ctx.beginPath();
        ctx.ellipse(
          fx + 4 * s,
          rY - 12 * s,
          2.5 * s,
          1.5 * s,
          0.3,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Rider head with gradient
        const rhG = ctx.createRadialGradient(
          fx - 0.5 * s,
          rY - 17 * s,
          0,
          fx,
          rY - 16.5 * s,
          4.5 * s
        );
        rhG.addColorStop(0, figureHighlight);
        rhG.addColorStop(0.4, figureLight);
        rhG.addColorStop(0.8, figureMid);
        rhG.addColorStop(1, figureDark);
        ctx.fillStyle = rhG;
        ctx.beginPath();
        ctx.ellipse(fx, rY - 17 * s, 3.8 * s, 4.2 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        // Helmet
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(
          fx,
          rY - 19.5 * s,
          4.2 * s,
          2.8 * s,
          0,
          Math.PI,
          Math.PI * 2
        );
        ctx.fill();
        // Helmet crest
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx, rY - 22 * s);
        ctx.bezierCurveTo(
          fx + 1 * s,
          rY - 21 * s,
          fx + 3 * s,
          rY - 19 * s,
          fx + 5 * s,
          rY - 18 * s
        );
        ctx.lineTo(fx + 4 * s, rY - 19 * s);
        ctx.bezierCurveTo(
          fx + 2 * s,
          rY - 19.5 * s,
          fx + 1 * s,
          rY - 20.5 * s,
          fx,
          rY - 21 * s
        );
        ctx.closePath();
        ctx.fill();
        // Visor slit
        ctx.strokeStyle = stoneDeep;
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 2 * s, rY - 16.5 * s);
        ctx.lineTo(fx + 2 * s, rY - 16.5 * s);
        ctx.stroke();

        // Lance with detail (all parts in same rotated transform)
        ctx.save();
        ctx.translate(fx + 4 * s, rY - 10 * s);
        ctx.rotate(-0.12);
        // Shaft with gradient
        const lnG = ctx.createLinearGradient(0, -42 * s, 0, 0);
        lnG.addColorStop(0, "#7a6a5a");
        lnG.addColorStop(0.5, "#5a4a3a");
        lnG.addColorStop(1, "#4a3a2a");
        ctx.fillStyle = lnG;
        ctx.fillRect(-1 * s, -42 * s, 2 * s, 42 * s);
        // Lance tip (aligned to shaft top in local coords)
        const ltG = ctx.createLinearGradient(0, -49 * s, 0, -42 * s);
        ltG.addColorStop(0, steelLight);
        ltG.addColorStop(1, figureShadow);
        ctx.fillStyle = ltG;
        ctx.beginPath();
        ctx.moveTo(0, -49 * s);
        ctx.lineTo(-1.5 * s, -42 * s);
        ctx.lineTo(1.5 * s, -42 * s);
        ctx.closePath();
        ctx.fill();
        // Pennant (just below tip in local coords)
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(-1 * s, -41 * s);
        ctx.bezierCurveTo(-4 * s, -39 * s, -5 * s, -37 * s, -4 * s, -35 * s);
        ctx.lineTo(-1 * s, -37 * s);
        ctx.closePath();
        ctx.fill();
        ctx.restore();

        // Shield on rider's left
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, rY - 10 * s);
        ctx.quadraticCurveTo(fx - 7 * s, rY - 8 * s, fx - 6 * s, rY - 3 * s);
        ctx.quadraticCurveTo(fx - 5 * s, rY - 2 * s, fx - 4 * s, rY - 3 * s);
        ctx.quadraticCurveTo(fx - 3 * s, rY - 7 * s, fx - 4 * s, rY - 10 * s);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 0.4 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, rY - 9 * s);
        ctx.lineTo(fx - 5 * s, rY - 3.5 * s);
        ctx.stroke();
      } else if (sv === 6) {
        // === VARIANT 6: CENTAUR ARCHER ===
        const cY = figureBase - 2 * s;

        // Tail with volume
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx + 7 * s, cY - 10 * s);
        ctx.bezierCurveTo(
          fx + 11 * s,
          cY - 8 * s,
          fx + 13 * s,
          cY - 3 * s,
          fx + 11 * s,
          cY + 1 * s
        );
        ctx.bezierCurveTo(
          fx + 10 * s,
          cY - 2 * s,
          fx + 9 * s,
          cY - 6 * s,
          fx + 7 * s,
          cY - 9 * s
        );
        ctx.closePath();
        ctx.fill();

        // Hind quarters with gradient
        const cHqG = ctx.createRadialGradient(
          fx + 2 * s,
          cY - 9 * s,
          1 * s,
          fx + 3 * s,
          cY - 8 * s,
          7 * s
        );
        cHqG.addColorStop(0, figureLight);
        cHqG.addColorStop(0.5, figureMid);
        cHqG.addColorStop(1, figureDark);
        ctx.fillStyle = cHqG;
        ctx.beginPath();
        ctx.ellipse(fx + 3 * s, cY - 8 * s, 6 * s, 6 * s, 0.1, 0, Math.PI * 2);
        ctx.fill();
        // Haunch muscle
        ctx.fillStyle = "rgba(200,195,185,0.12)";
        ctx.beginPath();
        ctx.ellipse(
          fx + 1 * s,
          cY - 10 * s,
          2.5 * s,
          2.5 * s,
          0.2,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Barrel with gradient
        const cBarG = ctx.createLinearGradient(
          fx - 5 * s,
          cY - 14 * s,
          fx + 2 * s,
          cY - 3 * s
        );
        cBarG.addColorStop(0, figureLight);
        cBarG.addColorStop(0.5, figureMid);
        cBarG.addColorStop(1, figureDark);
        ctx.fillStyle = cBarG;
        ctx.beginPath();
        ctx.moveTo(fx + 1 * s, cY - 13 * s);
        ctx.bezierCurveTo(
          fx + 1 * s,
          cY - 4 * s,
          fx - 4 * s,
          cY - 3 * s,
          fx - 5 * s,
          cY - 4 * s
        );
        ctx.lineTo(fx - 5 * s, cY - 13 * s);
        ctx.closePath();
        ctx.fill();
        // Belly shadow
        ctx.fillStyle = "rgba(60,55,45,0.08)";
        ctx.beginPath();
        ctx.ellipse(fx - 2 * s, cY - 4 * s, 4 * s, 1.2 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Front chest with gradient
        const cChG = ctx.createRadialGradient(
          fx - 5 * s,
          cY - 12 * s,
          1 * s,
          fx - 4 * s,
          cY - 10 * s,
          7 * s
        );
        cChG.addColorStop(0, figureHighlight);
        cChG.addColorStop(0.4, figureLight);
        cChG.addColorStop(1, figureMid);
        ctx.fillStyle = cChG;
        ctx.beginPath();
        ctx.ellipse(
          fx - 4 * s,
          cY - 10 * s,
          4.5 * s,
          6.5 * s,
          -0.15,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Horse legs (shaped with joints)
        const drawCentLeg = (
          lx: number,
          ly: number,
          h: number,
          knee: number,
          col: string
        ) => {
          const clG = ctx.createLinearGradient(
            lx - 1.2 * s,
            ly,
            lx + 1.2 * s,
            ly + h * s
          );
          clG.addColorStop(0, col);
          clG.addColorStop(1, figureShadow);
          ctx.fillStyle = clG;
          ctx.beginPath();
          ctx.moveTo(lx - 1.2 * s, ly);
          ctx.quadraticCurveTo(
            lx - 1.5 * s + knee * s,
            ly + h * 0.4 * s,
            lx - 1 * s + knee * 0.5 * s,
            ly + h * 0.7 * s
          );
          ctx.lineTo(lx - 1.2 * s + knee * 0.3 * s, ly + h * s);
          ctx.lineTo(lx + 1.2 * s + knee * 0.3 * s, ly + h * s);
          ctx.lineTo(lx + 1 * s + knee * 0.5 * s, ly + h * 0.7 * s);
          ctx.quadraticCurveTo(
            lx + 1.5 * s + knee * s,
            ly + h * 0.4 * s,
            lx + 1.2 * s,
            ly
          );
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = stoneDeep;
          ctx.beginPath();
          ctx.ellipse(
            lx + knee * 0.3 * s,
            ly + (h + 0.5) * s,
            1.8 * s,
            0.8 * s,
            0,
            0,
            Math.PI * 2
          );
          ctx.fill();
        };
        drawCentLeg(fx + 2 * s, cY - 3 * s, 5, 0.4, figureDark);
        drawCentLeg(fx + 5 * s, cY - 3 * s, 5, 0.6, figureMid);
        drawCentLeg(fx - 5 * s, cY - 5 * s, 7, -0.4, figureMid);
        drawCentLeg(fx - 3 * s, cY - 5 * s, 7, -0.2, figureLight);

        // Human torso with gradient and muscle definition
        const cTB = cY - 14 * s;
        const ctG = ctx.createLinearGradient(
          fx - 5 * s,
          cTB - 18 * s,
          fx + 2 * s,
          cTB
        );
        ctG.addColorStop(0, figureLight);
        ctG.addColorStop(0.4, figureMid);
        ctG.addColorStop(1, figureDark);
        ctx.fillStyle = ctG;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, cTB);
        ctx.bezierCurveTo(
          fx - 4.5 * s,
          cTB - 6 * s,
          fx - 5 * s,
          cTB - 14 * s,
          fx - 3 * s,
          cTB - 18 * s
        );
        ctx.lineTo(fx + 1 * s, cTB - 18 * s);
        ctx.bezierCurveTo(
          fx + 2 * s,
          cTB - 14 * s,
          fx + 2 * s,
          cTB - 6 * s,
          fx + 1 * s,
          cTB
        );
        ctx.closePath();
        ctx.fill();
        // Center line / muscle def
        ctx.strokeStyle = "rgba(80,75,65,0.08)";
        ctx.lineWidth = 0.4 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 1 * s, cTB - 2 * s);
        ctx.lineTo(fx - 1 * s, cTB - 16 * s);
        ctx.stroke();
        // Chest highlight
        ctx.fillStyle = "rgba(200,195,185,0.1)";
        ctx.beginPath();
        ctx.ellipse(
          fx - 2 * s,
          cTB - 14 * s,
          2 * s,
          2.5 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Leather strap across chest
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, cTB - 16 * s);
        ctx.lineTo(fx + 1 * s, cTB - 8 * s);
        ctx.stroke();
        ctx.strokeStyle = figureDark;
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, cTB - 15.5 * s);
        ctx.lineTo(fx + 1 * s, cTB - 7.5 * s);
        ctx.stroke();

        // Left arm extended holding bow (muscular)
        const armG = ctx.createLinearGradient(
          fx - 4 * s,
          cTB - 15 * s,
          fx - 12 * s,
          cTB - 15 * s
        );
        armG.addColorStop(0, figureMid);
        armG.addColorStop(1, figureDark);
        ctx.fillStyle = armG;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, cTB - 15.5 * s);
        ctx.bezierCurveTo(
          fx - 7 * s,
          cTB - 17 * s,
          fx - 10 * s,
          cTB - 17 * s,
          fx - 12 * s,
          cTB - 16 * s
        );
        ctx.lineTo(fx - 12 * s, cTB - 14 * s);
        ctx.bezierCurveTo(
          fx - 10 * s,
          cTB - 14.5 * s,
          fx - 7 * s,
          cTB - 13 * s,
          fx - 4 * s,
          cTB - 12.5 * s
        );
        ctx.closePath();
        ctx.fill();
        // Bicep highlight
        ctx.fillStyle = "rgba(200,195,185,0.1)";
        ctx.beginPath();
        ctx.ellipse(
          fx - 6 * s,
          cTB - 15 * s,
          1.5 * s,
          1 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();

        // Right arm drawn back
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 1 * s, cTB - 15 * s);
        ctx.bezierCurveTo(
          fx + 3 * s,
          cTB - 14 * s,
          fx + 4.5 * s,
          cTB - 13 * s,
          fx + 5 * s,
          cTB - 14 * s
        );
        ctx.lineTo(fx + 5 * s, cTB - 15.5 * s);
        ctx.bezierCurveTo(
          fx + 3.5 * s,
          cTB - 16 * s,
          fx + 2 * s,
          cTB - 16 * s,
          fx + 1 * s,
          cTB - 16 * s
        );
        ctx.closePath();
        ctx.fill();

        // Bow (thicker, recurve shape)
        const bowG = ctx.createLinearGradient(
          fx - 13 * s,
          cTB - 22 * s,
          fx - 13 * s,
          cTB - 8 * s
        );
        bowG.addColorStop(0, "#7a6a5a");
        bowG.addColorStop(0.5, "#5a4a3a");
        bowG.addColorStop(1, "#7a6a5a");
        ctx.strokeStyle = bowG;
        ctx.lineWidth = 2 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 13 * s, cTB - 7 * s);
        ctx.bezierCurveTo(
          fx - 17 * s,
          cTB - 12 * s,
          fx - 17 * s,
          cTB - 18 * s,
          fx - 13 * s,
          cTB - 23 * s
        );
        ctx.stroke();
        // Recurve tips
        ctx.strokeStyle = "#5a4a3a";
        ctx.lineWidth = 1.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 13 * s, cTB - 7 * s);
        ctx.quadraticCurveTo(
          fx - 12 * s,
          cTB - 5 * s,
          fx - 11 * s,
          cTB - 6 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(fx - 13 * s, cTB - 23 * s);
        ctx.quadraticCurveTo(
          fx - 12 * s,
          cTB - 25 * s,
          fx - 11 * s,
          cTB - 24 * s
        );
        ctx.stroke();

        // Bowstring (taut)
        ctx.strokeStyle = figureShadow;
        ctx.lineWidth = 0.5 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 13 * s, cTB - 7 * s);
        ctx.lineTo(fx + 4 * s, cTB - 14 * s);
        ctx.lineTo(fx - 13 * s, cTB - 23 * s);
        ctx.stroke();

        // Arrow nocked
        ctx.save();
        ctx.translate(fx - 4 * s, cTB - 14.5 * s);
        ctx.rotate(0.1);
        const arG = ctx.createLinearGradient(0, 0, 15 * s, 0);
        arG.addColorStop(0, "#5a4a3a");
        arG.addColorStop(1, "#7a6a5a");
        ctx.fillStyle = arG;
        ctx.fillRect(0, -0.6 * s, 15 * s, 1.2 * s);
        ctx.restore();
        // Arrowhead
        const ahG = ctx.createLinearGradient(
          fx - 18 * s,
          cTB - 15 * s,
          fx - 14 * s,
          cTB - 15 * s
        );
        ahG.addColorStop(0, steelLight);
        ahG.addColorStop(1, figureShadow);
        ctx.fillStyle = ahG;
        ctx.beginPath();
        ctx.moveTo(fx - 18 * s, cTB - 15 * s);
        ctx.lineTo(fx - 14.5 * s, cTB - 16.5 * s);
        ctx.lineTo(fx - 14.5 * s, cTB - 13.5 * s);
        ctx.closePath();
        ctx.fill();
        // Fletching
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx + 8 * s, cTB - 14 * s);
        ctx.lineTo(fx + 10 * s, cTB - 16 * s);
        ctx.lineTo(fx + 10 * s, cTB - 14 * s);
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(fx + 8 * s, cTB - 14 * s);
        ctx.lineTo(fx + 10 * s, cTB - 12 * s);
        ctx.lineTo(fx + 10 * s, cTB - 14 * s);
        ctx.fill();

        // Quiver on back
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx + 1 * s, cTB - 17 * s);
        ctx.lineTo(fx + 3 * s, cTB - 18 * s);
        ctx.lineTo(fx + 4 * s, cTB - 6 * s);
        ctx.lineTo(fx + 2 * s, cTB - 5 * s);
        ctx.closePath();
        ctx.fill();
        // Arrow shafts sticking out
        ctx.strokeStyle = "#6a5a4a";
        ctx.lineWidth = 0.5 * s;
        for (let qi = 0; qi < 3; qi++) {
          ctx.beginPath();
          ctx.moveTo(fx + (2 + qi * 0.5) * s, cTB - 17.5 * s);
          ctx.lineTo(fx + (1.5 + qi * 0.6) * s, cTB - 21 * s - qi * 0.5 * s);
          ctx.stroke();
        }

        // Head with gradient
        const cHdY = cTB - 21 * s;
        const cHdG = ctx.createRadialGradient(
          fx - 1.5 * s,
          cHdY - 1 * s,
          0,
          fx - 1 * s,
          cHdY,
          5 * s
        );
        cHdG.addColorStop(0, figureHighlight);
        cHdG.addColorStop(0.35, figureLight);
        cHdG.addColorStop(0.7, figureMid);
        cHdG.addColorStop(1, figureDark);
        ctx.fillStyle = cHdG;
        ctx.beginPath();
        ctx.ellipse(fx - 1 * s, cHdY, 4.2 * s, 4.8 * s, 0, 0, Math.PI * 2);
        ctx.fill();

        // Flowing hair
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx + 2 * s, cHdY - 3 * s);
        ctx.bezierCurveTo(
          fx + 5 * s,
          cHdY - 1 * s,
          fx + 7 * s,
          cHdY + 2 * s,
          fx + 6 * s,
          cHdY + 8 * s
        );
        ctx.bezierCurveTo(
          fx + 5 * s,
          cHdY + 5 * s,
          fx + 4 * s,
          cHdY + 2 * s,
          fx + 3 * s,
          cHdY
        );
        ctx.closePath();
        ctx.fill();
        // Hair strands
        ctx.strokeStyle = figureDark;
        ctx.lineWidth = 0.4 * s;
        for (let hi = 0; hi < 3; hi++) {
          ctx.beginPath();
          ctx.moveTo(fx + (3 + hi) * s, cHdY + hi * s);
          ctx.bezierCurveTo(
            fx + (5 + hi) * s,
            cHdY + (2 + hi) * s,
            fx + (6 + hi * 0.5) * s,
            cHdY + (4 + hi) * s,
            fx + (5.5 + hi * 0.3) * s,
            cHdY + (6 + hi) * s
          );
          ctx.stroke();
        }

        // Eyes (deep set)
        ctx.fillStyle = stoneDeep;
        ctx.beginPath();
        ctx.ellipse(
          fx - 2.8 * s,
          cHdY - 0.5 * s,
          1 * s,
          0.7 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.ellipse(
          fx + 0.8 * s,
          cHdY - 0.5 * s,
          1 * s,
          0.7 * s,
          0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.arc(fx - 2.8 * s, cHdY - 0.5 * s, 0.5 * s, 0, Math.PI * 2);
        ctx.arc(fx + 0.8 * s, cHdY - 0.5 * s, 0.5 * s, 0, Math.PI * 2);
        ctx.fill();

        // Pointed ears
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx - 4 * s, cHdY - 1 * s);
        ctx.lineTo(fx - 7 * s, cHdY - 3 * s);
        ctx.lineTo(fx - 4.5 * s, cHdY + 0.5 * s);
        ctx.closePath();
        ctx.fill();

        // Brow / stern expression
        ctx.strokeStyle = figureDark;
        ctx.lineWidth = 0.6 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 3.5 * s, cHdY - 1.5 * s);
        ctx.quadraticCurveTo(
          fx - 2.5 * s,
          cHdY - 2 * s,
          fx - 1.5 * s,
          cHdY - 1.5 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(fx - 0.2 * s, cHdY - 1.5 * s);
        ctx.quadraticCurveTo(
          fx + 0.5 * s,
          cHdY - 2 * s,
          fx + 1.5 * s,
          cHdY - 1.5 * s
        );
        ctx.stroke();
      } else {
        // === VARIANT 7: GENERAL / COMMANDER with banner ===
        // Armored legs
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, figureBase);
        ctx.lineTo(fx - 5 * s, figureBase - 17 * s);
        ctx.lineTo(fx - 1 * s, figureBase - 19 * s);
        ctx.lineTo(fx - 1 * s, figureBase);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 5 * s, figureBase);
        ctx.lineTo(fx + 5 * s, figureBase - 17 * s);
        ctx.lineTo(fx + 1 * s, figureBase - 19 * s);
        ctx.lineTo(fx + 1 * s, figureBase);
        ctx.closePath();
        ctx.fill();

        // Torso with ornate breastplate
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, figureBase - 17 * s);
        ctx.lineTo(fx - 6 * s, figureBase - 32 * s);
        ctx.lineTo(fx, figureBase - 35 * s);
        ctx.lineTo(fx, figureBase - 19 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 5 * s, figureBase - 17 * s);
        ctx.lineTo(fx + 6 * s, figureBase - 32 * s);
        ctx.lineTo(fx, figureBase - 35 * s);
        ctx.lineTo(fx, figureBase - 19 * s);
        ctx.closePath();
        ctx.fill();
        // Breastplate emblem
        ctx.fillStyle = figureLight;
        ctx.beginPath();
        ctx.moveTo(fx, figureBase - 30 * s);
        ctx.lineTo(fx - 2 * s, figureBase - 27 * s);
        ctx.lineTo(fx, figureBase - 24 * s);
        ctx.lineTo(fx + 2 * s, figureBase - 27 * s);
        ctx.closePath();
        ctx.fill();

        // Cape flowing behind both sides
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, figureBase - 31 * s);
        ctx.quadraticCurveTo(
          fx - 14 * s,
          figureBase - 28 * s,
          fx - 12 * s,
          figureBase - 6 * s
        );
        ctx.quadraticCurveTo(
          fx - 10 * s,
          figureBase - 2 * s,
          fx - 8 * s,
          figureBase
        );
        ctx.lineTo(fx - 6 * s, figureBase - 17 * s);
        ctx.closePath();
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(fx + 5 * s, figureBase - 31 * s);
        ctx.quadraticCurveTo(
          fx + 14 * s,
          figureBase - 28 * s,
          fx + 12 * s,
          figureBase - 6 * s
        );
        ctx.quadraticCurveTo(
          fx + 10 * s,
          figureBase - 2 * s,
          fx + 8 * s,
          figureBase
        );
        ctx.lineTo(fx + 6 * s, figureBase - 17 * s);
        ctx.closePath();
        ctx.fill();

        // Left arm with sword at side
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx - 5 * s, figureBase - 30 * s);
        ctx.lineTo(fx - 8 * s, figureBase - 18 * s);
        ctx.lineTo(fx - 6 * s, figureBase - 17 * s);
        ctx.lineTo(fx - 4 * s, figureBase - 28 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = steelLight;
        ctx.beginPath();
        ctx.moveTo(fx - 8 * s, figureBase - 18 * s);
        ctx.lineTo(fx - 9 * s, figureBase - 2 * s);
        ctx.lineTo(fx - 8.5 * s, figureBase);
        ctx.lineTo(fx - 7 * s, figureBase - 2 * s);
        ctx.lineTo(fx - 7 * s, figureBase - 18 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = steelDark;
        ctx.beginPath();
        ctx.moveTo(fx - 8 * s, figureBase - 18 * s);
        ctx.lineTo(fx - 9 * s, figureBase - 2 * s);
        ctx.lineTo(fx - 8.5 * s, figureBase);
        ctx.lineTo(fx - 8 * s, figureBase - 2 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureDark;
        ctx.fillRect(fx - 10.5 * s, figureBase - 19 * s, 5 * s, 1.5 * s);

        // Right arm raised holding banner pole
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 4 * s, figureBase - 30 * s);
        ctx.lineTo(fx + 8 * s, figureBase - 42 * s);
        ctx.lineTo(fx + 10 * s, figureBase - 40 * s);
        ctx.lineTo(fx + 6 * s, figureBase - 28 * s);
        ctx.closePath();
        ctx.fill();

        // Banner pole
        ctx.fillStyle = "#5a4a3a";
        ctx.fillRect(fx + 8.5 * s, figureBase - 68 * s, 2 * s, 50 * s);
        // Banner flag
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx + 10.5 * s, figureBase - 68 * s);
        ctx.lineTo(fx + 22 * s, figureBase - 65 * s);
        ctx.quadraticCurveTo(
          fx + 20 * s,
          figureBase - 58 * s,
          fx + 22 * s,
          figureBase - 52 * s
        );
        ctx.lineTo(fx + 10.5 * s, figureBase - 55 * s);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx + 10.5 * s, figureBase - 68 * s);
        ctx.lineTo(fx + 16 * s, figureBase - 66 * s);
        ctx.quadraticCurveTo(
          fx + 15 * s,
          figureBase - 61 * s,
          fx + 16 * s,
          figureBase - 56 * s
        );
        ctx.lineTo(fx + 10.5 * s, figureBase - 55 * s);
        ctx.closePath();
        ctx.fill();
        // Banner star emblem
        ctx.fillStyle = figureLight;
        const bex = fx + 16 * s;
        const bey = figureBase - 61 * s;
        ctx.beginPath();
        for (let sp = 0; sp < 5; sp++) {
          const angle = (sp / 5) * Math.PI * 2 - Math.PI / 2;
          const innerAngle = angle + Math.PI / 5;
          const ox = bex + Math.cos(angle) * 3 * s;
          const oy = bey + Math.sin(angle) * 3 * s;
          const ix2 = bex + Math.cos(innerAngle) * 1.2 * s;
          const iy2 = bey + Math.sin(innerAngle) * 1.2 * s;
          if (sp === 0) {
            ctx.moveTo(ox, oy);
          } else {
            ctx.lineTo(ox, oy);
          }
          ctx.lineTo(ix2, iy2);
        }
        ctx.closePath();
        ctx.fill();
        // Pole top finial
        ctx.fillStyle = steelLight;
        ctx.beginPath();
        ctx.moveTo(fx + 9.5 * s, figureBase - 68 * s);
        ctx.lineTo(fx + 8 * s, figureBase - 71 * s);
        ctx.lineTo(fx + 9.5 * s, figureBase - 73 * s);
        ctx.lineTo(fx + 11 * s, figureBase - 71 * s);
        ctx.closePath();
        ctx.fill();

        // Head with plumed helm
        const gHeadY = figureBase - 39 * s;
        const gHG = ctx.createRadialGradient(
          fx - 1 * s,
          gHeadY - 1 * s,
          0,
          fx,
          gHeadY,
          5.5 * s
        );
        gHG.addColorStop(0, figureHighlight);
        gHG.addColorStop(0.4, figureLight);
        gHG.addColorStop(0.8, figureMid);
        gHG.addColorStop(1, figureDark);
        ctx.fillStyle = gHG;
        ctx.beginPath();
        ctx.ellipse(fx, gHeadY, 4.5 * s, 5.5 * s, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.ellipse(fx, gHeadY - 3 * s, 5 * s, 3 * s, 0, Math.PI, Math.PI * 2);
        ctx.fill();
        // Plume
        ctx.fillStyle = figureDark;
        ctx.beginPath();
        ctx.moveTo(fx, gHeadY - 5.5 * s);
        ctx.quadraticCurveTo(
          fx + 2 * s,
          gHeadY - 12 * s,
          fx - 2 * s,
          gHeadY - 14 * s
        );
        ctx.quadraticCurveTo(
          fx - 4 * s,
          gHeadY - 10 * s,
          fx - 1 * s,
          gHeadY - 6 * s
        );
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = figureMid;
        ctx.beginPath();
        ctx.moveTo(fx, gHeadY - 6 * s);
        ctx.quadraticCurveTo(
          fx + 1 * s,
          gHeadY - 10 * s,
          fx - 1 * s,
          gHeadY - 12 * s
        );
        ctx.quadraticCurveTo(
          fx - 2 * s,
          gHeadY - 9 * s,
          fx - 0.5 * s,
          gHeadY - 6.5 * s
        );
        ctx.closePath();
        ctx.fill();
        // Face
        ctx.fillStyle = figureShadow;
        ctx.beginPath();
        ctx.arc(fx - 1.5 * s, gHeadY - 0.5 * s, 0.7 * s, 0, Math.PI * 2);
        ctx.arc(fx + 1.5 * s, gHeadY - 0.5 * s, 0.7 * s, 0, Math.PI * 2);
        ctx.fill();
      }

      // === SHARED FIGURE EFFECTS (all variants) ===

      // --- Ambient occlusion at figure base (where figure meets pedestal) ---
      const aoGrad = ctx.createRadialGradient(
        fx,
        figureBase + 1 * s,
        0,
        fx,
        figureBase + 1 * s,
        10 * s
      );
      aoGrad.addColorStop(0, "rgba(40,35,28,0.4)");
      aoGrad.addColorStop(0.3, "rgba(40,35,28,0.2)");
      aoGrad.addColorStop(0.7, "rgba(40,35,28,0.05)");
      aoGrad.addColorStop(1, "rgba(40,35,28,0)");
      ctx.fillStyle = aoGrad;
      ctx.beginPath();
      ctx.ellipse(fx, figureBase + 1 * s, 10 * s, 4 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // --- Left rim light (strong lit edge) ---
      const rimG = ctx.createLinearGradient(
        fx - 6 * s,
        figureBase - 35 * s,
        fx - 1 * s,
        figureBase - 20 * s
      );
      rimG.addColorStop(0, "rgba(240,235,225,0.2)");
      rimG.addColorStop(0.5, "rgba(230,225,215,0.12)");
      rimG.addColorStop(1, "rgba(220,215,205,0)");
      ctx.fillStyle = rimG;
      ctx.beginPath();
      ctx.moveTo(fx - 5 * s, figureBase - 5 * s);
      ctx.quadraticCurveTo(
        fx - 7 * s,
        figureBase - 22 * s,
        fx - 5 * s,
        figureBase - 40 * s
      );
      ctx.lineTo(fx - 3 * s, figureBase - 38 * s);
      ctx.quadraticCurveTo(
        fx - 5 * s,
        figureBase - 22 * s,
        fx - 3 * s,
        figureBase - 5 * s
      );
      ctx.closePath();
      ctx.fill();

      // --- Right shadow edge (core shadow) ---
      const coreG = ctx.createLinearGradient(
        fx + 1 * s,
        figureBase - 20 * s,
        fx + 6 * s,
        figureBase - 20 * s
      );
      coreG.addColorStop(0, "rgba(30,28,22,0)");
      coreG.addColorStop(0.5, "rgba(30,28,22,0.08)");
      coreG.addColorStop(1, "rgba(30,28,22,0.15)");
      ctx.fillStyle = coreG;
      ctx.beginPath();
      ctx.moveTo(fx + 3 * s, figureBase - 5 * s);
      ctx.quadraticCurveTo(
        fx + 6 * s,
        figureBase - 22 * s,
        fx + 4 * s,
        figureBase - 38 * s
      );
      ctx.lineTo(fx + 6 * s, figureBase - 36 * s);
      ctx.quadraticCurveTo(
        fx + 8 * s,
        figureBase - 22 * s,
        fx + 5 * s,
        figureBase - 5 * s
      );
      ctx.closePath();
      ctx.fill();

      // --- Specular highlight on upper torso/shoulder ---
      const specG = ctx.createRadialGradient(
        fx - 1 * s,
        figureBase - 30 * s,
        0,
        fx - 1 * s,
        figureBase - 30 * s,
        5 * s
      );
      specG.addColorStop(0, "rgba(255,255,250,0.18)");
      specG.addColorStop(0.4, "rgba(255,255,250,0.08)");
      specG.addColorStop(1, "rgba(255,255,250,0)");
      ctx.fillStyle = specG;
      ctx.beginPath();
      ctx.ellipse(
        fx - 1 * s,
        figureBase - 30 * s,
        5 * s,
        6 * s,
        -0.1,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // --- Secondary specular on thigh/mid ---
      ctx.fillStyle = "rgba(255,255,250,0.07)";
      ctx.beginPath();
      ctx.ellipse(
        fx + 1 * s,
        figureBase - 14 * s,
        3 * s,
        4 * s,
        0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // --- Stone grain texture (seeded micro-dots) ---
      const figSeed = Math.abs(decorX * 59 + decorY * 97);
      ctx.globalAlpha = 0.06;
      for (let sg = 0; sg < 20; sg++) {
        const sgx = fx + (((figSeed + sg * 37) % 16) - 8) * s;
        const sgy = figureBase - ((figSeed + sg * 23) % 38) * s;
        const sgr = (0.3 + ((figSeed + sg * 13) % 4) * 0.15) * s;
        ctx.fillStyle = sg % 2 === 0 ? figureShadow : figureHighlight;
        ctx.beginPath();
        ctx.arc(sgx, sgy, sgr, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.globalAlpha = 1;

      // --- Chisel/tool marks on body ---
      ctx.globalAlpha = 0.06;
      ctx.strokeStyle = stoneDeep;
      ctx.lineWidth = 0.4 * s;
      for (let cm = 0; cm < 6; cm++) {
        const cmx = fx + (((figSeed + cm * 43) % 12) - 6) * s;
        const cmy = figureBase - (5 + ((figSeed + cm * 31) % 30)) * s;
        const cmLen = (1.5 + ((figSeed + cm * 19) % 3)) * s;
        const cmAng = ((figSeed + cm * 7) % 6) * 0.3 - 0.5;
        ctx.beginPath();
        ctx.moveTo(cmx, cmy);
        ctx.lineTo(
          cmx + Math.cos(cmAng) * cmLen,
          cmy + Math.sin(cmAng) * cmLen
        );
        ctx.stroke();
      }
      ctx.globalAlpha = 1;

      // --- Weathering crack lines ---
      ctx.strokeStyle = "rgba(50,45,38,0.12)";
      ctx.lineWidth = 0.5 * s;
      // Main crack from mid to base
      ctx.beginPath();
      ctx.moveTo(fx + 3 * s, figureBase - 2 * s);
      ctx.quadraticCurveTo(
        fx + 4.5 * s,
        figureBase - 12 * s,
        fx + 3 * s,
        figureBase - 22 * s
      );
      ctx.stroke();
      // Secondary crack
      ctx.beginPath();
      ctx.moveTo(fx - 2 * s, figureBase - 8 * s);
      ctx.quadraticCurveTo(
        fx - 3 * s,
        figureBase - 14 * s,
        fx - 1 * s,
        figureBase - 18 * s
      );
      ctx.stroke();
      // Branching hairline cracks
      ctx.strokeStyle = "rgba(50,45,38,0.07)";
      ctx.lineWidth = 0.3 * s;
      ctx.beginPath();
      ctx.moveTo(fx + 4 * s, figureBase - 14 * s);
      ctx.lineTo(fx + 6 * s, figureBase - 16 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(fx - 2.5 * s, figureBase - 13 * s);
      ctx.lineTo(fx - 4 * s, figureBase - 12 * s);
      ctx.stroke();

      // --- Mineral deposit / water stain streaks ---
      ctx.globalAlpha = 0.05;
      ctx.fillStyle = stoneLight;
      for (let ms = 0; ms < 3; ms++) {
        const msx = fx + (((figSeed + ms * 71) % 10) - 5) * s;
        const msy1 = figureBase - (12 + ms * 8) * s;
        ctx.beginPath();
        ctx.ellipse(msx, msy1, 0.8 * s, 4 * s, 0.1, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.globalAlpha = 1;

      // --- Edge highlight along figure silhouette (top-left lit side) ---
      ctx.strokeStyle = "rgba(255,255,250,0.08)";
      ctx.lineWidth = 0.6 * s;
      ctx.beginPath();
      ctx.moveTo(fx - 4 * s, figureBase);
      ctx.quadraticCurveTo(
        fx - 6 * s,
        figureBase - 18 * s,
        fx - 4 * s,
        figureBase - 32 * s
      );
      ctx.quadraticCurveTo(
        fx - 2 * s,
        figureBase - 38 * s,
        fx,
        figureBase - 42 * s
      );
      ctx.stroke();

      // Regional weathering overlay on figure and pedestal
      if (mapTheme === "swamp") {
        // Heavy vine/moss overgrowth
        ctx.fillStyle = "rgba(50,80,30,0.25)";
        drawOrganicBlobAt(
          ctx,
          fx - 5 * s,
          figureBase - 2 * s,
          3 * s,
          1.5 * s,
          pedSeed + 20.1,
          0.22,
          14
        );
        ctx.fill();
        drawOrganicBlobAt(
          ctx,
          fx + 4 * s,
          figureBase - 3 * s,
          2.5 * s,
          1.2 * s,
          pedSeed + 21.3,
          0.22,
          12
        );
        ctx.fill();
        drawOrganicBlobAt(
          ctx,
          fx - 2 * s,
          figureBase - 12 * s,
          2 * s,
          1 * s,
          pedSeed + 22.7,
          0.2,
          12
        );
        ctx.fill();
        // Draping vines
        ctx.strokeStyle = "rgba(40,70,25,0.4)";
        ctx.lineWidth = 1.2 * s;
        ctx.beginPath();
        ctx.moveTo(fx - 6 * s, figureBase - 18 * s);
        ctx.quadraticCurveTo(
          fx - 7 * s,
          figureBase - 10 * s,
          fx - 5 * s,
          figureBase - 4 * s
        );
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(fx + 5 * s, figureBase - 22 * s);
        ctx.quadraticCurveTo(
          fx + 7 * s,
          figureBase - 14 * s,
          fx + 4 * s,
          figureBase - 6 * s
        );
        ctx.stroke();
      } else if (mapTheme === "desert") {
        // Sand accumulation at base and in crevices
        ctx.fillStyle = "rgba(210,190,150,0.3)";
        drawOrganicBlobAt(
          ctx,
          fx,
          figureBase + 1 * s,
          6 * s,
          2 * s,
          pedSeed + 23.1,
          0.2,
          16
        );
        ctx.fill();
        // Wind-worn patches
        ctx.fillStyle = "rgba(180,160,120,0.2)";
        drawOrganicBlobAt(
          ctx,
          fx - 3 * s,
          figureBase - 8 * s,
          2 * s,
          1 * s,
          pedSeed + 24.3,
          0.2,
          12
        );
        ctx.fill();
        drawOrganicBlobAt(
          ctx,
          fx + 4 * s,
          figureBase - 5 * s,
          1.5 * s,
          0.8 * s,
          pedSeed + 25.7,
          0.2,
          10
        );
        ctx.fill();
      } else if (mapTheme === "winter") {
        // Snow cap on head — layered for organic look
        ctx.fillStyle = "#f0f4f8";
        drawOrganicBlobAt(
          ctx,
          fx,
          figureBase - 42 * s,
          5 * s,
          2 * s,
          pedSeed + 26.1,
          0.2,
          16
        );
        ctx.fill();
        ctx.save();
        ctx.globalAlpha *= 0.6;
        ctx.fillStyle = "#f8fafc";
        drawOrganicBlobAt(
          ctx,
          fx - 1 * s,
          figureBase - 42.5 * s,
          3.5 * s,
          1.4 * s,
          pedSeed + 26.5,
          0.25,
          12
        );
        ctx.fill();
        ctx.restore();
        // Snow on left shoulder
        ctx.fillStyle = "#f0f4f8";
        drawOrganicBlobAt(
          ctx,
          fx - 5 * s,
          figureBase - 30 * s,
          3 * s,
          1.2 * s,
          pedSeed + 27.3,
          0.22,
          14
        );
        ctx.fill();
        // Snow on right shoulder
        drawOrganicBlobAt(
          ctx,
          fx + 5 * s,
          figureBase - 28 * s,
          2.5 * s,
          1 * s,
          pedSeed + 28.7,
          0.22,
          12
        );
        ctx.fill();
        // Snow on pedestal top — large layered cap
        ctx.fillStyle = "#e8eef4";
        drawOrganicBlobAt(
          ctx,
          fx,
          baseY - t1H - 1 * s,
          10 * s,
          4 * s,
          pedSeed + 29.1,
          0.18,
          20
        );
        ctx.fill();
        ctx.save();
        ctx.globalAlpha *= 0.5;
        ctx.fillStyle = "#f2f6fa";
        drawOrganicBlobAt(
          ctx,
          fx + 2 * s,
          baseY - t1H - 1.5 * s,
          6 * s,
          2.5 * s,
          pedSeed + 29.9,
          0.22,
          14
        );
        ctx.fill();
        ctx.restore();
      } else if (mapTheme === "volcanic") {
        // Ash deposits and ember glow
        ctx.fillStyle = "rgba(50,40,40,0.25)";
        drawOrganicBlobAt(
          ctx,
          fx,
          figureBase + 1 * s,
          5 * s,
          2 * s,
          pedSeed + 30.1,
          0.2,
          14
        );
        ctx.fill();
        // Ember glow at base cracks
        ctx.fillStyle = "rgba(255,120,40,0.2)";
        drawOrganicBlobAt(
          ctx,
          fx - 3 * s,
          figureBase - 1 * s,
          1.5 * s,
          0.8 * s,
          pedSeed + 31.3,
          0.2,
          10
        );
        ctx.fill();
        drawOrganicBlobAt(
          ctx,
          fx + 4 * s,
          figureBase - 2 * s,
          1.2 * s,
          0.6 * s,
          pedSeed + 32.7,
          0.2,
          10
        );
        ctx.fill();
        // Subtle red heat glow on figure
        ctx.fillStyle = "rgba(200,60,20,0.08)";
        drawOrganicBlobAt(
          ctx,
          fx,
          figureBase - 20 * s,
          8 * s,
          15 * s,
          pedSeed + 33.1,
          0.12,
          20
        );
        ctx.fill();
      } else {
        // Grassland default - subtle moss spots
        ctx.fillStyle = "rgba(65,85,50,0.18)";
        drawOrganicBlobAt(
          ctx,
          fx - 4 * s,
          figureBase - 1 * s,
          2 * s,
          1 * s,
          pedSeed + 34.1,
          0.2,
          12
        );
        ctx.fill();
        drawOrganicBlobAt(
          ctx,
          fx + 3 * s,
          figureBase - 2 * s,
          1.5 * s,
          0.8 * s,
          pedSeed + 35.3,
          0.2,
          10
        );
        ctx.fill();
      }
      break;
    }

    case "tombstone": {
      const cx = screenPos.x;
      const cy = screenPos.y;

      if (!skipShadow) {
        drawDirectionalShadow(
          ctx,
          cx,
          cy + 3 * s,
          s,
          14 * s,
          7 * s,
          30 * s,
          0.32
        );
      }
      if (shadowOnly) {
        break;
      }

      const tilt = [0.04, -0.03, 0.06, -0.05][variant % 4];
      ctx.save();
      ctx.translate(cx, cy);
      ctx.rotate(tilt);

      // Dirt mound base
      const dirtGrad = ctx.createRadialGradient(
        0,
        5 * s,
        2 * s,
        0,
        5 * s,
        14 * s
      );
      dirtGrad.addColorStop(0, "#4a3a28");
      dirtGrad.addColorStop(0.6, "#3a2a1a");
      dirtGrad.addColorStop(1, "rgba(40,30,15,0)");
      ctx.fillStyle = dirtGrad;
      ctx.beginPath();
      ctx.ellipse(0, 5 * s, 14 * s, 5 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Moss patches
      ctx.fillStyle = "#2a4a28";
      ctx.beginPath();
      ctx.ellipse(-4 * s, 4 * s, 7 * s, 3 * s, -0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#1e3a1c";
      ctx.beginPath();
      ctx.ellipse(3 * s, 5 * s, 5 * s, 2.5 * s, 0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#355a32";
      ctx.beginPath();
      ctx.ellipse(-1 * s, 3 * s, 4 * s, 2 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Stone base slab with beveled edges
      drawIsometricPrism(
        ctx,
        0,
        2 * s,
        14 * s,
        14 * s,
        4 * s,
        "#55555c",
        "#2a2a32",
        "#3a3a44"
      );
      // Base edge highlight
      ctx.strokeStyle = "rgba(255,255,255,0.06)";
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.moveTo(0, 2 * s - 7 * s * 0.5);
      ctx.lineTo(14 * s * 0.433, 2 * s);
      ctx.stroke();

      const stoneGrad = ctx.createLinearGradient(-6 * s, -34 * s, 6 * s, 2 * s);
      stoneGrad.addColorStop(0, "#727278");
      stoneGrad.addColorStop(0.15, "#65656c");
      stoneGrad.addColorStop(0.5, "#555560");
      stoneGrad.addColorStop(0.8, "#4a4a52");
      stoneGrad.addColorStop(1, "#3c3c44");

      if (variant % 4 === 0) {
        // Gothic cross tombstone
        ctx.fillStyle = stoneGrad;
        ctx.beginPath();
        ctx.moveTo(-5.5 * s, 0);
        ctx.lineTo(-5.5 * s, -28 * s);
        ctx.lineTo(-3.5 * s, -32 * s);
        ctx.lineTo(3.5 * s, -32 * s);
        ctx.lineTo(5.5 * s, -28 * s);
        ctx.lineTo(5.5 * s, 0);
        ctx.closePath();
        ctx.fill();
        // Right face bevel
        ctx.fillStyle = "#606068";
        ctx.beginPath();
        ctx.moveTo(5.5 * s, 0);
        ctx.lineTo(5.5 * s, -28 * s);
        ctx.lineTo(3.5 * s, -32 * s);
        ctx.lineTo(3.5 * s, -31 * s);
        ctx.lineTo(6.5 * s, -27 * s);
        ctx.lineTo(6.5 * s, 1 * s);
        ctx.closePath();
        ctx.fill();
        // Cross arms with chamfered ends
        ctx.fillStyle = "#58585e";
        ctx.beginPath();
        ctx.moveTo(-10 * s, -25 * s);
        ctx.lineTo(-9 * s, -27 * s);
        ctx.lineTo(9 * s, -27 * s);
        ctx.lineTo(10 * s, -25 * s);
        ctx.lineTo(9 * s, -23 * s);
        ctx.lineTo(-9 * s, -23 * s);
        ctx.closePath();
        ctx.fill();
        // Cross vertical top
        ctx.fillStyle = "#5a5a62";
        ctx.beginPath();
        ctx.moveTo(-3.5 * s, -32 * s);
        ctx.lineTo(-2.5 * s, -38 * s);
        ctx.lineTo(2.5 * s, -38 * s);
        ctx.lineTo(3.5 * s, -32 * s);
        ctx.closePath();
        ctx.fill();
        // Engraved border lines
        ctx.strokeStyle = "rgba(0,0,0,0.15)";
        ctx.lineWidth = 0.5 * s;
        ctx.strokeRect(-4 * s, -20 * s, 8 * s, 14 * s);
      } else if (variant % 4 === 1) {
        // Rounded skull tombstone
        ctx.fillStyle = stoneGrad;
        ctx.beginPath();
        ctx.moveTo(-6.5 * s, 0);
        ctx.lineTo(-6.5 * s, -24 * s);
        ctx.quadraticCurveTo(-6.5 * s, -32 * s, 0, -35 * s);
        ctx.quadraticCurveTo(6.5 * s, -32 * s, 6.5 * s, -24 * s);
        ctx.lineTo(6.5 * s, 0);
        ctx.closePath();
        ctx.fill();
        // Right bevel
        ctx.fillStyle = "#606068";
        ctx.beginPath();
        ctx.moveTo(6.5 * s, 0);
        ctx.lineTo(6.5 * s, -24 * s);
        ctx.quadraticCurveTo(6.5 * s, -31 * s, 1 * s, -34 * s);
        ctx.quadraticCurveTo(7.5 * s, -30 * s, 7.5 * s, -23 * s);
        ctx.lineTo(7.5 * s, 1 * s);
        ctx.closePath();
        ctx.fill();
        // Carved skull
        const skY = -20 * s;
        ctx.fillStyle = "#7a7a82";
        ctx.beginPath();
        ctx.arc(0, skY, 5.5 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "#8a8a92";
        ctx.beginPath();
        ctx.arc(-0.5 * s, skY - 1 * s, 4 * s, 0, Math.PI * 2);
        ctx.fill();
        // Eye sockets
        ctx.fillStyle = "#1a1a22";
        ctx.beginPath();
        ctx.ellipse(
          -2 * s,
          skY - 1 * s,
          1.5 * s,
          1.8 * s,
          -0.1,
          0,
          Math.PI * 2
        );
        ctx.fill();
        ctx.beginPath();
        ctx.ellipse(2 * s, skY - 1 * s, 1.5 * s, 1.8 * s, 0.1, 0, Math.PI * 2);
        ctx.fill();
        // Nose
        ctx.fillStyle = "#2a2a32";
        ctx.beginPath();
        ctx.moveTo(0, skY + 1 * s);
        ctx.lineTo(-1 * s, skY + 3 * s);
        ctx.lineTo(1 * s, skY + 3 * s);
        ctx.closePath();
        ctx.fill();
        // Teeth
        ctx.fillStyle = "#9a9aa0";
        ctx.fillRect(-2.5 * s, skY + 3.5 * s, 5 * s, 1.5 * s);
        ctx.strokeStyle = "#2a2a32";
        ctx.lineWidth = 0.4 * s;
        for (let t = -2; t <= 2; t++) {
          ctx.beginPath();
          ctx.moveTo(t * s, skY + 3.5 * s);
          ctx.lineTo(t * s, skY + 5 * s);
          ctx.stroke();
        }
      } else if (variant % 4 === 2) {
        // Angel wing tombstone
        ctx.fillStyle = stoneGrad;
        ctx.beginPath();
        ctx.moveTo(-5.5 * s, 0);
        ctx.lineTo(-5.5 * s, -30 * s);
        ctx.lineTo(0, -36 * s);
        ctx.lineTo(5.5 * s, -30 * s);
        ctx.lineTo(5.5 * s, 0);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#606068";
        ctx.beginPath();
        ctx.moveTo(5.5 * s, 0);
        ctx.lineTo(5.5 * s, -30 * s);
        ctx.lineTo(0, -36 * s);
        ctx.lineTo(1 * s, -35 * s);
        ctx.lineTo(6.5 * s, -29 * s);
        ctx.lineTo(6.5 * s, 1 * s);
        ctx.closePath();
        ctx.fill();
        // Detailed wings with feathers
        [-1, 1].forEach((side) => {
          const featherGrad = ctx.createLinearGradient(
            side * 4 * s,
            -26 * s,
            side * 14 * s,
            -18 * s
          );
          featherGrad.addColorStop(0, "#6e6e78");
          featherGrad.addColorStop(1, "#5a5a64");
          ctx.fillStyle = featherGrad;
          ctx.beginPath();
          ctx.moveTo(side * 4 * s, -24 * s);
          ctx.quadraticCurveTo(side * 10 * s, -32 * s, side * 14 * s, -24 * s);
          ctx.quadraticCurveTo(side * 12 * s, -20 * s, side * 9 * s, -17 * s);
          ctx.quadraticCurveTo(side * 6 * s, -15 * s, side * 4 * s, -18 * s);
          ctx.closePath();
          ctx.fill();
          // Feather lines
          ctx.strokeStyle = "rgba(0,0,0,0.12)";
          ctx.lineWidth = 0.4 * s;
          for (let f = 0; f < 4; f++) {
            const ft = 0.2 + f * 0.2;
            ctx.beginPath();
            ctx.moveTo(side * (4 + ft * 6) * s, -(24 - ft * 5) * s);
            ctx.lineTo(side * (6 + ft * 7) * s, -(27 - ft * 4) * s);
            ctx.stroke();
          }
        });
        // Carved halo
        ctx.strokeStyle = "#7a7a84";
        ctx.lineWidth = 1 * s;
        ctx.beginPath();
        ctx.ellipse(0, -28 * s, 3 * s, 1.5 * s, 0, 0, Math.PI * 2);
        ctx.stroke();
      } else {
        // Broken / shattered tombstone
        ctx.fillStyle = stoneGrad;
        ctx.beginPath();
        ctx.moveTo(-7 * s, 0);
        ctx.lineTo(-7 * s, -18 * s);
        ctx.lineTo(-4 * s, -22 * s);
        ctx.lineTo(-1 * s, -18 * s);
        ctx.lineTo(2 * s, -24 * s);
        ctx.lineTo(5 * s, -19 * s);
        ctx.lineTo(7 * s, -20 * s);
        ctx.lineTo(7 * s, 0);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#606068";
        ctx.beginPath();
        ctx.moveTo(7 * s, 0);
        ctx.lineTo(7 * s, -20 * s);
        ctx.lineTo(5 * s, -19 * s);
        ctx.lineTo(6 * s, -18 * s);
        ctx.lineTo(8 * s, -19 * s);
        ctx.lineTo(8 * s, 1 * s);
        ctx.closePath();
        ctx.fill();
        // Fallen chunk with matching stone
        ctx.fillStyle = "#4a4a52";
        ctx.save();
        ctx.translate(9 * s, 2 * s);
        ctx.rotate(0.7);
        ctx.beginPath();
        ctx.moveTo(-4 * s, -6 * s);
        ctx.lineTo(-2 * s, -8 * s);
        ctx.lineTo(4 * s, -5 * s);
        ctx.lineTo(3 * s, 6 * s);
        ctx.lineTo(-3 * s, 5 * s);
        ctx.closePath();
        ctx.fill();
        ctx.restore();
        // Rubble
        ctx.fillStyle = "#505058";
        ctx.beginPath();
        ctx.arc(6 * s, 5 * s, 2 * s, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "#484850";
        ctx.beginPath();
        ctx.arc(10 * s, 4 * s, 1.5 * s, 0, Math.PI * 2);
        ctx.fill();
      }

      // Weathering cracks (multiple)
      ctx.strokeStyle = "rgba(0,0,0,0.18)";
      ctx.lineWidth = 0.5 * s;
      ctx.beginPath();
      ctx.moveTo(-2 * s, -8 * s);
      ctx.lineTo(-0.5 * s, -12 * s);
      ctx.lineTo(1 * s, -10 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(3 * s, -5 * s);
      ctx.lineTo(2 * s, -9 * s);
      ctx.stroke();
      // Lichen spots
      ctx.fillStyle = "rgba(80,100,60,0.25)";
      ctx.beginPath();
      ctx.arc(-3 * s, -14 * s, 1.5 * s, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(60,85,50,0.2)";
      ctx.beginPath();
      ctx.arc(2 * s, -6 * s, 1 * s, 0, Math.PI * 2);
      ctx.fill();
      // Edge highlight (moonlight catch)
      ctx.strokeStyle = "rgba(200,200,220,0.08)";
      ctx.lineWidth = 0.8 * s;
      ctx.beginPath();
      ctx.moveTo(-5 * s, -4 * s);
      ctx.lineTo(-5 * s, -26 * s);
      ctx.stroke();

      ctx.restore();
      break;
    }

    case "ice_bridge": {
      const cx = screenPos.x;
      const cy = screenPos.y;
      const t = decorTime;

      if (!skipShadow) {
        const shGrad = ctx.createRadialGradient(
          cx,
          cy + 14 * s,
          0,
          cx,
          cy + 14 * s,
          95 * s
        );
        shGrad.addColorStop(0, "rgba(0,25,55,0.45)");
        shGrad.addColorStop(0.35, "rgba(0,18,45,0.25)");
        shGrad.addColorStop(0.7, "rgba(0,10,30,0.1)");
        shGrad.addColorStop(1, "rgba(0,0,0,0)");
        ctx.fillStyle = shGrad;
        ctx.beginPath();
        ctx.ellipse(cx, cy + 14 * s, 95 * s, 42 * s, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      if (shadowOnly) {
        break;
      }

      const isoW = 60 * s;
      const isoD = isoW * ISO_Y_RATIO;

      // Frozen ground pool beneath the ruin
      const poolGrad = ctx.createRadialGradient(
        cx,
        cy + 8 * s,
        0,
        cx,
        cy + 8 * s,
        70 * s
      );
      poolGrad.addColorStop(0, "rgba(120,200,235,0.22)");
      poolGrad.addColorStop(0.3, "rgba(100,185,225,0.14)");
      poolGrad.addColorStop(0.6, "rgba(80,160,200,0.07)");
      poolGrad.addColorStop(1, "rgba(50,120,160,0)");
      ctx.fillStyle = poolGrad;
      ctx.beginPath();
      ctx.ellipse(cx, cy + 8 * s, 70 * s, 30 * s, 0, 0, Math.PI * 2);
      ctx.fill();

      // Frozen surface texture - ice sheet with reflections
      ctx.fillStyle = "rgba(180,225,245,0.06)";
      ctx.beginPath();
      ctx.ellipse(
        cx - 5 * s,
        cy + 6 * s,
        40 * s,
        16 * s,
        -0.15,
        0,
        Math.PI * 2
      );
      ctx.fill();

      // Radiating frost cracks in the ice
      ctx.lineWidth = 0.5 * s;
      for (let i = 0; i < 12; i++) {
        const a = (i / 12) * Math.PI * 2 + 0.2;
        const r1 = 14 * s;
        const r2 = (30 + Math.sin(decorX * 2 + i * 1.9) * 15) * s;
        const midR = (r1 + r2) * 0.5;
        const jitter = Math.sin(decorX * 3 + i * 3.1) * 3 * s;
        ctx.strokeStyle = `rgba(150,210,238,${0.12 + Math.sin(i * 1.7) * 0.06})`;
        ctx.beginPath();
        ctx.moveTo(cx + Math.cos(a) * r1, cy + Math.sin(a) * r1 * 0.44 + 6 * s);
        ctx.lineTo(
          cx + Math.cos(a) * midR + jitter,
          cy + Math.sin(a) * midR * 0.44 + 5 * s
        );
        ctx.lineTo(cx + Math.cos(a) * r2, cy + Math.sin(a) * r2 * 0.44 + 6 * s);
        ctx.stroke();
        if (i % 3 === 0) {
          const branchA = a + 0.4;
          ctx.beginPath();
          ctx.moveTo(
            cx + Math.cos(a) * midR + jitter,
            cy + Math.sin(a) * midR * 0.44 + 5 * s
          );
          ctx.lineTo(
            cx + Math.cos(branchA) * (midR + 8 * s),
            cy + Math.sin(branchA) * (midR + 8 * s) * 0.44 + 5 * s
          );
          ctx.stroke();
        }
      }

      // Left abutment - grand crumbling stone pier
      const aLx = cx - isoW * 0.6;
      const aLy = cy + isoD * 0.6;
      drawIsometricPrism(
        ctx,
        aLx,
        aLy + 2 * s,
        22 * s,
        22 * s,
        4 * s,
        "#4a5868",
        "#2a3545",
        "#384856"
      );
      drawIsometricPrism(
        ctx,
        aLx,
        aLy,
        20 * s,
        20 * s,
        24 * s,
        "#546878",
        "#2d3e50",
        "#3a4e5e"
      );
      drawIsometricPrism(
        ctx,
        aLx,
        aLy - 24 * s,
        22 * s,
        22 * s,
        3 * s,
        "#647888",
        "#3d5060",
        "#4a5e70"
      );

      // Left archway remnant - tall broken arch pillar
      drawIsometricPrism(
        ctx,
        aLx - 7 * s,
        aLy - 27 * s,
        6 * s,
        6 * s,
        28 * s,
        "#5a6e80",
        "#344858",
        "#445a6c"
      );
      drawIsometricPrism(
        ctx,
        aLx + 8 * s,
        aLy - 2 * s - 27 * s,
        6 * s,
        6 * s,
        22 * s,
        "#586c7e",
        "#324656",
        "#42586a"
      );

      // Left arch keystone fragment (the top curve piece, broken)
      ctx.fillStyle = "#5e7284";
      ctx.beginPath();
      ctx.moveTo(aLx - 7 * s, aLy - 55 * s);
      ctx.quadraticCurveTo(
        aLx + 2 * s,
        aLy - 64 * s,
        aLx + 8 * s,
        aLy - 51 * s
      );
      ctx.lineTo(aLx + 5 * s, aLy - 49 * s);
      ctx.quadraticCurveTo(
        aLx + 1 * s,
        aLy - 58 * s,
        aLx - 4 * s,
        aLy - 53 * s
      );
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = "#4a5e70";
      ctx.beginPath();
      ctx.moveTo(aLx - 7 * s, aLy - 55 * s);
      ctx.lineTo(aLx - 4 * s, aLy - 53 * s);
      ctx.lineTo(aLx - 4 * s, aLy - 50 * s);
      ctx.lineTo(aLx - 7 * s, aLy - 52 * s);
      ctx.closePath();
      ctx.fill();

      // Ornate carved details on left pillar
      ctx.strokeStyle = "rgba(100,140,165,0.4)";
      ctx.lineWidth = 0.8 * s;
      for (let i = 0; i < 4; i++) {
        const bandY = aLy - 30 * s - i * 6 * s;
        ctx.beginPath();
        ctx.moveTo(aLx - 7 * s, bandY);
        ctx.lineTo(aLx - 7 * s + 6 * s, bandY - 3 * s);
        ctx.stroke();
      }

      // Right abutment - more collapsed
      const aRx = cx + isoW * 0.6;
      const aRy = cy - isoD * 0.6;
      drawIsometricPrism(
        ctx,
        aRx,
        aRy + 2 * s,
        22 * s,
        22 * s,
        4 * s,
        "#4a5868",
        "#2a3545",
        "#384856"
      );
      drawIsometricPrism(
        ctx,
        aRx,
        aRy,
        20 * s,
        20 * s,
        20 * s,
        "#546878",
        "#2d3e50",
        "#3a4e5e"
      );
      drawIsometricPrism(
        ctx,
        aRx,
        aRy - 20 * s,
        22 * s,
        22 * s,
        2 * s,
        "#647888",
        "#3d5060",
        "#4a5e70"
      );

      // Right archway remnant - shorter, more broken
      drawIsometricPrism(
        ctx,
        aRx - 6 * s,
        aRy - 22 * s,
        6 * s,
        6 * s,
        18 * s,
        "#5a6e80",
        "#344858",
        "#445a6c"
      );
      drawIsometricPrism(
        ctx,
        aRx + 7 * s,
        aRy - 1 * s - 22 * s,
        6 * s,
        6 * s,
        10 * s,
        "#546878",
        "#304454",
        "#405664"
      );

      // Collapsed bridge deck - broken cleanly in the middle, sagging
      const midSag = 10 * s;

      // Left deck slab - slopes from abutment down toward broken center
      const lDeckGrad = ctx.createLinearGradient(
        aLx,
        aLy - 26 * s,
        cx - 3 * s,
        cy + midSag
      );
      lDeckGrad.addColorStop(0, "#82b8d2");
      lDeckGrad.addColorStop(0.2, "#98cce0");
      lDeckGrad.addColorStop(0.5, "#b4dced");
      lDeckGrad.addColorStop(0.8, "#caeaf6");
      lDeckGrad.addColorStop(1, "#68a4be");
      ctx.fillStyle = lDeckGrad;
      ctx.beginPath();
      ctx.moveTo(aLx - 10 * s, aLy - 24 * s + isoD * 0.06);
      ctx.lineTo(cx - 3 * s, cy - 3 * s + midSag);
      ctx.lineTo(cx + 4 * s, cy + isoD * 0.14 + midSag);
      ctx.lineTo(aLx - 3 * s, aLy - 24 * s + isoD * 0.35);
      ctx.closePath();
      ctx.fill();

      // Left deck thickness face
      ctx.fillStyle = "#5696b2";
      ctx.beginPath();
      ctx.moveTo(aLx - 10 * s, aLy - 24 * s + isoD * 0.06);
      ctx.lineTo(cx - 3 * s, cy - 3 * s + midSag);
      ctx.lineTo(cx - 3 * s, cy + midSag + 4 * s);
      ctx.lineTo(aLx - 10 * s, aLy - 20 * s + isoD * 0.06);
      ctx.closePath();
      ctx.fill();

      // Right deck slab - steeper tilt, partially submerged in ice
      const rDeckGrad = ctx.createLinearGradient(
        cx + 5 * s,
        cy + midSag,
        aRx,
        aRy - 20 * s
      );
      rDeckGrad.addColorStop(0, "#62a0ba");
      rDeckGrad.addColorStop(0.3, "#a0d4e6");
      rDeckGrad.addColorStop(0.6, "#c2e6f2");
      rDeckGrad.addColorStop(1, "#8ac4d8");
      ctx.fillStyle = rDeckGrad;
      ctx.beginPath();
      ctx.moveTo(cx + 5 * s, cy - 2 * s + midSag + 3 * s);
      ctx.lineTo(aRx + 9 * s, aRy - 20 * s + isoD * 0.08);
      ctx.lineTo(aRx + 3 * s, aRy - 20 * s + isoD * 0.36);
      ctx.lineTo(cx + 10 * s, cy + isoD * 0.16 + midSag + 3 * s);
      ctx.closePath();
      ctx.fill();

      // Right deck thickness
      ctx.fillStyle = "#72b0c8";
      ctx.beginPath();
      ctx.moveTo(aRx + 9 * s, aRy - 20 * s + isoD * 0.08);
      ctx.lineTo(cx + 5 * s, cy - 2 * s + midSag + 3 * s);
      ctx.lineTo(cx + 5 * s, cy + midSag + 6 * s);
      ctx.lineTo(aRx + 9 * s, aRy - 17 * s + isoD * 0.08);
      ctx.closePath();
      ctx.fill();

      // Jagged broken edges at center gap
      ctx.strokeStyle = "rgba(80,150,185,0.65)";
      ctx.lineWidth = 1.3 * s;
      ctx.beginPath();
      ctx.moveTo(cx - 3 * s, cy - 3 * s + midSag);
      ctx.lineTo(cx - 1 * s, cy + midSag - 1 * s);
      ctx.lineTo(cx + 1 * s, cy + midSag + 2 * s);
      ctx.lineTo(cx + 4 * s, cy + isoD * 0.14 + midSag);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(cx + 5 * s, cy - 2 * s + midSag + 3 * s);
      ctx.lineTo(cx + 6 * s, cy + midSag + 1 * s);
      ctx.lineTo(cx + 8 * s, cy + midSag + 4 * s);
      ctx.lineTo(cx + 10 * s, cy + isoD * 0.16 + midSag + 3 * s);
      ctx.stroke();

      // Surface cracks on both deck sections
      ctx.strokeStyle = "rgba(55,125,160,0.3)";
      ctx.lineWidth = 0.6 * s;
      for (let i = 0; i < 6; i++) {
        const frac = 0.12 + i * 0.15;
        const crX = aLx + (cx - aLx) * frac - 4 * s;
        const crY = aLy - 24 * s + (cy + midSag - (aLy - 24 * s)) * frac;
        ctx.beginPath();
        ctx.moveTo(crX - 3 * s, crY - 1 * s);
        ctx.lineTo(crX + 1 * s, crY + 1.5 * s);
        ctx.lineTo(crX + 5 * s, crY - 0.5 * s);
        ctx.stroke();
      }

      // Fallen rubble blocks in and around the gap
      const rubble = [
        { d: 3.5, h: 5, w: 7, x: cx - 8 * s, y: cy + 5 * s },
        { d: 3, h: 4, w: 6, x: cx + 3 * s, y: cy + 10 * s },
        { d: 2.5, h: 3.5, w: 5, x: cx - 12 * s, y: cy + 12 * s },
        { d: 3, h: 4.5, w: 6, x: cx + 12 * s, y: cy + 3 * s },
        { d: 2, h: 3, w: 4, x: cx + 1 * s, y: cy + 16 * s },
        { d: 2.5, h: 3.5, w: 4.5, x: cx + 8 * s, y: cy + 14 * s },
        { d: 2, h: 2.5, w: 3.5, x: cx - 4 * s, y: cy + 20 * s },
        { d: 2, h: 3, w: 4, x: cx + 16 * s, y: cy + 8 * s },
      ];
      rubble.forEach((rb) => {
        drawIsometricPrism(
          ctx,
          rb.x,
          rb.y,
          rb.w * s,
          rb.h * s,
          rb.d * s,
          "#6498ad",
          "#3a5868",
          "#4a6a7c"
        );
      });

      // Fallen arch keystone block (the crown piece, fallen to the ground)
      drawIsometricPrism(
        ctx,
        cx - 2 * s,
        cy + 7 * s,
        10 * s,
        7 * s,
        5 * s,
        "#5a7080",
        "#344858",
        "#445a6c"
      );
      // Carved detail on the keystone
      ctx.strokeStyle = "rgba(100,145,170,0.35)";
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(cx - 5 * s, cy + 3 * s);
      ctx.lineTo(cx - 2 * s, cy + 1.5 * s);
      ctx.lineTo(cx + 1 * s, cy + 3 * s);
      ctx.stroke();

      // Ice encrustation on rubble
      ctx.fillStyle = "rgba(155,218,242,0.14)";
      rubble.forEach((rb) => {
        ctx.beginPath();
        ctx.ellipse(
          rb.x,
          rb.y - rb.d * s,
          rb.w * 0.7 * s,
          rb.h * 0.35 * s,
          0,
          0,
          Math.PI * 2
        );
        ctx.fill();
      });

      // Frozen waterfall effect - ice cascading from break point
      const waterfallX = cx + 1 * s;
      const waterfallY = cy + midSag + 2 * s;
      for (let i = 0; i < 5; i++) {
        const wfGrad = ctx.createLinearGradient(
          waterfallX + (i - 2) * 3 * s,
          waterfallY,
          waterfallX + (i - 2) * 3 * s,
          waterfallY + (12 + i * 3) * s
        );
        wfGrad.addColorStop(0, `rgba(160,215,240,${0.25 - i * 0.03})`);
        wfGrad.addColorStop(0.5, `rgba(180,228,248,${0.15 - i * 0.02})`);
        wfGrad.addColorStop(1, `rgba(200,238,252,${0.05})`);
        ctx.fillStyle = wfGrad;
        const wfW = (2.5 - i * 0.3) * s;
        ctx.beginPath();
        ctx.moveTo(waterfallX + (i - 2) * 3 * s - wfW, waterfallY);
        ctx.lineTo(waterfallX + (i - 2) * 3 * s, waterfallY + (12 + i * 3) * s);
        ctx.lineTo(waterfallX + (i - 2) * 3 * s + wfW, waterfallY);
        ctx.closePath();
        ctx.fill();
      }

      // Massive icicles from broken deck edges and abutments
      const icicles = [
        { len: 16, w: 2.2, x: cx - 5 * s, y: cy + midSag + 3 * s },
        { len: 22, w: 2.8, x: cx - 1 * s, y: cy + midSag + 5 * s },
        { len: 14, w: 1.8, x: cx + 3 * s, y: cy + midSag + 4 * s },
        { len: 19, w: 2.4, x: cx + 6 * s, y: cy + midSag + 5 * s },
        { len: 12, w: 1.6, x: cx + 9 * s, y: cy + midSag + 4 * s },
        { len: 11, w: 1.6, x: aLx - 8 * s, y: aLy - 18 * s },
        { len: 9, w: 1.3, x: aLx + 3 * s, y: aLy - 20 * s },
        { len: 13, w: 1.8, x: aLx - 3 * s, y: aLy - 19 * s },
        { len: 10, w: 1.4, x: aRx + 6 * s, y: aRy - 15 * s },
        { len: 14, w: 2, x: aRx - 4 * s, y: aRy - 17 * s },
        { len: 8, w: 1.2, x: aRx + 1 * s, y: aRy - 16 * s },
        // From archway pillars
        { len: 7, w: 1, x: aLx - 6 * s, y: aLy - 50 * s },
        { len: 6, w: 0.9, x: aLx + 6 * s, y: aLy - 45 * s },
        { len: 5, w: 0.8, x: aRx - 5 * s, y: aRy - 38 * s },
      ];
      icicles.forEach((ic, i) => {
        const shimmer = 0.5 + Math.sin(t * 2 + i * 0.95) * 0.3;
        const icGrad = ctx.createLinearGradient(
          ic.x,
          ic.y,
          ic.x,
          ic.y + ic.len * s
        );
        icGrad.addColorStop(0, `rgba(165,215,238,${0.78 + shimmer * 0.12})`);
        icGrad.addColorStop(0.4, `rgba(185,228,246,${0.55 + shimmer * 0.1})`);
        icGrad.addColorStop(0.8, `rgba(205,238,250,${0.3 + shimmer * 0.08})`);
        icGrad.addColorStop(1, `rgba(220,245,255,${0.1})`);
        ctx.fillStyle = icGrad;
        ctx.beginPath();
        ctx.moveTo(ic.x - ic.w * s, ic.y);
        ctx.lineTo(ic.x, ic.y + ic.len * s);
        ctx.lineTo(ic.x + ic.w * s, ic.y);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = `rgba(220,245,255,${0.25 + shimmer * 0.2})`;
        ctx.lineWidth = 0.35 * s;
        ctx.beginPath();
        ctx.moveTo(ic.x - ic.w * s * 0.3, ic.y + 1 * s);
        ctx.lineTo(ic.x - ic.w * s * 0.08, ic.y + ic.len * s * 0.75);
        ctx.stroke();
      });

      // Fallen railing fragments
      ctx.strokeStyle = "rgba(110,145,168,0.5)";
      ctx.lineWidth = 2 * s;
      ctx.beginPath();
      ctx.moveTo(aLx - 12 * s, aLy - 26 * s);
      ctx.lineTo(aLx - 16 * s, aLy - 18 * s);
      ctx.lineTo(cx - 18 * s, cy + 4 * s);
      ctx.stroke();
      ctx.lineWidth = 1.5 * s;
      ctx.beginPath();
      ctx.moveTo(aRx + 10 * s, aRy - 20 * s);
      ctx.lineTo(cx + 20 * s, cy + 6 * s);
      ctx.stroke();

      // Broken railing post stubs on abutments
      ctx.lineWidth = 2.5 * s;
      ctx.strokeStyle = "rgba(95,130,152,0.6)";
      ctx.beginPath();
      ctx.moveTo(aLx - 8 * s, aLy - 27 * s);
      ctx.lineTo(aLx - 8 * s, aLy - 36 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(aLx + 9 * s, aLy - 29 * s);
      ctx.lineTo(aLx + 9 * s, aLy - 34 * s);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(aRx + 8 * s, aRy - 22 * s);
      ctx.lineTo(aRx + 8 * s, aRy - 28 * s);
      ctx.stroke();

      // Frost/snow caps on abutments and pillars
      const ibSnowSeed = Math.abs(decorX * 53 + decorY * 97);
      ctx.fillStyle = "rgba(218,238,250,0.5)";
      drawOrganicBlobAt(
        ctx,
        aLx,
        aLy - 27 * s,
        12 * s,
        5.5 * s,
        ibSnowSeed,
        0.18,
        14
      );
      ctx.fill();
      drawOrganicBlobAt(
        ctx,
        aRx,
        aRy - 22 * s,
        11 * s,
        5 * s,
        ibSnowSeed + 31,
        0.2,
        14
      );
      ctx.fill();
      ctx.fillStyle = "rgba(210,232,248,0.4)";
      drawOrganicBlobAt(
        ctx,
        aLx - 7 * s,
        aLy - 55 * s,
        4 * s,
        2 * s,
        ibSnowSeed + 67,
        0.22,
        10
      );
      ctx.fill();
      drawOrganicBlobAt(
        ctx,
        aLx + 8 * s,
        aLy - 49 * s,
        3.5 * s,
        1.8 * s,
        ibSnowSeed + 89,
        0.22,
        10
      );
      ctx.fill();

      // Snow drifts around the base
      ctx.fillStyle = "rgba(208,232,246,0.3)";
      drawOrganicBlobAt(
        ctx,
        cx - 24 * s,
        cy + 20 * s,
        16 * s,
        6 * s,
        ibSnowSeed + 113,
        0.18,
        16
      );
      ctx.fill();
      drawOrganicBlobAt(
        ctx,
        cx + 22 * s,
        cy + 17 * s,
        14 * s,
        5 * s,
        ibSnowSeed + 137,
        0.2,
        14
      );
      ctx.fill();
      drawOrganicBlobAt(
        ctx,
        cx - 5 * s,
        cy + 24 * s,
        10 * s,
        4 * s,
        ibSnowSeed + 157,
        0.2,
        12
      );
      ctx.fill();

      // Glowing ice runes on the abutment faces
      const iceRunes = ["ᛁ", "ᛋ", "ᚺ", "ᛇ", "ᛉ", "ᚠ"];
      ctx.font = `bold ${6 * s}px serif`;
      ctx.textAlign = "center";
      ctx.textBaseline = "middle";
      for (let i = 0; i < 6; i++) {
        const runeAlpha = 0.08 + Math.sin(t * 1.5 + i * 1.3) * 0.08;
        ctx.fillStyle = `rgba(130,205,238,${runeAlpha})`;
        if (i < 3) {
          ctx.fillText(
            iceRunes[i],
            aLx + (i - 1) * 7 * s,
            aLy - (8 + i * 4) * s
          );
        } else {
          ctx.fillText(
            iceRunes[i],
            aRx + (i - 4) * 7 * s,
            aRy - (6 + (i - 3) * 4) * s
          );
        }
      }
      // Runes on archway pillars
      ctx.font = `bold ${4.5 * s}px serif`;
      for (let i = 0; i < 3; i++) {
        const pAlpha = 0.06 + Math.sin(t * 1.2 + i * 2.1) * 0.06;
        ctx.fillStyle = `rgba(130,205,238,${pAlpha})`;
        ctx.fillText(iceRunes[i], aLx - 7 * s, aLy - (32 + i * 7) * s);
      }

      // Frozen mist particles drifting through the ruin
      for (let i = 0; i < 16; i++) {
        const phase = t * 0.6 + i * 0.65 + decorX * 0.25;
        const sparkAlpha = 0.12 + Math.sin(phase * 2.5) * 0.18;
        if (sparkAlpha > 0.06) {
          const px = cx + Math.cos(phase * 0.4) * isoW * 0.9;
          const py = cy - 12 * s + Math.sin(phase * 0.75) * isoD * 0.9;
          const drift = Math.sin(phase * 1.3) * 4 * s;
          const sparkSize = (1 + Math.sin(phase * 2) * 0.7) * s;
          ctx.fillStyle = `rgba(195,232,248,${sparkAlpha})`;
          ctx.beginPath();
          ctx.arc(px + drift, py, sparkSize, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // Subtle cold ambient glow from the center gap
      const coldGlow = ctx.createRadialGradient(
        cx + 2 * s,
        cy + midSag,
        0,
        cx + 2 * s,
        cy + midSag,
        20 * s
      );
      coldGlow.addColorStop(
        0,
        `rgba(140,210,240,${0.06 + Math.sin(t * 1.8) * 0.03})`
      );
      coldGlow.addColorStop(
        0.5,
        `rgba(120,195,230,${0.03 + Math.sin(t * 1.8) * 0.015})`
      );
      coldGlow.addColorStop(1, "rgba(100,180,220,0)");
      ctx.fillStyle = coldGlow;
      ctx.beginPath();
      ctx.arc(cx + 2 * s, cy + midSag, 20 * s, 0, Math.PI * 2);
      ctx.fill();

      break;
    }

    case "war_monument": {
      renderWarMonument({
        ctx,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        seedX: decorX,
        seedY: decorY,
        shadowOnly,
        skipShadow: landmarkSkipShadow,
        time: decorTime,
        variant,
        zoom,
      });
      break;
    }

    case "bone_altar": {
      renderBoneAltar({
        ctx,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        seedX: decorX,
        seedY: decorY,
        shadowOnly,
        skipShadow: landmarkSkipShadow,
        time: decorTime,
        variant,
        zoom,
      });
      break;
    }

    case "sun_obelisk": {
      renderSunObelisk({
        ctx,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        seedX: decorX,
        seedY: decorY,
        shadowOnly,
        skipShadow: landmarkSkipShadow,
        time: decorTime,
        variant,
        zoom,
      });
      break;
    }

    case "frost_citadel": {
      renderFrostCitadel({
        ctx,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        seedX: decorX,
        seedY: decorY,
        shadowOnly,
        skipShadow: landmarkSkipShadow,
        time: decorTime,
        variant,
        zoom,
      });
      break;
    }

    case "infernal_gate": {
      renderInfernalGate({
        ctx,
        s,
        screenX: screenPos.x,
        screenY: screenPos.y,
        seedX: decorX,
        seedY: decorY,
        shadowOnly,
        skipShadow: landmarkSkipShadow,
        time: decorTime,
        variant,
        zoom,
      });
      break;
    }
  }
}

function _drawPalmFrondItem(
  ctx: CanvasRenderingContext2D,
  baseX: number,
  baseY: number,
  angle: number,
  length: number,
  colors: { rib: string; lit: string; dark: string },
  scale: number,
  sway: number
): void {
  const s = scale;
  const cosA = Math.cos(angle);
  const sinA = Math.sin(angle);
  const tipX = baseX + cosA * length + sway;
  const tipY = baseY + sinA * length * 0.55 + length * 0.35;
  const ctrlX = baseX + cosA * length * 0.4;
  const ctrlY = baseY - 4 * s + sinA * 5 * s;

  ctx.strokeStyle = colors.rib;
  ctx.lineWidth = 3.5 * s;
  ctx.lineCap = "round";
  ctx.beginPath();
  ctx.moveTo(baseX, baseY);
  ctx.quadraticCurveTo(ctrlX, ctrlY, tipX, tipY);
  ctx.stroke();

  const bladeMaxLen = 14 * s;
  for (let b = 0; b < 6; b++) {
    const t = 0.06 + b * 0.155;
    const mt = 1 - t;
    const px = mt * mt * baseX + 2 * mt * t * ctrlX + t * t * tipX;
    const py = mt * mt * baseY + 2 * mt * t * ctrlY + t * t * tipY;
    const tx = 2 * mt * (ctrlX - baseX) + 2 * t * (tipX - ctrlX);
    const ty2 = 2 * mt * (ctrlY - baseY) + 2 * t * (tipY - ctrlY);
    const tangentAngle = Math.atan2(ty2, tx);
    const cosT = Math.cos(tangentAngle);
    const sinT = Math.sin(tangentAngle);
    const bladeLen = bladeMaxLen * (1 - t * 0.4);
    const spread = 1.4 + t * 0.5;

    for (let side = -1; side <= 1; side += 2) {
      const ba = tangentAngle + side * spread;
      const cosB = Math.cos(ba);
      const sinB = Math.sin(ba);
      ctx.fillStyle = side === 1 ? colors.lit : colors.dark;
      ctx.beginPath();
      ctx.moveTo(px - cosT * 1 * s, py - sinT * 0.5 * s);
      ctx.lineTo(px + cosB * bladeLen, py + sinB * bladeLen * 0.6);
      ctx.lineTo(px + cosT * 1 * s, py + sinT * 0.5 * s);
      ctx.closePath();
      ctx.fill();
    }
  }
}
